<!-- Generated by scripts/build_control_classification_docs.py. Do not edit by hand. -->

# `docs/engineering/11-developer-experience-platform-and-delivery.md` — classification reference, part 1 of 1

[Back to the classification index](../README.md).

Controls: **752** · Deterministic: **63** · Nondeterministic: **689**

## `USEQ-836D2261` — Nondeterministic

**Exact statement**

> Repository access uses individual identities, strong authentication, and least privilege.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Individual identity and MFA settings are partial; strong authentication, least privilege, and complete repository/provider identity scope need threat and authority judgment. Therefore the proposed structured-record composite does not prove the whole rule “Repository access uses individual identities, strong authentication, and least privilege.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Listed users can use MFA while a bot token or vendor administrator remains shared and overprivileged. The proposal can Pass while “Repository access uses individual identities, strong authentication, and least privilege.” remains false.

## `USEQ-3D3EF3D3` — Nondeterministic

**Exact statement**

> Material changes require independent review; critical areas require designated reviewers.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Material changes require independent review; critical areas require designated reviewers.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0BBDD49A` — Nondeterministic

**Exact statement**

> Administrative review bypass cannot occur silently.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Silence can occur through omitted logs, provider administrators, alternate merge paths, or disabled audit delivery, and bypass authorization is human. Therefore the proposed structured-record composite does not prove the whole rule “Administrative review bypass cannot occur silently.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Known bypass events can be logged while a provider owner changes policy without a captured event. The proposal can Pass while “Administrative review bypass cannot occur silently.” remains false.

## `USEQ-4B76B0C6` — Nondeterministic

**Exact statement**

> Merge requirements include all required automated checks.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A branch rule can include declared checks while the complete required-check set, generated pipelines, release branches, and bypass paths remain unsealed. Therefore the proposed structured-record composite does not prove the whole rule “Merge requirements include all required automated checks.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Main can require all listed checks while a release branch or new mandatory scanner is omitted. The proposal can Pass while “Merge requirements include all required automated checks.” remains false.

## `USEQ-2163B1AC` — Nondeterministic

**Exact statement**

> Security-sensitive, infrastructure, pipeline, authorization, cryptography, payment, and migration changes receive heightened review.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Security-sensitive, infrastructure, pipeline, authorization, cryptography, payment, and migration changes receive heightened review.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-32DAA868` — Nondeterministic

**Exact statement**

> Generated code has a trusted source and reproducible regeneration process.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Generated code has a trusted source and reproducible regeneration process.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2C510B3A` — Nondeterministic

**Exact statement**

> Emergency changes use a documented break-glass process and receive retrospective review.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Break-glass records and review timestamps are exact, but emergency legitimacy, complete usage capture, and substantive retrospective review require accountability. Therefore the proposed structured-record composite does not prove the whole rule “Emergency changes use a documented break-glass process and receive retrospective review.”.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

A normal convenience change can be labeled emergency and receive a rubber-stamped retrospective. The proposal can Pass while “Emergency changes use a documented break-glass process and receive retrospective review.” remains false.

## `USEQ-AE4E848F` — Nondeterministic

**Exact statement**

> Repository backup and recovery are tested.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Repository backup and recovery are tested.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3560AE8E` — Nondeterministic

**Exact statement**

> Repository webhooks, applications, bots, and deploy keys are inventoried and restricted.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Repository app/key inventories can omit provider-level integrations, inherited apps, dormant credentials, and indirect webhooks; restriction is contextual. Therefore the proposed structured-record composite does not prove the whole rule “Repository webhooks, applications, bots, and deploy keys are inventoried and restricted.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All listed bots can be narrow while an organization app retains broad repository write access. The proposal can Pass while “Repository webhooks, applications, bots, and deploy keys are inventoried and restricted.” remains false.

## `USEQ-394F34E8` — Nondeterministic

**Exact statement**

> Builds run automatically from reviewed source and are repeatable from declared inputs.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Builds run automatically from reviewed source and are repeatable from declared inputs.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D1076D92` — Nondeterministic

**Exact statement**

> Dependency versions and build inputs are pinned sufficiently to prevent unexpected substitution.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Dependency versions and build inputs are pinned sufficiently to prevent unexpected substitution.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-267DAD35` — Deterministic

**Exact statement**

> The production artifact is built once and promoted rather than independently rebuilt per environment.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Build artifact digest and environment-promotion references are exact; a sealed release graph can require every environment to reference the one production artifact identity. This proves the complete bounded meaning of “The production artifact is built once and promoted rather than independently rebuilt per environment.”.

### Exact deterministic check

1. The production artifact is built once and promoted rather than independently rebuilt per environment.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any environment rebuild or distinct binary changes the digest and deterministically fails promotion reconciliation. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-3381F5D0` — Deterministic

**Exact statement**

> Deployable artifacts are immutable and have cryptographic digests.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Deployable artifact immutability, content digest, and object identity are bounded cryptographic state facts. This proves the complete bounded meaning of “Deployable artifacts are immutable and have cryptographic digests.”.

### Exact deterministic check

1. Deployable artifacts are immutable and have cryptographic digests.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Mutation, alias replacement, or missing/changed digest deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-2F6AA097` — Deterministic

**Exact statement**

> Build provenance records source, builder, inputs, workflow, time, and output digest.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Source, builder, input, workflow, time, and output-digest links form a finite signed provenance graph for a sealed build. This proves the complete bounded meaning of “Build provenance records source, builder, inputs, workflow, time, and output digest.”.

### Exact deterministic check

1. Build provenance records source, builder, inputs, workflow, time, and output digest.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A missing field, dangling reference, invalid signature, or digest mismatch deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-4480E17F` — Nondeterministic

**Exact statement**

> Build and signing identities use least privilege; keys are protected outside source and output.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Build and signing identities use least privilege; keys are protected outside source and output.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-73488048` — Nondeterministic

**Exact statement**

> Untrusted contribution workflows cannot access production secrets or overwrite trusted artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing all untrusted workflows from secrets and trusted artifact writes is an adversarial flow claim across jobs, caches, identities, actions, and providers. Therefore the proposed structured-record composite does not prove the whole rule “Untrusted contribution workflows cannot access production secrets or overwrite trusted artifacts.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

The main fork job can be isolated while an imported action reads a metadata credential or poisons a mutable cache. The proposal can Pass while “Untrusted contribution workflows cannot access production secrets or overwrite trusted artifacts.” remains false.

## `USEQ-A0338698` — Deterministic

**Exact statement**

> Failed or canceled checks cannot be represented as passed.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A check-run state machine can forbid failed/canceled terminal states from mapping to passed, using immutable run identity and status transitions. This proves the complete bounded meaning of “Failed or canceled checks cannot be represented as passed.”.

### Exact deterministic check

1. Failed or canceled checks cannot be represented as passed.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Any failed or canceled run represented as pass deterministically violates the state mapping. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-4C73564C` — Nondeterministic

**Exact statement**

> Previous known-good artifacts remain available.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Artifact retention is exact, but known-good truth and operational availability for restore require a defined verification/recovery oracle. Therefore the proposed structured-record composite does not prove the whole rule “Previous known-good artifacts remain available.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A signed old binary can remain stored while its configuration or key is missing and it cannot run safely. The proposal can Pass while “Previous known-good artifacts remain available.” remains false.

## `USEQ-7112CE32` — Nondeterministic

**Exact statement**

> Build logs and caches do not expose secrets, sensitive source, or unverified cross-trust artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Build logs and caches do not expose secrets, sensitive source, or unverified cross-trust artifacts.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D4ABAEAF` — Nondeterministic

**Exact statement**

> Build-platform compromise has containment and recovery procedures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Build-platform compromise has containment and recovery procedures.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9ED6000B` — Nondeterministic

**Exact statement**

> Production credentials do not work outside production; non-production users cannot reach production control planes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Credential/environment and control-plane isolation spans all identities, federation, recovery paths, provider roots, networks, and caches. Therefore the proposed structured-record composite does not prove the whole rule “Production credentials do not work outside production; non-production users cannot reach production control planes.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal credentials can fail cross-environment tests while a shared provider token or inherited trust still reaches production. The proposal can Pass while “Production credentials do not work outside production; non-production users cannot reach production control planes.” remains false.

## `USEQ-23C9AA06` — Nondeterministic

**Exact statement**

> Test accounts, bypasses, simulators, debug endpoints, and development consoles are absent or inaccessible in production.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence/inaccessibility covers every account, bypass, simulator, route, console, binary, flag, and undocumented provider path. Therefore the proposed structured-record composite does not prove the whole rule “Test accounts, bypasses, simulators, debug endpoints, and development consoles are absent or inaccessible in production.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Route scans can pass while an unlisted debug handler or dormant test account remains usable. The proposal can Pass while “Test accounts, bypasses, simulators, debug endpoints, and development consoles are absent or inaccessible in production.” remains false.

## `USEQ-DAC810AD` — Nondeterministic

**Exact statement**

> Non-production email, payment, notifications, and webhooks cannot affect real users or systems accidentally.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing nonproduction side effects requires complete notification, payment, webhook, integration, credential, fallback, and provider inventories. Therefore the proposed structured-record composite does not prove the whole rule “Non-production email, payment, notifications, and webhooks cannot affect real users or systems accidentally.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Sandbox paths can pass while a fallback email or live webhook reaches a real user. The proposal can Pass while “Non-production email, payment, notifications, and webhooks cannot affect real users or systems accidentally.” remains false.

## `USEQ-AF8234B1` — Nondeterministic

**Exact statement**

> Environment boundaries are documented, tested, logged, and periodically reviewed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Environment boundaries are documented, tested, logged, and periodically reviewed.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7A52E01F` — Nondeterministic

**Exact statement**

> Configuration is versioned or otherwise auditable and changes require review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Version/change records are exact, but complete configuration scope and substantive authorized review are not proven. Therefore the proposed structured-record composite does not prove the whole rule “Configuration is versioned or otherwise auditable and changes require review.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Managed files can pass while a console-only setting changes without review. The proposal can Pass while “Configuration is versioned or otherwise auditable and changes require review.” remains false.

## `USEQ-447535A7` — Nondeterministic

**Exact statement**

> Secure defaults and explicit environment-specific values are used.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Secure defaults and explicit environment-specific values are used.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5E325DC6` — Nondeterministic

**Exact statement**

> Production configuration can be reconstructed and rolled back.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Reconstruction and rollback need complete configuration dependencies, provider state, historical artifacts, and exact recovery oracles. Therefore the proposed structured-record composite does not prove the whole rule “Production configuration can be reconstructed and rolled back.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A sample rollback can pass while an implicit provider default or expired secret prevents real reconstruction. The proposal can Pass while “Production configuration can be reconstructed and rolled back.” remains false.

## `USEQ-294721C4` — Nondeterministic

**Exact statement**

> Debug mode, verbose exceptions, profiling, test routes, and development consoles are disabled or restricted.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Disabled/restricted status across every debug feature, exception path, profiler, route, and console is not sealed; acceptable restriction needs threat judgment. Therefore the proposed structured-record composite does not prove the whole rule “Debug mode, verbose exceptions, profiling, test routes, and development consoles are disabled or restricted.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Known debug routes can be off while verbose errors leak through an untested handler. The proposal can Pass while “Debug mode, verbose exceptions, profiling, test routes, and development consoles are disabled or restricted.” remains false.

## `USEQ-B66A3064` — Nondeterministic

**Exact statement**

> Time zones, locales, encodings, units, time synchronization, resource limits, and timeouts are explicit.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Time zones, locales, encodings, units, time synchronization, resource limits, and timeouts are explicit.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6A62F162` — Nondeterministic

**Exact statement**

> Source maps and diagnostic artifacts do not expose secrets or inappropriate internal information.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Source maps and diagnostic artifacts do not expose secrets or inappropriate internal information.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DE280DA4` — Nondeterministic

**Exact statement**

> Configuration ownership, review dates, emergency changes, and approvals are recorded.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record fields are finite, but complete configuration-change capture and valid ownership/approval authority are not established. Therefore the proposed structured-record composite does not prove the whole rule “Configuration ownership, review dates, emergency changes, and approvals are recorded.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Every managed change can have fields while a provider-console emergency edit is absent. The proposal can Pass while “Configuration ownership, review dates, emergency changes, and approvals are recorded.” remains false.

## `USEQ-3E50B6BE` — Nondeterministic

**Exact statement**

> Every flag has an owner, purpose, default state, and removal date where temporary.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every runtime/build/provider flag needs a complete inventory, and purpose/default/removal applicability can be semantic. Therefore the proposed structured-record composite does not prove the whole rule “Every flag has an owner, purpose, default state, and removal date where temporary.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Registered flags can pass while an inherited provider toggle has no owner or removal date. The proposal can Pass while “Every flag has an owner, purpose, default state, and removal date where temporary.” remains false.

## `USEQ-7BDEFE8A` — Nondeterministic

**Exact statement**

> Flag-service failure produces safe behavior.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Flag-service failure produces safe behavior.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-ADCFB9BE` — Nondeterministic

**Exact statement**

> High-impact flag changes are restricted and audited.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** High-impact classification, adequate restriction, valid authorization, and complete flag-change capture require accountable context. Therefore the proposed structured-record composite does not prove the whole rule “High-impact flag changes are restricted and audited.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A dangerous flag can be mislabeled low impact and changed through an unlogged provider console. The proposal can Pass while “High-impact flag changes are restricted and audited.” remains false.

## `USEQ-002EFF2A` — Nondeterministic

**Exact statement**

> High-risk new functionality has a kill switch where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “High-risk new functionality has a kill switch where appropriate.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-98C06345` — Nondeterministic

**Exact statement**

> Untrusted clients cannot manipulate flags to bypass controls.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** No finite client test proves absence of every flag-manipulation path, cache inconsistency, parameter smuggling, identity race, or server fallback. Therefore the proposed structured-record composite does not prove the whole rule “Untrusted clients cannot manipulate flags to bypass controls.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal API tests can pass while a crafted stale token or hidden header overrides an authorization flag. The proposal can Pass while “Untrusted clients cannot manipulate flags to bypass controls.” remains false.

## `USEQ-A088A7BA` — Nondeterministic

**Exact statement**

> Secrets do not appear in source, artifacts, frontend bundles, logs, tickets, documentation, chat, or analytics.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Secret absence across source, history, artifacts, bundles, logs, tickets, docs, chat, and analytics cannot be proven by pattern scanners and declared locations. Therefore the proposed structured-record composite does not prove the whole rule “Secrets do not appear in source, artifacts, frontend bundles, logs, tickets, documentation, chat, or analytics.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Scans can pass while an encoded key, high-entropy token fragment, or provider credential remains. The proposal can Pass while “Secrets do not appear in source, artifacts, frontend bundles, logs, tickets, documentation, chat, or analytics.” remains false.

## `USEQ-DDA7DCA9` — Nondeterministic

**Exact statement**

> Least privilege and short-lived credentials are used where possible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Least privilege and short-lived credentials are used where possible.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5970F4E7` — Nondeterministic

**Exact statement**

> Secret retrieval and use are audited where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Secret retrieval and use are audited where practical.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F341514B` — Nondeterministic

**Exact statement**

> Rotation and emergency revocation are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Rotation/revocation tests sample key classes, consumers, caches, failure states, replicas, and recovery paths without exhaustive oracles. Therefore the proposed structured-record composite does not prove the whole rule “Rotation and emergency revocation are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A service key drill can pass while an offline client or cached token keeps the old secret valid. The proposal can Pass while “Rotation and emergency revocation are tested.” remains false.

## `USEQ-402D416B` — Nondeterministic

**Exact statement**

> Expiry and rotation failures are monitored.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring expiry/rotation failures needs a complete secret/key/certificate inventory, authoritative events, freshness bounds, and alert delivery. Therefore the proposed structured-record composite does not prove the whole rule “Expiry and rotation failures are monitored.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known certificates can alert while an omitted provider key expires silently. The proposal can Pass while “Expiry and rotation failures are monitored.” remains false.

## `USEQ-2CE27D9F` — Nondeterministic

**Exact statement**

> Backup and recovery preserve or securely reconstruct required secrets.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Backup and recovery preserve or securely reconstruct required secrets.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BF6C9A29` — Nondeterministic

**Exact statement**

> Break-glass credentials are protected, monitored, and tested.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Break-glass credentials are protected, monitored, and tested.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-24DBEE2F` — Nondeterministic

**Exact statement**

> Historical secret exposure is addressed by rotation or revocation, not merely deletion.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete historical exposure discovery and proof that every exposed credential was rotated/revoked require histories, copies, providers, and consumers beyond deletion. Therefore the proposed structured-record composite does not prove the whole rule “Historical secret exposure is addressed by rotation or revocation, not merely deletion.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

A leaked token can be deleted from git while a copy remains active in a downstream system. The proposal can Pass while “Historical secret exposure is addressed by rotation or revocation, not merely deletion.” remains false.

## `USEQ-69535E01` — Nondeterministic

**Exact statement**

> Integrate small complete changes frequently rather than allowing long-lived divergence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Integrate small complete changes frequently rather than allowing long-lived divergence.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-782703AC` — Nondeterministic

**Exact statement**

> Keep the authoritative branch releasable or restore it promptly when a gate fails.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep the authoritative branch releasable or restore it promptly when a gate fails.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0B41367E` — Deterministic

**Exact statement**

> Require review and required checks before protected-branch change.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed protected-branch policy, required review/check identities and change commit can be bound to an exact gate transition before branch update. This proves the complete bounded meaning of “Require review and required checks before protected-branch change.”.

### Exact deterministic check

1. Require review and required checks before protected-branch change.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A change lacking a required approval/check or updated before them deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-7B40E59A` — Nondeterministic

**Exact statement**

> Use one trusted pipeline definition under the same change control as source.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One trusted pipeline and equivalent change control require complete release entry points, imported definitions, generators, and valid review authority. Therefore the proposed structured-record composite does not prove the whole rule “Use one trusted pipeline definition under the same change control as source.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

The main YAML can be protected while an editable remote action or second manual release path exists. The proposal can Pass while “Use one trusted pipeline definition under the same change control as source.” remains false.

## `USEQ-8CA318E4` — Nondeterministic

**Exact statement**

> Run builds and tests from clean, declared inputs.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clean builds from declared inputs require absence of hidden network, cache, clock, environment, provider, and host dependencies. Therefore the proposed structured-record composite does not prove the whole rule “Run builds and tests from clean, declared inputs.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Manifests can pass while a mutable tool download or warm cache changes output. The proposal can Pass while “Run builds and tests from clean, declared inputs.” remains false.

## `USEQ-8574E516` — Deterministic

**Exact statement**

> Fail closed when mandatory checks cannot run or produce ambiguous status.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Given a versioned mandatory-check set, run states can deterministically forbid promotion when a check is missing, unable to run, or has ambiguous status. This proves the complete bounded meaning of “Fail closed when mandatory checks cannot run or produce ambiguous status.”.

### Exact deterministic check

1. Fail closed when mandatory checks cannot run or produce ambiguous status.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Any incomplete/ambiguous mandatory check mapped to pass violates the exact gate state machine. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-D8198F66` — Nondeterministic

**Exact statement**

> Separate trusted branch and release jobs from untrusted contribution contexts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Separation of all trusted branch/release jobs from untrusted contexts is an adversarial identity, cache, network, and workflow-flow claim. Therefore the proposed structured-record composite does not prove the whole rule “Separate trusted branch and release jobs from untrusted contribution contexts.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Distinct jobs can exist while they share a mutable cache or runner credential. The proposal can Pass while “Separate trusted branch and release jobs from untrusted contribution contexts.” remains false.

## `USEQ-E3F935AB` — Nondeterministic

**Exact statement**

> Prevent untrusted jobs from accessing secrets, signing material, deployment rights, or mutable trusted caches.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing all untrusted jobs from every secret, key, right, and mutable cache path requires complete workflow and provider threat scope. Therefore the proposed structured-record composite does not prove the whole rule “Prevent untrusted jobs from accessing secrets, signing material, deployment rights, or mutable trusted caches.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Declared permissions can pass while a third-party action reads a signing token from metadata. The proposal can Pass while “Prevent untrusted jobs from accessing secrets, signing material, deployment rights, or mutable trusted caches.” remains false.

## `USEQ-CB522CDF` — Nondeterministic

**Exact statement**

> Run fast high-signal checks early and broader risk-based checks before acceptance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Run fast high-signal checks early and broader risk-based checks before acceptance.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-007A0FED` — Nondeterministic

**Exact statement**

> Verify formatting, compilation, static analysis, unit tests, contracts, secrets, dependencies, policy, and packaging as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Applicable check classes, tool/rule adequacy, and complete code/package scope depend on language, product, threat, and packaging context. Therefore the proposed structured-record composite does not prove the whole rule “Verify formatting, compilation, static analysis, unit tests, contracts, secrets, dependencies, policy, and packaging as applicable.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

All listed jobs can run while the needed generated-code or runtime-policy check is absent. The proposal can Pass while “Verify formatting, compilation, static analysis, unit tests, contracts, secrets, dependencies, policy, and packaging as applicable.” remains false.

## `USEQ-BAE610E4` — Nondeterministic

**Exact statement**

> Make check output diagnostic and link failures to evidence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make check output diagnostic and link failures to evidence.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3B38BEB9` — Nondeterministic

**Exact statement**

> Track flaky tests, unstable infrastructure, and long queue time as integration defects.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Track flaky tests, unstable infrastructure, and long queue time as integration defects.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F97BCD7F` — Nondeterministic

**Exact statement**

> Avoid rerunning failed jobs until they pass without understanding the cause.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid rerunning failed jobs until they pass without understanding the cause.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F4D6E3B3` — Nondeterministic

**Exact statement**

> Pin toolchain, action, plugin, and dependency versions sufficiently for trustworthy results.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Pinning is measurable, but sufficiency for trustworthy results depends on resolver behavior, transitive inputs, signatures, and update policy. Therefore the proposed structured-record composite does not prove the whole rule “Pin toolchain, action, plugin, and dependency versions sufficiently for trustworthy results.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Top-level versions can be pinned while a mutable action tag or transitive tool changes. The proposal can Pass while “Pin toolchain, action, plugin, and dependency versions sufficiently for trustworthy results.” remains false.

## `USEQ-E307C67C` — Nondeterministic

**Exact statement**

> Use isolated workspaces and clean up credentials and artifacts after each run.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Workspace isolation and cleanup cover every file, process, cache, mount, credential, artifact, failure, and provider side effect. Therefore the proposed structured-record composite does not prove the whole rule “Use isolated workspaces and clean up credentials and artifacts after each run.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Normal cleanup can pass while a canceled job leaves credentials or a poisoned cache. The proposal can Pass while “Use isolated workspaces and clean up credentials and artifacts after each run.” remains false.

## `USEQ-37F01160` — Nondeterministic

**Exact statement**

> Make generated files reproducible and detect uncommitted or stale generation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Generated reproducibility and stale detection require controlling every generator input/platform and complete generated-output inventory. Therefore the proposed structured-record composite does not prove the whole rule “Make generated files reproducible and detect uncommitted or stale generation.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Regeneration can match locally while locale or remote schema changes output in CI. The proposal can Pass while “Make generated files reproducible and detect uncommitted or stale generation.” remains false.

## `USEQ-609408D4` — Deterministic

**Exact statement**

> Prevent merge queues from accepting a combination never tested together.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A merge-queue test result can bind the exact combined commit tree/digest that the queue proposes to accept. This proves the complete bounded meaning of “Prevent merge queues from accepting a combination never tested together.”.

### Exact deterministic check

1. Prevent merge queues from accepting a combination never tested together.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Accepting a different combination or one without that digest's successful result deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-FF347E02` — Deterministic

**Exact statement**

> Record source revision, pipeline version, inputs, environment, and artifacts for each result.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Source revision, pipeline version, input/environment identities, artifact digests, and result identity form a finite execution record. This proves the complete bounded meaning of “Record source revision, pipeline version, inputs, environment, and artifacts for each result.”.

### Exact deterministic check

1. Record source revision, pipeline version, inputs, environment, and artifacts for each result.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Missing links or mismatched digests deterministically fail. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-B022185A` — Nondeterministic

**Exact statement**

> Monitor lead time, failure rate, recovery time, queue time, and feedback latency without using them as individual performance scores.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Monitor lead time, failure rate, recovery time, queue time, and feedback latency without using them as individual performance scores.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A0BC4022` — Nondeterministic

**Exact statement**

> Exercise the pipeline's ability to reject deliberately broken, vulnerable, or unsigned changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Deliberately broken/vulnerable/unsigned fixtures prove only those cases, not all gate bypasses or vulnerability/signature states. Therefore the proposed structured-record composite does not prove the whole rule “Exercise the pipeline's ability to reject deliberately broken, vulnerable, or unsigned changes.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

The pipeline can reject the canned sample while a different unsigned format or scanner blind spot passes. The proposal can Pass while “Exercise the pipeline's ability to reject deliberately broken, vulnerable, or unsigned changes.” remains false.

## `USEQ-8DA2AF16` — Deterministic

**Exact statement**

> Build a release artifact once and promote the identical immutable artifact through environments.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. One build artifact identity, immutable digest, and environment-promotion edges are exact release graph facts. This proves the complete bounded meaning of “Build a release artifact once and promote the identical immutable artifact through environments.”.

### Exact deterministic check

1. Build a release artifact once and promote the identical immutable artifact through environments.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any environment rebuild, mutation, or distinct digest deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-517E9DBD` — Deterministic

**Exact statement**

> Separate artifact creation from deployment authorization.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Artifact-creation identity, deployment-authorizer identity, roles, and ordered workflow transitions are finite separation-of-duty facts. This proves the complete bounded meaning of “Separate artifact creation from deployment authorization.”.

### Exact deterministic check

1. Separate artifact creation from deployment authorization.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

The same unauthorized transition or deployment before authorization deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-5FBAC758` — Deterministic

**Exact statement**

> Verify provenance, signature, digest, policy, and environment eligibility before promotion.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed promotion gate, provenance/signature/digest validity, policy version, environment eligibility state, artifact identity, and ordering are exact inputs. This proves the complete bounded meaning of “Verify provenance, signature, digest, policy, and environment eligibility before promotion.”.

### Exact deterministic check

1. Verify provenance, signature, digest, policy, and environment eligibility before promotion.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A missing/invalid check, wrong artifact, or promotion before gate success deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-455A9A23` — Nondeterministic

**Exact statement**

> Use least-privilege deployment identities scoped to intended targets.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Deployment identity scopes are inspectable, but least privilege requires complete target, action, emergency, provider, and operational-need authority. Therefore the proposed structured-record composite does not prove the whole rule “Use least-privilege deployment identities scoped to intended targets.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Each pipeline can use a dedicated role while it retains broad wildcard rights or an omitted provider administrator bypasses it. The proposal can Pass while “Use least-privilege deployment identities scoped to intended targets.” remains false.

## `USEQ-E2718EA6` — Deterministic

**Exact statement**

> Store deployment definitions, configuration references, and policy as reviewed versioned artifacts.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The original explicitly requires stored reviewed versioned artifacts, so artifact bytes, digests, versions, and review bindings must be verified directly.

### Exact deterministic check

1. The complete sealed deployment-artifact inventory contains retrievable deployment definitions, configuration references, and policy artifacts, each content-addressed, versioned, and bound to an authenticated completed review.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A registry can list all three artifact classes and review identifiers while an artifact is missing, mutable, or digest-mismatched. Direct artifact verification fails.

## `USEQ-20FFD65B` — Nondeterministic

**Exact statement**

> Require explicit approval where risk, law, contract, or segregation of duties demands it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Require explicit approval where risk, law, contract, or segregation of duties demands it.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F987D323` — Nondeterministic

**Exact statement**

> Make automated approvals evidence-based and auditable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Audit records are exact, but whether evidence justifies approval and is complete/current requires accountable release judgment. Therefore the proposed structured-record composite does not prove the whole rule “Make automated approvals evidence-based and auditable.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

An automated approval can cite green irrelevant tests while missing the critical migration evidence. The proposal can Pass while “Make automated approvals evidence-based and auditable.” remains false.

## `USEQ-82AC6F44` — Nondeterministic

**Exact statement**

> Use progressive exposure and health gates proportionate to blast radius.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use progressive exposure and health gates proportionate to blast radius.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BC77A748` — Nondeterministic

**Exact statement**

> Validate user-facing success, business integrity, latency, errors, saturation, security, and dependency health during rollout.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Validate user-facing success, business integrity, latency, errors, saturation, security, and dependency health during rollout.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D63DD56F` — Deterministic

**Exact statement**

> Pause or roll back automatically or operationally when predefined thresholds fail.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Given predefined metric thresholds and a sealed rollout state machine, threshold failure can be required to transition to pause or rollback before continuation. This proves the complete bounded meaning of “Pause or roll back automatically or operationally when predefined thresholds fail.”.

### Exact deterministic check

1. Pause or roll back automatically or operationally when predefined thresholds fail.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Continuing after a failed threshold or using a different policy version deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-1510AAE1` — Nondeterministic

**Exact statement**

> Keep previous known-good artifacts and configuration available.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Stored artifacts/config are exact, but known-good truth and operational availability for actual recovery need a complete restore oracle. Therefore the proposed structured-record composite does not prove the whole rule “Keep previous known-good artifacts and configuration available.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Old files can exist while their secrets, schemas, or dependencies make them unusable. The proposal can Pass while “Keep previous known-good artifacts and configuration available.” remains false.

## `USEQ-E09E9C8D` — Nondeterministic

**Exact statement**

> Test rollback and safe roll-forward, including data and event compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test rollback and safe roll-forward, including data and event compatibility.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-61A0F909` — Deterministic

**Exact statement**

> Prevent deployment when required evidence is stale, missing, or tied to another artifact.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Evidence freshness bounds, artifact digest binding, required-evidence identities, and deployment gate states are exact finite facts. This proves the complete bounded meaning of “Prevent deployment when required evidence is stale, missing, or tied to another artifact.”.

### Exact deterministic check

1. Prevent deployment when required evidence is stale, missing, or tied to another artifact.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Stale, missing, or differently bound evidence mapped to deployment pass deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-79029B47` — Nondeterministic

**Exact statement**

> Serialize or coordinate changes that cannot safely overlap.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Serialize or coordinate changes that cannot safely overlap.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-98255DC6` — Nondeterministic

**Exact statement**

> Make deployment idempotent and resumable after interruption.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idempotence/resumption must hold across every interruption point, partial side effect, dependency state, retry count, and concurrency schedule. Therefore the proposed structured-record composite does not prove the whole rule “Make deployment idempotent and resumable after interruption.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A normal retry can pass while interruption after an external side effect duplicates work. The proposal can Pass while “Make deployment idempotent and resumable after interruption.” remains false.

## `USEQ-C3E713ED` — Deterministic

**Exact statement**

> Record who or what authorized, initiated, changed, verified, paused, and completed the deployment.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed deployment, authorizer, initiator, changer, verifier, pauser, completer identities and ordered events form a finite audit schema. This proves the complete bounded meaning of “Record who or what authorized, initiated, changed, verified, paused, and completed the deployment.”.

### Exact deterministic check

1. Record who or what authorized, initiated, changed, verified, paused, and completed the deployment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing actors/events, invalid references, or impossible ordering deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-913347A6` — Nondeterministic

**Exact statement**

> Prevent direct production mutation outside controlled emergency procedures.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing every direct mutation requires complete production control-plane, provider, credential, emergency, and audit scope plus authorization judgment. Therefore the proposed structured-record composite does not prove the whole rule “Prevent direct production mutation outside controlled emergency procedures.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Pipeline-only records can pass while a provider administrator edits production directly. The proposal can Pass while “Prevent direct production mutation outside controlled emergency procedures.” remains false.

## `USEQ-BE458954` — Nondeterministic

**Exact statement**

> Test emergency release paths without normalizing bypass.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A drill proves one emergency path, not safe behavior across outage, compromise, rollback, staffing, and audit states; whether use becomes normalized is cultural. Therefore the proposed structured-record composite does not prove the whole rule “Test emergency release paths without normalizing bypass.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `human_accountability`

### Skeptical rejection counterexample

Periodic drills can pass while routine releases begin using the bypass or a different emergency failure remains untested. The proposal can Pass while “Test emergency release paths without normalizing bypass.” remains false.

## `USEQ-5A5B48C9` — Nondeterministic

**Exact statement**

> Separate environment secrets and trust relationships.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Environment secret/trust separation spans all identities, federation, keys, recovery paths, caches, provider roots, and network relationships. Therefore the proposed structured-record composite does not prove the whole rule “Separate environment secrets and trust relationships.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Separate vault paths can pass while a shared root role or trust anchor spans environments. The proposal can Pass while “Separate environment secrets and trust relationships.” remains false.

## `USEQ-44042363` — Nondeterministic

**Exact statement**

> Measure deployment outcomes and improve the system after failures and near misses.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure deployment outcomes and improve the system after failures and near misses.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8555A412` — Nondeterministic

**Exact statement**

> Declare source, dependencies, toolchain, environment, configuration, and build parameters as explicit inputs.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Explicit input declarations cannot prove absence of implicit network, clock, cache, host, provider, generated, and transitive inputs. Therefore the proposed structured-record composite does not prove the whole rule “Declare source, dependencies, toolchain, environment, configuration, and build parameters as explicit inputs.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The manifest can list source/toolchain/config while a mutable remote schema affects output. The proposal can Pass while “Declare source, dependencies, toolchain, environment, configuration, and build parameters as explicit inputs.” remains false.

## `USEQ-E9C8C459` — Deterministic

**Exact statement**

> Pin mutable inputs or verify them cryptographically.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For each declared mutable build input, an immutable version/digest or cryptographic verification rule is an exact provenance constraint. This proves the complete bounded meaning of “Pin mutable inputs or verify them cryptographically.”.

### Exact deterministic check

1. Pin mutable inputs or verify them cryptographically.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

An unresolved mutable reference, missing verification, or digest mismatch deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-907EC8FF` — Nondeterministic

**Exact statement**

> Restrict undeclared network access during trusted builds where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Restrict undeclared network access during trusted builds where practical.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DED09248` — Nondeterministic

**Exact statement**

> Use clean isolated builders and prevent cross-tenant or cross-trust contamination.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use clean isolated builders and prevent cross-tenant or cross-trust contamination.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1D16BCDA` — Deterministic

**Exact statement**

> Make locale, time zone, timestamps, file order, random seeds, and environment influence deterministic or recorded.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Locale, time zone, timestamp, file-order policy, random seed, and environment digest are finite execution inputs that can be fixed or recorded exactly. This proves the complete bounded meaning of “Make locale, time zone, timestamps, file order, random seeds, and environment influence deterministic or recorded.”.

### Exact deterministic check

1. Make locale, time zone, timestamps, file order, random seeds, and environment influence deterministic or recorded.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any declared influence lacking a fixed/recorded value or mismatching its run binding deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-269DB62C` — Nondeterministic

**Exact statement**

> Separate build-time secrets from output and prevent them from entering artifacts or logs.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing build-secret leakage requires complete secret inventory and every output/log/intermediate/error/cache/encoding path. Therefore the proposed structured-record composite does not prove the whole rule “Separate build-time secrets from output and prevent them from entering artifacts or logs.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Scans can pass while a tool embeds a token in debug metadata or an encoded log. The proposal can Pass while “Separate build-time secrets from output and prevent them from entering artifacts or logs.” remains false.

## `USEQ-F73B5A06` — Nondeterministic

**Exact statement**

> Use minimum build permissions and short-lived credentials.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Permission sets and credential TTLs are exact, but minimum privilege and sufficiently short life depend on complete build actions and threat context. Therefore the proposed structured-record composite does not prove the whole rule “Use minimum build permissions and short-lived credentials.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A short-lived role can still grant broad signing and deployment rights unused by the build. The proposal can Pass while “Use minimum build permissions and short-lived credentials.” remains false.

## `USEQ-4B359B84` — Nondeterministic

**Exact statement**

> Prevent source from modifying trusted builder policy or accessing unrelated release secrets.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Builder-policy immutability and release-secret isolation are adversarial flows across source hooks, plugins, generators, workers, caches, and provider control planes. Therefore the proposed structured-record composite does not prove the whole rule “Prevent source from modifying trusted builder policy or accessing unrelated release secrets.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Source can pass declared restrictions while a build plugin modifies policy or reads unrelated metadata credentials. The proposal can Pass while “Prevent source from modifying trusted builder policy or accessing unrelated release secrets.” remains false.

## `USEQ-2A35153B` — Deterministic

**Exact statement**

> Generate immutable output digests and machine-verifiable provenance.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Immutable output digests and signed machine-verifiable provenance with exact artifact identity are bounded cryptographic facts. This proves the complete bounded meaning of “Generate immutable output digests and machine-verifiable provenance.”.

### Exact deterministic check

1. Generate immutable output digests and machine-verifiable provenance.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A changed artifact, invalid signature, missing digest, or broken provenance edge deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-BE8EF1D3` — Nondeterministic

**Exact statement**

> Rebuild independently and compare outputs or meaningful normalized content for high-assurance releases.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Rebuild independently and compare outputs or meaningful normalized content for high-assurance releases.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B910BD14` — Deterministic

**Exact statement**

> Detect generated content that differs from checked-in or declared source.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Given declared source and generator versions, generated-content digests can be compared exactly with checked-in/declared expected digests. This proves the complete bounded meaning of “Detect generated content that differs from checked-in or declared source.”.

### Exact deterministic check

1. Detect generated content that differs from checked-in or declared source.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any stale, uncommitted, or differently generated content creates a deterministic diff. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-D60E6950` — Nondeterministic

**Exact statement**

> Verify final packages contain only intended files, permissions, metadata, and dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Intended package files/dependencies require an authoritative complete oracle, while permissions/metadata vary by format and platform. Therefore the proposed structured-record composite does not prove the whole rule “Verify final packages contain only intended files, permissions, metadata, and dependencies.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

A package fixture can pass while an unintended generated secret file or platform-only dependency remains. The proposal can Pass while “Verify final packages contain only intended files, permissions, metadata, and dependencies.” remains false.

## `USEQ-37514564` — Nondeterministic

**Exact statement**

> Scan and inspect the final artifact, not only its source inputs.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Scan and inspect the final artifact, not only its source inputs.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-48587D52` — Deterministic

**Exact statement**

> Sign artifacts only after required verification succeeds.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Required-verification identities/statuses, artifact digest, signing event, and timestamps form an exact ordered gate: signing may occur only after success. This proves the complete bounded meaning of “Sign artifacts only after required verification succeeds.”.

### Exact deterministic check

1. Sign artifacts only after required verification succeeds.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

Signing before a required verification or signing another digest deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-587FE097` — Nondeterministic

**Exact statement**

> Protect signing keys and separate signing authority from untrusted build execution.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Signing-key protection and authority separation span all key copies, recovery mechanisms, identities, APIs, logs, and provider administrators. Therefore the proposed structured-record composite does not prove the whole rule “Protect signing keys and separate signing authority from untrusted build execution.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

An HSM can pass while the untrusted build identity can invoke signing or a backup key is exposed. The proposal can Pass while “Protect signing keys and separate signing authority from untrusted build execution.” remains false.

## `USEQ-C33F05AA` — Deterministic

**Exact statement**

> Retain builders, inputs, provenance, artifacts, and logs for incident investigation.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A sealed build/release retention manifest can bind builder identity, inputs, provenance, artifact digests, and log artifacts. This proves the complete bounded meaning of “Retain builders, inputs, provenance, artifacts, and logs for incident investigation.”.

### Exact deterministic check

1. Retain builders, inputs, provenance, artifacts, and logs for incident investigation.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any missing retained object, broken reference, or changed digest deterministically fails archival integrity. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-1A4ABD7D` — Nondeterministic

**Exact statement**

> Maintain a clean-room rebuild procedure after suspected compromise.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Maintain a clean-room rebuild procedure after suspected compromise.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-764FB544` — Nondeterministic

**Exact statement**

> Rotate and revoke signing identity and redistribute corrected artifacts when necessary.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** When rotation/revocation is necessary, rapid completeness, corrected-artifact scope, and distribution to all consumers require incident and ecosystem judgment. Therefore the proposed structured-record composite does not prove the whole rule “Rotate and revoke signing identity and redistribute corrected artifacts when necessary.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

One key can be replaced while old signatures remain trusted by offline clients or a package channel. The proposal can Pass while “Rotate and revoke signing identity and redistribute corrected artifacts when necessary.” remains false.

## `USEQ-6B385742` — Nondeterministic

**Exact statement**

> Test reproducibility after toolchain and platform updates.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test reproducibility after toolchain and platform updates.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FC20FB0F` — Nondeterministic

**Exact statement**

> Document remaining nondeterminism and why it does not undermine trust.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Document remaining nondeterminism and why it does not undermine trust.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E4DC3BC6` — Nondeterministic

**Exact statement**

> Represent material infrastructure, policy, networking, identity, observability, storage, and deployment configuration as versioned code or equivalent declarative records.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Materiality and complete infrastructure/policy/network/identity/observability/storage/deployment configuration scope cannot be proven by a declared code inventory. Therefore the proposed structured-record composite does not prove the whole rule “Represent material infrastructure, policy, networking, identity, observability, storage, and deployment configuration as versioned code or equivalent declarative records.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Most resources can be versioned while a material provider-console policy remains implicit. The proposal can Pass while “Represent material infrastructure, policy, networking, identity, observability, storage, and deployment configuration as versioned code or equivalent declarative records.” remains false.

## `USEQ-F53D21B2` — Nondeterministic

**Exact statement**

> Apply review, protected branches, traceability, testing, and release controls to infrastructure changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Branch/gate records are exact, but complete infrastructure-change scope, substantive review, test adequacy, and authority are not. Therefore the proposed structured-record composite does not prove the whole rule “Apply review, protected branches, traceability, testing, and release controls to infrastructure changes.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Managed templates can pass while a manual console change or imported module bypasses controls. The proposal can Pass while “Apply review, protected branches, traceability, testing, and release controls to infrastructure changes.” remains false.

## `USEQ-9F1306DB` — Nondeterministic

**Exact statement**

> Separate reusable modules from environment-specific composition.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Separate reusable modules from environment-specific composition.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FC258A07` — Nondeterministic

**Exact statement**

> Define secure defaults and prohibit dangerous options through policy where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define secure defaults and prohibit dangerous options through policy where practical.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-59037C06` — Nondeterministic

**Exact statement**

> Validate syntax, types, references, policy, cost, security, and destructive impact before apply.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Syntax/reference checks are bounded, but cost, security, policy, and destructive-impact correctness require live scope, risk thresholds, and semantic oracles. Therefore the proposed structured-record composite does not prove the whole rule “Validate syntax, types, references, policy, cost, security, and destructive impact before apply.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A plan can parse and pass policy while deleting a critical implicit resource or causing unaffordable cost. The proposal can Pass while “Validate syntax, types, references, policy, cost, security, and destructive impact before apply.” remains false.

## `USEQ-E0C505B4` — Nondeterministic

**Exact statement**

> Generate a change plan and require human review for high-impact or destructive differences.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Generate a change plan and require human review for high-impact or destructive differences.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-AAC85EFD` — Nondeterministic

**Exact statement**

> Use least-privilege automation identities and avoid shared administrator credentials.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identity records are exact, but least privilege and absence of shared admin credentials require complete automation/provider identity and operational-need scope. Therefore the proposed structured-record composite does not prove the whole rule “Use least-privilege automation identities and avoid shared administrator credentials.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Dedicated roles can exist while a shared root token remains in a fallback workflow. The proposal can Pass while “Use least-privilege automation identities and avoid shared administrator credentials.” remains false.

## `USEQ-595C5316` — Nondeterministic

**Exact statement**

> Keep secrets out of source and state files; protect state as sensitive production data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Secret absence from source/state and adequate state protection span histories, backups, caches, encodings, identities, storage layers, and access paths. Therefore the proposed structured-record composite does not prove the whole rule “Keep secrets out of source and state files; protect state as sensitive production data.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Scanners can pass while an encrypted state backup is broadly readable with its key nearby. The proposal can Pass while “Keep secrets out of source and state files; protect state as sensitive production data.” remains false.

## `USEQ-A4FA3E49` — Nondeterministic

**Exact statement**

> Detect and reconcile unauthorized or manual drift.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Detect and reconcile unauthorized or manual drift.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7EA84BA2` — Nondeterministic

**Exact statement**

> Make creation, update, replacement, scaling, failover, and destruction idempotent where possible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make creation, update, replacement, scaling, failover, and destruction idempotent where possible.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-73830565` — Nondeterministic

**Exact statement**

> Protect critical resources against accidental deletion and uncontrolled replacement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect critical resources against accidental deletion and uncontrolled replacement.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5617B518` — Nondeterministic

**Exact statement**

> Version and migrate state safely.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Version IDs are exact, but safe state migration requires every historical state, interruption, provider behavior, concurrency, and recovery oracle. Therefore the proposed structured-record composite does not prove the whole rule “Version and migrate state safely.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Normal migration can pass while an interrupted upgrade corrupts an older state snapshot. The proposal can Pass while “Version and migrate state safely.” remains false.

## `USEQ-506A79E0` — Nondeterministic

**Exact statement**

> Test modules and complete environments in isolated accounts or namespaces.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test modules and complete environments in isolated accounts or namespaces.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3AFFAE81` — Nondeterministic

**Exact statement**

> Reconstruct an environment from source, artifacts, configuration, data recovery, and documented external prerequisites.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reconstruct an environment from source, artifacts, configuration, data recovery, and documented external prerequisites.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-47EDD512` — Nondeterministic

**Exact statement**

> Record provider defaults and implicit resources that code does not create.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Implicit/provider-default resources are precisely those missing from code, so record completeness needs external discovery and provider semantics. Therefore the proposed structured-record composite does not prove the whole rule “Record provider defaults and implicit resources that code does not create.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Listed defaults can pass while a new provider-created network rule or identity remains implicit. The proposal can Pass while “Record provider defaults and implicit resources that code does not create.” remains false.

## `USEQ-CFDDFBBF` — Nondeterministic

**Exact statement**

> Define import and ownership procedures for existing resources.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Procedure presence is exact, but correct import boundaries, ownership authority, conflict handling, and completeness require human platform judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define import and ownership procedures for existing resources.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A template procedure can exist while importing a shared resource into the wrong owner/state. The proposal can Pass while “Define import and ownership procedures for existing resources.” remains false.

## `USEQ-FCC3E68C` — Nondeterministic

**Exact statement**

> Coordinate changes across application, schema, network, identity, and observability dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Coordinate changes across application, schema, network, identity, and observability dependencies.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-632F6BEB` — Nondeterministic

**Exact statement**

> Plan for provider API deprecation and module lifecycle.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Plan for provider API deprecation and module lifecycle.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6A024846` — Nondeterministic

**Exact statement**

> Measure provisioning time, drift, failed changes, rollback, policy violations, and orphaned resources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Times and failure counts are numeric, but complete drift/orphan populations, policy-violation truth, and rollback outcome scope are not. Therefore the proposed structured-record composite does not prove the whole rule “Measure provisioning time, drift, failed changes, rollback, policy violations, and orphaned resources.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Metrics can look good because unmanaged orphan resources and console drift never enter telemetry. The proposal can Pass while “Measure provisioning time, drift, failed changes, rollback, policy violations, and orphaned resources.” remains false.

## `USEQ-17F99C8E` — Nondeterministic

**Exact statement**

> Exercise disaster recovery without relying on inaccessible production control planes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Exercise disaster recovery without relying on inaccessible production control planes.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-80674A43` — Nondeterministic

**Exact statement**

> Choose rolling, canary, blue-green, cohort, shadow, parallel, regional, or other strategies according to risk and state compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Choose rolling, canary, blue-green, cohort, shadow, parallel, regional, or other strategies according to risk and state compatibility.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-92842952` — Nondeterministic

**Exact statement**

> Define the smallest safe initial exposure and an explicit expansion sequence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define the smallest safe initial exposure and an explicit expansion sequence.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2A0FF5B4` — Nondeterministic

**Exact statement**

> Ensure cohorts are representative enough to reveal relevant failures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure cohorts are representative enough to reveal relevant failures.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FDF15AD0` — Nondeterministic

**Exact statement**

> Keep control and treatment populations comparable when using comparative gates.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep control and treatment populations comparable when using comparative gates.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4896CCAE` — Nondeterministic

**Exact statement**

> Separate code deployment, feature exposure, data migration, and customer communication when decoupling reduces risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Separate code deployment, feature exposure, data migration, and customer communication when decoupling reduces risk.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DC837F09` — Deterministic

**Exact statement**

> Define stop, hold, rollback, roll-forward, incident, and communication thresholds before launch.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A launch policy can bind stop, hold, rollback, roll-forward, incident, and communication thresholds to an exact version and pre-launch timestamp. This proves the complete bounded meaning of “Define stop, hold, rollback, roll-forward, incident, and communication thresholds before launch.”.

### Exact deterministic check

1. Define stop, hold, rollback, roll-forward, incident, and communication thresholds before launch.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing or late thresholds, or launch against another policy version, deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-203A1CDD` — Nondeterministic

**Exact statement**

> Verify old and new versions can coexist with schemas, messages, caches, sessions, and clients.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Coexistence spans every version pair, schema/message/cache/session/client state, traffic pattern, and failure sequence. Therefore the proposed structured-record composite does not prove the whole rule “Verify old and new versions can coexist with schemas, messages, caches, sessions, and clients.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Adjacent-version fixtures can pass while an old client writes a cached representation the new server cannot read. The proposal can Pass while “Verify old and new versions can coexist with schemas, messages, caches, sessions, and clients.” remains false.

## `USEQ-AED1FAFC` — Nondeterministic

**Exact statement**

> Prevent one-way data changes from making rollback appear available when it is not.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A rollback drill cannot prove reversibility across every one-way data transform, partial deployment, side effect, and historical state. Therefore the proposed structured-record composite does not prove the whole rule “Prevent one-way data changes from making rollback appear available when it is not.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

UI rollback can pass while an irreversible migration has already discarded data. The proposal can Pass while “Prevent one-way data changes from making rollback appear available when it is not.” remains false.

## `USEQ-906DDAA4` — Nondeterministic

**Exact statement**

> Warm capacity, caches, connections, and dependencies before traffic shift where needed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Warm capacity, caches, connections, and dependencies before traffic shift where needed.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5554C95B` — Nondeterministic

**Exact statement**

> Drain and terminate old versions safely after in-flight work completes or transfers.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safe draining and completion/transfer of all in-flight work require complete queues, sessions, retries, timers, and business-effect oracles. Therefore the proposed structured-record composite does not prove the whole rule “Drain and terminate old versions safely after in-flight work completes or transfers.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Connection counts can reach zero while a delayed job or leased message is lost on termination. The proposal can Pass while “Drain and terminate old versions safely after in-flight work completes or transfers.” remains false.

## `USEQ-C8FC60DB` — Nondeterministic

**Exact statement**

> Observe user success, business integrity, latency, errors, saturation, dependency health, security, and support demand.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Observe user success, business integrity, latency, errors, saturation, dependency health, security, and support demand.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-AEFC8DCA` — Nondeterministic

**Exact statement**

> Account for delayed effects, background work, billing cycles, data pipelines, and long-lived sessions before expansion.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Account for delayed effects, background work, billing cycles, data pipelines, and long-lived sessions before expansion.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-722D65DC` — Nondeterministic

**Exact statement**

> Prevent automatic rollout from outrunning observability or human response.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent automatic rollout from outrunning observability or human response.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A66E17A6` — Nondeterministic

**Exact statement**

> Use kill switches for high-risk behavior, but test them and keep defaults safe.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** High-risk classification and safe defaults require judgment, while finite kill-switch tests do not prove effectiveness across all paths/failures. Therefore the proposed structured-record composite does not prove the whole rule “Use kill switches for high-risk behavior, but test them and keep defaults safe.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

The switch can disable the main route while cached workers continue the risky behavior. The proposal can Pass while “Use kill switches for high-risk behavior, but test them and keep defaults safe.” remains false.

## `USEQ-FD5F6635` — Nondeterministic

**Exact statement**

> Keep deployment and feature state visible in incident telemetry.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Telemetry visibility requires complete deployment/feature state inventory, accurate correlation, freshness, and incident usefulness. Therefore the proposed structured-record composite does not prove the whole rule “Keep deployment and feature state visible in incident telemetry.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Dashboards can show declared flags while a region's stale exposure state is absent. The proposal can Pass while “Keep deployment and feature state visible in incident telemetry.” remains false.

## `USEQ-33B2FE53` — Nondeterministic

**Exact statement**

> Verify traffic routing, stickiness, failover, and health-check semantics.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Routing, stickiness, failover, and health checks span identities, caches, regions, failures, timing, and semantic health oracles. Therefore the proposed structured-record composite does not prove the whole rule “Verify traffic routing, stickiness, failover, and health-check semantics.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Known failover cases can pass while sticky sessions route to an incompatible version. The proposal can Pass while “Verify traffic routing, stickiness, failover, and health-check semantics.” remains false.

## `USEQ-993B7855` — Nondeterministic

**Exact statement**

> Test reverse traffic shift and cleanup before launch.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One reverse-shift/cleanup test cannot prove every route, stateful session, queue, cache, resource, and interruption path before launch. Therefore the proposed structured-record composite does not prove the whole rule “Test reverse traffic shift and cleanup before launch.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A sample shift can pass while a long-lived connection remains on the old version. The proposal can Pass while “Test reverse traffic shift and cleanup before launch.” remains false.

## `USEQ-2CFDB0CB` — Nondeterministic

**Exact statement**

> Record every exposure change and decision rationale.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Exposure event fields are exact, but every-change capture and decision-rationale truth/completeness are not. Therefore the proposed structured-record composite does not prove the whole rule “Record every exposure change and decision rationale.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Registered changes can have rationale while an automatic cohort expansion is omitted or justified with boilerplate. The proposal can Pass while “Record every exposure change and decision rationale.” remains false.

## `USEQ-FFB58250` — Nondeterministic

**Exact statement**

> Define when the rollout is complete and temporary infrastructure can be removed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Completion criteria and temporary-resource scope require rollout semantics, rollback windows, in-flight work, and dependency judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define when the rollout is complete and temporary infrastructure can be removed.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A policy can mark rollout complete while old clients or rollback windows still require temporary infrastructure. The proposal can Pass while “Define when the rollout is complete and temporary infrastructure can be removed.” remains false.

## `USEQ-AF20E3D9` — Nondeterministic

**Exact statement**

> Conduct a review when rollout gates fail or produce unexplained anomalies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Conduct a review when rollout gates fail or produce unexplained anomalies.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3F4E7A24` — Nondeterministic

**Exact statement**

> Version every schema, migration, data transform, permission, index, and pipeline change.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every schema/migration/transform/permission/index/pipeline change needs complete code, runtime, provider, and manual-change discovery. Therefore the proposed structured-record composite does not prove the whole rule “Version every schema, migration, data transform, permission, index, and pipeline change.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Managed migrations can be versioned while a manual permission or generated index change is omitted. The proposal can Pass while “Version every schema, migration, data transform, permission, index, and pipeline change.” remains false.

## `USEQ-A97FB4A9` — Nondeterministic

**Exact statement**

> Analyze application, worker, report, export, cache, search, integration, and rollback compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Analyze application, worker, report, export, cache, search, integration, and rollback compatibility.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-29E45716` — Nondeterministic

**Exact statement**

> Use staged compatible changes when versions coexist.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Staged compatibility depends on exact coexistence matrix, data semantics, client behavior, transition ordering, and safe duration. Therefore the proposed structured-record composite does not prove the whole rule “Use staged compatible changes when versions coexist.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A staged plan can pass docs while an old writer creates data unreadable by the new version. The proposal can Pass while “Use staged compatible changes when versions coexist.” remains false.

## `USEQ-11851D0A` — Nondeterministic

**Exact statement**

> Run destructive or long migrations separately from application deployment when separation reduces risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Run destructive or long migrations separately from application deployment when separation reduces risk.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D7A998B9` — Nondeterministic

**Exact statement**

> Estimate duration, locks, I/O, storage, log, replication, and downstream impact using representative data.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Estimate duration, locks, I/O, storage, log, replication, and downstream impact using representative data.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1F838C1D` — Deterministic

**Exact statement**

> Validate preconditions and stop on unexpected state.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed change protocol with explicit preconditions and expected-state predicates, execution can be required to stop before mutation on any mismatch. This proves the complete bounded meaning of “Validate preconditions and stop on unexpected state.”.

### Exact deterministic check

1. Validate preconditions and stop on unexpected state.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Applying after a failed/unknown precondition or using a different expected-state version deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-8C9DCBEF` — Deterministic

**Exact statement**

> Take a verified recovery point before irreversible change.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Recovery-point artifact identity, verification result, change identity, and timestamps form an exact pre-change gate. This proves the complete bounded meaning of “Take a verified recovery point before irreversible change.”.

### Exact deterministic check

1. Take a verified recovery point before irreversible change.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Missing/invalid recovery evidence or creation after irreversible mutation deterministically fails. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-EEB2E713` — Nondeterministic

**Exact statement**

> Make migrations idempotent, resumable, observable, and safe under retry.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idempotence, resumability, observability, and retry safety must hold at every step, interruption, concurrent state, dependency failure, and side effect. Therefore the proposed structured-record composite does not prove the whole rule “Make migrations idempotent, resumable, observable, and safe under retry.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Normal reruns can pass while interruption after an external write duplicates or loses data. The proposal can Pass while “Make migrations idempotent, resumable, observable, and safe under retry.” remains false.

## `USEQ-A69FE821` — Nondeterministic

**Exact statement**

> Throttle and checkpoint long-running changes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Throttle/checkpoint settings are exact, but long-running applicability, safe rates, checkpoint semantics, and recovery adequacy require workload/data judgment. Therefore the proposed structured-record composite does not prove the whole rule “Throttle and checkpoint long-running changes.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A job can checkpoint regularly while checkpoints omit side effects or the throttle still overloads production. The proposal can Pass while “Throttle and checkpoint long-running changes.” remains false.

## `USEQ-ADDBE0D0` — Nondeterministic

**Exact statement**

> Monitor locks, errors, lag, throughput, capacity, queue growth, and user impact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Monitor locks, errors, lag, throughput, capacity, queue growth, and user impact.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1612BF52` — Nondeterministic

**Exact statement**

> Verify counts, checksums, constraints, totals, samples, and business invariants.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Counts/checksums are exact for scoped data, but samples and business invariants cannot prove complete correctness without an authoritative data/consumer oracle. Therefore the proposed structured-record composite does not prove the whole rule “Verify counts, checksums, constraints, totals, samples, and business invariants.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Totals can balance while records are assigned to wrong users or an unmodeled invariant fails. The proposal can Pass while “Verify counts, checksums, constraints, totals, samples, and business invariants.” remains false.

## `USEQ-48E8D418` — Nondeterministic

**Exact statement**

> Prevent old code from writing data incompatible with the new representation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing every old writer from creating incompatible data spans all versions, clients, retries, queues, cached sessions, and representations. Therefore the proposed structured-record composite does not prove the whole rule “Prevent old code from writing data incompatible with the new representation.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Version tests can pass while a delayed old worker writes after the compatibility guard changes. The proposal can Pass while “Prevent old code from writing data incompatible with the new representation.” remains false.

## `USEQ-33DC8C50` — Nondeterministic

**Exact statement**

> Keep old fields and indexes until consumers and rollback windows are complete.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Consumer completion and rollback-window end need complete consumer inventory, rollout state, retention policy, and human approval. Therefore the proposed structured-record composite does not prove the whole rule “Keep old fields and indexes until consumers and rollback windows are complete.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known consumers can migrate while an offline client still needs the old field. The proposal can Pass while “Keep old fields and indexes until consumers and rollback windows are complete.” remains false.

## `USEQ-D692F341` — Nondeterministic

**Exact statement**

> Define safe rollback or explicit roll-forward when data cannot be reversed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safe rollback/roll-forward choice and irreversibility depend on data semantics, operational state, and accountable recovery judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define safe rollback or explicit roll-forward when data cannot be reversed.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A documented roll-forward plan can omit a destructive partial state and still satisfy headings. The proposal can Pass while “Define safe rollback or explicit roll-forward when data cannot be reversed.” remains false.

## `USEQ-B18B8ADB` — Nondeterministic

**Exact statement**

> Reconcile dual writes and backfills continuously.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Continuous dual-write/backfill reconciliation needs complete copies, identities, timing, lag, conflict semantics, and error paths. Therefore the proposed structured-record composite does not prove the whole rule “Reconcile dual writes and backfills continuously.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known totals can match while one downstream partition silently diverges. The proposal can Pass while “Reconcile dual writes and backfills continuously.” remains false.

## `USEQ-FC5FCD6B` — Nondeterministic

**Exact statement**

> Review and audit manual corrections.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Manual-correction records are exact, but correction validity, reviewer independence, and audit sufficiency require human/data authority. Therefore the proposed structured-record composite does not prove the whole rule “Review and audit manual corrections.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A harmful correction can receive a rubber-stamped review and complete audit fields. The proposal can Pass while “Review and audit manual corrections.” remains false.

## `USEQ-64F2139F` — Nondeterministic

**Exact statement**

> Protect retention, deletion, consent, residency, and legal-hold semantics during migration.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect retention, deletion, consent, residency, and legal-hold semantics during migration.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-22A52F1C` — Nondeterministic

**Exact statement**

> Test restored backups against the new application and schema.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test restored backups against the new application and schema.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5C9EF78B` — Nondeterministic

**Exact statement**

> Remove temporary elevated permissions and migration infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Removal must cover every temporary role, token, policy, worker, network path, storage object, and migration service. Therefore the proposed structured-record composite does not prove the whole rule “Remove temporary elevated permissions and migration infrastructure.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Known roles can be deleted while a provider trust or temporary network rule remains. The proposal can Pass while “Remove temporary elevated permissions and migration infrastructure.” remains false.

## `USEQ-89C3CCB0` — Deterministic

**Exact statement**

> Record exact migration version, operator, start, end, outcome, and evidence.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Migration version, operator identity, start/end timestamps, outcome state, and evidence references form a finite execution schema. This proves the complete bounded meaning of “Record exact migration version, operator, start, end, outcome, and evidence.”.

### Exact deterministic check

1. Record exact migration version, operator, start, end, outcome, and evidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing fields, invalid ordering, wrong version binding, or broken evidence links deterministically fail. Within the sealed platform/delivery evidence, the rule cannot Pass while broken.

## `USEQ-D9F7262D` — Nondeterministic

**Exact statement**

> Define which changes can be rolled back, which require roll-forward, and which require data restoration or reconciliation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Classification among rollback, roll-forward, restoration, and reconciliation requires complete change semantics, data effects, and accountable recovery judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define which changes can be rolled back, which require roll-forward, and which require data restoration or reconciliation.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A matrix can classify a lossy transform as rollback-safe and pass enum checks. The proposal can Pass while “Define which changes can be rolled back, which require roll-forward, and which require data restoration or reconciliation.” remains false.

## `USEQ-2F23054A` — Nondeterministic

**Exact statement**

> Keep previous trusted application, infrastructure, configuration, and policy artifacts available.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Artifact retention is exact, but previous trusted status and practical availability across application, infrastructure, configuration, and policy need validation. Therefore the proposed structured-record composite does not prove the whole rule “Keep previous trusted application, infrastructure, configuration, and policy artifacts available.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Old artifacts can be stored while revoked keys, missing providers, or incompatible state make them unusable. The proposal can Pass while “Keep previous trusted application, infrastructure, configuration, and policy artifacts available.” remains false.

## `USEQ-52FBF8D0` — Deterministic

**Exact statement**

> Test rollback using the same automation and permissions used in production.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Rollback-test automation digest, permission-policy version, production automation digest, and test result are exact workflow/provenance facts. This proves the complete bounded meaning of “Test rollback using the same automation and permissions used in production.”.

### Exact deterministic check

1. Test rollback using the same automation and permissions used in production.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Using different automation or permissions creates an identity/policy mismatch and deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-B540AC6B` — Nondeterministic

**Exact statement**

> Ensure schema, messages, sessions, caches, files, and external side effects remain compatible with rollback.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure schema, messages, sessions, caches, files, and external side effects remain compatible with rollback.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-92B08EA4` — Nondeterministic

**Exact statement**

> Avoid destructive data change before the rollback window closes unless risk is explicitly accepted.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid destructive data change before the rollback window closes unless risk is explicitly accepted.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2B807409` — Nondeterministic

**Exact statement**

> Define rollback triggers based on user impact and integrity, not only technical health.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define rollback triggers based on user impact and integrity, not only technical health.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-66FE3634` — Nondeterministic

**Exact statement**

> Make rollback authority and initiation paths available during an incident.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authority and path records do not prove availability during every incident, provider outage, identity failure, staffing state, or that authority is valid. Therefore the proposed structured-record composite does not prove the whole rule “Make rollback authority and initiation paths available during an incident.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

A documented rollback role can be unreachable during SSO failure or held by an unavailable person. The proposal can Pass while “Make rollback authority and initiation paths available during an incident.” remains false.

## `USEQ-3264C430` — Nondeterministic

**Exact statement**

> Ensure rollback does not repeat charges, notifications, jobs, exports, or other side effects.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing repeated side effects spans every charge, notification, job, export, retry, consumer, and interruption sequence. Therefore the proposed structured-record composite does not prove the whole rule “Ensure rollback does not repeat charges, notifications, jobs, exports, or other side effects.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A rollback fixture can pass while a delayed worker repeats a real charge. The proposal can Pass while “Ensure rollback does not repeat charges, notifications, jobs, exports, or other side effects.” remains false.

## `USEQ-EB2A1C1A` — Nondeterministic

**Exact statement**

> Reconcile data written by the failed version.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Reconciliation needs authoritative correct-state oracles and complete data/copy/consumer scope for everything the failed version wrote. Therefore the proposed structured-record composite does not prove the whole rule “Reconcile data written by the failed version.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Primary counts can balance while a downstream export or cached aggregate remains wrong. The proposal can Pass while “Reconcile data written by the failed version.” remains false.

## `USEQ-36BF8E85` — Nondeterministic

**Exact statement**

> Use kill switches for isolated risky behavior and define safe failure when the flag system is unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Risk isolation and safe failure when flag service fails require threat/domain judgment and exhaustive failure-path behavior. Therefore the proposed structured-record composite does not prove the whole rule “Use kill switches for isolated risky behavior and define safe failure when the flag system is unavailable.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

The switch can cover the main path while cached workers continue risky behavior, or fail-open behavior is labeled safe. The proposal can Pass while “Use kill switches for isolated risky behavior and define safe failure when the flag system is unavailable.” remains false.

## `USEQ-24436BCA` — Nondeterministic

**Exact statement**

> Restrict and audit kill-switch access.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Restriction/audit records cannot prove every kill-switch identity, provider root, cached credential, bypass, and access event or valid authority. Therefore the proposed structured-record composite does not prove the whole rule “Restrict and audit kill-switch access.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Listed roles can be narrow while a shared administrator token changes the switch without audit. The proposal can Pass while “Restrict and audit kill-switch access.” remains false.

## `USEQ-CE7FB317` — Nondeterministic

**Exact statement**

> Test kill switches in production-like conditions and periodically in production-safe drills.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Production likeness, safe drills, periodicity, and kill-switch effectiveness across all paths/failures lack complete oracles. Therefore the proposed structured-record composite does not prove the whole rule “Test kill switches in production-like conditions and periodically in production-safe drills.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

A regular drill can disable a fixture while a real long-lived client ignores the switch. The proposal can Pass while “Test kill switches in production-like conditions and periodically in production-safe drills.” remains false.

## `USEQ-F343F4EA` — Nondeterministic

**Exact statement**

> Prevent stale clients, workers, and long-lived connections from continuing disabled behavior.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing all stale clients/workers/connections from continuing behavior requires complete distributed identity, cache, queue, session, and timing scope. Therefore the proposed structured-record composite does not prove the whole rule “Prevent stale clients, workers, and long-lived connections from continuing disabled behavior.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known clients can stop while an offline worker reconnects with stale state and continues. The proposal can Pass while “Prevent stale clients, workers, and long-lived connections from continuing disabled behavior.” remains false.

## `USEQ-C708B71B` — Nondeterministic

**Exact statement**

> Make configuration and feature state visible to responders.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Telemetry fields do not prove complete/current configuration and feature state or that responders can understand and use it. Therefore the proposed structured-record composite does not prove the whole rule “Make configuration and feature state visible to responders.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A dashboard can show main-region flags while another region's stale state is omitted. The proposal can Pass while “Make configuration and feature state visible to responders.” remains false.

## `USEQ-6C422A58` — Nondeterministic

**Exact statement**

> Define recovery from a bad configuration separately from bad code.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Separate documents or workflow names do not prove recovery procedures are correct for bad configuration versus bad code. Therefore the proposed structured-record composite does not prove the whole rule “Define recovery from a bad configuration separately from bad code.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Two runbook sections can exist while both recommend the same unsafe rollback. The proposal can Pass while “Define recovery from a bad configuration separately from bad code.” remains false.

## `USEQ-2CA6BC3B` — Nondeterministic

**Exact statement**

> Use roll-forward when reverting would violate data or protocol compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use roll-forward when reverting would violate data or protocol compatibility.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-059BE10D` — Nondeterministic

**Exact statement**

> Verify user journeys and business invariants after containment.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** User journeys and business invariants require complete journey/domain scope and exact post-containment correctness oracles. Therefore the proposed structured-record composite does not prove the whole rule “Verify user journeys and business invariants after containment.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Common journeys can pass while a financial invariant remains broken for an alternate path. The proposal can Pass while “Verify user journeys and business invariants after containment.” remains false.

## `USEQ-C98A9821` — Nondeterministic

**Exact statement**

> Communicate rollback effects to support and customers where material.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Communicate rollback effects to support and customers where material.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-05EB12CC` — Nondeterministic

**Exact statement**

> Remove temporary rollback compatibility and emergency flags after stability is established.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Stability establishment and complete temporary compatibility/flag inventory require operational judgment and distributed state scope. Therefore the proposed structured-record composite does not prove the whole rule “Remove temporary rollback compatibility and emergency flags after stability is established.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Registered flags can be removed after dashboards look stable while an omitted old client still depends on compatibility. The proposal can Pass while “Remove temporary rollback compatibility and emergency flags after stability is established.” remains false.

## `USEQ-1A767899` — Nondeterministic

**Exact statement**

> Capture lessons from every rollback or emergency disablement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Capture lessons from every rollback or emergency disablement.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9DFE53B9` — Nondeterministic

**Exact statement**

> Treat developer experience as the ability of people to understand, change, verify, deliver, operate, and retire software safely and effectively.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Treat developer experience as the ability of people to understand, change, verify, deliver, operate, and retire software safely and effectively.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E834F54B` — Nondeterministic

**Exact statement**

> Assign accountable ownership for developer experience across team boundaries rather than treating it as an informal tooling concern.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Assign accountable ownership for developer experience across team boundaries rather than treating it as an informal tooling concern.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-41F60C9D` — Nondeterministic

**Exact statement**

> Measure developer experience through multiple dimensions including feedback loops, cognitive load, flow, satisfaction, collaboration, quality, and delivery outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure developer experience through multiple dimensions including feedback loops, cognitive load, flow, satisfaction, collaboration, quality, and delivery outcomes.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DE7FAAF5` — Nondeterministic

**Exact statement**

> Segment findings by role, tenure, product area, location, accessibility needs, and workflow without exposing individuals or creating unsafe comparisons.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Segment findings by role, tenure, product area, location, accessibility needs, and workflow without exposing individuals or creating unsafe comparisons.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8AC9F599` — Nondeterministic

**Exact statement**

> Use qualitative research, workflow observation, support demand, incident evidence, and tool telemetry together rather than relying on one survey or metric.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use qualitative research, workflow observation, support demand, incident evidence, and tool telemetry together rather than relying on one survey or metric.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E3C0E730` — Nondeterministic

**Exact statement**

> Prioritize improvements by repeated friction, risk, time lost, error probability, interruption cost, and impact on user outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize improvements by repeated friction, risk, time lost, error probability, interruption cost, and impact on user outcomes.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C8FCDEDB` — Nondeterministic

**Exact statement**

> Publish the engineering-system roadmap, service ownership, supported workflows, known limitations, and ways to request improvement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Publication headings cannot prove current ownership, complete workflows/limitations, roadmap realism, or an effective improvement route. Therefore the proposed structured-record composite does not prove the whole rule “Publish the engineering-system roadmap, service ownership, supported workflows, known limitations, and ways to request improvement.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A portal can contain every section while ownership is stale and known limitations are omitted. The proposal can Pass while “Publish the engineering-system roadmap, service ownership, supported workflows, known limitations, and ways to request improvement.” remains false.

## `USEQ-496042E7` — Nondeterministic

**Exact statement**

> Treat internal developers, operators, data practitioners, security teams, and automated agents as distinct platform users with different needs and permissions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Treat internal developers, operators, data practitioners, security teams, and automated agents as distinct platform users with different needs and permissions.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-351D0CF1` — Nondeterministic

**Exact statement**

> Include accessibility and inclusive design in internal tools, documentation, terminals, dashboards, portals, development environments, and support processes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Include accessibility and inclusive design in internal tools, documentation, terminals, dashboards, portals, development environments, and support processes.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C9B06FE8` — Nondeterministic

**Exact statement**

> Prevent local heroics and undocumented workarounds from being accepted as evidence that the engineering system works.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent local heroics and undocumented workarounds from being accepted as evidence that the engineering system works.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3A8FC2D5` — Nondeterministic

**Exact statement**

> Turn recurring onboarding questions, build failures, deployment tickets, access requests, and operational toil into product and platform improvements.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Turn recurring onboarding questions, build failures, deployment tickets, access requests, and operational toil into product and platform improvements.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-EC6588B5` — Nondeterministic

**Exact statement**

> Review developer-experience risks whenever tools, organizational structure, architecture, security policy, or delivery processes materially change.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review developer-experience risks whenever tools, organizational structure, architecture, security policy, or delivery processes materially change.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B09E273B` — Deterministic

**Exact statement**

> Define target feedback times for editing, validation, formatting, compilation or analysis, focused tests, integration tests, review, build, deployment, and production verification.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The ten named feedback stages can have explicit target durations, units, scope/version bindings, and effective dates in a finite policy schema. This proves the complete bounded meaning of “Define target feedback times for editing, validation, formatting, compilation or analysis, focused tests, integration tests, review, build, deployment, and production verification.”.

### Exact deterministic check

1. Define target feedback times for editing, validation, formatting, compilation or analysis, focused tests, integration tests, review, build, deployment, and production verification.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Any missing target, invalid unit, or unbound policy version deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-C262BE4B` — Deterministic

**Exact statement**

> Measure end-to-end waiting time and queue time rather than only tool execution time.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Given sealed workflow start/end/queue/execution events, end-to-end waiting and queue time are exact timestamp differences distinct from tool runtime. This proves the complete bounded meaning of “Measure end-to-end waiting time and queue time rather than only tool execution time.”.

### Exact deterministic check

1. Measure end-to-end waiting time and queue time rather than only tool execution time.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Reporting only execution time, omitting queue events, or computing the wrong duration deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-DAC53EAB` — Nondeterministic

**Exact statement**

> Make fast local or isolated verification representative enough to catch common defects before expensive shared pipelines.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make fast local or isolated verification representative enough to catch common defects before expensive shared pipelines.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3C5DA5E8` — Nondeterministic

**Exact statement**

> Keep slow, broad, or expensive checks available without making every small change wait for unrelated coverage.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep slow, broad, or expensive checks available without making every small change wait for unrelated coverage.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C1D8AF64` — Nondeterministic

**Exact statement**

> Use test selection, caching, parallelism, incremental analysis, and prebuilt environments only when correctness and reproducibility remain trustworthy.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use test selection, caching, parallelism, incremental analysis, and prebuilt environments only when correctness and reproducibility remain trustworthy.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E29091F1` — Nondeterministic

**Exact statement**

> Surface failures with the exact failing step, actionable cause, relevant logs, ownership, and remediation guidance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fields can be populated without an exact causal diagnosis, actionable guidance, relevant evidence, or correct ownership. Therefore the proposed structured-record composite does not prove the whole rule “Surface failures with the exact failing step, actionable cause, relevant logs, ownership, and remediation guidance.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A message can name a step and owner while giving generic cause unknown and rerun advice. The proposal can Pass while “Surface failures with the exact failing step, actionable cause, relevant logs, ownership, and remediation guidance.” remains false.

## `USEQ-37226F1E` — Nondeterministic

**Exact statement**

> Ensure developers can reproduce material pipeline failures locally or in an accessible diagnostic environment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure developers can reproduce material pipeline failures locally or in an accessible diagnostic environment.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-CC581ACE` — Nondeterministic

**Exact statement**

> Prevent flaky infrastructure, intermittent tests, and overloaded shared environments from becoming normalized background noise.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent flaky infrastructure, intermittent tests, and overloaded shared environments from becoming normalized background noise.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BDB4D329` — Nondeterministic

**Exact statement**

> Prioritize repair of broken mainline builds and shared development infrastructure according to their organization-wide cost.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize repair of broken mainline builds and shared development infrastructure according to their organization-wide cost.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-36CB4809` — Nondeterministic

**Exact statement**

> Make code review assignment, status, required expertise, unresolved comments, and decision authority visible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Assignment/status/comment fields are exact, but required expertise, decision authority validity, and complete review state need human/organizational truth. Therefore the proposed structured-record composite does not prove the whole rule “Make code review assignment, status, required expertise, unresolved comments, and decision authority visible.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

A visible review can assign an unqualified approver or hide unresolved discussion in another system. The proposal can Pass while “Make code review assignment, status, required expertise, unresolved comments, and decision authority visible.” remains false.

## `USEQ-AB3A0B9D` — Nondeterministic

**Exact statement**

> Set review response expectations that preserve quality without creating long invisible queues.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Set review response expectations that preserve quality without creating long invisible queues.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-658ADA6B` — Nondeterministic

**Exact statement**

> Provide early production-like feedback for configuration, migrations, permissions, performance, accessibility, security, and observability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide early production-like feedback for configuration, migrations, permissions, performance, accessibility, security, and observability.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D040219F` — Nondeterministic

**Exact statement**

> Measure how often failures are detected at each lifecycle stage and move detection earlier when economically and technically justified.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure how often failures are detected at each lifecycle stage and move detection earlier when economically and technically justified.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0F9A3D1B` — Nondeterministic

**Exact statement**

> Ensure feedback remains available during provider, network, identity, or platform degradation through documented fallback paths where required.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure feedback remains available during provider, network, identity, or platform degradation through documented fallback paths where required.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-011FC4C6` — Nondeterministic

**Exact statement**

> Limit the number of unrelated systems, interfaces, credentials, concepts, and manual decisions required for common workflows.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Limit the number of unrelated systems, interfaces, credentials, concepts, and manual decisions required for common workflows.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E8B11D40` — Nondeterministic

**Exact statement**

> Provide one discoverable authoritative entry point for services, ownership, documentation, environments, deployment, support, and operational status.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One URL is countable, but discoverability, authority, completeness, and freshness across services/docs/environments/support/status require user and organizational judgment. Therefore the proposed structured-record composite does not prove the whole rule “Provide one discoverable authoritative entry point for services, ownership, documentation, environments, deployment, support, and operational status.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A portal can exist while developers cannot find it or it omits a critical service and stale status. The proposal can Pass while “Provide one discoverable authoritative entry point for services, ownership, documentation, environments, deployment, support, and operational status.” remains false.

## `USEQ-1211B978` — Nondeterministic

**Exact statement**

> Use consistent terminology, identity, navigation, commands, templates, and policy messages across internal capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use consistent terminology, identity, navigation, commands, templates, and policy messages across internal capabilities.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C7AADF90` — Nondeterministic

**Exact statement**

> Make the safe path easier than bypassing controls or assembling an unsupported custom path.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make the safe path easier than bypassing controls or assembling an unsupported custom path.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DBBC45BB` — Nondeterministic

**Exact statement**

> Reduce context switching by limiting unnecessary meetings, interruptions, simultaneous priorities, handoffs, and approval queues.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reduce context switching by limiting unnecessary meetings, interruptions, simultaneous priorities, handoffs, and approval queues.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B2FE9B81` — Nondeterministic

**Exact statement**

> Protect focused work while preserving explicit channels for incidents and genuinely urgent requests.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect focused work while preserving explicit channels for incidents and genuinely urgent requests.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5D046A65` — Nondeterministic

**Exact statement**

> Limit work in progress at team and portfolio levels and finish high-value work before starting additional low-priority work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Limit work in progress at team and portfolio levels and finish high-value work before starting additional low-priority work.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-64A222AA` — Nondeterministic

**Exact statement**

> Decompose work into small independently valuable batches without losing system-level design and integration thinking.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Decompose work into small independently valuable batches without losing system-level design and integration thinking.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8CC09A8B` — Nondeterministic

**Exact statement**

> Keep ownership boundaries clear enough that teams know where to change behavior and whom to involve.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep ownership boundaries clear enough that teams know where to change behavior and whom to involve.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-23A2452C` — Nondeterministic

**Exact statement**

> Avoid shifting platform, security, operations, or compliance complexity onto every application team through poorly designed self-service.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid shifting platform, security, operations, or compliance complexity onto every application team through poorly designed self-service.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A0FAB7FB` — Nondeterministic

**Exact statement**

> Provide explanations and recovery paths when automated policy blocks work; do not return opaque denial messages.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide explanations and recovery paths when automated policy blocks work; do not return opaque denial messages.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-390C0893` — Nondeterministic

**Exact statement**

> Design documentation and tools for recognition and guided choice rather than requiring memorization of volatile procedures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Design documentation and tools for recognition and guided choice rather than requiring memorization of volatile procedures.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-66FBAB61` — Nondeterministic

**Exact statement**

> Track cognitive load introduced by legacy systems, fragmented platforms, excessive customization, incompatible conventions, and organizational coupling.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Track cognitive load introduced by legacy systems, fragmented platforms, excessive customization, incompatible conventions, and organizational coupling.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D41144E6` — Nondeterministic

**Exact statement**

> Retire redundant tools and paths after migration rather than preserving every historical option indefinitely.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Retire redundant tools and paths after migration rather than preserving every historical option indefinitely.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C4A6187E` — Nondeterministic

**Exact statement**

> Define the minimum supported path from account creation to a verified useful change and measure it with new or reset users.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Minimum supported path, useful change, representative new/reset users, and meaningful measurement require user/product judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define the minimum supported path from account creation to a verified useful change and measure it with new or reset users.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A scripted trivial edit can meet timing while real newcomers cannot make a useful product change. The proposal can Pass while “Define the minimum supported path from account creation to a verified useful change and measure it with new or reset users.” remains false.

## `USEQ-262F3070` — Nondeterministic

**Exact statement**

> Automate account, repository, documentation, environment, test-data, secret, and deployment access according to role and least privilege.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Automation records do not prove complete role/resource coverage or least privilege for accounts, repos, docs, data, secrets, and deployment. Therefore the proposed structured-record composite does not prove the whole rule “Automate account, repository, documentation, environment, test-data, secret, and deployment access according to role and least privilege.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A role can auto-provision broad wildcard access while a shadow system remains manual. The proposal can Pass while “Automate account, repository, documentation, environment, test-data, secret, and deployment access according to role and least privilege.” remains false.

## `USEQ-D031FE99` — Nondeterministic

**Exact statement**

> Ensure onboarding does not depend on a particular person being available.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure onboarding does not depend on a particular person being available.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-72AF77C1` — Nondeterministic

**Exact statement**

> Provide a current architectural orientation, service map, glossary, development workflow, quality gates, support route, and first-task guide.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Document presence cannot prove current architecture, correct maps/glossary, usable workflow/gates/support, or effective first-task guidance. Therefore the proposed structured-record composite does not prove the whole rule “Provide a current architectural orientation, service map, glossary, development workflow, quality gates, support route, and first-task guide.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Every guide can exist while referencing retired services and failing for clean users. The proposal can Pass while “Provide a current architectural orientation, service map, glossary, development workflow, quality gates, support route, and first-task guide.” remains false.

## `USEQ-E3FF70A7` — Nondeterministic

**Exact statement**

> Keep development setup reproducible from documented declarations and validate it regularly from a clean state.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clean-state validation samples platforms, networks, tools, accounts, and time; it cannot prove reproducibility across all supported setups and mutable dependencies. Therefore the proposed structured-record composite does not prove the whole rule “Keep development setup reproducible from documented declarations and validate it regularly from a clean state.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

CI setup can pass while a new laptop, offline developer, or mutable package mirror fails. The proposal can Pass while “Keep development setup reproducible from documented declarations and validate it regularly from a clean state.” remains false.

## `USEQ-1EA7115D` — Nondeterministic

**Exact statement**

> Minimize the use of production data and production access in ordinary development.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Minimize the use of production data and production access in ordinary development.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-AC9B4FCF` — Nondeterministic

**Exact statement**

> Provide representative synthetic, generated, masked, or contract-based test data with known limitations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide representative synthetic, generated, masked, or contract-based test data with known limitations.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-CD7A2F8E` — Nondeterministic

**Exact statement**

> Support isolated and ephemeral environments when shared environments create collision, queueing, privacy, or reliability problems.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Support isolated and ephemeral environments when shared environments create collision, queueing, privacy, or reliability problems.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4A4087C7` — Nondeterministic

**Exact statement**

> Control environment drift and make differences from production explicit.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Known diffs are exact, but complete drift discovery and which differences matter relative to production are not. Therefore the proposed structured-record composite does not prove the whole rule “Control environment drift and make differences from production explicit.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Manifests can match while a provider-only setting drifts or a security boundary difference is labeled immaterial. The proposal can Pass while “Control environment drift and make differences from production explicit.” remains false.

## `USEQ-63ACFD44` — Nondeterministic

**Exact statement**

> Ensure local and remote development workflows meet security, accessibility, performance, and support requirements.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure local and remote development workflows meet security, accessibility, performance, and support requirements.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2C0ADD3B` — Nondeterministic

**Exact statement**

> Provide safe seed, reset, migration, and cleanup procedures for development and test state.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Procedure existence does not prove seed/reset/migration/cleanup safety, correctness, isolation, and recoverability across failure states. Therefore the proposed structured-record composite does not prove the whole rule “Provide safe seed, reset, migration, and cleanup procedures for development and test state.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A reset guide can work normally while timeout deletes another developer's data. The proposal can Pass while “Provide safe seed, reset, migration, and cleanup procedures for development and test state.” remains false.

## `USEQ-7E8B9D53` — Nondeterministic

**Exact statement**

> Make common debugging, tracing, profiling, and dependency simulation capabilities discoverable and permissioned.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make common debugging, tracing, profiling, and dependency simulation capabilities discoverable and permissioned.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-598AC30F` — Nondeterministic

**Exact statement**

> Document offline, low-bandwidth, travel, and device-replacement recovery where work continuity requires it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Work-continuity applicability and adequate offline/travel/device recovery require role, threat, security, and usability judgment. Therefore the proposed structured-record composite does not prove the whole rule “Document offline, low-bandwidth, travel, and device-replacement recovery where work continuity requires it.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A document can exist while missing credential recovery during travel or prescribing insecure copying. The proposal can Pass while “Document offline, low-bandwidth, travel, and device-replacement recovery where work continuity requires it.” remains false.

## `USEQ-DEFAB837` — Nondeterministic

**Exact statement**

> Revalidate onboarding after major platform, identity, repository, architecture, or organizational changes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Revalidate onboarding after major platform, identity, repository, architecture, or organizational changes.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-37A8A1E2` — Nondeterministic

**Exact statement**

> Treat the internal platform as a product with named users, product management, service ownership, roadmap, support, research, adoption goals, and lifecycle funding.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Treat the internal platform as a product with named users, product management, service ownership, roadmap, support, research, adoption goals, and lifecycle funding.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5BFBA215` — Nondeterministic

**Exact statement**

> Build platform capabilities from observed user workflows and organization-specific constraints rather than copying a reference stack blindly.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Build platform capabilities from observed user workflows and organization-specific constraints rather than copying a reference stack blindly.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C5BF78A7` — Nondeterministic

**Exact statement**

> Deliver small usable capabilities early and evolve through feedback instead of attempting a complete big-bang platform.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Deliver small usable capabilities early and evolve through feedback instead of attempting a complete big-bang platform.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0E7CE9E5` — Nondeterministic

**Exact statement**

> Offer self-service for frequent, well-understood, automatable operations while retaining human review where consequence requires it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Offer self-service for frequent, well-understood, automatable operations while retaining human review where consequence requires it.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F2F34933` — Nondeterministic

**Exact statement**

> Provide paved or golden paths that integrate security, privacy, observability, reliability, cost, documentation, and compliance by default.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide paved or golden paths that integrate security, privacy, observability, reliability, cost, documentation, and compliance by default.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F2B89A92` — Nondeterministic

**Exact statement**

> Allow justified escape paths and extensions without letting every team create an ungoverned parallel platform.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Allow justified escape paths and extensions without letting every team create an ungoverned parallel platform.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-52F5768B` — Nondeterministic

**Exact statement**

> Define platform personas, supported use cases, service levels, prerequisites, quotas, costs, responsibilities, and exit paths.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Persona/use-case/service-level fields are structured, but completeness, supportability, cost truth, responsibility, and usable exit paths require product governance. Therefore the proposed structured-record composite does not prove the whole rule “Define platform personas, supported use cases, service levels, prerequisites, quotas, costs, responsibilities, and exit paths.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Every section can exist while an important persona is omitted and exit is practically impossible. The proposal can Pass while “Define platform personas, supported use cases, service levels, prerequisites, quotas, costs, responsibilities, and exit paths.” remains false.

## `USEQ-57EB708C` — Nondeterministic

**Exact statement**

> Use stable, versioned platform interfaces and avoid forcing consumers to depend on internal implementation details.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Interface versioning is exact, but stability over time and whether consumers depend on internal details require complete usage and semantic compatibility scope. Therefore the proposed structured-record composite does not prove the whole rule “Use stable, versioned platform interfaces and avoid forcing consumers to depend on internal implementation details.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A versioned API can pass while docs require consumers to parse an internal file or a minor update breaks behavior. The proposal can Pass while “Use stable, versioned platform interfaces and avoid forcing consumers to depend on internal implementation details.” remains false.

## `USEQ-77692B0A` — Nondeterministic

**Exact statement**

> Provide a service and software catalog with ownership, criticality, dependencies, runbooks, lifecycle, data classification, and operational health.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Catalog fields are exact, but complete service/software scope, ownership truth, criticality, dependencies, classification, and health are not. Therefore the proposed structured-record composite does not prove the whole rule “Provide a service and software catalog with ownership, criticality, dependencies, runbooks, lifecycle, data classification, and operational health.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known services can have entries while a shadow service is omitted or health is incorrectly green. The proposal can Pass while “Provide a service and software catalog with ownership, criticality, dependencies, runbooks, lifecycle, data classification, and operational health.” remains false.

## `USEQ-8E00CB58` — Nondeterministic

**Exact statement**

> Provide reusable templates only when their generated outputs remain understandable, owned, upgradeable, and removable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide reusable templates only when their generated outputs remain understandable, owned, upgradeable, and removable.” is not deterministic because its full meaning requires risk, organizational, workflow, usability, cognitive-load, platform-fit, or developer-outcome judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8AF5B8F4` — Nondeterministic

**Exact statement**

> Make platform policies testable before deployment and explain violations in user terms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A policy test endpoint does not prove all policies are testable before deploy, and user-understandable explanations require semantic/usability judgment. Therefore the proposed structured-record composite does not prove the whole rule “Make platform policies testable before deployment and explain violations in user terms.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A sample rule can evaluate while another depends on runtime state and violation text exposes internal jargon. The proposal can Pass while “Make platform policies testable before deployment and explain violations in user terms.” remains false.

## `USEQ-E5D8E500` — Nondeterministic

**Exact statement**

> Minimize ticket-based provisioning for routine capabilities and measure ticket demand as evidence of missing self-service or unclear ownership.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Minimization has no threshold, and ticket demand does not uniquely prove missing self-service or unclear ownership without complete routing and causal analysis. Therefore the proposed structured-record composite does not prove the whole rule “Minimize ticket-based provisioning for routine capabilities and measure ticket demand as evidence of missing self-service or unclear ownership.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Ticket counts can fall because users abandon requests, or rise from an outage despite good self-service. The proposal can Pass while “Minimize ticket-based provisioning for routine capabilities and measure ticket demand as evidence of missing self-service or unclear ownership.” remains false.

## `USEQ-AC94BEB4` — Nondeterministic

**Exact statement**

> Prevent platform adoption metrics from rewarding forced use while ignoring task success, satisfaction, reliability, or downstream outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent platform adoption metrics from rewarding forced use while ignoring task success, satisfaction, reliability, or downstream outcomes.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8B054594` — Nondeterministic

**Exact statement**

> Provide migration support and coexistence periods when changing platform interfaces or golden paths.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Adequate migration support and coexistence depend on complete consumer needs, compatibility behavior and contextual support duration.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Documentation and a dual-version window can exist while an offline consumer cannot migrate and the coexistence period ends too early.

## `USEQ-707DB5CA` — Nondeterministic

**Exact statement**

> Retire deprecated paths only after consumer inventory, communication, migration evidence, and recovery planning are complete.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Completeness of consumer inventory and migration evidence cannot be proved by their records, and recovery planning requires operational judgment.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All registered consumers can show migration while an undocumented automation client still uses the deprecated path when it is retired.

## `USEQ-D23E6AE9` — Nondeterministic

**Exact statement**

> Embed least privilege, strong identity, secret handling, provenance, policy checks, logging, and approved network patterns into default platform paths.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Seven default-control labels do not prove least privilege, secure secret use or approved behavior across every generated platform path.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

A template can contain the expected settings while a generated role is overprivileged or an alternate network path bypasses policy checks.

## `USEQ-DA324337` — Nondeterministic

**Exact statement**

> Separate platform control-plane privileges from ordinary application-team privileges.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A role matrix cannot prove control-plane privileges are separated from ordinary teams across every API, emergency path and inherited identity.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Declared roles can be distinct while a shared service account lets an application team perform control-plane changes.

## `USEQ-79C11C18` — Nondeterministic

**Exact statement**

> Make high-impact platform actions attributable, reviewable, time-bounded, and auditable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Which actions are high impact and whether they are truly reviewable and auditable require complete action-path coverage beyond record fields.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All registered high-impact actions can have actor and expiry fields while an undocumented admin endpoint remains unattributed.

## `USEQ-21D7885D` — Nondeterministic

**Exact statement**

> Prevent templates and self-service workflows from creating public exposure, excessive privilege, unbounded cost, unsupported versions, or missing ownership by default.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing five harmful defaults across arbitrary template parameters and generated resources is a security, cost and lifecycle outcome claim.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Standard template tests can pass while a rare parameter combination creates a public resource or unbounded-cost workload.

## `USEQ-F9F2BEED` — Nondeterministic

**Exact statement**

> Validate user-supplied parameters before provisioning and bound resource, region, data, network, and identity choices.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Validation and safe bounds require a complete parameter inventory plus project-specific resource, region, data, network and identity policies.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known fields can validate while an extension parameter bypasses region restrictions or a configured bound is unsafe for sensitive data.

## `USEQ-2E333332` — Nondeterministic

**Exact statement**

> Use policy as code where reliable, but retain an accountable exception and appeal process.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use policy as code where reliable, but retain an accountable exception and appeal process.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-85AD04E2` — Nondeterministic

**Exact statement**

> Test policy behavior against intended allow, deny, exception, rollback, and degraded cases.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Five policy-case tests do not prove full rule, parameter, exception, rollback and degraded-path coverage or correct authorization oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All named fixtures can pass while an untested exception combination permits a denied production action.

## `USEQ-6E35D785` — Nondeterministic

**Exact statement**

> Ensure platform automation cannot access unrelated tenant, team, environment, repository, or production secrets.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of access to unrelated tenants, teams, environments, repositories and secrets is universal over every automation identity and path.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured IAM tests can pass while a inherited token or control-plane API exposes another team's production secret.

## `USEQ-8BC1E850` — Nondeterministic

**Exact statement**

> Inventory nonhuman users, automation agents, CI identities, service accounts, and AI agents as first-class platform consumers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The platform consumer register cannot prove it contains every nonhuman user, automation, CI identity, service account and AI agent.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All known identities can be present while a vendor bot or dynamically created service account remains unrecorded.

## `USEQ-94C7E270` — Nondeterministic

**Exact statement**

> Provide scoped temporary credentials rather than distributing long-lived shared credentials.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Temporary scoped credential issuance can exist while long-lived shared credentials remain distributed through an unobserved workflow or legacy system.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The approved path passes TTL and scope checks but an old shared deployment key still grants the same platform access.

## `USEQ-F5A77AE2` — Nondeterministic

**Exact statement**

> Make compliance evidence an output of normal workflows rather than a separate manual reconstruction where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make compliance evidence an output of normal workflows rather than a separate manual reconstruction where practical.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-23AEDF92` — Deterministic

**Exact statement**

> Keep platform-generated configurations, artifacts, attestations, and audit records traceable to request, policy, source, and approver.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each platform-generated configuration, artifact, attestation and audit record can be content-addressed and linked to exact request, policy, source and approver identities.

### Exact deterministic check

1. Keep platform-generated configurations, artifacts, attestations, and audit records traceable to request, policy, source, and approver.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Any generated item lacking one provenance edge or carrying a mismatched policy digest fails the finite lineage graph.

## `USEQ-3712C085` — Nondeterministic

**Exact statement**

> Threat-model the platform as a high-leverage dependency and test compromise, abuse, supply-chain, and insider scenarios.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Threat-model the platform as a high-leverage dependency and test compromise, abuse, supply-chain, and insider scenarios.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C5BBCBD4` — Nondeterministic

**Exact statement**

> Maintain break-glass recovery that works when normal identity, documentation, or platform control planes are unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Break-glass usability during simultaneous identity, documentation and control-plane loss is an empirical resilience property, not a procedure record.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A documented emergency path can pass normal drills while depending indirectly on the failed identity provider.

## `USEQ-104A029B` — Deterministic

**Exact statement**

> Define user-facing SLIs and SLOs for critical platform workflows such as environment creation, build, test, artifact publication, deployment, rollback, and secret delivery.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A versioned service-level policy can define user-facing SLI and SLO records for the seven finite critical workflow classes.

### Exact deterministic check

1. Define user-facing SLIs and SLOs for critical platform workflows such as environment creation, build, test, artifact publication, deployment, rollback, and secret delivery.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

If rollback or secret delivery lacks a user-perspective indicator or objective, the exact seven-workflow join fails.

## `USEQ-105A2D16` — Nondeterministic

**Exact statement**

> Monitor task success from the consumer perspective, not only platform component uptime.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Consumer-perspective success requires defining complete user tasks and meaningful outcomes; component-uptime plus one synthetic check can falsely pass.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

The synthetic environment-creation task can succeed while real consumers consistently fail at a later permission step.

## `USEQ-1B99F055` — Deterministic

**Exact statement**

> Provide status, incident communication, ownership, support, escalation, and postmortem processes for internal platform services.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each registered internal platform service can be required to publish six exact process references for status, incidents, ownership, support, escalation and postmortems.

### Exact deterministic check

1. Provide status, incident communication, ownership, support, escalation, and postmortem processes for internal platform services.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A service without an escalation path or postmortem process fails the finite service-process schema.

## `USEQ-E2721BE2` — Nondeterministic

**Exact statement**

> Design the platform so failure of an optional capability does not block unrelated delivery unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Design the platform so failure of an optional capability does not block unrelated delivery unnecessarily.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-859CA94E` — Nondeterministic

**Exact statement**

> Test queue saturation, provider throttling, control-plane outage, identity failure, registry failure, network partition, and corrupt template scenarios.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Seven scenario executions do not establish complete platforms, workloads, combinations, timing paths or correctness and recovery oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Each named fault can pass alone while throttling during an identity outage deadlocks the queue.

## `USEQ-6FE68803` — Nondeterministic

**Exact statement**

> Keep a supported manual or alternate path for genuinely critical operations when the primary platform is unavailable, and test it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Genuinely critical and supported are contextual classifications, while a passing alternate-path test does not prove operation during real platform loss.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

An emergency command can work in staging yet require a production credential issued by the unavailable primary platform.

## `USEQ-99B27008` — Nondeterministic

**Exact statement**

> Prevent a platform outage from removing access to the runbooks, credentials, artifacts, or communication needed for recovery.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Guaranteeing outage-time access to runbooks, credentials, artifacts and communication requires exhaustive dependency and correlated-failure coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All four items can have backups while every backup depends on the same unavailable identity or network plane.

## `USEQ-85373024` — Nondeterministic

**Exact statement**

> Apply capacity planning, quotas, fairness, backpressure, and cost controls to shared capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Apply capacity planning, quotas, fairness, backpressure, and cost controls to shared capabilities.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D8F0B0C5` — Deterministic

**Exact statement**

> Publish maintenance windows, deprecation schedules, breaking changes, and reliability limitations.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Publish is an effective availability promise. The exact check must read the declared publication surfaces and compare their current topic/version inventory.

### Exact deterministic check

1. Every publication surface in the sealed platform-service audience inventory exposes the current approved maintenance windows, deprecation schedules, breaking changes, and reliability limitations at the declared publication endpoint.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A publication registry can say all four topics were published while the audience endpoint omits one or serves a stale version. The environment query fails.

## `USEQ-23DC86B8` — Nondeterministic

**Exact statement**

> Keep rollback and version compatibility for platform interfaces, templates, policies, and generated assets.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Rollback and version compatibility across four interface and asset classes are runtime properties over incomplete versions, consumers and state transitions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known combinations can pass while an older generated asset cannot be rolled back after a policy migration.

## `USEQ-A96AE686` — Nondeterministic

**Exact statement**

> Measure adoption alongside failure rate, support burden, lead time, stability, user satisfaction, and cost.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** User satisfaction, stability, support burden, adoption, lead time, failure rate, and cost require different populations, windows, attribution rules, instruments, and normalization choices that are not fixed by the rule. A named metric record is not a complete measurement oracle.
- **Nondeterministic reason codes:** `undefined_protocol`

### Strength-audit rejection counterexample

A platform report can provide seven numeric fields while measuring satisfaction only among successful users, excluding support handled outside one queue, or attributing shared cost arbitrarily. Schema and calculation checks pass although the promised outcome measurement is misleading or incomplete.

## `USEQ-C326D283` — Nondeterministic

**Exact statement**

> Review whether platform centralization creates unacceptable correlated failure or organizational bottlenecks.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether platform centralization creates unacceptable correlated failure or organizational bottlenecks.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A84EEA72` — Nondeterministic

**Exact statement**

> Use the current DORA delivery metrics as team or service-level outcome signals: change lead time, deployment frequency, failed deployment recovery time, change fail rate, and deployment rework rate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** The control requires the five measurements to be used as team or service-level outcome signals, not merely present in telemetry. Whether a team actually uses them for decisions is contextual, and the meaning of current DORA metrics is maintained by an external body rather than fixed by this versioned control. Exact event calculations therefore prove only part of the original promise.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Strength-audit rejection counterexample

A service can emit correctly calculated values for all five named measurements while its team ignores the dashboard and bases delivery decisions on unrelated targets. Every deterministic event, unit, window, and formula check passes, but the measurements are not used as outcome signals, so the complete control remains false.

## `USEQ-CAC06908` — Deterministic

**Exact statement**

> Define events and denominators consistently before comparing delivery metrics over time.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Delivery events and denominators can be versioned with effective periods so comparisons require the same definitions or explicit normalization.

### Exact deterministic check

1. Define events and denominators consistently before comparing delivery metrics over time.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A trend crossing a silent denominator change fails the metric-version compatibility check.

## `USEQ-401CFDDA` — Nondeterministic

**Exact statement**

> Measure metrics at a level where one flow of work and one operating context make interpretation meaningful.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure metrics at a level where one flow of work and one operating context make interpretation meaningful.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-641B8692` — Nondeterministic

**Exact statement**

> Do not use delivery metrics to rank individual engineers or teams with materially different contexts.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not use delivery metrics to rank individual engineers or teams with materially different contexts.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-EF22FA21` — Nondeterministic

**Exact statement**

> Pair throughput metrics with stability, quality, security, privacy, accessibility, user outcome, well-being, and sustainability measures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Pair throughput metrics with stability, quality, security, privacy, accessibility, user outcome, well-being, and sustainability measures.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6B03300D` — Nondeterministic

**Exact statement**

> Use SPACE dimensions—satisfaction and well-being, performance, activity, communication and collaboration, and efficiency and flow—as a multidimensional lens rather than one composite score.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use SPACE dimensions—satisfaction and well-being, performance, activity, communication and collaboration, and efficiency and flow—as a multidimensional lens rather than one composite score.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D038E87B` — Nondeterministic

**Exact statement**

> Use DevEx dimensions—feedback loops, cognitive load, and flow state—to identify causes rather than merely reporting sentiment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use DevEx dimensions—feedback loops, cognitive load, and flow state—to identify causes rather than merely reporting sentiment.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-51FAFA2F` — Nondeterministic

**Exact statement**

> Protect survey confidentiality and avoid collecting more employee data than needed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect survey confidentiality and avoid collecting more employee data than needed.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4BA2DDFB` — Nondeterministic

**Exact statement**

> Publish metric definitions, data sources, exclusions, known biases, confidence, and intended decisions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** Publishing named metric metadata is checkable, but proving that all known biases were disclosed requires unbounded knowledge of what people know and judgment about what counts as a bias.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Strength-audit rejection counterexample

The metric page can contain definitions, sources, exclusions, confidence, and one declared bias while an analyst knows of a material sampling bias that is omitted; the bounded record passes while the original promise fails.

## `USEQ-23DD5632` — Nondeterministic

**Exact statement**

> Audit telemetry for gaming, selection bias, missing work, bot activity, reclassification, and changed process semantics.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Audit occurrence across six bias labels does not prove gaming, missing work or changed process semantics were correctly detected.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All checklist sections can be complete while teams reclassify failed deployments in a way the audit does not recognize.

## `USEQ-AA2239E3` — Nondeterministic

**Exact statement**

> Prefer trends and distributions to isolated averages and vanity totals.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preference, vanity and appropriate use of trends or distributions are interpretive reporting choices rather than objective record states.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A report can include a distribution yet emphasize a misleading isolated average as the decision signal.

## `USEQ-98265229` — Nondeterministic

**Exact statement**

> Use qualitative inquiry to explain metric changes before acting on them.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use qualitative inquiry to explain metric changes before acting on them.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-67683E93` — Nondeterministic

**Exact statement**

> Retire metrics that no longer drive decisions or that create harmful incentives.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Retire metrics that no longer drive decisions or that create harmful incentives.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-078B9EDE` — Deterministic

**Exact statement**

> Evaluate interventions with a baseline, expected mechanism, counter-metrics, observation period, and rollback criteria.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An intervention record can require the five exact predeclared elements: baseline, mechanism, counter-metrics, observation period and rollback criteria.

### Exact deterministic check

1. Evaluate interventions with a baseline, expected mechanism, counter-metrics, observation period, and rollback criteria.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

An intervention launched without a baseline or with rollback criteria added afterward fails the finite schema and timestamp rule.

## `USEQ-FD4E143E` — Nondeterministic

**Exact statement**

> State the decision, alternatives, objectives, constraints, affected stakeholders, time horizon, and decision owner for every material investment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** The named decision fields are objective, but determining the complete set of affected stakeholders requires contextual impact judgment and an unbounded stakeholder universe.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Strength-audit rejection counterexample

An investment record can populate every required field and list several stakeholders while omitting a materially affected group not present in any sealed inventory; the record passes while the original promise fails.

## `USEQ-2A4B44EE` — Nondeterministic

**Exact statement**

> Compare continuing, improving, replacing, buying, building, outsourcing, partnering, open sourcing, delaying, reducing scope, and stopping where plausible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Compare continuing, improving, replacing, buying, building, outsourcing, partnering, open sourcing, delaying, reducing scope, and stopping where plausible.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-01C9C8B4` — Nondeterministic

**Exact statement**

> Estimate full lifecycle cost including discovery, acquisition, implementation, integration, migration, data conversion, security, privacy, accessibility, testing, operations, support, training, compliance, vendor management, downtime, exit, and retirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Estimate full lifecycle cost including discovery, acquisition, implementation, integration, migration, data conversion, security, privacy, accessibility, testing, operations, support, training, compliance, vendor management, downtime, exit, and retirement.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FD955F18` — Nondeterministic

**Exact statement**

> Include opportunity cost and the cost of delayed value, not only direct expenditure.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Include opportunity cost and the cost of delayed value, not only direct expenditure.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-01C3D941` — Nondeterministic

**Exact statement**

> Identify benefits as measurable changes in user outcomes, revenue, risk, cost, capacity, quality, or strategic option value.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Identify benefits as measurable changes in user outcomes, revenue, risk, cost, capacity, quality, or strategic option value.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-606B2B76` — Nondeterministic

**Exact statement**

> Avoid claiming time savings as cash savings unless the released capacity can be used or removed in a defined way.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid claiming time savings as cash savings unless the released capacity can be used or removed in a defined way.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5906325E` — Nondeterministic

**Exact statement**

> Model ranges and scenarios rather than one unjustifiably precise point estimate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Whether a point estimate is unjustifiably precise and which ranges and scenarios are decision-relevant require estimation judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A model can show a narrow range and two nominal scenarios while hiding the major supplier-risk uncertainty.

## `USEQ-B7F42705` — Nondeterministic

**Exact statement**

> Discount future costs and benefits consistently when the decision horizon makes present-value analysis material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Materiality of present-value analysis and the correct discount treatment require economic, horizon and policy judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A consistent discount formula can pass while using an inappropriate rate or omitting a material long-term benefit.

## `USEQ-D09828A7` — Nondeterministic

**Exact statement**

> Separate sunk costs from future decision-relevant costs.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Correctly distinguishing sunk from future decision-relevant costs requires economic interpretation of obligations and alternatives.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

Every cost row can have a category while a cancelable future commitment is incorrectly labeled sunk.

## `USEQ-402B58A8` — Nondeterministic

**Exact statement**

> Identify irreversible commitments, lock-in, switching cost, and the value of preserving options.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete identification of irreversible commitments, lock-in and switching costs plus valuation of options requires open-ended commercial judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All four sections can be populated while a proprietary data-format lock-in or valuable delay option is omitted.

## `USEQ-8B570320` — Nondeterministic

**Exact statement**

> Include expected loss from reliability, security, privacy, safety, legal, supplier, schedule, and adoption risks.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Expected loss across eight risk domains requires uncertain probability, consequence and legal estimates from specialist evidence.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

All eight values can be present while privacy or supplier loss is severely understated under an invalid model.

## `USEQ-BF1DBF5E` — Nondeterministic

**Exact statement**

> Use sensitivity analysis to identify assumptions that dominate the decision.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Sensitivity execution is bounded, but choosing the assumption space and interpreting which assumptions dominate the real decision is contextual.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A model can show one dominant parameter while omitting the uncertainty that actually changes the preferred option.

## `USEQ-FB93CC82` — Deterministic

**Exact statement**

> Define leading indicators and stop, pivot, expand, and review thresholds before committing the full investment.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An investment gate can require defined leading indicators and exact stop, pivot, expand and review thresholds before full commitment.

### Exact deterministic check

1. Define leading indicators and stop, pivot, expand, and review thresholds before committing the full investment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A threshold missing or timestamped after commitment fails the finite precommitment policy record.

## `USEQ-CEE325A1` — Nondeterministic

**Exact statement**

> Update the business case with actual cost, benefit, adoption, quality, and risk evidence after delivery.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Links to five actual evidence types do not prove all costs, benefits, adoption, quality and risks are correct or complete after delivery.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

The case can update each section while omitting indirect support cost and an emerging security risk.

## `USEQ-8A77D825` — Deterministic

**Exact statement**

> Record who accepted uncertainty and residual risk rather than hiding them in optimistic estimates.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every uncertainty and residual-risk entry can require the accepting identity, authority, timestamp and explicit acceptance event.

### Exact deterministic check

1. Record who accepted uncertainty and residual risk rather than hiding them in optimistic estimates.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A risk hidden only inside an estimate or lacking an accountable acceptor fails the ledger join.

## `USEQ-B9C50500` — Deterministic

**Exact statement**

> Define the estimate's purpose, scope, decision date, confidence needs, currency, price basis, and included lifecycle stages.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An estimate can be checked for the eight exact purpose, scope, date, confidence, currency, price-basis and lifecycle-stage fields.

### Exact deterministic check

1. Define the estimate's purpose, scope, decision date, confidence needs, currency, price basis, and included lifecycle stages.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing confidence needs or lifecycle stages creates a deterministic estimate-schema failure.

## `USEQ-9AEFE830` — Nondeterministic

**Exact statement**

> Create a technical and operational baseline describing intended capability, architecture, users, scale, data, integrations, quality targets, schedule, and constraints.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Create a technical and operational baseline describing intended capability, architecture, users, scale, data, integrations, quality targets, schedule, and constraints.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-73709F91` — Nondeterministic

**Exact statement**

> Use a work breakdown that covers all material work and avoids double counting.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use a work breakdown that covers all material work and avoids double counting.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A898AA51` — Nondeterministic

**Exact statement**

> Document ground rules, assumptions, exclusions, dependencies, productivity factors, staffing model, inflation, exchange rates, and contingency treatment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Document ground rules, assumptions, exclusions, dependencies, productivity factors, staffing model, inflation, exchange rates, and contingency treatment.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F4AA620F` — Nondeterministic

**Exact statement**

> Use relevant historical data and normalize it for size, complexity, technology, team maturity, quality obligations, and economic conditions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use relevant historical data and normalize it for size, complexity, technology, team maturity, quality obligations, and economic conditions.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8596CFFB` — Nondeterministic

**Exact statement**

> Select estimation methods appropriate to maturity and triangulate high-impact estimates using more than one method where possible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Select estimation methods appropriate to maturity and triangulate high-impact estimates using more than one method where possible.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BFF44BBF` — Nondeterministic

**Exact statement**

> Separate estimate uncertainty from management reserve and from intentionally deferred scope.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Separate estimate uncertainty from management reserve and from intentionally deferred scope.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A67CAF6F` — Nondeterministic

**Exact statement**

> Model correlation among risks rather than summing independent optimistic contingencies blindly.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Model correlation among risks rather than summing independent optimistic contingencies blindly.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8DBEC4BA` — Nondeterministic

**Exact statement**

> Use probabilistic ranges for uncertain cost and schedule where consequence warrants it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use probabilistic ranges for uncertain cost and schedule where consequence warrants it.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7C70CDE8` — Nondeterministic

**Exact statement**

> Perform sensitivity and risk analysis and identify the variables that most affect outcome.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Perform sensitivity and risk analysis and identify the variables that most affect outcome.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B74D3C1D` — Nondeterministic

**Exact statement**

> Reconcile estimates with available capacity, hiring lead time, procurement time, and critical dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reconcile estimates with available capacity, hiring lead time, procurement time, and critical dependencies.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5E3A5C8B` — Nondeterministic

**Exact statement**

> Have material estimates independently reviewed for scope completeness, assumptions, data quality, method, arithmetic, bias, and credibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Have material estimates independently reviewed for scope completeness, assumptions, data quality, method, arithmetic, bias, and credibility.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-DD3C0E57` — Nondeterministic

**Exact statement**

> Document estimates so another qualified person can reproduce the logic and inputs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Actual reproducibility and reviewer qualification require human competence plus an executable, complete calculation model, not documentation presence.
- **Nondeterministic reason codes:** `human_accountability`, `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

A seemingly detailed estimate can omit an implicit spreadsheet adjustment so another qualified estimator cannot reproduce it.

## `USEQ-5E58EFDA` — Nondeterministic

**Exact statement**

> Compare actuals with estimates at consistent milestones and explain variance by cause.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Milestone alignment and numeric variance are bounded, but explaining variance by its true cause requires causal business and delivery judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

The report can compare every milestone and assign causes while blaming staffing rather than the actual supplier delay.

## `USEQ-0B9A4977` — Nondeterministic

**Exact statement**

> Re-estimate after material scope, architecture, supplier, staffing, risk, quality, or schedule changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Materiality and complete detection of seven change types are contextual and unbounded, so re-estimation triggers can miss real changes.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Configured changes can trigger estimates while an indirect architecture change is missed or a quality change is deemed immaterial.

## `USEQ-E0A45D6A` — Deterministic

**Exact statement**

> Do not convert an early exploratory estimate into a commitment without increasing evidence and explicitly changing its confidence classification.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An estimate state machine can forbid commitment from exploratory status unless new evidence is linked and confidence classification explicitly changes.

### Exact deterministic check

1. Do not convert an early exploratory estimate into a commitment without increasing evidence and explicitly changing its confidence classification.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A direct exploratory-to-committed transition with unchanged confidence or no new evidence fails the exact gate.

## `USEQ-961773F5` — Nondeterministic

**Exact statement**

> Represent all material work, milestones, external dependencies, approvals, migrations, training, operational readiness, and retirement activities in the delivery model.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** All material work and external dependencies cannot be proven complete from the delivery model, and materiality is contextual.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Every named category can have tasks while an approval, migration or retirement activity remains undiscovered.

## `USEQ-884A77F0` — Deterministic

**Exact statement**

> Define task logic and dependencies rather than choosing dates independently.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A delivery model can require explicit dependency edges and task-logic fields and prohibit independent free-standing date commitments.

### Exact deterministic check

1. Define task logic and dependencies rather than choosing dates independently.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A task date with no duration, predecessor or scheduling rule fails the finite graph schema.

## `USEQ-AB5BD18C` — Nondeterministic

**Exact statement**

> Identify the critical and near-critical paths and monitor changes to them.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The critical path is computable, but near-critical lacks a stated threshold and complete task/dependency discovery is unbounded.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`, `unbounded_scope`

### Skeptical rejection counterexample

The schedule can monitor computed paths while an omitted supplier dependency is actually critical or the near-critical cutoff is arbitrary.

## `USEQ-3682C3C5` — Deterministic

**Exact statement**

> Model resource availability, specialized skills, leave, support obligations, review capacity, environments, and supplier lead times.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A delivery resource model can require the seven exact availability, skill, leave, support, review, environment and supplier-lead-time dimensions.

### Exact deterministic check

1. Model resource availability, specialized skills, leave, support obligations, review capacity, environments, and supplier lead times.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A schedule model without review capacity or supplier lead times fails the finite model schema; forecast accuracy is not inferred.

## `USEQ-1B383C61` — Nondeterministic

**Exact statement**

> Avoid planning people at full theoretical utilization; preserve capacity for coordination, incidents, learning, and uncertainty.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid planning people at full theoretical utilization; preserve capacity for coordination, incidents, learning, and uncertainty.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4C01CA36` — Nondeterministic

**Exact statement**

> Use ranges or probabilistic forecasts when task duration and scope are uncertain.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use ranges or probabilistic forecasts when task duration and scope are uncertain.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-82AC181E` — Deterministic

**Exact statement**

> Track blocked time, queue time, rework, dependency age, and decision latency.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A work-flow ledger can compute the five exact blocked, queue, rework, dependency-age and decision-latency durations from state timestamps.

### Exact deterministic check

1. Track blocked time, queue time, rework, dependency age, and decision latency.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Missing transition time or one required measure fails the finite metric calculation.

## `USEQ-163CD3CD` — Deterministic

**Exact statement**

> Keep milestones tied to verifiable outcomes and evidence, not percentage-complete estimates.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Milestone completion can require linked verifiable outcome and evidence identities and reject percentage-complete as the sole criterion.

### Exact deterministic check

1. Keep milestones tied to verifiable outcomes and evidence, not percentage-complete estimates.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A milestone marked complete at ninety percent with no outcome evidence fails the state-transition guard.

## `USEQ-EEBE0AC0` — Deterministic

**Exact statement**

> Reforecast using actual throughput and discovered work rather than preserving obsolete dates.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A reforecast can be bound to observed throughput and discovered-work snapshots and supersede rather than preserve the prior obsolete date.

### Exact deterministic check

1. Reforecast using actual throughput and discovered work rather than preserving obsolete dates.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A forecast copied from the old date without current input identities fails provenance and supersession checks.

## `USEQ-65304AB1` — Nondeterministic

**Exact statement**

> Make scope, time, cost, quality, and risk trade-offs explicit when constraints conflict.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make scope, time, cost, quality, and risk trade-offs explicit when constraints conflict.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D1A09FF0` — Nondeterministic

**Exact statement**

> Prevent schedule pressure from silently removing testing, accessibility, security, documentation, recovery, or maintenance work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent schedule pressure from silently removing testing, accessibility, security, documentation, recovery, or maintenance work.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-57586986` — Nondeterministic

**Exact statement**

> Define release, migration, rollback, stabilization, and support windows with adequate observation time.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Adequate observation time is contextual and depends on risk, traffic cycles and failure latency despite five window fields being present.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All five windows can be defined while stabilization ends before a monthly workload or delayed migration failure appears.

## `USEQ-2E6ADA23` — Nondeterministic

**Exact statement**

> Escalate dependencies before they reach the critical path rather than after they miss a date.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Escalate dependencies before they reach the critical path rather than after they miss a date.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E630B3A9` — Deterministic

**Exact statement**

> Maintain a decision log explaining changes to committed scope and schedule.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every committed scope or schedule change can require an append-only decision-log entry with reason, actor, time and affected baseline.

### Exact deterministic check

1. Maintain a decision log explaining changes to committed scope and schedule.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A changed committed date without a linked decision event fails the baseline-diff join.

## `USEQ-0070EAA7` — Nondeterministic

**Exact statement**

> Prioritize work using expected user and business value, strategic fit, urgency, risk reduction, learning value, dependency enablement, lifecycle cost, and capacity—not stakeholder volume alone.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize work using expected user and business value, strategic fit, urgency, risk reduction, learning value, dependency enablement, lifecycle cost, and capacity—not stakeholder volume alone.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-98ACEC61` — Nondeterministic

**Exact statement**

> Reserve capacity for reliability, security, privacy, accessibility, platform health, maintenance, debt, and retirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reserve capacity for reliability, security, privacy, accessibility, platform health, maintenance, debt, and retirement.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B54B7203` — Nondeterministic

**Exact statement**

> Evaluate portfolio concentration by product, provider, technology, region, revenue source, data class, and key person.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Evaluate portfolio concentration by product, provider, technology, region, revenue source, data class, and key person.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E49765AE` — Nondeterministic

**Exact statement**

> Limit simultaneous initiatives to the organization's actual integration and change capacity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Limit simultaneous initiatives to the organization's actual integration and change capacity.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0D1C652B` — Nondeterministic

**Exact statement**

> Fund discovery and risk retirement before making irreversible large commitments.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Fund discovery and risk retirement before making irreversible large commitments.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3D020109` — Nondeterministic

**Exact statement**

> Use staged investment with explicit evidence gates for uncertain initiatives.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use staged investment with explicit evidence gates for uncertain initiatives.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5C653F02` — Nondeterministic

**Exact statement**

> Define success, failure, stop, pivot, and sunset criteria before launch.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define success, failure, stop, pivot, and sunset criteria before launch.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BBF30CFF` — Nondeterministic

**Exact statement**

> Stop or reduce work when evidence no longer supports the expected outcome rather than protecting sunk cost.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Stop or reduce work when evidence no longer supports the expected outcome rather than protecting sunk cost.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-96ECBC49` — Nondeterministic

**Exact statement**

> Review whether a local optimization shifts greater cost or risk to customers, support, operations, security, another team, or the future.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether a local optimization shifts greater cost or risk to customers, support, operations, security, another team, or the future.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0ACF35D3` — Nondeterministic

**Exact statement**

> Include decommissioning and benefit-realization work in portfolio capacity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Include decommissioning and benefit-realization work in portfolio capacity.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-82D9539C` — Nondeterministic

**Exact statement**

> Make priority decisions and trade-offs visible so teams are not forced to infer them from interruptions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make priority decisions and trade-offs visible so teams are not forced to infer them from interruptions.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BB0B4D99` — Nondeterministic

**Exact statement**

> Review the portfolio after material market, regulatory, supplier, incident, capacity, or strategy changes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review the portfolio after material market, regulatory, supplier, incident, capacity, or strategy changes.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1B32D90B` — Deterministic

**Exact statement**

> Define the capability and quality requirements before selecting a product, provider, framework, platform, or outsourcing arrangement.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A selection gate can require versioned capability and quality requirements dated before any product, provider, framework or sourcing choice.

### Exact deterministic check

1. Define the capability and quality requirements before selecting a product, provider, framework, platform, or outsourcing arrangement.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A provider selected before the requirement-set timestamp or with no quality requirements fails the temporal gate.

## `USEQ-735862F8` — Nondeterministic

**Exact statement**

> Evaluate functional fit, security, privacy, accessibility, reliability, performance, scalability, interoperability, data portability, operability, maintainability, support, and total cost.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Evaluate functional fit, security, privacy, accessibility, reliability, performance, scalability, interoperability, data portability, operability, maintainability, support, and total cost.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-590D0692` — Nondeterministic

**Exact statement**

> Assess supplier ownership, financial health, governance, development practices, vulnerability handling, roadmap, support model, and concentration risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Assess supplier ownership, financial health, governance, development practices, vulnerability handling, roadmap, support model, and concentration risk.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D6C76249` — Nondeterministic

**Exact statement**

> Inventory sub-suppliers and critical external dependencies where risk warrants it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Where risk warrants and complete discovery of sub-suppliers and external dependencies require risk judgment and supply-chain inventory completeness.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known high-risk suppliers can be listed while a critical fourth party remains hidden or wrongly rated low risk.

## `USEQ-4C04425F` — Deterministic

**Exact statement**

> Require evidence for security, accessibility, compliance, data location, deletion, backup, incident notification, and continuity claims.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A procurement evidence package can require linked evidence for the eight exact security, accessibility, compliance, location, deletion, backup, notification and continuity claims.

### Exact deterministic check

1. Require evidence for security, accessibility, compliance, data location, deletion, backup, incident notification, and continuity claims.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A selected supplier claim without accessibility or deletion evidence fails the finite evidence matrix.

## `USEQ-D6453C61` — Nondeterministic

**Exact statement**

> Evaluate integration and migration cost using a representative proof, not only vendor demonstrations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Representative proof and complete integration and migration costs require workload, architecture and economic judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A pilot can use a nominal sample while excluding the hardest data migration and production authentication integration.

## `USEQ-56890F18` — Nondeterministic

**Exact statement**

> Test contractual service levels against actual user objectives and define remedies, escalation, and evidence access.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Actual user objectives, adequate service-level tests and contract remedies require empirical outcome evidence plus legal and business judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

A synthetic SLA test can pass while the contractual target misses a critical user journey and remedies are unenforceable.

## `USEQ-672B794C` — Nondeterministic

**Exact statement**

> Retain rights to necessary data, configuration, logs, documentation, artifacts, and export formats.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Whether contracts retain enforceable rights to six data and artifact classes is a legal interpretation, not a clause-presence fact.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

All six nouns can appear while provider exceptions prevent export of necessary logs at termination.

## `USEQ-565684CB` — Deterministic

**Exact statement**

> Define termination, migration assistance, credential revocation, data return, deletion, transition period, and exit cost before commitment.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A precommitment exit plan can be checked for seven exact termination, assistance, revocation, return, deletion, transition and cost definitions.

### Exact deterministic check

1. Define termination, migration assistance, credential revocation, data return, deletion, transition period, and exit cost before commitment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A commitment without credential-revocation or exit-cost terms fails the finite plan schema.

## `USEQ-8754EEDE` — Nondeterministic

**Exact statement**

> Avoid proprietary lock-in unless its value exceeds modeled switching risk and an accountable owner accepts it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid proprietary lock-in unless its value exceeds modeled switching risk and an accountable owner accepts it.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-34334A55` — Nondeterministic

**Exact statement**

> Review open-source governance, license, maintainer health, release cadence, security response, transitive dependencies, and contribution strategy.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review open-source governance, license, maintainer health, release cadence, security response, transitive dependencies, and contribution strategy.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2CBC3606` — Deterministic

**Exact statement**

> Define ownership for forks, patches, vendored code, unsupported versions, and upstream contributions.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A code-ownership policy can define accountable owners for forks, patches, vendored code, unsupported versions and upstream contributions.

### Exact deterministic check

1. Define ownership for forks, patches, vendored code, unsupported versions, and upstream contributions.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Any of the five code states without an owner fails the exact mapping.

## `USEQ-41AD9DE5` — Nondeterministic

**Exact statement**

> Use escrow, source access, alternate providers, or internal capability where loss of a supplier would create unacceptable continuity risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use escrow, source access, alternate providers, or internal capability where loss of a supplier would create unacceptable continuity risk.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FE5899D5` — Nondeterministic

**Exact statement**

> Continuously monitor supplier changes that can alter price, terms, data use, functionality, security, support, or geographic availability.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Continuous detection of supplier changes across seven effects requires complete supplier inventory and external price, terms and product feeds.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured alerts can all be current while a supplier silently changes data use or regional availability.

## `USEQ-D91347DE` — Nondeterministic

**Exact statement**

> Maintain a capability map for product, domain, architecture, code, data, security, privacy, accessibility, operations, and supplier knowledge.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A ten-area capability map cannot prove actual human knowledge, skill depth, coverage or correctness of self-reported capabilities.
- **Nondeterministic reason codes:** `human_accountability`

### Skeptical rejection counterexample

All areas can have named people and ratings while no one can operate a critical recovery procedure.

## `USEQ-8394400E` — Nondeterministic

**Exact statement**

> Ensure every critical service and recovery procedure has more than one qualified person and accessible documentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Critical-service completeness and qualified-person status require human competence judgment, while accessible documentation does not prove operational ability.
- **Nondeterministic reason codes:** `human_accountability`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Two people can be listed with documentation access while neither can execute the recovery procedure during an incident.

## `USEQ-8D1332CC` — Nondeterministic

**Exact statement**

> Plan training before introducing technology or controls that teams are expected to operate safely.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Planning occurrence is visible, but whether training is adequate before teams operate technology safely requires competence and safety judgment.
- **Nondeterministic reason codes:** `human_accountability`

### Skeptical rejection counterexample

A short recorded course can precede launch while operators remain unable to handle a dangerous failure mode.

## `USEQ-C1AF4BE4` — Nondeterministic

**Exact statement**

> Provide mentoring, review, communities of practice, and protected learning time rather than relying only on courses.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide mentoring, review, communities of practice, and protected learning time rather than relying only on courses.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-457EF747` — Nondeterministic

**Exact statement**

> Design roles and on-call expectations that are sustainable and compatible with health, leave, and local obligations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Design roles and on-call expectations that are sustainable and compatible with health, leave, and local obligations.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-44E36BD7` — Nondeterministic

**Exact statement**

> Track burnout, interruption, after-hours load, incident frequency, support burden, and chronic understaffing as system risks.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Track burnout, interruption, after-hours load, incident frequency, support burden, and chronic understaffing as system risks.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A48592E0` — Nondeterministic

**Exact statement**

> Do not use heroics as a capacity plan or performance expectation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not use heroics as a capacity plan or performance expectation.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-CCCA9A78` — Nondeterministic

**Exact statement**

> Include contractors and suppliers in necessary knowledge transfer, documentation, incident, security, and exit processes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Include contractors and suppliers in necessary knowledge transfer, documentation, incident, security, and exit processes.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1479A1E6` — Nondeterministic

**Exact statement**

> Ensure performance management does not reward risky volume, hidden work, gate bypass, knowledge hoarding, or incident concealment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure performance management does not reward risky volume, hidden work, gate bypass, knowledge hoarding, or incident concealment.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-51E72868` — Nondeterministic

**Exact statement**

> Create psychologically safe routes to raise quality, security, ethics, accessibility, and schedule concerns.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Create psychologically safe routes to raise quality, security, ethics, accessibility, and schedule concerns.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9EDBBD06` — Nondeterministic

**Exact statement**

> Protect whistleblowing and escalation from retaliation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect whistleblowing and escalation from retaliation.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-447508A8` — Deterministic

**Exact statement**

> Plan succession, offboarding, access removal, artifact transfer, and ownership reassignment.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A workforce transition plan can require the five exact succession, offboarding, access-removal, artifact-transfer and ownership-reassignment procedures.

### Exact deterministic check

1. Plan succession, offboarding, access removal, artifact transfer, and ownership reassignment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting artifact transfer or access removal fails the finite plan schema.

## `USEQ-59C8FAD1` — Nondeterministic

**Exact statement**

> Review whether reorganizations create orphaned services, broken escalation paths, duplicated platforms, or unsupported commitments.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether reorganizations create orphaned services, broken escalation paths, duplicated platforms, or unsupported commitments.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BF8D8060` — Nondeterministic

**Exact statement**

> Do not declare the engineering system ready when a routine critical workflow depends on undocumented manual intervention or one person's availability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not declare the engineering system ready when a routine critical workflow depends on undocumented manual intervention or one person's availability.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C67086D4` — Nondeterministic

**Exact statement**

> Do not introduce a mandatory platform path that cannot meet required reliability, security, accessibility, and recovery objectives.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not introduce a mandatory platform path that cannot meet required reliability, security, accessibility, and recovery objectives.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8BBAAB6D` — Nondeterministic

**Exact statement**

> Do not use individual productivity rankings, raw activity counts, or one delivery metric as a quality or performance verdict.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not use individual productivity rankings, raw activity counts, or one delivery metric as a quality or performance verdict.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4156513F` — Nondeterministic

**Exact statement**

> Do not commit a material investment whose scope, lifecycle cost, dominant assumptions, alternatives, and exit path are unknown.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not commit a material investment whose scope, lifecycle cost, dominant assumptions, alternatives, and exit path are unknown.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3F0AD88C` — Nondeterministic

**Exact statement**

> Do not commit to a date that omits critical dependencies, quality work, migration, stabilization, or support capacity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not commit to a date that omits critical dependencies, quality work, migration, stabilization, or support capacity.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A70E005C` — Nondeterministic

**Exact statement**

> Do not buy or build a critical capability without an accountable owner, operating model, support plan, and retirement path.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** Records can name an owner and plans, but whether that owner is genuinely accountable and the operating/support/retirement arrangements are accepted before commitment requires organizational authority.
- **Nondeterministic reason codes:** `human_accountability`

### Strength-audit rejection counterexample

A purchase record can contain a placeholder owner and template plans before approval even though the named person never accepted responsibility; schema checks pass while the original accountability promise fails.

## `USEQ-8BA058A0` — Nondeterministic

**Exact statement**

> Do not accept chronic broken builds, flaky tests, access delays, deployment tickets, or environment instability as normal developer experience.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not accept chronic broken builds, flaky tests, access delays, deployment tickets, or environment instability as normal developer experience.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-31618E9F` — Nondeterministic

**Exact statement**

> Do not force migration away from a supported path without consumer inventory, compatibility evidence, communication, and recovery.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Consumer inventory completeness, compatibility evidence and recovery effectiveness cannot be proved by records before forced migration.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All known consumers can receive communication while an offline supported client remains incompatible and unrecoverable.

## `USEQ-92FFBA7B` — Nondeterministic

**Exact statement**

> Do not use platform adoption as success evidence when user outcomes, stability, throughput, satisfaction, or total cost deteriorate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not use platform adoption as success evidence when user outcomes, stability, throughput, satisfaction, or total cost deteriorate.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D933D63C` — Nondeterministic

**Exact statement**

> Do not proceed when workload, on-call, knowledge concentration, or staffing makes safe operation unsustainable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not proceed when workload, on-call, knowledge concentration, or staffing makes safe operation unsustainable.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2837A62E` — Deterministic

**Exact statement**

> Treat internal engineering capabilities as products with identifiable users, outcomes, owners, roadmaps, support, and lifecycle decisions.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each registered internal engineering capability can be required to define users, outcomes, owners, roadmap, support and lifecycle decisions.

### Exact deterministic check

1. Treat internal engineering capabilities as products with identifiable users, outcomes, owners, roadmaps, support, and lifecycle decisions.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A capability without identifiable users or retirement decision policy fails the six-field product schema.

## `USEQ-2087E7B1` — Deterministic

**Exact statement**

> Define which developer, operator, analyst, data, security, support, and partner personas the platform serves.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A platform persona policy can define the seven finite developer, operator, analyst, data, security, support and partner personas.

### Exact deterministic check

1. Define which developer, operator, analyst, data, security, support, and partner personas the platform serves.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

If partner or support users lack a defined persona, the exact seven-class schema fails.

## `USEQ-7872D116` — Nondeterministic

**Exact statement**

> Research user workflows and friction directly rather than inferring needs only from leadership or tool owners.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Research user workflows and friction directly rather than inferring needs only from leadership or tool owners.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2ADEDFF1` — Deterministic

**Exact statement**

> Define the outcomes the platform should improve, such as safe delivery, feedback speed, reliability, compliance, onboarding, or cognitive load.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A platform strategy can explicitly define its target outcome set and bind each outcome to a versioned indicator identity.

### Exact deterministic check

1. Define the outcomes the platform should improve, such as safe delivery, feedback speed, reliability, compliance, onboarding, or cognitive load.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A strategy with no safe-delivery or feedback outcome definition fails its declared outcome schema; improvement is not inferred.

## `USEQ-9624E443` — Nondeterministic

**Exact statement**

> Do not create a platform merely to centralize control or standardize tools without demonstrated user value.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not create a platform merely to centralize control or standardize tools without demonstrated user value.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C98C9BC6` — Nondeterministic

**Exact statement**

> Distinguish mandatory organization controls from optional accelerators and clearly explain why each mandatory control exists.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** A mandatory-or-optional classification can be checked, but whether the explanation clearly communicates why every mandatory control exists requires interpretation by its intended readers.
- **Nondeterministic reason codes:** `contextual_judgment`

### Strength-audit rejection counterexample

Every control can have a nonempty rationale field containing circular or incomprehensible prose; field and enum checks pass while the required clear explanation is absent.

## `USEQ-F8CE1AED` — Deterministic

**Exact statement**

> Define what the platform owns, what product teams own, and what suppliers own.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A responsibility matrix can assign each registered platform boundary to exactly one of platform, product-team or supplier ownership.

### Exact deterministic check

1. Define what the platform owns, what product teams own, and what suppliers own.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A responsibility with no owner or conflicting direct owners fails the finite three-party mapping.

## `USEQ-84F487EA` — Deterministic

**Exact statement**

> Publish a service boundary, supported use cases, non-goals, dependencies, and escalation model.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Publish is an effective availability promise, requiring read-only evidence from the target publication surface rather than a publication record.

### Exact deterministic check

1. Every publication surface in the sealed platform-service audience inventory exposes the current approved service boundary, supported use cases, non-goals, dependencies, and escalation model at the declared publication endpoint.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A registry can list a service document while the published endpoint lacks non-goals or the escalation model. The environment query fails.

## `USEQ-D88D34D6` — Nondeterministic

**Exact statement**

> Prioritize capabilities using user impact, delivery risk, security, reliability, cost, and total organizational effort.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize capabilities using user impact, delivery risk, security, reliability, cost, and total organizational effort.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9D173BAA` — Nondeterministic

**Exact statement**

> Fund platform maintenance, support, security, migration, and deprecation—not only initial construction.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Fund platform maintenance, support, security, migration, and deprecation—not only initial construction.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2BC4E867` — Nondeterministic

**Exact statement**

> Use an incremental “thinnest viable platform” or equivalent approach instead of attempting to build every capability before learning from use.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use an incremental “thinnest viable platform” or equivalent approach instead of attempting to build every capability before learning from use.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-624E74B9` — Nondeterministic

**Exact statement**

> Keep platform adoption voluntary where risk permits and earn adoption through value; where controls are mandatory, make the compliant path the easiest path.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep platform adoption voluntary where risk permits and earn adoption through value; where controls are mandatory, make the compliant path the easiest path.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F61BB327` — Nondeterministic

**Exact statement**

> Review whether the platform increases autonomy or merely moves tickets and bottlenecks to a different team.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether the platform increases autonomy or merely moves tickets and bottlenecks to a different team.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BD39D438` — Nondeterministic

**Exact statement**

> Measure and manage platform-induced lock-in, concentration risk, and loss of local expertise.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure and manage platform-induced lock-in, concentration risk, and loss of local expertise.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-75B23248` — Nondeterministic

**Exact statement**

> Maintain a searchable catalog of services, applications, libraries, data products, pipelines, environments, infrastructure, owners, dependencies, criticality, and lifecycle status.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A searchable catalog with eleven fields cannot prove inclusion of every real service, pipeline, environment and dependency.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All entries can be complete while a shadow application or unregistered data pipeline remains absent.

## `USEQ-0537910D` — Nondeterministic

**Exact statement**

> Assign an accountable owner and operational contact to every production component.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Assigning owner and contact to every production component requires a complete production-component inventory not proven by the catalog.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Every catalog entry can have both fields while an undocumented production job remains unowned.

## `USEQ-D80B97CE` — Deterministic

**Exact statement**

> Record source repository, documentation, runbook, dashboards, alerts, SLOs, data classifications, APIs, dependencies, and deployment location for each catalog entry.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each catalog entry can be validated for the ten exact repository, documentation, operations, SLO, data, API, dependency and deployment fields.

### Exact deterministic check

1. Record source repository, documentation, runbook, dashboards, alerts, SLOs, data classifications, APIs, dependencies, and deployment location for each catalog entry.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An entry missing data classification or deployment location fails the finite catalog schema.

## `USEQ-010C8741` — Nondeterministic

**Exact statement**

> Identify orphaned, duplicate, abandoned, unsupported, and unowned components.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Orphaned, duplicate, abandoned, unsupported and unowned status requires complete component discovery plus semantic lifecycle and equivalence judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known entries can be classified while a duplicate shadow service remains undiscovered or an active component is wrongly marked abandoned.

## `USEQ-312DD67A` — Nondeterministic

**Exact statement**

> Keep catalog data synchronized automatically where reliable and review manually supplied metadata for staleness.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Reliable automation and metadata staleness require source-specific judgment, thresholds and complete synchronization-path coverage.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `undefined_threshold`

### Skeptical rejection counterexample

A sync job can be marked reliable while silently dropping owners, and manual metadata can pass review just before becoming stale.

## `USEQ-ACF53040` — Deterministic

**Exact statement**

> Distinguish authoritative metadata from discovered or inferred metadata.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Catalog metadata can carry an exact authority type distinguishing authoritative values from discovered and inferred values with source provenance.

### Exact deterministic check

1. Distinguish authoritative metadata from discovered or inferred metadata.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An inferred owner labeled authoritative or an untyped field violates the finite metadata schema.

## `USEQ-DEDBCA46` — Nondeterministic

**Exact statement**

> Expose dependency and ownership relationships without revealing sensitive topology to unauthorized users.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Useful relationship exposure and absence of sensitive topology leakage require authorization semantics and complete response and query-path coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Authorized users can view known dependencies while an unauthenticated search or error response leaks a restricted network relationship.

## `USEQ-86095E9D` — Nondeterministic

**Exact statement**

> Integrate catalog ownership with incident routing, vulnerability notifications, deprecation notices, and cost allocation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Integration records do not prove incident, vulnerability, deprecation and cost events are routed correctly across all ownership changes and failure paths.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All four connectors can exist while stale ownership sends a critical vulnerability notice to the wrong team.

## `USEQ-C2E31CA3` — Nondeterministic

**Exact statement**

> Define registration and retirement workflows so new and deleted components do not bypass inventory.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Defined registration and retirement workflows cannot prove that every creation and deletion path is forced through inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The official workflow can update the catalog while a direct infrastructure API creates a component without registration.

## `USEQ-8972276D` — Deterministic

**Exact statement**

> Provide stable identifiers for catalog entities across renames, moves, and platform migrations.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Catalog entity identities can be immutable and aliases can map renames, moves and platform migrations back to the same identifier.

### Exact deterministic check

1. Provide stable identifiers for catalog entities across renames, moves, and platform migrations.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Issuing a new identity during a rename or losing the migration alias fails the exact identity-continuity check.

## `USEQ-489B6ACB` — Nondeterministic

**Exact statement**

> Allow teams to discover approved components, examples, policies, and reusable capabilities without tribal knowledge.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Allow teams to discover approved components, examples, policies, and reusable capabilities without tribal knowledge.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6385F8F8` — Nondeterministic

**Exact statement**

> Monitor coverage and freshness of the catalog as an operational control.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Catalog coverage cannot be known without a complete external inventory, and freshness requires source-specific thresholds and semantics.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`, `unbounded_scope`

### Skeptical rejection counterexample

The dashboard can show full coverage of registered sources while a shadow service is absent and stale owner data appears current.

## `USEQ-5FDA8D4E` — Nondeterministic

**Exact statement**

> Do not treat catalog presence as evidence that documentation, support, or ownership is actually effective.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not treat catalog presence as evidence that documentation, support, or ownership is actually effective.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FB32D303` — Nondeterministic

**Exact statement**

> Keep emergency contact information available when the normal catalog or identity platform is unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Availability of emergency contacts during correlated catalog and identity failure is an empirical recovery property, not a backup-record fact.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

An offline contact file can exist but be encrypted with a key available only through the failed identity platform.

## `USEQ-07FDA5F4` — Nondeterministic

**Exact statement**

> Provide recommended paths for common workflows using secure, observable, supportable, and maintained defaults.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Secure, observable, supportable and maintained defaults are platform-quality and lifecycle judgments across incomplete common workflows.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A recommended template can be documented and instrumented while its default role is overprivileged or dependency abandoned.

## `USEQ-4B12B7BF` — Nondeterministic

**Exact statement**

> Design paths around complete user outcomes such as creating, testing, deploying, operating, and retiring a service—not isolated tool installation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Design paths around complete user outcomes such as creating, testing, deploying, operating, and retiring a service—not isolated tool installation.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2603D264` — Deterministic

**Exact statement**

> Keep recommended paths versioned, documented, testable, and reproducible.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each registered recommended path can be content-versioned, linked to documentation and tests, and reproduced from pinned inputs and artifacts.

### Exact deterministic check

1. Keep recommended paths versioned, documented, testable, and reproducible.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A path with mutable inputs, no test identity or a replay digest mismatch fails the bounded provenance contract.

## `USEQ-2B0F618E` — Nondeterministic

**Exact statement**

> Generate the minimum necessary starter material and avoid creating large opaque templates teams cannot understand.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Generate the minimum necessary starter material and avoid creating large opaque templates teams cannot understand.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1FA6A3A8` — Deterministic

**Exact statement**

> Ensure generated projects have clear ownership, dependency provenance, update paths, tests, and removal instructions.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every generated project manifest can require owner, dependency provenance, update path, test references and removal instructions.

### Exact deterministic check

1. Ensure generated projects have clear ownership, dependency provenance, update paths, tests, and removal instructions.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A project missing dependency lineage or removal instructions fails the finite five-field generation schema.

## `USEQ-372F71E0` — Nondeterministic

**Exact statement**

> Provide examples that follow current standards and are continuously tested.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Current standards require changing external and internal authority, while continuous test occurrence does not prove examples behave correctly.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Examples can run on every commit while still teach an obsolete security pattern or assert the wrong output.

## `USEQ-69A27DFC` — Nondeterministic

**Exact statement**

> Use shared libraries and components only when their support, compatibility, and change model are clear.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clear support, compatibility and change models are qualitative and do not prove actual maintenance or compatibility across consumers.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All three documents can exist while the shared library silently breaks an older supported client.

## `USEQ-2BCB3A9A` — Nondeterministic

**Exact statement**

> Avoid forcing unrelated products into one architecture when risk and requirements differ.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid forcing unrelated products into one architecture when risk and requirements differ.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-710C661D` — Nondeterministic

**Exact statement**

> Provide reviewed escape hatches for legitimate exceptions, with explicit ownership and risk treatment.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Legitimacy of exceptions, reviewer quality and adequate risk treatment require accountable security and product judgment.
- **Nondeterministic reason codes:** `human_accountability`

### Skeptical rejection counterexample

Every escape hatch can have approval and owner fields while an unjustified exception removes a critical control.

## `USEQ-8CF50CBD` — Nondeterministic

**Exact statement**

> Ensure escaping a recommended path does not silently remove security, privacy, accessibility, reliability, or audit controls.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Guaranteeing escape paths do not silently remove five quality controls requires complete generated configurations and runtime path coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The standard exception test can pass while one escape option disables audit logging or accessibility behavior.

## `USEQ-ADE1D409` — Nondeterministic

**Exact statement**

> Track adoption, abandonment, support burden, defect rates, and user satisfaction for each path.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** User satisfaction and per-path adoption or abandonment require a defined path population, attribution, observation window, survey instrument, and missing-response treatment. The rule supplies none of these, so record completeness cannot prove the full tracking promise.
- **Nondeterministic reason codes:** `undefined_protocol`

### Strength-audit rejection counterexample

A dashboard can show five fields for each declared path while omitting users who abandon before telemetry starts or surveying only completed journeys. Exact field and path-inventory checks pass although abandonment and satisfaction tracking are substantively incomplete.

## `USEQ-B3750A1C` — Nondeterministic

**Exact statement**

> Deprecate obsolete paths with migration tooling, communication, and sufficient support windows.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Migration tooling and communication are observable, but sufficient support windows depend on consumer risk, usage cycles and migration complexity.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A deprecated path can have all three records while the window ends before a dormant supported consumer can migrate.

## `USEQ-A33D1C6F` — Nondeterministic

**Exact statement**

> Allow teams to inspect and understand generated configuration and infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Inspection access can be provided, but whether teams understand generated configuration and infrastructure is a human comprehension outcome.
- **Nondeterministic reason codes:** `human_accountability`

### Skeptical rejection counterexample

Configuration can be visible and documented while users misunderstand a generated privilege or network rule.

## `USEQ-9BCE1201` — Nondeterministic

**Exact statement**

> Prevent templates from embedding secrets, obsolete dependencies, insecure defaults, or organization-specific identifiers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of secrets, obsolete dependencies, insecure defaults and organization identifiers is an open-world negative security claim across generated output.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Scans can pass while an encoded secret or transitive obsolete dependency remains in one generated variant.

## `USEQ-2E72ABDB` — Nondeterministic

**Exact statement**

> Enable authorized users to perform frequent low-risk actions without waiting for manual tickets.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Low-risk classification requires judgment, and working self-service access across all authorized users and actions is an operational behavior claim.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

The catalog can label actions low risk and ticketless while one action is actually high impact or fails for an authorized role.

## `USEQ-2B4C917C` — Nondeterministic

**Exact statement**

> Automate policy checks and approvals that are deterministic; reserve human approval for judgment, exceptions, and high-impact risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Determining which policies are deterministic and which decisions require human approval, exceptions or high-impact risk judgment is itself contextual governance work.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A rule can be wrongly automated despite needing interpretation, while a subjective low-impact request is needlessly sent to humans.

## `USEQ-4B11585B` — Nondeterministic

**Exact statement**

> Use clear inputs, previews, validation, cost implications, and consequences before self-service actions execute.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clarity of inputs, previews and consequences plus adequacy of validation and cost implications require user and domain judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All five UI sections can exist while the preview hides a destructive consequence or cost estimate misleads users.

## `USEQ-E31ED114` — Nondeterministic

**Exact statement**

> Make destructive and high-impact operations reversible, confirmable, or subject to additional authorization as appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Make destructive and high-impact operations reversible, confirmable, or subject to additional authorization as appropriate.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8CBDC7EA` — Nondeterministic

**Exact statement**

> Keep self-service workflows idempotent and safe to retry.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idempotence and safe retry are runtime semantic properties across every workflow state, side effect, failure and timing path.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known retry tests can pass while a timeout after commit causes a duplicate resource or repeated charge.

## `USEQ-C2F428A8` — Nondeterministic

**Exact statement**

> Provide progress, failure, rollback, and recovery information for long-running operations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Status fields can exist without complete/current progress, accurate failure state, usable rollback, or recovery information for every long-running operation. Therefore the proposed structured-record composite does not prove the whole rule “Provide progress, failure, rollback, and recovery information for long-running operations.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A job can show 90% and a rollback link while a hidden failed child makes both misleading. The proposal can Pass while “Provide progress, failure, rollback, and recovery information for long-running operations.” remains false.

## `USEQ-3B63475F` — Nondeterministic

**Exact statement**

> Do not hide partial failure or leave ambiguous resource ownership.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of hidden partial failures and ambiguity requires complete resource/event scope and semantic ownership across providers and async work. Therefore the proposed structured-record composite does not prove the whole rule “Do not hide partial failure or leave ambiguous resource ownership.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Visible tasks can succeed while an untracked child resource fails and has no clear owner. The proposal can Pass while “Do not hide partial failure or leave ambiguous resource ownership.” remains false.

## `USEQ-5B6344BA` — Nondeterministic

**Exact statement**

> Use scoped identities and least privilege for automation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identity scope is inspectable, but least privilege requires complete automation actions, resources, emergency paths, and authority context. Therefore the proposed structured-record composite does not prove the whole rule “Use scoped identities and least privilege for automation.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Dedicated roles can pass while retaining wildcard rights or a shared fallback administrator. The proposal can Pass while “Use scoped identities and least privilege for automation.” remains false.

## `USEQ-B737F580` — Nondeterministic

**Exact statement**

> Record actor, request, policy decision, artifact, configuration, outcome, and affected resources for auditable actions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The seven fields are exact, but complete auditable-action capture, affected-resource truth, and valid policy/actor authority are not proven. Therefore the proposed structured-record composite does not prove the whole rule “Record actor, request, policy decision, artifact, configuration, outcome, and affected resources for auditable actions.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Every recorded action can have fields while a provider-console mutation is omitted or affected children are missing. The proposal can Pass while “Record actor, request, policy decision, artifact, configuration, outcome, and affected resources for auditable actions.” remains false.

## `USEQ-6325634F` — Nondeterministic

**Exact statement**

> Apply quotas, rate limits, budgets, and guardrails to prevent accidental or malicious resource exhaustion.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Quota and limit values are exact, but adequacy against accidental/malicious exhaustion requires workload, threat, fairness, and failure testing. Therefore the proposed structured-record composite does not prove the whole rule “Apply quotas, rate limits, budgets, and guardrails to prevent accidental or malicious resource exhaustion.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Limits can exist while one request fans out enough to exhaust a shared backend. The proposal can Pass while “Apply quotas, rate limits, budgets, and guardrails to prevent accidental or malicious resource exhaustion.” remains false.

## `USEQ-4B82601E` — Nondeterministic

**Exact statement**

> Prevent users from provisioning unsupported versions or bypassing required lifecycle controls.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing every unsupported version and lifecycle bypass spans all interfaces, providers, caches, manual paths, and version semantics. Therefore the proposed structured-record composite does not prove the whole rule “Prevent users from provisioning unsupported versions or bypassing required lifecycle controls.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

The primary API can reject while a direct provider tool provisions an old unsupported version. The proposal can Pass while “Prevent users from provisioning unsupported versions or bypassing required lifecycle controls.” remains false.

## `USEQ-DBFE6DD8` — Nondeterministic

**Exact statement**

> Provide an escalation path when automation cannot satisfy a valid request.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** An escalation contact is recordable, but request validity, reachability, authority, timeliness, and successful resolution require human judgment. Therefore the proposed structured-record composite does not prove the whole rule “Provide an escalation path when automation cannot satisfy a valid request.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A valid request can receive an unstaffed queue or an escalation owner without permissions. The proposal can Pass while “Provide an escalation path when automation cannot satisfy a valid request.” remains false.

## `USEQ-CB367A5D` — Nondeterministic

**Exact statement**

> Measure whether self-service reduces total wait time and toil rather than only ticket count.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure whether self-service reduces total wait time and toil rather than only ticket count.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A3EF0CB5` — Nondeterministic

**Exact statement**

> Test automation against duplicate requests, cancellation, dependency failure, and platform outages.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The four injected cases have many timing, state, dependency, retry, and compound-failure combinations without exhaustive oracles. Therefore the proposed structured-record composite does not prove the whole rule “Test automation against duplicate requests, cancellation, dependency failure, and platform outages.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A duplicate fixture can pass while cancellation during dependency recovery creates two resources. The proposal can Pass while “Test automation against duplicate requests, cancellation, dependency failure, and platform outages.” remains false.

## `USEQ-587143E3` — Nondeterministic

**Exact statement**

> Provide a documented, testable path from a new authorized workstation or workspace to a functioning development environment.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One workstation setup test does not prove a path works across authorized identities, supported platforms, networks, devices, and tool changes; functioning is contextual. Therefore the proposed structured-record composite does not prove the whole rule “Provide a documented, testable path from a new authorized workstation or workspace to a functioning development environment.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

A clean CI image can succeed while a supported laptop or restricted network cannot. The proposal can Pass while “Provide a documented, testable path from a new authorized workstation or workspace to a functioning development environment.” remains false.

## `USEQ-DBC5BFD6` — Nondeterministic

**Exact statement**

> Minimize required manual installation, local machine mutation, and hidden prerequisites.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Minimize required manual installation, local machine mutation, and hidden prerequisites.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0B335E6B` — Nondeterministic

**Exact statement**

> Pin or verify toolchain, runtime, dependency, and generator versions sufficiently for reproducibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Pins/digests are exact, but sufficiency for reproducibility depends on hidden/transitive inputs, resolver behavior, platform, and generator effects. Therefore the proposed structured-record composite does not prove the whole rule “Pin or verify toolchain, runtime, dependency, and generator versions sufficiently for reproducibility.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Top-level versions can be pinned while a mutable image or remote generator changes results. The proposal can Pass while “Pin or verify toolchain, runtime, dependency, and generator versions sufficiently for reproducibility.” remains false.

## `USEQ-AB6CABAF` — Nondeterministic

**Exact statement**

> Keep setup scripts idempotent and safe to rerun.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idempotence and rerun safety must hold at every interruption point, prior state, concurrency, platform, and external side effect. Therefore the proposed structured-record composite does not prove the whole rule “Keep setup scripts idempotent and safe to rerun.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Two normal reruns can pass while resuming after timeout duplicates an account or deletes local work. The proposal can Pass while “Keep setup scripts idempotent and safe to rerun.” remains false.

## `USEQ-1629715E` — Nondeterministic

**Exact statement**

> Validate prerequisites early and produce actionable errors.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Early validation needs complete prerequisite/error scope, and actionability depends on user context and diagnostic truth. Therefore the proposed structured-record composite does not prove the whole rule “Validate prerequisites early and produce actionable errors.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known prerequisites can fail early while a later hidden dependency fails with generic guidance. The proposal can Pass while “Validate prerequisites early and produce actionable errors.” remains false.

## `USEQ-1AAC867A` — Nondeterministic

**Exact statement**

> Provide sanitized representative test data and avoid copying production data by default.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Sanitization, representativeness, authorization, and default avoidance of production copies require privacy authority and data-quality judgment. Therefore the proposed structured-record composite does not prove the whole rule “Provide sanitized representative test data and avoid copying production data by default.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

A masked copy can remain reidentifiable or synthetic data can omit the relationships needed for tests. The proposal can Pass while “Provide sanitized representative test data and avoid copying production data by default.” remains false.

## `USEQ-53AE4D3E` — Nondeterministic

**Exact statement**

> Use separate identities, credentials, data, endpoints, and side-effect controls for non-production environments.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Separation across identities, credentials, data, endpoints, and side effects requires complete environment/provider/trust/fallback scope. Therefore the proposed structured-record composite does not prove the whole rule “Use separate identities, credentials, data, endpoints, and side-effect controls for non-production environments.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Separate URLs can pass while federation, a shared key, or live notification fallback crosses environments. The proposal can Pass while “Use separate identities, credentials, data, endpoints, and side-effect controls for non-production environments.” remains false.

## `USEQ-D5E12F9F` — Nondeterministic

**Exact statement**

> Make environment reset and cleanup reliable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Reset/cleanup reliability spans all resources, async jobs, failures, cancellations, provider state, and repeated runs. Therefore the proposed structured-record composite does not prove the whole rule “Make environment reset and cleanup reliable.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Normal reset can pass while a timeout leaves an untagged resource that contaminates the next run. The proposal can Pass while “Make environment reset and cleanup reliable.” remains false.

## `USEQ-8C2638B8` — Nondeterministic

**Exact statement**

> Ensure local, remote, containerized, virtual, or hosted development options meet security and accessibility requirements.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Ensure local, remote, containerized, virtual, or hosted development options meet security and accessibility requirements.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-78829D95` — Nondeterministic

**Exact statement**

> Provide an alternative when a developer cannot use a specific hardware platform, biometric method, visual tool, or network location.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide an alternative when a developer cannot use a specific hardware platform, biometric method, visual tool, or network location.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1B1561EC` — Nondeterministic

**Exact statement**

> Document meaningful differences between local, test, staging, and production behavior.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Document presence cannot prove complete behavior differences or that described differences are meaningful and accurate. Therefore the proposed structured-record composite does not prove the whole rule “Document meaningful differences between local, test, staging, and production behavior.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

The table can omit production rate limiting or dismiss a security-boundary difference as minor. The proposal can Pass while “Document meaningful differences between local, test, staging, and production behavior.” remains false.

## `USEQ-C59AAC06` — Nondeterministic

**Exact statement**

> Measure onboarding task success and time without turning individual speed into a performance score.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure onboarding task success and time without turning individual speed into a performance score.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-547DB4A5` — Nondeterministic

**Exact statement**

> Test onboarding instructions with people unfamiliar with the system.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test onboarding instructions with people unfamiliar with the system.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-697FCFA9` — Nondeterministic

**Exact statement**

> Remove obsolete setup paths and prevent conflicting documentation from remaining discoverable.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Obsolete/conflicting setup paths may remain in search indexes, forks, generated docs, caches, scripts, and external links; discoverability is user-contextual. Therefore the proposed structured-record composite does not prove the whole rule “Remove obsolete setup paths and prevent conflicting documentation from remaining discoverable.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Canonical docs can be fixed while an indexed old guide still attracts new developers. The proposal can Pass while “Remove obsolete setup paths and prevent conflicting documentation from remaining discoverable.” remains false.

## `USEQ-B739ED54` — Nondeterministic

**Exact statement**

> Keep the fastest relevant validation close to the developer and defer expensive checks without losing release assurance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep the fastest relevant validation close to the developer and defer expensive checks without losing release assurance.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9450BC5E` — Deterministic

**Exact statement**

> Define target feedback times for formatting, compilation, static analysis, unit tests, integration tests, review, builds, and deployments.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The eight feedback stages can have explicit target durations, units, scope/version bindings, and effective dates in a finite policy schema. This proves the complete bounded meaning of “Define target feedback times for formatting, compilation, static analysis, unit tests, integration tests, review, builds, and deployments.”.

### Exact deterministic check

1. Define target feedback times for formatting, compilation, static analysis, unit tests, integration tests, review, builds, and deployments.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Any missing target, invalid unit, or unbound version deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-03172925` — Deterministic

**Exact statement**

> Monitor feedback latency percentiles and queue time rather than averages alone.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. With sealed start/queue/run/end events, latency percentiles and queue-time distributions are exact calculations distinct from averages. This proves the complete bounded meaning of “Monitor feedback latency percentiles and queue time rather than averages alone.”.

### Exact deterministic check

1. Monitor feedback latency percentiles and queue time rather than averages alone.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Omitting queue time, reporting only an average, or computing the wrong percentile deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-65A42809` — Nondeterministic

**Exact statement**

> Prioritize deterministic, actionable failures over large volumes of noisy output.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize deterministic, actionable failures over large volumes of noisy output.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-62AAA93F` — Nondeterministic

**Exact statement**

> Fail early on configuration, dependency, schema, and policy errors that would invalidate later work.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Failing early requires complete error discovery and dependency ordering plus judgment that an error invalidates later work. Therefore the proposed structured-record composite does not prove the whole rule “Fail early on configuration, dependency, schema, and policy errors that would invalidate later work.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Known schema checks can pass while a hidden policy or transitive dependency error is found only after expensive work. The proposal can Pass while “Fail early on configuration, dependency, schema, and policy errors that would invalidate later work.” remains false.

## `USEQ-F9B57D3D` — Nondeterministic

**Exact statement**

> Parallelize independent checks without introducing nondeterminism or excessive resource cost.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Independence, nondeterminism, and excessive resource cost require scheduling experiments, cost thresholds, and complete shared-dependency scope. Therefore the proposed structured-record composite does not prove the whole rule “Parallelize independent checks without introducing nondeterminism or excessive resource cost.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Parallel checks can pass normal runs while a rare shared cache race or provider bill spike occurs. The proposal can Pass while “Parallelize independent checks without introducing nondeterminism or excessive resource cost.” remains false.

## `USEQ-FEDEFC91` — Nondeterministic

**Exact statement**

> Use caching only when cache keys, invalidation, isolation, provenance, and poisoning risks are controlled.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safe caching needs complete key inputs, invalidation events, trust boundaries, provenance, isolation, and poisoning attack space. Therefore the proposed structured-record composite does not prove the whole rule “Use caching only when cache keys, invalidation, isolation, provenance, and poisoning risks are controlled.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Declared keys can pass while an omitted tool version or shared untrusted cache returns poisoned output. The proposal can Pass while “Use caching only when cache keys, invalidation, isolation, provenance, and poisoning risks are controlled.” remains false.

## `USEQ-0F71EDF0` — Nondeterministic

**Exact statement**

> Make it easy to reproduce a CI failure locally or in an isolated diagnostic environment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Ease, local equivalence, and successful reproduction depend on user skill, platform, hidden CI state, provider behavior, and failure semantics. Therefore the proposed structured-record composite does not prove the whole rule “Make it easy to reproduce a CI failure locally or in an isolated diagnostic environment.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A replay command can exist while it omits the secret timing or service race that caused CI failure. The proposal can Pass while “Make it easy to reproduce a CI failure locally or in an isolated diagnostic environment.” remains false.

## `USEQ-31021B4A` — Nondeterministic

**Exact statement**

> Provide logs and artifacts sufficient to diagnose failures without broad privileged access.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Log/artifact presence cannot prove diagnostic sufficiency, least needed privilege, privacy, or correct causal information. Therefore the proposed structured-record composite does not prove the whole rule “Provide logs and artifacts sufficient to diagnose failures without broad privileged access.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

All standard logs can exist while the missing trace needs root access and is the only useful evidence. The proposal can Pass while “Provide logs and artifacts sufficient to diagnose failures without broad privileged access.” remains false.

## `USEQ-6E3F668E` — Nondeterministic

**Exact statement**

> Track flaky tests, intermittent infrastructure failures, canceled jobs, and reruns separately from product defects.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Separate enum fields are exact, but complete event capture and causal classification among flaky test, infrastructure, cancellation, rerun, and product defect are not. Therefore the proposed structured-record composite does not prove the whole rule “Track flaky tests, intermittent infrastructure failures, canceled jobs, and reruns separately from product defects.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A product race can be mislabeled infrastructure failure, or an unreported rerun is omitted. The proposal can Pass while “Track flaky tests, intermittent infrastructure failures, canceled jobs, and reruns separately from product defects.” remains false.

## `USEQ-725337F7` — Nondeterministic

**Exact statement**

> Prevent teams from normalizing repeated reruns as a valid delivery process.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent teams from normalizing repeated reruns as a valid delivery process.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E454B7C4` — Nondeterministic

**Exact statement**

> Keep code-review queues visible and balance reviewer load and expertise.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Queue visibility is measurable, but balanced load and sufficient expertise require human capacity, domain, and fairness judgment. Therefore the proposed structured-record composite does not prove the whole rule “Keep code-review queues visible and balance reviewer load and expertise.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A dashboard can be complete while specialists are overloaded and unqualified reviewers receive critical changes. The proposal can Pass while “Keep code-review queues visible and balance reviewer load and expertise.” remains false.

## `USEQ-66533E37` — Nondeterministic

**Exact statement**

> Encourage small batches that can be reviewed, tested, deployed, and reversed safely.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Encourage small batches that can be reviewed, tested, deployed, and reversed safely.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5A81540A` — Nondeterministic

**Exact statement**

> Measure end-to-end feedback loops, including waiting for environments, data, approvals, reviews, and external teams.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure end-to-end feedback loops, including waiting for environments, data, approvals, reviews, and external teams.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-90D7EA9F` — Nondeterministic

**Exact statement**

> Treat platform interfaces as supported products with explicit contracts, versioning, ownership, and compatibility policies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Contract/version/owner fields do not prove product-level support, compatibility policy adequacy, or accountable ownership. Therefore the proposed structured-record composite does not prove the whole rule “Treat platform interfaces as supported products with explicit contracts, versioning, ownership, and compatibility policies.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A versioned interface can have a nominal owner while breaking consumers without support. The proposal can Pass while “Treat platform interfaces as supported products with explicit contracts, versioning, ownership, and compatibility policies.” remains false.

## `USEQ-A87EE218` — Nondeterministic

**Exact statement**

> Publish schemas, examples, error semantics, rate limits, quotas, authentication, authorization, idempotency, and lifecycle states.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Publication presence cannot prove complete/correct schemas, semantics, limits, auth, idempotency, and lifecycle states for every interface/version. Therefore the proposed structured-record composite does not prove the whole rule “Publish schemas, examples, error semantics, rate limits, quotas, authentication, authorization, idempotency, and lifecycle states.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

All sections can exist while retry semantics or a lifecycle transition is wrong. The proposal can Pass while “Publish schemas, examples, error semantics, rate limits, quotas, authentication, authorization, idempotency, and lifecycle states.” remains false.

## `USEQ-C38502CA` — Nondeterministic

**Exact statement**

> Provide machine-readable interfaces where automation is expected and human-readable interfaces where judgment is required.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Where automation is expected and judgment required depends on user/task semantics, and machine/human readability is not established by format alone. Therefore the proposed structured-record composite does not prove the whole rule “Provide machine-readable interfaces where automation is expected and human-readable interfaces where judgment is required.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

JSON can be unsuitable for an expert decision while a prose-only routine task blocks automation. The proposal can Pass while “Provide machine-readable interfaces where automation is expected and human-readable interfaces where judgment is required.” remains false.

## `USEQ-CDE257DC` — Nondeterministic

**Exact statement**

> Keep command-line, user-interface, API, and configuration behavior semantically consistent.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Semantic consistency across CLI, UI, API, and config requires complete operation/state coverage and meaning oracles. Therefore the proposed structured-record composite does not prove the whole rule “Keep command-line, user-interface, API, and configuration behavior semantically consistent.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

All surfaces can share names while defaults or error behavior differ materially. The proposal can Pass while “Keep command-line, user-interface, API, and configuration behavior semantically consistent.” remains false.

## `USEQ-15FDD113` — Nondeterministic

**Exact statement**

> Use stable identifiers and avoid exposing volatile internal implementation details.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identifier stability over time and whether exposed fields are internal require semantic ownership, consumer inventories, and future evolution. Therefore the proposed structured-record composite does not prove the whole rule “Use stable identifiers and avoid exposing volatile internal implementation details.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

An ID can look stable while encoding a database shard that later changes. The proposal can Pass while “Use stable identifiers and avoid exposing volatile internal implementation details.” remains false.

## `USEQ-B0E23E13` — Nondeterministic

**Exact statement**

> Validate inputs before creating partial resources.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Input validation and absence of partial resources require every invalid form, race, dependency side effect, and resource-creation path. Therefore the proposed structured-record composite does not prove the whole rule “Validate inputs before creating partial resources.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Known invalid inputs can reject while a late cross-field error occurs after creating a child resource. The proposal can Pass while “Validate inputs before creating partial resources.” remains false.

## `USEQ-CF3EEC00` — Nondeterministic

**Exact statement**

> Provide preview, plan, or dry-run behavior for consequential changes where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide preview, plan, or dry-run behavior for consequential changes where practical.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-1D76E3B6` — Deterministic

**Exact statement**

> Define asynchronous operation status, cancellation, retry, timeout, and reconciliation behavior.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A versioned asynchronous-operation contract can explicitly enumerate status states, cancellation, retry, timeout, and reconciliation transitions in a finite state machine. This proves the complete bounded meaning of “Define asynchronous operation status, cancellation, retry, timeout, and reconciliation behavior.”.

### Exact deterministic check

1. Define asynchronous operation status, cancellation, retry, timeout, and reconciliation behavior.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

Missing transitions, ambiguous terminal states, or invalid references deterministically fail the contract schema. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-2702E53D` — Nondeterministic

**Exact statement**

> Provide contract tests or conformance suites for clients and providers.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Contract tests/suites sample clients/providers, versions, states, errors, timing, and extensions; occurrence does not prove conformance. Therefore the proposed structured-record composite does not prove the whole rule “Provide contract tests or conformance suites for clients and providers.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Known fixtures can pass while a real provider's retry or pagination behavior differs. The proposal can Pass while “Provide contract tests or conformance suites for clients and providers.” remains false.

## `USEQ-74089988` — Nondeterministic

**Exact statement**

> Use deprecation notices that reach actual consumers and include migration guidance and deadlines.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Actual-consumer reach, usable migration, and sufficient deadlines require complete consumer identity, delivery, compatibility, and timing judgment. Therefore the proposed structured-record composite does not prove the whole rule “Use deprecation notices that reach actual consumers and include migration guidance and deadlines.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A notice can publish while an unknown consumer never receives it or guidance is unusable. The proposal can Pass while “Use deprecation notices that reach actual consumers and include migration guidance and deadlines.” remains false.

## `USEQ-6DDF8C21` — Nondeterministic

**Exact statement**

> Monitor use of deprecated interfaces and identify unknown consumers before removal.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Usage monitoring cannot prove complete clients, offline consumers, indirect calls, caches, and identity attribution before removal. Therefore the proposed structured-record composite does not prove the whole rule “Monitor use of deprecated interfaces and identify unknown consumers before removal.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known traffic can reach zero while an offline or uninstrumented consumer remains. The proposal can Pass while “Monitor use of deprecated interfaces and identify unknown consumers before removal.” remains false.

## `USEQ-B74367BE` — Nondeterministic

**Exact statement**

> Keep error messages actionable and linked to current documentation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep error messages actionable and linked to current documentation.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6707A6FE` — Nondeterministic

**Exact statement**

> Do not require consumers to scrape logs or user interfaces when a stable interface is appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not require consumers to scrape logs or user interfaces when a stable interface is appropriate.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4F9EA2A9` — Nondeterministic

**Exact statement**

> Test interface accessibility and localization when humans interact with it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Test interface accessibility and localization when humans interact with it.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-48758704` — Nondeterministic

**Exact statement**

> Encode repeatable mandatory controls in versioned, reviewed, tested policy where practical.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Mandatory/repeatable classification, where policy encoding is practical, substantive review, and test adequacy require governance judgment. Therefore the proposed structured-record composite does not prove the whole rule “Encode repeatable mandatory controls in versioned, reviewed, tested policy where practical.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A policy file can exist while omitting a mandatory manual control or relying on weak fixtures. The proposal can Pass while “Encode repeatable mandatory controls in versioned, reviewed, tested policy where practical.” remains false.

## `USEQ-A3C4E315` — Nondeterministic

**Exact statement**

> Keep policy source, rationale, owner, scope, severity, remediation guidance, and exception process visible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Visible fields do not prove rationale/remediation quality, correct scope/severity, accountable ownership, or a workable exception process. Therefore the proposed structured-record composite does not prove the whole rule “Keep policy source, rationale, owner, scope, severity, remediation guidance, and exception process visible.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Every field can contain boilerplate while severity is understated and remediation unusable. The proposal can Pass while “Keep policy source, rationale, owner, scope, severity, remediation guidance, and exception process visible.” remains false.

## `USEQ-91D01B47` — Deterministic

**Exact statement**

> Test policy rules against allowed and prohibited examples before enforcement.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed policy-rule conformance set, authoritative allowed/prohibited fixture identities and expected decisions form a complete bounded protocol. This proves the complete bounded meaning of “Test policy rules against allowed and prohibited examples before enforcement.”.

### Exact deterministic check

1. Test policy rules against allowed and prohibited examples before enforcement.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Allowing a prohibited fixture or denying an allowed fixture deterministically fails. Within the sealed platform evidence, the rule cannot Pass while broken.

## `USEQ-CFDE8E86` — Nondeterministic

**Exact statement**

> Roll out high-impact policy changes progressively and measure false positives and operational impact.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Progressive rollout and measurements are partial; false-positive truth and operational impact require representative populations and human evaluation. Therefore the proposed structured-record composite does not prove the whole rule “Roll out high-impact policy changes progressively and measure false positives and operational impact.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

A canary can pass while another team/workload sees widespread false blocks. The proposal can Pass while “Roll out high-impact policy changes progressively and measure false positives and operational impact.” remains false.

## `USEQ-DA1E0181` — Nondeterministic

**Exact statement**

> Fail closed for critical security and authorization controls unless a documented safety analysis requires another behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fail-closed transitions are testable, but criticality, complete security/auth failure space, and validity of safety exceptions require judgment. Therefore the proposed structured-record composite does not prove the whole rule “Fail closed for critical security and authorization controls unless a documented safety analysis requires another behavior.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known outages can fail closed while a policy-service error falls open or an unsafe exception is documented. The proposal can Pass while “Fail closed for critical security and authorization controls unless a documented safety analysis requires another behavior.” remains false.

## `USEQ-C663C195` — Nondeterministic

**Exact statement**

> Use warning or advisory modes only when the residual risk is explicitly accepted and monitored.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Explicit acceptance dates are exact, but residual-risk truth, owner authority, acceptable monitoring, and advisory-mode suitability require accountable judgment. Therefore the proposed structured-record composite does not prove the whole rule “Use warning or advisory modes only when the residual risk is explicitly accepted and monitored.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A waiver can be signed while the risk is understated and monitoring cannot detect harm. The proposal can Pass while “Use warning or advisory modes only when the residual risk is explicitly accepted and monitored.” remains false.

## `USEQ-A04912A7` — Nondeterministic

**Exact statement**

> Provide safe defaults for identity, network access, encryption, logging, backups, resource limits, and exposure.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safe defaults across seven domains require threat/product context and complete configuration/path inventories. Therefore the proposed structured-record composite does not prove the whole rule “Provide safe defaults for identity, network access, encryption, logging, backups, resource limits, and exposure.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Declared defaults can pass while a provider-created public endpoint or missing backup remains unsafe. The proposal can Pass while “Provide safe defaults for identity, network access, encryption, logging, backups, resource limits, and exposure.” remains false.

## `USEQ-DF36238E` — Nondeterministic

**Exact statement**

> Do not make essential security capabilities a premium or optional path when users reasonably expect safe defaults.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not make essential security capabilities a premium or optional path when users reasonably expect safe defaults.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7B592261` — Nondeterministic

**Exact statement**

> Allow scoped time-bounded exceptions with owner, evidence, compensating controls, and automatic expiry.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Scope/expiry fields are exact, but owner authority, evidence truth, and compensating-control adequacy require accountable judgment. Therefore the proposed structured-record composite does not prove the whole rule “Allow scoped time-bounded exceptions with owner, evidence, compensating controls, and automatic expiry.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A narrow unexpired exception can cite an ineffective control and unauthorized owner. The proposal can Pass while “Allow scoped time-bounded exceptions with owner, evidence, compensating controls, and automatic expiry.” remains false.

## `USEQ-C6CEA1DC` — Nondeterministic

**Exact statement**

> Prevent policy bypass through alternate tools, direct provider access, unmanaged accounts, or emergency paths.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing every policy bypass across tools, provider access, accounts, and emergency paths is an adversarial identity/workflow claim. Therefore the proposed structured-record composite does not prove the whole rule “Prevent policy bypass through alternate tools, direct provider access, unmanaged accounts, or emergency paths.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Managed CLI tests can pass while a provider console or unmanaged account bypasses policy. The proposal can Pass while “Prevent policy bypass through alternate tools, direct provider access, unmanaged accounts, or emergency paths.” remains false.

## `USEQ-4F850A3C` — Nondeterministic

**Exact statement**

> Log and review break-glass and exception use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Log/review records are exact, but complete break-glass/exception event capture and substantive review require authority and external paths. Therefore the proposed structured-record composite does not prove the whole rule “Log and review break-glass and exception use.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Known uses can be reviewed while a provider-root action is omitted or review rubber-stamped. The proposal can Pass while “Log and review break-glass and exception use.” remains false.

## `USEQ-B8027624` — Nondeterministic

**Exact statement**

> Keep policy evaluation available and performant enough not to create pressure for bypass.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Availability/performance sufficient to avoid bypass pressure depends on latency, outage, user, workload, and organizational behavior thresholds. Therefore the proposed structured-record composite does not prove the whole rule “Keep policy evaluation available and performant enough not to create pressure for bypass.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Policy service can meet a nominal SLA while developers routinely bypass it due to critical-path latency. The proposal can Pass while “Keep policy evaluation available and performant enough not to create pressure for bypass.” remains false.

## `USEQ-BBB80D01` — Nondeterministic

**Exact statement**

> Provide automated remediation where it is safe and transparent.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safety and transparency of automated remediation require domain, reversibility, user-impact, and complete failure-state judgment. Therefore the proposed structured-record composite does not prove the whole rule “Provide automated remediation where it is safe and transparent.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A visible remediation can fix a sample while deleting valid custom configuration. The proposal can Pass while “Provide automated remediation where it is safe and transparent.” remains false.

## `USEQ-49857976` — Nondeterministic

**Exact statement**

> Review whether guardrails continue to address real risk rather than obsolete architecture.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether guardrails continue to address real risk rather than obsolete architecture.” is not deterministic because its full meaning requires organizational, economic, user-research, workflow, incentive, accessibility, sustainability, or platform-value judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BA808893` — Nondeterministic

**Exact statement**

> Define user-centered SLIs and SLOs for critical platform journeys such as build, test, provision, deploy, retrieve secrets, and diagnose incidents.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** User-centered indicators, critical journeys, objectives, and target adequacy require product research, platform risk, and accountable SLO judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define user-centered SLIs and SLOs for critical platform journeys such as build, test, provision, deploy, retrieve secrets, and diagnose incidents.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Every named journey can have an SLO while excluding the real user waiting time or omitting a critical workflow. The proposal can Pass while “Define user-centered SLIs and SLOs for critical platform journeys such as build, test, provision, deploy, retrieve secrets, and diagnose incidents.” remains false.

## `USEQ-791F51DA` — Nondeterministic

**Exact statement**

> Include correctness, latency, availability, freshness, capacity, and support response where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Include correctness, latency, availability, freshness, capacity, and support response where relevant.” has an objectively checkable artifact, record, configuration, metric, or bounded test, but full pass meaning also depends on semantic completeness, project scope, or outcome quality.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-8121C780` — Nondeterministic

**Exact statement**

> Ensure platform objectives are compatible with the reliability objectives of dependent products.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure platform objectives are compatible with the reliability objectives of dependent products.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-B52AE8A4` — Nondeterministic

**Exact statement**

> Monitor platform dependencies, quotas, certificate expiry, identity, DNS, artifact storage, and control planes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because declared platform objects cannot prove complete dependencies, quotas, certificates, identities, DNS, storage, and control planes or useful monitors.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An unregistered signing service certificate expires, while every declared platform object has current monitors.

## `USEQ-87EFFA25` — Nondeterministic

**Exact statement**

> Provide synthetic checks for critical self-service journeys.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide synthetic checks for critical self-service journeys.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-263E0518` — Nondeterministic

**Exact statement**

> Expose service status and known incidents to platform users.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because status records do not prove every known incident is disclosed accurately, promptly, accessibly, and to all affected users.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A regional incident is classified unknown and omitted, while the published status query is current.

## `USEQ-2ADCF482` — Nondeterministic

**Exact statement**

> Use error budgets or an equivalent policy to balance feature work with reliability work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use error budgets or an equivalent policy to balance feature work with reliability work.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-40DBA950` — Nondeterministic

**Exact statement**

> Provide runbooks and on-call ownership for platform components whose failure blocks delivery or recovery.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide runbooks and on-call ownership for platform components whose failure blocks delivery or recovery.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B6288BBC` — Nondeterministic

**Exact statement**

> Ensure support personnel can diagnose problems without inappropriate access to product secrets or customer data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure support personnel can diagnose problems without inappropriate access to product secrets or customer data.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-62FDFE99` — Nondeterministic

**Exact statement**

> Define support channels, hours, severity, escalation, and expected response.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because channel, severity, escalation, hours, and response fields do not prove staffed authority, capacity, or appropriate service.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: The critical channel routes to an unstaffed mailbox and promises a month; every field exists.

## `USEQ-D2E7BAC3` — Nondeterministic

**Exact statement**

> Capture repeated support requests as product or documentation defects.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Capture repeated support requests as product or documentation defects.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-98754BDB` — Nondeterministic

**Exact statement**

> Test platform disaster recovery and restore, including source, configuration, registries, metadata, identity, and signing material.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because DR scenarios do not exhaust platform assets, dependencies, data scale, corruption, identity loss, signing material, and recovery oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Source and registry restore passes, but an omitted identity metadata store makes production unusable.

## `USEQ-0B9A6478` — Nondeterministic

**Exact statement**

> Ensure product teams can continue critical incident response if the normal platform is unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure product teams can continue critical incident response if the normal platform is unavailable.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-F7346E05` — Nondeterministic

**Exact statement**

> Publish maintenance, degradation, and emergency change procedures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because published procedure headings do not prove feasible, safe, authorized maintenance, degradation, and emergency changes.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: The emergency procedure allows irreversible edits without rollback; all three documents are published.

## `USEQ-58A2631E` — Nondeterministic

**Exact statement**

> Measure whether engineers can understand and complete common tasks without memorizing undocumented system details.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Measure whether engineers can understand and complete common tasks without memorizing undocumented system details.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-C802111B` — Nondeterministic

**Exact statement**

> Reduce unnecessary tool switching, duplicate data entry, repeated authentication, and manual translation between systems.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reduce unnecessary tool switching, duplicate data entry, repeated authentication, and manual translation between systems.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-6924F2DC` — Nondeterministic

**Exact statement**

> Provide clear defaults while revealing advanced options progressively.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide clear defaults while revealing advanced options progressively.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-C31E7F6F` — Nondeterministic

**Exact statement**

> Use consistent terminology, navigation, status, and error patterns across engineering tools.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use consistent terminology, navigation, status, and error patterns across engineering tools.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-09A914D6` — Nondeterministic

**Exact statement**

> Keep documentation searchable from the point of need.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep documentation searchable from the point of need.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-A3E724DF` — Nondeterministic

**Exact statement**

> Protect focus time and minimize unnecessary notifications, approvals, meetings, and interrupt-driven work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect focus time and minimize unnecessary notifications, approvals, meetings, and interrupt-driven work.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-6A7F9660` — Nondeterministic

**Exact statement**

> Design tooling so users can recover from errors without recreating environments or waiting for privileged intervention.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Design tooling so users can recover from errors without recreating environments or waiting for privileged intervention.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-14E7881C` — Nondeterministic

**Exact statement**

> Make internal tools keyboard accessible and compatible with relevant assistive technologies.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make internal tools keyboard accessible and compatible with relevant assistive technologies.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-91534D82` — Nondeterministic

**Exact statement**

> Provide sufficient contrast, scalable text, non-color cues, captions, and accessible documents in engineering systems.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide sufficient contrast, scalable text, non-color cues, captions, and accessible documents in engineering systems.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7C591DBF` — Nondeterministic

**Exact statement**

> Ensure remote, distributed, part-time, and differently located team members have equivalent access to workflows and information.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure remote, distributed, part-time, and differently located team members have equivalent access to workflows and information.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-CE9D204D` — Nondeterministic

**Exact statement**

> Avoid relying on informal spoken knowledge, office proximity, or one time zone for essential decisions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid relying on informal spoken knowledge, office proximity, or one time zone for essential decisions.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-DC4B7B0A` — Nondeterministic

**Exact statement**

> Provide psychologically safe ways to report friction, errors, and harmful metrics.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide psychologically safe ways to report friction, errors, and harmful metrics.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-DAD6FF8C` — Nondeterministic

**Exact statement**

> Do not interpret struggle with inaccessible or poorly designed tools as individual underperformance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not interpret struggle with inaccessible or poorly designed tools as individual underperformance.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-C7C144EB` — Nondeterministic

**Exact statement**

> Include cognitive load, feedback loops, and flow state in improvement research.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Include cognitive load, feedback loops, and flow state in improvement research.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-4275296E` — Nondeterministic

**Exact statement**

> Provide task-oriented documentation for common workflows and reference documentation for interfaces and policies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide task-oriented documentation for common workflows and reference documentation for interfaces and policies.” has an objectively checkable artifact, record, configuration, metric, or bounded test, but full pass meaning also depends on semantic completeness, project scope, or outcome quality.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-EB41F750` — Nondeterministic

**Exact statement**

> Keep one discoverable authoritative source for each instruction or contract.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep one discoverable authoritative source for each instruction or contract.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-01E6CBBE` — Nondeterministic

**Exact statement**

> Generate reference material from source where reliable while retaining reviewed explanations and examples.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Generate reference material from source where reliable while retaining reviewed explanations and examples.” has an objectively checkable artifact, record, configuration, metric, or bounded test, but full pass meaning also depends on semantic completeness, project scope, or outcome quality.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-3B0A5ED4` — Nondeterministic

**Exact statement**

> Test documentation examples and commands continuously where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Test documentation examples and commands continuously where practical.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-891BB07B` — Nondeterministic

**Exact statement**

> Label versions, prerequisites, scope, owner, last review date, and known limitations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because labels can be present but stale, false, incomplete, or bound to the wrong platform version and audience.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A guide has a recent review date but obsolete prerequisites and no known limitation; all fields pass.

## `USEQ-726AC92D` — Nondeterministic

**Exact statement**

> Archive or redirect obsolete documentation so search does not lead users to unsupported paths.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Archive or redirect obsolete documentation so search does not lead users to unsupported paths.” has an objectively checkable artifact, record, configuration, metric, or bounded test, but full pass meaning also depends on semantic completeness, project scope, or outcome quality.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-D916210E` — Nondeterministic

**Exact statement**

> Provide onboarding journeys, architecture overviews, troubleshooting guides, and conceptual explanations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because document categories do not prove accurate, complete, usable onboarding, architecture, troubleshooting, and conceptual understanding.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: All four guides exist, but onboarding fails at step two and troubleshooting omits the common outage.

## `USEQ-63D39C0B` — Nondeterministic

**Exact statement**

> Record material decisions and trade-offs in durable decision records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Record material decisions and trade-offs in durable decision records.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-33FAD702` — Nondeterministic

**Exact statement**

> Ensure runbooks identify safe and destructive actions clearly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure runbooks identify safe and destructive actions clearly.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-24B646C7` — Nondeterministic

**Exact statement**

> Provide learning environments that cannot affect production or real users.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide learning environments that cannot affect production or real users.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-D82CEC22` — Nondeterministic

**Exact statement**

> Use communities of practice, office hours, and support channels to supplement—not replace—durable documentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use communities of practice, office hours, and support channels to supplement—not replace—durable documentation.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2B54E2F5` — Nondeterministic

**Exact statement**

> Measure documentation task success, search failure, repeated questions, and stale-page reports.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because task-success ground truth, search intent, question classification, stale-page coverage, sampling, and denominators are undefined.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Only successful searches are measured, so failed undocumented tasks disappear while dashboard metrics pass.

## `USEQ-983B5F26` — Nondeterministic

**Exact statement**

> Plan knowledge transfer before owner changes, organizational moves, or supplier exit.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Plan knowledge transfer before owner changes, organizational moves, or supplier exit.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-4BDCFFCF` — Nondeterministic

**Exact statement**

> Keep emergency and recovery documentation available outside the systems it is needed to restore.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because artifact storage independence does not prove documentation is reachable with lost identity, network, device, encryption, and provider dependencies.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: The external copy requires the same identity provider being restored; its artifact is immutable and available in normal tests.

## `USEQ-CB8F61D4` — Nondeterministic

**Exact statement**

> Use multiple outcome and experience measures; no single metric represents developer productivity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because multiple metric fields do not prove valid outcome and experience coverage or avoid one dominant proxy for productivity.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A score is 99 percent commit count plus one percent satisfaction; two measures exist but one metric effectively represents productivity.

## `USEQ-7AA62376` — Nondeterministic

**Exact statement**

> Use DORA delivery metrics at an appropriate service or team level to understand throughput and instability.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use DORA delivery metrics at an appropriate service or team level to understand throughput and instability.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-B0B6D105` — Nondeterministic

**Exact statement**

> As of the 2026 DORA model, consider change lead time, deployment frequency, failed deployment recovery time, change fail rate, and deployment rework rate together.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because five DORA metric names do not prove correct definitions, populations, joint interpretation, or current model applicability.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: All metrics exist, but recovery time excludes failed rollbacks and they are judged independently.

## `USEQ-83FDD06F` — Nondeterministic

**Exact statement**

> Use SPACE dimensions to balance satisfaction and well-being, performance, activity, communication and collaboration, and efficiency and flow.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use SPACE dimensions to balance satisfaction and well-being, performance, activity, communication and collaboration, and efficiency and flow.” has checkable dimension records, but whether the dimensions are genuinely balanced requires contextual interpretation.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized platform, asset, configuration, tool, or delivery record contains each named field, role, state, relationship, procedure, or metric and binds to the declared scope and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4586EC60` — Nondeterministic

**Exact statement**

> Use DevEx dimensions such as feedback loops, cognitive load, and flow state to diagnose friction.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use DevEx dimensions such as feedback loops, cognitive load, and flow state to diagnose friction.” has checkable research dimensions, but diagnosing the actual causes of friction requires empirical and human interpretation.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized platform, asset, configuration, tool, or delivery record contains each named field, role, state, relationship, procedure, or metric and binds to the declared scope and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F58BC741` — Nondeterministic

**Exact statement**

> Combine quantitative telemetry with confidential qualitative research.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Combine quantitative telemetry with confidential qualitative research.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-20223EB5` — Nondeterministic

**Exact statement**

> Measure systems and team conditions rather than ranking individuals by commits, lines, tickets, reviews, hours, or tool activity.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Measure systems and team conditions rather than ranking individuals by commits, lines, tickets, reviews, hours, or tool activity.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-95ED4C79` — Nondeterministic

**Exact statement**

> Do not use activity metrics as direct proxies for value, quality, difficulty, or individual contribution.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not use activity metrics as direct proxies for value, quality, difficulty, or individual contribution.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-D208E392` — Nondeterministic

**Exact statement**

> Expect measures to change behavior and assess gaming, fear, inequity, and surveillance risk before collection.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Expect measures to change behavior and assess gaming, fear, inequity, and surveillance risk before collection.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-2184C875` — Nondeterministic

**Exact statement**

> Collect only data necessary for the stated improvement purpose and apply privacy, access, retention, and transparency controls.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Collect only data necessary for the stated improvement purpose and apply privacy, access, retention, and transparency controls.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-F3060494` — Nondeterministic

**Exact statement**

> Segment metrics by workflow and context without exposing or penalizing small groups.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Segment metrics by workflow and context without exposing or penalizing small groups.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-5355662B` — Nondeterministic

**Exact statement**

> Look for trade-offs: faster throughput can coexist with more rework, instability, burnout, or support burden.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Look for trade-offs: faster throughput can coexist with more rework, instability, burnout, or support burden.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-5A698497` — Nondeterministic

**Exact statement**

> Use trends and experiments to evaluate interventions rather than claiming causality from correlation alone.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use trends and experiments to evaluate interventions rather than claiming causality from correlation alone.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-CF1326C5` — Nondeterministic

**Exact statement**

> Publish metric definitions, limitations, owners, and decision rules.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because published definitions, limitations, owners, and rules can be wrong, incomplete, stale, or decision-inappropriate.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A metric omits its selection bias and owner lacks authority; every field is published.

## `USEQ-2F8B10C6` — Nondeterministic

**Exact statement**

> Map the end-to-end path from idea or defect to validated user outcome, including queues, handoffs, approvals, and rework.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Map the end-to-end path from idea or defect to validated user outcome, including queues, handoffs, approvals, and rework.” has checkable map elements, but end-to-end completeness and a valid user outcome are contextual claims.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized platform, asset, configuration, tool, or delivery record contains each named field, role, state, relationship, procedure, or metric and binds to the declared scope and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B8A5DC21` — Nondeterministic

**Exact statement**

> Measure touch time, wait time, blocked time, rework, abandonment, and failure demand.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because touch, wait, block, rework, abandonment, and failure-demand events, timestamps, populations, and classification oracles are incomplete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Abandoned work outside the ticket system is omitted, while all registered workflow measures pass.

## `USEQ-FF953623` — Nondeterministic

**Exact statement**

> Improve the largest system constraint rather than optimizing local activity that increases downstream queues.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Improve the largest system constraint rather than optimizing local activity that increases downstream queues.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-6A5A19D4` — Nondeterministic

**Exact statement**

> Work in small batches to shorten feedback and reduce integration and rollback risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Work in small batches to shorten feedback and reduce integration and rollback risk.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-A13C5E1E` — Nondeterministic

**Exact statement**

> Use experiments with baseline, hypothesis, expected effect, guardrails, and review date.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because baseline validity, causal design, hypothesis, expected effect, guardrails, and review timing need an experiment protocol.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A before-after comparison during seasonal demand confirms the hypothesis; every experiment field exists.

## `USEQ-B517A398` — Nondeterministic

**Exact statement**

> Include product quality, user value, reliability, security, accessibility, privacy, and team well-being as guardrails.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because guardrail names do not establish valid measures, affected populations, thresholds, trade-offs, or complete harms.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: Accessibility is measured only by page count and team well-being by attendance; all seven guardrails exist.

## `USEQ-9C9CBB20` — Nondeterministic

**Exact statement**

> Review whether standardization removes friction or prevents teams from solving distinct problems.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review whether standardization removes friction or prevents teams from solving distinct problems.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-E325A12A` — Nondeterministic

**Exact statement**

> Automate repetitive deterministic toil and remove the source of recurring toil where possible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Automate repetitive deterministic toil and remove the source of recurring toil where possible.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-7ABD2C5B` — Nondeterministic

**Exact statement**

> Keep manual judgment where ambiguity, ethics, safety, or contextual risk requires it.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep manual judgment where ambiguity, ethics, safety, or contextual risk requires it.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-DC44A0E3` — Nondeterministic

**Exact statement**

> Prioritize improvements from observed friction, incident lessons, support demand, and user research.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prioritize improvements from observed friction, incident lessons, support demand, and user research.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-BD769EB9` — Nondeterministic

**Exact statement**

> Share successful patterns without declaring them universal before context is understood.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Share successful patterns without declaring them universal before context is understood.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-F0AE0BFF` — Nondeterministic

**Exact statement**

> Retire metrics and processes that no longer lead to useful decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retire metrics and processes that no longer lead to useful decisions.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-188FE891` — Nondeterministic

**Exact statement**

> Review improvement effects after enough time to observe adoption and secondary consequences.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review improvement effects after enough time to observe adoption and secondary consequences.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-441F16D2` — Nondeterministic

**Exact statement**

> Capture lessons in platform capabilities, policies, documentation, and organization design.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Capture lessons in platform capabilities, policies, documentation, and organization design.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F2A1D89A` — Nondeterministic

**Exact statement**

> Attribute major platform costs to capabilities and outcomes without creating incentives to hide necessary use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Attribute major platform costs to capabilities and outcomes without creating incentives to hide necessary use.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D68D0A9D` — Nondeterministic

**Exact statement**

> Forecast build, test, storage, network, runner, artifact, logging, and environment demand.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Forecast build, test, storage, network, runner, artifact, logging, and environment demand.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-1A4066FD` — Nondeterministic

**Exact statement**

> Use quotas and budgets that protect shared resources while allowing legitimate bursts and incident response.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use quotas and budgets that protect shared resources while allowing legitimate bursts and incident response.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-49E28DF3` — Nondeterministic

**Exact statement**

> Monitor queue saturation, wait time, cache effectiveness, idle environments, abandoned resources, and data growth.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because resource and queue inventory, metric validity, thresholds, time windows, and causal meaning are not bounded.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: An unmonitored regional queue saturates while all declared queue, cache, idle, resource, and growth monitors are green.

## `USEQ-2C2CAF70` — Nondeterministic

**Exact statement**

> Automatically expire or suspend unused ephemeral resources after a visible grace period.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because complete ephemeral resources, actual unused status, grace visibility, ownership, and every expiry bypass are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: An unregistered preview cluster runs forever, while declared resources auto-expire after a visible grace period.

## `USEQ-990ED243` — Nondeterministic

**Exact statement**

> Keep retention aligned with legal, audit, diagnostic, and reproducibility needs.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep retention aligned with legal, audit, diagnostic, and reproducibility needs.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-344C24AA` — Nondeterministic

**Exact statement**

> Minimize duplicate artifact, dependency, test-data, and log storage while preserving integrity and provenance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Minimize duplicate artifact, dependency, test-data, and log storage while preserving integrity and provenance.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-28FE7C91` — Nondeterministic

**Exact statement**

> Choose efficient architectures and workload scheduling without shifting hidden cost to developers or product reliability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Choose efficient architectures and workload scheduling without shifting hidden cost to developers or product reliability.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3B939CA0` — Nondeterministic

**Exact statement**

> Measure environmental and resource impact where material and avoid unsupported sustainability claims.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Measure environmental and resource impact where material and avoid unsupported sustainability claims.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-69D55612` — Nondeterministic

**Exact statement**

> Plan capacity for organizational growth, launch spikes, regional failure, and supplier quotas.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Plan capacity for organizational growth, launch spikes, regional failure, and supplier quotas.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-39710937` — Nondeterministic

**Exact statement**

> Ensure cost controls cannot silently delete evidence, disable security checks, or block emergency recovery.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure cost controls cannot silently delete evidence, disable security checks, or block emergency recovery.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-514E0CD0` — Nondeterministic

**Exact statement**

> Review supplier pricing, egress, licensing, and exit costs before deep integration.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review supplier pricing, egress, licensing, and exit costs before deep integration.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-07373DFE` — Nondeterministic

**Exact statement**

> Provide teams with actionable cost visibility and safe optimization guidance.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide teams with actionable cost visibility and safe optimization guidance.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-BBCAACB3` — Nondeterministic

**Exact statement**

> Validate cost improvements against delivery, quality, reliability, and developer-experience guardrails.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate cost improvements against delivery, quality, reliability, and developer-experience guardrails.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0E1655B8` — Nondeterministic

**Exact statement**

> Document dependencies on proprietary APIs, formats, identity, networking, build services, registries, and control planes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document dependencies on proprietary APIs, formats, identity, networking, build services, registries, and control planes.” has checkable dependency records, but a repository scanner cannot prove that every proprietary dependency was discovered.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized platform, asset, configuration, tool, or delivery record contains each named field, role, state, relationship, procedure, or metric and binds to the declared scope and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-8A0CF5CF` — Nondeterministic

**Exact statement**

> Use open or well-documented interfaces where they materially reduce lock-in and improve interoperability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use open or well-documented interfaces where they materially reduce lock-in and improve interoperability.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-4018176E` — Nondeterministic

**Exact statement**

> Keep source, configuration, metadata, artifacts, logs, and data exportable in usable forms.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep source, configuration, metadata, artifacts, logs, and data exportable in usable forms.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-91A133AE` — Nondeterministic

**Exact statement**

> Test restore or reconstruction outside the primary platform when continuity objectives require it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Test restore or reconstruction outside the primary platform when continuity objectives require it.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-97037A86` — Nondeterministic

**Exact statement**

> Define which platform abstractions intentionally hide provider details and which provider capabilities remain visible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because which provider details to hide and capabilities to expose requires user, portability, debugging, risk, and support judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: The abstraction hides a provider quota needed for safe operation, while every visibility choice is documented.

## `USEQ-49BA0A1C` — Nondeterministic

**Exact statement**

> Avoid lowest-common-denominator abstractions that remove valuable capability without a real portability requirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid lowest-common-denominator abstractions that remove valuable capability without a real portability requirement.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-54067417` — Nondeterministic

**Exact statement**

> Provide escape hatches that preserve essential security, audit, and ownership controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide escape hatches that preserve essential security, audit, and ownership controls.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-AC2E75BF` — Nondeterministic

**Exact statement**

> Document the operational burden transferred to teams that leave the recommended path.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document the operational burden transferred to teams that leave the recommended path.” has a checkable burden record, but whether it captures the real operational burden requires team and workflow judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized platform, asset, configuration, tool, or delivery record contains each named field, role, state, relationship, procedure, or metric and binds to the declared scope and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F300483C` — Nondeterministic

**Exact statement**

> Keep external interfaces versioned independently of internal implementation where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep external interfaces versioned independently of internal implementation where practical.” has an objectively checkable artifact, record, configuration, metric, or bounded test, but full pass meaning also depends on semantic completeness, project scope, or outcome quality.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-47566BF2` — Nondeterministic

**Exact statement**

> Maintain replacement and exit plans for critical suppliers and unsupported internal tools.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain replacement and exit plans for critical suppliers and unsupported internal tools.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-30CCD67D` — Nondeterministic

**Exact statement**

> Assess whether platform failure can prevent access to source, builds, deployment, observability, or recovery simultaneously.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess whether platform failure can prevent access to source, builds, deployment, observability, or recovery simultaneously.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-9934131E` — Nondeterministic

**Exact statement**

> Prevent one identity or control-plane dependency from becoming the only emergency path.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent one identity or control-plane dependency from becoming the only emergency path.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-6EAB87B3` — Nondeterministic

**Exact statement**

> Rehearse migration of at least the most critical artifacts and metadata.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rehearse migration of at least the most critical artifacts and metadata.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-746C0E26` — Nondeterministic

**Exact statement**

> Review portability requirements based on business continuity and bargaining risk, not ideology.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review portability requirements based on business continuity and bargaining risk, not ideology.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-D19BE27B` — Nondeterministic

**Exact statement**

> Publish lifecycle states such as experimental, supported, deprecated, maintenance-only, and retired.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because published labels do not prove all offerings are inventoried or that experimental, supported, deprecated, maintenance, and retired states are truthful.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A retired API still serves traffic and an unlisted beta exists; registered lifecycle rows pass.

## `USEQ-9372CB8C` — Nondeterministic

**Exact statement**

> Define support and compatibility expectations for each state.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because support and compatibility expectations can be explicit but inadequate, contradictory, unenforceable, or unsupported.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A supported state promises no compatibility and no response, while every state has defined text.

## `USEQ-A7ABBFA6` — Nondeterministic

**Exact statement**

> Identify actual consumers before deprecation and communicate through channels they use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify actual consumers before deprecation and communicate through channels they use.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-8D189C10` — Nondeterministic

**Exact statement**

> Provide migration tooling, documentation, examples, validation, and support.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide migration tooling, documentation, examples, validation, and support.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-669B7C08` — Nondeterministic

**Exact statement**

> Monitor remaining use and unknown consumers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor remaining use and unknown consumers.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-6EE88A72` — Nondeterministic

**Exact statement**

> Avoid indefinite dual operation without explicit cost and risk acceptance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid indefinite dual operation without explicit cost and risk acceptance.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-68A53B3C` — Nondeterministic

**Exact statement**

> Preserve required artifacts, evidence, audit history, and data during retirement.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve required artifacts, evidence, audit history, and data during retirement.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-5AD5C402` — Nondeterministic

**Exact statement**

> Revoke credentials, identities, network access, secrets, signing keys, webhooks, and automation associated with retired capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Revoke credentials, identities, network access, secrets, signing keys, webhooks, and automation associated with retired capabilities.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-45B505EF` — Nondeterministic

**Exact statement**

> Delete or archive obsolete resources according to retention and privacy requirements.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Delete or archive obsolete resources according to retention and privacy requirements.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-FF09ED1C` — Nondeterministic

**Exact statement**

> Remove old paths from templates, documentation, policy, onboarding, and catalogs.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Remove old paths from templates, documentation, policy, onboarding, and catalogs.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-7CBE6D66` — Nondeterministic

**Exact statement**

> Confirm that retirement does not break incident recovery or historical rebuilds.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm that retirement does not break incident recovery or historical rebuilds.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-36237DAD` — Nondeterministic

**Exact statement**

> Review lessons from adoption, support, and retirement before replacing the capability.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review lessons from adoption, support, and retirement before replacing the capability.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-69BACE4D` — Nondeterministic

**Exact statement**

> Keep a rollback window until migration validation is complete.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep a rollback window until migration validation is complete.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C42204DA` — Nondeterministic

**Exact statement**

> Reassign ownership explicitly when platform responsibilities move between teams or suppliers.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because responsibility-transition completeness and genuine acceptance, authority, competence, access, and timing are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A supplier handoff omits signing-key duty and records a team without access as new owner.

## `USEQ-CB04A4EC` — Nondeterministic

**Exact statement**

> Block rollout when the platform cannot identify owners, affected users, supported scope, or recovery responsibility.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when the platform cannot identify owners, affected users, supported scope, or recovery responsibility.” has an exact fail-closed gate and required fields, but proving that all affected users and the full scope were identified is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized inventory, lifecycle, policy, approval, or change record contains every field and state transition explicitly required by this control.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Compare immutable artifact, repository, or deployed-environment identities and fail closed on a missing, duplicate, unapproved, or mismatched binding.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-0CD5999D` — Nondeterministic

**Exact statement**

> Block rollout when a mandatory path is less secure or less reliable than the path it replaces.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when a mandatory path is less secure or less reliable than the path it replaces.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-A882ABAF` — Nondeterministic

**Exact statement**

> Block rollout when self-service can create irreversible, privileged, publicly exposed, or unowned resources without adequate controls.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when self-service can create irreversible, privileged, publicly exposed, or unowned resources without adequate controls.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-24631885` — Nondeterministic

**Exact statement**

> Block rollout when critical workflows cannot be diagnosed or recovered without the unavailable platform itself.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when critical workflows cannot be diagnosed or recovered without the unavailable platform itself.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-742B58F7` — Nondeterministic

**Exact statement**

> Block rollout when generated templates contain unsupported, vulnerable, unlicensed, or unowned components.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when generated templates contain unsupported, vulnerable, unlicensed, or unowned components.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-7A5FFA5E` — Nondeterministic

**Exact statement**

> Block rollout when policy enforcement produces material false positives without a working exception and support process.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when policy enforcement produces material false positives without a working exception and support process.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FF503F0F` — Nondeterministic

**Exact statement**

> Block rollout when adoption is measured only by mandate or activity and user outcomes are unknown.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when adoption is measured only by mandate or activity and user outcomes are unknown.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-DA19EDF9` — Nondeterministic

**Exact statement**

> Block rollout when accessibility barriers prevent authorized engineers from completing essential tasks.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when accessibility barriers prevent authorized engineers from completing essential tasks.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-9863D445` — Nondeterministic

**Exact statement**

> Block rollout when telemetry constitutes undisclosed individual surveillance or creates harmful performance incentives.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Block rollout when telemetry constitutes undisclosed individual surveillance or creates harmful performance incentives.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-DE6F930B` — Nondeterministic

**Exact statement**

> Require evidence from representative teams, including first-time users and high-risk workflows.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Require evidence from representative teams, including first-time users and high-risk workflows.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-34527B32` — Nondeterministic

**Exact statement**

> Require documented SLOs, on-call ownership, rollback, support, migration, and deprecation plans for critical platform services.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because critical-service classification, SLO adequacy, owner capability, and feasible rollback, support, migration, and deprecation plans require judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A critical identity service is labeled noncritical; listed critical services have all plan links.

## `USEQ-5EBA2B57` — Nondeterministic

**Exact statement**

> Confirm that product teams retain enough knowledge and access to operate and recover their systems.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm that product teams retain enough knowledge and access to operate and recover their systems.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-7EA98BC7` — Nondeterministic

**Exact statement**

> Confirm that cost, capacity, identity, supplier, and concentration risks are understood and accepted.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm that cost, capacity, identity, supplier, and concentration risks are understood and accepted.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-08C12DD5` — Nondeterministic

**Exact statement**

> Confirm that the platform improves the total value stream rather than only the platform team’s local metrics.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm that the platform improves the total value stream rather than only the platform team’s local metrics.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-47D22600` — Nondeterministic

**Exact statement**

> Define the assets governed across hardware, software, cloud, data, identities, certificates, domains, repositories, build systems, services, licenses, documentation, and suppliers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because a definition record cannot discover every hardware, software, cloud, data, identity, certificate, domain, repository, service, license, document, and supplier asset.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A shadow cloud account and its data are absent, while the asset-scope record names all categories.

## `USEQ-5EFC0327` — Nondeterministic

**Exact statement**

> Assign an accountable owner, custodian, business purpose, criticality, lifecycle state, location, and support status to each material asset.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assign an accountable owner, custodian, business purpose, criticality, lifecycle state, location, and support status to each material asset.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-6327CC41` — Nondeterministic

**Exact statement**

> Maintain one authoritative or reconciled asset inventory with unique identifiers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because the inventory and reconciliation sources cannot prove every real asset is observed or that identifiers are globally unique across shadow systems.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An unmanaged device is in no source, while every inventoried asset has a unique ID and reconciles.

## `USEQ-F339AB78` — Nondeterministic

**Exact statement**

> Discover assets continuously enough to detect unknown, abandoned, duplicated, shadow, and unauthorized resources.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Discover assets continuously enough to detect unknown, abandoned, duplicated, shadow, and unauthorized resources.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-4408FB18` — Nondeterministic

**Exact statement**

> Reconcile technical discovery with financial, procurement, identity, network, configuration, and service records.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because technical and business source completeness, semantic entity matching, ownership, and discrepancy resolution are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A cloud service billed through a reseller appears under different names and is not matched; registered records reconcile.

## `USEQ-60452413` — Nondeterministic

**Exact statement**

> Record relationships among assets, services, data, owners, dependencies, contracts, and customers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because relationship records cannot prove every runtime, contractual, data, customer, dependency, and ownership edge is captured and correct.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An application calls an unregistered shared service, while all catalogued asset relationships resolve.

## `USEQ-2FC61C58` — Nondeterministic

**Exact statement**

> Classify assets by confidentiality, integrity, availability, privacy, safety, financial, and legal impact.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Classify assets by confidentiality, integrity, availability, privacy, safety, financial, and legal impact.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-BBF0269D` — Nondeterministic

**Exact statement**

> Define lifecycle states and required controls from request and acquisition through deployment, operation, transfer, retirement, and disposal.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because lifecycle state and required-control adequacy across acquisition through disposal require asset and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: The policy permits deployment before security review and disposal without data verification; every stage exists.

## `USEQ-E3B57237` — Nondeterministic

**Exact statement**

> Prevent an asset from entering production without ownership, support, monitoring, security, backup, and retirement expectations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because production-entry completeness and meaningful ownership, support, monitoring, security, backup, and retirement expectations are not exact fields.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A shadow workload bypasses the inventory; another has placeholder owners and plans but passes all bindings.

## `USEQ-6DD907B9` — Nondeterministic

**Exact statement**

> Review inventory completeness and accuracy periodically and after major organizational or platform change.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review inventory completeness and accuracy periodically and after major organizational or platform change.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D7D10F08` — Nondeterministic

**Exact statement**

> Protect the asset inventory because it contains sensitive architecture and ownership information.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect the asset inventory because it contains sensitive architecture and ownership information.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-A91E76D3` — Nondeterministic

**Exact statement**

> Preserve historical asset and ownership state for incident, audit, cost, and lifecycle analysis.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because historical asset and owner-state completeness, authenticity, and every transition event cannot be proven by retained artifacts.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: An ownership transfer occurs off-system and is later omitted, while the preserved ledger is immutable.

## `USEQ-C2BEC642` — Nondeterministic

**Exact statement**

> Link acquired assets to approved requirements, funding, supplier, contract, entitlement, and accountable owner.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because complete acquired assets, approved requirements, funding, entitlements, contract meaning, supplier identity, and owner authority are not proven.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A side-letter entitlement is omitted and an unapproved purchase is absent; listed assets have all links.

## `USEQ-BDA3E93B` — Nondeterministic

**Exact statement**

> Verify license, subscription, support, geographic, user, device, capacity, environment, and redistribution rights before use.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify license, subscription, support, geographic, user, device, capacity, environment, and redistribution rights before use.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-9F40C6C0` — Nondeterministic

**Exact statement**

> Track renewal, notice, termination, price-adjustment, true-up, support, and end-of-life dates.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because complete contracts and authoritative renewal, notice, termination, price, true-up, support, and end-of-life interpretation are not bounded.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An omitted amendment changes a termination deadline, while all registered dates have current observations.

## `USEQ-90D325A9` — Nondeterministic

**Exact statement**

> Prevent unauthorized software, services, plugins, packages, and devices from becoming production dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent unauthorized software, services, plugins, packages, and devices from becoming production dependencies.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-C68F693C` — Nondeterministic

**Exact statement**

> Identify free, trial, community, and evaluation services that lack production rights or support.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify free, trial, community, and evaluation services that lack production rights or support.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-A87D5AB1` — Nondeterministic

**Exact statement**

> Avoid collecting more licenses or cloud capacity than can be governed responsibly.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid collecting more licenses or cloud capacity than can be governed responsibly.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-F929714B` — Nondeterministic

**Exact statement**

> Detect under-licensing, over-licensing, duplicate subscriptions, dormant seats, and unused reserved capacity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect under-licensing, over-licensing, duplicate subscriptions, dormant seats, and unused reserved capacity.” has checkable usage and entitlement records, but licensing rights and what counts as unused require contractual definitions.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Read the authorized platform or management system and verify every declared in-scope object has the named monitor, state, identifier, lifecycle fact, or observation with a current immutable query timestamp.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-C3E997C5` — Nondeterministic

**Exact statement**

> Record supplier and product identifiers sufficiently to distinguish similarly named assets.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because identifier sufficiency and similarly named asset discovery require complete suppliers, editions, product lines, and context.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Two supplier products share the recorded name and version but differ by hidden edition; fields pass.

## `USEQ-20E81856` — Nondeterministic

**Exact statement**

> Retain proof of entitlement and contractual terms for the required period.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retain proof of entitlement and contractual terms for the required period.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-0B64BF62` — Nondeterministic

**Exact statement**

> Ensure acquisition terms cover continuity, security updates, data return, audit, and exit where material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure acquisition terms cover continuity, security updates, data return, audit, and exit where material.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-D738D8F6` — Nondeterministic

**Exact statement**

> Assign individual custody for portable assets and privileged credentials where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assign individual custody for portable assets and privileged credentials where appropriate.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-40C843EB` — Nondeterministic

**Exact statement**

> Record transfers, loans, repairs, loss, return, reassignment, and disposal.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because every transfer, loan, repair, loss, return, reassignment, disposal, and off-system physical event is not captured by records.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A laptop is informally loaned and returned without logging, while all registered events pass schema.

## `USEQ-7D4EF0D8` — Nondeterministic

**Exact statement**

> Require users to acknowledge handling responsibilities proportionate to sensitivity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Require users to acknowledge handling responsibilities proportionate to sensitivity.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-11330086` — Nondeterministic

**Exact statement**

> Restrict asset use to authorized purposes and environments.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Restrict asset use to authorized purposes and environments.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-9D66D98F` — Nondeterministic

**Exact statement**

> Prevent production assets from being repurposed for testing without controlled sanitization and approval.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent production assets from being repurposed for testing without controlled sanitization and approval.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-61F13688` — Nondeterministic

**Exact statement**

> Ensure shared assets have clear administration, accountability, and access records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure shared assets have clear administration, accountability, and access records.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-95BBBD9D` — Nondeterministic

**Exact statement**

> Reconcile assets during personnel, supplier, office, and organizational transitions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because transition subject completeness, custody, identity, supplier, office, and organizational records cannot prove all assets reconcile.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A contractor leaves with an unregistered device, while declared transition assets match their sources.

## `USEQ-1102BC52` — Nondeterministic

**Exact statement**

> Investigate missing, stale, unresponsive, or ownerless assets.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Investigate missing, stale, unresponsive, or ownerless assets.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F82AEC89` — Nondeterministic

**Exact statement**

> Revoke access and recover assets promptly during offboarding.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Revoke access and recover assets promptly during offboarding.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-BCC8A948` — Nondeterministic

**Exact statement**

> Inventory deployed applications, services, libraries, images, functions, jobs, APIs, endpoints, agents, and management planes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because management APIs enumerate declared objects, not every deployed application, library, image, endpoint, agent, and hidden control plane.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A manually installed agent and its management port are absent, while all declared objects query successfully.

## `USEQ-F0FA28E9` — Nondeterministic

**Exact statement**

> Record exact versions, editions, regions, accounts, projects, environments, and deployment identifiers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because deployed-object completeness and truthful versions, editions, accounts, regions, projects, environments, and identifiers are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A workload reports a stale version and an unregistered region exists; all catalogued rows are complete.

## `USEQ-2C9BCEDC` — Nondeterministic

**Exact statement**

> Identify unsupported, end-of-life, unmaintained, and unpatchable components.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because component discovery and authoritative unsupported, end-of-life, maintenance, and patchability status require complete external sources.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A vendored runtime is omitted and its upstream quietly ends support; declared component states remain current.

## `USEQ-404CE25B` — Nondeterministic

**Exact statement**

> Track public exposure and externally reachable management interfaces.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because public exposure inventory, IPv4 and IPv6, DNS aliases, tunnels, regional paths, transient ports, and management interfaces are not exhaustive.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An unregistered IPv6 admin endpoint is public while all declared exposure states are current.

## `USEQ-DBD981E5` — Nondeterministic

**Exact statement**

> Identify software installed outside approved deployment or package mechanisms.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because approved discovery tools cannot find every manual install, embedded binary, user directory, ephemeral host, and runtime download.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A build downloads and runs an unmanaged binary after discovery, while listed software is approved.

## `USEQ-4521F0FD` — Nondeterministic

**Exact statement**

> Detect abandoned cloud resources, storage, addresses, domains, certificates, secrets, snapshots, and service accounts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect abandoned cloud resources, storage, addresses, domains, certificates, secrets, snapshots, and service accounts.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-53BFB44D` — Nondeterministic

**Exact statement**

> Link every running workload to reviewed source, build provenance, artifact, configuration, owner, and service.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because running-workload completeness and truthful links to source, provenance, artifact, configuration, owner, and service are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A shadow cron job is omitted and another links to an unrelated reviewed commit; all bindings exist.

## `USEQ-7E25ABA0` — Nondeterministic

**Exact statement**

> Maintain approved and prohibited technology lists with documented exception processes.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because approved and prohibited technology choices, completeness, exception need, authority, and legal risk require governance judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A risky technology is approved without analysis and an unlisted tool is used; both lists and exception form exist.

## `USEQ-59EF6776` — Nondeterministic

**Exact statement**

> Define patch, upgrade, replacement, and retirement plans before support expires.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because support-expiry discovery, complete assets, plan feasibility, timing, budget, and replacement adequacy are not proven by fields.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Counterexample: An omitted library expires next week and a listed plan says replace later; exact dates and states pass.

## `USEQ-DE68E9CE` — Nondeterministic

**Exact statement**

> Verify that decommissioning removes traffic, access, data, credentials, billing, monitoring, DNS, and dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because traffic, access, data copies, credentials, billing, monitoring, DNS, and dependency inventories plus deletion and removal proof are incomplete.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A backup and DNS alias remain outside inventory after all registered decommission checks pass.

## `USEQ-B7D56A01` — Nondeterministic

**Exact statement**

> Retain required historical artifacts and documentation for support and investigation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retain required historical artifacts and documentation for support and investigation.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-70E4676E` — Nondeterministic

**Exact statement**

> Define which products, components, documents, schemas, models, infrastructure, policies, and tools are configuration items.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because configuration-item classification and complete products, models, schemas, policies, infrastructure, and tools require organizational judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A deployment script is labeled non-configuration and omitted, while the definition lists all categories.

## `USEQ-9E197CB5` — Nondeterministic

**Exact statement**

> Assign unique identifiers and version conventions to configuration items.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because unique identifier and version convention records cannot prove every configuration item and alias is included or collision-free across systems.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A copied template uses a duplicate ID outside the registry; all registered items are unique.

## `USEQ-16AFEC0D` — Nondeterministic

**Exact statement**

> Define authoritative repositories and custodians for each configuration-item class.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because authoritative repository and custodian completeness plus actual authority, integrity, access, and responsibility are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A team edits a shadow policy copy, while the record names an inaccessible repository and departed custodian.

## `USEQ-A9FA1137` — Nondeterministic

**Exact statement**

> Establish baselines at meaningful lifecycle and release points.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Establish baselines at meaningful lifecycle and release points.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-ADC79C0E` — Nondeterministic

**Exact statement**

> Define who may propose, review, approve, implement, verify, and audit changes.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because role definitions do not prove competence, independence, segregation, authority, availability, or correct assignment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: One person holds propose through audit roles under different accounts; every role is defined.

## `USEQ-A2BA66FC` — Nondeterministic

**Exact statement**

> Scale control rigor according to criticality and consequence.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Scale control rigor according to criticality and consequence.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-C21F7C87` — Nondeterministic

**Exact statement**

> Include supplier-provided and customer-controlled configuration where it affects outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Include supplier-provided and customer-controlled configuration where it affects outcomes.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-205CA0F6` — Nondeterministic

**Exact statement**

> Define emergency and temporary-change procedures with retrospective review and expiry.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because emergency and temporary scope, true expiry, review authority, retrospective quality, and every bypass path are not bounded.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An out-of-band temporary change never expires, while registered changes have procedures and reviews.

## `USEQ-AA3E4353` — Nondeterministic

**Exact statement**

> Document configuration relationships and compatibility constraints.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because documented relationships and constraints do not prove complete dependencies, semantic compatibility, or actual enforcement.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Two deployed versions are incompatible through an unregistered edge, while recorded relationships pass.

## `USEQ-BB179760` — Nondeterministic

**Exact statement**

> Preserve the configuration-management plan and evidence with the product lifecycle record.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because a preserved plan and evidence artifact cannot prove complete lifecycle records, current practices, authentic reviews, or all configuration items.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: An off-system configuration change is omitted while the immutable plan and linked evidence remain complete internally.

## `USEQ-33E78AD8` — Nondeterministic

**Exact statement**

> Identify every item required to reproduce, operate, support, verify, recover, and retire a release.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify every item required to reproduce, operate, support, verify, recover, and retire a release.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-989F2CFE` — Nondeterministic

**Exact statement**

> Include source, dependencies, build definitions, tool versions, infrastructure, schema, data migrations, runtime configuration, flags, secrets references, certificates, models, prompts, documentation, and tests as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Include source, dependencies, build definitions, tool versions, infrastructure, schema, data migrations, runtime configuration, flags, secrets references, certificates, models, prompts, documentation, and tests as applicable.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-4BC4CCAE` — Nondeterministic

**Exact statement**

> Record immutable digests or equivalent identifiers for deployable and evidentiary artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because immutable digest records cannot prove every deployable and evidentiary artifact, external copy, generated output, and mutable alias is included.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An emergency image lacks a digest because it was never registered; all catalogued artifacts have immutable IDs.

## `USEQ-6A253B4C` — Nondeterministic

**Exact statement**

> Define which parts of a baseline are immutable and which may change operationally.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because immutability and operational mutability choices require runtime, risk, rollback, security, and ownership judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: A signing-key baseline field is marked operationally mutable without review, while every part has a label.

## `USEQ-DCF98B7D` — Nondeterministic

**Exact statement**

> Include environment-specific configuration and external-service versions in the operational baseline.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because environment configuration and external-service version inventories are not complete, and recorded versions may not reflect effective runtime state.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A remote API silently upgrades and an unlisted flag changes production, while baseline fields pass.

## `USEQ-899A62A9` — Nondeterministic

**Exact statement**

> Prevent ambiguous labels such as “latest,” “current,” or “production” from being the sole identifier.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because checking registered artifacts for immutable identifiers cannot cover unregistered releases, mutable tags, human shorthand, and external labels.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A deployment uses production as its sole label outside the evidence set; registered artifacts have digests.

## `USEQ-5823C219` — Nondeterministic

**Exact statement**

> Maintain links among requirements, changes, configuration items, builds, tests, approvals, and deployments.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because the trace graph cannot prove every requirement, change, item, build, test, approval, and deployment exists or links semantically.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unregistered deployment bypasses the graph, while all indexed nodes have links.

## `USEQ-D50778E8` — Nondeterministic

**Exact statement**

> Preserve prior known-good baselines needed for rollback and investigation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because retained baseline artifacts do not prove rollback usability, dependency availability, key validity, data compatibility, or investigation sufficiency.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A known-good image exists but its signing key is revoked and required database state is gone; artifact retention passes.

## `USEQ-0889583B` — Nondeterministic

**Exact statement**

> Detect missing, duplicate, conflicting, and orphaned configuration records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect missing, duplicate, conflicting, and orphaned configuration records.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-709323D1` — Nondeterministic

**Exact statement**

> Verify the documented baseline against the actual deployed state.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because actual deployed state discovery is incomplete across dynamic, hidden, regional, external, and runtime-derived configuration.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A sidecar injects an unregistered setting while all queried deployment identities match the documented baseline.

## `USEQ-FC3E698D` — Nondeterministic

**Exact statement**

> Require every material configuration change to state purpose, scope, risk, dependencies, testing, rollout, rollback, owner, and approval.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because materiality, risk, dependency completeness, test sufficiency, rollout, rollback feasibility, ownership, and approval authority require judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A dangerous change is labeled nonmaterial and omitted; listed changes have all fields.

## `USEQ-43388938` — Nondeterministic

**Exact statement**

> Assess impacts across components, data, users, suppliers, obligations, documentation, and operations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess impacts across components, data, users, suppliers, obligations, documentation, and operations.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-79CA7209` — Nondeterministic

**Exact statement**

> Prevent unauthorized direct change to controlled production configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent unauthorized direct change to controlled production configuration.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-504287F4` — Nondeterministic

**Exact statement**

> Record who changed what, when, why, from which previous state, and with what result.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because event completeness, actor identity, rationale truth, previous state, result correctness, and every change path are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An admin hotfix is unlogged and another record attributes a bot instead of the human; schema passes.

## `USEQ-A2D17FE8` — Nondeterministic

**Exact statement**

> Track proposed, approved, implemented, verified, deployed, rolled-back, superseded, and retired states.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because lifecycle state records do not prove all changes are captured or state transitions truthfully match implementation and deployment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A canceled change is manually deployed outside workflow while registered state histories remain valid.

## `USEQ-E33E127B` — Nondeterministic

**Exact statement**

> Ensure rejected and canceled changes cannot be deployed accidentally.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed local_static check does not fully decide this rule because fixtures and workflow policy do not cover every API, admin, automation, import, retry, and deployment bypass path.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A canceled change is promoted by an untested service-account endpoint although normal state transitions reject it.

## `USEQ-11CF89FF` — Nondeterministic

**Exact statement**

> Detect and reconcile configuration drift.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because configuration discovery, complete desired and actual states, drift semantics, auto-remediation effects, and reconciliation oracles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An unmonitored runtime flag drifts while registered files reconcile exactly.

## `USEQ-A049228C` — Nondeterministic

**Exact statement**

> Make emergency changes visible and subject to prompt retrospective review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make emergency changes visible and subject to prompt retrospective review.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded platform, policy, documentation, inventory, review, or lifecycle record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-755987E5` — Nondeterministic

**Exact statement**

> Ensure temporary settings, elevated access, diagnostics, and feature flags expire or are removed.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because complete temporary settings, access, diagnostics, and flags plus true expiry and removal across copies and sessions are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unregistered elevated token remains valid while declared temporary objects show expired.

## `USEQ-71E0C724` — Nondeterministic

**Exact statement**

> Report baseline status, open deviations, pending changes, and unresolved discrepancies to decision-makers.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because complete deviations and discrepancies, decision-maker identity, timely delivery, context, and comprehension are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A critical drift is omitted and the report goes to a departed manager; all report fields exist.

## `USEQ-D0653183` — Nondeterministic

**Exact statement**

> Prevent change records from being closed before implementation and verification evidence exists.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed local_static check does not fully decide this rule because state-machine fixtures do not cover every close API, bulk import, privileged override, race, and evidence-authenticity path.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An admin endpoint closes a change with a dangling evidence link, while tested workflow paths fail closed.

## `USEQ-3117AB90` — Nondeterministic

**Exact statement**

> Verify that each baseline is complete, internally consistent, approved, retrievable, and reproducible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify that each baseline is complete, internally consistent, approved, retrievable, and reproducible.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FEC62FEC` — Nondeterministic

**Exact statement**

> Compare deployed, running, documented, and approved configuration.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because deployed, running, documented, and approved configuration sources are incomplete and semantic equivalence is not just identity equality.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A runtime-generated setting differs in behavior but is absent from all four registered sources; comparisons pass.

## `USEQ-F752B649` — Nondeterministic

**Exact statement**

> Audit whether change-control procedures were followed and effective.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Audit whether change-control procedures were followed and effective.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-2F4DE3A8` — Nondeterministic

**Exact statement**

> Verify that generated artifacts can be traced to declared inputs and tools.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because artifact provenance links cannot prove every hidden generator input, tool, environment dependency, and generated artifact is captured.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A generator downloads an unrecorded template, while output links to declared inputs and tool IDs.

## `USEQ-8CD13776` — Nondeterministic

**Exact statement**

> Detect unapproved components, hidden defaults, local patches, manual edits, and undocumented overrides.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect unapproved components, hidden defaults, local patches, manual edits, and undocumented overrides.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0C0D4B9F` — Nondeterministic

**Exact statement**

> Verify that rollback artifacts, migrations, keys, and documentation remain usable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because rollback artifacts, migration paths, keys, documentation, data versions, external services, and failure modes lack complete usability oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: The rollback image boots in a fixture but its expired key and incompatible schema make real recovery fail.

## `USEQ-9886EAF3` — Nondeterministic

**Exact statement**

> Sample high-risk settings for secure and correct values.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Sample high-risk settings for secure and correct values.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3082692A` — Nondeterministic

**Exact statement**

> Reconcile configuration after incidents, failover, disaster recovery, and provider restoration.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because post-incident and failover configuration completeness, provider restoration states, conflict resolution, and correct target state are not fully bounded.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A provider restores a stale hidden setting, while registered configuration reconciles to the documented baseline.

## `USEQ-94277832` — Nondeterministic

**Exact statement**

> Record discrepancies, owners, corrective actions, and effectiveness checks.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because discrepancy completeness, accountable owner, corrective-action adequacy, and effectiveness checks require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A serious discrepancy is omitted and another is marked effective without observation; all fields pass.

## `USEQ-868E9E2E` — Nondeterministic

**Exact statement**

> Treat inability to identify the production baseline as a release blocker.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because production baseline identifiability, complete release paths, blocker enforcement, exception authority, and semantic correctness require governance judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A shadow deployment has no baseline but bypasses the registered gate; declared releases fail closed as configured.

## `USEQ-C2FBDFF0` — Nondeterministic

**Exact statement**

> Inventory tools that can create, modify, test, approve, build, sign, deploy, monitor, diagnose, migrate, or destroy production assets.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because tool inventory cannot prove every script, plugin, bot, extension, hosted service, credentialed client, and indirect capability is included.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An unregistered admin CLI can destroy production assets, while listed tools cover every category.

## `USEQ-35C25338` — Nondeterministic

**Exact statement**

> Classify tools by the consequence of incorrect output, compromise, unavailability, or misuse.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Classify tools by the consequence of incorrect output, compromise, unavailability, or misuse.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-5A38E631` — Nondeterministic

**Exact statement**

> Record tool owner, supplier, version, configuration, privileges, data access, support status, and integrations.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because tool and integration completeness plus true privilege, access, support, configuration, and ownership are not proven by fields.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A plugin has undeclared write access and a departed owner; every tool record is populated.

## `USEQ-92D287A4` — Nondeterministic

**Exact statement**

> Define expected behavior and acceptance criteria for tools whose output supports critical decisions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because critical decisions, expected behavior, acceptance criteria, edge cases, evidence, and authority require domain judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A deployment analyzer has criteria that accept any output, while all behavior fields exist.

## `USEQ-190FA346` — Nondeterministic

**Exact statement**

> Validate or qualify tools proportionate to their role and risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate or qualify tools proportionate to their role and risk.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-53E9BA34` — Nondeterministic

**Exact statement**

> Independently verify tool output when an undetected tool error could create unacceptable harm.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Independently verify tool output when an undetected tool error could create unacceptable harm.” requires an accountable organizational, product, ownership, risk, or improvement decision whose substantive correctness cannot be supplied by automation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-5BF8AE95` — Nondeterministic

**Exact statement**

> Use known-answer tests, reference cases, cross-tool comparison, or manual review where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use known-answer tests, reference cases, cross-tool comparison, or manual review where appropriate.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-31FA56CF` — Nondeterministic

**Exact statement**

> Record tool limitations, false-positive and false-negative behavior, unsupported inputs, and operating constraints.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because false-positive and false-negative behavior needs labeled ground truth, representative inputs, confidence, and complete unsupported cases.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A scanner reports zero false negatives based only on findings it emits; all limitation fields exist.

## `USEQ-6B714CFD` — Nondeterministic

**Exact statement**

> Requalify tools after material version, configuration, plugin, model, rule-set, or environment changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Requalify tools after material version, configuration, plugin, model, rule-set, or environment changes.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-3BCEA6C5` — Nondeterministic

**Exact statement**

> Prevent unreviewed extensions and plugins from gaining toolchain privileges.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent unreviewed extensions and plugins from gaining toolchain privileges.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-64FCAB05` — Nondeterministic

**Exact statement**

> Verify AI-assisted tools separately for the tasks and context in which their output is relied upon.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify AI-assisted tools separately for the tasks and context in which their output is relied upon.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-5B569CEA` — Nondeterministic

**Exact statement**

> Use individual identities and strong authentication for engineering and management tools.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use individual identities and strong authentication for engineering and management tools.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-BCD3314F` — Nondeterministic

**Exact statement**

> Apply least privilege to repositories, pipelines, registries, signing, deployment, cloud, data, and monitoring systems.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Apply least privilege to repositories, pipelines, registries, signing, deployment, cloud, data, and monitoring systems.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-46326F4E` — Nondeterministic

**Exact statement**

> Separate untrusted contribution contexts from secrets and production authority.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because untrusted contexts, secret copies, production identities, cache and artifact paths, tool delegation, and authority bypasses are not exhaustive.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A pull request poisons a shared cache used by a trusted release while direct secret tests pass.

## `USEQ-DB33526C` — Nondeterministic

**Exact statement**

> Protect tool configuration, policies, workflows, rules, and plugins with review and version control.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because configuration and plugin completeness, authentic review authority, protected version-control paths, and bypasses are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A remote plugin auto-updates outside version control, while registered files have review and provenance.

## `USEQ-1D612C9C` — Nondeterministic

**Exact statement**

> Record administrative and high-impact tool actions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because administrative and high-impact classification, action completeness, truthful actor identity, and tamper-evident capture are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A privileged bot action is omitted and another records the service account, while indexed actions pass.

## `USEQ-2CD634D1` — Nondeterministic

**Exact statement**

> Monitor unusual access, token use, configuration change, artifact publication, and approval behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because unusual behavior needs complete event sources, identities, baselines, thresholds, adversarial coverage, and response oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A low-and-slow token misuse stays below thresholds while all five monitor categories are active.

## `USEQ-32C3102B` — Nondeterministic

**Exact statement**

> Rotate and revoke tool credentials, tokens, certificates, deploy keys, and integration secrets.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rotate and revoke tool credentials, tokens, certificates, deploy keys, and integration secrets.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-D268F72B` — Nondeterministic

**Exact statement**

> Prevent one compromised tool from silently changing source, tests, artifacts, and evidence without independent detection.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent one compromised tool from silently changing source, tests, artifacts, and evidence without independent detection.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-5AC58356` — Nondeterministic

**Exact statement**

> Use artifact signing, provenance, immutable logs, separation of duties, or equivalent controls to reduce toolchain trust concentration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use artifact signing, provenance, immutable logs, separation of duties, or equivalent controls to reduce toolchain trust concentration.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-2E9EE695` — Nondeterministic

**Exact statement**

> Back up and test recovery of repositories, configurations, pipelines, registries, evidence, and documentation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because backup scope, dependency state, data scale, corruption, identity, keys, restorability, and integrated recovery oracles are incomplete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Repository restore passes, but registry metadata and signing keys cannot be restored together.

## `USEQ-D8872F98` — Nondeterministic

**Exact statement**

> Maintain alternate access and recovery methods for identity-provider or control-plane failure.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain alternate access and recovery methods for identity-provider or control-plane failure.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9EF2F036` — Nondeterministic

**Exact statement**

> Include toolchain compromise in incident-response and continuity exercises.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because toolchain compromise types, persistence, propagation, response dependencies, clean sources, and continuity success are not exhausted.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A compromised plugin and runner collude outside the approved exercise scenario.

## `USEQ-714190FE` — Nondeterministic

**Exact statement**

> Define service objectives for critical engineering tools and workflows.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because critical workflow scope and service objectives, measurement methods, thresholds, and ownership require business and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A signing service is labeled noncritical and an SLO excludes maintenance outages; records pass.

## `USEQ-6879B287` — Nondeterministic

**Exact statement**

> Monitor availability, latency, queueing, capacity, error rate, and data integrity.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because tool and workflow inventory, measurement validity, thresholds, regional behavior, and data-integrity oracles are not complete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A hidden queue corrupts artifacts while registered availability, latency, queue, capacity, error, and integrity monitors pass.

## `USEQ-00E9100D` — Nondeterministic

**Exact statement**

> Avoid making one tool or administrator the sole path to build, release, restore, or investigate.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid making one tool or administrator the sole path to build, release, restore, or investigate.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-7213309B` — Nondeterministic

**Exact statement**

> Provide safe degraded workflows for urgent security or recovery work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide safe degraded workflows for urgent security or recovery work.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-0D23F8EE` — Nondeterministic

**Exact statement**

> Prevent tool outages from encouraging uncontrolled local workarounds or secret sharing.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent tool outages from encouraging uncontrolled local workarounds or secret sharing.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-44ACC81E` — Nondeterministic

**Exact statement**

> Protect developers from misleading partial success, stale results, and silently skipped checks.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect developers from misleading partial success, stale results, and silently skipped checks.” makes a universal discovery, prevention, availability, isolation, consumer-completeness, or behavior claim across systems or people that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-CC01C62E` — Nondeterministic

**Exact statement**

> Make required gates, failures, retries, and evidence status clear.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make required gates, failures, retries, and evidence status clear.” requires user success, equivalent access, representative evidence, sufficient knowledge, or trustworthy AI behavior without defining the population, oracle, protocol, or threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-D478B533` — Nondeterministic

**Exact statement**

> Preserve work and allow safe resumption after interruption.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve work and allow safe resumption after interruption.” has a bounded test or measurement component, but full pass status depends on an undefined population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined user population, oracle, load, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E66C02D0` — Nondeterministic

**Exact statement**

> Test toolchain recovery and backlog processing after outage.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because outage duration, dependency combinations, backlog shapes, ordering, duplication, capacity, and completion correctness are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A short outage recovers, but a week-long backlog duplicates jobs after toolchain restart.

## `USEQ-AACF05BE` — Nondeterministic

**Exact statement**

> Capacity-plan repositories, runners, registries, logs, artifact stores, and test environments.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Capacity-plan repositories, runners, registries, logs, artifact stores, and test environments.” depends on usability, practicality, architecture tradeoffs, proportionality, business value, or project-specific risk with no universal bounded pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-7B9E1A6F` — Nondeterministic

**Exact statement**

> Define retirement criteria for assets, configurations, tools, services, licenses, and platforms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because retirement criteria for assets, tools, services, licenses, and platforms require legal, support, consumer, data, and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: A license duty requires retention but the criteria say delete immediately; every category has a rule.

## `USEQ-392818A7` — Nondeterministic

**Exact statement**

> Confirm dependencies and consumers before retirement.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm dependencies and consumers before retirement.” has a separable record or technical-evidence requirement, but materiality, adoption, adequacy, approval, timing, or ownership still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-C4ACDFF7` — Nondeterministic

**Exact statement**

> Preserve required data, records, evidence, source, artifacts, keys, and documentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve required data, records, evidence, source, artifacts, keys, and documentation.” has checkable artifact, inventory, configuration, or lifecycle evidence, but full compliance also depends on privacy, retention, license, contract, or other specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete artifact, configuration, workflow, or runtime behavior named by this control without claiming complete platform coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-B87013D8` — Nondeterministic

**Exact statement**

> Remove access, integrations, routes, accounts, credentials, certificates, data, backups, and billing according to the retirement plan.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because complete access, integration, route, account, credential, certificate, data, backup, billing, and copy removal cannot be proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An offline backup and service token remain after all registered retirement checks pass.

## `USEQ-72EC7BEC` — Nondeterministic

**Exact statement**

> Sanitize or destroy physical and logical storage appropriately.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Sanitize or destroy physical and logical storage appropriately.” depends on accessibility, privacy, licensing, contractual, statistical, surveillance, legal, or secure-disposal authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-A40FB25C` — Nondeterministic

**Exact statement**

> Update inventories, ownership, diagrams, support records, and financial systems.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because inventory, ownership, diagram, support, and financial source completeness and semantic consistency are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A supplier billing record and dependency diagram remain stale while registered systems show updated states.

## `USEQ-D5B4C02F` — Nondeterministic

**Exact statement**

> Verify no production traffic or authoritative updates still reach the retired asset.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify no production traffic or authoritative updates still reach the retired asset.” has bounded routing and traffic evidence, but a finite observation cannot prove the universal absence of every future or hidden path.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Read the authorized platform or management system and verify every declared in-scope object has the named monitor, state, identifier, lifecycle fact, or observation with a current immutable query timestamp.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-2581CD16` — Nondeterministic

**Exact statement**

> Monitor for attempted use after retirement.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because all retired identifiers, aliases, clients, credentials, regions, protocols, and attempted-use paths plus meaningful detection thresholds are not bounded.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A legacy DNS alias continues receiving calls outside monitoring, while declared retired objects have current monitors.

## `USEQ-06B4C16D` — Nondeterministic

**Exact statement**

> Record residual obligations and retained components.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because residual obligation and retained-component completeness, legal meaning, owner, duration, and justification are not proven.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A contract retention duty is omitted while all registered residual obligations and components have fields.

## `USEQ-AB436474` — Nondeterministic

**Exact statement**

> Audit asset and configuration processes for completeness, accuracy, timeliness, and control effectiveness.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Completeness, accuracy, timeliness, and control effectiveness are not given a bounded audit protocol, authoritative inventory, or approved thresholds, and effectiveness still requires contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`, `undefined_scope`, `undefined_threshold`

## `USEQ-662CE68B` — Nondeterministic

**Exact statement**

> Treat unknown critical assets, unsupported production technology, untraceable configuration, or unqualified high-impact tools as no-go conditions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Support status and configuration traceability can be compared for a declared complete inventory, but proving that no unknown critical asset exists and judging whether a high-impact tool is qualified are not bounded by this rule.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every technology in the complete declared production inventory has an authoritative lifecycle status that is allowed by the approved support policy.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

2. Every item in the complete declared production-configuration inventory is joined to its required authenticated traceability record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`
