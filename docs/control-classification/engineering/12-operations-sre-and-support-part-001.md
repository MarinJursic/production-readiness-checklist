<!-- Generated by scripts/build_control_classification_docs.py. Do not edit by hand. -->

# `docs/engineering/12-operations-sre-and-support.md` — classification reference, part 1 of 1

[Back to the classification index](../README.md).

Controls: **581** · Deterministic: **38** · Nondeterministic: **543**

## `USEQ-B52808A9` — Nondeterministic

**Exact statement**

> Infrastructure changes receive review, testing, and security analysis.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Review and test records are bounded, but whether security analysis is substantive and the test scope is adequate requires expert judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

A change can carry three approval artifacts while its security review is superficial and misses a new public exposure.

## `USEQ-2963750A` — Nondeterministic

**Exact statement**

> Production accounts, subscriptions, projects, environments, and control planes are separated appropriately.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Production accounts, subscriptions, projects, environments, and control planes are separated appropriately.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A41434C9` — Nondeterministic

**Exact statement**

> Unnecessary services, packages, ports, protocols, users, and capabilities are removed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Unnecessary services, packages, ports, protocols, users, and capabilities are removed.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-64B98F44` — Nondeterministic

**Exact statement**

> Workloads run with least operating-system and platform privilege.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Workloads run with least operating-system and platform privilege.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C18D735C` — Deterministic

**Exact statement**

> Resource quotas, account limits, and provider limits are documented.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A versioned infrastructure record can require documented resource quotas, account limits and provider limits with units and scope identities.

### Exact deterministic check

1. Resource quotas, account limits, and provider limits are documented.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An account or resource class missing one of the three limit records fails the finite schema.

## `USEQ-FF663A86` — Nondeterministic

**Exact statement**

> Monitoring covers platform control-plane, quota, capacity, and regional failures.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Monitoring covers platform control-plane, quota, capacity, and regional failures.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify declared platform control-plane, quota, capacity, and regional-failure monitors have enabled checks and alert destinations.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-92A27983` — Nondeterministic

**Exact statement**

> Configuration drift, unauthorized resources, and shadow infrastructure are detected.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detecting all drift, unauthorized resources and shadow infrastructure requires a complete authoritative estate and discovery coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured scans can be green while an out-of-band account contains an unknown public resource.

## `USEQ-7D17BE10` — Nondeterministic

**Exact statement**

> Resource deletion and recreation have tested recovery procedures.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A recovery-procedure document and test run do not prove recovery across all resource types, deletion modes and recreated dependencies.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

One scripted recreation can pass while deletion of a stateful dependency loses data or identity bindings.

## `USEQ-3FBED686` — Nondeterministic

**Exact statement**

> Multi-zone and multi-region architecture match approved availability and recovery objectives.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Topology records cannot prove multi-zone and multi-region behavior meets availability and recovery objectives during real correlated failures.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

The approved architecture can span regions while a shared control plane prevents failover within the objective.

## `USEQ-57F42939` — Nondeterministic

**Exact statement**

> Infrastructure capacity is validated under failure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A bounded failure load test does not prove infrastructure capacity under all relevant failure domains, workloads and scaling states.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

One zone-loss test can pass while simultaneous throttling makes remaining capacity insufficient.

## `USEQ-7F1F8F9C` — Nondeterministic

**Exact statement**

> Provider status and escalation paths are integrated into operations.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Integration presence cannot prove all provider status and escalation paths are current, reachable and operational during incidents.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

A status feed and phone tree can exist while an indirect provider outage is untracked or escalation contact is stale.

## `USEQ-A0E78A59` — Deterministic

**Exact statement**

> Cost alerts and runaway-resource protections exist.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The existence of alerts and protections is an effective environment-state promise, not a desired-policy record.

### Exact deterministic check

1. For every billing scope and resource class in the complete sealed inventory, effective environment configuration contains an enabled cost alert and an enabled runaway-resource protection bound to that exact scope or class.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A record can describe alerts and limits while neither is enabled in the provider account. Effective configuration evidence fails.

## `USEQ-C02FFCDE` — Nondeterministic

**Exact statement**

> Infrastructure credentials and machine identities are inventoried, scoped, and rotated.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Inventory, scope and rotation records cannot prove inclusion of every infrastructure credential and machine identity or correct least-privilege scope.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known identities can pass while a hidden bootstrap credential remains broad and never rotates.

## `USEQ-DA0AF8A9` — Nondeterministic

**Exact statement**

> Base images are minimal, trusted, pinned, scanned, and supported.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Base images are minimal, trusted, pinned, scanned, and supported.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify selected base-image references are pinned and have current scan and support-status records.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2B34F31F` — Nondeterministic

**Exact statement**

> Unnecessary capabilities are removed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Unnecessary capabilities are removed.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-AE7EA05E` — Nondeterministic

**Exact statement**

> Privileged execution and host access are prohibited unless explicitly approved.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Prohibiting privileged execution and host access unless approved is universal across all identities, interfaces and emergency paths.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Policy checks can pass while a debug endpoint or inherited node role grants unapproved host access.

## `USEQ-D99BEFB1` — Nondeterministic

**Exact statement**

> Platform access follows least privilege.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Platform access follows least privilege.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FF113370` — Nondeterministic

**Exact statement**

> Node, cluster, and control-plane upgrades are rehearsed.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Upgrade-rehearsal occurrence does not prove complete node, cluster, control-plane, version, rollback or failure-path coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A standard upgrade drill can pass while a mixed-version control-plane rollback fails.

## `USEQ-4B41FF50` — Nondeterministic

**Exact statement**

> Persistent-storage failure and restoration are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A storage failure and restoration test is one observation without complete storage classes, corruption modes, datasets and integrity oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A volume-loss drill can restore sample files while a database consistency failure remains untested.

## `USEQ-6EB9C058` — Nondeterministic

**Exact statement**

> Service identities are scoped per workload where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Service identities are scoped per workload where practical.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For workloads already marked applicable, verify each has a distinct scoped service identity in the deployed configuration.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-3D0C3C21` — Nondeterministic

**Exact statement**

> Regional availability and failover match approved objectives.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Regional failover meeting availability objectives is a runtime behavior claim over provider failures, traffic and shared dependencies.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A simulated failover can meet RTO while a regional identity dependency prevents failover in production.

## `USEQ-364C0C23` — Nondeterministic

**Exact statement**

> Logs, metrics, traces, synthetic checks, and real-user signals cover critical journeys.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Logs, metrics, traces, synthetic checks, and real-user signals cover critical journeys.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For journeys already declared critical, verify configured logs, metrics, traces, synthetic checks, and real-user signals are mapped to them.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-15DAEE33` — Nondeterministic

**Exact statement**

> Telemetry consistently identifies service, environment, release, instance, region, and tenant where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Telemetry consistently identifies service, environment, release, instance, region, and tenant where appropriate.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For telemetry dimensions already marked applicable, verify emitted records contain the declared service, environment, release, instance, region, and tenant identifiers.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-ACC724DF` — Nondeterministic

**Exact statement**

> Trace context propagates across trusted service boundaries.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Trace propagation across every trusted service boundary requires a complete boundary and execution-path inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Registered HTTP calls can propagate trace context while an asynchronous or retry path silently drops it.

## `USEQ-9C8C69CA` — Nondeterministic

**Exact statement**

> Latency uses meaningful percentiles.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Latency uses meaningful percentiles.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-2CAC6BCD` — Nondeterministic

**Exact statement**

> Traffic, workload, saturation, correctness, freshness, durability, queue lag, and reconciliation are measured where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Traffic, workload, saturation, correctness, freshness, durability, queue lag, and reconciliation are measured where relevant.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For signals already marked relevant, verify configured measurements exist for the declared traffic, workload, saturation, correctness, freshness, durability, queue-lag, and reconciliation fields.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-1172021F` — Nondeterministic

**Exact statement**

> Logs are structured, searchable, and use consistent levels.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Structured, searchable and consistently leveled logs are qualitative and universal over every writer, format, store and query path.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known schemas can validate while a plugin emits unstructured logs or critical errors use an inappropriate level.

## `USEQ-ACC02B9C` — Nondeterministic

**Exact statement**

> Passwords, reusable tokens, session identifiers, secret keys, payment credentials, and private keys are never logged.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Passwords, reusable tokens, session identifiers, secret keys, payment credentials, and private keys are never logged.” is nondeterministic because it makes an absolute or continuing guarantee that bounded repository, record, and runtime evidence cannot prove for every future time and execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-8605B669` — Nondeterministic

**Exact statement**

> Log injection and unsafe control characters are handled.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Log injection and unsafe control characters are handled.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E25ED114` — Nondeterministic

**Exact statement**

> Authentication, authorization changes, administrative actions, sensitive reads, exports, deletions, and configuration changes are audited.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Auditing seven security-sensitive action families requires complete API, background-job, admin and data-access path coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured events can pass while a sensitive export through an uninstrumented integration leaves no audit record.

## `USEQ-4CEDEF76` — Nondeterministic

**Exact statement**

> Log and audit retention match operational, security, privacy, legal, and contractual requirements.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Log and audit retention match operational, security, privacy, legal, and contractual requirements.”, the retained record or configuration fact is mechanically checkable, but the full result still requires specialist interpretation of the applicable legal, contractual, privacy, security, or sustainability obligations; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify each declared telemetry store uses the retention value in the already approved retention schedule.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B4212946` — Nondeterministic

**Exact statement**

> Telemetry pipeline failure, backlog, cost, and quota exhaustion are monitored.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring four telemetry-pipeline risks cannot prove complete pipeline coverage or detect manipulation that also disables its monitor.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All dashboards can exist while an unregistered shard backlogs or quota exhaustion suppresses both telemetry and heartbeat.

## `USEQ-5FEB8562` — Nondeterministic

**Exact statement**

> Investigators can locate relevant evidence within the incident-response objective.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Investigators can locate relevant evidence within the incident-response objective.”, the retained record or configuration fact is mechanically checkable, but the full result still requires whether the empirical exercise truly retrieves all relevant evidence under a representative incident protocol; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify a bounded evidence-retrieval exercise has a recorded completion time and compare it with the declared incident-response time objective.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CAC36B21` — Nondeterministic

**Exact statement**

> Dashboards, alerts, and diagnostic queries are tested during simulated incidents.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Simulated-incident test occurrence does not prove dashboards, alerts and queries work for all incidents, permissions, data loss and timing states.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A known outage scenario can pass while an incident that breaks the telemetry source leaves diagnostics useless.

## `USEQ-2D2D4869` — Deterministic

**Exact statement**

> Alerts are tied to user impact, SLO risk, data integrity, security, fraud, or impending capacity failure.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every alert in the authoritative registry can be required to link at least one of six exact user, SLO, integrity, security, fraud or capacity-risk categories.

### Exact deterministic check

1. Alerts are tied to user impact, SLO risk, data integrity, security, fraud, or impending capacity failure.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An alert with no impact-category link fails the finite registry constraint.

## `USEQ-768749B7` — Nondeterministic

**Exact statement**

> Non-actionable information uses non-paging channels.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Non-actionable information uses non-paging channels.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-402BD03D` — Deterministic

**Exact statement**

> Every alert has an owner and a corresponding diagnostic or mitigation runbook.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every registered alert can be joined to an accountable owner identity and a current diagnostic or mitigation runbook.

### Exact deterministic check

1. Every alert has an owner and a corresponding diagnostic or mitigation runbook.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A missing owner or runbook produces an exact referential-integrity failure.

## `USEQ-846BA894` — Nondeterministic

**Exact statement**

> Alert routing and escalation are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Routing and escalation test occurrence cannot prove every alert, responder schedule, provider and failure mode routes correctly.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A test page can reach on-call while a real high-severity route loops or escalates to a stale contact.

## `USEQ-467D6189` — Nondeterministic

**Exact statement**

> Responders have required access and tools.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required responder access and tools depend on complete incident-task analysis and actual permission and tool availability under failure.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All listed responders can have roles and tools while a necessary console or emergency permission is missing during an incident.

## `USEQ-57378001` — Deterministic

**Exact statement**

> Fast and slow SLO consumption are detected.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For every registered SLO, exact fast- and slow-burn alert formulas can be validated against the versioned objective and window policy.

### Exact deterministic check

1. Fast and slow SLO consumption are detected.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

An SLO lacking either burn-rate detector or using the wrong objective value fails the finite formula join.

## `USEQ-37471699` — Nondeterministic

**Exact statement**

> Thresholds avoid obvious noise and missed incidents.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Thresholds avoid obvious noise and missed incidents.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-06E3518C` — Nondeterministic

**Exact statement**

> Failure of monitoring or paging generates an independent signal.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** True independence and operation of a monitoring or paging failure signal require dependency and correlated-failure analysis plus execution evidence.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A heartbeat alert can exist while sharing the failed paging provider and therefore never reaches responders.

## `USEQ-4A5C9EF3` — Deterministic

**Exact statement**

> Runbooks include symptoms, impact, diagnosis, containment, rollback or roll-forward, recovery, verification, and escalation.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every runbook can be checked for the eight exact symptoms, impact, diagnosis, containment, change, recovery, verification and escalation sections.

### Exact deterministic check

1. Runbooks include symptoms, impact, diagnosis, containment, rollback or roll-forward, recovery, verification, and escalation.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A runbook missing recovery verification or escalation fails the finite document schema.

## `USEQ-9B7E1084` — Nondeterministic

**Exact statement**

> Runbooks do not rely on undocumented tribal knowledge.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Runbooks do not rely on undocumented tribal knowledge.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-12D77066` — Nondeterministic

**Exact statement**

> Runbook commands are reviewed and clearly identify destructive actions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Runbook commands are reviewed and clearly identify destructive actions.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify each runbook command has a review record and an explicit destructive-action marker.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-11AF58B5` — Nondeterministic

**Exact statement**

> Required dashboards, consoles, credentials, and documentation remain accessible during incidents.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Availability of dashboards, consoles, credentials and documentation during arbitrary incidents is an empirical correlated-failure claim.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All four can work in a routine drill while a shared identity outage makes every recovery resource inaccessible.

## `USEQ-B43706EC` — Nondeterministic

**Exact statement**

> A customer-facing status mechanism exists where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “A customer-facing status mechanism exists where appropriate.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For services already marked applicable in the approved catalog, verify a customer-facing status mechanism is configured.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-1D858AD2` — Nondeterministic

**Exact statement**

> On-call load and alert volume are sustainable.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “On-call load and alert volume are sustainable.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-9E774722` — Deterministic

**Exact statement**

> On-call, alerting, and runbook drills have been completed.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The drill ledger can require completed, version-bound events for the three exact on-call, alerting and runbook drill types.

### Exact deterministic check

1. On-call, alerting, and runbook drills have been completed.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A missing runbook drill or a drill against an obsolete process version fails the finite occurrence record.

## `USEQ-004EF7C7` — Nondeterministic

**Exact statement**

> Business-impact analysis identifies critical services, journeys, data, dependencies, and manual operations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Criticality and complete discovery of services, journeys, data, dependencies and manual operations require business-impact judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All five sections can exist while an indirect manual recovery step or critical journey is omitted.

## `USEQ-F69270D3` — Nondeterministic

**Exact statement**

> Recovery Point Objectives are defined for every stateful system.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Defining RPO for every stateful system requires a complete system inventory and correct statefulness classification.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All cataloged stateful services can have RPOs while an unregistered queue or cache with critical state is omitted.

## `USEQ-72E6B0CB` — Nondeterministic

**Exact statement**

> Recovery Time Objectives are defined for every critical journey.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Critical-journey identification and complete journey inventory are contextual and cannot be proved by the RTO register.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Every recorded critical journey can have an RTO while a support or recovery journey is missing.

## `USEQ-0DF2FA43` — Nondeterministic

**Exact statement**

> Backups run automatically at the required frequency and success is monitored.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Backup schedule and monitor checks cannot prove every required data set and backup path runs at the right cadence and succeeds.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Registered jobs can be green while an untracked data store is never backed up or a corrupt backup reports success.

## `USEQ-98D70D6C` — Nondeterministic

**Exact statement**

> Backups are encrypted and access follows least privilege.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Backups are encrypted and access follows least privilege.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify declared backup stores and artifacts use the encryption settings required by the versioned backup policy.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-76293243` — Nondeterministic

**Exact statement**

> Immutable or tamper-resistant copies exist where risk warrants them.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Immutable or tamper-resistant copies exist where risk warrants them.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For systems already marked as requiring immutable copies, verify a tamper-resistant backup configuration and current copy exist.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-F6E2640E` — Nondeterministic

**Exact statement**

> Full, partial, clean-environment, regional-loss, and provider-loss restoration are tested as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Full, partial, clean-environment, regional-loss, and provider-loss restoration are tested as applicable.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For recovery scenarios already marked applicable, verify completed restoration-test records exist.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8C6EF82F` — Deterministic

**Exact statement**

> DNS, certificates, secrets, identity, networking, external integrations, and communication systems are included in recovery planning.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A recovery plan can be checked for the seven exact DNS, certificate, secret, identity, network, integration and communication-system sections.

### Exact deterministic check

1. DNS, certificates, secrets, identity, networking, external integrations, and communication systems are included in recovery planning.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting external integrations or certificates fails the finite plan schema.

## `USEQ-7C161E16` — Nondeterministic

**Exact statement**

> Restored data is checked for integrity and reconciled.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Integrity checks and reconciliation require complete restored datasets and authoritative correctness oracles, not merely a successful command.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Checksums can match copied files while logical database relationships are inconsistent or records are missing from the manifest.

## `USEQ-D4E7515B` — Deterministic

**Exact statement**

> Actual tested recovery time and recovery point meet approved objectives.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a bounded recovery experiment, measured RTO and RPO can be compared exactly with versioned approved numeric objectives.

### Exact deterministic check

1. Actual tested recovery time and recovery point meet approved objectives.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A test whose actual recovery time or point exceeds its objective fails the numeric threshold comparison.

## `USEQ-C41BABC1` — Nondeterministic

**Exact statement**

> Failover and failback are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Failover and failback test occurrence does not establish complete regions, versions, data states, traffic patterns and correctness oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A normal drill can pass while failback after writes in both regions loses data.

## `USEQ-0AAD5260` — Nondeterministic

**Exact statement**

> Disaster-recovery capacity can carry required load.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A load test cannot prove disaster-recovery capacity for every required workload, failure combination and scaling constraint.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

The test carries nominal demand while a peak recovery backlog or provider quota makes real capacity insufficient.

## `USEQ-F4AB063C` — Nondeterministic

**Exact statement**

> Recovery does not create duplicate notifications, charges, webhooks, messages, or jobs.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Recovery does not create duplicate notifications, charges, webhooks, messages, or jobs.” is nondeterministic because it makes an absolute or continuing guarantee that bounded repository, record, and runtime evidence cannot prove for every future time and execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-FD45C5FC` — Nondeterministic

**Exact statement**

> Restoration does not silently re-enable revoked users, revoked secrets, expired permissions, or deleted data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Restoration does not silently re-enable revoked users, revoked secrets, expired permissions, or deleted data.” is nondeterministic because it makes an absolute or continuing guarantee that bounded repository, record, and runtime evidence cannot prove for every future time and execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-75188025` — Nondeterministic

**Exact statement**

> Ransomware and destructive-administrator scenarios are exercised where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Ransomware and destructive-administrator scenarios are exercised where relevant.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For systems already marked applicable, verify completed ransomware and destructive-administrator exercise records exist.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DFA9E714` — Nondeterministic

**Exact statement**

> Recovery does not depend on a single person, device, credential, or unavailable provider.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of single-person, device, credential or provider dependencies requires a complete recovery dependency graph and correlated-failure proof.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Two operators can be listed while both rely on one hardware token issuer or unavailable provider.

## `USEQ-AB371F8F` — Deterministic

**Exact statement**

> Continuity procedures cover customer support, communications, essential manual operations, and decision authority.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Continuity procedures can be checked for four finite sections covering customer support, communication, essential manual operations and decision authority.

### Exact deterministic check

1. Continuity procedures cover customer support, communications, essential manual operations, and decision authority.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A continuity plan without manual operations or decision authority fails the exact document schema.

## `USEQ-D1BD4768` — Nondeterministic

**Exact statement**

> Identify the user journeys and service outcomes whose failure matters.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Identify the user journeys and service outcomes whose failure matters.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B3E613B3` — Nondeterministic

**Exact statement**

> Define service-level indicators from user-observable availability, latency, correctness, durability, freshness, and quality.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define service-level indicators from user-observable availability, latency, correctness, durability, freshness, and quality.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0FC335A7` — Nondeterministic

**Exact statement**

> Specify good and total events or equivalent valid measurement semantics.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Specify good and total events or equivalent valid measurement semantics.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify the SLI specification declares its good-event and total-event predicates or another explicitly versioned calculation formula.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-DD333F33` — Nondeterministic

**Exact statement**

> Use windows and percentiles aligned to user and business consequences.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use windows and percentiles aligned to user and business consequences.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-14445940` — Nondeterministic

**Exact statement**

> Set objectives from user need, dependency capability, cost, and risk rather than arbitrary perfection.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Set objectives from user need, dependency capability, cost, and risk rather than arbitrary perfection.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-B6C7F51A` — Nondeterministic

**Exact statement**

> Measure from multiple points when server health can differ from user experience.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure from multiple points when server health can differ from user experience.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-A332E7D6` — Nondeterministic

**Exact statement**

> Exclude events only through documented semantics that cannot hide provider-caused failure.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Exclude events only through documented semantics that cannot hide provider-caused failure.” is nondeterministic because it makes an absolute or continuing guarantee that bounded repository, record, and runtime evidence cannot prove for every future time and execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-918009AE` — Deterministic

**Exact statement**

> Define an error-budget policy before budget exhaustion occurs.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An error-budget policy can be versioned and timestamped before the first exhaustion event for its SLO.

### Exact deterministic check

1. Define an error-budget policy before budget exhaustion occurs.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A policy created after exhaustion or lacking an effective date fails the temporal rule.

## `USEQ-D85BEB1A` — Nondeterministic

**Exact statement**

> Link budget status to release pace, change risk, reliability work, and escalation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Link budget status to release pace, change risk, reliability work, and escalation.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E1B20661` — Deterministic

**Exact statement**

> Use fast and slow burn alerts to detect acute and chronic objective risk.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each SLO can require approved fast- and slow-burn formulas with exact windows and thresholds tied to its objective.

### Exact deterministic check

1. Use fast and slow burn alerts to detect acute and chronic objective risk.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A missing chronic-risk alert or mismatched burn threshold fails the finite policy calculation.

## `USEQ-0616ED60` — Nondeterministic

**Exact statement**

> Review whether objectives are too loose, too strict, or measuring the wrong outcome.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review whether objectives are too loose, too strict, or measuring the wrong outcome.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-77209F37` — Deterministic

**Exact statement**

> Define dependencies' contribution to the end-to-end objective.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An end-to-end objective model can require an explicit contribution definition for every registered dependency edge.

### Exact deterministic check

1. Define dependencies' contribution to the end-to-end objective.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A dependency without an objective contribution or using another service's SLO identity fails the graph schema.

## `USEQ-4E57FA91` — Nondeterministic

**Exact statement**

> Avoid multiplying optimistic component availability claims without shared-failure analysis.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid multiplying optimistic component availability claims without shared-failure analysis.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3F631903` — Nondeterministic

**Exact statement**

> Publish objectives and current status to relevant product, engineering, support, and leadership stakeholders.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Publish objectives and current status to relevant product, engineering, support, and leadership stakeholders.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For stakeholder groups already declared relevant, verify publication records contain the current objective and status.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C7355F4C` — Nondeterministic

**Exact statement**

> Use SLO misses to prioritize systemic improvement, not punish individuals.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use SLO misses to prioritize systemic improvement, not punish individuals.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-A8A37E99` — Deterministic

**Exact statement**

> Preserve separate integrity and safety gates that cannot be averaged into availability.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The promise concerns enforced gate behavior. It requires effective CI/release policy evidence, including the actual aggregation and blocking expression.

### Exact deterministic check

1. The effective release-gate policy contains separate integrity, safety, and availability gates, and its aggregation/blocking expression cannot offset a failing integrity or safety gate with the availability value.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A policy record can state that gates are separate while the live release formula averages them. The effective CI policy check fails.

## `USEQ-5BB4035B` — Nondeterministic

**Exact statement**

> Review objectives after material product, traffic, architecture, dependency, or contractual change.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Review objectives after material product, traffic, architecture, dependency, or contractual change.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For changes already designated material by an approved record, verify an objective-review record follows the change.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D72ACBC8` — Nondeterministic

**Exact statement**

> Measure planned maintenance according to user and contractual semantics.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** User and contractual semantics for planned maintenance require product and legal interpretation; a numeric calculation can consistently encode the wrong meaning.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A metric can exclude maintenance while the customer contract counts it as downtime, yet the configured formula still passes.

## `USEQ-F2601113` — Deterministic

**Exact statement**

> Keep raw measurement and calculation reproducible.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Raw measurement inputs and calculation code can be content-addressed, preserved and replayed to reproduce the published result exactly.

### Exact deterministic check

1. Keep raw measurement and calculation reproducible.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A missing input or calculation mismatch fails the bounded reproducibility check.

## `USEQ-38C611A2` — Nondeterministic

**Exact statement**

> Do not promise external service levels that internal evidence and capacity cannot support.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Do not promise external service levels that internal evidence and capacity cannot support.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-6D436E9F` — Nondeterministic

**Exact statement**

> Maintain a service catalog with owner, purpose, users, dependencies, support hours, objectives, data, and lifecycle state.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Eight fields can validate entries, but a service catalog cannot prove it includes every actual operational service and dependency.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All rows can be complete while a shadow production service remains outside the catalog.

## `USEQ-9AD25B90` — Deterministic

**Exact statement**

> Define support channels, severity, response, escalation, entitlement, and communication expectations.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A support policy can define the six exact channel, severity, response, escalation, entitlement and communication expectation sections.

### Exact deterministic check

1. Define support channels, severity, response, escalation, entitlement, and communication expectations.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting entitlement or escalation rules fails the finite policy schema.

## `USEQ-48319180` — Nondeterministic

**Exact statement**

> Provide support staff with current product behavior, known issues, diagnostics, privacy, security, and escalation guidance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Provide support staff with current product behavior, known issues, diagnostics, privacy, security, and escalation guidance.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7751FFCD` — Nondeterministic

**Exact statement**

> Restrict and audit support access to customer data and administrative functions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Restrict and audit support access to customer data and administrative functions.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify support access to customer data and administrative functions is logged with actor, target, action, time, and outcome.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-EE8096B5` — Nondeterministic

**Exact statement**

> Verify requester identity before disclosing or changing sensitive information.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identity verification before every sensitive disclosure or change is a security property across complete support and administration paths.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The standard support form can verify identity while a phone escalation or integration changes sensitive data without it.

## `USEQ-303A0806` — Deterministic

**Exact statement**

> Separate service requests, defects, incidents, security events, privacy requests, and complaints while preserving linkage.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A case-management schema can assign six distinct request types while preserving explicit typed links among related cases.

### Exact deterministic check

1. Separate service requests, defects, incidents, security events, privacy requests, and complaints while preserving linkage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Merging a privacy request into a defect or losing its incident link violates the enum and relation constraints.

## `USEQ-8921C2A9` — Nondeterministic

**Exact statement**

> Use consistent priority based on impact and urgency.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Impact, urgency and consistent priority assignment require contextual assessment of varied users, harms and incidents.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A priority matrix can be applied consistently to incorrect low impact and urgency ratings.

## `USEQ-8BD3AE1B` — Nondeterministic

**Exact statement**

> Keep customers informed with accurate status, workaround, risk, and resolution information.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Keep customers informed with accurate status, workaround, risk, and resolution information.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-52C655EE` — Nondeterministic

**Exact statement**

> Avoid promising dates or causes before evidence supports them.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid promising dates or causes before evidence supports them.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7E1DF527` — Nondeterministic

**Exact statement**

> Capture recurring requests and friction as product and documentation improvement signals.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Recurrence, friction and useful conversion into product or documentation signals require semantic grouping and user-experience judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

Repeated requests can be split across labels and never recognized as the same friction despite complete records.

## `USEQ-5CA932E3` — Deterministic

**Exact statement**

> Maintain searchable knowledge articles with owner, effective version, review date, and retirement.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Article existence and metadata require artifact evidence; searchable requires a bounded query against the effective search surface. Both clauses are necessary.

### Exact deterministic check

1. Every knowledge article in the complete sealed article inventory is a retrievable content-addressed artifact bound to its owner, effective version, review date, and retirement state.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

2. Every non-retired article in the complete sealed article inventory is returned by the bounded search query for its exact stable identifier and independently sealed lookup term set.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A catalog record can contain perfect metadata while the article artifact is missing or absent from search results. The composite fails either broken state.

## `USEQ-320B9CC3` — Nondeterministic

**Exact statement**

> Coordinate changes, releases, maintenance, suppliers, and support readiness.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Coordinate changes, releases, maintenance, suppliers, and support readiness.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-3847EF4D` — Nondeterministic

**Exact statement**

> Measure resolution quality, reopen rate, escalation, user effort, satisfaction, and recurrence—not closure volume alone.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `strength_audit_reclassified` — A third rule-by-rule strength audit found that the earlier deterministic clause weakened the original promise, so the complete rule is nondeterministic.
- **Reason:** Resolution quality, user effort, and satisfaction require populations, instruments, scales, sampling, attribution, and interpretation that the rule does not define. The measurable reopen and escalation fields cannot make the full compound outcome rule deterministic.
- **Nondeterministic reason codes:** `undefined_protocol`, `mixed_with_nondeterministic_child`

### Strength-audit rejection counterexample

A support report can contain values for all six named labels, computed from an unrepresentative optional survey or self-assigned quality score, while actual resolution quality and user effort are poor. Field presence and arithmetic pass without proving the original measurement promise.

## `USEQ-17B8BFF4` — Nondeterministic

**Exact statement**

> Provide accessible support channels and alternatives.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Accessible support and effective alternatives are user outcomes across disabilities, languages, devices and failure conditions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A channel can meet automated checks while remaining unusable to a screen-reader user or when the primary channel fails.

## `USEQ-6EEB4C4C` — Deterministic

**Exact statement**

> Define handoff across time zones, teams, suppliers, and incident command.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A handoff policy can define procedures for four finite contexts: time zones, teams, suppliers and incident command.

### Exact deterministic check

1. Define handoff across time zones, teams, suppliers, and incident command.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A missing supplier or incident-command handoff fails the exact policy matrix.

## `USEQ-6509842E` — Nondeterministic

**Exact statement**

> Protect support notes, attachments, recordings, and exports according to data sensitivity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Protection according to sensitivity requires correct data classification and complete access, storage, export and retention path enforcement.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Policy records can pass while a sensitive attachment is exported to an unapproved support tool.

## `USEQ-23D14CF4` — Nondeterministic

**Exact statement**

> Review complaints and vulnerable-user cases with appropriate expertise.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Review complaints and vulnerable-user cases with appropriate expertise.” is nondeterministic because it depends on identifying or applying obligations and expertise that require an accountable specialist.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-BB51346B` — Deterministic

**Exact statement**

> Define service acceptance before transfer from project to operation.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A lifecycle workflow can require a versioned service-acceptance definition before the project-to-operations transfer transition.

### Exact deterministic check

1. Define service acceptance before transfer from project to operation.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Transfer without acceptance criteria or with criteria dated afterward fails the temporal gate.

## `USEQ-DFA02FCC` — Deterministic

**Exact statement**

> Retire support obligations only through communicated lifecycle policy.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Retirement authorization is a structured relation, while communicated means the notice and policy were actually exposed to the declared audience before retirement.

### Exact deterministic check

1. Every retired support obligation in the complete sealed obligation inventory is bound to the exact approved lifecycle-policy version that authorizes its retirement.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. For every such retired obligation, the declared audience publication surface exposes the bound lifecycle-policy version and retirement notice before the obligation retirement timestamp.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A retirement record can cite a policy and claim communication while the audience never received or could access the notice. The environment clause fails.

## `USEQ-115E72C7` — Nondeterministic

**Exact statement**

> Use continual improvement to reduce demand caused by product defects and confusing design.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Use continual improvement to reduce demand caused by product defects and confusing design.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F8D01DA0` — Deterministic

**Exact statement**

> Define incident criteria, severity, authority, roles, communication, and escalation before incidents occur.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An incident policy can define six exact criteria, severity, authority, roles, communication and escalation sections before any incident event.

### Exact deterministic check

1. Define incident criteria, severity, authority, roles, communication, and escalation before incidents occur.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A policy missing decision authority or created after an incident fails the schema or temporal rule.

## `USEQ-FD4D66CA` — Nondeterministic

**Exact statement**

> Prioritize protection of people, data integrity, security, and essential service over preserving change plans.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize protection of people, data integrity, security, and essential service over preserving change plans.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-A3F2248F` — Deterministic

**Exact statement**

> Establish incident command and one authoritative timeline.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each incident can require a named command identity and exactly one authoritative append-only timeline.

### Exact deterministic check

1. Establish incident command and one authoritative timeline.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Multiple competing authoritative timelines or missing command identity violate the finite incident schema.

## `USEQ-C36750C1` — Nondeterministic

**Exact statement**

> Separate command, operations, investigation, communication, and documentation roles when scale requires it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Separate command, operations, investigation, communication, and documentation roles when scale requires it.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For incidents already assigned a scale tier requiring role separation, verify distinct command, operations, investigation, communication, and documentation assignees.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D18DD1C3` — Nondeterministic

**Exact statement**

> Use clear hypotheses, evidence, decisions, owners, and timestamps during response.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use clear hypotheses, evidence, decisions, owners, and timestamps during response.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify incident records contain hypothesis, evidence, decision, owner, and timestamp fields.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-232D5A01` — Nondeterministic

**Exact statement**

> Contain blast radius and stop harmful automation or change.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Contain blast radius and stop harmful automation or change.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-D3AB5A8E` — Nondeterministic

**Exact statement**

> Preserve evidence while restoring service.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Preserve evidence while restoring service.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C6CD577E` — Nondeterministic

**Exact statement**

> Communicate known facts, uncertainty, impact, mitigation, and next update without speculation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Communicate known facts, uncertainty, impact, mitigation, and next update without speculation.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-84C1C16C` — Deterministic

**Exact statement**

> Declare recovery only after user journeys, integrity, backlog, dependencies, and monitoring are verified.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The incident state machine can block recovery declaration until five exact journey, integrity, backlog, dependency and monitoring verification states pass.

### Exact deterministic check

1. Declare recovery only after user journeys, integrity, backlog, dependencies, and monitoring are verified.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A recovery declaration with an unchecked dependency or nonempty unapproved backlog fails the transition guard.

## `USEQ-B5D09F0D` — Nondeterministic

**Exact statement**

> Monitor for recurrence and delayed effects.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring cannot prove detection of every recurrence and delayed effect without complete symptom, time-horizon and telemetry coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Dashboards can stay green while a latent corruption appears after the configured observation window.

## `USEQ-D2A2831E` — Nondeterministic

**Exact statement**

> Link related incidents to a problem record when systemic cause or recurrence exists.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Link related incidents to a problem record when systemic cause or recurrence exists.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-864FDC25` — Nondeterministic

**Exact statement**

> Analyze technical, process, interface, organizational, supplier, and incentive contributors.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Analyze technical, process, interface, organizational, supplier, and incentive contributors.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C87A0E16` — Nondeterministic

**Exact statement**

> Avoid blame, hindsight bias, and a single human-error root cause.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid blame, hindsight bias, and a single human-error root cause.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-887E41C0` — Nondeterministic

**Exact statement**

> Identify why controls did not prevent, detect, contain, or recover sooner.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Identify why controls did not prevent, detect, contain, or recover sooner.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-B45BF06B` — Nondeterministic

**Exact statement**

> Assign corrective actions that are specific, prioritized, owned, funded, and verifiable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Assign corrective actions that are specific, prioritized, owned, funded, and verifiable.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify corrective-action records contain priority, owner, funding decision, and verification fields.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A0EC83FE` — Nondeterministic

**Exact statement**

> Prefer actions that change systems and incentives over reminders alone.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prefer actions that change systems and incentives over reminders alone.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-4EE52A7C` — Nondeterministic

**Exact statement**

> Track actions to effective completion and review recurrence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Track actions to effective completion and review recurrence.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-FEA91FF6` — Nondeterministic

**Exact statement**

> Share lessons with other systems that have the same failure class.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Share lessons with other systems that have the same failure class.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-7617216D` — Nondeterministic

**Exact statement**

> Update tests, monitors, runbooks, architecture, requirements, training, and risk records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Update tests, monitors, runbooks, architecture, requirements, training, and risk records.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify post-incident records link to any changed test, monitor, runbook, architecture, requirement, training, or risk artifact.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2805E81B` — Nondeterministic

**Exact statement**

> Measure restoration, impact, detection, recurrence, action effectiveness, and learning—not incident count alone.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Several durations are computable, but recurrence, action effectiveness and learning require causal and qualitative interpretation.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

All six metrics can be present while an ineffective action is marked complete and no real learning changes practice.

## `USEQ-88305F87` — Deterministic

**Exact statement**

> Maintain forecasts for ordinary, peak, seasonal, launch, growth, recovery, and abuse demand.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A capacity forecast package can require seven exact ordinary, peak, seasonal, launch, growth, recovery and abuse demand scenarios.

### Exact deterministic check

1. Maintain forecasts for ordinary, peak, seasonal, launch, growth, recovery, and abuse demand.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting recovery or abuse demand fails the finite scenario schema; forecast accuracy is not inferred.

## `USEQ-1012D643` — Nondeterministic

**Exact statement**

> Track capacity limits and quotas across compute, memory, storage, network, connections, queues, databases, third parties, and control planes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Tracking ten resource-limit classes requires a complete component, provider and control-plane inventory that limit records cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All known quotas can be present while an indirect database or third-party limit remains untracked.

## `USEQ-874742D9` — Nondeterministic

**Exact statement**

> Measure saturation and headroom at the actual bottleneck.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure saturation and headroom at the actual bottleneck.” is nondeterministic because it asks for an empirical conclusion without fixing the workload, environment, oracle, sample, or repeatable acceptance protocol.
- **Nondeterministic reason codes:** `undefined_protocol`

## `USEQ-CD9DAD72` — Nondeterministic

**Exact statement**

> Define minimum safe headroom for failure, deployment, backup, recovery, and traffic shift.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define minimum safe headroom for failure, deployment, backup, recovery, and traffic shift.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-D9DC2BF5` — Nondeterministic

**Exact statement**

> Review forecasts against actual demand and update assumptions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Numeric forecast comparison is bounded, but updating the correct assumptions requires causal demand and model judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

Actual demand can be compared while the wrong assumption is edited and the real growth driver remains unchanged.

## `USEQ-7E0F4822` — Nondeterministic

**Exact statement**

> Scale based on leading indicators where reactive scaling would be too late.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Scale based on leading indicators where reactive scaling would be too late.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0C1180BC` — Nondeterministic

**Exact statement**

> Test scale-up, scale-down, rebalancing, and capacity during a failure domain loss.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Four capacity test labels do not prove complete workloads, failure domains, scaling states or correctness and stability outcomes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Scale-up and zone-loss tests can pass separately while rebalancing during loss overloads a queue.

## `USEQ-D9D69015` — Nondeterministic

**Exact statement**

> Prevent autoscaling from overwhelming a database or external dependency.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing autoscaling from overwhelming every database or external dependency is a runtime safety claim over open workloads and provider limits.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A standard load test can pass while a burst scaling event exhausts a hidden third-party quota.

## `USEQ-E699A7C6` — Nondeterministic

**Exact statement**

> Use per-tenant, per-user, and per-operation quotas to protect shared service.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use per-tenant, per-user, and per-operation quotas to protect shared service.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify configured per-tenant, per-user, and per-operation quotas exist for the declared shared service.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-46661625` — Deterministic

**Exact statement**

> Define overload admission, queuing, backpressure, prioritization, degradation, and shedding.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An overload policy can define the six exact admission, queuing, backpressure, prioritization, degradation and shedding mechanisms.

### Exact deterministic check

1. Define overload admission, queuing, backpressure, prioritization, degradation, and shedding.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A service policy missing load shedding or prioritization fails the finite schema.

## `USEQ-49019E6A` — Nondeterministic

**Exact statement**

> Protect critical operations before optional or expensive operations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect critical operations before optional or expensive operations.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-C8DBF731` — Deterministic

**Exact statement**

> Monitor latency distributions, throughput, errors, queue age, resource use, and unit cost together.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A service dashboard definition can require the six exact latency, throughput, error, queue-age, resource and unit-cost signals in one versioned view.

### Exact deterministic check

1. Monitor latency distributions, throughput, errors, queue age, resource use, and unit cost together.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A view lacking unit cost or queue age fails the finite signal contract.

## `USEQ-5E82CAD5` — Nondeterministic

**Exact statement**

> Detect leaks and gradual degradation through endurance signals.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detecting all leaks and gradual degradation requires complete endurance durations, platforms, resources, baselines and telemetry oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

An endurance test can pass for hours while a weekly leak or uninstrumented resource continues degrading.

## `USEQ-9197F66F` — Deterministic

**Exact statement**

> Plan storage, index, log, backup, and retention growth.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A growth plan can be checked for five finite storage, index, log, backup and retention forecast sections.

### Exact deterministic check

1. Plan storage, index, log, backup, and retention growth.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A plan lacking backup or retention growth fails the exact schema.

## `USEQ-5FE14FFF` — Nondeterministic

**Exact statement**

> Coordinate capacity with supplier limits and contract lead times.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete supplier limits, contract lead times and correct capacity coordination require external contract interpretation and provider inventory.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All known limits can be scheduled while an indirect provider quota or contractual notice period is missed.

## `USEQ-36E4D361` — Nondeterministic

**Exact statement**

> Keep emergency capacity actions documented, authorized, and reversible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Documentation and authorization are bounded, but proving every emergency capacity action is reversible requires empirical rollback and side-effect coverage.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

An approved runbook can exist while an emergency database resize cannot be rolled back without data migration.

## `USEQ-A73F088E` — Nondeterministic

**Exact statement**

> Verify capacity after architecture, data-shape, feature, dependency, or workload change.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Capacity verification after five change types requires complete change detection, representative load and failure scenarios, and exact oracles.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known changes can trigger tests while an indirect dependency or data-shape shift is missed.

## `USEQ-F46A2D61` — Nondeterministic

**Exact statement**

> Use performance incidents to update models and tests.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Model and test edits are observable, but whether they embody the performance-incident lesson requires causal and technical judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

Both artifacts can change while the mistaken capacity assumption that caused the incident remains.

## `USEQ-C40A674E` — Nondeterministic

**Exact statement**

> Avoid maintaining wasteful idle capacity when resilience can be met more efficiently.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid maintaining wasteful idle capacity when resilience can be met more efficiently.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8006F4CA` — Nondeterministic

**Exact statement**

> Do not reduce headroom below recovery and SLO needs solely to meet short-term cost targets.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Recovery and SLO headroom needs plus whether cost is the sole decision motive require operational and human decision judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A capacity record can exceed a formula while an omitted recovery workload makes real headroom inadequate, or rationale hides cost pressure.

## `USEQ-CD017015` — Nondeterministic

**Exact statement**

> Assign ownership for material technology cost and its business or user value.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Assign ownership for material technology cost and its business or user value.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For costs already designated material, verify an owner and linked business-value or user-value record exist.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2AA3663E` — Nondeterministic

**Exact statement**

> Tag or otherwise attribute resources, services, environments, teams, products, and tenants sufficiently for analysis.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Tag or otherwise attribute resources, services, environments, teams, products, and tenants sufficiently for analysis.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E8247F73` — Deterministic

**Exact statement**

> Separate fixed, variable, shared, support, data, network, license, supplier, and incident costs.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A cost ledger can require nine distinct fixed, variable, shared, support, data, network, license, supplier and incident categories.

### Exact deterministic check

1. Separate fixed, variable, shared, support, data, network, license, supplier, and incident costs.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Combining supplier and license cost or omitting incident cost violates the finite category schema.

## `USEQ-D39687B3` — Nondeterministic

**Exact statement**

> Define unit costs aligned to meaningful product outcomes and workloads.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Define unit costs aligned to meaningful product outcomes and workloads.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C6D9971F` — Deterministic

**Exact statement**

> Forecast base, growth, launch, seasonal, failure, backup, and recovery cost.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A cost forecast can require seven exact base, growth, launch, seasonal, failure, backup and recovery scenarios.

### Exact deterministic check

1. Forecast base, growth, launch, seasonal, failure, backup, and recovery cost.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A forecast missing failure or recovery cost fails the finite scenario manifest.

## `USEQ-AB343848` — Nondeterministic

**Exact statement**

> Set budgets and anomaly alerts without allowing cost controls to terminate critical service silently.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Budgets and alerts are inspectable, but ensuring cost controls never silently terminate any critical service is a universal runtime property.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

A configured safeguard can pass while a provider budget action disables an unregistered critical workload.

## `USEQ-8517773D` — Nondeterministic

**Exact statement**

> Detect idle, orphaned, overprovisioned, underutilized, duplicate, and forgotten resources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idle, orphaned, overprovisioned, underutilized, duplicate and forgotten status require thresholds, ownership semantics and complete resource discovery.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known resources can be classified while a shadow idle instance is absent or a critical standby is wrongly called unused.

## `USEQ-5887E8D3` — Deterministic

**Exact statement**

> Review data transfer, storage tiers, retention, logging, and high-cardinality telemetry cost.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A FinOps review record can require the five exact transfer, storage-tier, retention, logging and high-cardinality telemetry cost sections.

### Exact deterministic check

1. Review data transfer, storage tiers, retention, logging, and high-cardinality telemetry cost.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting retention or network-transfer analysis fails the finite review schema.

## `USEQ-72822800` — Nondeterministic

**Exact statement**

> Optimize architecture only when total lifecycle cost and quality improve.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Optimize architecture only when total lifecycle cost and quality improve.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0B270727` — Deterministic

**Exact statement**

> Account for engineering labor, complexity, support, migration, and lock-in—not provider invoice alone.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An investment cost model can require the five exact labor, complexity, support, migration and lock-in categories in addition to provider invoices.

### Exact deterministic check

1. Account for engineering labor, complexity, support, migration, and lock-in—not provider invoice alone.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A model with invoice-only cost or missing migration labor fails the finite category check.

## `USEQ-3C56970B` — Nondeterministic

**Exact statement**

> Use commitment and reservation mechanisms only with credible demand and exit analysis.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use commitment and reservation mechanisms only with credible demand and exit analysis.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify commitment and reservation decisions include demand evidence and an exit-analysis record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5464C515` — Nondeterministic

**Exact statement**

> Protect security, privacy, accessibility, reliability, and recovery requirements from harmful cost cutting.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect security, privacy, accessibility, reliability, and recovery requirements from harmful cost cutting.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-0A8057B2` — Nondeterministic

**Exact statement**

> Make expensive user actions, queries, abuse, and retry amplification visible and bounded.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Make expensive user actions, queries, abuse, and retry amplification visible and bounded.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For actions already classified as expensive, verify telemetry and configured bounds exist.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-D7F14772` — Nondeterministic

**Exact statement**

> Review supplier pricing changes and quota interactions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete supplier price changes and quota interactions require changing external feeds, contract interpretation and dependency inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

A review can cover known price sheets while a tier interaction or indirect supplier quota remains missed.

## `USEQ-5F69A20F` — Nondeterministic

**Exact statement**

> Measure cost per successful outcome, active user, transaction, tenant, or workload where useful.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Measure cost per successful outcome, active user, transaction, tenant, or workload where useful.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0EC26FF1` — Nondeterministic

**Exact statement**

> Include environmental resource efficiency where data is available.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Include environmental resource efficiency where data is available.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Where the authoritative data catalog marks environmental data available, verify an efficiency metric record exists.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4CA042C8` — Nondeterministic

**Exact statement**

> Verify savings after implementation and watch for shifted cost elsewhere.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Savings attribution and detection of all shifted costs require a complete cost boundary and causal economic analysis.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

Provider spend can fall while labor and network cost rise outside the measured account, producing a false savings pass.

## `USEQ-1E9FC44C` — Nondeterministic

**Exact statement**

> Remove temporary launch and migration capacity after exit criteria are met.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Exit-criteria state is bounded, but removing every temporary launch and migration resource requires a complete resource inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known temporary capacity can be deleted while a forgotten reserved instance or queue remains.

## `USEQ-26C038B6` — Deterministic

**Exact statement**

> Maintain cost evidence for major architecture and procurement decisions.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every policy-tagged major architecture or procurement decision can be required to link a versioned cost-evidence package.

### Exact deterministic check

1. Maintain cost evidence for major architecture and procurement decisions.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A major decision without cost evidence or linked to another version fails the registry join.

## `USEQ-8E6BBEEE` — Nondeterministic

**Exact statement**

> Treat unexpected cost growth as an operational incident when material.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Treat unexpected cost growth as an operational incident when material.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-64F3DC86` — Nondeterministic

**Exact statement**

> Maintain an authoritative inventory of software, services, infrastructure, devices, domains, certificates, licenses, accounts, and dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** An inventory cannot prove authoritative completeness across nine open-world asset and dependency classes.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All rows can be correct while a shadow domain, certificate, device or service remains undiscovered.

## `USEQ-BD4C9D3B` — Deterministic

**Exact statement**

> Record owner, purpose, environment, criticality, data, exposure, version, support status, and retirement date.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each asset-inventory row can be checked for nine exact owner, purpose, environment, criticality, data, exposure, version, support and retirement fields.

### Exact deterministic check

1. Record owner, purpose, environment, criticality, data, exposure, version, support status, and retirement date.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A row missing exposure or support status fails the finite schema; inventory completeness is not inferred.

## `USEQ-DB080B59` — Nondeterministic

**Exact statement**

> Discover unmanaged or shadow assets continuously.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Discover unmanaged or shadow assets continuously.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-E6C9769D` — Nondeterministic

**Exact statement**

> Track vendor support, end-of-life, certificate, license, key, and contract dates.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Tracking six date classes depends on complete asset, vendor, certificate, key and contract inventories plus current external information.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All registered dates can be valid while an unknown certificate or contract deadline is absent.

## `USEQ-D4A7A5BA` — Nondeterministic

**Exact statement**

> Classify patches by exploit activity, exposure, reachability, impact, and operational risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Classify patches by exploit activity, exposure, reachability, impact, and operational risk.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify every patch-classification record contains exploit-activity, exposure, reachability, impact, and operational-risk fields.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9099195E` — Nondeterministic

**Exact statement**

> Test patches in representative environments and define emergency paths.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Test patches in representative environments and define emergency paths.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For environments already designated representative, verify a completed patch-test record and a documented emergency path exist.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E6FF7190` — Nondeterministic

**Exact statement**

> Apply patches within risk-based targets and verify deployed versions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Risk-based patch targets require security judgment, and deployed-version verification depends on a complete asset inventory and all deployment paths.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known assets can meet target while an unsupported production copy remains unpatched and undiscovered.

## `USEQ-542BC36E` — Deterministic

**Exact statement**

> Use compensating controls only with owner, monitoring, expiry, and replacement plan.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Owner, expiry, and replacement plan are record facts; use and monitoring are effective-state facts. The full promise requires both authorities.

### Exact deterministic check

1. Every active compensating control in the complete sealed control inventory has an authenticated record containing a resolvable owner, future expiry timestamp, monitoring identity, and replacement-plan identity.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. For every active compensating control in the complete sealed control inventory, effective environment evidence shows the bound control and its bound monitor enabled for the assessed scope.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A compensating-control record can name monitoring while the monitor or control is disabled. The old structured clause passes; the environment clause fails.

## `USEQ-10EB8EC0` — Nondeterministic

**Exact statement**

> Prevent unsupported assets from silently remaining production dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing unsupported assets from silently remaining dependencies requires complete runtime dependency and restored-system discovery.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Catalog and scans can pass while an offline job or restored appliance still depends on an unsupported component.

## `USEQ-B10C60D7` — Nondeterministic

**Exact statement**

> Coordinate application, runtime, operating system, database, firmware, network, and supplier updates.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Coordinate application, runtime, operating system, database, firmware, network, and supplier updates.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-95DCE399` — Nondeterministic

**Exact statement**

> Test mixed versions, rollback, recovery, and data compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Four compatibility test categories do not define complete versions, rollback states, datasets or correctness and recovery oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known mixed-version tests can pass while rollback after a schema write corrupts older readers.

## `USEQ-EBD5EE42` — Nondeterministic

**Exact statement**

> Maintain clean trusted installation media, artifacts, and configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Maintain clean trusted installation media, artifacts, and configuration.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify installation artifacts have pinned digests and recorded integrity or provenance evidence.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-6A7A184A` — Nondeterministic

**Exact statement**

> Monitor failed, partial, or reverted patch deployments.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring failed, partial and reverted patches cannot prove complete patch-path instrumentation or correct state classification.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Dashboards can show known deployments while a disconnected device partially patches without reporting.

## `USEQ-E4979EC7` — Nondeterministic

**Exact statement**

> Remove obsolete packages, services, accounts, ports, and capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete removal of obsolete packages, services, accounts, ports and capabilities requires asset discovery and contextual obsolescence decisions.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known obsolete items can be removed while a compiled package or forgotten account persists.

## `USEQ-677D277F` — Nondeterministic

**Exact statement**

> Review license use and renewal before it can interrupt service.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Review license use and renewal before it can interrupt service.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify license-use review records and renewal dates exist for each declared production dependency.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-25987E4B` — Nondeterministic

**Exact statement**

> Protect management interfaces and inventory data.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Protect management interfaces and inventory data.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-BDBE6B90` — Nondeterministic

**Exact statement**

> Reassess capacity and performance after major updates.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Reassess capacity and performance after major updates.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For updates already designated major, verify capacity and performance reassessment evidence exists.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-3021D362` — Deterministic

**Exact statement**

> Include patch and asset state in incident investigation.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An incident-investigation schema can require exact patch-state and asset-state evidence references.

### Exact deterministic check

1. Include patch and asset state in incident investigation.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An investigation missing deployed patch version or affected asset identity fails the finite evidence schema.

## `USEQ-82716355` — Nondeterministic

**Exact statement**

> Retire duplicate and orphaned assets.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Duplicate and orphaned status requires semantic equivalence, ownership and complete asset discovery before retirement.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

A retirement job can remove known duplicates while a hidden orphan persists or two distinct assets are wrongly merged.

## `USEQ-A4575CA1` — Nondeterministic

**Exact statement**

> Report lifecycle risk to decision-makers before it becomes an emergency.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Report lifecycle risk to decision-makers before it becomes an emergency.” is nondeterministic because it does not provide an objective numeric threshold or name a versioned threshold source for its key acceptance term.
- **Nondeterministic reason codes:** `undefined_threshold`

## `USEQ-04858636` — Deterministic

**Exact statement**

> Define owner, scope, dependencies, customers, users, data, contracts, and completion criteria.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A decommissioning record can require eight exact owner, scope, dependency, customer, user, data, contract and completion-criteria fields.

### Exact deterministic check

1. Define owner, scope, dependencies, customers, users, data, contracts, and completion criteria.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A retirement plan missing customer impact or completion criteria fails the finite schema.

## `USEQ-A7C0D18F` — Nondeterministic

**Exact statement**

> Confirm replacement or continuity plans before disabling essential capability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Essential capability and adequacy of replacement or continuity plans require operational judgment beyond a confirmation record.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A plan can be approved before shutdown while it cannot handle a critical workload or failure mode.

## `USEQ-A8406215` — Nondeterministic

**Exact statement**

> Inventory inbound and outbound interfaces, jobs, queues, domains, certificates, secrets, accounts, suppliers, and data copies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete inventory of ten inbound, outbound, identity, supplier and data-copy classes cannot be proved by the decommission record itself.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All sections can be populated while a hidden scheduled job, certificate or archived data copy remains.

## `USEQ-AA9D494C` — Nondeterministic

**Exact statement**

> Notify affected users, customers, operators, support teams, and partners with sufficient notice.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Notify affected users, customers, operators, support teams, and partners with sufficient notice.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify notification timestamps precede retirement by the approved notice interval for each declared stakeholder.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0E6C800B` — Nondeterministic

**Exact statement**

> Provide migration, export, rollback, and support appropriate to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Provide migration, export, rollback, and support appropriate to impact.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify migration, export, rollback, and support artifacts selected by the approved impact tier exist.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5C76742D` — Nondeterministic

**Exact statement**

> Stop new use and dependency creation before final shutdown.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Stopping all new use and dependency creation requires complete client, job, supplier, integration, account, and discovery scope through shutdown. Therefore the proposed structured-record composite does not prove the whole rule “Stop new use and dependency creation before final shutdown.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Known callers can be blocked while an offline client or hidden dependency starts new use. The proposal can Pass while “Stop new use and dependency creation before final shutdown.” remains false.

## `USEQ-949BE861` — Nondeterministic

**Exact statement**

> Drain or reconcile in-flight transactions, messages, files, and background work.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete draining/reconciliation needs authoritative inventories and correctness oracles for every transaction, message, file, job, retry, and side effect. Therefore the proposed structured-record composite does not prove the whole rule “Drain or reconcile in-flight transactions, messages, files, and background work.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Queue depth can reach zero while a leased message or delayed job is lost. The proposal can Pass while “Drain or reconcile in-flight transactions, messages, files, and background work.” remains false.

## `USEQ-1276B959` — Nondeterministic

**Exact statement**

> Export, archive, retain, delete, or legally hold data according to approved rules.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Approved retention/deletion/hold rules require legal authority, and applying them needs complete copies, subjects, suppliers, and obligations. Therefore the proposed structured-record composite does not prove the whole rule “Export, archive, retain, delete, or legally hold data according to approved rules.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

Primary data can archive per policy while a legal hold or supplier copy is omitted. The proposal can Pass while “Export, archive, retain, delete, or legally hold data according to approved rules.” remains false.

## `USEQ-87571159` — Nondeterministic

**Exact statement**

> Verify deletion across primary stores, caches, indexes, replicas, backups, analytics, suppliers, and test copies as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Deletion verification across all stores, caches, indexes, replicas, backups, analytics, suppliers, and tests requires a complete copy inventory and negative evidence. Therefore the proposed structured-record composite does not prove the whole rule “Verify deletion across primary stores, caches, indexes, replicas, backups, analytics, suppliers, and test copies as applicable.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known systems can report deletion while an old regional backup or supplier export survives. The proposal can Pass while “Verify deletion across primary stores, caches, indexes, replicas, backups, analytics, suppliers, and test copies as applicable.” remains false.

## `USEQ-0CA1D171` — Nondeterministic

**Exact statement**

> Revoke credentials, keys, tokens, certificates, service identities, network paths, and administrative access.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete revocation spans every direct/indirect credential, key, token, cert, identity, path, federation, cache, and provider administrator. Therefore the proposed structured-record composite does not prove the whole rule “Revoke credentials, keys, tokens, certificates, service identities, network paths, and administrative access.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Listed credentials can be revoked while a cached token or inherited trust still grants access. The proposal can Pass while “Revoke credentials, keys, tokens, certificates, service identities, network paths, and administrative access.” remains false.

## `USEQ-C0FC8ACE` — Nondeterministic

**Exact statement**

> Remove DNS, routing, callbacks, webhooks, firewall rules, monitoring, alerts, and status entries.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Removing every DNS/routing/callback/webhook/firewall/monitor/alert/status artifact requires complete provider, region, integration, and history inventories. Therefore the proposed structured-record composite does not prove the whole rule “Remove DNS, routing, callbacks, webhooks, firewall rules, monitoring, alerts, and status entries.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Known records can be removed while a legacy DNS alias or supplier callback remains. The proposal can Pass while “Remove DNS, routing, callbacks, webhooks, firewall rules, monitoring, alerts, and status entries.” remains false.

## `USEQ-58AA8637` — Nondeterministic

**Exact statement**

> Terminate contracts, subscriptions, licenses, and provider resources only after data and continuity obligations are satisfied.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Terminate contracts, subscriptions, licenses, and provider resources only after data and continuity obligations are satisfied.”, the retained record or configuration fact is mechanically checkable, but the full result still requires specialist interpretation of the applicable legal, contractual, privacy, security, or sustainability obligations; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify termination occurs only after approved obligation records are marked satisfied.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-FFBF3B61` — Nondeterministic

**Exact statement**

> Preserve required source, artifacts, provenance, documentation, audit, incident, and decision records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Preserve required source, artifacts, provenance, documentation, audit, incident, and decision records.”, the retained record or configuration fact is mechanically checkable, but the full result still requires specialist interpretation of the applicable legal, contractual, privacy, security, or sustainability obligations; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify preservation of every artifact enumerated in the approved retention record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-1873E848` — Nondeterministic

**Exact statement**

> Verify no traffic, processing, billing, data collection, or externally reachable surface remains.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of all traffic, processing, billing, collection, and exposure is a universal runtime claim without complete observation or time bounds. Therefore the proposed structured-record composite does not prove the whole rule “Verify no traffic, processing, billing, data collection, or externally reachable surface remains.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Dashboards can show zero while an uninstrumented batch, low-volume call, or billable reserved resource remains. The proposal can Pass while “Verify no traffic, processing, billing, data collection, or externally reachable surface remains.” remains false.

## `USEQ-0173A672` — Nondeterministic

**Exact statement**

> Monitor for unexpected calls and dependencies after shutdown.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Post-shutdown monitoring cannot prove complete unexpected callers/dependencies without universal signals, identity mapping, freshness, and duration thresholds. Therefore the proposed structured-record composite does not prove the whole rule “Monitor for unexpected calls and dependencies after shutdown.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Configured telemetry can stay quiet while an offline or uninstrumented dependency calls later. The proposal can Pass while “Monitor for unexpected calls and dependencies after shutdown.” remains false.

## `USEQ-35351CBA` — Nondeterministic

**Exact statement**

> Destroy media and hardware according to sensitivity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Destroy media and hardware according to sensitivity.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify media-destruction evidence matches the control already assigned to the media sensitivity class.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-90749519` — Nondeterministic

**Exact statement**

> Update asset, architecture, data-flow, service, and ownership inventories.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Inventory updates can be checked for known records but not completeness across assets, architecture, flows, services, and real ownership. Therefore the proposed structured-record composite does not prove the whole rule “Update asset, architecture, data-flow, service, and ownership inventories.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Declared entries can be removed while an undocumented data flow or orphaned owner remains. The proposal can Pass while “Update asset, architecture, data-flow, service, and ownership inventories.” remains false.

## `USEQ-D1519935` — Nondeterministic

**Exact statement**

> Conduct a final security and privacy review.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Review occurrence is recordable, but final security/privacy scope, expertise, evidence, findings, and acceptance require accountable specialist judgment. Therefore the proposed structured-record composite does not prove the whole rule “Conduct a final security and privacy review.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A signed checklist can pass while an exposed backup or privacy obligation is missed. The proposal can Pass while “Conduct a final security and privacy review.” remains false.

## `USEQ-6E6CB31E` — Nondeterministic

**Exact statement**

> Record residual obligations and long-term record owners.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Record residual obligations and long-term record owners.”, the retained record or configuration fact is mechanically checkable, but the full result still requires specialist interpretation of the applicable legal, contractual, privacy, security, or sustainability obligations; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify residual-obligation records identify long-term record owners.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-ADC1A37E` — Nondeterministic

**Exact statement**

> Review lessons and update lifecycle planning.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Meeting notes do not prove lessons are correct, material, learned, or effectively reflected in lifecycle planning. Therefore the proposed structured-record composite does not prove the whole rule “Review lessons and update lifecycle planning.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A retrospective can produce boilerplate actions while the real shutdown failure is not addressed. The proposal can Pass while “Review lessons and update lifecycle planning.” remains false.

## `USEQ-BDDCDDAA` — Nondeterministic

**Exact statement**

> Use individual identities and strong authentication for production access.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Individual identity and authentication settings are partial; strength and complete production-access scope need threat and authority judgment. Therefore the proposed structured-record composite does not prove the whole rule “Use individual identities and strong authentication for production access.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

MFA can cover named users while a shared vendor root or weak recovery token remains. The proposal can Pass while “Use individual identities and strong authentication for production access.” remains false.

## `USEQ-A9A76931` — Nondeterministic

**Exact statement**

> Grant the least privilege for the shortest practical period.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Grant the least privilege for the shortest practical period.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0F159E5F` — Nondeterministic

**Exact statement**

> Separate ordinary development access from privileged production roles.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Role separation must hold across all humans, machines, federation, groups, emergency paths, provider roots, and inherited permissions. Therefore the proposed structured-record composite does not prove the whole rule “Separate ordinary development access from privileged production roles.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Distinct groups can exist while a developer's provider role still reaches production. The proposal can Pass while “Separate ordinary development access from privileged production roles.” remains false.

## `USEQ-7812911D` — Nondeterministic

**Exact statement**

> Require purpose, ticket or incident, approval, scope, and expiry for elevated access.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fields and expiry are exact, but purpose validity, incident linkage, approval authority, and appropriate scope require accountable judgment. Therefore the proposed structured-record composite does not prove the whole rule “Require purpose, ticket or incident, approval, scope, and expiry for elevated access.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

An elevated grant can have every field while using boilerplate purpose and excessive scope. The proposal can Pass while “Require purpose, ticket or incident, approval, scope, and expiry for elevated access.” remains false.

## `USEQ-731B3211` — Nondeterministic

**Exact statement**

> Use controlled administrative entry points and hardened workstations where risk warrants.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use controlled administrative entry points and hardened workstations where risk warrants.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For risks already marked as requiring them, verify controlled administrative entry points and hardened-workstation evidence.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-4C2D10C6` — Nondeterministic

**Exact statement**

> Record commands, queries, changes, targets, time, actor, reason, and outcome proportionately.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Record commands, queries, changes, targets, time, actor, reason, and outcome proportionately.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify production-access records contain command, query or change, target, time, actor, reason, and outcome fields.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5259F535` — Nondeterministic

**Exact statement**

> Protect recordings and audit data from tampering and excessive sensitive-content capture.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Protect recordings and audit data from tampering and excessive sensitive-content capture.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify recordings and audit data have the declared tamper-protection controls enabled.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-D8F85AD0` — Nondeterministic

**Exact statement**

> Avoid direct manual changes when reviewed reproducible automation can perform the work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid direct manual changes when reviewed reproducible automation can perform the work.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-13511541` — Nondeterministic

**Exact statement**

> Use dry run, preview, row limits, transaction, backup, and peer verification for data or destructive operations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Applicability and adequacy of dry runs, limits, transactions, backups, and peer verification depend on operation semantics and failure modes. Therefore the proposed structured-record composite does not prove the whole rule “Use dry run, preview, row limits, transaction, backup, and peer verification for data or destructive operations.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A destructive job can pass preview and backup checks while the backup omits related data. The proposal can Pass while “Use dry run, preview, row limits, transaction, backup, and peer verification for data or destructive operations.” remains false.

## `USEQ-FF5D690D` — Nondeterministic

**Exact statement**

> Prevent broad queries and exports without explicit authorization.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prevent broad queries and exports without explicit authorization.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C749F02A` — Nondeterministic

**Exact statement**

> Make emergency access available during identity or control-plane failure and alert on use.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Emergency-access availability and alerting span identity/control-plane outage combinations, providers, credentials, responders, and delivery channels. Therefore the proposed structured-record composite does not prove the whole rule “Make emergency access available during identity or control-plane failure and alert on use.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A drill can pass while simultaneous SSO and alert-provider failure makes access invisible or unavailable. The proposal can Pass while “Make emergency access available during identity or control-plane failure and alert on use.” remains false.

## `USEQ-E22FBA9C` — Nondeterministic

**Exact statement**

> Review break-glass activity promptly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Review break-glass activity promptly.”, the retained record or configuration fact is mechanically checkable, but the full result still requires whether the timing is prompt enough without a named numeric or policy threshold; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify every break-glass event has a linked review record and review timestamp.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-EC73D6ED` — Nondeterministic

**Exact statement**

> Revoke access automatically on expiry, role change, departure, or incident containment.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Automatic revocation requires complete access inventories and authoritative expiry/role/departure/incident events across every provider and cache. Therefore the proposed structured-record composite does not prove the whole rule “Revoke access automatically on expiry, role change, departure, or incident containment.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Primary IAM can revoke while an offline token or nested SaaS account remains valid. The proposal can Pass while “Revoke access automatically on expiry, role change, departure, or incident containment.” remains false.

## `USEQ-0A33DF21` — Nondeterministic

**Exact statement**

> Prevent support impersonation from hiding the true operator.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** True-operator attribution must survive every impersonation surface, delegated token, proxy, log transformation, and support tool. Therefore the proposed structured-record composite does not prove the whole rule “Prevent support impersonation from hiding the true operator.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

The main audit log can retain operator ID while an alternate support console records only the customer. The proposal can Pass while “Prevent support impersonation from hiding the true operator.” remains false.

## `USEQ-5C0FF165` — Nondeterministic

**Exact statement**

> Verify user and tenant context before acting on customer data.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** User/tenant verification requires authoritative identity/context, every support action path, race state, and valid human decision. Therefore the proposed structured-record composite does not prove the whole rule “Verify user and tenant context before acting on customer data.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `human_accountability`

### Skeptical rejection counterexample

Normal cases can pass while stale tenant context or lookalike identity causes action on the wrong account. The proposal can Pass while “Verify user and tenant context before acting on customer data.” remains false.

## `USEQ-BAD2E536` — Nondeterministic

**Exact statement**

> Use two-person control for irreversible high-impact operations where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use two-person control for irreversible high-impact operations where appropriate.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For operations already marked as requiring two-person control, verify two distinct approvals.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-EB86FC6E` — Nondeterministic

**Exact statement**

> Test access paths and required tools before an incident.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Pre-incident tests sample access paths, tools, outages, identities, regions, and future state rather than proving incident-time availability. Therefore the proposed structured-record composite does not prove the whole rule “Test access paths and required tools before an incident.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A drill can pass before a certificate or provider change makes the real path fail. The proposal can Pass while “Test access paths and required tools before an incident.” remains false.

## `USEQ-0126D4F9` — Nondeterministic

**Exact statement**

> Review dormant privileges and actual usage.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Usage times are exact, but dormant thresholds, privilege need, complete identity scope, and substantive review require judgment. Therefore the proposed structured-record composite does not prove the whole rule “Review dormant privileges and actual usage.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Listed roles can be reviewed while an omitted service identity keeps unused broad access. The proposal can Pass while “Review dormant privileges and actual usage.” remains false.

## `USEQ-D8EB3355` — Nondeterministic

**Exact statement**

> Measure manual change, failed intervention, access exception, and resulting incident trends.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Counts/trends are computable only from complete manual-change/intervention/exception/incident populations and correct causal classification. Therefore the proposed structured-record composite does not prove the whole rule “Measure manual change, failed intervention, access exception, and resulting incident trends.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Trends can improve because console changes and incidents without linked interventions are missing. The proposal can Pass while “Measure manual change, failed intervention, access exception, and resulting incident trends.” remains false.

## `USEQ-A5591561` — Nondeterministic

**Exact statement**

> Convert repeated manual operations into safe automation and runbooks.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Convert repeated manual operations into safe automation and runbooks.”, the retained record or configuration fact is mechanically checkable, but the full result still requires the adequacy, safety, credibility, clarity, or operational meaning of the recorded action; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify repeated manual operations selected for conversion have linked automation and runbook artifacts.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5CF3177D` — Nondeterministic

**Exact statement**

> Define which products, services, organizations, suppliers, users, regions, infrastructure, lifecycle stages, and environmental impact categories are inside the sustainability assessment boundary.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Assessment boundary completeness and materiality across products, suppliers, users, regions, lifecycle, infrastructure, and impact categories require specialist judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define which products, services, organizations, suppliers, users, regions, infrastructure, lifecycle stages, and environmental impact categories are inside the sustainability assessment boundary.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A boundary document can omit a major supplier or embodied-impact category while all fields are present. The proposal can Pass while “Define which products, services, organizations, suppliers, users, regions, infrastructure, lifecycle stages, and environmental impact categories are inside the sustainability assessment boundary.” remains false.

## `USEQ-13992319` — Nondeterministic

**Exact statement**

> Assign an accountable sustainability owner with authority to influence product, architecture, procurement, operations, and retirement decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Owner identity is recordable, but accountability and real authority across product, architecture, procurement, operations, and retirement are organizational facts. Therefore the proposed structured-record composite does not prove the whole rule “Assign an accountable sustainability owner with authority to influence product, architecture, procurement, operations, and retirement decisions.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A named owner can lack budget or decision rights and be unable to influence outcomes. The proposal can Pass while “Assign an accountable sustainability owner with authority to influence product, architecture, procurement, operations, and retirement decisions.” remains false.

## `USEQ-58F7D983` — Nondeterministic

**Exact statement**

> Define decision rights for resolving conflicts among sustainability, accessibility, security, privacy, safety, reliability, performance, affordability, and business requirements.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Decision-right fields do not prove legitimate authority or workable resolution of conflicts among nine values. Therefore the proposed structured-record composite does not prove the whole rule “Define decision rights for resolving conflicts among sustainability, accessibility, security, privacy, safety, reliability, performance, affordability, and business requirements.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A matrix can name an executive who lacks accessibility or safety competence and still pass. The proposal can Pass while “Define decision rights for resolving conflicts among sustainability, accessibility, security, privacy, safety, reliability, performance, affordability, and business requirements.” remains false.

## `USEQ-88774BC5` — Nondeterministic

**Exact statement**

> Include affected users, workers, communities, suppliers, and environmental stakeholders in material sustainability decisions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Include affected users, workers, communities, suppliers, and environmental stakeholders in material sustainability decisions.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-888DCA36` — Nondeterministic

**Exact statement**

> Treat environmental, social, and economic sustainability as connected concerns rather than optimizing carbon in isolation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Treat environmental, social, and economic sustainability as connected concerns rather than optimizing carbon in isolation.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-21CC1B21` — Nondeterministic

**Exact statement**

> Document the organization’s theory of change, material impacts, dependencies, assumptions, and limits of influence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Document sections cannot prove theory-of-change validity, material impacts, complete dependencies/assumptions, or realistic influence limits. Therefore the proposed structured-record composite does not prove the whole rule “Document the organization’s theory of change, material impacts, dependencies, assumptions, and limits of influence.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Plausible prose can omit rebound effects or a supplier dependency that reverses the claimed outcome. The proposal can Pass while “Document the organization’s theory of change, material impacts, dependencies, assumptions, and limits of influence.” remains false.

## `USEQ-D3F0C27B` — Nondeterministic

**Exact statement**

> Identify relevant environmental laws, reporting rules, customer commitments, procurement requirements, and sector-specific obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Identify relevant environmental laws, reporting rules, customer commitments, procurement requirements, and sector-specific obligations.” is nondeterministic because it depends on identifying or applying obligations and expertise that require an accountable specialist.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-6C5D6E42` — Nondeterministic

**Exact statement**

> Prohibit sustainability claims that cannot be traced to a defined boundary, method, evidence set, time period, and responsible approver.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Claim metadata is exact, but complete public/internal claim inventory, boundary/method validity, evidence sufficiency, and approver authority are not. Therefore the proposed structured-record composite does not prove the whole rule “Prohibit sustainability claims that cannot be traced to a defined boundary, method, evidence set, time period, and responsible approver.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

Registered claims can be traceable while an omitted marketing claim or invalid method still passes. The proposal can Pass while “Prohibit sustainability claims that cannot be traced to a defined boundary, method, evidence set, time period, and responsible approver.” remains false.

## `USEQ-7B0F07E8` — Nondeterministic

**Exact statement**

> Distinguish mandatory requirements, voluntary goals, experiments, estimates, and aspirational targets in all plans and communications.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Distinguish mandatory requirements, voluntary goals, experiments, estimates, and aspirational targets in all plans and communications.” is nondeterministic because it makes an absolute or continuing guarantee that bounded repository, record, and runtime evidence cannot prove for every future time and execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-7A460804` — Nondeterministic

**Exact statement**

> Include sustainability requirements in product requirements, architecture decisions, supplier contracts, Definition of Done, release gates, and retirement plans.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Inclusion across requirements, architecture, contracts, DoD, gates, and retirement needs complete artifact scope and meaningful sustainability content. Therefore the proposed structured-record composite does not prove the whole rule “Include sustainability requirements in product requirements, architecture decisions, supplier contracts, Definition of Done, release gates, and retirement plans.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Each document type can mention sustainability with boilerplate while material impacts remain ungated. The proposal can Pass while “Include sustainability requirements in product requirements, architecture decisions, supplier contracts, Definition of Done, release gates, and retirement plans.” remains false.

## `USEQ-A060C72B` — Nondeterministic

**Exact statement**

> Use independent review for material public claims, regulated disclosures, major lifecycle assessments, and high-impact trade-offs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Use independent review for material public claims, regulated disclosures, major lifecycle assessments, and high-impact trade-offs.”, the retained record or configuration fact is mechanically checkable, but the full result still requires specialist interpretation of the applicable legal, contractual, privacy, security, or sustainability obligations; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For decisions already assigned to an independent-review tier, verify an independent-review record exists.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-1885C630` — Nondeterministic

**Exact statement**

> Keep sustainability governance active after launch through operational ownership, reassessment triggers, and funded improvement work.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** In the compound rule “Keep sustainability governance active after launch through operational ownership, reassessment triggers, and funded improvement work.”, the retained record or configuration fact is mechanically checkable, but the full result still requires which cases are applicable, appropriate, representative, material, critical, or otherwise in scope; therefore the control is nondeterministic.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify post-launch sustainability records contain an owner, reassessment triggers, and a funded-work decision.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-CBFAABA8` — Nondeterministic

**Exact statement**

> Assess whether the product, feature, automation, data collection, model, or recurring process is necessary before optimizing its implementation.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Necessity of a product/feature/process is a value, user, and business decision not proven by an assessment record. Therefore the proposed structured-record composite does not prove the whole rule “Assess whether the product, feature, automation, data collection, model, or recurring process is necessary before optimizing its implementation.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A team can document that a low-value automation is necessary using unsupported assumptions. The proposal can Pass while “Assess whether the product, feature, automation, data collection, model, or recurring process is necessary before optimizing its implementation.” remains false.

## `USEQ-22D56445` — Nondeterministic

**Exact statement**

> Define the user or societal outcome that justifies the resource demand and identify lower-impact non-digital or simpler digital alternatives.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Justified societal/user outcomes and complete lower-impact alternatives require values, user evidence, system boundaries, and comparative analysis. Therefore the proposed structured-record composite does not prove the whole rule “Define the user or societal outcome that justifies the resource demand and identify lower-impact non-digital or simpler digital alternatives.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A document can state a vague benefit and ignore a viable non-digital alternative. The proposal can Pass while “Define the user or societal outcome that justifies the resource demand and identify lower-impact non-digital or simpler digital alternatives.” remains false.

## `USEQ-97517034` — Nondeterministic

**Exact statement**

> Reject features whose expected harm, waste, manipulation, exclusion, or maintenance burden outweighs evidenced benefit.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reject features whose expected harm, waste, manipulation, exclusion, or maintenance burden outweighs evidenced benefit.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-1D0695BB` — Nondeterministic

**Exact statement**

> Reduce unnecessary engagement loops, autoplay, infinite consumption, dark patterns, spam, and attention extraction that create avoidable resource use.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Reduce unnecessary engagement loops, autoplay, infinite consumption, dark patterns, spam, and attention extraction that create avoidable resource use.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5DC602F1` — Nondeterministic

**Exact statement**

> Design pricing, quotas, defaults, incentives, and success metrics so they do not reward wasteful usage or artificial activity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Design pricing, quotas, defaults, incentives, and success metrics so they do not reward wasteful usage or artificial activity.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-4705259A` — Nondeterministic

**Exact statement**

> Assess whether efficiency improvements could increase total demand and offset or reverse expected reductions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Rebound-effect assessment needs causal demand models, behavioral responses, time horizon, and uncertainty judgment. Therefore the proposed structured-record composite does not prove the whole rule “Assess whether efficiency improvements could increase total demand and offset or reverse expected reductions.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A model can show savings under fixed demand while efficiency induces usage growth that erases them. The proposal can Pass while “Assess whether efficiency improvements could increase total demand and offset or reverse expected reductions.” remains false.

## `USEQ-4FBD8481` — Nondeterministic

**Exact statement**

> Measure absolute environmental impact as well as normalized efficiency so growth is not hidden by better per-unit metrics.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Environmental impact measurement requires authoritative boundaries, factors, allocation, data quality, time periods, and specialist methods. Therefore the proposed structured-record composite does not prove the whole rule “Measure absolute environmental impact as well as normalized efficiency so growth is not hidden by better per-unit metrics.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`

### Skeptical rejection counterexample

Absolute and normalized numbers can be computed from incomplete emissions factors and still be wrong. The proposal can Pass while “Measure absolute environmental impact as well as normalized efficiency so growth is not hidden by better per-unit metrics.” remains false.

## `USEQ-F1113914` — Nondeterministic

**Exact statement**

> Establish environmental and human budgets for features, campaigns, experiments, models, data products, and infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Budget values are exact, but appropriate environmental/human limits, covered impacts, allocation, and enforceability require accountable judgment. Therefore the proposed structured-record composite does not prove the whole rule “Establish environmental and human budgets for features, campaigns, experiments, models, data products, and infrastructure.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Every feature can have a budget set so high that it constrains nothing. The proposal can Pass while “Establish environmental and human budgets for features, campaigns, experiments, models, data products, and infrastructure.” remains false.

## `USEQ-99D85387` — Nondeterministic

**Exact statement**

> Prioritize reuse, repair, extension, consolidation, and retirement before creating additional systems or duplicate capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Prioritize reuse, repair, extension, consolidation, and retirement before creating additional systems or duplicate capabilities.” is nondeterministic because it governs a risk, value, or trade-off decision whose acceptance must remain with accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-6701A91D` — Nondeterministic

**Exact statement**

> Define explicit sunset criteria for low-value features, experiments, data sets, models, environments, domains, and services.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Sunset-criteria fields cannot prove low-value classification, adequate thresholds, complete asset scope, or enforceable removal. Therefore the proposed structured-record composite does not prove the whole rule “Define explicit sunset criteria for low-value features, experiments, data sets, models, environments, domains, and services.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Criteria can exist while an omitted obsolete dataset or vague value test prevents retirement. The proposal can Pass while “Define explicit sunset criteria for low-value features, experiments, data sets, models, environments, domains, and services.” remains false.

## `USEQ-0EF58E65` — Nondeterministic

**Exact statement**

> Include long-term maintenance, migration, support, and end-of-life costs in business cases.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Business-case headings do not prove complete/accurate long-term maintenance, migration, support, and end-of-life cost estimates. Therefore the proposed structured-record composite does not prove the whole rule “Include long-term maintenance, migration, support, and end-of-life costs in business cases.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A model can include all categories while excluding supplier exit and long-tail support costs. The proposal can Pass while “Include long-term maintenance, migration, support, and end-of-life costs in business cases.” remains false.

## `USEQ-75B2BF2E` — Nondeterministic

**Exact statement**

> Avoid shifting environmental cost to users through excessive device requirements, bandwidth, battery consumption, printing, travel, or repeated manual effort.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The rule “Avoid shifting environmental cost to users through excessive device requirements, bandwidth, battery consumption, printing, travel, or repeated manual effort.” is nondeterministic because it requires judging adequacy, relevance, necessity, risk, or expected effects in project context, so competent reviewers can disagree on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F4CC9192` — Nondeterministic

**Exact statement**

> Define a reproducible measurement method before using a sustainability metric as a target or claim.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** A method record and ordering are checkable, but reproducibility needs a declared repeatability protocol and tolerance that this rule does not supply.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every sustainability metric used as a target or claim has a versioned measurement-method record created before that use.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2E6577FB` — Nondeterministic

**Exact statement**

> Specify the functional unit used to normalize impact and prove that it represents meaningful delivered value rather than convenient activity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The functional-unit field is objective, but whether it represents meaningful delivered value requires product and sustainability judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every impact measurement record identifies its normalization functional unit.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5F686CE1` — Nondeterministic

**Exact statement**

> Define organizational, operational, temporal, geographic, lifecycle, and supplier boundaries for every measurement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because measurement boundaries require complete organizational, operational, temporal, geographic, lifecycle, and supplier scope plus domain judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A supplier region is omitted while every registered measurement has six boundary fields.

## `USEQ-3FFF3A34` — Nondeterministic

**Exact statement**

> Separate operational energy and emissions from embodied impacts associated with hardware, facilities, networks, and manufacturing.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because operational versus embodied impact classification and complete hardware, facility, network, and manufacturing attribution require specialist methods.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Manufacturing energy is labeled operational, while all impact report categories remain separately populated.

## `USEQ-1D668E78` — Nondeterministic

**Exact statement**

> Measure or estimate greenhouse-gas emissions, energy, water, materials, electronic waste, land or biodiversity effects, and relevant pollution where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recorded values are checkable, but deciding which environmental effects are material requires contextual sustainability judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every environmental effect classified as material has a measured or estimated value and method reference.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9E6E23B4` — Nondeterministic

**Exact statement**

> Distinguish direct measurement, provider data, modeled estimates, proxies, and assumptions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because source-type labels do not prove truthful distinction among direct data, provider data, models, proxies, and assumptions.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A modeled estimate is labeled direct measurement; the required enum passes.

## `USEQ-1C242EF6` — Nondeterministic

**Exact statement**

> Record data sources, sampling, allocation rules, emission factors, grid factors, conversion methods, and versions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because source, sampling, allocation, factor, conversion, and version fields do not prove complete, authoritative, valid measurement methods.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A report uses an obsolete grid factor and biased sample while every field exists.

## `USEQ-18A9F03F` — Nondeterministic

**Exact statement**

> Report material uncertainty, data gaps, sensitivity, and confidence rather than presenting estimates as exact facts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Uncertainty fields and estimate labels are objective, but determining which uncertainty and gaps are material requires expert judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every estimated result is labeled as an estimate and records uncertainty, known gaps, sensitivity, and confidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-8E40F7EA` — Nondeterministic

**Exact statement**

> Use consistent methods across baselines and comparison periods or disclose and quantify method changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because method-version equality or a quantified-change field does not prove comparable populations or valid quantification of method effects.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Two periods use the same version but changed sampling coverage, so the comparison is invalid and still passes.

## `USEQ-D9D0B323` — Nondeterministic

**Exact statement**

> Separate absolute totals, intensity metrics, avoided impact, enabled impact, and offsets; do not substitute one for another.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because separate type labels do not prevent misleading substitution in narrative, aggregation, targets, or decisions.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: Offsets are reported separately but used to claim absolute emissions fell; record types pass.

## `USEQ-605351E9` — Nondeterministic

**Exact statement**

> Do not count hypothetical avoided emissions as equivalent to measured reductions in the product’s own footprint.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recognizing when wording treats a hypothetical counterfactual as equivalent to an observed reduction requires semantic and specialist review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-9B9B9BBD` — Nondeterministic

**Exact statement**

> Do not use offsets to avoid feasible direct reduction work, and disclose offset quality and boundaries when offsets are reported.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Offset disclosures are objective, but feasible direct reduction work and offset quality require accountable environmental judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every reported offset has a record of its type, amount, quality evidence, and accounting boundaries.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D9A7B76F` — Nondeterministic

**Exact statement**

> Protect measurement telemetry against tampering, accidental double counting, inappropriate aggregation, and privacy leakage.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequate protection against tampering, double counting, aggregation errors, and privacy leakage is a broad threat and privacy judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

## `USEQ-7F6B32D9` — Nondeterministic

**Exact statement**

> Validate measurement pipelines and reconcile them against billing, capacity, asset, and provider records where possible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Pipeline validation and reconciliation evidence are checkable, but where reconciliation is possible is not objectively defined.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every measurement pipeline has a current validation result and records reconciliation results for each declared billing, capacity, asset, or provider source.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-55AD6D41` — Nondeterministic

**Exact statement**

> Establish time-bounded, measurable targets linked to a documented baseline and product outcome.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because target baselines, product outcome links, thresholds, measurability, ambition, and deadline appropriateness require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A target reduces one unit by 2099 from a cherry-picked baseline; every field passes.

## `USEQ-41D1322A` — Nondeterministic

**Exact statement**

> Decompose organizational goals into product, service, workload, supplier, asset, and team responsibilities without double counting.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Responsibility assignments and overlap checks are objective for an approved scope, but deciding the applicable decomposition is organizational judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every approved goal allocation names its product, service, workload, supplier, asset, and team responsibilities and has no duplicate allocation identifier.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-006458B6` — Nondeterministic

**Exact statement**

> Use budgets for transfer, requests, compute, memory, storage, retention, model inference, training, build work, and observability where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Budget fields can be checked for approved material categories, but materiality of each resource category requires contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every resource category classified as material has an approved numeric budget and unit.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-1CDC439C` — Nondeterministic

**Exact statement**

> Define thresholds that trigger investigation, rollback, redesign, capacity review, or retirement.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because investigation, rollback, redesign, capacity, and retirement thresholds require risk, impact, feasibility, and authority judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: The policy sets every trigger above any possible value, so fields exist but no action ever triggers.

## `USEQ-4BBD1617` — Nondeterministic

**Exact statement**

> Prioritize work using expected impact, confidence, cost, reversibility, time to benefit, and effects on people and resilience.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Expected impact, confidence, cost, reversibility, benefit timing, and human or resilience effects require accountable prioritization judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-9CE76496` — Nondeterministic

**Exact statement**

> Record significant sustainability trade-offs in architecture and product decision records.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Decision-record linkage is objective, but deciding which trade-offs are significant requires accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every trade-off classified as significant has a linked architecture or product decision record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-63805953` — Nondeterministic

**Exact statement**

> Test whether an optimization simply moves impact between client and server, region and region, present and future, or organization and user.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because impact transfer across clients, servers, regions, time, organization, and users needs lifecycle boundaries, counterfactuals, and complete tests.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: The test sees server savings but omits future battery replacement caused on clients; all four test labels pass.

## `USEQ-81CEFB3A` — Nondeterministic

**Exact statement**

> Prefer interventions that improve multiple quality attributes without creating hidden harms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether an intervention improves several qualities and creates hidden harms is a context-dependent multi-objective design judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

## `USEQ-00554C4D` — Nondeterministic

**Exact statement**

> Require a compensating plan when a justified feature exceeds an approved budget.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because justification, exception scope, compensating-plan effectiveness, owner authority, and credible due date require judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A budget exception links to buy offsets later with a departed owner; record fields pass.

## `USEQ-04D91E1E` — Nondeterministic

**Exact statement**

> Do not average away severe local harm through favorable global totals.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Severe local harm and the acceptability of aggregation require environmental, ethical, and stakeholder judgment without fixed thresholds.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_threshold`, `contextual_judgment`

## `USEQ-B5D5DF49` — Nondeterministic

**Exact statement**

> Review targets after material traffic growth, architecture change, supplier change, hardware refresh, model change, incident, or new measurement evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recorded change events and target reviews are checkable, but deciding whether a change is material needs contextual judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change marked material in the change registry has a subsequent target-review record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5AE35A89` — Nondeterministic

**Exact statement**

> Track improvement actions to verified outcomes rather than closing them when implementation activity is complete.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because verified outcome requires causal measurement, baseline, success threshold, time horizon, and external effects beyond a link.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An action closes after a correlated metric improves due to lower traffic, while the verified-result field passes.

## `USEQ-CB209167` — Nondeterministic

**Exact statement**

> Design the shortest understandable path to the user’s legitimate goal and remove unnecessary steps, reloads, retries, and repeated data entry.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The shortest understandable legitimate journey and unnecessary interaction steps require user research and product-design judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

## `USEQ-C45FED74` — Nondeterministic

**Exact statement**

> Make essential journeys usable on lower-powered devices, slower or intermittent networks, small screens, assistive technologies, and constrained data plans where the intended population requires it.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Usability across devices, networks, screens, assistive technology, and data constraints needs a representative empirical protocol and population definition.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `contextual_judgment`

## `USEQ-4DFA935A` — Nondeterministic

**Exact statement**

> Preserve accessibility, readability, safety, and user control when reducing assets or functionality.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Preserving accessibility, readability, safety, and user control requires specialist evaluation of actual user outcomes.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-8C18F591` — Nondeterministic

**Exact statement**

> Use progressive enhancement and graceful degradation so core outcomes do not depend unnecessarily on high-cost capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Core outcomes, unnecessary dependency, graceful degradation, and acceptable enhancement behavior require contextual product and user testing.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

## `USEQ-630FB495` — Nondeterministic

**Exact statement**

> Let users control autoplay, animation, background activity, notifications, downloads, synchronization, quality levels, and data-heavy behavior where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The presence and operation of controls are testable, but practicality and the applicable behavior set require product judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every behavior classified as user-controllable has a tested control for enabling, disabling, or reducing it.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-01CB4937` — Nondeterministic

**Exact statement**

> Default nonessential high-impact behavior to off or to the lowest reasonable resource level.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Nonessential, high-impact, and the lowest reasonable default are contextual product and sustainability judgments with no fixed threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-F78DBB93` — Nondeterministic

**Exact statement**

> Communicate the resource, cost, privacy, or quality consequences of meaningful user choices without coercion or blame.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether consequences are meaningful, accurate, noncoercive, and nonblaming requires content, privacy, and user-experience judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-B2FF50FE` — Nondeterministic

**Exact statement**

> Prevent accidental repeated submissions, downloads, payments, renders, uploads, and notifications.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Duplicate-action tests are objective for known actions, but exhaustive identification of accidental repeat paths is not bounded by this rule.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered submission, download, payment, render, upload, and notification action passes its duplicate-trigger prevention test.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FDABAD0D` — Nondeterministic

**Exact statement**

> Use clear status, validation, recovery, and offline messaging to reduce wasted attempts and support demand.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Clarity and the claim that messages reduce wasted attempts or support demand require representative user evidence and judgment.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

## `USEQ-9040A903` — Nondeterministic

**Exact statement**

> Avoid forced device upgrades, unnecessary browser restrictions, or short compatibility windows without documented necessity.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether an upgrade, restriction, or compatibility window is forced, unnecessary, or justified depends on product and security context.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-63E6B914` — Nondeterministic

**Exact statement**

> Measure task success, abandonment, retries, support contacts, and time spent as sustainability signals as well as UX signals.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because telemetry cannot prove complete tasks, support paths, failures, retries, abandonment, duration, and valid sustainability interpretation.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Abandoned offline tasks are absent, while registered telemetry exposes all five named signals.

## `USEQ-CBB44B7C` — Nondeterministic

**Exact statement**

> Involve representative users early, including people affected by bandwidth, energy, device, language, disability, or economic constraints.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Representative participation and which constrained populations are affected require accountable research sampling and stakeholder judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_scope`, `contextual_judgment`

## `USEQ-8A741146` — Nondeterministic

**Exact statement**

> Publish only content that has an identified audience, owner, purpose, review date, retention rule, and retirement path.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because published-content completeness and meaningful audience, purpose, ownership, retention, and retirement require editorial judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A shadow microsite is omitted and another page names everyone as audience; records pass.

## `USEQ-FE511AC4` — Nondeterministic

**Exact statement**

> Remove or archive stale, duplicate, contradictory, unowned, and low-value content.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Staleness, duplication, contradiction, ownership, and low value across all content require semantic review and an unbounded content scope.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

## `USEQ-A08EC0B1` — Nondeterministic

**Exact statement**

> Use concise, structured, plain-language content that helps users complete tasks without repeated searches or support contacts.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Conciseness, plain language, task success, and avoided searches or contacts require user-centered content judgment and empirical evidence.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

## `USEQ-2C3E104A` — Nondeterministic

**Exact statement**

> Provide text or lower-bandwidth alternatives for media where they preserve the intended outcome.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a lower-bandwidth alternative preserves the intended outcome requires accessibility and user-experience evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-C291BB52` — Nondeterministic

**Exact statement**

> Select image, audio, video, animation, font, and document formats appropriate to actual quality needs and supported clients.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Appropriate formats and actual quality needs depend on media purpose, supported users, devices, and accessibility context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-97644690` — Nondeterministic

**Exact statement**

> Compress, resize, crop, transcode, and deliver media according to display context rather than shipping source-quality assets by default.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Generated variants and delivery rules are inspectable, but whether their quality matches each display context requires perceptual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered media asset has transformed variants and a delivery rule keyed to declared display contexts.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-A6FE8EE0` — Nondeterministic

**Exact statement**

> Avoid autoplay, decorative high-resolution media, unnecessary background video, and repeated asset downloads.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Autoplay flags and repeat downloads can be tested, but decorative, high-resolution, and unnecessary are contextual judgments.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Registered media surfaces do not autoplay and pass the configured repeated-download test unless an approved exception exists.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-816E7255` — Nondeterministic

**Exact statement**

> Use responsive media and explicit dimensions to avoid unnecessary transfer and relayout.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because visual-media inventory completeness and actual responsive delivery, unnecessary transfer, aspect ratio, and relayout behavior are not proven by metadata.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unregistered hero image downloads 8K bytes to phones, while registered media has width and height.

## `USEQ-53953C3F` — Nondeterministic

**Exact statement**

> Subset and preload fonts only when evidence shows benefit; minimize font families, weights, variants, and blocking behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Benchmark evidence for preloads is checkable, but benefit and minimal font variety or blocking behavior require contextual thresholds.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every font preload or subset decision links to current measurement evidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-6B5533CE` — Nondeterministic

**Exact statement**

> Design downloadable documents for reuse, accessibility, efficient file size, and long-term readability.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reuse, accessibility, efficient size, and long-term readability require document-format, user, and preservation judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-DD89FE18` — Nondeterministic

**Exact statement**

> Avoid requiring printing when an accessible digital workflow can achieve the outcome.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether an accessible digital workflow can achieve the outcome and make printing unnecessary is context-dependent.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

## `USEQ-E4ABE085` — Nondeterministic

**Exact statement**

> Prevent marketing, analytics, and notification campaigns from creating disproportionate transfer, storage, attention, or support waste.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Disproportionate transfer, storage, attention, and support waste requires campaign impact thresholds and stakeholder judgment absent here.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-A60E4DA2` — Nondeterministic

**Exact statement**

> Expire obsolete feeds, previews, cached assets, derivatives, and campaign resources safely.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because obsolete resource discovery, safe expiry, reference completeness, rollback, and recovery are not bounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An unregistered campaign feed is still referenced after removal, while classified resources pass expiry tests.

## `USEQ-7477455D` — Nondeterministic

**Exact statement**

> Set measurable client-side budgets for transferred bytes, requests, execution time, memory, energy-sensitive work, long tasks, and background activity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because client budgets can be populated but unsafe, incomparable, device-insensitive, or unrelated to user outcomes.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: Every budget is set to an effectively unlimited value; all seven limits exist.

## `USEQ-481F4341` — Nondeterministic

**Exact statement**

> Remove unused, duplicate, obsolete, unreachable, and unnecessarily generated code and assets.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Findings from bounded analyzers are objective, but proving absence of all unused or unreachable generated code and assets is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every item reported by the approved unused, duplicate, obsolete, or reachability analyzers is removed or has an approved exception.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `repository`

## `USEQ-99D96F89` — Nondeterministic

**Exact statement**

> Defer, lazy-load, stream, or conditionally load nonessential resources without harming accessibility, discoverability, security, or correctness.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Loading behavior and selected quality tests are objective, but nonessential scope and absence of harm across all qualities require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every resource classified as nonessential passes its configured deferred, lazy, streamed, or conditional loading test.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E2330712` — Nondeterministic

**Exact statement**

> Prefer native platform capabilities and simpler representations when they meet requirements with less code and dependency overhead.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a native capability or simpler representation meets all requirements with lower overhead is an architectural trade-off judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-49967306` — Nondeterministic

**Exact statement**

> Modularize high-cost functionality so users who do not need it do not pay its resource cost.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because high-cost feature and payload completeness plus all noninvoking user paths, cache states, preload, and shared bundles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A shared bundle loads the feature code on one locale not in tests, while registered paths load none.

## `USEQ-E0C0004A` — Nondeterministic

**Exact statement**

> Treat first-party and third-party code under the same performance, privacy, security, and sustainability budgets.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because first- and third-party component completeness and equivalently meaningful performance, privacy, security, and sustainability budgets are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An injected third-party tag is omitted, while all inventoried components have four budget links.

## `USEQ-588808D0` — Nondeterministic

**Exact statement**

> Minimize polling, timers, observers, background synchronization, speculative work, and network chatter.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Minimal polling, timers, observers, synchronization, speculative work, and chatter has no objective lower bound and depends on behavior needs.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-92BC5536` — Nondeterministic

**Exact statement**

> Pause or reduce work when the application is hidden, inactive, offline, on a constrained connection, or under user power-saving preferences where safe.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Lifecycle-state behavior tests are objective, but which work can safely pause or reduce is a contextual safety decision.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every task approved for reduction passes tests for hidden, inactive, offline, constrained-network, and power-saving states.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D0E5ADD2` — Nondeterministic

**Exact statement**

> Use caching carefully to avoid repeated transfer while preventing stale, private, or permanently orphaned data.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because cache keys, states, users, partitions, staleness, privacy leakage, expiry, orphan paths, and recovery are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A cross-tenant stale response leaks through an omitted key variant, while registered cache cases pass.

## `USEQ-0555DF66` — Nondeterministic

**Exact statement**

> Choose rendering and hydration strategies based on measured end-to-end cost across server, network, and client rather than fashion.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because measurement links do not prove representative end-to-end server, network, and client cost or that the strategy decision followed evidence.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: The team chooses hydration first then backfills tiny synthetic measurements; all three links pass.

## `USEQ-A084DB55` — Nondeterministic

**Exact statement**

> Prevent memory leaks, runaway event handlers, duplicate subscriptions, and retained resources that waste energy over long sessions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Long-session tests can detect specified leaks and runaway handlers, but universal absence across all runtime paths is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered long-session scenario passes memory-growth, event-handler, duplicate-subscription, and retained-resource thresholds.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7E8877C1` — Nondeterministic

**Exact statement**

> Include client resource regressions in continuous integration and production monitoring.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because CI and production metrics do not prove complete client resources, representative populations, comparable indicators, or safe thresholds.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A low-end device memory regression is absent from production sampling, while configured indicators pass.

## `USEQ-7110753C` — Nondeterministic

**Exact statement**

> Keep semantic, accessible, indexable output while optimizing implementation size and execution.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Semantic meaning, accessibility, indexability, and acceptable optimization outcomes need specialist and user evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-B2C8404E` — Nondeterministic

**Exact statement**

> Define resource budgets and cost models for high-volume endpoints, jobs, event flows, searches, reports, and data transformations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because high-volume subject completeness and valid resource budgets and cost models require demand and workload judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: An unregistered report job dominates cost, while listed endpoints and jobs have budget records.

## `USEQ-523BAFB1` — Nondeterministic

**Exact statement**

> Choose algorithms and data structures based on realistic scale, resource use, correctness, and maintainability.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Benchmark evidence is objective, but realistic scale, maintainability, and the final algorithm or data-structure trade-off require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every algorithm or data-structure decision classified as material links to scale, resource-use, and correctness measurements.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-FB833D84` — Nondeterministic

**Exact statement**

> Eliminate repeated computation, unnecessary serialization, avoidable fan-out, excessive round trips, and duplicate storage.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Unnecessary computation, avoidable fan-out, excessive trips, and duplicate storage are contextual and unbounded across system behavior.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

## `USEQ-E186F16E` — Nondeterministic

**Exact statement**

> Bound requests, pagination, query complexity, response size, retries, recursion, batch size, and execution duration.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because request and work-path inventories, dynamic recursion, retries, batching, response growth, and alternate interfaces are not complete.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An unlisted export query has recursive expansion and no maximum, while registered paths have numeric limits.

## `USEQ-90DB858D` — Nondeterministic

**Exact statement**

> Use batching, caching, materialization, precomputation, and asynchronous processing only when freshness, consistency, authorization, and failure behavior remain correct.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Strategy configuration and selected tests are objective, but proving freshness, consistency, authorization, and failure correctness over all behavior is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered batching, caching, materialization, precomputation, or async path passes its declared freshness, consistency, authorization, and failure tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8FC65F49` — Nondeterministic

**Exact statement**

> Optimize database access using measured plans, indexes, cardinality, locality, and workload behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because plan, index, cardinality, locality, and workload evidence do not prove an optimization improves real behavior without regressions.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A measured plan improves one query but worsens writes and another workload; all evidence links exist.

## `USEQ-B1F3AB1F` — Nondeterministic

**Exact statement**

> Prevent unbounded scans, N-plus-one patterns, unused columns, excessive joins, and repeatedly derived values where they are material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Known analyzer findings can be enforced, but materiality and complete absence of every costly database pattern require context and unbounded discovery.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every finding from approved scan, query-plan, and runtime detectors for the listed patterns is resolved or has an approved exception.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `repository`

## `USEQ-8F0A9252` — Nondeterministic

**Exact statement**

> Define data freshness and refresh frequencies based on user need rather than maximum technical frequency.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Data freshness and refresh frequency based on user need require product research and accountable trade-off decisions.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-10CDDC3D` — Nondeterministic

**Exact statement**

> Use event-driven or scheduled work rather than continuous polling when it is reliably more efficient.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** A comparative benchmark is objective, but the threshold for reliably more efficient and suitability of event or schedule semantics is contextual.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every polling replacement decision links to measured event-driven or scheduled-work efficiency and reliability results.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-21F0724F` — Nondeterministic

**Exact statement**

> Deduplicate idempotent work and prevent retry storms, hot loops, poison messages, and unbounded queue growth.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because work types, retries, interleavings, poison inputs, failure combinations, queue partitions, and maximum-growth oracles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A retry storm spans two queues outside the matrix, while each registered work type passes alone.

## `USEQ-951A3A18` — Nondeterministic

**Exact statement**

> Control observability volume, cardinality, sampling, and retention without losing required diagnostic, security, or audit evidence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because required diagnostic, security, and audit evidence plus acceptable volume, sampling, cardinality, and retention are contextual.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A required rare security field is sampled away but not marked required; policy checks pass.

## `USEQ-4C7049BA` — Nondeterministic

**Exact statement**

> Continuously identify and retire low-value jobs, indexes, replicas, caches, exports, reports, and derived data.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Continuous identification has no cadence and low value for jobs or data products requires accountable product and operations judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `undefined_protocol`

## `USEQ-365B130E` — Nondeterministic

**Exact statement**

> Test resource behavior with representative volume, skew, cache state, concurrency, failure, and recovery.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because representative volume, skew, cache, concurrency, failures, recovery, duration, and acceptable resource behavior lack complete definitions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: Tests use average load, while rare hot-key skew exhausts memory in production.

## `USEQ-B2C4739E` — Nondeterministic

**Exact statement**

> Minimize round trips and transfer volume while preserving correctness, freshness, privacy, security, and accessibility.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Minimizing transfer while preserving six cross-cutting qualities is a context-dependent architecture optimization without fixed thresholds.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-24A45B5D` — Nondeterministic

**Exact statement**

> Use compression suited to content type, client capability, latency, and compute trade-offs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Compression benchmarks are objective, but suitability across content, clients, latency, and compute trade-offs requires judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every compression decision for a registered content class links to client, size, latency, and compute measurements.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5FD8938A` — Nondeterministic

**Exact statement**

> Use durable caching directives and content-addressed assets where safe.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Cache directives and content-addressing are inspectable, but which assets are safe and what duration is durable require context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every asset approved as cacheable has an explicit cache directive and immutable assets use content-addressed identifiers.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-DDD0B8A8` — Nondeterministic

**Exact statement**

> Avoid invalidation strategies that repeatedly force full downloads when smaller or versioned updates suffice.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a smaller or versioned update suffices and an invalidation strategy is avoidable requires architecture and workload judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-8F05EC03` — Nondeterministic

**Exact statement**

> Use edge delivery, content distribution, peer distribution, or regional replication only when measured total impact and resilience justify them.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether measured total impact and resilience justify a distribution strategy is an accountable multi-objective architecture decision.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-2914A322` — Nondeterministic

**Exact statement**

> Keep data close to users and processing where it reduces transfer without violating residency, privacy, or resilience needs.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Placement benefits and residency, privacy, and resilience constraints require legal, security, and architecture judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-81C3B6CD` — Nondeterministic

**Exact statement**

> Reduce chatty protocols and choose efficient representations for high-volume paths.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Protocol benchmarks are objective, but chatty and efficient have no universal threshold and depend on path semantics.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every high-volume protocol decision links to measured round-trip and representation-size results.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-AB9AE435` — Nondeterministic

**Exact statement**

> Support resumable and partial transfer for large resources where interrupted transfer is plausible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Resumable and partial-transfer behavior is testable, but which resources are large and where interruption is plausible require context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every resource classified as large and interruption-prone passes resumable and partial-transfer tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D517D2B1` — Nondeterministic

**Exact statement**

> Prevent crawlers, bots, scrapers, abusive clients, and accidental loops from causing unbounded traffic.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because crawler and abuse strategies, identities, distributed clients, loops, amplification, protocols, and termination are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A distributed scraper evades per-client caps and creates unbounded aggregate traffic; registered tests pass.

## `USEQ-793660E3` — Nondeterministic

**Exact statement**

> Use rate limits, quotas, backpressure, and request coalescing to control waste and protect shared resources.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because ingress and queue completeness, policy interaction, overload behavior, fairness, and recovery oracles are incomplete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A hidden queue has no backpressure and overloads a shared database, while registered queues pass.

## `USEQ-2205156F` — Nondeterministic

**Exact statement**

> Monitor cache hit ratios, transfer per successful outcome, retransmission, aborted work, and regional paths.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because metric presence does not prove correct denominators, complete paths, retransmissions, aborted work, or useful regional attribution.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Failed outcomes are excluded from transfer denominator, making waste look low while every metric exists.

## `USEQ-707C1649` — Nondeterministic

**Exact statement**

> Include network equipment, intermediary services, and provider egress in impact boundaries where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Boundary fields are checkable, but deciding when network equipment, intermediaries, and egress are material requires judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every network impact source classified as material is included in the approved impact boundary.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-50405BB4` — Nondeterministic

**Exact statement**

> Select hosting and infrastructure using verifiable energy, emissions, water, resource, resilience, data-location, and supplier-governance evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Evidence fields are checkable, but selecting infrastructure from competing environmental, resilience, location, and governance factors is accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every hosting selection record links to verifiable energy, emissions, water, resource, resilience, data-location, and supplier-governance evidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5DAA5FA9` — Nondeterministic

**Exact statement**

> Right-size capacity using measured demand, required failure headroom, startup characteristics, and service objectives.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because right-sizing needs valid demand forecasts, failure scenarios, startup distributions, SLO trade-offs, and risk authority.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A plan uses average demand and no regional-failure case, yet all evidence fields pass.

## `USEQ-0C287229` — Nondeterministic

**Exact statement**

> Eliminate idle, abandoned, duplicate, oversized, and permanently overprovisioned resources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Inventory findings can be checked, but idle, oversized, duplicate, and permanent overprovisioning need declared thresholds and complete infrastructure scope.
- **Nondeterministic reason codes:** `undefined_threshold`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every resource identified by approved idle, abandonment, duplication, sizing, or overprovisioning policies is removed or has an approved exception.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-40D1DD20` — Nondeterministic

**Exact statement**

> Use autoscaling, scheduling, consolidation, suspension, and shutdown controls that preserve availability and recovery requirements.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because scaling and shutdown controls, dependencies, timing, load patterns, partitions, state, availability, and recovery cases are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Scale-to-zero loses an in-memory queue during a rare wake race, while approved tests pass.

## `USEQ-6D96904B` — Nondeterministic

**Exact statement**

> Schedule flexible workloads for lower-impact times or regions when privacy, latency, reliability, and contractual constraints permit.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Schedule and region configuration are objective, but flexibility, suitability, and permission under privacy, latency, reliability, or contract constraints need judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every workload approved as flexible has a schedule or region rule driven by the declared lower-impact signal.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-DF29852A` — Nondeterministic

**Exact statement**

> Distinguish grid average, market-based claims, marginal effects, and actual renewable matching in infrastructure decisions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because grid average, market claims, marginal effects, and renewable matching require technical and contractual interpretation beyond labels.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A certificate claim is labeled actual matching despite wrong time and region; all distinctions are recorded.

## `USEQ-8AB2AF58` — Nondeterministic

**Exact statement**

> Include backup power, cooling, storage, network, facility overhead, and embodied infrastructure in assessments where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Assessment inclusion is objective for classified inputs, but determining material facility and embodied infrastructure components requires context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every component classified as material among backup power, cooling, storage, network, facility overhead, and embodied infrastructure appears in the assessment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2738410D` — Nondeterministic

**Exact statement**

> Monitor energy, utilization, carbon intensity, water, waste, capacity, and service outcomes at a granularity that supports action.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Metric availability is queryable, but whether granularity supports action is an operational and sustainability judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Monitoring exposes energy, utilization, carbon intensity, water, waste, capacity, and service-outcome measurements with documented dimensions.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-F8EB7249` — Nondeterministic

**Exact statement**

> Avoid maintaining unused environments, replicas, clusters, virtual machines, containers, snapshots, volumes, addresses, and reserved capacity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Resources matching declared unused thresholds can be queried, but unused and abandoned across all infrastructure need thresholds and complete provider scope.
- **Nondeterministic reason codes:** `undefined_threshold`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every resource reported unused by approved environment policies is removed or has an approved exception.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-47DBC483` — Nondeterministic

**Exact statement**

> Test shutdown, scale-to-zero, wake-up, failover, and recovery behavior before relying on them for reduction.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because shutdown, wake, failover, and recovery tests do not cover every stateful dependency, duration, load, order, and data-integrity oracle.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A long-idle wake uses an expired certificate outside the current test matrix.

## `USEQ-6DF5A476` — Nondeterministic

**Exact statement**

> Ensure resilience architecture survives required failures without keeping unjustified permanent duplication.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Required-failure test results are objective, but whether permanent duplication is unjustified requires accountable resilience trade-off judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The resilience design passes every failure scenario in the approved required-failure set.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C1FC62BA` — Nondeterministic

**Exact statement**

> Include control planes, observability, build systems, development environments, and disaster-recovery infrastructure in the footprint.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because footprint completeness across control planes, observability, builds, development, DR, suppliers, and shared infrastructure is not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A supplier build farm is omitted, while each named internal category is present.

## `USEQ-EE0EEBE1` — Nondeterministic

**Exact statement**

> Negotiate access to provider sustainability data and document limitations when data is unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because material provider scope, negotiated access rights, data quality, availability, limitations, and contractual truth are not proven.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A major provider is labeled immaterial and an empty limitation is recorded; fields pass.

## `USEQ-CE56DAD4` — Nondeterministic

**Exact statement**

> Collect and retain only data justified by a current user, legal, safety, security, operational, or research purpose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Purpose, retention, and owner fields are checkable, but whether a purpose legally or operationally justifies collection requires specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every collected or retained data class has a current owner, purpose category, and approved retention record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B2FD1EBB` — Nondeterministic

**Exact statement**

> Classify hot, warm, cold, archival, backup, and disposable data and use storage suited to access and recovery needs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Storage class labels are objective, but whether the selected tier suits access and recovery needs requires architecture judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered data set is labeled hot, warm, cold, archival, backup, or disposable and maps to a storage tier.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5AA75D77` — Nondeterministic

**Exact statement**

> Define deletion, compaction, deduplication, compression, tiering, and archive policies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because policy sections do not prove complete data scope, safe behavior, lawful retention, recovery, or actual enforcement.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: Deletion excludes derived copies and archive policy conflicts with retention, while all six sections exist.

## `USEQ-8EF05145` — Nondeterministic

**Exact statement**

> Remove orphaned blobs, snapshots, logs, indexes, replicas, derivatives, test data, and abandoned exports.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Known orphan findings can be enforced, but proving discovery of all orphaned objects across every store and derivative is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every orphan reported by approved blob, snapshot, log, index, replica, derivative, test-data, and export detectors is removed or excepted.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-6CC15740` — Nondeterministic

**Exact statement**

> Use retention periods based on evidenced need rather than indefinite default retention.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Finite retention and linked evidence are objective, but sufficiency of the need and legal retention basis require accountable specialist judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every retained data class has a finite retention period and a linked current need record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7BC8164C` — Nondeterministic

**Exact statement**

> Distinguish backups from archives and avoid using repeated full backups as an unmanaged preservation strategy.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because preservation-job completeness and truthful backup or archive classification, owner, retention, and recovery objectives are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unmanaged repeated full backup is omitted, while registered jobs have classifications and fields.

## `USEQ-691E5191` — Nondeterministic

**Exact statement**

> Optimize backup frequency, incrementality, retention, geographic copies, and testing against recovery objectives and impact.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Balancing backup frequency, incrementality, retention, copies, testing, recovery objectives, and environmental impact is an accountable design decision.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-72E8E360` — Nondeterministic

**Exact statement**

> Preserve legal holds and evidentiary requirements without retaining unrelated data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The scope of a legal hold and whether retained data is related require legal authority and case-specific interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-F8EFD0C1` — Nondeterministic

**Exact statement**

> Design deletion to propagate across caches, indexes, replicas, analytical systems, derived assets, and suppliers.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Propagation tests are objective for known stores, but complete discovery of every cache, derivative, supplier, and future copy is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every store in the approved data-lineage graph passes the deletion propagation test for its registered data classes.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D6515E61` — Nondeterministic

**Exact statement**

> Monitor storage growth by owner, purpose, class, age, duplication, access frequency, and deletion eligibility.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because storage ground truth, owner, purpose, class, age, duplication, access, and deletion eligibility require complete inventory and semantics.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Uncatalogued backups grow rapidly, while monitoring reports every named dimension for registered storage.

## `USEQ-BC43D21A` — Nondeterministic

**Exact statement**

> Prevent compression, deduplication, or tiering from weakening encryption, access control, recovery, or integrity.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because compression, deduplication, tiering variants, algorithms, data classes, access paths, recovery failures, and integrity oracles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Tiering changes key handling for an omitted archive class, while registered tests preserve all declared invariants.

## `USEQ-5863DC60` — Nondeterministic

**Exact statement**

> Include the energy and material cost of long-term preservation, migration, and media refresh in lifecycle plans.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because energy and material cost estimates require lifecycle methods, boundaries, sources, uncertainty, and future assumptions beyond fields.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A plan records zero media-refresh cost without analysis; all three costs are present.

## `USEQ-267D27DD` — Nondeterministic

**Exact statement**

> Justify the use of an AI or high-compute method against simpler alternatives that can achieve the required outcome.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether simpler alternatives can achieve the required outcome and justify AI or high compute requires accountable architecture and product judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-FD5481AF` — Nondeterministic

**Exact statement**

> Define lifecycle boundaries for data preparation, training, fine-tuning, evaluation, inference, retrieval, monitoring, experimentation, and hardware.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because AI workload completeness and valid lifecycle boundaries across data, training, evaluation, inference, retrieval, monitoring, experiments, and hardware are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A supplier inference stage is omitted, while registered workloads list nine boundaries.

## `USEQ-329FEE22` — Nondeterministic

**Exact statement**

> Measure energy, emissions, water, hardware use, utilization, and functional outcomes for material AI workloads.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Measurements are objective, but which AI workloads and environmental effects are material requires sustainability judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every AI workload classified as material records energy, emissions, water, hardware use, utilization, and functional outcomes.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4F4E1909` — Nondeterministic

**Exact statement**

> Report model size and compute only alongside task quality, usage, lifetime, hardware, region, and uncertainty needed to interpret impact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because model size and compute context fields do not make task quality, lifetime, hardware, region, usage, and uncertainty accurate or sufficient.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A report uses a cherry-picked quality task and zero uncertainty; every field exists.

## `USEQ-5710DB96` — Nondeterministic

**Exact statement**

> Use the smallest model, precision, context, retrieval scope, generation length, and frequency that reliably meet requirements.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The smallest configuration that reliably meets requirements depends on comparative quality, safety, cost, and workload judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-2809A00E` — Nondeterministic

**Exact statement**

> Reuse appropriate existing models and artifacts when lifecycle impact, security, privacy, licensing, and quality are favorable.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Appropriateness and favorable lifecycle, security, privacy, license, and quality trade-offs require accountable specialist review.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-FB9693DF` — Nondeterministic

**Exact statement**

> Control repeated experiments, hyperparameter searches, evaluation duplication, speculative agents, and unattended loops.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because experiment and agent workflow inventories, distributed loops, spawned tasks, spend, work, stop paths, and failure combinations are not complete.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An agent spawns an unregistered subworkflow that loops after the parent stops; registered limits pass.

## `USEQ-BA68CA4F` — Nondeterministic

**Exact statement**

> Cache or reuse safe deterministic results where it reduces work without creating stale, private, or incorrect outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Cache behavior can be tested, but which results are safe and deterministic enough to reuse requires contextual security and correctness judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every result approved for reuse passes freshness, privacy-isolation, and correctness tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-AE78BFC0` — Nondeterministic

**Exact statement**

> Route requests by complexity only when routing quality, fairness, privacy, and failure behavior are validated.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Routing test results are objective, but adequate quality, fairness, privacy, and failure protocols and thresholds are not specified.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every complexity router has current executed tests for quality, fairness, privacy, and failure behavior.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-23A25A60` — Nondeterministic

**Exact statement**

> Schedule flexible training and batch inference according to capacity and environmental signals where suitable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Configured scheduling is queryable, but workload flexibility, suitability, and signal trade-offs require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every AI workload approved as flexible has a capacity or environmental-signal scheduling policy.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-6169B64D` — Nondeterministic

**Exact statement**

> Monitor resource and environmental regressions across model, prompt, data, provider, and traffic changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because model, prompt, data, provider, and traffic change completeness plus causal environmental regression detection are not established.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A provider hardware change is unreported and resource growth stays below a broad aggregate threshold.

## `USEQ-A024A74B` — Nondeterministic

**Exact statement**

> Include embodied accelerator impact and hardware replacement cycles in major procurement and architecture decisions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Decision-record content is checkable, but which procurements and architecture changes are major requires accountable materiality judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every procurement or architecture decision classified as major records embodied accelerator impact and hardware replacement cycles.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-12E1E291` — Nondeterministic

**Exact statement**

> Do not claim an AI system is sustainable from operational efficiency alone when training, hardware, rebound, or enabled effects are material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a sustainability claim improperly omits material training, hardware, rebound, or enabled effects needs environmental and claims review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-0B2665A3` — Nondeterministic

**Exact statement**

> Provide kill switches and spending or resource limits for autonomous and agentic workloads.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because autonomous workload completeness, kill paths, delegated tools, offline states, spending, resource accounting, races, and recovery are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A spawned worker survives the parent kill switch and spends through another account; registered tests pass.

## `USEQ-62B94A8E` — Nondeterministic

**Exact statement**

> Maintain an inventory of material hardware and virtualized assets with owner, age, location, utilization, support state, and retirement path.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because material physical and virtual asset completeness, owner truth, age, location, utilization, support, and retirement feasibility are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unregistered accelerator cluster is omitted and another asset has a departed owner; rows pass.

## `USEQ-348B40AE` — Nondeterministic

**Exact statement**

> Extend useful hardware life when security, reliability, compatibility, repairability, and efficiency remain acceptable.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Acceptable security, reliability, compatibility, repairability, and efficiency for extending hardware life require accountable risk trade-offs.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-6C407ACB` — Nondeterministic

**Exact statement**

> Avoid software requirements and update policies that force unjustified hardware replacement.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a software requirement or update policy forces unjustified hardware replacement requires product, security, and user-impact judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-3FE758C5` — Nondeterministic

**Exact statement**

> Include embodied impact, repairability, modularity, spare parts, support duration, and take-back options in procurement.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because procurement fields do not prove accurate embodied impact, labor and materials facts, repairability, spare availability, support, or take-back enforceability.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A supplier claim is unverified and spare parts are unavailable despite all fields being populated.

## `USEQ-FE206CBE` — Nondeterministic

**Exact statement**

> Prefer repair, redeployment, refurbishment, resale, donation, parts recovery, and certified recycling over disposal where safe and lawful.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Choosing among reuse and disposal options while determining safety and lawfulness requires accountable legal and security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-D6FAF009` — Nondeterministic

**Exact statement**

> Erase data securely and verify custody during reuse, return, recycling, and disposal.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because asset completeness, secure erase coverage, hidden storage, custody authenticity, recipient identity, and final disposition are not fully proven.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A device contains an unlisted cache chip and custody is backfilled, while erase and recipient records pass.

## `USEQ-04DF1172` — Nondeterministic

**Exact statement**

> Track electronic waste by type, destination, treatment, recovery, and evidence of responsible handling.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because e-waste event completeness, destination truth, treatment legality, recovery outcome, and responsible-handling evidence require external authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A shipment is diverted after its record says responsible recycler; all five fields pass.

## `USEQ-8A5BFC8A` — Nondeterministic

**Exact statement**

> Prevent asset hoarding, forgotten stock, premature replacement, and perpetual retention of unsupported equipment.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Inventory and support-state findings are objective, but hoarding, forgotten stock, and premature replacement require declared age and use thresholds.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every asset matching approved unused-stock, replacement-age, or unsupported-state policies has a disposition action or approved exception.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F19BB58C` — Nondeterministic

**Exact statement**

> Coordinate capacity planning and hardware refresh so efficiency gains are not erased by excess inventory or reduced lifetime.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether efficiency gains are erased by inventory or shorter lifetime requires lifecycle modeling and accountable capacity and refresh decisions.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-CBABBF8C` — Nondeterministic

**Exact statement**

> Require suppliers to disclose relevant lifecycle, materials, labor, repair, packaging, transport, and end-of-life information.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because material supplier completeness and truthful lifecycle, labor, repair, packaging, transport, and end-of-life disclosures are not proven.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A supplier omits subcontractor labor conditions but fills every disclosure field.

## `USEQ-016BEEB7` — Nondeterministic

**Exact statement**

> Include batteries, displays, peripherals, networking equipment, storage media, and user devices where the product materially drives their use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Inventory inclusion is checkable, but whether the product materially drives each peripheral or device class requires lifecycle judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every device class classified as materially product-driven is included in the approved asset and impact inventories.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-217653AB` — Nondeterministic

**Exact statement**

> Use asset identification and utilization records to support sustainability decisions and audits.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because material asset completeness, stable identity, current utilization accuracy, and valid use in decisions and audits are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A shadow server is omitted and another utilization record is stale; registered assets pass.

## `USEQ-8CDDB1C3` — Nondeterministic

**Exact statement**

> Assess water use and water-stress context for material computing, cooling, energy, and manufacturing dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Water-use and stress records are checkable, but material computing and manufacturing dependencies require contextual sustainability judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every dependency classified as material has water-use data and a location-specific water-stress assessment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-10A887ED` — Nondeterministic

**Exact statement**

> Avoid optimizing energy or emissions in ways that create disproportionate water, material, pollution, biodiversity, or community harm.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Disproportionate environmental or community harm requires specialist lifecycle, local-impact, and stakeholder judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-AFA6FD10` — Nondeterministic

**Exact statement**

> Identify hazardous materials, chemicals, refrigerants, batteries, packaging, and waste streams associated with material infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Hazard and waste inventories are objective for known infrastructure, but material infrastructure scope requires specialist materiality judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every infrastructure component classified as material lists associated hazardous materials, chemicals, refrigerants, batteries, packaging, and waste streams.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-51A9A772` — Nondeterministic

**Exact statement**

> Assess location-specific climate, grid, water, ecosystem, disaster, and community risks rather than relying only on global averages.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because location-specific climate, grid, water, ecosystem, disaster, and community risks require authoritative data, scenarios, and affected-party judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A facility uses national averages and ignores a local floodplain, while all six fields exist.

## `USEQ-89709CE3` — Nondeterministic

**Exact statement**

> Include upstream mining, manufacturing, transport, construction, and downstream disposal when they materially affect decisions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Upstream and downstream fields are objective, but whether they materially affect a decision requires lifecycle expert judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every effect classified as material from mining, manufacturing, transport, construction, or disposal is included in the decision record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7808AE7C` — Nondeterministic

**Exact statement**

> Use life-cycle assessment principles for major comparisons and document exclusions and data quality.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Exclusions and data-quality fields are objective, but major comparisons and adequate life-cycle-assessment principles require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every comparison classified as major documents lifecycle boundaries, exclusions, and data quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0D63B3D7` — Nondeterministic

**Exact statement**

> Prefer suppliers with transparent environmental management, credible targets, responsible sourcing, and independently supportable data.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Supplier transparency, credibility, responsible sourcing, and evidentiary support require accountable procurement and sustainability judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-7872C26C` — Nondeterministic

**Exact statement**

> Detect and prevent illegal, unsafe, or opaque waste export and disposal pathways.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Illegality, safety, opacity, and complete prevention of waste pathways require legal authority, supply-chain expertise, and unbounded external evidence.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

## `USEQ-6DA0EC95` — Nondeterministic

**Exact statement**

> Consider biodiversity and land-use effects in large facilities, energy sourcing, and hardware supply chains where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Assessment fields are objective, but material biodiversity and land-use effects need lifecycle and location-specific judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every large-facility, energy, or hardware decision with a classified material biodiversity or land-use effect records that effect.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F7780EDB` — Nondeterministic

**Exact statement**

> Assess adaptation needs for heat, drought, flood, wildfire, grid instability, and other climate-related conditions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because material infrastructure and climate condition completeness plus feasible adaptation actions require specialist forecasting and risk judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: Wildfire smoke is omitted and the flood action is move later; records pass.

## `USEQ-2B1C8F57` — Nondeterministic

**Exact statement**

> Include environmental justice and unequal community impacts in site and supplier decisions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Environmental justice and unequal community impact require affected-community input, ethical judgment, and qualified local assessment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-DBC553F9` — Nondeterministic

**Exact statement**

> Document when reliable data is unavailable and avoid making precise claims from weak proxies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Missing-data disclosures and proxy labels are objective, but reliability, weakness, and excessive precision require specialist interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every unavailable data source is documented and every proxy-derived result is labeled with its uncertainty and precision.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5804B79B` — Nondeterministic

**Exact statement**

> Include measurable sustainability requirements in supplier selection and renewal for material products and services.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Contract requirements and numeric fields are checkable, but supplier materiality and suitable measures require procurement judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every supplier classified as material has measurable sustainability requirements in selection and renewal records.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-106A573E` — Nondeterministic

**Exact statement**

> Require suppliers to define measurement boundaries, methods, data quality, assurance, and update cadence for reported claims.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because supplier measurement boundaries, methods, quality, assurance, and cadence must be technically valid and contractually enforceable.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An agreement accepts unaudited estimates every decade, yet all five terms are defined.

## `USEQ-92042528` — Nondeterministic

**Exact statement**

> Distinguish supplier-wide averages from the impact attributable to the purchased service.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because supplier-wide and purchased-service attribution requires allocation methodology, complete boundaries, and causal interpretation beyond separate fields.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: The report labels a company average as service-specific using an unsupported allocation, while both values exist.

## `USEQ-07CD6281` — Nondeterministic

**Exact statement**

> Obtain data on energy, emissions, water, waste, hardware lifecycle, regions, and subcontractors where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Data-field presence is objective, but which supplier impacts and subcontractors are material requires judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every supplier impact category classified as material has data for energy, emissions, water, waste, hardware lifecycle, regions, and subcontractors.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-BEE4F085` — Nondeterministic

**Exact statement**

> Require disclosure of significant method changes and restatements.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because significance, complete method changes, restatement duties, notice timing, and contract enforcement require judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: A major allocation change is labeled insignificant and not disclosed; the agreement field passes.

## `USEQ-CD3E24CA` — Nondeterministic

**Exact statement**

> Assess whether contractual renewable-energy claims correspond to the locations and time periods of actual consumption.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Matching renewable claims to actual consumption across place and time requires specialist energy-accounting evidence and judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-1129D00B` — Nondeterministic

**Exact statement**

> Include rights to audit, request evidence, remediate, terminate, export data, and migrate when material commitments are not met.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Contract rights are objective, but material commitments and adequacy of remedies require legal and procurement judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every contract with commitments classified as material includes audit, evidence request, remediation, termination, data export, and migration rights.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A80ADE7A` — Nondeterministic

**Exact statement**

> Avoid procurement criteria that privilege polished reporting over actual verified reduction and responsible practice.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Distinguishing polished reporting from actual verified reduction and responsible practice requires accountable procurement evaluation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-A984E0E3` — Nondeterministic

**Exact statement**

> Assess concentration, lock-in, data portability, and replacement impact before outsourcing critical capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Assessment records are checkable, but critical capability scope and acceptable concentration or lock-in risk require accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every capability classified as critical has concentration, lock-in, data-portability, and replacement-impact assessments before outsourcing.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-6C3BAA57` — Nondeterministic

**Exact statement**

> Flow applicable requirements to subcontractors and supply-chain participants.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Flow-down clauses are checkable, but determining which requirements and supply-chain participants are applicable requires legal and procurement judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every requirement marked applicable to a subcontractor is present in that subcontractor's governing agreement.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C0E91E6F` — Nondeterministic

**Exact statement**

> Use supplier scorecards carefully and prevent unverified composite scores from hiding severe weaknesses.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a composite score is verified or hides a severe weakness requires semantic evidence review and risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-1B6F08D8` — Nondeterministic

**Exact statement**

> Revoke access, delete data, recover assets, and close environmental obligations when suppliers are retired.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because supplier retirement completeness, all access and assets, every data copy, deletion proof, and environmental obligation closure are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A backup and subcontractor credential remain outside inventory while registered closure evidence passes.

## `USEQ-FD386600` — Nondeterministic

**Exact statement**

> Design stable interfaces, data formats, identifiers, and migration paths that reduce premature replacement and repeated redevelopment.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Interfaces, formats, identifiers, and migration paths are inspectable, but stability and reduced premature replacement require longitudinal judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every supported interface, data format, and identifier has a version policy and documented migration path.
   - **Checker family:** `structured_document`
   - **Evidence authority:** `repository`

## `USEQ-E5E831CE` — Nondeterministic

**Exact statement**

> Support reasonable client, operating-system, protocol, and assistive-technology lifetimes based on user context and security risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reasonable support lifetime depends on user context, technology availability, and changing security risk and needs accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-788DE467` — Nondeterministic

**Exact statement**

> Document compatibility windows and deprecation decisions with user impact, migration cost, and environmental consequences.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because user impact, migration cost, environmental consequence completeness, compatibility, and deprecation reasoning require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Counterexample: A deprecated API omits an offline consumer and migration energy cost; all fields exist.

## `USEQ-A676EC10` — Nondeterministic

**Exact statement**

> Use graceful degradation, offline capability, local recovery, and resumable work where they improve resilience and reduce waste.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Capability tests are objective, but whether each capability improves resilience and reduces waste requires contextual product judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every approved degradation, offline, local-recovery, or resumable-work capability has a passing recovery test.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D82A953C` — Nondeterministic

**Exact statement**

> Avoid brittle dependencies on one provider, region, proprietary format, or short-lived service without an exit path.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Known single-provider and proprietary dependencies can be checked for exit paths, but brittleness and short-lived status require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every dependency classified as single-provider, single-region, proprietary-format, or short-lived has a documented and tested exit path.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DD4E26D9` — Nondeterministic

**Exact statement**

> Preserve data portability and interoperable export so users can move without unnecessary recreation or loss.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because supported data classes, schemas, metadata, permissions, histories, scale, integrity, interoperability, and loss oracles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A small export round-trip passes but drops attachments and ACLs in production data.

## `USEQ-BD44967E` — Nondeterministic

**Exact statement**

> Maintain disaster recovery and business continuity without uncontrolled permanent resource duplication.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recovery capacity and duplication are queryable, but whether permanent duplication is uncontrolled or justified requires accountable resilience judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every permanent disaster-recovery resource maps to an approved recovery objective and capacity requirement.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E6F6B0C4` — Nondeterministic

**Exact statement**

> Design for climate-related infrastructure disruption, degraded connectivity, supply shortages, and power constraints where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Resilience plans are checkable for classified hazards, but relevance and adequate design for climate and supply conditions require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every disruption class marked relevant has a documented mitigation and current exercise result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2D6A7EF2` — Nondeterministic

**Exact statement**

> Keep recovery procedures, artifacts, keys, documentation, and skills viable for the intended service lifetime.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Current recovery artifacts and exercises are testable, but viability through the intended future lifetime depends on changing people, technology, and risk.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Recovery procedures, artifacts, keys, and documentation have current validation results and assigned skill coverage through their declared review dates.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-18554C68` — Nondeterministic

**Exact statement**

> Retire unsupported, insecure, or inefficient technology through planned migration rather than emergency replacement.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Migration plans and execution are checkable, but unsupported, insecure, inefficient, and emergency necessity require contextual lifecycle judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every technology formally marked for retirement has an approved planned migration with owner and target date.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-61E768BD` — Nondeterministic

**Exact statement**

> Measure product lifetime, upgrade frequency, migration work, and redevelopment as sustainability indicators.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because product lifetime, upgrade, migration, and redevelopment events, boundaries, causality, and sustainability meaning are not complete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Untracked customer migrations are omitted, while registered indicators report all four measures.

## `USEQ-CE286A5D` — Nondeterministic

**Exact statement**

> Ensure optimizations do not reduce diagnosability or make failures more expensive to detect and repair.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Before-and-after diagnostic tests are objective, but reduced diagnosability and unacceptable repair expense require declared protocols and thresholds.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every material optimization has comparative diagnostic-coverage and failure-detection or repair-cost results.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2C5DF06D` — Nondeterministic

**Exact statement**

> Do not weaken accessibility, privacy, security, safety, reliability, or user rights to meet an environmental target.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Determining whether an environmental change weakens accessibility, rights, safety, security, privacy, or reliability needs multiple qualified reviewers.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-FE5C9BBC` — Nondeterministic

**Exact statement**

> Collect sustainability telemetry at the least identifying granularity needed for the decision.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The least identifying telemetry granularity needed for a decision requires privacy, statistical, and product-purpose judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-B32833BD` — Nondeterministic

**Exact statement**

> Aggregate, minimize, protect, retain, and delete energy or behavior telemetry according to documented purpose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Purpose, retention, and deletion controls are checkable, but adequate aggregation, minimization, and protection require privacy and security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every sustainability telemetry class has a documented purpose, retention period, deletion path, protection policy, and aggregation setting.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C11D4863` — Nondeterministic

**Exact statement**

> Prevent sustainability scores, device characteristics, resource patterns, or power signals from becoming tracking or fingerprinting mechanisms.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Proving signals cannot become tracking or fingerprinting mechanisms requires privacy threat modeling and unbounded adversarial analysis.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

## `USEQ-13DA6246` — Nondeterministic

**Exact statement**

> Do not use resource efficiency as justification for omitting necessary authentication, encryption, backups, audit records, or abuse controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Presence of declared controls is objective, but which controls are necessary in every context requires security, legal, and resilience judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every control classified as necessary for authentication, encryption, backup, audit, or abuse prevention remains enabled after an environmental optimization.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-9CBBD1BC` — Nondeterministic

**Exact statement**

> Optimize security controls through architecture and proportional configuration rather than disabling them.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether architecture and configuration are proportional while security controls remain effective requires accountable security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-ABB8A79B` — Nondeterministic

**Exact statement**

> Include malicious traffic, fraud, scraping, spam, cryptomining, denial of service, and compromised automation in waste-reduction controls.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because waste-control coverage and effectiveness against evolving malicious traffic, fraud, scraping, spam, mining, DoS, and compromised automation are not proven.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: Distributed cryptomining through a new client path bypasses controls while every named category is listed.

## `USEQ-9B66981F` — Nondeterministic

**Exact statement**

> Ensure low-resource or offline modes preserve authorization, integrity, privacy, and safe recovery.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because low-resource and offline modes, identities, sync conflicts, local attacks, stale policies, privacy, integrity, and recovery cases are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An offline tenant switch exposes cached data outside approved tests, while all mode fixtures pass.

## `USEQ-19B4881C` — Nondeterministic

**Exact statement**

> Provide accessible alternatives when visual, audio, animation, or interaction reductions affect users differently.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Alternative availability is testable, but whether a reduction affects users differently and the alternative is accessible requires specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every reduction classified as affecting accessibility has a linked alternative and executed accessibility test result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-F55E8913` — Nondeterministic

**Exact statement**

> Review unequal effects on users with older devices, disabilities, limited connectivity, low income, or dependence on shared devices.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Unequal effects across disability, device, income, connectivity, and shared-device contexts require representative research and stakeholder judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `undefined_protocol`

## `USEQ-0C6BFCF8` — Nondeterministic

**Exact statement**

> Document and approve unavoidable trade-offs with affected stakeholders and qualified reviewers.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Approvals and stakeholder records are objective, but unavoidable, affected, and qualified require accountable specialist judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every trade-off classified as unavoidable has a documented decision and approvals from recorded affected stakeholders and reviewers.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3A2A4DEE` — Nondeterministic

**Exact statement**

> Monitor whether environmental controls create new exclusion, surveillance, manipulation, or safety risks.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Risk-monitor configuration is objective, but whether controls create exclusion, surveillance, manipulation, or safety risk needs specialist interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Monitoring records indicators for exclusion, surveillance, manipulation, and safety risks linked to each environmental control.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-346178E8` — Nondeterministic

**Exact statement**

> Define every public or contractual sustainability claim in testable terms.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because public and contractual claim scope, metric validity, threshold, comparison, testability, and verification authority require specialist meaning.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A vague net-zero claim defines a weak proxy and self-verification method; all fields pass.

## `USEQ-17FA871A` — Nondeterministic

**Exact statement**

> Identify the assessed product version, geography, time period, functional unit, lifecycle boundary, method, data sources, and exclusions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because product, geography, period, unit, lifecycle, method, source, and exclusion fields do not prove complete and correct assessment boundaries.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A material supplier stage is excluded without justification, while every assessment field exists.

## `USEQ-CB26CF79` — Nondeterministic

**Exact statement**

> Disclose uncertainty, estimates, supplier dependence, material limitations, and known negative impacts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Disclosure fields are checkable, but material limitations and completeness of known negative impacts require specialist judgment and unbounded discovery.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every claim records uncertainty, estimates, supplier dependence, identified limitations, and registered negative impacts.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-FEF4E43F` — Nondeterministic

**Exact statement**

> Do not describe partial conformance, a single efficiency improvement, renewable-energy procurement, or offsets as total sustainability.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether claim wording turns partial conformance or one instrument into total sustainability requires semantic and legal claims review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-808F4B6E` — Nondeterministic

**Exact statement**

> Do not compare products unless the boundary, functional unit, quality level, lifetime, and methods are sufficiently aligned.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Comparison metadata are objective, but sufficiently aligned boundaries, units, quality, lifetime, and methods require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every product comparison records boundary, functional unit, quality level, lifetime, and method for each compared product.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7C8C50E7` — Nondeterministic

**Exact statement**

> Keep evidence available for the life of the claim and any required retention period.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because claim inventory, applicable retention dates, evidence completeness, readability, integrity, and legal sufficiency are not established.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An applicable legal period is omitted and evidence is retained only as a broken link; date checks pass.

## `USEQ-3CB866B2` — Nondeterministic

**Exact statement**

> Have qualified independent reviewers verify high-impact claims and regulated disclosures.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Review evidence is objective, but high-impact or regulated scope and reviewer qualification require legal and professional judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every claim marked high-impact or regulated has approval from a recorded independent reviewer.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-34536507` — Nondeterministic

**Exact statement**

> Correct or withdraw claims promptly when data, methods, products, suppliers, or assumptions change materially.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Change and correction timestamps are objective, but materiality and promptly have no universal decision rule or deadline.
- **Nondeterministic reason codes:** `undefined_threshold`, `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every claim change classified as material has a linked correction or withdrawal record and completion timestamp.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4B9F5962` — Nondeterministic

**Exact statement**

> Publish progress and setbacks using consistent historical baselines or clearly restate prior values.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because same baseline version or restated values do not prove comparable methods, populations, valid restatement, or honest setbacks.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A setback is hidden by redefining the population while retaining the baseline label; fields pass.

## `USEQ-278F1D86` — Nondeterministic

**Exact statement**

> Separate measured reductions from projections, commitments, avoided emissions, enabled impacts, and compensatory instruments.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because distinct report labels do not prevent misleading aggregation, substitution, narrative equivalence, or false classification.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: Projected avoided emissions are labeled measured reduction in a summary while detail types remain separate.

## `USEQ-9771DA8A` — Nondeterministic

**Exact statement**

> Prevent dashboards from using incomplete precision, favorable denominators, omitted growth, or selective time windows.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Incomplete precision, favorable denominator selection, omitted growth, and selective windows are semantic presentation choices needing specialist review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-68334A49` — Deterministic

**Exact statement**

> Provide a contact and correction process for sustainability concerns and data errors.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The public sustainability contact is exercised with a bound data-error submission that enters the documented correction workflow, exactly deciding channel and process reachability.

### Exact deterministic check

1. Provide a contact and correction process for sustainability concerns and data errors.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

I tried an unpublished channel, rejected report, dead form, wrong queue, and report that never enters correction. Each fails the authenticated exercise; whether the eventual correction is substantively right is separate.

## `USEQ-1B038E56` — Nondeterministic

**Exact statement**

> Monitor resource and environmental indicators alongside user outcomes, service objectives, cost, and quality.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because indicator and outcome completeness, valid side-by-side populations, service and quality definitions, thresholds, and causal interpretation are not established.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Environmental metrics cover one region while user outcomes cover another; dashboards still present them together.

## `USEQ-E58D1198` — Nondeterministic

**Exact statement**

> Annotate deployments, model changes, campaigns, supplier changes, and traffic shifts on sustainability trends.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because deployment, model, campaign, supplier, and traffic event completeness and accurate annotation timing and meaning are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: An unregistered campaign causes a trend shift without annotation, while declared events are annotated.

## `USEQ-D899C2D4` — Nondeterministic

**Exact statement**

> Investigate unexpected increases in compute, storage, transfer, energy, water, waste, or hardware demand.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Investigation records are objective, but unexpected increases require project-specific baseline and alert thresholds.
- **Nondeterministic reason codes:** `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every increase that breaches an approved compute, storage, transfer, energy, water, waste, or hardware-demand threshold has an investigation record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-8642C1BC` — Nondeterministic

**Exact statement**

> Include sustainability in incident and problem reviews when waste, runaway activity, capacity, or supplier behavior contributed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Incident review content is objective, but determining whether waste, capacity, runaway activity, or suppliers contributed requires causal judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every incident marked as having a sustainability contributor includes sustainability in its incident and problem review.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-374A20F0` — Nondeterministic

**Exact statement**

> Create alerts or automated limits for uncontrolled jobs, agent loops, queue growth, data growth, traffic, builds, experiments, and scaling.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because uncontrolled jobs and loops, queues, data, traffic, builds, experiments, scaling, thresholds, delivery, and automatic limits are not fully bounded.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A spawned agent loop uses an unmonitored queue and exceeds resources while all registered alert categories exist.

## `USEQ-D2B9385B` — Nondeterministic

**Exact statement**

> Test that shutdown, throttling, degradation, and kill switches work without causing unsafe or irreversible outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Control exercises are objective, but unsafe or irreversible outcome scope and adequacy require safety and operations judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Shutdown, throttling, degradation, and kill-switch controls have current executed outcome and recovery tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-3ADC9F49` — Nondeterministic

**Exact statement**

> Convert recurring waste into architectural, product, operational, or supplier corrective action rather than one-time cleanup.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Corrective-action records are checkable, but recurring waste and avoidability require contextual operations judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every waste pattern classified as recurring has a linked architectural, product, operational, or supplier corrective action.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-1C89DDCE` — Nondeterministic

**Exact statement**

> Reassess after major changes to usage, regions, algorithms, models, devices, suppliers, regulations, or measurement methods.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reassessment records are objective, but major changes and material effect thresholds require accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change marked major in usage, region, algorithm, model, device, supplier, regulation, or method has a subsequent reassessment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7C1E20A5` — Nondeterministic

**Exact statement**

> Retain release evidence showing that applicable environmental budgets and no-go gates passed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Release evidence and passed gates are objective for an approved applicability set, but complete environmental applicability requires judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every release retains evidence that all environmental budgets and no-go gates marked applicable passed.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-624C88CF` — Nondeterministic

**Exact statement**

> Block release when a material impact is unknown because required measurement or ownership is absent.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** A release gate can check ownership and measurement, but materiality of an unknown impact requires accountable sustainability judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. A release is blocked whenever an impact classified as material lacks either required measurement evidence or an assigned owner.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `repository`

## `USEQ-5139B668` — Nondeterministic

**Exact statement**

> Block release when the product exceeds an approved hard budget without an authorized, monitored, expiring exception.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because approved hard budgets, complete release paths, measurement validity, exception authority, monitoring, and expiry cannot be reduced to gate states.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An emergency release bypasses the gate and another exception is self-approved with disabled monitoring.

## `USEQ-3820272D` — Nondeterministic

**Exact statement**

> Block release when a public environmental claim is unsupported, misleading, or materially inconsistent with actual operation.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Unsupported, misleading, and materially inconsistent environmental claims require specialist evidence and legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-BDA8D2C8` — Nondeterministic

**Exact statement**

> Block release when optimization creates an unresolved accessibility, privacy, security, safety, integrity, or reliability failure.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Known unresolved failures can block release deterministically, but complete discovery and severity across six quality areas need specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The release gate blocks when any registered unresolved accessibility, privacy, security, safety, integrity, or reliability failure is marked blocking.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `repository`

## `USEQ-8E27F1AD` — Nondeterministic

**Exact statement**

> Define the service, capability, users, organizations, suppliers, locations, data, and environments included in the transition.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because transition scope fields do not prove complete services, users, suppliers, locations, data, environments, and hidden dependencies.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A supplier-operated region is omitted while all eight fields are populated.

## `USEQ-35A71BDE` — Nondeterministic

**Exact statement**

> Assign an accountable transition owner with authority across product, delivery, operations, support, security, privacy, data, and suppliers.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because one named transition owner does not prove actual cross-domain authority, access, competence, availability, or accepted accountability.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: The owner lacks supplier and privacy authority but the authority field lists all domains.

## `USEQ-FDCEFCB5` — Nondeterministic

**Exact statement**

> Identify the people authorized to accept operational responsibility and residual risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because current authorization to accept operational responsibility and residual risk requires organizational and legal authority.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A project manager is listed as risk approver without mandate; record fields pass.

## `USEQ-FB298C6E` — Nondeterministic

**Exact statement**

> Establish transition objectives, success measures, entry criteria, exit criteria, stop conditions, rollback criteria, and observation period.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because objectives, success measures, entry and exit, stop and rollback, and observation period need risk, user, and service judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: Criteria mark success after one minute of no errors and omit data reconciliation; all fields exist.

## `USEQ-6FA91009` — Nondeterministic

**Exact statement**

> Maintain an integrated transition plan covering technology, data, people, process, contracts, communications, training, support, and retirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because integrated plan sections do not prove complete, consistent, feasible technology, data, people, contracts, training, support, and retirement.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Counterexample: A contract notice dependency is omitted and rollback conflicts with data migration, while all sections exist.

## `USEQ-0697E472` — Nondeterministic

**Exact statement**

> Identify dependencies, sequencing constraints, freeze periods, peak events, and competing organizational changes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because dependency, sequencing, freeze, peak, and organizational-change completeness and conflict impact require contextual discovery.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A seasonal peak and supplier freeze are omitted, while registered constraints pass.

## `USEQ-0904B9FE` — Nondeterministic

**Exact statement**

> Assess user, customer, worker, accessibility, safety, security, privacy, financial, and continuity impacts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Assessment records are checkable, but adequacy and severity across user, worker, legal, financial, and continuity impacts require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every transition assessment addresses user, customer, worker, accessibility, safety, security, privacy, financial, and continuity impacts.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-6159AEF8` — Nondeterministic

**Exact statement**

> Define governance for decisions during delivery–operations overlap.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because overlap decision ownership, authority, escalation, duration, conflicts, and actual acceptance require governance judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Both teams claim authority for incidents and the escalation owner lacks access; fields pass.

## `USEQ-A4F57314` — Nondeterministic

**Exact statement**

> Prevent schedule pressure from transferring an unaccepted or unsupported service into operation.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because signed acceptance and support records do not prove complete service scope, informed authority, real support coverage, or all transfer paths.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A service transfers through an unregistered emergency path and a powerless manager signs another; gate records pass.

## `USEQ-1243DFDC` — Nondeterministic

**Exact statement**

> Keep transition risks, assumptions, actions, owners, evidence, and decisions current.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Required record fields are objective, but current has no update interval and evidence sufficiency depends on transition context.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The transition register contains risks, assumptions, actions, owners, evidence links, and decisions with update timestamps.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-1CBA7858` — Nondeterministic

**Exact statement**

> Replan when scope, readiness, staffing, suppliers, data, or operating conditions change materially.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Replan records are objective, but material change in scope or operating conditions requires accountable transition judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every transition change marked material has a subsequent approved replan record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5FFA89EF` — Nondeterministic

**Exact statement**

> Define operational acceptance criteria while requirements and architecture are being established.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because requirements and architecture baseline timing is checkable, but acceptance completeness, approval authority, and genuine concurrent establishment are not.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: A trivial criteria document is approved early by an unauthorized actor; timestamps pass.

## `USEQ-58BC0BE5` — Nondeterministic

**Exact statement**

> Include service objectives, capacity, resilience, recoverability, security, privacy, accessibility, supportability, maintainability, cost, and sustainability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because acceptance category presence does not prove valid SLOs, capacity, resilience, recovery, security, privacy, accessibility, support, cost, and sustainability evidence.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: All eleven categories say acceptable without thresholds or tests; fields pass.

## `USEQ-A015BF2C` — Nondeterministic

**Exact statement**

> Include observability, alerting, on-call, escalation, incident, problem, change, and continuity readiness.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because observability, alerting, on-call, incident, problem, change, escalation, and continuity readiness require real operational capability.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: On-call is a departed user and alerts are disabled, while all readiness fields are present.

## `USEQ-B5D1F965` — Nondeterministic

**Exact statement**

> Include data integrity, migration, reconciliation, retention, deletion, backup, restore, and legal-hold readiness.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because data integrity, migration, retention, deletion, backup, restore, and legal-hold readiness require technical and legal oracles.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A backup test restores rows but loses legal-hold metadata; every readiness category passes.

## `USEQ-146FDE47` — Nondeterministic

**Exact statement**

> Include supplier support, contractual obligations, licenses, quotas, service limits, and exit provisions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because supplier support, contracts, licenses, quotas, limits, and exit terms need accurate external interpretation and feasibility.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A side letter removes support but is omitted; all six criteria fields exist.

## `USEQ-31DE5766` — Nondeterministic

**Exact statement**

> Include production access, privileged procedures, break-glass access, and separation of duties.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because production access, privileged procedures, break-glass protection, and real separation of duties require identity and authority analysis.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: One person uses two accounts for approval and execution, while criteria say duties separated.

## `USEQ-F5DC0C23` — Nondeterministic

**Exact statement**

> Include runbooks for routine, degraded, emergency, and recovery operation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because runbook category presence does not prove accurate, executable, complete operation across real routine, degraded, emergency, and recovery states.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: The recovery runbook references a deleted tool, while all four approved documents exist.

## `USEQ-EB933861` — Nondeterministic

**Exact statement**

> Include documentation, training, competence, staffing, handover, and support-channel readiness.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because documentation, training, competence, staffing, handover, and support readiness require representative tasks and authority judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: Staff complete a generic course but cannot restore service; every readiness field passes.

## `USEQ-DEB21FB0` — Nondeterministic

**Exact statement**

> Include known defects, exceptions, technical debt, workarounds, and end-of-support commitments.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because known defect, exception, debt, workaround, and support-commitment completeness and risk meaning are not proven.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A critical defect exists only in chat while all registered issues appear in acceptance criteria.

## `USEQ-FC3E6CE7` — Nondeterministic

**Exact statement**

> Make criteria measurable and linked to evidence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because measure and evidence fields do not prove valid methods, meaningful thresholds, complete populations, fresh evidence, or criterion semantics.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A recovery criterion links to a stale screenshot and threshold of any result; the schema passes.

## `USEQ-DA324CE6` — Nondeterministic

**Exact statement**

> Define which unmet criteria block acceptance and which may receive time-bounded conditional acceptance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because blocking and conditional classification, owner authority, compensating controls, and acceptable expiry require accountable risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Counterexample: A safety failure is marked conditionally acceptable for ten years by a powerless owner; fields pass.

## `USEQ-01C02CC6` — Nondeterministic

**Exact statement**

> Require the receiving organization to review and agree to the criteria before final testing.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because timestamps show order but not that the authorized receiving organization was correctly identified, informed, competent, and genuinely agreed.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A delegate without mandate approves superficial criteria before testing; exact dates pass.

## `USEQ-CFA62933` — Nondeterministic

**Exact statement**

> Design the service so ordinary operation does not depend on undocumented expert intervention.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Ordinary operation, dependency on experts, and completeness of undocumented intervention paths are contextual and unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

## `USEQ-40F75816` — Nondeterministic

**Exact statement**

> Provide clear ownership for every service, component, data set, integration, job, alert, runbook, and supplier.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because complete service, component, dataset, integration, job, alert, runbook, and supplier inventories and true current ownership are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A shadow job lacks an owner and another record names a departed owner; registered rows pass.

## `USEQ-212321D8` — Nondeterministic

**Exact statement**

> Expose health, version, configuration, dependencies, queueing, saturation, and user-impact indicators.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because indicator presence does not prove complete dependencies and queues, correct health and impact semantics, current config, or user coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Health stays green during corrupt writes in an unregistered worker, while all seven indicators exist.

## `USEQ-9A08AF02` — Nondeterministic

**Exact statement**

> Provide safe diagnostics without exposing secrets or personal data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Diagnostic behavior is testable, but proving safety and absence of all secrets or personal data across every output is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered diagnostic endpoint passes approved secret and personal-data scanning and access-control tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-6C68E857` — Nondeterministic

**Exact statement**

> Make routine maintenance, patching, scaling, backup, restore, failover, and certificate rotation executable and testable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because maintenance, patching, scaling, backup, restore, failover, and certificate paths, versions, load, failures, and success oracles are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Routine cases pass, but certificate rotation during failover breaks one regional endpoint.

## `USEQ-4AEF1160` — Nondeterministic

**Exact statement**

> Provide bounded and auditable administrative actions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because administrative action completeness, safe bounds, human attribution, tamper evidence, alternate APIs, and side effects are not established.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: An unregistered support script performs an unbounded tenant-wide edit without an audit event.

## `USEQ-F2960F7A` — Nondeterministic

**Exact statement**

> Prevent support and operations tools from bypassing authorization, tenant isolation, or data-governance controls.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because support tool inventory, identities, roles, tenants, data classes, authorization paths, and governance combinations are not exhaustive.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A hidden impersonation endpoint crosses tenants, while all registered tools pass tests.

## `USEQ-D06B97D0` — Nondeterministic

**Exact statement**

> Provide safe maintenance and degraded modes where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Mode implementation and tests are objective, but which maintenance and degraded modes are required needs architecture and risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every maintenance or degraded mode marked required has a current passing safety and recovery test.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FB76AF44` — Nondeterministic

**Exact statement**

> Design batch, queue, scheduled, and long-running work for visibility, retry, cancellation, reconciliation, and recovery.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because job inventory, state machines, side effects, retry interleavings, cancellation, reconciliation, recovery, and visibility oracles are not complete.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A scheduled job retries after cancellation and duplicates work outside the approved matrix.

## `USEQ-4B6D8A92` — Nondeterministic

**Exact statement**

> Provide clear procedures for data correction and exceptional manual processing.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because procedure presence does not prove authorized, complete, safe, auditable data correction and exceptional processing.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: The manual process allows silent edits without reconciliation, while owner, steps, authorization, and audit fields exist.

## `USEQ-BFBBB220` — Nondeterministic

**Exact statement**

> Ensure customer-facing status and support information can be updated during control-plane failure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The bounded exercise does not exhaust control-plane failure modes or prove the supposedly independent update path lacks shared identity, DNS, network, device, and provider dependencies.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A simulated application control-plane outage allows a status update, but a real identity-plus-DNS failure disables both the control plane and the status publisher. The approved exercise passes while customer-facing information cannot be updated during that control-plane failure.

## `USEQ-E25419F0` — Nondeterministic

**Exact statement**

> Verify operational tasks are accessible and usable by intended staff.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Accessibility and usability by intended staff require representative operational user testing, but no population, protocol, or threshold is defined.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

## `USEQ-1BD6FF0C` — Nondeterministic

**Exact statement**

> Measure operational toil and eliminate recurring avoidable manual work.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Toil measurement and action records are objective, but recurring and avoidable manual work require contextual operations judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Operational toil is measured and every toil item classified as recurring and avoidable has a linked elimination action.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0BFDE64A` — Nondeterministic

**Exact statement**

> Confirm every required operational and support role is staffed for the intended service hours and risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required-role scope, intended service hours, risk-driven coverage, competence, availability, authority, and sustainable staffing are accountable organizational judgments, not roster joins.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Every listed shift has a named person, but a critical recovery role is omitted and the overnight assignee lacks the access and competence to perform it. The schedule check passes while required operational staffing is not ready for the intended risk.

## `USEQ-9730C439` — Nondeterministic

**Exact statement**

> Provide primary, secondary, management, specialist, supplier, and executive escalation coverage as needed.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide primary, secondary, management, specialist, supplier, and executive escalation coverage as needed.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-20DF5585` — Nondeterministic

**Exact statement**

> Verify personnel have required access, equipment, credentials, training, authority, and contact information.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify personnel have required access, equipment, credentials, training, authority, and contact information.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-501984F9` — Nondeterministic

**Exact statement**

> Assess demonstrated competence for high-impact tasks.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess demonstrated competence for high-impact tasks.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-905419B1` — Nondeterministic

**Exact statement**

> Provide practical exercises for deployments, rollback, incidents, failover, restoration, data repair, and customer communication.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide practical exercises for deployments, rollback, incidents, failover, restoration, data repair, and customer communication.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8E55BA3A` — Nondeterministic

**Exact statement**

> Avoid reliance on one person, device, location, time zone, or undocumented skill.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid reliance on one person, device, location, time zone, or undocumented skill.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-2B75F633` — Nondeterministic

**Exact statement**

> Define handover, leave, offboarding, role-change, and contractor-transition procedures.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because procedure headings do not prove complete transition cases, authorized roles, feasibility, timing, or safe execution.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: Contractor offboarding is documented but omits privileged tokens and the responsible owner lacks revocation access.

## `USEQ-3B38E750` — Nondeterministic

**Exact statement**

> Ensure working hours, on-call load, and alert volume are sustainable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure working hours, on-call load, and alert volume are sustainable.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-E46B4EE7` — Nondeterministic

**Exact statement**

> Provide accessible accommodations and inclusive tools for operational personnel.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide accessible accommodations and inclusive tools for operational personnel.” depends on accessibility, language, support obligation, legal permission, or safe-fallback authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-8814F7F9` — Nondeterministic

**Exact statement**

> Confirm support teams understand new behavior, limitations, known issues, eligibility, pricing, and customer remedies.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm support teams understand new behavior, limitations, known issues, eligibility, pricing, and customer remedies.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-A58F8861` — Nondeterministic

**Exact statement**

> Confirm legal, privacy, security, safety, finance, and communications contacts can be reached during an event.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm legal, privacy, security, safety, finance, and communications contacts can be reached during an event.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E04DCCED` — Nondeterministic

**Exact statement**

> Transfer architecture, dependencies, data flows, trust boundaries, operating assumptions, and failure modes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because records cannot prove complete architecture, dependency, flow, boundary, assumption, and failure-mode transfer or receiver understanding.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An undocumented queue and trust boundary are omitted while every handover field is populated.

## `USEQ-4B01668D` — Nondeterministic

**Exact statement**

> Transfer deployment, configuration, migration, rollback, failover, restoration, and decommissioning procedures.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because procedure links do not prove every deployment, migration, rollback, failover, restore, and decommission path is current and executable.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Rollback documentation points to a removed artifact, while all seven procedure fields exist.

## `USEQ-A7916E27` — Nondeterministic

**Exact statement**

> Transfer service objectives, dashboards, alerts, synthetic checks, logs, traces, and diagnostic queries.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because service objectives and observability artifact presence do not prove complete signals, correct queries, alert delivery, or receiver competence.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A critical data-integrity alert is absent, while dashboards, logs, traces, and synthetic links transfer successfully.

## `USEQ-F42A3E39` — Nondeterministic

**Exact statement**

> Transfer supplier contracts, support routes, quotas, escalation terms, and planned changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Transfer supplier contracts, support routes, quotas, escalation terms, and planned changes.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded transition, support, inventory, review, or policy record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B4F1C28A` — Nondeterministic

**Exact statement**

> Transfer open incidents, defects, risks, exceptions, workarounds, debt, and expiry dates.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because open incident, defect, risk, exception, workaround, debt, and expiry completeness cannot be proven by a transition record.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A serious exception exists only in chat and is omitted; every registered open item transfers with dates.

## `USEQ-37F13993` — Nondeterministic

**Exact statement**

> Transfer data classifications, retention, rights, legal holds, privacy obligations, and sensitive operations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Transfer data classifications, retention, rights, legal holds, privacy obligations, and sensitive operations.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded transition, support, inventory, review, or policy record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A245042A` — Nondeterministic

**Exact statement**

> Transfer access-control models, privileged processes, break-glass methods, keys, certificates, and secret-recovery procedures.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because complete privileged paths, key and certificate scope, access authority, recovery feasibility, and safe secret transfer are not established.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An unregistered break-glass credential remains with the old team, while listed access records transfer.

## `USEQ-4DE727BA` — Nondeterministic

**Exact statement**

> Provide customer-support scripts, troubleshooting guides, user communications, and accessibility support.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide customer-support scripts, troubleshooting guides, user communications, and accessibility support.” has checkable support artifacts, but whether the accessibility support is suitable for affected users requires specialist and empirical review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized operations or transition record contains every named field, role, state, procedure, metric, acceptance decision, or evidence link and binds to the declared service and transition.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0CA85D67` — Nondeterministic

**Exact statement**

> Identify authoritative documents and archive superseded material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify authoritative documents and archive superseded material.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-B0CE4AC1` — Nondeterministic

**Exact statement**

> Validate documentation through task execution by the receiving team.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because a finite task exercise cannot prove documentation covers every normal, degraded, emergency, rare, and future task or genuine team competence.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: The receiver completes one deployment but cannot restore a partitioned database outside the approved exercise.

## `USEQ-C35F42F2` — Nondeterministic

**Exact statement**

> Record unanswered questions and close or accept them before transition completion.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because unanswered-question completeness, valid closure, informed risk acceptance, authority, and transition readiness require judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A critical unknown is omitted and another is accepted by someone without authority; record states pass.

## `USEQ-C7B53656` — Nondeterministic

**Exact statement**

> Ensure knowledge remains available after project personnel or suppliers depart.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure knowledge remains available after project personnel or suppliers depart.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-CC3D9793` — Nondeterministic

**Exact statement**

> Verify production accounts, networks, domains, certificates, storage, compute, databases, queues, registries, and observability are provisioned correctly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify production accounts, networks, domains, certificates, storage, compute, databases, queues, registries, and observability are provisioned correctly.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DF105206` — Nondeterministic

**Exact statement**

> Verify production configuration and feature-flag control are versioned, reviewed, and recoverable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because configuration and flag inventory completeness, authentic review, exact version binding, and recoverability across failures are not fully bounded.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A remote flag is omitted and a partial restore loses it, while registered configuration recovery passes.

## `USEQ-C59CC418` — Nondeterministic

**Exact statement**

> Verify least-privilege access for operations, support, responders, automation, and suppliers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify least-privilege access for operations, support, responders, automation, and suppliers.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-CCFEF50D` — Nondeterministic

**Exact statement**

> Test break-glass access and its audit trail.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed bounded_execution check does not fully decide this rule because all break-glass identities, paths, degraded conditions, alert and audit capture, and actual responder use are not exhausted.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A legacy recovery account bypasses audit, while the registered path passes its test and emits records.

## `USEQ-FFCE14A7` — Nondeterministic

**Exact statement**

> Verify monitoring and paging work independently enough to detect failure of the primary service.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify monitoring and paging work independently enough to detect failure of the primary service.” depends on independence, launch demand, risk reduction, least privilege, usability, or acceptable design with no universal bounded threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-EB2AAD9A` — Nondeterministic

**Exact statement**

> Verify service desk, case management, status communication, incident coordination, and evidence repositories are ready.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify service desk, case management, status communication, incident coordination, and evidence repositories are ready.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-3D557FAC` — Nondeterministic

**Exact statement**

> Verify backups, restore tooling, alternate regions, replacement equipment, and required licenses are available.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify backups, restore tooling, alternate regions, replacement equipment, and required licenses are available.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CFCD783F` — Nondeterministic

**Exact statement**

> Confirm quotas, limits, support plans, spending controls, and provider contacts match launch demand.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm quotas, limits, support plans, spending controls, and provider contacts match launch demand.” depends on independence, launch demand, risk reduction, least privilege, usability, or acceptable design with no universal bounded threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-6560F301` — Nondeterministic

**Exact statement**

> Detect non-production credentials, debug paths, test recipients, test payment routes, and synthetic data in production configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect non-production credentials, debug paths, test recipients, test payment routes, and synthetic data in production configuration.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-1A30E6F1` — Nondeterministic

**Exact statement**

> Confirm administrative tools are protected, observable, and recoverable.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm administrative tools are protected, observable, and recoverable.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-5559DBAC` — Nondeterministic

**Exact statement**

> Confirm every operational tool has an owner and support path.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm every operational tool has an owner and support path.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-BB017E08` — Nondeterministic

**Exact statement**

> Define the exact cutover sequence, dependencies, checkpoints, approvals, communications, and rollback points.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because exact cutover steps can be documented while dependencies, checkpoints, approvals, communication audiences, and rollback feasibility remain incomplete.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An external consumer is omitted and rollback depends on deleted data, although every sequence field exists.

## `USEQ-A9709FE9` — Nondeterministic

**Exact statement**

> Rehearse cutover using representative data, volume, permissions, integrations, and timings.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rehearse cutover using representative data, volume, permissions, integrations, and timings.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-2E28DB48` — Nondeterministic

**Exact statement**

> Establish a protected recovery point before irreversible change.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed artifact_verification check does not fully decide this rule because artifact identity and timing are objective, but linked verification does not prove a complete, usable recovery point without a full restore oracle.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

Counterexample: A checksum-valid snapshot predates change and restores one database, but omits an object store required for service.

## `USEQ-FFD30078` — Nondeterministic

**Exact statement**

> Prevent overlapping authoritative writers unless conflict behavior is explicitly designed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent overlapping authoritative writers unless conflict behavior is explicitly designed.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9D527478` — Nondeterministic

**Exact statement**

> Reconcile data, configuration, permissions, jobs, queues, caches, indexes, integrations, and financial totals after cutover.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reconcile data, configuration, permissions, jobs, queues, caches, indexes, integrations, and financial totals after cutover.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D127155D` — Nondeterministic

**Exact statement**

> Validate critical user and operator journeys in production.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate critical user and operator journeys in production.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-A82DE51D` — Nondeterministic

**Exact statement**

> Use progressive adoption or cohort-based transition where it materially reduces risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use progressive adoption or cohort-based transition where it materially reduces risk.” depends on independence, launch demand, risk reduction, least privilege, usability, or acceptable design with no universal bounded threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-B6713B9D` — Nondeterministic

**Exact statement**

> Maintain enhanced monitoring and staffing for a defined early-life period.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain enhanced monitoring and staffing for a defined early-life period.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-D329E9E1` — Nondeterministic

**Exact statement**

> Define how incidents are attributed and owned during overlap.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because overlap incident boundaries, attribution, shared responsibility, authority, escalation, and genuine ownership require organizational judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: A cross-team incident falls between definitions and neither owner accepts it, while the documented rule passes.

## `USEQ-3EB7D7A3` — Nondeterministic

**Exact statement**

> Track support volume, user failure, performance, error, data integrity, accessibility, abuse, and business outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed external_readonly_query check does not fully decide this rule because complete services and users, event sources, metric validity, thresholds, windows, and outcome attribution are not established by observations.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Counterexample: A regional accessibility failure is unmonitored while all named service signals have current values.

## `USEQ-2E22A580` — Nondeterministic

**Exact statement**

> Pause expansion or roll back when predefined thresholds are crossed.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed local_static check does not fully decide this rule because workflow fixtures do not cover every rollout path, threshold input, race, override, stale metric, and safe pause or rollback outcome.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A service account expands rollout using stale metrics after a threshold crossing, while normal fixtures pause correctly.

## `USEQ-5AB3637F` — Nondeterministic

**Exact statement**

> Avoid declaring transition complete before the observation and reconciliation period ends.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed local_static check does not fully decide this rule because state-machine fixtures cannot prove all completion APIs, privileged overrides, clocks, observation periods, and reconciliation evidence are enforced.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: An admin closes transition through an untested API before the period ends; registered paths reject early completion.

## `USEQ-8F0B7D76` — Nondeterministic

**Exact statement**

> Record the final acceptance decision and residual obligations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because final acceptance authority, evidence sufficiency, residual-obligation completeness, and legal or operational meaning are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A manager accepts transition while omitting a retention duty and unresolved recovery risk; fields pass.

## `USEQ-CA4E11E2` — Nondeterministic

**Exact statement**

> Define supported users, service hours, channels, languages, accessibility accommodations, and response expectations.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because supported-user completeness, practical hours, accessible channels, language coverage, accommodations, and response expectations require service judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A disabled user has no accessible channel and a supported locale is omitted, while record fields are complete.

## `USEQ-7593DD19` — Nondeterministic

**Exact statement**

> Provide a clear path for urgent security, privacy, safety, fraud, accessibility, and data-integrity issues.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide a clear path for urgent security, privacy, safety, fraud, accessibility, and data-integrity issues.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-7839483B` — Nondeterministic

**Exact statement**

> Train support staff to authenticate requesters without unsafe data collection or social-engineering bypass.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Train support staff to authenticate requesters without unsafe data collection or social-engineering bypass.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-80257BB3` — Nondeterministic

**Exact statement**

> Give support staff only the data and privileges needed for their role.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Give support staff only the data and privileges needed for their role.” depends on independence, launch demand, risk reduction, least privilege, usability, or acceptable design with no universal bounded threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-1B3A2CEF` — Nondeterministic

**Exact statement**

> Make support actions attributable and auditable.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because support-action completeness, true human actor identity, tamper resistance, and every admin and off-channel path are not proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Counterexample: A support bot changes an account outside the audit stream and another row attributes only the bot; registered actions pass.

## `USEQ-99F82FB7` — Nondeterministic

**Exact statement**

> Provide escalation paths that reach people able to diagnose and change the service.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide escalation paths that reach people able to diagnose and change the service.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-E27FB6F6` — Nondeterministic

**Exact statement**

> Define incident linkage so related support cases contribute to impact assessment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because case and incident completeness and semantic relatedness cannot be proven from linkage fields alone.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: Related support cases use different symptoms and remain unlinked, while all known links resolve.

## `USEQ-744C959B` — Nondeterministic

**Exact statement**

> Prepare communications for maintenance, degradation, data issues, security events, delays, and rollback.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because communication templates do not prove correct facts, lawful disclosure, complete audiences, timeliness, accessibility, or usefulness.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A security template reveals sensitive details and omits affected users, while all six categories have documents.

## `USEQ-F189D5B3` — Nondeterministic

**Exact statement**

> Ensure public status information is accurate and does not expose sensitive detail.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure public status information is accurate and does not expose sensitive detail.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-CD98BD1C` — Nondeterministic

**Exact statement**

> Provide user migration, onboarding, recovery, and rollback guidance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because guidance presence does not prove accurate, complete, accessible, feasible migration, onboarding, recovery, and rollback behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Counterexample: Rollback guidance references an obsolete screen and loses local data, while every guide exists.

## `USEQ-8BD86B01` — Nondeterministic

**Exact statement**

> Preserve customer commitments, entitlements, preferences, accessibility needs, and case history during service transition.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve customer commitments, entitlements, preferences, accessibility needs, and case history during service transition.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-A86B1DF6` — Nondeterministic

**Exact statement**

> Measure resolution quality, recurrence, customer effort, accessibility, and escalation effectiveness rather than closure count alone.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because resolution quality, recurrence, effort, accessibility, and escalation effectiveness need ground truth, sampling, causal definitions, and thresholds.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: Only closed cases are surveyed, so abandoned inaccessible cases vanish and all measures look good.

## `USEQ-F5A6CAF3` — Nondeterministic

**Exact statement**

> Identify stakeholders whose responsibilities, incentives, workflows, skills, or authority will change.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify stakeholders whose responsibilities, incentives, workflows, skills, or authority will change.” makes a universal continuity, access, protection, ownership, knowledge, status-accuracy, or stakeholder-completeness claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-C8079013` — Nondeterministic

**Exact statement**

> Assess readiness, resistance, constraints, and likely unintended consequences.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess readiness, resistance, constraints, and likely unintended consequences.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-C14B308B` — Nondeterministic

**Exact statement**

> Explain the purpose, impact, timeline, support, and decision rights honestly.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Explain the purpose, impact, timeline, support, and decision rights honestly.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-AD2234A0` — Nondeterministic

**Exact statement**

> Involve affected people in design, pilots, and transition decisions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Involve affected people in design, pilots, and transition decisions.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded transition, support, inventory, review, or policy record explicitly named by this control without treating it as proof of complete scope or outcome quality.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-175BD173` — Nondeterministic

**Exact statement**

> Avoid using training to compensate for a fundamentally unusable or unsafe design.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid using training to compensate for a fundamentally unusable or unsafe design.” depends on independence, launch demand, risk reduction, least privilege, usability, or acceptable design with no universal bounded threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-148C33A9` — Nondeterministic

**Exact statement**

> Update policies, procedures, performance measures, roles, and incentives to match the new operating model.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Update policies, procedures, performance measures, roles, and incentives to match the new operating model.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-18C79536` — Nondeterministic

**Exact statement**

> Provide accessible training and support in the languages and formats needed.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide accessible training and support in the languages and formats needed.” depends on accessibility, language, support obligation, legal permission, or safe-fallback authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-4619C514` — Nondeterministic

**Exact statement**

> Stage adoption so feedback can change the rollout.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Stage adoption so feedback can change the rollout.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-38E54A2A` — Nondeterministic

**Exact statement**

> Preserve a safe fallback for users who cannot transition immediately where obligations permit.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve a safe fallback for users who cannot transition immediately where obligations permit.” depends on accessibility, language, support obligation, legal permission, or safe-fallback authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-A809C9C6` — Nondeterministic

**Exact statement**

> Monitor workarounds, shadow systems, abandonment, error, burden, and inequitable impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor workarounds, shadow systems, abandonment, error, burden, and inequitable impact.” has a bounded test or reconciliation component, but full pass status depends on an undefined environment, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2DD0C134` — Nondeterministic

**Exact statement**

> Verify promised benefits and identify displaced costs or harms.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify promised benefits and identify displaced costs or harms.” requires demonstrated competence, sustainable work, team understanding, readiness, representative rehearsal, production journey validity, or benefit evidence without a defined oracle and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-627B9907` — Nondeterministic

**Exact statement**

> Stop or redesign adoption when evidence shows the change is not producing acceptable outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Stop or redesign adoption when evidence shows the change is not producing acceptable outcomes.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-7CE19A5C` — Nondeterministic

**Exact statement**

> Conduct formal operational acceptance against agreed criteria and current evidence.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Conduct formal operational acceptance against agreed criteria and current evidence.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-02C84E3D` — Nondeterministic

**Exact statement**

> Have the receiving owner explicitly accept or reject responsibility.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because an accept or reject field does not prove receiver authority, competence, informed understanding, or genuine assumption of responsibility.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: A temporary delegate checks accept although the team lacks access and rejects responsibility operationally.

## `USEQ-6808A5B2` — Nondeterministic

**Exact statement**

> Record conditional acceptance with compensating controls, monitoring, owner, remediation date, and expiry.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed structured_record_validation check does not fully decide this rule because conditional-acceptance completeness, compensating-control effectiveness, monitoring adequacy, owner authority, remediation credibility, and expiry are judgments.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Counterexample: A disabled alert is listed as compensation and expiry auto-renews; all fields pass.

## `USEQ-00F86BC0` — Nondeterministic

**Exact statement**

> Do not treat project completion or budget exhaustion as operational acceptance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not treat project completion or budget exhaustion as operational acceptance.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-354C8516` — Nondeterministic

**Exact statement**

> Confirm unresolved work has transferred to active owners and backlogs.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because unresolved-work completeness and true active owner authority, backlog visibility, acceptance, priority, and capacity are not proven.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Counterexample: An unregistered defect remains with a departed owner, while registered items link to active backlog accounts.

## `USEQ-E8D0E95A` — Nondeterministic

**Exact statement**

> Confirm temporary access, environments, flags, data copies, supplier accounts, and elevated support are removed or governed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm temporary access, environments, flags, data copies, supplier accounts, and elevated support are removed or governed.” has checkable transition, supplier, configuration, availability, or preservation evidence, but full compliance also depends on legal, privacy, contract, entitlement, or retention authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or reconciliation result, without treating an undefined environment, oracle, threshold, or coverage boundary as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A157B5DF` — Nondeterministic

**Exact statement**

> Confirm old systems, routes, jobs, credentials, and communications are decommissioned or intentionally retained.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed deterministic_composite check does not fully decide this rule because old systems, routes, jobs, credentials, communications, copies, dependencies, and every retained rationale cannot be exhaustively discovered or removal-proven.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

Counterexample: A legacy DNS route and token remain outside inventory while all registered items show decommissioned or retained.

## `USEQ-B049F5B4` — Nondeterministic

**Exact statement**

> Review benefits, incidents, support demand, reliability, costs, human workload, and customer outcomes after transition.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review benefits, incidents, support demand, reliability, costs, human workload, and customer outcomes after transition.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-3C44939B` — Nondeterministic

**Exact statement**

> Conduct a transition retrospective and verify corrective actions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Conduct a transition retrospective and verify corrective actions.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-E1759D28` — Nondeterministic

**Exact statement**

> Reopen acceptance if material assumptions prove false or required controls cease to operate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reopen acceptance if material assumptions prove false or required controls cease to operate.” has a separable record or technical-evidence requirement, but readiness, adequacy, clarity, staffing, involvement, timing, or materiality still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, management query, or bounded test to record the concrete configuration, transition, access, workflow, or operational behavior named by this control without claiming complete production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-66B7CDAC` — Nondeterministic

**Exact statement**

> Treat missing ownership, untested recovery, inadequate staffing, failed migration reconciliation, or unsupported operations as no-go conditions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Treat missing ownership, untested recovery, inadequate staffing, failed migration reconciliation, or unsupported operations as no-go conditions.” requires an accountable staffing, readiness, communication, acceptance, outcome, retrospective, or no-go decision whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`
