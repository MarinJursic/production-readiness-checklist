<!-- Generated by scripts/build_control_classification_docs.py. Do not edit by hand. -->

# `docs/engineering/16-specialized-domains-and-release-assurance.md` — classification reference, part 1 of 1

[Back to the classification index](../README.md).

Controls: **522** · Deterministic: **13** · Nondeterministic: **509**

## `USEQ-5FC4DFAA` — Nondeterministic

**Exact statement**

> Payment credentials never enter logs, analytics, support tools, error reports, or ordinary databases unless explicitly required and protected.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Payment credentials never enter logs, analytics, support tools, error reports, or ordinary databases unless explicitly required and protected.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-04ED44A7` — Nondeterministic

**Exact statement**

> Amount, currency, tax, discount, exchange-rate, fee, commission, and rounding behavior is correct.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Amount, currency, tax, discount, exchange-rate, fee, commission, and rounding behavior is correct.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Amount, currency, tax, discount, exchange-rate, fee, commission, and rounding behavior is correct.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-333EF029` — Nondeterministic

**Exact statement**

> Duplicate-charge and duplicate-payout protection is implemented.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Duplicate protection must hold across every retry, race, provider timeout, callback, reconciliation, and side-effect path. Therefore the proposed structured-record composite does not prove the whole rule “Duplicate-charge and duplicate-payout protection is implemented.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Known duplicate fixtures can pass while response loss plus retry issues a second payout. The proposal can Pass while “Duplicate-charge and duplicate-payout protection is implemented.” remains false.

## `USEQ-197BC003` — Nondeterministic

**Exact statement**

> Payment, refund, payout, and callback requests are idempotent.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Idempotence is universal across request identities, concurrency, retries, provider behavior, and external side effects. Therefore the proposed structured-record composite does not prove the whole rule “Payment, refund, payout, and callback requests are idempotent.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Repeated happy-path requests can pass while an interruption after provider acceptance duplicates a refund. The proposal can Pass while “Payment, refund, payout, and callback requests are idempotent.” remains false.

## `USEQ-6592AF43` — Nondeterministic

**Exact statement**

> Payment webhooks are authenticated or signed and replay-protected.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Webhook security spans all providers, keys, algorithms, rotations, timestamps, parsing, retries, and replay stores. Therefore the proposed structured-record composite does not prove the whole rule “Payment webhooks are authenticated or signed and replay-protected.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known signatures can verify while a legacy endpoint lacks replay protection. The proposal can Pass while “Payment webhooks are authenticated or signed and replay-protected.” remains false.

## `USEQ-CA5B22D9` — Nondeterministic

**Exact statement**

> Pending, authorized, captured, settled, failed, canceled, refunded, disputed, reversed, and chargeback states are modeled correctly.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Correct financial states/transitions require provider, ledger, dispute, legal, and product semantics over every sequence. Therefore the proposed structured-record composite does not prove the whole rule “Pending, authorized, captured, settled, failed, canceled, refunded, disputed, reversed, and chargeback states are modeled correctly.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A complete enum can allow an invalid captured-to-canceled transition. The proposal can Pass while “Pending, authorized, captured, settled, failed, canceled, refunded, disputed, reversed, and chargeback states are modeled correctly.” remains false.

## `USEQ-98E13A36` — Nondeterministic

**Exact statement**

> The product does not claim financial success before authoritative confirmation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every success claim across UI, email, API, reports, and support must be ordered after a correctly identified authoritative confirmation. Therefore the proposed structured-record composite does not prove the whole rule “The product does not claim financial success before authoritative confirmation.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Checkout can wait correctly while an email or analytics event claims success early. The proposal can Pass while “The product does not claim financial success before authoritative confirmation.” remains false.

## `USEQ-FB9EBF41` — Nondeterministic

**Exact statement**

> Reconciliation compares application records with provider, processor, bank, or ledger records.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Reconciliation occurrence does not prove complete records, authoritative sources, matching semantics, discrepancy handling, or all providers. Therefore the proposed structured-record composite does not prove the whole rule “Reconciliation compares application records with provider, processor, bank, or ledger records.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Main processor rows can compare while a bank adjustment or secondary ledger is omitted. The proposal can Pass while “Reconciliation compares application records with provider, processor, bank, or ledger records.” remains false.

## `USEQ-4CDBDA9F` — Nondeterministic

**Exact statement**

> Full refund, partial refund, reversal, chargeback, dispute, and failed-payment workflows are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Workflow tests sample amounts, partial states, timelines, providers, currencies, disputes, and failure combinations. Therefore the proposed structured-record composite does not prove the whole rule “Full refund, partial refund, reversal, chargeback, dispute, and failed-payment workflows are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Each named workflow can pass a fixture while partial refund after chargeback corrupts balance. The proposal can Pass while “Full refund, partial refund, reversal, chargeback, dispute, and failed-payment workflows are tested.” remains false.

## `USEQ-4E901444` — Nondeterministic

**Exact statement**

> Retry behavior cannot cause accidental duplicate charges or payouts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Retry behavior cannot cause accidental duplicate charges or payouts.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-8F8CB05C` — Nondeterministic

**Exact statement**

> Regional authentication, consumer-protection, and financial requirements are applied where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Regional authentication, consumer-protection, and financial requirements are applied where relevant.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-513F01A1` — Nondeterministic

**Exact statement**

> Fraud controls are monitored and reviewed for discriminatory, accessibility, and customer-harm impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Fraud controls are monitored and reviewed for discriminatory, accessibility, and customer-harm impact.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Fraud controls are monitored and reviewed for discriminatory, accessibility, and customer-harm impact.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A47801E4` — Nondeterministic

**Exact statement**

> Payout-destination and bank-detail changes receive enhanced verification.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Payout-destination and bank-detail changes receive enhanced verification.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Payout-destination and bank-detail changes receive enhanced verification.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-BE1302B9` — Nondeterministic

**Exact statement**

> Financial exports and reports are access-controlled and audited.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Access/audit settings cannot prove every export/report copy, identity, provider path, purpose, or valid authority. Therefore the proposed structured-record composite does not prove the whole rule “Financial exports and reports are access-controlled and audited.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Primary reports can be restricted while emailed exports or provider administrators bypass audit. The proposal can Pass while “Financial exports and reports are access-controlled and audited.” remains false.

## `USEQ-E056DB88` — Nondeterministic

**Exact statement**

> Provider outage, delay, duplicate event, out-of-order event, and reconciliation failure are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Named provider failures have many timings, sequences, durations, compound states, and financial side-effect oracles. Therefore the proposed structured-record composite does not prove the whole rule “Provider outage, delay, duplicate event, out-of-order event, and reconciliation failure are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Individual fixtures can pass while delay plus duplicate/out-of-order events double-settle. The proposal can Pass while “Provider outage, delay, duplicate event, out-of-order event, and reconciliation failure are tested.” remains false.

## `USEQ-F59B2D4E` — Nondeterministic

**Exact statement**

> Invoices, receipts, statements, tax documents, and disclosures are correct.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Invoices, receipts, statements, tax documents, and disclosures are correct.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Invoices, receipts, statements, tax documents, and disclosures are correct.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C12F67B1` — Nondeterministic

**Exact statement**

> Subscription renewal, trial conversion, proration, upgrade, downgrade, pause, cancellation, grace period, and retry behavior are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Subscription behavior spans plan/currency/time-zone/tax/proration/payment states and their combinatorial transitions. Therefore the proposed structured-record composite does not prove the whole rule “Subscription renewal, trial conversion, proration, upgrade, downgrade, pause, cancellation, grace period, and retry behavior are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Each feature can pass alone while downgrade during grace-period retry charges incorrectly. The proposal can Pass while “Subscription renewal, trial conversion, proration, upgrade, downgrade, pause, cancellation, grace period, and retry behavior are tested.” remains false.

## `USEQ-6DF3B045` — Nondeterministic

**Exact statement**

> Customer support cannot alter money movement without controlled authorization, separation of duties where required, and audit.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Customer support cannot alter money movement without controlled authorization, separation of duties where required, and audit.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Customer support cannot alter money movement without controlled authorization, separation of duties where required, and audit.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-40B1DC25` — Nondeterministic

**Exact statement**

> Connection authentication and authorization remain valid for the lifetime of the connection.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Connection-long authentication/authorization must handle every role/token/tenant/policy change, cache, outage, and race. Therefore the proposed structured-record composite does not prove the whole rule “Connection authentication and authorization remain valid for the lifetime of the connection.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Initial auth can pass while later suspension or role change leaves the stream authorized. The proposal can Pass while “Connection authentication and authorization remain valid for the lifetime of the connection.” remains false.

## `USEQ-2DF2A48C` — Nondeterministic

**Exact statement**

> Browser-origin and cross-site connection risks are addressed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Browser-origin and cross-site connection risks are addressed.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9A67EA71` — Nondeterministic

**Exact statement**

> Messages and events are schema-validated.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Schema validation coverage depends on every producer/consumer, version, message path, serializer, error route, and semantic constraint. Therefore the proposed structured-record composite does not prove the whole rule “Messages and events are schema-validated.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Declared schemas can validate while a legacy producer or semantically invalid payload bypasses them. The proposal can Pass while “Messages and events are schema-validated.” remains false.

## `USEQ-65100565` — Nondeterministic

**Exact statement**

> Message size, rate, fan-out, subscription count, and connection count are bounded.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Bounds need complete interface/subscription/fan-out scope and workload/threat-appropriate thresholds and enforcement paths. Therefore the proposed structured-record composite does not prove the whole rule “Message size, rate, fan-out, subscription count, and connection count are bounded.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Limits can exist while one message expands recursively beyond downstream capacity. The proposal can Pass while “Message size, rate, fan-out, subscription count, and connection count are bounded.” remains false.

## `USEQ-085B1CFB` — Nondeterministic

**Exact statement**

> Ordering guarantees and conflict-resolution behavior are documented.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Documentation headings do not prove complete/correct ordering and conflict semantics across all operations and versions. Therefore the proposed structured-record composite does not prove the whole rule “Ordering guarantees and conflict-resolution behavior are documented.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

The contract can omit cross-partition conflicts or describe behavior unlike production. The proposal can Pass while “Ordering guarantees and conflict-resolution behavior are documented.” remains false.

## `USEQ-AC384A6E` — Nondeterministic

**Exact statement**

> Duplicate, delayed, reordered, and lost message behavior is handled.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Handling duplicate/delay/reorder/loss must be correct across all message types, states, concurrency, retries, and persistence failures. Therefore the proposed structured-record composite does not prove the whole rule “Duplicate, delayed, reordered, and lost message behavior is handled.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Four fixtures can pass while loss during reconciliation leaves permanent inconsistency. The proposal can Pass while “Duplicate, delayed, reordered, and lost message behavior is handled.” remains false.

## `USEQ-61B2D449` — Nondeterministic

**Exact statement**

> Backpressure prevents memory, queue, and downstream-resource exhaustion.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Backpressure effectiveness spans every queue/resource, fan-out, dependency, workload, failure, and admission path. Therefore the proposed structured-record composite does not prove the whole rule “Backpressure prevents memory, queue, and downstream-resource exhaustion.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Load tests can pass while an unbounded retry queue exhausts memory. The proposal can Pass while “Backpressure prevents memory, queue, and downstream-resource exhaustion.” remains false.

## `USEQ-880F8C81` — Nondeterministic

**Exact statement**

> Presence, typing, cursor, draft, and collaboration data do not leak across users or tenants.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Presence, typing, cursor, draft, and collaboration data do not leak across users or tenants.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-1F0175D9` — Nondeterministic

**Exact statement**

> Permission changes, logout, suspension, and deletion terminate or constrain active streams.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** All active streams must react correctly to permission/logout/suspension/deletion across caches, workers, reconnects, and races. Therefore the proposed structured-record composite does not prove the whole rule “Permission changes, logout, suspension, and deletion terminate or constrain active streams.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

A live stream can close while an offline client reconnects with stale authorization. The proposal can Pass while “Permission changes, logout, suspension, and deletion terminate or constrain active streams.” remains false.

## `USEQ-50CCE7A2` — Nondeterministic

**Exact statement**

> Fan-out capacity and hot-room or hot-topic behavior are load-tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Load tests sample room/topic size, message mix, topology, network, duration, churn, and provider behavior. Therefore the proposed structured-record composite does not prove the whole rule “Fan-out capacity and hot-room or hot-topic behavior are load-tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A hot-room fixture can pass while reconnection fan-out after regional loss collapses capacity. The proposal can Pass while “Fan-out capacity and hot-room or hot-topic behavior are load-tested.” remains false.

## `USEQ-58F7B452` — Nondeterministic

**Exact statement**

> Event history and replay preserve authorization, retention, deletion, and tenant boundaries.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Replay/history must preserve authorization, retention, deletion, tenant scope over every stored copy, event, subject, and policy change. Therefore the proposed structured-record composite does not prove the whole rule “Event history and replay preserve authorization, retention, deletion, and tenant boundaries.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Replay can enforce current tenant access while resurrecting legally deleted historical data. The proposal can Pass while “Event history and replay preserve authorization, retention, deletion, and tenant boundaries.” remains false.

## `USEQ-44AC2BF0` — Nondeterministic

**Exact statement**

> Collaborative edits handle concurrency, conflict, undo, attribution, and recovery correctly.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Collaborative correctness spans all edit interleavings, partitions, conflicts, undo graphs, attribution, and recovery states. Therefore the proposed structured-record composite does not prove the whole rule “Collaborative edits handle concurrency, conflict, undo, attribution, and recovery correctly.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Common concurrent edits can pass while an offline conflict loses attribution or corrupts undo. The proposal can Pass while “Collaborative edits handle concurrency, conflict, undo, attribution, and recovery correctly.” remains false.

## `USEQ-49E96333` — Nondeterministic

**Exact statement**

> Establish fail-safe and fail-secure behavior.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fail-safe/fail-secure behavior depends on hazards, threats, human safety, state, and failure-mode judgment. Therefore the proposed structured-record composite does not prove the whole rule “Establish fail-safe and fail-secure behavior.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A policy can label fail-open behavior safe despite enabling physical harm. The proposal can Pass while “Establish fail-safe and fail-secure behavior.” remains false.

## `USEQ-C010CAA8` — Nondeterministic

**Exact statement**

> Safety alerts are perceptible, timely, actionable, and accessible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Safety alerts are perceptible, timely, actionable, and accessible.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-742FE91A` — Nondeterministic

**Exact statement**

> Safety thresholds are conservative, documented, and validated.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Safety thresholds are conservative, documented, and validated.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Safety thresholds are conservative, documented, and validated.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2A52C1E5` — Nondeterministic

**Exact statement**

> Sensor, input, model, communication, timing, power, and actuator failure modes are tested as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Sensor, input, model, communication, timing, power, and actuator failure modes are tested as applicable.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Sensor, input, model, communication, timing, power, and actuator failure modes are tested as applicable.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-69A5E188` — Nondeterministic

**Exact statement**

> Recall, disablement, rollback, and customer-notification procedures exist.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Procedure presence does not prove workable recall/disable/rollback/customer reach, authority, timing, or complete affected scope. Therefore the proposed structured-record composite does not prove the whole rule “Recall, disablement, rollback, and customer-notification procedures exist.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A recall document can exist while affected customers cannot be identified. The proposal can Pass while “Recall, disablement, rollback, and customer-notification procedures exist.” remains false.

## `USEQ-EBD1622C` — Nondeterministic

**Exact statement**

> Camera, microphone, screen, and device permissions are requested only when needed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Camera, microphone, screen, and device permissions are requested only when needed.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-E6602317` — Nondeterministic

**Exact statement**

> Permission and recording state are visible and revocable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Visibility and revocability require complete devices/sessions/recording paths, current state, usable controls, and authority. Therefore the proposed structured-record composite does not prove the whole rule “Permission and recording state are visible and revocable.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

The UI can show recording off while a provider recorder remains active. The proposal can Pass while “Permission and recording state are visible and revocable.” remains false.

## `USEQ-A1A11D01` — Nondeterministic

**Exact statement**

> Signaling, room membership, invitations, and media access are authenticated and authorized.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authentication/authorization must cover every signaling, membership, invite, media path, token state, race, and provider. Therefore the proposed structured-record composite does not prove the whole rule “Signaling, room membership, invitations, and media access are authenticated and authorized.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal joins can pass while a stale invite or media URL bypasses membership. The proposal can Pass while “Signaling, room membership, invitations, and media access are authenticated and authorized.” remains false.

## `USEQ-2534B0B7` — Nondeterministic

**Exact statement**

> Session and room identifiers are sufficiently unpredictable where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Session and room identifiers are sufficiently unpredictable where required.” has checkable evidence clauses, but its full result also requires an explicit entropy, response-time, disablement-time, or triage-time threshold.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Session and room identifiers are sufficiently unpredictable where required.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-7F4FC4B0` — Nondeterministic

**Exact statement**

> Users cannot join, observe, record, or retrieve communications without authorization.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of unauthorized joining/observation/recording/retrieval is an adversarial universal claim across protocols and stored media. Therefore the proposed structured-record composite does not prove the whole rule “Users cannot join, observe, record, or retrieve communications without authorization.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Access tests can pass while a guessed recording URL or compromised relay exposes content. The proposal can Pass while “Users cannot join, observe, record, or retrieve communications without authorization.” remains false.

## `USEQ-FA268344` — Nondeterministic

**Exact statement**

> Media relay, direct peer connectivity, network-address exposure, and metadata leakage are threat-modeled.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Threat-model records cannot prove complete relay/peer/network/metadata threats or mitigations across clients/providers. Therefore the proposed structured-record composite does not prove the whole rule “Media relay, direct peer connectivity, network-address exposure, and metadata leakage are threat-modeled.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

The model can omit IP exposure in a fallback relay path. The proposal can Pass while “Media relay, direct peer connectivity, network-address exposure, and metadata leakage are threat-modeled.” remains false.

## `USEQ-033AF627` — Nondeterministic

**Exact statement**

> Recording requires appropriate consent, indication, retention, access control, and deletion.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Recording requires appropriate consent, indication, retention, access control, and deletion.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Recording requires appropriate consent, indication, retention, access control, and deletion.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-B9628E94` — Nondeterministic

**Exact statement**

> Transcription, translation, moderation, and analysis follow privacy and consent requirements.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Transcription, translation, moderation, and analysis follow privacy and consent requirements.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-DF4B3471` — Nondeterministic

**Exact statement**

> Background noise, echo, poor bandwidth, jitter, packet loss, device changes, and reconnection are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Media tests sample device/network/acoustic values, combinations, transitions, durations, and user contexts. Therefore the proposed structured-record composite does not prove the whole rule “Background noise, echo, poor bandwidth, jitter, packet loss, device changes, and reconnection are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Each condition can pass alone while device change during packet loss breaks reconnection. The proposal can Pass while “Background noise, echo, poor bandwidth, jitter, packet loss, device changes, and reconnection are tested.” remains false.

## `USEQ-BD86BC9E` — Nondeterministic

**Exact statement**

> Participant identity, speaking state, recording state, muting, removal, and access controls are clear and reliable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Participant identity, speaking state, recording state, muting, removal, and access controls are clear and reliable.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Participant identity, speaking state, recording state, muting, removal, and access controls are clear and reliable.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-4C121133` — Nondeterministic

**Exact statement**

> Abuse-reporting and moderation controls exist where needed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Abuse-reporting and moderation controls exist where needed.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Abuse-reporting and moderation controls exist where needed.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-A3195B6F` — Nondeterministic

**Exact statement**

> Encryption and key-management behavior match the communication risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Encryption and key-management behavior match the communication risk.” is nondeterministic because it is a safety, residual-harm, essential-access, or cryptographic-risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-51374A62` — Nondeterministic

**Exact statement**

> Live media cannot expose private network, location, device, or identity information beyond accepted risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Live media cannot expose private network, location, device, or identity information beyond accepted risk.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Live media cannot expose private network, location, device, or identity information beyond accepted risk.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-90C7782A` — Nondeterministic

**Exact statement**

> Device capabilities are requested only when necessary.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Device capabilities are requested only when necessary.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-9F507275` — Nondeterministic

**Exact statement**

> Permission requests are contextual, understandable, revocable, and accessible.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Permission requests are contextual, understandable, revocable, and accessible.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-967F9F61` — Nondeterministic

**Exact statement**

> Denial of permission has a safe and usable path.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Denial of permission has a safe and usable path.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-001D20FE` — Nondeterministic

**Exact statement**

> Precise data is not collected when coarse data is sufficient.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Precise data is not collected when coarse data is sufficient.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-CD98743E` — Nondeterministic

**Exact statement**

> Location and sensor-data retention is minimized.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Location and sensor-data retention is minimized.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8DA60AC4` — Nondeterministic

**Exact statement**

> Access is auditable where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Access is auditable where appropriate.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Access is auditable where appropriate.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-49F1368C` — Nondeterministic

**Exact statement**

> Shared-device and background-access risks are addressed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Shared-device and background-access risks are addressed.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-12A5A8D0` — Nondeterministic

**Exact statement**

> Spoofed, stale, inaccurate, unavailable, or manipulated sensor data is handled safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Spoofed, stale, inaccurate, unavailable, or manipulated sensor data is handled safely.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Spoofed, stale, inaccurate, unavailable, or manipulated sensor data is handled safely.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FD11B478` — Nondeterministic

**Exact statement**

> Map, geocoding, routing, and location providers receive only approved data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Approved-data truth requires complete provider/request/field/purpose/jurisdiction scope and accountable privacy authority. Therefore the proposed structured-record composite does not prove the whole rule “Map, geocoding, routing, and location providers receive only approved data.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

The main geocoder can receive approved fields while routing logs transmit an unapproved identifier. The proposal can Pass while “Map, geocoding, routing, and location providers receive only approved data.” remains false.

## `USEQ-374B2E5E` — Nondeterministic

**Exact statement**

> Location sharing has clear audience, precision, duration, and expiry controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Location sharing has clear audience, precision, duration, and expiry controls.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Location sharing has clear audience, precision, duration, and expiry controls.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-49F3B2EC` — Nondeterministic

**Exact statement**

> Safety implications of exposing home, work, travel, or real-time location are reviewed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Safety implications of exposing home, work, travel, or real-time location are reviewed.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Safety implications of exposing home, work, travel, or real-time location are reviewed.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-948F1E8C` — Nondeterministic

**Exact statement**

> Permission changes take effect promptly across caches, sessions, background jobs, and integrations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Permission changes take effect promptly across caches, sessions, background jobs, and integrations.” has checkable evidence clauses, but its full result also requires an explicit entropy, response-time, disablement-time, or triage-time threshold.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Permission changes take effect promptly across caches, sessions, background jobs, and integrations.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DCB37BC2` — Nondeterministic

**Exact statement**

> Every event, property, identifier, destination, and retention period is inventoried.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every event/property/identifier/destination/retention inventory spans dynamic code, providers, copies, exports, and legal periods. Therefore the proposed structured-record composite does not prove the whole rule “Every event, property, identifier, destination, and retention period is inventoried.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Declared analytics can pass while an experiment property or supplier destination is omitted. The proposal can Pass while “Every event, property, identifier, destination, and retention period is inventoried.” remains false.

## `USEQ-03BBB392` — Nondeterministic

**Exact statement**

> Event names and semantics are versioned and documented.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Versions/names are exact, but event-semantic accuracy, compatibility, and complete event scope require business/data judgment. Therefore the proposed structured-record composite does not prove the whole rule “Event names and semantics are versioned and documented.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A versioned event can document semantics that do not match emitted behavior. The proposal can Pass while “Event names and semantics are versioned and documented.” remains false.

## `USEQ-E990B2BF` — Nondeterministic

**Exact statement**

> Analytics never receives secrets, credentials, full payment data, prohibited health data, or unnecessary sensitive content.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Analytics never receives secrets, credentials, full payment data, prohibited health data, or unnecessary sensitive content.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-376714FD` — Nondeterministic

**Exact statement**

> Consent and opt-out choices are enforced at collection and transmission time.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Consent validity/requirement and enforcement must cover all collection/transmission paths, retries, providers, purposes, and jurisdictions. Therefore the proposed structured-record composite does not prove the whole rule “Consent and opt-out choices are enforced at collection and transmission time.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

The browser can block before opt-in while server-side analytics transmits anyway. The proposal can Pass while “Consent and opt-out choices are enforced at collection and transmission time.” remains false.

## `USEQ-1DB8EA36` — Nondeterministic

**Exact statement**

> User and tenant isolation is preserved in analytics and reporting.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Analytics/report tenant isolation is an adversarial claim across queries, caches, exports, aggregates, models, and authorization states. Therefore the proposed structured-record composite does not prove the whole rule “User and tenant isolation is preserved in analytics and reporting.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal reports can pass while a crafted filter or shared cache exposes another tenant. The proposal can Pass while “User and tenant isolation is preserved in analytics and reporting.” remains false.

## `USEQ-E52D44A3` — Nondeterministic

**Exact statement**

> Experiment assignment is deterministic and does not bypass authorization, pricing, safety, or legal controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Assignment determinism is testable, but complete bypass prevention across authorization/pricing/safety/legal controls is not. Therefore the proposed structured-record composite does not prove the whole rule “Experiment assignment is deterministic and does not bypass authorization, pricing, safety, or legal controls.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Stable assignment can pass while an experiment variant skips an unmodeled legal or pricing control. The proposal can Pass while “Experiment assignment is deterministic and does not bypass authorization, pricing, safety, or legal controls.” remains false.

## `USEQ-7E879111` — Nondeterministic

**Exact statement**

> Experiment combinations and interactions are bounded and tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Experiment interaction space grows combinatorially across flags, cohorts, timing, order, products, and failures. Therefore the proposed structured-record composite does not prove the whole rule “Experiment combinations and interactions are bounded and tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Pairwise tests can pass while a three-experiment combination violates safety. The proposal can Pass while “Experiment combinations and interactions are bounded and tested.” remains false.

## `USEQ-9DB9E528` — Nondeterministic

**Exact statement**

> Experiments have owners, hypotheses, stop criteria, monitoring, and expiry dates.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Owner/expiry fields are exact, but hypothesis quality, stop criteria, monitoring adequacy, and owner authority require judgment. Therefore the proposed structured-record composite does not prove the whole rule “Experiments have owners, hypotheses, stop criteria, monitoring, and expiry dates.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Every experiment can have fields while stop criteria ignore harmful outcomes. The proposal can Pass while “Experiments have owners, hypotheses, stop criteria, monitoring, and expiry dates.” remains false.

## `USEQ-838BB8B2` — Nondeterministic

**Exact statement**

> Harmful or degraded variants can be disabled immediately.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Harmful or degraded variants can be disabled immediately.” has checkable evidence clauses, but its full result also requires an explicit entropy, response-time, disablement-time, or triage-time threshold.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Harmful or degraded variants can be disabled immediately.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-36303028` — Nondeterministic

**Exact statement**

> Metric definitions resist double counting, bot traffic, missing events, retries, and clock differences.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Metric robustness needs complete bots/retry/loss/duplication/clock/source behavior and authoritative truth over time. Therefore the proposed structured-record composite does not prove the whole rule “Metric definitions resist double counting, bot traffic, missing events, retries, and clock differences.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Fixtures can pass while a new bot or late duplicate inflates results. The proposal can Pass while “Metric definitions resist double counting, bot traffic, missing events, retries, and clock differences.” remains false.

## `USEQ-6F0BAC43` — Nondeterministic

**Exact statement**

> Attribution and advertising code does not alter critical behavior or leak sensitive data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Attribution and advertising code does not alter critical behavior or leak sensitive data.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-A77F523C` — Nondeterministic

**Exact statement**

> Session replay and recording tools mask sensitive fields and are privacy-reviewed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Session replay and recording tools mask sensitive fields and are privacy-reviewed.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Session replay and recording tools mask sensitive fields and are privacy-reviewed.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-5C3A4846` — Nondeterministic

**Exact statement**

> Data quality, loss, duplication, delay, schema drift, and provider outage are monitored.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring cannot prove complete data/provider pipeline scope, event loss, schema drift, and alert sensitivity/freshness. Therefore the proposed structured-record composite does not prove the whole rule “Data quality, loss, duplication, delay, schema drift, and provider outage are monitored.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known streams can be healthy while an uninstrumented provider silently drops events. The proposal can Pass while “Data quality, loss, duplication, delay, schema drift, and provider outage are monitored.” remains false.

## `USEQ-4BF18A72` — Nondeterministic

**Exact statement**

> Deletion and privacy requests propagate to analytics providers where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Deletion and privacy requests propagate to analytics providers where required.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Deletion and privacy requests propagate to analytics providers where required.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-7C1F74A2` — Nondeterministic

**Exact statement**

> Reports do not expose small-group or individual data improperly.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Reports do not expose small-group or individual data improperly.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-FAC20782` — Nondeterministic

**Exact statement**

> Product, price, tax, discount, availability, inventory, delivery, and return information is accurate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Product, price, tax, discount, availability, inventory, delivery, and return information is accurate.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Product, price, tax, discount, availability, inventory, delivery, and return information is accurate.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D4A8D9FB` — Nondeterministic

**Exact statement**

> Inventory reservation, decrement, release, oversell, backorder, and cancellation behavior is correct under concurrency.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Inventory correctness under concurrency spans all reservation/cancel/backorder/interruption/race/replica sequences. Therefore the proposed structured-record composite does not prove the whole rule “Inventory reservation, decrement, release, oversell, backorder, and cancellation behavior is correct under concurrency.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Load tests can pass while simultaneous cancellation and fulfillment oversells. The proposal can Pass while “Inventory reservation, decrement, release, oversell, backorder, and cancellation behavior is correct under concurrency.” remains false.

## `USEQ-AA20A64E` — Nondeterministic

**Exact statement**

> Cart and checkout totals cannot be manipulated by the client.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing client manipulation requires every price/tax/shipping/discount/rounding/identity path and server-side authoritative calculation. Therefore the proposed structured-record composite does not prove the whole rule “Cart and checkout totals cannot be manipulated by the client.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Main checkout can recompute totals while a legacy endpoint trusts a client fee. The proposal can Pass while “Cart and checkout totals cannot be manipulated by the client.” remains false.

## `USEQ-100B6DA2` — Nondeterministic

**Exact statement**

> Coupon, promotion, referral, loyalty, and gift-card rules cannot be abused or stacked improperly.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Improper stacking/abuse depends on complete promotion rules, identities, timing, business intent, and adversarial combinations. Therefore the proposed structured-record composite does not prove the whole rule “Coupon, promotion, referral, loyalty, and gift-card rules cannot be abused or stacked improperly.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known coupon pairs can reject while referral plus gift-card timing yields unintended stacking. The proposal can Pass while “Coupon, promotion, referral, loyalty, and gift-card rules cannot be abused or stacked improperly.” remains false.

## `USEQ-2F559873` — Nondeterministic

**Exact statement**

> Order creation is idempotent and reconciled with payment and inventory systems.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Order idempotence/reconciliation spans retries, provider timeouts, inventory races, partial side effects, and all source records. Therefore the proposed structured-record composite does not prove the whole rule “Order creation is idempotent and reconciled with payment and inventory systems.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Request retries can pass while payment succeeds and inventory timeout creates a duplicate order. The proposal can Pass while “Order creation is idempotent and reconciled with payment and inventory systems.” remains false.

## `USEQ-F072E319` — Nondeterministic

**Exact statement**

> Partial fulfillment, split shipment, substitution, backorder, cancellation, refund, and return workflows are tested.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fulfillment/refund/return workflows have combinatorial item, shipment, provider, timing, tax, and failure states. Therefore the proposed structured-record composite does not prove the whole rule “Partial fulfillment, split shipment, substitution, backorder, cancellation, refund, and return workflows are tested.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Each named flow can pass alone while split shipment plus partial return refunds incorrectly. The proposal can Pass while “Partial fulfillment, split shipment, substitution, backorder, cancellation, refund, and return workflows are tested.” remains false.

## `USEQ-9818DD41` — Nondeterministic

**Exact statement**

> Delivery addresses, contact details, and fulfillment instructions are protected.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Delivery addresses, contact details, and fulfillment instructions are protected.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-96A83B0F` — Nondeterministic

**Exact statement**

> Shipping, customs, tax, and regional restrictions are applied correctly.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Shipping, customs, tax, and regional restrictions are applied correctly.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-2B9E1A68` — Nondeterministic

**Exact statement**

> Order status reflects authoritative state and handles delayed or out-of-order provider events.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authoritative order state and delayed/out-of-order handling require provider/business semantics and complete transition histories. Therefore the proposed structured-record composite does not prove the whole rule “Order status reflects authoritative state and handles delayed or out-of-order provider events.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A state machine can accept a stale provider event that regresses a fulfilled order. The proposal can Pass while “Order status reflects authoritative state and handles delayed or out-of-order provider events.” remains false.

## `USEQ-8848D08B` — Nondeterministic

**Exact statement**

> Marketplace seller and buyer permissions, payouts, disputes, and fees are isolated and audited.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Marketplace isolation/audit spans all seller/buyer identities, balances, fees, disputes, exports, support, and provider paths. Therefore the proposed structured-record composite does not prove the whole rule “Marketplace seller and buyer permissions, payouts, disputes, and fees are isolated and audited.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal permissions can pass while shared reports or support actions cross sellers. The proposal can Pass while “Marketplace seller and buyer permissions, payouts, disputes, and fees are isolated and audited.” remains false.

## `USEQ-E1CC2FBF` — Nondeterministic

**Exact statement**

> Customer support cannot modify prices, refunds, inventory, or fulfillment without controlled authorization.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Controlled authorization must cover every support tool/API/provider path, valid purpose, amount, role, and audit event. Therefore the proposed structured-record composite does not prove the whole rule “Customer support cannot modify prices, refunds, inventory, or fulfillment without controlled authorization.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

The main console can restrict actions while a legacy support endpoint changes refunds. The proposal can Pass while “Customer support cannot modify prices, refunds, inventory, or fulfillment without controlled authorization.” remains false.

## `USEQ-2DA04192` — Nondeterministic

**Exact statement**

> Product recalls and safety notifications can identify and contact affected customers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Recall capability requires complete product/batch/customer/channel/contact/jurisdiction scope and legally adequate communication. Therefore the proposed structured-record composite does not prove the whole rule “Product recalls and safety notifications can identify and contact affected customers.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Known buyers can be found while marketplace or offline customers remain unidentified. The proposal can Pass while “Product recalls and safety notifications can identify and contact affected customers.” remains false.

## `USEQ-FB281AC4` — Nondeterministic

**Exact statement**

> On-chain and off-chain components, networks, contracts, bridges, oracles, custodians, signers, and keys are inventoried.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete on/off-chain inventory spans networks, upgrades, bridges, oracles, custodians, signers, keys, providers, and hidden dependencies. Therefore the proposed structured-record composite does not prove the whole rule “On-chain and off-chain components, networks, contracts, bridges, oracles, custodians, signers, and keys are inventoried.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Declared contracts can pass while a fallback oracle or signer key is omitted. The proposal can Pass while “On-chain and off-chain components, networks, contracts, bridges, oracles, custodians, signers, and keys are inventoried.” remains false.

## `USEQ-4EE7054B` — Nondeterministic

**Exact statement**

> Smart-contract code and upgrades receive independent review and testing.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Independent review and tests occur, but reviewer independence/competence and smart-contract correctness across all states are not proven. Therefore the proposed structured-record composite does not prove the whole rule “Smart-contract code and upgrades receive independent review and testing.”.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_protocol`

### Skeptical rejection counterexample

A formal-looking review can miss a reentrancy path or share the implementer's incentives. The proposal can Pass while “Smart-contract code and upgrades receive independent review and testing.” remains false.

## `USEQ-5A5F7869` — Nondeterministic

**Exact statement**

> Irreversible actions require clear confirmation, address validation, amount validation, and network validation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clarity and correct address/amount/network validation require UX, chain semantics, and every irreversible-action path. Therefore the proposed structured-record composite does not prove the whole rule “Irreversible actions require clear confirmation, address validation, amount validation, and network validation.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

### Skeptical rejection counterexample

A confirmation can be clear while validating a syntactically valid but wrong-chain address. The proposal can Pass while “Irreversible actions require clear confirmation, address validation, amount validation, and network validation.” remains false.

## `USEQ-367B41AA` — Nondeterministic

**Exact statement**

> Replay, reorganization, finality, nonce, fee, front-running, sandwiching, and transaction-replacement behavior is addressed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Replay, reorganization, finality, nonce, fee, front-running, sandwiching, and transaction-replacement behavior is addressed.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Replay, reorganization, finality, nonce, fee, front-running, sandwiching, and transaction-replacement behavior is addressed.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-102C77C7` — Nondeterministic

**Exact statement**

> Private keys and signing devices follow strong key-management controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Private keys and signing devices follow strong key-management controls.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Private keys and signing devices follow strong key-management controls.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-416C0FA5` — Nondeterministic

**Exact statement**

> Multi-party approval is used for high-value or administrative actions where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Multi-party approval is used for high-value or administrative actions where appropriate.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Multi-party approval is used for high-value or administrative actions where appropriate.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-76F26E01` — Nondeterministic

**Exact statement**

> Contract ownership, upgrade authority, pause authority, and emergency controls are documented and restricted.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authority fields are exact, but complete restriction, valid owners, emergency paths, provider keys, and real control require accountability. Therefore the proposed structured-record composite does not prove the whole rule “Contract ownership, upgrade authority, pause authority, and emergency controls are documented and restricted.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Docs can name roles while an undisclosed key or multisig threshold permits unauthorized upgrade. The proposal can Pass while “Contract ownership, upgrade authority, pause authority, and emergency controls are documented and restricted.” remains false.

## `USEQ-20A50596` — Nondeterministic

**Exact statement**

> Oracle, bridge, exchange, custodian, and wallet-provider failures are threat-modeled.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Threat-model presence cannot prove complete oracle/bridge/exchange/custodian/wallet failure scope, attacker behavior, or mitigations. Therefore the proposed structured-record composite does not prove the whole rule “Oracle, bridge, exchange, custodian, and wallet-provider failures are threat-modeled.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A model can omit correlated bridge/oracle manipulation. The proposal can Pass while “Oracle, bridge, exchange, custodian, and wallet-provider failures are threat-modeled.” remains false.

## `USEQ-C38E0EC6` — Nondeterministic

**Exact statement**

> Chain congestion, network fork, delayed finality, and provider outage are handled.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Congestion/fork/finality/outage handling spans chains, depths, providers, timing, reorgs, retries, and financial state. Therefore the proposed structured-record composite does not prove the whole rule “Chain congestion, network fork, delayed finality, and provider outage are handled.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Known testnets can pass while a deep reorg after delayed finality duplicates settlement. The proposal can Pass while “Chain congestion, network fork, delayed finality, and provider outage are handled.” remains false.

## `USEQ-14B6379E` — Deterministic

**Exact statement**

> User balances reconcile across internal records and authoritative ledgers.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. For a sealed reconciliation window, user/account identities, internal balance totals, authoritative-ledger entries, units, and comparison rules form an exact numeric relation. This proves the complete bounded meaning of “User balances reconcile across internal records and authoritative ledgers.”.

### Exact deterministic check

1. User balances reconcile across internal records and authoritative ledgers.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Any missing account, mismatched unit, unreconciled delta, or wrong ledger reference deterministically fails. Within the sealed specialized-domain evidence, the rule cannot Pass while broken.

## `USEQ-F6401644` — Nondeterministic

**Exact statement**

> Token, asset, sanctions, tax, consumer-protection, and financial-regulatory applicability is reviewed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Token, asset, sanctions, tax, consumer-protection, and financial-regulatory applicability is reviewed.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Token, asset, sanctions, tax, consumer-protection, and financial-regulatory applicability is reviewed.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-FA3950BD` — Nondeterministic

**Exact statement**

> Recovery limitations and irreversible-risk warnings are clear and accurate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Recovery limitations and irreversible-risk warnings are clear and accurate.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Recovery limitations and irreversible-risk warnings are clear and accurate.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-427D419B` — Nondeterministic

**Exact statement**

> Devices, firmware, gateways, protocols, commands, sensors, actuators, and ownership states are inventoried.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every device/firmware/gateway/protocol/command/sensor/actuator/owner state is a dynamic physical/provider inventory. Therefore the proposed structured-record composite does not prove the whole rule “Devices, firmware, gateways, protocols, commands, sensors, actuators, and ownership states are inventoried.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Registered devices can pass while a resold unit or gateway firmware remains omitted. The proposal can Pass while “Devices, firmware, gateways, protocols, commands, sensors, actuators, and ownership states are inventoried.” remains false.

## `USEQ-D9C174A7` — Nondeterministic

**Exact statement**

> Device identity, enrollment, ownership transfer, reset, and decommissioning are secure.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Device identity, enrollment, ownership transfer, reset, and decommissioning are secure.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Device identity, enrollment, ownership transfer, reset, and decommissioning are secure.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0E3D9A51` — Nondeterministic

**Exact statement**

> Commands are authenticated, authorized, scoped, replay-protected, and auditable.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Command security/audit must hold across all devices, keys, protocols, replays, offline modes, gateways, and emergency paths. Therefore the proposed structured-record composite does not prove the whole rule “Commands are authenticated, authorized, scoped, replay-protected, and auditable.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Normal signed commands can pass while a stale key or legacy protocol accepts replay. The proposal can Pass while “Commands are authenticated, authorized, scoped, replay-protected, and auditable.” remains false.

## `USEQ-3A5CABA3` — Nondeterministic

**Exact statement**

> Unsafe or conflicting commands are rejected.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Unsafe/conflicting command meaning depends on physical state, timing, device capabilities, hazards, and command combinations. Therefore the proposed structured-record composite does not prove the whole rule “Unsafe or conflicting commands are rejected.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Rules can reject known conflicts while two individually safe actions combine dangerously. The proposal can Pass while “Unsafe or conflicting commands are rejected.” remains false.

## `USEQ-922D07FC` — Nondeterministic

**Exact statement**

> Offline, delayed, duplicated, reordered, and partially executed commands are handled safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Offline, delayed, duplicated, reordered, and partially executed commands are handled safely.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Offline, delayed, duplicated, reordered, and partially executed commands are handled safely.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-01750D1D` — Nondeterministic

**Exact statement**

> Device firmware and configuration have trusted update, rollback, and integrity verification.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Trusted update/rollback/integrity spans all firmware/config versions, keys, bootloaders, interruption points, devices, and supply paths. Therefore the proposed structured-record composite does not prove the whole rule “Device firmware and configuration have trusted update, rollback, and integrity verification.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Signature checks can pass while rollback protection or interrupted update bricks one device. The proposal can Pass while “Device firmware and configuration have trusted update, rollback, and integrity verification.” remains false.

## `USEQ-7C316720` — Nondeterministic

**Exact statement**

> Lost, stolen, compromised, or resold devices can be revoked and reset.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Revocation/reset across lost/stolen/compromised/resold devices needs every offline state, owner transfer, key, cache, and physical-access path. Therefore the proposed structured-record composite does not prove the whole rule “Lost, stolen, compromised, or resold devices can be revoked and reset.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Cloud revocation can pass while an offline device retains old data and commands. The proposal can Pass while “Lost, stolen, compromised, or resold devices can be revoked and reset.” remains false.

## `USEQ-72F94CB0` — Nondeterministic

**Exact statement**

> Local physical access and network exposure are threat-modeled.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Threat-model records cannot prove complete physical/network exposure, attacker capabilities, device variants, and mitigations. Therefore the proposed structured-record composite does not prove the whole rule “Local physical access and network exposure are threat-modeled.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

The model can omit debug-port access or local radio exposure. The proposal can Pass while “Local physical access and network exposure are threat-modeled.” remains false.

## `USEQ-A69FFAC6` — Nondeterministic

**Exact statement**

> Safety interlocks remain effective if the web application, network, cloud, or identity provider fails.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Safety interlocks must remain effective for every web/network/cloud/identity failure, combination, timing, and physical state. Therefore the proposed structured-record composite does not prove the whole rule “Safety interlocks remain effective if the web application, network, cloud, or identity provider fails.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Individual outages can pass while simultaneous cloud and sensor failure disables an interlock. The proposal can Pass while “Safety interlocks remain effective if the web application, network, cloud, or identity provider fails.” remains false.

## `USEQ-BC5663A0` — Nondeterministic

**Exact statement**

> Telemetry accuracy, spoofing, calibration, drift, and missing-data behavior are monitored.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring cannot prove complete telemetry/sensor scope, spoofing/drift/calibration truth, missing-data detection, and physical ground truth. Therefore the proposed structured-record composite does not prove the whole rule “Telemetry accuracy, spoofing, calibration, drift, and missing-data behavior are monitored.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Dashboards can be green while a biased uninstrumented sensor drifts. The proposal can Pass while “Telemetry accuracy, spoofing, calibration, drift, and missing-data behavior are monitored.” remains false.

## `USEQ-958BBF9F` — Nondeterministic

**Exact statement**

> Device and user privacy, location, audio, video, and household data are minimized and protected.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Device and user privacy, location, audio, video, and household data are minimized and protected.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Device and user privacy, location, audio, video, and household data are minimized and protected.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-2D51DCAA` — Nondeterministic

**Exact statement**

> Emergency disablement, field support, recall, and end-of-life procedures exist.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Procedure existence does not prove emergency disablement, field support, recall, and end-of-life capability, authority, or reach. Therefore the proposed structured-record composite does not prove the whole rule “Emergency disablement, field support, recall, and end-of-life procedures exist.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A recall plan can exist while deployed devices cannot receive disablement. The proposal can Pass while “Emergency disablement, field support, recall, and end-of-life procedures exist.” remains false.

## `USEQ-6497B434` — Nondeterministic

**Exact statement**

> Publish a clear license and ensure all included code and assets are legally compatible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Publish a clear license and ensure all included code and assets are legally compatible.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Publish a clear license and ensure all included code and assets are legally compatible.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D73106BF` — Nondeterministic

**Exact statement**

> Define governance, maintainer roles, decision process, code of conduct, and conflict resolution.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Governance/role/process/code/conflict documents do not prove legitimate authority, participation, fairness, or effective resolution. Therefore the proposed structured-record composite does not prove the whole rule “Define governance, maintainer roles, decision process, code of conduct, and conflict resolution.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Every section can exist while one maintainer controls all decisions. The proposal can Pass while “Define governance, maintainer roles, decision process, code of conduct, and conflict resolution.” remains false.

## `USEQ-89F5E97A` — Nondeterministic

**Exact statement**

> Provide contribution, development, testing, review, security, and release guidance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Guidance presence cannot prove accuracy, completeness, usability, security, or compatibility across contributors/platforms. Therefore the proposed structured-record composite does not prove the whole rule “Provide contribution, development, testing, review, security, and release guidance.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A complete guide can omit a release signing step or fail on supported setup. The proposal can Pass while “Provide contribution, development, testing, review, security, and release guidance.” remains false.

## `USEQ-6C1118DF` — Nondeterministic

**Exact statement**

> Require contributor provenance or attestation appropriate to project risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Require contributor provenance or attestation appropriate to project risk.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Require contributor provenance or attestation appropriate to project risk.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-94A60F9D` — Nondeterministic

**Exact statement**

> Protect maintainer accounts, repositories, packages, domains, registries, and signing keys.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect maintainer accounts, repositories, packages, domains, registries, and signing keys.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-23087C10` — Nondeterministic

**Exact statement**

> Use protected branches, independent review, and trusted release automation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Branch settings and automation identities are exact, but complete repository/release scope, review independence, and trust are not. Therefore the proposed structured-record composite does not prove the whole rule “Use protected branches, independent review, and trusted release automation.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Main can be protected while a maintenance branch or editable release action bypasses review. The proposal can Pass while “Use protected branches, independent review, and trusted release automation.” remains false.

## `USEQ-888978BC` — Nondeterministic

**Exact statement**

> Publish release artifacts, checksums, provenance, SBOM, compatibility, and change notes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Artifact/checksum/provenance publication is exact, but complete SBOM, compatibility truth, change-note completeness, and all release channels are not. Therefore the proposed structured-record composite does not prove the whole rule “Publish release artifacts, checksums, provenance, SBOM, compatibility, and change notes.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Signed artifacts can publish while the SBOM omits a component or notes miss a breaking change. The proposal can Pass while “Publish release artifacts, checksums, provenance, SBOM, compatibility, and change notes.” remains false.

## `USEQ-B88FA04F` — Nondeterministic

**Exact statement**

> Define supported versions, security maintenance, deprecation, and end-of-life.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Policy fields are exact, but support scope, security-maintenance commitments, deprecation, and EOL adequacy/authority require governance judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define supported versions, security maintenance, deprecation, and end-of-life.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

A table can define dates that are unrealistic or omit a maintained branch. The proposal can Pass while “Define supported versions, security maintenance, deprecation, and end-of-life.” remains false.

## `USEQ-BC106078` — Nondeterministic

**Exact statement**

> Provide a private vulnerability-reporting path and coordinated disclosure policy.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A private reporting channel/policy does not prove availability, staffing, safe-harbor meaning, coordination, or response effectiveness. Therefore the proposed structured-record composite does not prove the whole rule “Provide a private vulnerability-reporting path and coordinated disclosure policy.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

The mailbox can bounce or the disclosure policy be legally ineffective. The proposal can Pass while “Provide a private vulnerability-reporting path and coordinated disclosure policy.” remains false.

## `USEQ-DBBB57B9` — Nondeterministic

**Exact statement**

> Triage reports promptly and publish advisories with affected versions and mitigation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Triage reports promptly and publish advisories with affected versions and mitigation.” has checkable evidence clauses, but its full result also requires an explicit entropy, response-time, disablement-time, or triage-time threshold.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Triage reports promptly and publish advisories with affected versions and mitigation.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A1F8E702` — Nondeterministic

**Exact statement**

> Avoid exposing reporter identity or exploit detail before users can protect themselves.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Avoid exposing reporter identity or exploit detail before users can protect themselves.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-8BB2D4E1` — Nondeterministic

**Exact statement**

> Review dependencies, install scripts, generated content, examples, and package metadata.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Review occurrence does not prove complete dependency/script/generated/example/metadata scope or substantive security correctness. Therefore the proposed structured-record composite does not prove the whole rule “Review dependencies, install scripts, generated content, examples, and package metadata.”.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

Reviewers can approve while an install script fetches unreviewed code. The proposal can Pass while “Review dependencies, install scripts, generated content, examples, and package metadata.” remains false.

## `USEQ-01F8B782` — Nondeterministic

**Exact statement**

> Prevent package-name takeover, abandoned namespace, and unauthorized release.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing takeover/abandonment/unauthorized release spans registries, domains, credentials, maintainers, transfers, future attackers, and providers. Therefore the proposed structured-record composite does not prove the whole rule “Prevent package-name takeover, abandoned namespace, and unauthorized release.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Repo controls can pass while a package namespace or old domain remains claimable. The proposal can Pass while “Prevent package-name takeover, abandoned namespace, and unauthorized release.” remains false.

## `USEQ-795582EF` — Nondeterministic

**Exact statement**

> Track downstream consumers and ecosystem impact for breaking or security changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Track downstream consumers and ecosystem impact for breaking or security changes.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Track downstream consumers and ecosystem impact for breaking or security changes.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-C714CD1A` — Nondeterministic

**Exact statement**

> Use semantic or otherwise documented versioning consistently.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Version-format checks cannot prove semantic compatibility classification or consistency across every artifact/interface/channel. Therefore the proposed structured-record composite does not prove the whole rule “Use semantic or otherwise documented versioning consistently.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A breaking change can receive a patch version while strings parse correctly. The proposal can Pass while “Use semantic or otherwise documented versioning consistently.” remains false.

## `USEQ-93F18228` — Nondeterministic

**Exact statement**

> Provide reproducible setup and tests for contributors.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Setup/test reproducibility samples contributor platforms, networks, identities, time, toolchains, and mutable dependencies. Therefore the proposed structured-record composite does not prove the whole rule “Provide reproducible setup and tests for contributors.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

CI can pass while a supported laptop or mirror produces different results. The proposal can Pass while “Provide reproducible setup and tests for contributors.” remains false.

## `USEQ-266D92FB` — Nondeterministic

**Exact statement**

> Make public documentation accessible and free of secrets or private data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Make public documentation accessible and free of secrets or private data.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Make public documentation accessible and free of secrets or private data.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-F4CC9891` — Nondeterministic

**Exact statement**

> Plan maintainer succession and avoid single-person release authority.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Succession documents and signer counts do not prove trust, availability, authority diversity, or workable emergency release. Therefore the proposed structured-record composite does not prove the whole rule “Plan maintainer succession and avoid single-person release authority.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

Two maintainers can be inactive or colluding, and the plan can be unusable. The proposal can Pass while “Plan maintainer succession and avoid single-person release authority.” remains false.

## `USEQ-8A380A72` — Nondeterministic

**Exact statement**

> Disclose commercial sponsorship, conflicts of interest, and governance changes.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Disclosure completeness and conflict/sponsorship/governance-change meaning require legal/governance authority across all funding and relationships. Therefore the proposed structured-record composite does not prove the whole rule “Disclose commercial sponsorship, conflicts of interest, and governance changes.”.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

### Skeptical rejection counterexample

A disclosure page can omit indirect commercial influence. The proposal can Pass while “Disclose commercial sponsorship, conflicts of interest, and governance changes.” remains false.

## `USEQ-ADC4E444` — Nondeterministic

**Exact statement**

> Archive or transfer abandoned projects responsibly rather than leaving vulnerable packages silently active.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Archive or transfer abandoned projects responsibly rather than leaving vulnerable packages silently active.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Archive or transfer abandoned projects responsibly rather than leaving vulnerable packages silently active.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FBF0A7B0` — Nondeterministic

**Exact statement**

> Define supported operating systems, versions, device classes, architectures, input modes, and assistive technologies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Defined support matrices cannot prove complete device/input/assistive-tech scope, real compatibility, or appropriate minimums. Therefore the proposed structured-record composite does not prove the whole rule “Define supported operating systems, versions, device classes, architectures, input modes, and assistive technologies.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A listed screen reader or OS can fail despite appearing supported. The proposal can Pass while “Define supported operating systems, versions, device classes, architectures, input modes, and assistive technologies.” remains false.

## `USEQ-C56B91A7` — Nondeterministic

**Exact statement**

> Sign distributable applications and protect signing and store accounts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Application signatures are exact, but signing/store-account protection spans every key copy, role, recovery path, provider admin, and release channel. Therefore the proposed structured-record composite does not prove the whole rule “Sign distributable applications and protect signing and store accounts.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `human_accountability`

### Skeptical rejection counterexample

Apps can be signed while a shared store credential or backup key remains exposed. The proposal can Pass while “Sign distributable applications and protect signing and store accounts.” remains false.

## `USEQ-B7D00E9D` — Nondeterministic

**Exact statement**

> Use trusted distribution channels and verify update authenticity and integrity.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Trusted-channel and update authenticity/integrity must cover every distributor, mirror, updater, key rotation, rollback, and failure path. Therefore the proposed structured-record composite does not prove the whole rule “Use trusted distribution channels and verify update authenticity and integrity.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Main-store updates can verify while a side channel accepts unsigned packages. The proposal can Pass while “Use trusted distribution channels and verify update authenticity and integrity.” remains false.

## `USEQ-222045D3` — Nondeterministic

**Exact statement**

> Make updates compatible, resumable, rollback-aware, and safe under interruption.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Update compatibility/resume/rollback/interruption safety spans every supported version, state, device, storage, power, network, and partial step. Therefore the proposed structured-record composite does not prove the whole rule “Make updates compatible, resumable, rollback-aware, and safe under interruption.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Common upgrades can pass while interruption after schema change makes rollback unsafe. The proposal can Pass while “Make updates compatible, resumable, rollback-aware, and safe under interruption.” remains false.

## `USEQ-328B6D42` — Nondeterministic

**Exact statement**

> Define minimum and recommended versions and communicate end-of-support.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Version fields are exact, but recommended/minimum suitability, support commitments, audience reach, and timing require product judgment. Therefore the proposed structured-record composite does not prove the whole rule “Define minimum and recommended versions and communicate end-of-support.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

### Skeptical rejection counterexample

Docs can communicate a minimum that cannot securely run or fail to reach installed clients. The proposal can Pass while “Define minimum and recommended versions and communicate end-of-support.” remains false.

## `USEQ-C7371C71` — Nondeterministic

**Exact statement**

> Request device permissions only at the moment of need with clear purpose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Request device permissions only at the moment of need with clear purpose.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Request device permissions only at the moment of need with clear purpose.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-ADE1D9DE` — Nondeterministic

**Exact statement**

> Provide functional alternatives when optional permissions are denied.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Functional equivalence and permission optionality require complete features, platforms, journeys, and user/accessibility judgment. Therefore the proposed structured-record composite does not prove the whole rule “Provide functional alternatives when optional permissions are denied.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

A denied-location alternative can exist but omit the critical function. The proposal can Pass while “Provide functional alternatives when optional permissions are denied.” remains false.

## `USEQ-01CFD666` — Nondeterministic

**Exact statement**

> Protect local databases, files, caches, logs, notifications, clipboard, backups, and screenshots according to sensitivity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect local databases, files, caches, logs, notifications, clipboard, backups, and screenshots according to sensitivity.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect local databases, files, caches, logs, notifications, clipboard, backups, and screenshots according to sensitivity.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-8955879F` — Nondeterministic

**Exact statement**

> Clear or protect user data during logout, account switch, uninstall, device transfer, and remote revocation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clear/protect behavior must cover every data copy, cache, key, backup, account/device transition, provider, and retention duty. Therefore the proposed structured-record composite does not prove the whole rule “Clear or protect user data during logout, account switch, uninstall, device transfer, and remote revocation.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`

### Skeptical rejection counterexample

Logout can clear UI data while backups, shared storage, or another account expose it. The proposal can Pass while “Clear or protect user data during logout, account switch, uninstall, device transfer, and remote revocation.” remains false.

## `USEQ-B9555C7E` — Nondeterministic

**Exact statement**

> Handle offline changes, conflict, replay, and synchronization safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Handle offline changes, conflict, replay, and synchronization safely.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Handle offline changes, conflict, replay, and synchronization safely.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0EC9D8F4` — Nondeterministic

**Exact statement**

> Prevent stale clients from bypassing current authorization, safety, or data rules.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing stale-client bypass spans every client version, cached policy, token, offline state, protocol, and server path. Therefore the proposed structured-record composite does not prove the whole rule “Prevent stale clients from bypassing current authorization, safety, or data rules.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known stale clients can reject while a legacy endpoint accepts old authorization semantics. The proposal can Pass while “Prevent stale clients from bypassing current authorization, safety, or data rules.” remains false.

## `USEQ-AA7C2DC9` — Nondeterministic

**Exact statement**

> Validate deep links, inter-application messages, extensions, custom schemes, and shared files.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Validation must cover all link/message/extension/scheme/file formats, parsers, origins, permissions, states, and attacker inputs. Therefore the proposed structured-record composite does not prove the whole rule “Validate deep links, inter-application messages, extensions, custom schemes, and shared files.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Fixture links can pass while a crafted shared file or custom scheme bypasses checks. The proposal can Pass while “Validate deep links, inter-application messages, extensions, custom schemes, and shared files.” remains false.

## `USEQ-51B9E38A` — Nondeterministic

**Exact statement**

> Use platform security storage for credentials and keys where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use platform security storage for credentials and keys where appropriate.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use platform security storage for credentials and keys where appropriate.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-B5BDC412` — Nondeterministic

**Exact statement**

> Avoid embedding long-lived secrets in distributed binaries.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Secret absence from distributed binaries is universal across code, resources, obfuscation, generated assets, dependencies, histories, and platforms. Therefore the proposed structured-record composite does not prove the whole rule “Avoid embedding long-lived secrets in distributed binaries.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Scanners can pass while a long-lived credential is encoded or derived at runtime. The proposal can Pass while “Avoid embedding long-lived secrets in distributed binaries.” remains false.

## `USEQ-0EA47DFB` — Nondeterministic

**Exact statement**

> Detect rooted, jailbroken, tampered, or compromised environments only where signals are reliable and user impact is justified.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Detect rooted, jailbroken, tampered, or compromised environments only where signals are reliable and user impact is justified.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0E6064FC` — Nondeterministic

**Exact statement**

> Test battery, memory, storage, network, background, interruption, suspension, and resume behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Device/resource lifecycle tests span hardware, OS versions, capacities, networks, schedules, interruptions, suspension/resume combinations, and duration. Therefore the proposed structured-record composite does not prove the whole rule “Test battery, memory, storage, network, background, interruption, suspension, and resume behavior.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Each condition can pass alone while low storage plus background suspension corrupts state. The proposal can Pass while “Test battery, memory, storage, network, background, interruption, suspension, and resume behavior.” remains false.

## `USEQ-5053A39C` — Nondeterministic

**Exact statement**

> Provide accessible keyboard, screen-reader, scaling, contrast, and reduced-motion behavior.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Accessibility presence/tests cannot prove correct keyboard, screen-reader, scaling, contrast, and motion behavior across all views/platforms/users. Therefore the proposed structured-record composite does not prove the whole rule “Provide accessible keyboard, screen-reader, scaling, contrast, and reduced-motion behavior.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Main screens can pass while a modal traps focus or dynamic content is unannounced. The proposal can Pass while “Provide accessible keyboard, screen-reader, scaling, contrast, and reduced-motion behavior.” remains false.

## `USEQ-4C38FC1C` — Nondeterministic

**Exact statement**

> Respect enterprise management and privacy boundaries where supported.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Respect enterprise management and privacy boundaries where supported.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Respect enterprise management and privacy boundaries where supported.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-389EAD2A` — Nondeterministic

**Exact statement**

> Make crash and usage telemetry consented, minimized, redacted, and diagnosable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Make crash and usage telemetry consented, minimized, redacted, and diagnosable.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Make crash and usage telemetry consented, minimized, redacted, and diagnosable.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-F43D141E` — Nondeterministic

**Exact statement**

> Test clean install, upgrade from supported versions, migration, repair, uninstall, and reinstallation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Install/upgrade/migrate/repair/uninstall/reinstall behavior spans every supported version, device state, interruption, data shape, and platform. Therefore the proposed structured-record composite does not prove the whole rule “Test clean install, upgrade from supported versions, migration, repair, uninstall, and reinstallation.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Clean paths can pass while repair after partial upgrade loses data. The proposal can Pass while “Test clean install, upgrade from supported versions, migration, repair, uninstall, and reinstallation.” remains false.

## `USEQ-B7FB0BC0` — Nondeterministic

**Exact statement**

> Document the shared-responsibility boundary for every managed service and control.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Responsibility documentation cannot prove complete managed-service/control scope, correct ownership, customer comprehension, or contractual truth. Therefore the proposed structured-record composite does not prove the whole rule “Document the shared-responsibility boundary for every managed service and control.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Every section can exist while a provider/customer responsibility gap remains. The proposal can Pass while “Document the shared-responsibility boundary for every managed service and control.” remains false.

## `USEQ-40B56A39` — Nondeterministic

**Exact statement**

> Separate production, nonproduction, security, logging, and shared-platform accounts or projects according to risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Separate production, nonproduction, security, logging, and shared-platform accounts or projects according to risk.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Separate production, nonproduction, security, logging, and shared-platform accounts or projects according to risk.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-45C59E66` — Nondeterministic

**Exact statement**

> Use workload and human identities with least privilege and strong authentication.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use workload and human identities with least privilege and strong authentication.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use workload and human identities with least privilege and strong authentication.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-0A87718E` — Nondeterministic

**Exact statement**

> Inventory regions, zones, endpoints, public exposure, service policies, keys, and quotas.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete region/zone/endpoint/exposure/policy/key/quota inventory spans dynamic provider-created and implicit resources. Therefore the proposed structured-record composite does not prove the whole rule “Inventory regions, zones, endpoints, public exposure, service policies, keys, and quotas.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Declared regions can pass while a public endpoint or inherited key is omitted. The proposal can Pass while “Inventory regions, zones, endpoints, public exposure, service policies, keys, and quotas.” remains false.

## `USEQ-242E52E4` — Nondeterministic

**Exact statement**

> Encrypt and control data according to sensitivity and residency requirements.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Encrypt and control data according to sensitivity and residency requirements.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Encrypt and control data according to sensitivity and residency requirements.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-4842963A` — Nondeterministic

**Exact statement**

> Protect provider control planes, organization roots, billing, DNS, and identity administration.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect provider control planes, organization roots, billing, DNS, and identity administration.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-542A6C62` — Nondeterministic

**Exact statement**

> Use policy and automation to prevent prohibited configurations.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing prohibited configurations across every provider API, console, import, default, race, and emergency path is an adversarial claim. Therefore the proposed structured-record composite does not prove the whole rule “Use policy and automation to prevent prohibited configurations.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Policy tests can pass while a direct provider action or new resource type bypasses them. The proposal can Pass while “Use policy and automation to prevent prohibited configurations.” remains false.

## `USEQ-8155C7A6` — Nondeterministic

**Exact statement**

> Monitor control-plane, quota, billing, identity, network, region, and managed-service failures.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring cannot prove complete control-plane/quota/billing/identity/network/region/service scope, event sources, freshness, or detection. Therefore the proposed structured-record composite does not prove the whole rule “Monitor control-plane, quota, billing, identity, network, region, and managed-service failures.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known metrics can be healthy while an uninstrumented managed-service failure occurs. The proposal can Pass while “Monitor control-plane, quota, billing, identity, network, region, and managed-service failures.” remains false.

## `USEQ-0DCA89D1` — Nondeterministic

**Exact statement**

> Design resilience across actual failure domains rather than nominal instance count.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Design resilience across actual failure domains rather than nominal instance count.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Design resilience across actual failure domains rather than nominal instance count.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B68F5F66` — Nondeterministic

**Exact statement**

> Test provider-service outage, throttling, regional loss, identity failure, and quota exhaustion.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Provider failure tests sample services, regions, durations, rates, identities, quotas, compound failures, and changing provider behavior. Therefore the proposed structured-record composite does not prove the whole rule “Test provider-service outage, throttling, regional loss, identity failure, and quota exhaustion.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

Named fixtures can pass while identity failure plus regional throttling breaks recovery. The proposal can Pass while “Test provider-service outage, throttling, regional loss, identity failure, and quota exhaustion.” remains false.

## `USEQ-28D7D9E6` — Nondeterministic

**Exact statement**

> Maintain backups and recovery that are isolated from ordinary account compromise.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Backup/recovery isolation must withstand every ordinary-account compromise path, provider root, key, network, deletion, and restore dependency. Therefore the proposed structured-record composite does not prove the whole rule “Maintain backups and recovery that are isolated from ordinary account compromise.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Separate backups can pass while the same compromised identity can delete keys or recovery configuration. The proposal can Pass while “Maintain backups and recovery that are isolated from ordinary account compromise.” remains false.

## `USEQ-FE43BE53` — Nondeterministic

**Exact statement**

> Avoid provider defaults that create public access, weak retention, or broad identity trust.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identifying unsafe provider defaults requires complete provider/resource/version scope and security/retention/identity judgment. Therefore the proposed structured-record composite does not prove the whole rule “Avoid provider defaults that create public access, weak retention, or broad identity trust.”.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

### Skeptical rejection counterexample

Known defaults can be overridden while a new managed service creates public or broad trust. The proposal can Pass while “Avoid provider defaults that create public access, weak retention, or broad identity trust.” remains false.

## `USEQ-7ACBEBF3` — Nondeterministic

**Exact statement**

> Version infrastructure and detect drift.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Versioning/drift detection needs complete infrastructure/provider state and authoritative desired/actual models for implicit and manual resources. Therefore the proposed structured-record composite does not prove the whole rule “Version infrastructure and detect drift.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Managed manifests can match while a console-only policy or provider default drifts. The proposal can Pass while “Version infrastructure and detect drift.” remains false.

## `USEQ-43BE2B24` — Nondeterministic

**Exact statement**

> Review managed-service upgrade, deprecation, and maintenance behavior.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Review occurrence does not prove complete managed-service changes, upgrade impact, deprecation timing, maintenance risk, or decisions. Therefore the proposed structured-record composite does not prove the whole rule “Review managed-service upgrade, deprecation, and maintenance behavior.”.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A review can overlook a forced upgrade or compatibility break. The proposal can Pass while “Review managed-service upgrade, deprecation, and maintenance behavior.” remains false.

## `USEQ-A842153A` — Nondeterministic

**Exact statement**

> Model data egress, inter-region transfer, support access, and supplier subprocessor implications.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Model data egress, inter-region transfer, support access, and supplier subprocessor implications.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C2368A23` — Nondeterministic

**Exact statement**

> Maintain provider support and escalation appropriate to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Maintain provider support and escalation appropriate to impact.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Maintain provider support and escalation appropriate to impact.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-FFE8903C` — Nondeterministic

**Exact statement**

> Forecast cost and prevent runaway resource creation without disabling critical service.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Forecast cost and prevent runaway resource creation without disabling critical service.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Forecast cost and prevent runaway resource creation without disabling critical service.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-29CD22E1` — Nondeterministic

**Exact statement**

> Define exit, export, migration, key, DNS, and continuity plans for critical services.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Define exit, export, migration, key, DNS, and continuity plans for critical services.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Define exit, export, migration, key, DNS, and continuity plans for critical services.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A4D95CE3` — Nondeterministic

**Exact statement**

> Avoid claiming multi-cloud portability that has not been exercised.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Portability claims require representative exercised workloads/providers/failures and semantic equivalence; one migration does not prove multi-cloud portability. Therefore the proposed structured-record composite does not prove the whole rule “Avoid claiming multi-cloud portability that has not been exercised.”.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`

### Skeptical rejection counterexample

A demo can run on two clouds while data, identity, or operational features remain locked in. The proposal can Pass while “Avoid claiming multi-cloud portability that has not been exercised.” remains false.

## `USEQ-C0562936` — Nondeterministic

**Exact statement**

> Include provider incidents and control changes in internal risk and postmortem processes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete provider incident/control-change capture and correct risk/postmortem linkage require external feeds and causal judgment. Therefore the proposed structured-record composite does not prove the whole rule “Include provider incidents and control changes in internal risk and postmortem processes.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

### Skeptical rejection counterexample

An unreported provider control change can affect risk without entering records. The proposal can Pass while “Include provider incidents and control changes in internal risk and postmortem processes.” remains false.

## `USEQ-7D4DA192` — Nondeterministic

**Exact statement**

> Use minimal trusted base images pinned by immutable identity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use minimal trusted base images pinned by immutable identity.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use minimal trusted base images pinned by immutable identity.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A2E62F38` — Nondeterministic

**Exact statement**

> Build images from reviewed source with provenance and SBOM.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Image/source digest/provenance are exact, but reviewed-source authority and complete SBOM/component scope are not. Therefore the proposed structured-record composite does not prove the whole rule “Build images from reviewed source with provenance and SBOM.”.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `human_accountability`, `unbounded_scope`

### Skeptical rejection counterexample

A signed provenance record can bind reviewed source while the final image adds an undeclared binary. The proposal can Pass while “Build images from reviewed source with provenance and SBOM.” remains false.

## `USEQ-726D8BC7` — Nondeterministic

**Exact statement**

> Scan final images and monitor them after release.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Final-image scans and monitoring cannot prove complete vulnerability/malware/config/runtime scope or future intelligence/detection. Therefore the proposed structured-record composite does not prove the whole rule “Scan final images and monitor them after release.”.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Scans can pass while a hidden component or newly disclosed runtime flaw remains. The proposal can Pass while “Scan final images and monitor them after release.” remains false.

## `USEQ-A3559C64` — Nondeterministic

**Exact statement**

> Run workloads as non-root and without unnecessary privilege, capabilities, host access, or writable filesystem.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Run workloads as non-root and without unnecessary privilege, capabilities, host access, or writable filesystem.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Run workloads as non-root and without unnecessary privilege, capabilities, host access, or writable filesystem.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-13CBD6C6` — Deterministic

**Exact statement**

> Define resource requests, limits, priorities, disruption, and eviction behavior.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A workload policy can define the five exact resource request, limit, priority, disruption and eviction behavior fields.

### Exact deterministic check

1. Define resource requests, limits, priorities, disruption, and eviction behavior.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A workload class missing an eviction rule or resource limit fails the finite policy schema.

## `USEQ-B1114E9E` — Nondeterministic

**Exact statement**

> Use workload identities rather than shared node or cluster credentials.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Workload-identity records cannot prove shared node or cluster credentials are absent from every workload, bootstrap and emergency path.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known workloads can use unique identities while a legacy job inherits the node credential.

## `USEQ-43AEBF8E` — Nondeterministic

**Exact statement**

> Keep secrets out of images and ordinary environment output.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of secrets from all image layers, build outputs, process environments and ordinary diagnostic output is an open-world negative claim.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured scanners can pass while an encoded key remains in an image layer or exception dump.

## `USEQ-84D35810` — Nondeterministic

**Exact statement**

> Restrict network communication to declared flows.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Restricting all communication to declared flows requires a complete flow inventory and enforcement across every protocol, sidecar, host and control-plane path.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Network policies can match declared flows while DNS tunneling or a host-network workload reaches an undeclared destination.

## `USEQ-071F5865` — Nondeterministic

**Exact statement**

> Protect admission, policy, registry, control-plane, and cluster administration.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect admission, policy, registry, control-plane, and cluster administration.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-D72B3DB5` — Nondeterministic

**Exact statement**

> Verify image signatures or attestations before admission where risk warrants.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Verify image signatures or attestations before admission where risk warrants.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Verify image signatures or attestations before admission where risk warrants.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-2C2AF8DA` — Nondeterministic

**Exact statement**

> Separate tenants and trust levels across namespaces, nodes, clusters, or accounts according to isolation needs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Separate tenants and trust levels across namespaces, nodes, clusters, or accounts according to isolation needs.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Separate tenants and trust levels across namespaces, nodes, clusters, or accounts according to isolation needs.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-901CB8A9` — Deterministic

**Exact statement**

> Define readiness, liveness, startup, graceful termination, and drain behavior.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A workload lifecycle policy can define five exact readiness, liveness, startup, graceful-termination and drain behaviors.

### Exact deterministic check

1. Define readiness, liveness, startup, graceful termination, and drain behavior.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting drain or startup behavior fails the finite schema; runtime correctness is not inferred.

## `USEQ-5154706D` — Nondeterministic

**Exact statement**

> Test rescheduling, rolling update, node loss, zone loss, storage failure, and control-plane degradation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Six test executions do not prove complete versions, workloads, topology combinations, data states and recovery oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Each named failure can pass alone while node loss during rolling update corrupts storage.

## `USEQ-CD9A34E1` — Nondeterministic

**Exact statement**

> Protect persistent volumes, snapshots, backups, and restoration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect persistent volumes, snapshots, backups, and restoration.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect persistent volumes, snapshots, backups, and restoration.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7E6911D8` — Nondeterministic

**Exact statement**

> Keep cluster, node, runtime, image, policy, and add-on versions supported and patched.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Support and patch status across six component classes requires complete cluster inventory plus changing vendor and vulnerability authority.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known components can be current while an unmanaged add-on or node image is unsupported.

## `USEQ-4E9A0CB6` — Nondeterministic

**Exact statement**

> Prevent untrusted workloads from reaching metadata, host sockets, control APIs, or other tenants.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Preventing untrusted workloads from reaching four sensitive targets is a security property over arbitrary workloads and escape paths.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known attack tests can fail closed while a host socket mount or metadata proxy exposes another route.

## `USEQ-41A84AD1` — Nondeterministic

**Exact statement**

> Audit privileged operations, exec, port-forward, secret access, and policy changes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Auditing five privileged action families requires complete API, runtime, node and control-plane path coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Registered operations can log while an alternate exec or secret-read interface leaves no audit event.

## `USEQ-8EAA66DC` — Nondeterministic

**Exact statement**

> Bound autoscaling and prevent scale storms against dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Autoscaling bounds are inspectable, but preventing scale storms against every dependency is an open workload and runtime interaction claim.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`, `unbounded_scope`

### Skeptical rejection counterexample

A maximum replica count can pass while retries cause many functions or clusters to overwhelm one downstream service.

## `USEQ-884ADA51` — Nondeterministic

**Exact statement**

> Detect configuration drift and unmanaged workloads.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detection of all configuration drift and unmanaged workloads depends on a complete desired-state and runtime discovery boundary.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The scanner can report no drift while a shadow namespace or out-of-band cluster is never observed.

## `USEQ-472604C8` — Nondeterministic

**Exact statement**

> Practice cluster rebuild and workload recovery from trusted source.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A rebuild practice record does not prove recovery is complete or that every source, image and dependency is actually trusted.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `undefined_protocol`

### Skeptical rejection counterexample

A cluster can rebuild in staging while using a compromised registry mirror or omitting production state.

## `USEQ-2BE63A79` — Nondeterministic

**Exact statement**

> Inventory every function, trigger, event source, destination, identity, secret, region, and failure target.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** An inventory cannot prove inclusion of every serverless function, trigger, source, destination, identity, secret, region and failure target.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All rows can be complete while an out-of-band trigger or dynamically created function remains unknown.

## `USEQ-1A3AAD5A` — Nondeterministic

**Exact statement**

> Use a separate least-privilege identity per workload or cohesive group.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use a separate least-privilege identity per workload or cohesive group.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use a separate least-privilege identity per workload or cohesive group.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-7FAE65A8` — Deterministic

**Exact statement**

> Assume event retries and duplicate delivery unless guarantees prove otherwise.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An event-source policy can require retry-and-duplicate semantics by default and permit an exception only with a bound authoritative delivery guarantee.

### Exact deterministic check

1. Assume event retries and duplicate delivery unless guarantees prove otherwise.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A source marked exactly-once without a valid guarantee reference fails the finite default-and-exception rule.

## `USEQ-21DF7AD2` — Nondeterministic

**Exact statement**

> Make handlers idempotent and safe under concurrent execution.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Handler idempotence and concurrent safety are behavioral properties over every state, side effect, timing and retry interleaving.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known duplicate tests can pass while a timeout after commit creates a second effect on retry.

## `USEQ-AAC6692F` — Deterministic

**Exact statement**

> Define timeout, memory, payload, temporary storage, connection, and concurrency limits.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A serverless policy can define the six exact timeout, memory, payload, temporary-storage, connection and concurrency limits.

### Exact deterministic check

1. Define timeout, memory, payload, temporary storage, connection, and concurrency limits.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A function class missing a connection or payload limit fails the finite configuration schema.

## `USEQ-4919EBF1` — Nondeterministic

**Exact statement**

> Protect downstream systems from burst scaling and retry amplification.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Protecting every downstream dependency from burst scaling and retry amplification requires complete dependency inventory and unbounded workload behavior.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Load tests can pass while an event storm triggers retries that exhaust an indirect provider quota.

## `USEQ-2916316A` — Deterministic

**Exact statement**

> Configure dead-letter, failure destination, replay, and reconciliation.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Configure is an effective environment-state promise. A design record does not prove the deployed workflow configuration.

### Exact deterministic check

1. For every event workflow in the complete sealed workflow inventory, effective environment configuration contains enabled dead-letter handling, failure destination, replay, and reconciliation mechanisms bound to that workflow.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A workflow record can name all four mechanisms while replay or reconciliation is disabled in production. Effective configuration evidence fails.

## `USEQ-C6F8CEBE` — Nondeterministic

**Exact statement**

> Keep deployment packages minimal, signed or attested, and dependency-scanned.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Keep deployment packages minimal, signed or attested, and dependency-scanned.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Keep deployment packages minimal, signed or attested, and dependency-scanned.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-28FDB25A` — Nondeterministic

**Exact statement**

> Do not place secrets in code, packages, logs, or event payloads.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Do not place secrets in code, packages, logs, or event payloads.” is nondeterministic because it makes an absolute no-leak, no-duplicate, or no-secret claim that bounded evidence cannot prove for every future execution and data path.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-6843A15C` — Nondeterministic

**Exact statement**

> Control public invocation, cross-account triggers, and event source permissions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Controlling public invocation, cross-account triggers and event permissions requires complete functions, accounts, identities and trigger-path inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known policies can pass while an external account retains invocation permission through an inherited resource rule.

## `USEQ-4B7E5463` — Nondeterministic

**Exact statement**

> Measure cold-start and scale behavior against user objectives.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Cold-start and scaling measurements depend on workload, region, platform state and user-objective interpretation not defined by the rule.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A small warm-biased test can meet the objective while real cold bursts violate it for users.

## `USEQ-B43F9D53` — Nondeterministic

**Exact statement**

> Reuse connections safely without assuming process lifetime.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Reuse connections safely without assuming process lifetime.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Reuse connections safely without assuming process lifetime.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C47082A7` — Nondeterministic

**Exact statement**

> Handle process reuse so one invocation cannot leak state to another user or tenant.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Guaranteeing process reuse never leaks state between any users or tenants is a security property over arbitrary invocation sequences and runtimes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Pairwise tests can pass while a rare warm-instance error exposes a prior tenant's cached data.

## `USEQ-AFE31124` — Nondeterministic

**Exact statement**

> Propagate trace, identity, tenant, deadline, and correlation context.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Propagation of five context fields across every synchronous, asynchronous, retry and provider boundary requires complete path coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Standard invocations can pass while a dead-letter replay loses tenant or deadline context.

## `USEQ-A5E51F84` — Nondeterministic

**Exact statement**

> Monitor throttling, duration, errors, retries, age, concurrency, cost, and downstream saturation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Eight configured monitors do not prove complete function, region, downstream and failure-path telemetry or correct detection.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Dashboards can include every metric type while an unregistered function or dependency saturation remains invisible.

## `USEQ-C27ADF5F` — Nondeterministic

**Exact statement**

> Test provider outage, delayed events, partial region failure, and control-plane failure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Four fault tests do not establish complete providers, regions, event states, combinations, timing and continuity oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Single-fault drills can pass while delayed events during partial region failure are lost.

## `USEQ-E28C525F` — Deterministic

**Exact statement**

> Define versioning, aliases, traffic shift, rollback, and event compatibility.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A serverless release policy can define five exact versioning, alias, traffic-shift, rollback and event-compatibility behaviors.

### Exact deterministic check

1. Define versioning, aliases, traffic shift, rollback, and event compatibility.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A workflow without rollback or compatibility definition fails the finite policy schema.

## `USEQ-2BFBFB1A` — Nondeterministic

**Exact statement**

> Avoid unbounded invocation chains and recursive triggers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Absence of unbounded invocation chains and recursion is a universal runtime and graph claim with dynamic triggers and data-dependent paths.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Static analysis and capped tests can pass while an indirect cross-service trigger loop grows without bound.

## `USEQ-BACD0649` — Deterministic

**Exact statement**

> Forecast cost amplification from abuse, loops, and large events.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A cost-forecast model can require separate abuse, recursive-loop and large-event amplification scenarios with versioned inputs and calculation outputs.

### Exact deterministic check

1. Forecast cost amplification from abuse, loops, and large events.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting one scenario or using an unbound calculation input fails the finite forecast manifest; accuracy is not inferred.

## `USEQ-6D182068` — Nondeterministic

**Exact statement**

> Maintain a migration and continuity plan for critical provider-specific execution.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Maintain a migration and continuity plan for critical provider-specific execution.” has checkable evidence clauses, but its full result also requires a fixed selector for which risks, data, platforms, permissions, services, controls, releases, or scenarios are applicable.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Maintain a migration and continuity plan for critical provider-specific execution.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-2E4F8C0C` — Nondeterministic

**Exact statement**

> Determine every applicable healthcare, medical-device, research, records, privacy, security, and professional obligation.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Determine every applicable healthcare, medical-device, research, records, privacy, security, and professional obligation.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-7EEBE134` — Nondeterministic

**Exact statement**

> Classify clinical, patient, subject, caregiver, provider, identity, insurance, and research data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Correct classification of eight healthcare data classes requires regulated-domain, privacy and contextual interpretation plus complete data discovery.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

Every recorded field can have a label while a derived research value is misclassified or an unknown copy remains unclassified.

## `USEQ-EAE30A78` — Nondeterministic

**Exact statement**

> Apply minimum-necessary access and purpose-based disclosure.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Apply minimum-necessary access and purpose-based disclosure.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-CC64FD64` — Nondeterministic

**Exact statement**

> Verify patient, professional, organization, device, and service identity proportionately.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Verify patient, professional, organization, device, and service identity proportionately.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Verify patient, professional, organization, device, and service identity proportionately.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-5136CD3A` — Nondeterministic

**Exact statement**

> Protect emergency access while logging, limiting, and reviewing its use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Emergency-access protection across all paths plus adequate limits and review requires security, clinical and human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

Logging and approvals can exist while an emergency role is too broad or reviews fail to recognize misuse.

## `USEQ-053C255E` — Nondeterministic

**Exact statement**

> Maintain complete tamper-evident audit of access, correction, disclosure, order, result, and administrative action where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Maintain complete tamper-evident audit of access, correction, disclosure, order, result, and administrative action where required.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Maintain complete tamper-evident audit of access, correction, disclosure, order, result, and administrative action where required.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-D2E3E2DF` — Deterministic

**Exact statement**

> Preserve data provenance, units, reference ranges, time, author, status, correction, and version.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A regulated data schema and append-only history can require preservation of eight exact provenance, unit, range, time, author, status, correction and version fields.

### Exact deterministic check

1. Preserve data provenance, units, reference ranges, time, author, status, correction, and version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A record or transformation missing units, correction lineage or version fails the finite schema and provenance-chain checks.

## `USEQ-8A2B11D6` — Nondeterministic

**Exact statement**

> Prevent stale, incomplete, duplicate, misattributed, or unit-confused information from appearing authoritative.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent stale, incomplete, duplicate, misattributed, or unit-confused information from appearing authoritative.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Prevent stale, incomplete, duplicate, misattributed, or unit-confused information from appearing authoritative.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5C75B50F` — Nondeterministic

**Exact statement**

> Use controlled vocabularies and interoperability standards where applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use controlled vocabularies and interoperability standards where applicable.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use controlled vocabularies and interoperability standards where applicable.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-2855968A` — Nondeterministic

**Exact statement**

> Validate clinical rules, calculations, alerts, and decision support with qualified domain experts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Validate clinical rules, calculations, alerts, and decision support with qualified domain experts.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Validate clinical rules, calculations, alerts, and decision support with qualified domain experts.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5F39178C` — Nondeterministic

**Exact statement**

> Prevent alert fatigue and unsafe suppression.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent alert fatigue and unsafe suppression.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-781C2D61` — Nondeterministic

**Exact statement**

> Separate advisory output from authoritative diagnosis or order when appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Separate advisory output from authoritative diagnosis or order when appropriate.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Separate advisory output from authoritative diagnosis or order when appropriate.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-7ABEA012` — Deterministic

**Exact statement**

> Provide human review, correction, escalation, and downtime procedures.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A healthcare operations catalog can require four exact human-review, correction, escalation and downtime procedures.

### Exact deterministic check

1. Provide human review, correction, escalation, and downtime procedures.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting correction or downtime handling fails the finite procedure schema; clinical quality is not inferred.

## `USEQ-AC848C87` — Nondeterministic

**Exact statement**

> Test continuity during network, provider, device, identity, and regional failure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Five continuity test labels do not provide complete clinical workflows, devices, providers, regions, combinations and safety oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Each fault can pass separately while identity and network failure together blocks an essential patient workflow.

## `USEQ-92E715A2` — Nondeterministic

**Exact statement**

> Protect research consent, protocol, cohort, deidentification, and data-use restrictions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect research consent, protocol, cohort, deidentification, and data-use restrictions.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-B931B65F` — Nondeterministic

**Exact statement**

> Validate data migration and reconciliation with clinical or regulated invariants.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Clinical or regulated invariants and complete migration reconciliation require specialist authority, data completeness and substantive correctness oracles.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

Counts can reconcile while units change incorrectly or a clinically significant relationship is lost.

## `USEQ-5A73044C` — Nondeterministic

**Exact statement**

> Train users for high-impact workflows and assess competence where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Train users for high-impact workflows and assess competence where required.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Train users for high-impact workflows and assess competence where required.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A07EC898` — Deterministic

**Exact statement**

> Maintain incident, breach, safety, and regulatory reporting procedures.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A regulated reporting catalog can require four exact incident, breach, safety and regulatory reporting procedures with authority and version bindings.

### Exact deterministic check

1. Maintain incident, breach, safety, and regulatory reporting procedures.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A missing breach or safety-report procedure fails the finite catalog; legal sufficiency is not inferred.

## `USEQ-99FF0E3C` — Nondeterministic

**Exact statement**

> Use independent validation and documented change control for high-impact functionality.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use independent validation and documented change control for high-impact functionality.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use independent validation and documented change control for high-impact functionality.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7AFC88F2` — Nondeterministic

**Exact statement**

> Do not deploy when patient or subject harm cannot be bounded, detected, and mitigated.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Do not deploy when patient or subject harm cannot be bounded, detected, and mitigated.” is nondeterministic because it is a safety, residual-harm, essential-access, or cryptographic-risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-0A7E51B2` — Nondeterministic

**Exact statement**

> Determine applicable public-records, procurement, accessibility, identity, security, privacy, localization, and continuity obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Determine applicable public-records, procurement, accessibility, identity, security, privacy, localization, and continuity obligations.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-02E0F51C` — Nondeterministic

**Exact statement**

> Design for equitable access across disability, language, device, bandwidth, literacy, geography, and lack of commercial identity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Design for equitable access across disability, language, device, bandwidth, literacy, geography, and lack of commercial identity.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-80BCEB64` — Nondeterministic

**Exact statement**

> Provide non-digital or assisted alternatives where exclusion would deny essential service.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide non-digital or assisted alternatives where exclusion would deny essential service.” is nondeterministic because it is a safety, residual-harm, essential-access, or cryptographic-risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-3358D1EF` — Nondeterministic

**Exact statement**

> Use transparent eligibility, decision, appeal, correction, and complaint processes.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Transparency of five public-sector processes is a user, administrative-law and accessibility judgment, not a document-presence fact.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

All five procedures can be published while decision reasons remain opaque or appeal is practically unusable.

## `USEQ-9D2A742A` — Nondeterministic

**Exact statement**

> Avoid requiring unnecessary private accounts, tracking, or third-party platforms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Avoid requiring unnecessary private accounts, tracking, or third-party platforms.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-08693ABE` — Nondeterministic

**Exact statement**

> Protect high-value identity, benefit, tax, justice, licensing, voting, or civic records according to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect high-value identity, benefit, tax, justice, licensing, voting, or civic records according to impact.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect high-value identity, benefit, tax, justice, licensing, voting, or civic records according to impact.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-D39A0138` — Nondeterministic

**Exact statement**

> Provide complete auditability and records retention without exposing sensitive information.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide complete auditability and records retention without exposing sensitive information.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Provide complete auditability and records retention without exposing sensitive information.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E3C13E8C` — Nondeterministic

**Exact statement**

> Use strong supplier transparency, data ownership, exit, and continuity terms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use strong supplier transparency, data ownership, exit, and continuity terms.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-0E684597` — Nondeterministic

**Exact statement**

> Avoid proprietary lock-in that prevents public accountability or service continuity without explicit justification.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Avoid proprietary lock-in that prevents public accountability or service continuity without explicit justification.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Avoid proprietary lock-in that prevents public accountability or service continuity without explicit justification.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-0701D270` — Deterministic

**Exact statement**

> Publish service status, maintenance, accessibility, privacy, security contact, and support information.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A public service can be checked for published status, maintenance, accessibility, privacy, security-contact and support resources at stable locations.

### Exact deterministic check

1. Publish service status, maintenance, accessibility, privacy, security contact, and support information.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

Omitting any of the six resources or returning an unapproved version fails the finite publication schema.

## `USEQ-A0B4A6E4` — Nondeterministic

**Exact statement**

> Test peak civic events, deadlines, emergencies, and coordinated abuse.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Four scenario executions do not establish realistic peak, deadline, emergency and coordinated-abuse scale, combinations or public-service outcomes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A nominal civic-event load test can pass while coordinated abuse during a deadline overwhelms identity verification.

## `USEQ-E1AB8D23` — Nondeterministic

**Exact statement**

> Design for operation during disasters, regional failure, identity outage, and supplier failure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Operation during disasters and four correlated failure categories is an availability and continuity claim over undefined severity, duration and dependencies.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Documented alternate paths can exist while regional and supplier failure together makes the service unavailable.

## `USEQ-39F51DEC` — Nondeterministic

**Exact statement**

> Protect public interfaces against denial, defacement, disinformation, impersonation, and data manipulation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect public interfaces against denial, defacement, disinformation, impersonation, and data manipulation.” has checkable evidence clauses, but its full result also requires a representative domain-specific workload, failure model, safety oracle, or empirical acceptance protocol.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect public interfaces against denial, defacement, disinformation, impersonation, and data manipulation.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-24FDB6CD` — Nondeterministic

**Exact statement**

> Use independent assurance for high-impact releases and security claims.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use independent assurance for high-impact releases and security claims.” has checkable evidence clauses, but its full result also requires human judgment about verification strength, safety, clarity, adequacy, materiality, trust, impact, or feasibility.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use independent assurance for high-impact releases and security claims.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-9806BBFD` — Nondeterministic

**Exact statement**

> Make algorithmic or rules-based decisions explainable and contestable where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Make algorithmic or rules-based decisions explainable and contestable where required.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Make algorithmic or rules-based decisions explainable and contestable where required.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-59716E09` — Nondeterministic

**Exact statement**

> Maintain multilingual and accessible notices and forms.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required languages and accessibility depend on population, law, content and assistive technologies; automated tests cannot prove notices and forms are usable.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

Published translations can be incomplete or misleading and a form can pass machine checks while failing screen-reader users.

## `USEQ-797FD404` — Nondeterministic

**Exact statement**

> Preserve evidence for audit, oversight, legal challenge, and historical record.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Preserve evidence for audit, oversight, legal challenge, and historical record.” has checkable evidence clauses, but its full result also requires specialist interpretation of financial, safety, privacy, consent, licensing, healthcare, residency, or public-sector obligations.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Preserve evidence for audit, oversight, legal challenge, and historical record.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-7C31FB6D` — Nondeterministic

**Exact statement**

> Separate political or administrative preference from authorized policy and law.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Separate political or administrative preference from authorized policy and law.” is nondeterministic because it requires accountable financial, privacy, consent, healthcare, public-records, policy, or legal interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-979BA5B0` — Nondeterministic

**Exact statement**

> Review disparate impact and barriers to essential access.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Review disparate impact and barriers to essential access.” is nondeterministic because competent reviewers can differ about necessity, safety, usability, minimization, protection, fairness, resilience, or impact on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-459E7E15` — Nondeterministic

**Exact statement**

> Do not launch an essential service without demonstrated continuity and support capacity.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not launch an essential service without demonstrated continuity and support capacity.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-5DB27264` — Nondeterministic

**Exact statement**

> Define supported languages, runtimes, platforms, package managers, shells, architectures, and versions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Define supported languages, runtimes, platforms, package managers, shells, architectures, and versions.

## `USEQ-D73379F6` — Nondeterministic

**Exact statement**

> Use stable idiomatic contracts appropriate to each target ecosystem.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use stable idiomatic contracts appropriate to each target ecosystem.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-36FDC9A4` — Nondeterministic

**Exact statement**

> Keep default behavior safe, predictable, non-destructive, and suitable for automation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep default behavior safe, predictable, non-destructive, and suitable for automation.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-6F66FA4B` — Nondeterministic

**Exact statement**

> Separate machine-readable output from human-readable diagnostics.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

JSON mode writes stdout and diagnostics write stderr for tested commands, while an unregistered subcommand mixes a warning into machine-readable stdout.

## `USEQ-4C2BBB0D` — Nondeterministic

**Exact statement**

> Use meaningful exit status, structured errors, and actionable messages.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use meaningful exit status, structured errors, and actionable messages.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-09D01674` — Nondeterministic

**Exact statement**

> Support noninteractive, quiet, dry-run, confirmation, timeout, retry, and cancellation behavior as appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Support noninteractive, quiet, dry-run, confirmation, timeout, retry, and cancellation behavior as appropriate.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-AAC6303F` — Nondeterministic

**Exact statement**

> Do not expose secrets in command history, process listings, logs, examples, or error output.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not expose secrets in command history, process listings, logs, examples, or error output.” has a bounded test or analysis component, but full pass status depends on an undefined domain population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D027BA27` — Nondeterministic

**Exact statement**

> Validate paths, URLs, configuration, environment, and untrusted project content.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate paths, URLs, configuration, environment, and untrusted project content.” has a bounded test or analysis component, but full pass status depends on an undefined domain population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-939DF129` — Nondeterministic

**Exact statement**

> Avoid executing project or remote code implicitly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid executing project or remote code implicitly.” has a bounded test or analysis component, but full pass status depends on an undefined domain population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CC6BECF8` — Nondeterministic

**Exact statement**

> Protect update, plugin, extension, and package-loading mechanisms.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect update, plugin, extension, and package-loading mechanisms.” has a bounded test or analysis component, but full pass status depends on an undefined domain population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-BB61ED88` — Nondeterministic

**Exact statement**

> Version contracts and publish compatibility and deprecation policy.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Version contracts and publish compatibility and deprecation policy.

## `USEQ-F8B8E814` — Nondeterministic

**Exact statement**

> Provide migration guidance and test against representative downstream consumers.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide migration guidance and test against representative downstream consumers.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-C6981D19` — Nondeterministic

**Exact statement**

> Generate signed or attested packages with provenance, SBOM, and checksums.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared package is signed with a checksum, while an undeclared platform package lacks an SBOM and ships from the same release.

## `USEQ-09B1BA8F` — Nondeterministic

**Exact statement**

> Keep examples executable and documentation synchronized.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The checked workflow gate is objective for tracked paths, but complete path coverage and meaningful review, approval, or operational fitness are not bounded. For this rule, the proposed clause only proves: Inspect the versioned workflow policy and run fixtures proving missing, stale, malformed, incompatible, unapproved, or threshold-breaching evidence cannot advance.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

CI rejects the registered fixture, while a manual, external, emergency, or misclassified path bypasses the gate and violates the original statement. Specifically, it can pass while this remains false: Keep examples executable and documentation synchronized.

## `USEQ-D91045B1` — Nondeterministic

**Exact statement**

> Provide deterministic behavior and output where automation depends on it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide deterministic behavior and output where automation depends on it.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9AF6A86A` — Nondeterministic

**Exact statement**

> Respect platform conventions without silently changing semantics across platforms.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Respect platform conventions without silently changing semantics across platforms.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-D2CD8EC4` — Nondeterministic

**Exact statement**

> Make telemetry optional, transparent, minimized, and safe.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make telemetry optional, transparent, minimized, and safe.” has checkable artifact, presentation, preservation, minimization, or cryptographic evidence, but full compliance also depends on legal, privacy, consent, records, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-933B0296` — Nondeterministic

**Exact statement**

> Test fresh install, upgrade, downgrade where supported, offline, proxy, permission, and restricted-network behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Test fresh install, upgrade, downgrade where supported, offline, proxy, permission, and restricted-network behavior.

## `USEQ-AC6AC548` — Nondeterministic

**Exact statement**

> Provide security reporting and supported-version information.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Provide security reporting and supported-version information.

## `USEQ-65DF370E` — Nondeterministic

**Exact statement**

> Avoid making a convenience tool a hidden production single point of failure.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid making a convenience tool a hidden production single point of failure.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-77183203` — Nondeterministic

**Exact statement**

> Define supported devices, controls, displays, performance modes, network conditions, and accessibility options.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Define supported devices, controls, displays, performance modes, network conditions, and accessibility options.

## `USEQ-CC1754CB` — Nondeterministic

**Exact statement**

> Maintain fair authoritative state for competitive or economic outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain fair authoritative state for competitive or economic outcomes.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-4AEFAFB8` — Nondeterministic

**Exact statement**

> Protect against cheating, automation, item duplication, economy manipulation, and account theft.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect against cheating, automation, item duplication, economy manipulation, and account theft.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-96AE9F3B` — Nondeterministic

**Exact statement**

> Validate matchmaking, ranking, rewards, randomization, and progression for integrity and unintended bias.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate matchmaking, ranking, rewards, randomization, and progression for integrity and unintended bias.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-925DD3FB` — Nondeterministic

**Exact statement**

> Disclose probabilities, purchases, recurring costs, virtual currency, expiration, and irreversible choices clearly.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Disclose probabilities, purchases, recurring costs, virtual currency, expiration, and irreversible choices clearly.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-FFAB4E8F` — Nondeterministic

**Exact statement**

> Protect children and vulnerable users from exploitative monetization and social interaction.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect children and vulnerable users from exploitative monetization and social interaction.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-3DEFD074` — Nondeterministic

**Exact statement**

> Provide controls for harassment, blocking, reporting, moderation, voice, chat, and user-generated content.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Blocking and reporting tests pass, while voice harassment through an untested party channel cannot be blocked or appealed.

## `USEQ-7473B28D` — Nondeterministic

**Exact statement**

> Provide motion, flashing, color, audio, input, text, difficulty, and cognitive accessibility options.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide motion, flashing, color, audio, input, text, difficulty, and cognitive accessibility options.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-F7140184` — Nondeterministic

**Exact statement**

> Avoid forcing sensory effects or physical movement without alternatives and safety warnings.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid forcing sensory effects or physical movement without alternatives and safety warnings.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-F84463E8` — Nondeterministic

**Exact statement**

> Preserve progress across crash, disconnect, patch, device change, and account recovery.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Crash and reconnect fixtures preserve progress, while account recovery restores an older save and loses paid progress.

## `USEQ-7083EEB1` — Nondeterministic

**Exact statement**

> Test latency, synchronization, prediction, rollback, reconnect, and regional capacity.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Test latency, synchronization, prediction, rollback, reconnect, and regional capacity.

## `USEQ-7A4816F3` — Nondeterministic

**Exact statement**

> Protect local save, cloud save, downloadable content, mods, and plugins.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect local save, cloud save, downloadable content, mods, and plugins.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-5002400A` — Nondeterministic

**Exact statement**

> Use anti-cheat controls proportionate to privacy, device control, and false-positive harm.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use anti-cheat controls proportionate to privacy, device control, and false-positive harm.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-7FABC8C3` — Nondeterministic

**Exact statement**

> Provide appeal and recovery for enforcement actions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Provide appeal and recovery for enforcement actions.

## `USEQ-2A00C28A` — Nondeterministic

**Exact statement**

> Monitor toxic behavior, fraud, economy inflation, crash, performance, and player safety.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Current observations for registered systems cannot prove complete monitored scope, correct meaning, or adequate thresholds for every relevant condition. For this rule, the proposed clause only proves: Read the authorized monitoring system and verify current observations exist for every named signal with bound release, service, owner, threshold definition, and immutable query timestamp.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All queried signals are current for known systems, while an unregistered region, provider, dependency, or harmful condition violates the original statement. Specifically, it can pass while this remains false: Monitor toxic behavior, fraud, economy inflation, crash, performance, and player safety.

## `USEQ-C873F83A` — Nondeterministic

**Exact statement**

> Avoid deceptive engagement loops and make time or spending controls available where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid deceptive engagement loops and make time or spending controls available where appropriate.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-C6F35C3F` — Nondeterministic

**Exact statement**

> Respect streaming, recording, spectator, and privacy preferences.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Respect streaming, recording, spectator, and privacy preferences.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-F2EE2423` — Nondeterministic

**Exact statement**

> Test long-session heat, battery, memory, network, and motion comfort.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Test long-session heat, battery, memory, network, and motion comfort.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-D2EEA3DA` — Nondeterministic

**Exact statement**

> Plan seasonal content, live operations, event rollback, and end-of-service continuity.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Plan seasonal content, live operations, event rollback, and end-of-service continuity.

## `USEQ-EA1879A2` — Nondeterministic

**Exact statement**

> Do not launch high-stakes economies or social features without abuse and support readiness.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not launch high-stakes economies or social features without abuse and support readiness.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-7EFB51F6` — Nondeterministic

**Exact statement**

> Record exact source commits and release tags.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record exact source commits and release tags.

## `USEQ-8C855A06` — Nondeterministic

**Exact statement**

> Record immutable digests for every deployable service, worker, job, frontend bundle, and infrastructure artifact.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Record immutable digests for every deployable service, worker, job, frontend bundle, and infrastructure artifact.

## `USEQ-18DBA736` — Nondeterministic

**Exact statement**

> Record production configuration versions and feature-flag states.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record production configuration versions and feature-flag states.

## `USEQ-E03B5E82` — Nondeterministic

**Exact statement**

> Record target regions, accounts, projects, environments, clusters, and domains.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record target regions, accounts, projects, environments, clusters, and domains.

## `USEQ-EDFA793D` — Nondeterministic

**Exact statement**

> Record the deployment date and change window.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record the deployment date and change window.

## `USEQ-A68072E7` — Nondeterministic

**Exact statement**

> Record the previous known-good artifact and configuration.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record the previous known-good artifact and configuration.

## `USEQ-B1A0C473` — Nondeterministic

**Exact statement**

> Record the rollback artifact or tested roll-forward plan.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record the rollback artifact or tested roll-forward plan.

## `USEQ-D1D57EA1` — Nondeterministic

**Exact statement**

> Confirm source, artifacts, configuration, migrations, tests, and approvals all refer to the same release.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All release-record links share a label, while the approval actually covers a different artifact digest.

## `USEQ-8B5934D8` — Nondeterministic

**Exact statement**

> List every API, webhook, callback, streaming connection, and machine-to-machine interface.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List every API, webhook, callback, streaming connection, and machine-to-machine interface.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7F798459` — Nondeterministic

**Exact statement**

> List every worker, queue, scheduled task, batch process, administrative tool, and support tool.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List every worker, queue, scheduled task, batch process, administrative tool, and support tool.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9EE38C0B` — Nondeterministic

**Exact statement**

> List every database, object store, search index, cache, message broker, analytics store, and backup location.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List every database, object store, search index, cache, message broker, analytics store, and backup location.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D8184B36` — Nondeterministic

**Exact statement**

> List every identity, payment, communications, analytics, advertising, experimentation, support, logging, monitoring, CDN, DNS, hosting, and infrastructure provider.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List every identity, payment, communications, analytics, advertising, experimentation, support, logging, monitoring, CDN, DNS, hosting, and infrastructure provider.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-305EF1D6` — Nondeterministic

**Exact statement**

> List all user types, administrative roles, support roles, service accounts, external actors, and partner systems.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List all user types, administrative roles, support roles, service accounts, external actors, and partner systems.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C7F3DF66` — Nondeterministic

**Exact statement**

> List supported countries, languages, locales, currencies, time zones, browsers, devices, and viewport classes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List supported countries, languages, locales, currencies, time zones, browsers, devices, and viewport classes.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CEECE86D` — Nondeterministic

**Exact statement**

> List accessibility conformance targets.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: List accessibility conformance targets.

## `USEQ-D81B1472` — Nondeterministic

**Exact statement**

> List all data classifications and regulated or contractually restricted data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “List all data classifications and regulated or contractually restricted data.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-282C5B2F` — Nondeterministic

**Exact statement**

> List excluded components and explain why they are outside the assessment.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: List excluded components and explain why they are outside the assessment.

## `USEQ-3D969CA9` — Nondeterministic

**Exact statement**

> Identify shared platforms and dependencies that can affect the application even when another team operates them.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify shared platforms and dependencies that can affect the application even when another team operates them.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8CF2C791` — Nondeterministic

**Exact statement**

> The deployment plan identifies exact artifacts, target environments, migrations, configuration changes, infrastructure changes, feature flags, and external dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: The deployment plan identifies exact artifacts, target environments, migrations, configuration changes, infrastructure changes, feature flags, and external dependencies.

## `USEQ-34987B94` — Nondeterministic

**Exact statement**

> The deployer cannot silently substitute an unapproved artifact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The checked workflow gate is objective for tracked paths, but complete path coverage and meaningful review, approval, or operational fitness are not bounded. For this rule, the proposed clause only proves: Inspect the versioned workflow policy and run fixtures proving missing, stale, malformed, incompatible, unapproved, or threshold-breaching evidence cannot advance.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

The workflow pins approved digests, while an operator replaces a mounted production bundle after the gate.

## `USEQ-2222C9D0` — Nondeterministic

**Exact statement**

> Predeployment checks validate environment health, data state, provider health, quotas, and available capacity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The checked workflow gate is objective for tracked paths, but complete path coverage and meaningful review, approval, or operational fitness are not bounded. For this rule, the proposed clause only proves: Inspect the versioned workflow policy and run fixtures proving missing, stale, malformed, incompatible, unapproved, or threshold-breaching evidence cannot advance.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

CI rejects the registered fixture, while a manual, external, emergency, or misclassified path bypasses the gate and violates the original statement. Specifically, it can pass while this remains false: Predeployment checks validate environment health, data state, provider health, quotas, and available capacity.

## `USEQ-CF32EB78` — Nondeterministic

**Exact statement**

> Migration steps and ordering are explicit.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Migration steps and ordering are explicit.

## `USEQ-EB431FD0` — Nondeterministic

**Exact statement**

> Application, worker, scheduled-job, event, and schema compatibility is maintained during rollout.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Application, worker, scheduled-job, event, and schema compatibility is maintained during rollout.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-321B8525` — Nondeterministic

**Exact statement**

> Progressive, canary, blue-green, cohort, regional, or equivalent controlled rollout is used where risk warrants it.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Progressive, canary, blue-green, cohort, regional, or equivalent controlled rollout is used where risk warrants it.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-DC6E1BDA` — Nondeterministic

**Exact statement**

> Health gates examine user-facing success, latency, errors, saturation, data integrity, security, abuse, and business outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Health gates examine user-facing success, latency, errors, saturation, data integrity, security, abuse, and business outcomes.

## `USEQ-58678F75` — Nondeterministic

**Exact statement**

> Rollout pauses automatically or operationally on predefined signals.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The checked workflow gate is objective for tracked paths, but complete path coverage and meaningful review, approval, or operational fitness are not bounded. For this rule, the proposed clause only proves: Inspect the versioned workflow policy and run fixtures proving missing, stale, malformed, incompatible, unapproved, or threshold-breaching evidence cannot advance.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

CI rejects the registered fixture, while a manual, external, emergency, or misclassified path bypasses the gate and violates the original statement. Specifically, it can pass while this remains false: Rollout pauses automatically or operationally on predefined signals.

## `USEQ-BBD42F35` — Nondeterministic

**Exact statement**

> Stop, rollback, roll-forward, and incident-declaration criteria are documented before launch.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Stop, rollback, roll-forward, and incident-declaration criteria are documented before launch.

## `USEQ-1409AA23` — Nondeterministic

**Exact statement**

> Rollback can be initiated quickly and has been rehearsed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rollback can be initiated quickly and has been rehearsed.” has an objectively checkable record, artifact, configuration, protocol, or bounded test, but full pass meaning also depends on semantic completeness, domain scope, or safety outcome.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded release, scope, research, credential, policy, migration, or evidence record explicitly named by this control without treating it as proof of domain suitability or complete coverage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9EEEEBB8` — Nondeterministic

**Exact statement**

> Rollback is compatible with database, event, cache, and interface changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rollback is compatible with database, event, cache, and interface changes.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-99B06FF3` — Nondeterministic

**Exact statement**

> Feature flags or kill switches can independently disable risky behavior where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Feature flags or kill switches can independently disable risky behavior where appropriate.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3E2AD22B` — Nondeterministic

**Exact statement**

> Traffic shifting, load-balancer behavior, cache invalidation, CDN invalidation, DNS changes, and certificate changes are planned and tested.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized rollout or support record contains every named change, target, owner, expected state, and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All declared bindings and fixtures agree, while an omitted artifact, environment, party, trust path, or failure state violates the original statement. Specifically, it can pass while this remains false: Traffic shifting, load-balancer behavior, cache invalidation, CDN invalidation, DNS changes, and certificate changes are planned and tested.

## `USEQ-96297CBB` — Nondeterministic

**Exact statement**

> Rate limits, quotas, and capacities are adjusted where needed.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rate limits, quotas, and capacities are adjusted where needed.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3AC480C5` — Nondeterministic

**Exact statement**

> No critical step exists only in someone's memory.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “No critical step exists only in someone's memory.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-4C6B2EA9` — Nondeterministic

**Exact statement**

> Manual steps are minimized, scripted where practical, and peer-checked where material.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Manual steps are minimized, scripted where practical, and peer-checked where material.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-7BC2F497` — Nondeterministic

**Exact statement**

> The full procedure has been rehearsed in a production-like environment.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “The full procedure has been rehearsed in a production-like environment.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-161C6591` — Nondeterministic

**Exact statement**

> Engineering, operations, security, data, support, communications, and provider contacts are available as needed.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Engineering, operations, security, data, support, communications, and provider contacts are available as needed.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-CA947752` — Nondeterministic

**Exact statement**

> Verify the deployed artifact digest, configuration, feature-flag state, schema version, and migration completion.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized rollout or support record contains every named change, target, owner, expected state, and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The rollout record reports matching state, while one live cluster runs a different flag and unfinished migration.

## `USEQ-5E936430` — Nondeterministic

**Exact statement**

> Test login, logout, account recovery, and privileged authentication.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Test login, logout, account recovery, and privileged authentication.

## `USEQ-C2B79849` — Nondeterministic

**Exact statement**

> Test authorization with multiple roles and tenants where applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Test authorization with multiple roles and tenants where applicable.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A5477858` — Nondeterministic

**Exact statement**

> Verify public, authenticated, administrative, API, worker, scheduled-job, and batch paths.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Verify public, authenticated, administrative, API, worker, scheduled-job, and batch paths.

## `USEQ-F30D75DE` — Nondeterministic

**Exact statement**

> Verify payment, webhook, email, SMS, push, identity, storage, search, analytics, and other critical integrations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify payment, webhook, email, SMS, push, identity, storage, search, analytics, and other critical integrations.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CA491689` — Nondeterministic

**Exact statement**

> Verify transport security, certificates, redirects, cross-origin behavior, cache controls, and security headers.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Verify transport security, certificates, redirects, cross-origin behavior, cache controls, and security headers.

## `USEQ-CCE6C371` — Nondeterministic

**Exact statement**

> Verify CDN and application-cache behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Verify CDN and application-cache behavior.

## `USEQ-3904EEC5` — Nondeterministic

**Exact statement**

> Review errors, latency, traffic, saturation, queues, database health, replication, and dependency metrics.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Current observations for registered systems cannot prove complete monitored scope, correct meaning, or adequate thresholds for every relevant condition. For this rule, the proposed clause only proves: Read the authorized monitoring system and verify current observations exist for every named signal with bound release, service, owner, threshold definition, and immutable query timestamp.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All queried signals are current for known systems, while an unregistered region, provider, dependency, or harmful condition violates the original statement. Specifically, it can pass while this remains false: Review errors, latency, traffic, saturation, queues, database health, replication, and dependency metrics.

## `USEQ-2A66A878` — Nondeterministic

**Exact statement**

> Review logs and traces for unexpected exceptions, data leakage, authorization failures, and abnormal behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review logs and traces for unexpected exceptions, data leakage, authorization failures, and abnormal behavior.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8780BABB` — Nondeterministic

**Exact statement**

> Verify alerts, synthetic monitoring, dashboards, and paging.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Verify alerts, synthetic monitoring, dashboards, and paging.

## `USEQ-6FAC2881` — Nondeterministic

**Exact statement**

> Confirm support receives expected telemetry and current documentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized rollout or support record contains every named change, target, owner, expected state, and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All declared bindings and fixtures agree, while an omitted artifact, environment, party, trust path, or failure state violates the original statement. Specifically, it can pass while this remains false: Confirm support receives expected telemetry and current documentation.

## `USEQ-6DFB4E7F` — Nondeterministic

**Exact statement**

> Confirm no unexpected increase in abuse, fraud, failed payments, duplicate operations, data-integrity failures, or support volume.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm no unexpected increase in abuse, fraud, failed payments, duplicate operations, data-integrity failures, or support volume.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-46A643BE` — Nondeterministic

**Exact statement**

> Roll back, roll forward, or declare an incident rather than rationalizing unexplained anomalies.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Roll back, roll forward, or declare an incident rather than rationalizing unexplained anomalies.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-23CEBF4F` — Nondeterministic

**Exact statement**

> Record the final go, hold, rollback, roll-forward, or incident decision.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record the final go, hold, rollback, roll-forward, or incident decision.

## `USEQ-603F2296` — Nondeterministic

**Exact statement**

> Product owner signs off on scope, behavior, acceptance criteria, launch objectives, and customer impact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The product owner signs every field, while customer impact evidence is missing and the acceptance criteria are unsafe.

## `USEQ-DFE5DFDB` — Nondeterministic

**Exact statement**

> Engineering owner signs off on architecture, implementation, tests, migrations, maintainability, and technical risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Engineering sign-off exists, while the signer did not review the migration and is not authorized.

## `USEQ-9A317D5C` — Nondeterministic

**Exact statement**

> Quality owner signs off on test completeness, evidence, defects, and regression status.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Quality sign-off exists, while the test inventory omits a critical path and the signer never saw it.

## `USEQ-4A5E2EB1` — Nondeterministic

**Exact statement**

> Security owner signs off on threat modeling, application-security verification, findings, supply chain, and residual security risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Security sign-off exists, while a supply-chain finding is absent and residual risk is misrepresented.

## `USEQ-3077E62C` — Nondeterministic

**Exact statement**

> Reliability or operations owner signs off on SLOs, capacity, observability, on-call, rollout, rollback, and recovery.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Operations sign-off exists, while the recovery procedure has never restored the production data volume.

## `USEQ-F81F6CE4` — Nondeterministic

**Exact statement**

> Data owner signs off on integrity, migration, retention, reconciliation, and restoration.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Data-owner sign-off exists, while reconciliation omits a downstream replica.

## `USEQ-17F410B4` — Nondeterministic

**Exact statement**

> Privacy owner signs off on data inventory, purpose, rights, notices, transfers, and privacy risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Privacy sign-off exists, while a cross-border transfer is absent from the inventory.

## `USEQ-1C6662C7` — Nondeterministic

**Exact statement**

> Accessibility owner or qualified reviewer signs off on target conformance and unresolved barriers.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Accessibility sign-off exists, while a keyboard-only blocker was never included in evidence.

## `USEQ-92006B53` — Nondeterministic

**Exact statement**

> Legal or compliance owner signs off on applicable obligations, terms, contracts, regulatory risk, and required notices.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Legal sign-off exists, while a required notice in one jurisdiction is absent.

## `USEQ-615F9CB7` — Nondeterministic

**Exact statement**

> Support owner signs off on support readiness, escalation, documentation, and customer communications.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Support sign-off exists, while the escalation number is dead and current documentation is missing.

## `USEQ-2A0C5DBF` — Nondeterministic

**Exact statement**

> Business risk owner explicitly accepts remaining material risk.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A risk owner accepts the record, while the material risk and affected parties were understated.

## `USEQ-3437198D` — Nondeterministic

**Exact statement**

> Executive or designated release authority makes the final decision for high-impact launches.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A release authority signs, while the launch was never classified high impact and bypassed that authority.

## `USEQ-9A466E24` — Nondeterministic

**Exact statement**

> No person signs an area for which evidence is absent or for which they are not qualified or authorized.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All signers have populated role fields, while one used borrowed credentials and lacked the required qualification.

## `USEQ-A4723C18` — Nondeterministic

**Exact statement**

> Every critical requirement has current evidence tied to the exact production artifact and configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Every declared critical requirement links current evidence, while an omitted critical requirement has none.

## `USEQ-98349D6B` — Nondeterministic

**Exact statement**

> Critical journeys meet approved correctness, performance, security, privacy, accessibility, and reliability objectives.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Critical journeys meet approved correctness, performance, security, privacy, accessibility, and reliability objectives.” has bounded journey evidence and numeric thresholds, but full privacy, accessibility, security, and correctness outcomes require specialist evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-4CECD69F` — Nondeterministic

**Exact statement**

> No known residual risk exceeds the organization's approved risk appetite.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “No known residual risk exceeds the organization's approved risk appetite.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-A8D674CC` — Nondeterministic

**Exact statement**

> Every accepted risk has an accountable owner, compensating control, monitoring, remediation plan, and expiry.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Every accepted risk has an accountable owner, compensating control, monitoring, remediation plan, and expiry.

## `USEQ-3FE0D592` — Nondeterministic

**Exact statement**

> Rollout, rollback or roll-forward, restoration, incident response, and customer communication are demonstrably workable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rollout, rollback or roll-forward, restoration, incident response, and customer communication are demonstrably workable.” has bounded drills, but whether the combined technical and human response is workable requires an empirical protocol and accountable review.
- **Nondeterministic reason codes:** `undefined_protocol`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-4084EC49` — Nondeterministic

**Exact statement**

> Required operational, support, security, legal, privacy, and business personnel are ready.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Required operational, support, security, legal, privacy, and business personnel are ready.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-A4B3AB62` — Nondeterministic

**Exact statement**

> User, security, privacy, integrity, financial, legal, accessibility, reliability, and safety impact is bounded and understood.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “User, security, privacy, integrity, financial, legal, accessibility, reliability, and safety impact is bounded and understood.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-87C1B515` — Nondeterministic

**Exact statement**

> The affected feature can be disabled, isolated, rolled back, or rolled forward safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “The affected feature can be disabled, isolated, rolled back, or rolled forward safely.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-88CFEFEC` — Nondeterministic

**Exact statement**

> Evidence is missing, stale, contradictory, or tied to a different artifact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All declared bindings and fixtures agree, while an omitted artifact, environment, party, trust path, or failure state violates the original statement. Specifically, it can pass while this remains false: Evidence is missing, stale, contradictory, or tied to a different artifact.

## `USEQ-F96A1B86` — Nondeterministic

**Exact statement**

> Rollback, safe roll-forward, restoration, or incident response is unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rollback, safe roll-forward, restoration, or incident response is unavailable.” has checkable artifacts and rehearsal records, but whether roll-forward or response is safe across the real incident scope requires contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Compare immutable source, artifact, configuration, evidence-package, key-domain, or credential identities and fail closed on missing or mismatched bindings.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-F38CE10F` — Nondeterministic

**Exact statement**

> Required legal, regulatory, contractual, privacy, accessibility, safety, or security approval is absent.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The record says all approvals are present, while a legally required accessibility approval is not recognized as applicable.

## `USEQ-6E882462` — Nondeterministic

**Exact statement**

> The team relies on "we will monitor it" without a concrete, tested detection and mitigation path.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

A detector and mitigation fixture pass, while a new failure mode is only monitored and cannot be contained.

## `USEQ-75FBD07C` — Nondeterministic

**Exact statement**

> Apply the scientific and computational reproducibility controls when software creates, transforms, analyzes, simulates, or publishes evidence used for research, engineering, medicine, policy, finance, safety, or other material conclusions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Apply the scientific and computational reproducibility controls when software creates, transforms, analyzes, simulates, or publishes evidence used for research, engineering, medicine, policy, finance, safety, or other material conclusions.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-E2073ED7` — Nondeterministic

**Exact statement**

> Apply the digital-credential and trust-service controls when software issues, stores, presents, verifies, signs, seals, timestamps, notarizes, or relies on portable cryptographic claims.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Apply the digital-credential and trust-service controls when software issues, stores, presents, verifies, signs, seals, timestamps, notarizes, or relies on portable cryptographic claims.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-CC2A5D3C` — Nondeterministic

**Exact statement**

> Document which sections apply, the intended assurance level, legal context, relying parties, and consequences of error.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Document which sections apply, the intended assurance level, legal context, relying parties, and consequences of error.

## `USEQ-6FD0AD80` — Nondeterministic

**Exact statement**

> Obtain qualified domain, statistical, cryptographic, identity, legal, and accessibility review where the stakes warrant it.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Obtain qualified domain, statistical, cryptographic, identity, legal, and accessibility review where the stakes warrant it.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-22EC4A27` — Nondeterministic

**Exact statement**

> Define the research question, hypothesis, intended use, decision context, stakeholders, and prohibited interpretations before analysis.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A protocol lists question and hypothesis, while prohibited interpretations omit a foreseeable discriminatory use.

## `USEQ-03CB3B9C` — Nondeterministic

**Exact statement**

> Distinguish exploratory, confirmatory, operational, and regulatory analysis.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Distinguish exploratory, confirmatory, operational, and regulatory analysis.

## `USEQ-E60FC501` — Nondeterministic

**Exact statement**

> Predefine primary outcomes, inclusion criteria, exclusions, transformations, and stopping rules for confirmatory work where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Predefine primary outcomes, inclusion criteria, exclusions, transformations, and stopping rules for confirmatory work where appropriate.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-3CEE0B8F` — Nondeterministic

**Exact statement**

> Register or preserve time-stamped analysis plans when undisclosed flexibility could distort conclusions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Register or preserve time-stamped analysis plans when undisclosed flexibility could distort conclusions.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B2EFE726` — Nondeterministic

**Exact statement**

> Disclose conflicts of interest, sponsorship, incentives, and material prior beliefs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Disclose conflicts of interest, sponsorship, incentives, and material prior beliefs.” has checkable disclosure fields, but a bounded system cannot prove that every material conflict or prior belief was disclosed truthfully.
- **Nondeterministic reason codes:** `human_accountability`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F7A3E9DF` — Nondeterministic

**Exact statement**

> Obtain required ethics, privacy, safety, legal, data-access, and participant approvals.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Obtain required ethics, privacy, safety, legal, data-access, and participant approvals.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-B67B4198` — Nondeterministic

**Exact statement**

> Define authorship, contribution, review, data stewardship, and correction responsibilities.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Define authorship, contribution, review, data stewardship, and correction responsibilities.

## `USEQ-1E65593D` — Nondeterministic

**Exact statement**

> Protect the ability to report negative, null, inconvenient, or contradictory results.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect the ability to report negative, null, inconvenient, or contradictory results.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-6C67A496` — Nondeterministic

**Exact statement**

> Prohibit fabrication, falsification, selective deletion, deceptive presentation, and undisclosed result manipulation.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prohibit fabrication, falsification, selective deletion, deceptive presentation, and undisclosed result manipulation.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-E736C3A5` — Nondeterministic

**Exact statement**

> Preserve decisions and deviations from the original plan with rationale.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Preserve decisions and deviations from the original plan with rationale.

## `USEQ-859AA9C7` — Nondeterministic

**Exact statement**

> Define correction, retraction, and downstream-notification procedures.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Define correction, retraction, and downstream-notification procedures.

## `USEQ-C0B40BD6` — Nondeterministic

**Exact statement**

> Record the origin, collection method, time, population, sampling, consent, rights, instruments, and custodianship of input data.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Data-origin fields are complete, while a hidden merged source lacks consent and sampling history.

## `USEQ-799CC460` — Nondeterministic

**Exact statement**

> Preserve raw or source data where lawful and necessary for verification.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve raw or source data where lawful and necessary for verification.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-3AFF5D69` — Nondeterministic

**Exact statement**

> Separate source data from cleaned, transformed, imputed, labeled, and derived data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All declared bindings and fixtures agree, while an omitted artifact, environment, party, trust path, or failure state violates the original statement. Specifically, it can pass while this remains false: Separate source data from cleaned, transformed, imputed, labeled, and derived data.

## `USEQ-1B66E032` — Nondeterministic

**Exact statement**

> Version data sets and assign immutable identifiers or digests.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Version data sets and assign immutable identifiers or digests.

## `USEQ-58289267` — Nondeterministic

**Exact statement**

> Record every material transformation, parameter, rule, exclusion, and manual correction.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Record every material transformation, parameter, rule, exclusion, and manual correction.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B1A1ADE9` — Nondeterministic

**Exact statement**

> Preserve metadata needed to interpret units, codes, missingness, uncertainty, calibration, and measurement conditions.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve metadata needed to interpret units, codes, missingness, uncertainty, calibration, and measurement conditions.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-FD67EB4A` — Nondeterministic

**Exact statement**

> Identify selection, survivorship, measurement, labeling, and missing-data biases.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify selection, survivorship, measurement, labeling, and missing-data biases.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-8EAB17B5` — Nondeterministic

**Exact statement**

> Validate data integrity through checksums, constraints, reconciliation, and representative review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate data integrity through checksums, constraints, reconciliation, and representative review.” has a bounded test or analysis component, but full pass status depends on an undefined domain population, oracle, protocol, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-96CE023A` — Nondeterministic

**Exact statement**

> Protect participant and confidential data while retaining sufficient verification evidence.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect participant and confidential data while retaining sufficient verification evidence.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-0AEEC3E1` — Nondeterministic

**Exact statement**

> Record access, export, sharing, retention, legal hold, and deletion decisions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record access, export, sharing, retention, legal hold, and deletion decisions.

## `USEQ-12965298` — Nondeterministic

**Exact statement**

> Ensure synthetic or simulated data is labeled and not confused with observed data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure synthetic or simulated data is labeled and not confused with observed data.” has checkable labels and schema distinctions, but whether users actually confuse the data types requires empirical evaluation.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-ECC129AC` — Nondeterministic

**Exact statement**

> Maintain provenance when data passes through suppliers, instruments, platforms, or collaborators.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain provenance when data passes through suppliers, instruments, platforms, or collaborators.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B736D113` — Nondeterministic

**Exact statement**

> Version all source code, scripts, notebooks, queries, models, prompts, configuration, workflow definitions, and analysis documents.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Version all source code, scripts, notebooks, queries, models, prompts, configuration, workflow definitions, and analysis documents.

## `USEQ-99DC8215` — Nondeterministic

**Exact statement**

> Record exact dependencies, operating environment, hardware-relevant behavior, random seeds, locale, time zone, and numerical settings.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record exact dependencies, operating environment, hardware-relevant behavior, random seeds, locale, time zone, and numerical settings.

## `USEQ-D619E4EE` — Nondeterministic

**Exact statement**

> Use immutable artifacts and environment descriptions sufficient for reconstruction.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use immutable artifacts and environment descriptions sufficient for reconstruction.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-89F501C0` — Nondeterministic

**Exact statement**

> Avoid hidden manual steps and undocumented interactive state.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid hidden manual steps and undocumented interactive state.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-CD20CF4C` — Nondeterministic

**Exact statement**

> Convert critical notebook or exploratory steps into reviewable, repeatable workflows.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Convert critical notebook or exploratory steps into reviewable, repeatable workflows.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DF7C3CCE` — Nondeterministic

**Exact statement**

> Record external services, data versions, model endpoints, and provider behavior relied upon.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Record external services, data versions, model endpoints, and provider behavior relied upon.

## `USEQ-EA49C886` — Nondeterministic

**Exact statement**

> Preserve intermediate outputs needed to diagnose and independently verify results.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve intermediate outputs needed to diagnose and independently verify results.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A5E603FC` — Nondeterministic

**Exact statement**

> Make pipelines fail visibly on missing, stale, malformed, or incompatible inputs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The checked workflow gate is objective for tracked paths, but complete path coverage and meaningful review, approval, or operational fitness are not bounded. For this rule, the proposed clause only proves: Inspect the versioned workflow policy and run fixtures proving missing, stale, malformed, incompatible, unapproved, or threshold-breaching evidence cannot advance.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

CI rejects the registered fixture, while a manual, external, emergency, or misclassified path bypasses the gate and violates the original statement. Specifically, it can pass while this remains false: Make pipelines fail visibly on missing, stale, malformed, or incompatible inputs.

## `USEQ-B8C41BE3` — Nondeterministic

**Exact statement**

> Use deterministic execution where scientifically appropriate and record unavoidable nondeterminism.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use deterministic execution where scientifically appropriate and record unavoidable nondeterminism.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A4FE6369` — Nondeterministic

**Exact statement**

> Quantify sensitivity to seeds, ordering, hardware, precision, and implementation differences where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Quantify sensitivity to seeds, ordering, hardware, precision, and implementation differences where relevant.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-8B7B27AC` — Nondeterministic

**Exact statement**

> Preserve build and execution logs with timestamps and artifact identifiers.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Preserve build and execution logs with timestamps and artifact identifiers.

## `USEQ-A075D067` — Nondeterministic

**Exact statement**

> Test reconstruction from a clean environment.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Test reconstruction from a clean environment.

## `USEQ-4F2985C0` — Nondeterministic

**Exact statement**

> Maintain migration plans for obsolete formats, dependencies, hardware, and services.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Maintain migration plans for obsolete formats, dependencies, hardware, and services.

## `USEQ-73892384` — Nondeterministic

**Exact statement**

> Use methods appropriate to the design, data, assumptions, sample, and intended inference.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use methods appropriate to the design, data, assumptions, sample, and intended inference.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-0288301B` — Nondeterministic

**Exact statement**

> State assumptions and test or justify them.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “State assumptions and test or justify them.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-937EF1C5` — Nondeterministic

**Exact statement**

> Distinguish association, prediction, causal effect, and mechanism.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The report labels an analysis predictive, while its claims and downstream decisions treat correlation as causal.

## `USEQ-B973FBCE` — Nondeterministic

**Exact statement**

> Avoid causal language when the design does not support it.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid causal language when the design does not support it.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-2152BD91` — Nondeterministic

**Exact statement**

> Define the population to which results may generalize.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A generalization population is named, while its sampling excludes the rural subgroup receiving the decision.

## `USEQ-2BEA31BC` — Nondeterministic

**Exact statement**

> Address sampling bias, confounding, dependence, censoring, clustering, and repeated measurement where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Address sampling bias, confounding, dependence, censoring, clustering, and repeated measurement where relevant.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-7DF3111E` — Nondeterministic

**Exact statement**

> Justify sample size and power or precision for confirmatory work.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Justify sample size and power or precision for confirmatory work.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-43A3105F` — Nondeterministic

**Exact statement**

> Report effect sizes and uncertainty, not significance labels alone.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Effect size and interval are reported, while invalid model assumptions make both misleading.

## `USEQ-3D01DFEE` — Nondeterministic

**Exact statement**

> Control or disclose multiplicity, repeated testing, subgroup search, and adaptive analysis.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Control or disclose multiplicity, repeated testing, subgroup search, and adaptive analysis.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-A8BAA9E5` — Nondeterministic

**Exact statement**

> Distinguish prespecified from post hoc analyses.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Distinguish prespecified from post hoc analyses.

## `USEQ-1C27B4EA` — Nondeterministic

**Exact statement**

> Validate model assumptions, calibration, discrimination, residuals, and stability.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate model assumptions, calibration, discrimination, residuals, and stability.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-47485FE7` — Nondeterministic

**Exact statement**

> Use held-out, external, temporal, or prospective validation appropriate to the claim.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use held-out, external, temporal, or prospective validation appropriate to the claim.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-75EECCDF` — Nondeterministic

**Exact statement**

> Avoid leakage between training, validation, test, and future data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid leakage between training, validation, test, and future data.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-BCDA51BA` — Nondeterministic

**Exact statement**

> Assess robustness to reasonable alternative preprocessing, models, exclusions, and parameters.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess robustness to reasonable alternative preprocessing, models, exclusions, and parameters.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-27E7E9A7` — Nondeterministic

**Exact statement**

> Report missing data, exclusions, failures, and sensitivity analyses.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Report missing data, exclusions, failures, and sensitivity analyses.

## `USEQ-C5DE8CA3` — Nondeterministic

**Exact statement**

> Avoid misleading axes, aggregation, smoothing, scales, truncation, or visual emphasis.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid misleading axes, aggregation, smoothing, scales, truncation, or visual emphasis.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-B8334A46` — Nondeterministic

**Exact statement**

> Make tables and figures accessible and interpretable without color alone.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make tables and figures accessible and interpretable without color alone.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-2A24761B` — Nondeterministic

**Exact statement**

> Have material calculations independently reviewed or reproduced.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A second analyst reproduces the spreadsheet, while both use the same wrong formula and no independent oracle.

## `USEQ-D22F346B` — Nondeterministic

**Exact statement**

> Define the materials needed for an independent party to reproduce or replicate the result.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Define the materials needed for an independent party to reproduce or replicate the result.” has a checkable materials manifest, but its completeness cannot be established without an actual independent reproduction attempt.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0B22A42C` — Nondeterministic

**Exact statement**

> Provide data, code, environment, parameters, documentation, and expected outputs unless restricted for a documented reason.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide data, code, environment, parameters, documentation, and expected outputs unless restricted for a documented reason.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-3D4A2C29` — Nondeterministic

**Exact statement**

> Use durable repositories, persistent identifiers, licenses, and citation information.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Use durable repositories, persistent identifiers, licenses, and citation information.

## `USEQ-2473255B` — Nondeterministic

**Exact statement**

> State access restrictions and provide a lawful controlled-access process where possible.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “State access restrictions and provide a lawful controlled-access process where possible.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-FF062A21` — Nondeterministic

**Exact statement**

> Validate shared artifacts from a clean environment before publication.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered positive and negative fixtures pass, while a valid but untested input, failure state, or integration path violates the original statement. Specifically, it can pass while this remains false: Validate shared artifacts from a clean environment before publication.

## `USEQ-A9A1F3FE` — Nondeterministic

**Exact statement**

> Include machine-readable metadata and provenance.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Include machine-readable metadata and provenance.

## `USEQ-974F199E` — Nondeterministic

**Exact statement**

> Preserve original and corrected versions with clear status.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve original and corrected versions with clear status.” has checkable immutable versions and status fields, but whether the displayed status is clear to intended users requires contextual evaluation.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-21A6AB1A` — Nondeterministic

**Exact statement**

> Record known limitations, unsupported uses, uncertainty, and negative findings.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Record known limitations, unsupported uses, uncertainty, and negative findings.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-ED92B2E3` — Nondeterministic

**Exact statement**

> Link publications and decisions to exact artifact and data versions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Verification of declared artifacts does not prove the evidence set is a complete inventory or that semantic metadata such as SBOM, license, or provenance is complete. For this rule, the proposed clause only proves: Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every declared artifact and digest verifies, while an omitted applicable build, data set, model, log, or publication artifact violates the original statement. Specifically, it can pass while this remains false: Link publications and decisions to exact artifact and data versions.

## `USEQ-D88815E2` — Nondeterministic

**Exact statement**

> Monitor material downstream use and issue corrections when a defect changes conclusions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor material downstream use and issue corrections when a defect changes conclusions.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-386A9BCD` — Nondeterministic

**Exact statement**

> Avoid claiming reproducibility merely because files were uploaded.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid claiming reproducibility merely because files were uploaded.” requires accountable launch, readiness, anomaly, risk, research-integrity, stakeholder, or operational judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-D66C65D4` — Nondeterministic

**Exact statement**

> Use independent artifact review or replication for high-impact conclusions where feasible.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use independent artifact review or replication for high-impact conclusions where feasible.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-01B4FBC4` — Nondeterministic

**Exact statement**

> Define issuers, holders, subjects, verifiers, relying parties, registries, wallets, trust frameworks, and governance authorities.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

All named credential parties are recorded, while a delegated verifier outside the inventory relies on the credential.

## `USEQ-6FF86021` — Nondeterministic

**Exact statement**

> Define the legal and semantic meaning of each credential type and each claim.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Define the legal and semantic meaning of each credential type and each claim.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-BC76C6E3` — Nondeterministic

**Exact statement**

> Specify who is authorized to issue, suspend, revoke, renew, update, and verify credentials.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Authorized credential actions are listed, while a backup issuer key can revoke credentials outside those roles.

## `USEQ-146AF17B` — Nondeterministic

**Exact statement**

> Define assurance requirements for identity proofing, source evidence, authentication, authorization, and issuance.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Assurance requirements have fields, while the identity-proofing level is inadequate for the claimed legal effect.

## `USEQ-F3A59165` — Nondeterministic

**Exact statement**

> Prevent a technically valid credential from implying a broader claim than the issuer established.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent a technically valid credential from implying a broader claim than the issuer established.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-698D6079` — Nondeterministic

**Exact statement**

> Establish governance for schemas, namespaces, identifiers, trust lists, status methods, and dispute resolution.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Governance records exist, while a namespace collision and unresolved dispute path undermine the trust framework.

## `USEQ-61D8F4B6` — Nondeterministic

**Exact statement**

> Define liability, support, incident, correction, and termination responsibilities among ecosystem participants.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Define liability, support, incident, correction, and termination responsibilities among ecosystem participants.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-FAC43689` — Nondeterministic

**Exact statement**

> Avoid one proprietary wallet, ledger, registry, or verifier becoming an unexamined universal trust anchor.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid one proprietary wallet, ledger, registry, or verifier becoming an unexamined universal trust anchor.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-E4E575DA` — Nondeterministic

**Exact statement**

> Provide interoperability and exit paths proportionate to the credential’s expected lifetime.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide interoperability and exit paths proportionate to the credential’s expected lifetime.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-FFB35851` — Nondeterministic

**Exact statement**

> Document jurisdictional, accessibility, privacy, records, and consumer-protection obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document jurisdictional, accessibility, privacy, records, and consumer-protection obligations.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-7F2CC1B2` — Nondeterministic

**Exact statement**

> Verify source evidence and issuer authority before issuance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify source evidence and issuer authority before issuance.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0D3FDB91` — Nondeterministic

**Exact statement**

> Bind the credential to the correct subject or holder using an assurance process appropriate to harm.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Bind the credential to the correct subject or holder using an assurance process appropriate to harm.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-D893965C` — Nondeterministic

**Exact statement**

> Collect and disclose only claims necessary for the credential purpose.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Collect and disclose only claims necessary for the credential purpose.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3911661A` — Nondeterministic

**Exact statement**

> Use stable, governed schemas and unambiguous semantics.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use stable, governed schemas and unambiguous semantics.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-92C1B996` — Nondeterministic

**Exact statement**

> Include issuer, validity, status, version, and intended-use information.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Credential fields are present, while intended-use text is false and the status source is stale.

## `USEQ-2DA81E4D` — Nondeterministic

**Exact statement**

> Protect issuer signing keys using controls proportionate to credential impact and volume.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect issuer signing keys using controls proportionate to credential impact and volume.” depends on ecosystem convention, risk proportionality, practicality, portability, necessity, business fit, or project-specific design with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-BB83CC0C` — Nondeterministic

**Exact statement**

> Separate test and production issuers, keys, identifiers, and trust lists.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record bindings and gates are checkable fragments, but the full compound statement requires complete scope plus semantic, operational, scientific, legal, or human meaning. For this rule, the proposed clause only proves: Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Declared test and production identifiers differ, while both trust lists contain the same production root key.

## `USEQ-C228C307` — Nondeterministic

**Exact statement**

> Prevent duplicate, conflicting, backdated, or unauthorized issuance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent duplicate, conflicting, backdated, or unauthorized issuance.” has checkable uniqueness, timestamp, and authorization rules, but no bounded fixture proves prevention across every concurrent or external issuance path.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_protocol`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, approval, research, issuance, or credential record contains every explicitly required state, signer, scope, owner, and evidence reference.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Compare immutable source, artifact, configuration, evidence-package, key-domain, or credential identities and fail closed on missing or mismatched bindings.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-FA251CC8` — Nondeterministic

**Exact statement**

> Record issuance evidence and audit events without storing unnecessary subject data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Record issuance evidence and audit events without storing unnecessary subject data.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-829899FE` — Nondeterministic

**Exact statement**

> Provide secure correction, renewal, suspension, and revocation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide secure correction, renewal, suspension, and revocation.” has an objectively checkable record, artifact, configuration, protocol, or bounded test, but full pass meaning also depends on semantic completeness, domain scope, or safety outcome.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded release, scope, research, credential, policy, migration, or evidence record explicitly named by this control without treating it as proof of domain suitability or complete coverage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0F08A687` — Nondeterministic

**Exact statement**

> Define behavior after issuer merger, compromise, insolvency, renaming, or retirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The authorized record contains every named field for declared items, while an omitted applicable item, party, or scenario makes the original statement false. Specifically, it can pass while this remains false: Define behavior after issuer merger, compromise, insolvency, renaming, or retirement.

## `USEQ-75AC7967` — Nondeterministic

**Exact statement**

> Ensure credentials remain verifiable for their required lifetime or clearly state limits.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure credentials remain verifiable for their required lifetime or clearly state limits.” has checkable artifact, presentation, preservation, minimization, or cryptographic evidence, but full compliance also depends on legal, privacy, consent, records, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5FC720EB` — Nondeterministic

**Exact statement**

> Notify affected parties when a credential type, issuer key, or trust framework is compromised.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Notify affected parties when a credential type, issuer key, or trust framework is compromised.” has checkable notification records, but proving that every affected party was identified and reached is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-EDD63A64` — Nondeterministic

**Exact statement**

> Protect holder keys and credentials against theft, malware, backup leakage, coercion, and unauthorized synchronization.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect holder keys and credentials against theft, malware, backup leakage, coercion, and unauthorized synchronization.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-382CDFE5` — Nondeterministic

**Exact statement**

> Use strong, accessible holder authentication appropriate to risk.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use strong, accessible holder authentication appropriate to risk.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-C381ED25` — Nondeterministic

**Exact statement**

> Provide secure recovery without enabling account takeover or issuer surveillance.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide secure recovery without enabling account takeover or issuer surveillance.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-AD6C7346` — Nondeterministic

**Exact statement**

> Explain which claims will be presented, to whom, for what purpose, and with what linkability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Disclosure fields name claims and purpose, while a stable identifier enables undeclared cross-verifier linkability.

## `USEQ-9A840858` — Nondeterministic

**Exact statement**

> Obtain meaningful holder authorization before presentation except where lawfully designed otherwise.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Obtain meaningful holder authorization before presentation except where lawfully designed otherwise.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-607BBB3A` — Nondeterministic

**Exact statement**

> Support selective disclosure or data minimization where the ecosystem and threat model permit it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Support selective disclosure or data minimization where the ecosystem and threat model permit it.” has checkable artifact, presentation, preservation, minimization, or cryptographic evidence, but full compliance also depends on legal, privacy, consent, records, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A61D2298` — Nondeterministic

**Exact statement**

> Avoid presenting stable identifiers unnecessarily across unrelated verifiers.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid presenting stable identifiers unnecessarily across unrelated verifiers.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-27926632` — Nondeterministic

**Exact statement**

> Prevent one verifier from silently requesting broader claims than the holder expects.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent one verifier from silently requesting broader claims than the holder expects.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-08205AC1` — Nondeterministic

**Exact statement**

> Make credential status, expiry, issuer, and trust level understandable.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make credential status, expiry, issuer, and trust level understandable.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-7088AFC1` — Nondeterministic

**Exact statement**

> Provide accessible rendering, alternative modalities, and support for assistive technology.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide accessible rendering, alternative modalities, and support for assistive technology.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-2F8BCF2A` — Nondeterministic

**Exact statement**

> Protect credentials during device migration, backup, restoration, loss, and replacement.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect credentials during device migration, backup, restoration, loss, and replacement.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-5B79CEF1` — Nondeterministic

**Exact statement**

> Provide a safe way to remove credentials and revoke wallet access.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide a safe way to remove credentials and revoke wallet access.” has a bounded removal and revocation test, but safety across recovery, synchronization, and legal retention contexts is not fully specified.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-83FE6385` — Nondeterministic

**Exact statement**

> Distinguish locally stored, cloud-synchronized, custodial, and noncustodial key arrangements.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Key arrangement is labeled noncustodial, while the cloud provider can recover and use the key.

## `USEQ-25EBD53E` — Nondeterministic

**Exact statement**

> Disclose material provider access and retention.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Disclose material provider access and retention.” has checkable disclosure fields, but materiality and completeness depend on provider behavior, contract interpretation, and accountable privacy review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A27982C6` — Nondeterministic

**Exact statement**

> Authenticate the verifier or presentation destination when misdirection would create harm.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Authenticate the verifier or presentation destination when misdirection would create harm.” has an objectively checkable record, artifact, configuration, protocol, or bounded test, but full pass meaning also depends on semantic completeness, domain scope, or safety outcome.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded release, scope, research, credential, policy, migration, or evidence record explicitly named by this control without treating it as proof of domain suitability or complete coverage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-AB8D7525` — Nondeterministic

**Exact statement**

> Bind presentations to the intended verifier, session, challenge, purpose, and time where replay is a risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Bind presentations to the intended verifier, session, challenge, purpose, and time where replay is a risk.” has an objectively checkable record, artifact, configuration, protocol, or bounded test, but full pass meaning also depends on semantic completeness, domain scope, or safety outcome.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded release, scope, research, credential, policy, migration, or evidence record explicitly named by this control without treating it as proof of domain suitability or complete coverage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-11A087E3` — Nondeterministic

**Exact statement**

> Validate credential structure, issuer, proof, algorithm, key, status, validity, audience, schema, and required claims.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All registered credential fixtures validate, while a critical duplicate-key encoding is parsed differently and accepted.

## `USEQ-D9F22BC7` — Nondeterministic

**Exact statement**

> Reject unsupported algorithms, ambiguous encodings, malformed proofs, and invalid status information.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Known bad proofs are rejected, while an alternate ambiguous encoding selects an unsupported algorithm.

## `USEQ-676F80FE` — Nondeterministic

**Exact statement**

> Resolve issuer and status data through authenticated, available, privacy-preserving mechanisms.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Resolve issuer and status data through authenticated, available, privacy-preserving mechanisms.” has an objectively checkable record, artifact, configuration, protocol, or bounded test, but full pass meaning also depends on semantic completeness, domain scope, or safety outcome.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded release, scope, research, credential, policy, migration, or evidence record explicitly named by this control without treating it as proof of domain suitability or complete coverage.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C2412308` — Nondeterministic

**Exact statement**

> Prevent presentation replay and cross-context reuse.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Nonce tests pass, while a presentation is replayed across two verifiers that share the same context identifier.

## `USEQ-BBE4D4DE` — Nondeterministic

**Exact statement**

> Verify the holder binding when the credential requires proof of possession or subject control.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A registered finite protocol does not establish an exhaustive input and state population, complete behavioral oracle, or sufficient acceptance threshold. For this rule, the proposed clause only proves: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Proof-of-possession fixtures pass, while a delegated key is accepted without binding it to the credential subject.

## `USEQ-95D3A5B4` — Nondeterministic

**Exact statement**

> Distinguish cryptographic validity from truth, current eligibility, legal effect, and business authorization.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A complete-looking record cannot prove the named facts, judgments, qualifications, decisions, or scope are true and adequate. For this rule, the proposed clause only proves: Validate the authorized release, research, credential, sign-off, lifecycle, or support record contains every named field, role, state, version, decision, procedure, responsibility, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A record distinguishes cryptographic validity, while business logic still treats a valid signature as current legal eligibility.

## `USEQ-EB08BD56` — Nondeterministic

**Exact statement**

> Apply independent authorization and business rules after credential verification.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Credential verification order can be checked, but complete relying-party paths and the correctness and applicability of independent business rules are not bounded.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The main verifier applies a post-verification authorization rule, while a fallback verifier treats a valid credential as sufficient business authorization.

## `USEQ-F96152CF` — Nondeterministic

**Exact statement**

> Minimize storage of presented credentials and derived identity data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Minimize storage of presented credentials and derived identity data.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-4639163D` — Nondeterministic

**Exact statement**

> Avoid unnecessary verifier–issuer correlation and tracking.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid unnecessary verifier–issuer correlation and tracking.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-BF72D1D6` — Nondeterministic

**Exact statement**

> Provide understandable reasons and appeal or correction paths for rejected credentials.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide understandable reasons and appeal or correction paths for rejected credentials.” requires continuity capacity, idiomatic usability, representative testing, comfort, data interpretation, sensitivity, robustness, visualization quality, or user understanding without a complete protocol and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-845CAC28` — Nondeterministic

**Exact statement**

> Log material verification decisions without recording secrets or excessive personal data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Log material verification decisions without recording secrets or excessive personal data.” has checkable artifact, presentation, preservation, minimization, or cryptographic evidence, but full compliance also depends on legal, privacy, consent, records, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-930768B8` — Nondeterministic

**Exact statement**

> Test expired, revoked, suspended, unknown, conflicting, offline, and compromised-issuer scenarios.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Named scenario fixtures do not define all combinations, correct relying-party outcomes, issuer states, offline freshness, or acceptance thresholds.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

One fixture for each label passes, while an offline verifier accepts a revoked credential because its stale status cache is treated as authoritative.

## `USEQ-CE0AAADE` — Nondeterministic

**Exact statement**

> Define the intended legal, evidentiary, and business effect of each signature, seal, or timestamp.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Define the intended legal, evidentiary, and business effect of each signature, seal, or timestamp.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-DC979C4C` — Nondeterministic

**Exact statement**

> Select identity, key, consent, and ceremony controls proportionate to that effect.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Select identity, key, consent, and ceremony controls proportionate to that effect.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-F3521B8A` — Nondeterministic

**Exact statement**

> Present the exact document, transaction, scope, and consequences before signature.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Present the exact document, transaction, scope, and consequences before signature.” has checkable document and transaction bindings, but complete consequences and user comprehension cannot be established by a bounded interface test.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, versions, identities, expected transitions, negative cases, outputs, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D2D9767A` — Deterministic

**Exact statement**

> Prevent substitution of content between review and signing.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The exact reviewed byte sequence can be identified by an immutable digest and the signing operation can be required to consume that same digest atomically, fully deciding substitution between these two bounded steps.

### Exact deterministic check

1. Prevent substitution of content between review and signing.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

A changed byte, replaced attachment, reordered field, alternate canonical encoding, stale review digest, or post-review swap changes the consumed digest and is rejected before signing.

## `USEQ-3F86DFBF` — Nondeterministic

**Exact statement**

> Bind the signature to the complete intended content and relevant context.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Cryptographic byte binding is objective, but deciding the complete intended content and every relevant legal or transaction context is semantic and scope dependent.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The signature covers the displayed PDF bytes, while an omitted annex and transaction identifier are relevant to the intended agreement but remain unsigned.

## `USEQ-B994594D` — Nondeterministic

**Exact statement**

> Preserve signer intent, authentication evidence, time, certificate status, and validation material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve signer intent, authentication evidence, time, certificate status, and validation material.” has checkable ceremony artifacts, but the signer's actual intent and legal effect require human and legal authority.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Verify every declared artifact exists in the authorized evidence set, has immutable identity and provenance, and contains or links each required digest, version, environment, source, correction, or validation binding.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-3EB6D593` — Nondeterministic

**Exact statement**

> Distinguish human signatures from organizational seals and automated service signatures.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A type field is checkable, but whether a credential represents a natural person's signature, an organizational seal, or an automated service signature has legal and trust-framework meaning.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A record labels a key as human, while it is controlled by an automated corporate service and has the legal effect of an organizational seal.

## `USEQ-C6BF8280` — Nondeterministic

**Exact statement**

> Protect signing keys against unauthorized use and provide rapid revocation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect signing keys against unauthorized use and provide rapid revocation.” makes a universal safety, non-disclosure, abuse-prevention, hidden-state, leakage, key-protection, or failure-absence claim that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-6E1D8851` — Nondeterministic

**Exact statement**

> Use trusted time sources and preserve timestamp-chain validation where long-term proof matters.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use trusted time sources and preserve timestamp-chain validation where long-term proof matters.” has checkable artifact, presentation, preservation, minimization, or cryptographic evidence, but full compliance also depends on legal, privacy, consent, records, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E31E1530` — Nondeterministic

**Exact statement**

> Support long-term validation through archival evidence, algorithm migration, and preserved status information.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Support long-term validation through archival evidence, algorithm migration, and preserved status information.” has a separable record or technical-evidence requirement, but completeness, materiality, suitability, readiness, interpretation, or adequacy still requires accountable human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined domain population, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0A67A96B` — Nondeterministic

**Exact statement**

> Make the signing process accessible and provide equivalent review of all content.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make the signing process accessible and provide equivalent review of all content.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-FC05CB3D` — Nondeterministic

**Exact statement**

> Avoid forcing consent to unrelated terms as part of a required signature ceremony.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid forcing consent to unrelated terms as part of a required signature ceremony.” depends on scientific, statistical, legal, clinical, safety, accessibility, privacy, identity, cryptographic-assurance, or consumer-protection authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-4CD7BD25` — Nondeterministic

**Exact statement**

> Define procedures for disputed signatures, compromised keys, corrected documents, and superseded agreements.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Procedure headings are observable, but adequate handling of disputes, compromised keys, corrected documents, and superseded agreements requires legal authority and context-specific remedies.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

All four procedure sections exist, but a disputed signature procedure destroys evidence and fails to notify a relying party bound by the superseded agreement.

## `USEQ-E8034D11` — Nondeterministic

**Exact statement**

> Verify exported or printed representations do not misstate signature validity.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Approved status fixtures do not exhaust export formats, print paths, verifier states, or what presentation would materially misstate to a relying party.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Tested PDF and paper outputs match the verifier, while an offline QR export displays valid after the credential is revoked.

## `USEQ-E071C3CF` — Nondeterministic

**Exact statement**

> Perform privacy and human-rights assessment for credential ecosystems that can enable tracking, exclusion, or surveillance.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Privacy and human-rights effects of tracking, exclusion, and surveillance require qualified legal, social, and affected-person assessment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-79ED6733` — Nondeterministic

**Exact statement**

> Avoid central logs of every credential presentation unless strictly necessary and authorized.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Central logging and authorization records are objective, but strict necessity requires privacy-purpose and proportionality judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every central credential-presentation log has an explicit scoped authorization and necessity decision record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4D4D7C2A` — Nondeterministic

**Exact statement**

> Limit verifier requests to claims necessary for a declared purpose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Declared purpose and requested-claim sets are checkable, but necessity of each claim requires privacy and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every verifier request contains only claims listed in its approved declared-purpose mapping.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B8DDA406` — Nondeterministic

**Exact statement**

> Prevent status checks from revealing holder activity unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Status-request fields and correlation tests are objective, but unnecessary holder-activity disclosure requires privacy threat judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every status-check protocol passes its approved holder-correlation and activity-disclosure tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-BCE7A5A9` — Nondeterministic

**Exact statement**

> Provide alternatives for people without compatible devices, stable connectivity, official documents, biometrics, or particular abilities.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Alternative-path presence and execution are objective, but whether alternatives adequately include people with each constraint requires accessibility and rights judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Documented alternative paths exist and execute without requiring a compatible device, stable connectivity, official document, biometric, or excluded ability.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9CEB51E5` — Nondeterministic

**Exact statement**

> Ensure loss of a device or credential does not permanently exclude a legitimate person.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recovery behavior and permanent-denial states can be tested, but legitimate identity and adequate recovery safeguards require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. A registered legitimate-holder fixture can recover after device or credential loss without entering a permanent-denial state.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-F3DD21EB` — Nondeterministic

**Exact statement**

> Provide accessible enrollment, presentation, recovery, support, and dispute processes.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Accessibility of enrollment, presentation, recovery, support, and disputes requires representative disabled-user and expert evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

## `USEQ-C016832D` — Nondeterministic

**Exact statement**

> Test names, scripts, addresses, family structures, and identity situations across intended populations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Executed cases are objective, but intended-population scope and representative names, scripts, households, and identity situations require research judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every population case approved in the identity test plan has executed name, script, address, family-structure, and identity-situation tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-271DE6C6` — Nondeterministic

**Exact statement**

> Prevent automated decisions from treating inability to present one credential format as evidence of ineligibility when alternatives are required.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Which alternatives are required and what constitutes eligibility are legal and policy judgments, and complete decision-path coverage is not bounded.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

The tested wallet format falls back correctly, while an applicant without that wallet is denied despite a legally required paper alternative.

## `USEQ-DA631250` — Nondeterministic

**Exact statement**

> Publish understandable information about data flows, retention, trust, status, and remedies.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Understandability of data-flow, retention, trust, status, and remedy information requires audience research, accessibility, and legal review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

## `USEQ-A48D2BAD` — Nondeterministic

**Exact statement**

> Inventory algorithms, keys, certificates, identifiers, proofs, status mechanisms, registries, and trust anchors used by the ecosystem.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Registered cryptographic assets are objective, but proving discovery of every identifier, proof, registry, and trust anchor is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every discovered algorithm, key, certificate, identifier, proof, status mechanism, registry, and trust anchor has an inventory entry.
   - **Checker family:** `inventory_fact`
   - **Evidence authority:** `repository`

## `USEQ-FD3F890D` — Nondeterministic

**Exact statement**

> Define migration before an algorithm, format, provider, registry, or issuer becomes obsolete.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Dates and plan records are checkable, but complete obsolescence inventory, advance sufficiency, and approval validity are not.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

Every registered issuer with an end date has a prior plan, while an unregistered dependency becomes obsolete and strands credentials.

## `USEQ-FB095D6A` — Nondeterministic

**Exact statement**

> Support key rotation without invalidating legitimate credentials unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rotation and credential-validation tests are objective, but legitimacy and unnecessary invalidation require policy and rights judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Key-rotation tests preserve every credential fixture marked valid under the approved transition policy.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5E18FC51` — Nondeterministic

**Exact statement**

> Test compromise, revocation, offline verification, registry outage, wallet loss, and issuer retirement.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One test per named scenario cannot cover combinations, timings, status states, relying parties, or define correct recovery outcomes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All six fixtures pass separately, while issuer retirement during registry outage causes an offline verifier to accept a revoked credential.

## `USEQ-BEB1A131` — Nondeterministic

**Exact statement**

> Preserve evidence needed to validate long-lived credentials and signatures after ordinary services change.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Future service changes and the evidence required for all long-lived credential types cannot be completely enumerated or validated by present fixtures.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Fixtures survive a simulated outage, while retirement of the timestamp authority makes a decade-old signature unverifiable.

## `USEQ-942AB317` — Nondeterministic

**Exact statement**

> Prevent downgrade to weaker proof or status behavior.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Every downgrade attempt spans encodings, negotiation paths, algorithms, status modes, and future attacker strategies without a complete formal state space.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `undefined_protocol`

### Skeptical rejection counterexample

Known algorithm downgrades are rejected, while an alternate ambiguous encoding selects a weaker status check.

## `USEQ-1B9B5CDF` — Nondeterministic

**Exact statement**

> Verify independent implementations interoperate against the declared profile.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Two implementations passing selected flows does not prove full profile interoperability, independence, error semantics, extensions, and boundary behavior.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Both implementations pass ordinary issuance and presentation, while they canonicalize duplicate claims differently and reject each other's edge-case credential.

## `USEQ-137674FC` — Nondeterministic

**Exact statement**

> Perform security, privacy, accessibility, usability, conformance, and abuse testing.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Evidence that six test categories ran cannot prove representative scope, correct oracles, qualified review, or acceptable results.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Each category has a completed report, while keyboard users cannot complete presentation and the accessibility test never exercises that flow.

## `USEQ-48D94432` — Nondeterministic

**Exact statement**

> Monitor issuance anomalies, verification failures, replay, fraud, mass correlation, and support burden.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Metric-series presence does not establish complete ecosystem coverage, meaningful baselines, thresholds, or detection of all named harms.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All six dashboards exist, while cross-wallet mass correlation is untagged and remains invisible.

## `USEQ-07A28D8C` — Nondeterministic

**Exact statement**

> Maintain incident coordination among issuers, wallets, verifiers, trust services, and governance authorities.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Contacts and an exercise record cannot prove every relevant party is included or that coordination is timely, authorized, and effective in a real incident.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The plan lists five party types and a tabletop, but the issuer's contact lacks revocation authority during an actual compromise.

## `USEQ-87B20C17` — Nondeterministic

**Exact statement**

> Treat ambiguous semantics, unverifiable status, inaccessible presentation, compromised issuer keys, or missing recovery as no-go conditions when material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** No-go gate configuration is objective, but materiality of ambiguous semantics or assurance failures requires accountable specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The release gate blocks every ambiguous-semantics, unverifiable-status, inaccessible-presentation, compromised-key, or missing-recovery condition marked material.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `repository`
