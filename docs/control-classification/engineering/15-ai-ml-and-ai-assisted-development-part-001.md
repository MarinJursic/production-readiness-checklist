<!-- Generated by scripts/build_control_classification_docs.py. Do not edit by hand. -->

# `docs/engineering/15-ai-ml-and-ai-assisted-development.md` — classification reference, part 1 of 1

[Back to the classification index](../README.md).

Controls: **632** · Deterministic: **19** · Nondeterministic: **613**

## `USEQ-2C5A990D` — Nondeterministic

**Exact statement**

> Inventory every model, model version, provider, prompt, instruction template, tool, data source, retrieval system, embedding store, safety component, and evaluation set.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Registered component coverage is checkable, but proving discovery of every dynamic prompt, tool, data source, and safety component is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every discovered model, version, provider, prompt, instruction, tool, data source, retrieval system, embedding store, safety component, and evaluation set has an inventory entry.
   - **Checker family:** `inventory_fact`
   - **Evidence authority:** `repository`

## `USEQ-08ACDAA3` — Nondeterministic

**Exact statement**

> Classify the consequences of incorrect, biased, unsafe, manipulated, unavailable, or overly confident output.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Consequences of incorrect, biased, unsafe, manipulated, unavailable, or overconfident output require use-case risk and stakeholder judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-32B13D60` — Nondeterministic

**Exact statement**

> Model instructions cannot override authorization, privacy, safety, compliance, or business rules.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** External policy enforcement can be tested, but universal resistance to arbitrary model instructions is an unbounded adversarial claim.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every privileged action path enforces authorization, privacy, safety, compliance, and business rules outside model instructions.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-197BC632` — Nondeterministic

**Exact statement**

> The model cannot choose credentials, tenants, resources, recipients, permissions, or financial destinations beyond the user's authorization.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every model-initiated privileged action is rejected when its target or authority is outside the authenticated user's grant.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The checked API denies an unauthorized tenant, but an uninstrumented tool sends funds to an unauthorized recipient.

## `USEQ-BC3602E0` — Nondeterministic

**Exact statement**

> Model output is validated before use in code, queries, commands, templates, transactions, identity decisions, or security controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every registered code, query, command, template, transaction, identity, and security-control sink is preceded by an approved output validator.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

JSON schema validation passes a syntactically valid query that updates the wrong tenant's rows.

## `USEQ-96538752` — Nondeterministic

**Exact statement**

> Sensitive data is excluded from prompts, retrieval, fine-tuning, evaluation, and training unless specifically approved.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Approval and data-flow checks are objective for classified data, but complete sensitivity discovery and approval adequacy require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every data item classified as sensitive is excluded from AI lifecycle paths unless it has an explicit scoped approval.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `repository`

## `USEQ-0D090FAC` — Nondeterministic

**Exact statement**

> Provider retention, logging, human-review, model-training, and data-location terms are understood.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Understanding provider retention, review, training, logging, and location terms requires accountable legal and privacy interpretation.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`

## `USEQ-A518D322` — Nondeterministic

**Exact statement**

> Vector stores and embeddings preserve tenant boundaries, retention, deletion, and access changes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every registered vector or embedding store passes tenant-isolation, retention-expiry, deletion-propagation, and revoked-access tests.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The main vector index deletes a user, but a forgotten cache replica still serves the vectors cross-tenant.

## `USEQ-DC5087FC` — Nondeterministic

**Exact statement**

> Retrieval poisoning, malicious documents, data contamination, model poisoning, and supply-chain compromise are addressed.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequately addressing poisoning, contamination, model compromise, and supply-chain threats requires system-specific security threat modeling.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `unbounded_scope`

## `USEQ-A85B7AA6` — Nondeterministic

**Exact statement**

> Training, fine-tuning, evaluation, prompt, and model data have provenance and governance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Provenance records are objective, but adequate governance for all AI data uses requires legal, privacy, and organizational judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered training, fine-tuning, evaluation, prompt, and model data set has versioned provenance and an owner.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2E49E2AA` — Nondeterministic

**Exact statement**

> Model, prompt, tool, retrieval, and safety changes are versioned, reviewed, tested, and approved.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every model, prompt, tool, retrieval, or safety change has a version, review, test result, and authorized approval.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Every change has a version and approval checkbox, but an untested retrieval route leaks secrets.

## `USEQ-296AC402` — Nondeterministic

**Exact statement**

> Evaluation sets cover accuracy, reliability, security, safety, privacy, bias, fairness, refusal, adversarial behavior, and misuse as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Evaluation executions are objective for an approved applicability map, but complete applicable risk coverage requires expert judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every evaluation dimension marked applicable has a versioned evaluation set and current result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C203537F` — Nondeterministic

**Exact statement**

> Evaluations represent production languages, user groups, data distributions, and edge cases.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Representativeness across production languages, groups, distributions, and edge cases needs a sampling protocol and coverage thresholds not provided.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `undefined_scope`

## `USEQ-0B71A9C7` — Nondeterministic

**Exact statement**

> Hallucination, uncertainty, sources, and limitations are communicated appropriately.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Appropriate communication of hallucination, uncertainty, sources, and limitations depends on audience, consequence, interface, and evidence context.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-26AC7838` — Nondeterministic

**Exact statement**

> High-impact decisions have suitable human review, explanation, appeal, and correction paths.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** High impact, suitable human review, adequate explanation, appeal, and correction require legal, domain, and human-factors judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-E48B3005` — Nondeterministic

**Exact statement**

> Output moderation and abuse detection match product risk.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Matching moderation and abuse detection to product risk requires threat modeling, legal policy, and harm-specific judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-009D08C1` — Nondeterministic

**Exact statement**

> Model denial of service, context exhaustion, tool loops, recursive calls, and cost amplification are bounded.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Model denial-of-service, context use, tool loops, recursive calls, and cost amplification stay within approved numeric limits.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Top-level token and loop caps pass, but delegated parallel tools create an unbounded bill.

## `USEQ-D7B17CB8` — Nondeterministic

**Exact statement**

> Rate, token, context, tool, concurrency, and spending limits are configured.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every model endpoint has configured rate, token, context, tool, concurrency, and spending limits.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The public inference endpoint has limits, but an unlisted batch endpoint has no spending cap.

## `USEQ-C9282DB5` — Nondeterministic

**Exact statement**

> Model and provider outages have tested fallback behavior.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Every model and provider dependency has a current passing outage and fallback-behavior test.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A simulated HTTP 503 falls back, but a DNS stall hangs forever without fallback.

## `USEQ-EF076436` — Nondeterministic

**Exact statement**

> Model drift, quality regression, safety regression, and provider behavior changes are monitored.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Production monitoring tracks model drift, quality regression, safety regression, and provider behavior changes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Average accuracy is monitored, but a severe safety regression in one language is invisible.

## `USEQ-6D4A4F4E` — Nondeterministic

**Exact statement**

> AI telemetry protects user privacy and does not unnecessarily record sensitive prompts or outputs.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Privacy protection and necessity of recorded prompts or outputs require data-purpose, minimization, and threat judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-701EF00D` — Nondeterministic

**Exact statement**

> Logs record model, prompt, tool, retrieval, and policy versions sufficiently for investigation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Version fields are checkable, but whether they are sufficient for every future investigation depends on incident context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every AI request log records model, prompt, tool, retrieval, and policy versions.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-1F35FBC7` — Nondeterministic

**Exact statement**

> Users know when they are interacting with or materially affected by AI where required.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** When AI disclosure is required and what users need to understand depend on law, policy, safety, and product context.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-0C2884C7` — Nondeterministic

**Exact statement**

> Applicable transparency, documentation, risk-assessment, copyright, data, human-oversight, and automated-decision rules are mapped.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Determining applicable AI transparency, copyright, oversight, automated-decision, and data rules requires legal and sector expertise.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-E5718654` — Nondeterministic

**Exact statement**

> Retirement removes obsolete models, credentials, prompts, indexes, retained data, and tools safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every retired capability removes its registered models, credentials, prompts, indexes, retained data, and tools and passes a residual-reference test.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The registry entry and key are removed, but a retrieval cache keeps serving regulated data.

## `USEQ-C529B9D3` — Nondeterministic

**Exact statement**

> Apply this checklist whenever a learned, probabilistic, generative, ranking, recommendation, classification, optimization, forecasting, biometric, or autonomous component materially influences product behavior or engineering work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a probabilistic component materially influences behavior or engineering work requires product and risk context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-814E1CE9` — Nondeterministic

**Exact statement**

> Maintain a current inventory of models, model versions, prompts, system instructions, agents, tools, retrieval sources, embeddings, feature stores, training pipelines, evaluation sets, safety systems, providers, deployment endpoints, and downstream consumers.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Current registered coverage is objective, but proving discovery of every downstream consumer, prompt, agent, and data component is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every discovered AI asset in the listed component classes has a current inventory entry.
   - **Checker family:** `inventory_fact`
   - **Evidence authority:** `repository`

## `USEQ-FD2B24B1` — Nondeterministic

**Exact statement**

> Assign accountable business, product, model, data, security, privacy, safety, legal, compliance, accessibility, operations, and human-oversight owners according to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Owner assignments are checkable, but which roles are required according to impact needs accountable risk and legal judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every role marked required in an AI impact assessment has a named accountable owner.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-74CF2922` — Nondeterministic

**Exact statement**

> Classify each system by affected users, decision consequence, reversibility, autonomy, scale, sensitivity, legal status, abuse potential, and dependence on probabilistic output.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Classification fields are objective, but assigning consequence, sensitivity, legal status, abuse, and autonomy levels requires accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every AI system records affected users, consequence, reversibility, autonomy, scale, sensitivity, legal status, abuse potential, and probabilistic dependence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-FAD9068A` — Nondeterministic

**Exact statement**

> Document the intended purpose, excluded purposes, users, affected parties, operating conditions, expected benefit, assumptions, limitations, prohibited actions, and retirement criteria.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every AI system documents intended purpose, excluded purposes, users, affected parties, conditions, benefit, assumptions, limitations, prohibited actions, and retirement criteria.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every required heading exists, but the document omits children affected by the system.

## `USEQ-7E87BDD7` — Nondeterministic

**Exact statement**

> Define an AI risk-management lifecycle covering design, acquisition, data, development, evaluation, deployment, monitoring, incident response, change, and retirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: The AI risk-management lifecycle covers design, acquisition, data, development, evaluation, deployment, monitoring, incident response, change, and retirement.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A policy lists every lifecycle phase, but acquired third-party models bypass incident handling.

## `USEQ-F7AD4215` — Nondeterministic

**Exact statement**

> Require proportionately independent review for high-impact, safety-related, rights-affecting, financial, employment, education, healthcare, identity, law-enforcement, or public-sector uses.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** High-impact scope and proportionate independence across sensitive domains require legal, safety, and governance judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-823F07DE` — Nondeterministic

**Exact statement**

> Map applicable laws, sector rules, contracts, organizational policies, standards, licenses, intellectual-property rights, data-use commitments, and provider terms before development or procurement.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Mapping applicable laws, rights, licenses, contracts, and provider terms requires qualified legal and compliance interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-D63FB3F4` — Nondeterministic

**Exact statement**

> Maintain an AI system impact assessment that considers direct, indirect, cumulative, group, societal, environmental, accessibility, labor, and downstream effects.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Impact category coverage is checkable, but complete direct, indirect, cumulative, societal, and downstream effects are unbounded and judgment-heavy.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The current AI impact assessment addresses direct, indirect, cumulative, group, societal, environmental, accessibility, labor, and downstream effects.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-6A97D822` — Nondeterministic

**Exact statement**

> Identify people who may be affected without being direct users and provide appropriate protection, notice, recourse, and monitoring.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Identifying indirectly affected people and appropriate protection, notice, recourse, and monitoring requires stakeholder and legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-D8BEBA66` — Nondeterministic

**Exact statement**

> Define risk appetite, unacceptable outcomes, stop conditions, escalation thresholds, approval authorities, and residual-risk owners before production use.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Before production, the system defines risk appetite, unacceptable outcomes, stop conditions, escalation thresholds, approval authorities, and residual-risk owners.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Risk fields are filled, but an unauthorized employee owns residual risk and unsafe stop limits.

## `USEQ-96B9FB18` — Nondeterministic

**Exact statement**

> Separate responsibility for building, evaluating, approving, operating, auditing, and accepting material AI risk where independence is required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Role separation can be checked, but where independence is required and which risks are material need governance and legal judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every activity marked independence-required assigns distinct builders, evaluators, approvers, operators, auditors, and risk acceptors.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B8096925` — Nondeterministic

**Exact statement**

> Keep a traceable decision record for model selection, data selection, evaluation thresholds, safety controls, launch, exceptions, and material changes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Traceable records cover model selection, data selection, thresholds, safety controls, launch, exceptions, and material changes.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The decision log exists, but quietly omits the failed safety result that changed launch.

## `USEQ-DABACCB2` — Nondeterministic

**Exact statement**

> Review the AI risk assessment whenever the purpose, population, geography, data, provider, model, prompt, tool, autonomy, or downstream use changes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every registered purpose, population, geography, data, provider, model, prompt, tool, autonomy, or downstream-use change has a later AI risk review.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

A provider version change triggers review, while a silent population shift creates unchecked harm.

## `USEQ-8867EC56` — Nondeterministic

**Exact statement**

> Prevent an experimental label from being used to bypass security, privacy, accessibility, safety, legal, documentation, or reliability requirements when real users or production data are involved.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: An experimental capability involving real users or production data cannot bypass registered security, privacy, accessibility, safety, legal, documentation, or reliability gates.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Production flags prevent bypass, but real patient data in staging receives no privacy review.

## `USEQ-B399DE71` — Nondeterministic

**Exact statement**

> Provide governance for shadow AI use, unregistered models, personal accounts, browser extensions, local models, and unsanctioned data uploads.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Governance defines controls for shadow AI, unregistered models, personal accounts, browser extensions, local models, and unsanctioned uploads.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The approved registry is clean while an employee uploads customer data through a personal extension.

## `USEQ-AC261112` — Nondeterministic

**Exact statement**

> Define who can pause, disable, roll back, isolate, or permanently retire an AI capability during an incident.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: The AI incident plan identifies authorized people for pause, disable, rollback, isolation, and permanent retirement.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A runbook names a responder who lacks permission to disable the provider during an incident.

## `USEQ-041598F3` — Nondeterministic

**Exact statement**

> Use AI only where its demonstrated benefit exceeds the risks and a simpler deterministic method is not clearly more suitable.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Demonstrated benefit, risk balance, and suitability of a simpler deterministic method require accountable product and architecture judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-0D09CDA5` — Nondeterministic

**Exact statement**

> Do not delegate a decision merely because data and a model are available; establish a legitimate purpose and decision owner.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Each AI-delegated decision has an approved purpose and named decision owner.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A purpose string and owner exist, but the processing has no legitimate or lawful basis.

## `USEQ-FB01B22D` — Nondeterministic

**Exact statement**

> Define which outputs are advisory, draft, ranked, automated, determinative, or capable of triggering tools and side effects.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every AI output is classified advisory, draft, ranked, automated, determinative, or tool/side-effect capable.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Output is labeled advisory, but downstream automation treats its ranking as a final denial.

## `USEQ-7D39EBC4` — Nondeterministic

**Exact statement**

> Enforce purpose and authority outside the model so prompts or outputs cannot expand the system's permitted role.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every model-initiated action outside its approved purpose or authenticated authority is rejected by trusted code.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The main proxy enforces scopes, while a plugin follows a model URL into an unauthorized resource.

## `USEQ-20555083` — Nondeterministic

**Exact statement**

> Identify decisions that require a human, dual control, professional judgment, due process, or a deterministic rule.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Which decisions require humans, dual control, professional judgment, due process, or deterministic rules is a legal and risk decision.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-2FC26B87` — Nondeterministic

**Exact statement**

> Keep irreversible, destructive, safety-critical, rights-affecting, financial, legal, and externally binding actions behind independently validated controls and appropriate confirmation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Independent controls are testable for classified actions, but impact classification and appropriate confirmation require legal and safety judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every action classified as irreversible, destructive, safety-critical, rights-affecting, financial, legal, or binding passes its independent-control and confirmation tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-8521E67B` — Nondeterministic

**Exact statement**

> Define confidence, uncertainty, abstention, escalation, and fallback behavior for each material task.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Behavior fields are checkable, but material tasks and appropriate thresholds require accountable domain judgment.
- **Nondeterministic reason codes:** `human_accountability`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every task classified as material defines confidence, uncertainty, abstention, escalation, and fallback behavior.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-48E1F506` — Nondeterministic

**Exact statement**

> Prefer refusal, safe degradation, or handoff when inputs are outside the validated operating domain.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Operating-domain rejection tests are objective, but safe degradation and correct domain boundaries require contextual evaluation.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Inputs outside the declared operating domain trigger a configured refusal, degradation, or handoff path.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-99E4E2DD` — Nondeterministic

**Exact statement**

> Ensure the system does not present generated content, predictions, classifications, or recommendations as established facts beyond available evidence.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether generated statements overclaim established fact beyond evidence requires semantic, factual, and domain-authority review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-4549C1EB` — Nondeterministic

**Exact statement**

> Prevent conversational fluency, personalization, anthropomorphism, emotional language, or interface design from overstating capability, authority, consciousness, confidentiality, or professional qualification.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Overstated capability, authority, consciousness, confidentiality, or qualification depends on language, interface, audience, and consumer-protection judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-4820BFF1` — Nondeterministic

**Exact statement**

> Disclose material AI involvement when users need it to make an informed choice or when law, policy, contract, or safety requires it.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Material involvement, informed choice, and disclosure duties require legal, safety, and audience-specific judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-A79E9D12` — Nondeterministic

**Exact statement**

> Provide a non-AI or assisted path where exclusion, accessibility, due process, or essential-service access requires one.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** When exclusion, accessibility, due process, or essential-service access requires an alternative is a legal and human-rights determination.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-34C36FCB` — Nondeterministic

**Exact statement**

> Make material decisions contestable and correctable, with an understandable route to human review where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Material decisions, understandable recourse, and appropriate human review require legal, accessibility, and user-experience judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-22AA7070` — Nondeterministic

**Exact statement**

> Do not infer or use highly sensitive traits unless necessity, legality, validity, proportionality, and protection have been established.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Necessity, legality, validity, proportionality, and protection for sensitive-trait inference require qualified legal and domain review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-26F1B463` — Nondeterministic

**Exact statement**

> Do not repurpose outputs for a materially different decision without a new impact assessment and validation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Assessment and validation records are objective, but materially different repurposing requires accountable context and legal interpretation.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every output repurposing event marked materially different has a new impact assessment and validation result.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3FA42F8C` — Nondeterministic

**Exact statement**

> Prevent secondary users from treating an output as valid for populations, contexts, languages, or decisions outside the documented scope.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Preventing every secondary user from applying outputs beyond population, language, context, or decision scope is not fully observable or bounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`

## `USEQ-9C03D06C` — Nondeterministic

**Exact statement**

> Define the acceptable frequency and impact of false positives, false negatives, ranking errors, hallucinations, refusals, and harmful recommendations by use case.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Every use case defines acceptable frequency and impact thresholds for false positives, false negatives, ranking errors, hallucinations, refusals, and harmful recommendations.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

Error thresholds are documented, but the accepted false-negative rate is unsafe for medical triage.

## `USEQ-89A25C8A` — Nondeterministic

**Exact statement**

> Require safer defaults for children, vulnerable people, coercive contexts, and users who may mistake the system for a human or authority.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Identifying vulnerable or coercive contexts and selecting safer defaults requires safeguarding, legal, and human-factors judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-C65B1084` — Nondeterministic

**Exact statement**

> Document the origin, ownership, license, consent, collection context, allowed purposes, geographic constraints, sensitivity, quality, representation, and retention of every material data source.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Required metadata are objective, but material source scope, rights, quality, representation, and consent validity require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every data source classified as material records origin, owner, license, consent, context, purposes, geography, sensitivity, quality, representation, and retention.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4F1A4158` — Nondeterministic

**Exact statement**

> Verify that training, fine-tuning, retrieval, evaluation, logging, feedback, and monitoring uses are authorized independently rather than assuming one permission covers all uses.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Separate authorization records are checkable, but validity and sufficiency of each authorization require legal and privacy expertise.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Training, fine-tuning, retrieval, evaluation, logging, feedback, and monitoring each have an independent authorization record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-480594DD` — Nondeterministic

**Exact statement**

> Exclude data that was unlawfully obtained, contractually prohibited, inappropriately sensitive, materially inaccurate, or incapable of required deletion and governance.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Lawfulness, contractual prohibition, inappropriate sensitivity, material inaccuracy, and governance capability require legal and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-C72C1169` — Nondeterministic

**Exact statement**

> Minimize personal, confidential, copyrighted, regulated, and security-sensitive data throughout the AI lifecycle.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Minimizing personal, confidential, copyrighted, regulated, and security data requires purpose, legal, and threat-model judgment across an unbounded lifecycle.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `contextual_judgment`

## `USEQ-9E30F902` — Nondeterministic

**Exact statement**

> Use privacy-preserving, synthetic, aggregated, pseudonymized, or deidentified data where it can meet the validated objective with less risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether privacy-preserving alternatives meet the validated objective with less risk requires empirical comparison and accountable privacy judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-23095819` — Nondeterministic

**Exact statement**

> Assess whether deidentification remains effective after linkage, embedding, memorization, model access, retrieval, and output generation.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Effective deidentification after linkage, embedding, memorization, retrieval, and generation requires privacy attack evaluation and expert judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`

## `USEQ-7C20FBBF` — Nondeterministic

**Exact statement**

> Preserve data lineage from raw source through filtering, labeling, transformation, splitting, augmentation, training, evaluation, and deployment.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every deployed data artifact has an unbroken provenance path to raw sources through filtering, labeling, transformation, splitting, augmentation, training, and evaluation.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Tracked pipeline stages have lineage, but a manual training export enters production without provenance.

## `USEQ-210D4834` — Nondeterministic

**Exact statement**

> Record dataset, label, feature, prompt, retrieval-corpus, and preprocessing versions in every reproducible experiment and release.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every reproducible experiment and release records dataset, label, feature, prompt, retrieval corpus, and preprocessing versions.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Registered runs record versions, while an unregistered notebook creates the released prompt.

## `USEQ-A53F8A85` — Nondeterministic

**Exact statement**

> Define data-quality requirements for relevance, representativeness, completeness, accuracy, timeliness, consistency, duplication, contamination, and labeling reliability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every material dataset defines requirements for relevance, representation, completeness, accuracy, timeliness, consistency, duplication, contamination, and labeling reliability.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every quality dimension is named, but sampling and freshness limits do not fit the live population.

## `USEQ-839FD21F` — Nondeterministic

**Exact statement**

> Measure coverage across intended languages, regions, demographics, disabilities, devices, domains, edge conditions, and rare but consequential cases.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every intended language, region, demographic, disability, device, domain, edge condition, and rare consequential case has a recorded coverage measure.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

One sample exists per named group, but a rare disability-language combination fails.

## `USEQ-3A16FE4E` — Nondeterministic

**Exact statement**

> Investigate sampling, measurement, historical, survivorship, selection, label, annotation, and feedback-loop bias.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Investigating eight forms of bias requires statistical, historical, domain, and societal interpretation rather than a universal binary test.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-F5094424` — Nondeterministic

**Exact statement**

> Use qualified annotators, documented instructions, disagreement handling, quality sampling, worker protection, and fair labor practices for human labeling.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Instructions and quality sampling records are objective, but qualification, worker protection, and fair labor require legal and ethical judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every human-labeling project records annotators, instructions, disagreement process, and quality-sampling results.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4CEB862D` — Nondeterministic

**Exact statement**

> Separate training, validation, test, red-team, and audit sets sufficiently to prevent leakage and optimistic evaluation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Exact and near-duplicate checks are objective, but sufficient separation and leakage risk across all evaluation behavior require context.
- **Nondeterministic reason codes:** `undefined_threshold`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Training, validation, test, red-team, and audit sets have distinct identifiers and pass approved overlap and near-duplicate thresholds.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `artifact`

## `USEQ-49A7E146` — Nondeterministic

**Exact statement**

> Detect benchmark contamination, near duplicates, memorized test examples, and evaluation-set tuning.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Approved detectors yield objective findings, but complete contamination, memorization, and tuning discovery is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every finding from approved contamination, near-duplicate, memorization, and evaluation-tuning detectors is resolved or excepted.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `artifact`

## `USEQ-7F84BAAD` — Nondeterministic

**Exact statement**

> Protect data pipelines, labeling systems, feature stores, retrieval stores, and feedback channels against poisoning, unauthorized change, and provenance loss.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequate protection of diverse AI data systems against poisoning, unauthorized change, and provenance loss requires threat modeling and security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

## `USEQ-F61A6BFD` — Nondeterministic

**Exact statement**

> Record deletions, corrections, consent withdrawals, legal holds, and source restrictions, and propagate them to derived datasets and retrieval systems as required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Recorded events and propagation tests are objective, but which derived stores are required depends on legal and lineage interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered deletion, correction, withdrawal, hold, or restriction propagates to all stores in its approved lineage graph.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-41415588` — Nondeterministic

**Exact statement**

> Define whether and how a trained artifact can honor deletion, unlearning, suppression, retraining, or future exclusion obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The statement requires legal or specialist interpretation and accountable approval that cannot be reduced to a bounded file, schema, or test result. The proposed clause only establishes: Every trained artifact defines supported deletion, unlearning, suppression, retraining, and future-exclusion behavior.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A deletion plan says retrain, but retained checkpoints still reveal the deleted subject's data.

## `USEQ-5B1BC6E2` — Nondeterministic

**Exact statement**

> Prevent prompts, conversations, feedback, traces, and outputs from becoming training data by default unless the purpose, notice, permission, retention, and safeguards permit it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Default training exclusion and approval records are checkable, but sufficiency of notice, permission, retention, and safeguards requires legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Prompts, conversations, feedback, traces, and outputs are excluded from training by default and enabled only by a scoped approval record.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-A764BBDF` — Nondeterministic

**Exact statement**

> Review public or scraped data for access conditions, privacy expectations, robots or contractual restrictions, intellectual-property risk, harmful content, and vulnerable-population impact.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Public-data access, privacy expectations, robots terms, copyright risk, harmful content, and vulnerable impact require legal and ethical review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-E13A6AE0` — Nondeterministic

**Exact statement**

> Document unavoidable data gaps and restrict deployment or confidence claims accordingly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Gap records and deployment restrictions are checkable, but unavoidability and proportionate confidence restrictions require accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every accepted data gap has a record and is linked to explicit deployment or confidence restrictions.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9AE49D07` — Nondeterministic

**Exact statement**

> Inventory every foundation model, adapter, checkpoint, tokenizer, embedding model, reranker, safety model, library, runtime, dataset, evaluation tool, and hosted service in the deployed system.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Registered component entries are objective, but proving discovery of every deployed library, hosted service, data set, and model artifact is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every discovered component in the listed model, data, runtime, evaluation, and service classes has an inventory entry.
   - **Checker family:** `inventory_fact`
   - **Evidence authority:** `repository`

## `USEQ-18F634F5` — Nondeterministic

**Exact statement**

> Record publisher, source, version, immutable digest, license, provenance, release date, support status, known limitations, and approved use for each component.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every registered component records publisher, source, version, immutable digest, license, provenance, release date, support status, limitations, and approved use.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Registry rows have every field, but an embedded tokenizer is missing and use is unsafe.

## `USEQ-CB45E043` — Nondeterministic

**Exact statement**

> Obtain models and artifacts through authenticated, integrity-checked, approved channels.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every model or artifact is admitted only from an approved authenticated source after its digest or signature verifies.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

A verified model passes, but the build downloads an unsigned adapter from an unapproved mirror.

## `USEQ-ED7BBF05` — Nondeterministic

**Exact statement**

> Scan model files and supporting artifacts for unsafe serialization, executable payloads, secrets, malware, unexpected code, and incompatible licenses.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Scanner executions and findings are objective, but complete malware, behavior, secret, and license incompatibility detection is unbounded and partly legal.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every model file and supporting artifact has current approved scans for serialization risk, executables, secrets, malware, unexpected code, and license findings.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `artifact`

## `USEQ-71ADB251` — Nondeterministic

**Exact statement**

> Treat model cards, benchmark claims, certifications, safety reports, and provider assurances as evidence inputs rather than proof of local fitness.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Provider cards, benchmarks, certifications, reports, and assurances cannot satisfy the local-fitness release gate without current local evaluation evidence.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A local report exists but copies vendor benchmarks and never tests the deployed population.

## `USEQ-3F6CA513` — Nondeterministic

**Exact statement**

> Evaluate a provider's security, privacy, data use, retention, training, isolation, availability, change-control, incident, audit, deletion, geographic, support, and exit terms.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Provider assessment fields are objective, but interpreting legal, security, privacy, isolation, audit, and exit adequacy requires specialists.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every provider assessment addresses security, privacy, data use, retention, training, isolation, availability, change control, incidents, audit, deletion, geography, support, and exit.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-27B05EE5` — Nondeterministic

**Exact statement**

> Ensure provider contracts address input and output rights, confidential data, model improvement, subprocessors, breach notification, service changes, deletion, portability, and continuity.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The statement requires legal or specialist interpretation and accountable approval that cannot be reduced to a bounded file, schema, or test result. The proposed clause only establishes: Every provider contract addresses input/output rights, confidential data, improvement use, subprocessors, breach notice, service changes, deletion, portability, and continuity.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

The contract mentions deletion, but a carve-out permits indefinite retention of confidential prompts.

## `USEQ-122CF489` — Nondeterministic

**Exact statement**

> Prevent a provider from changing a model, safety policy, retention behavior, endpoint, price, or material capability without detection and impact review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Monitoring detects model, safety policy, retention, endpoint, price, and capability changes and creates a blocking impact-review record.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Version headers detect model changes, but the provider silently changes retention behavior.

## `USEQ-8B70FE23` — Nondeterministic

**Exact statement**

> Pin model and API versions when stability is required and monitor announced and silent behavior changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Version pins and change monitors are objective, but when stability is required and behavior change is material require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every dependency marked stability-required is version-pinned and has announced and silent behavior-change monitoring.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-FD546F9D` — Nondeterministic

**Exact statement**

> Maintain an alternative, fallback, degraded mode, or orderly shutdown path for critical provider dependence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Fallback-path existence and tests are objective, but critical provider dependence and acceptable degradation require accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every provider dependency classified as critical has a tested alternative, fallback, degraded mode, or orderly shutdown path.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-6B13F795` — Nondeterministic

**Exact statement**

> Evaluate concentration, geopolitical, export-control, sanctions, availability, lock-in, and proprietary-format risks.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Concentration, geopolitical, export-control, sanctions, lock-in, and proprietary-format risks require legal and strategic assessment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-6518E104` — Nondeterministic

**Exact statement**

> Track vulnerabilities and security advisories for models, runtimes, orchestration libraries, data tools, vector stores, accelerators, and inference infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Vulnerability and security-advisory tracking covers models, runtimes, orchestration libraries, data tools, vector stores, accelerators, and inference infrastructure.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Package CVEs are tracked, but an advisory for a separately loaded model artifact is missed.

## `USEQ-CA69E6B9` — Nondeterministic

**Exact statement**

> Require signed or otherwise verifiable release artifacts and maintain a software and model bill of materials proportionate to risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Artifact verification and bill-of-materials generation are objective, but proportionate scope based on risk requires specialist judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every released model artifact has a verifiable signature or digest and a linked software/model bill of materials.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-DFDEDE7C` — Nondeterministic

**Exact statement**

> Control access to model registries and prevent unauthorized replacement, rollback, deletion, promotion, or public publication.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Unauthorized identities cannot replace, roll back, delete, promote, or publicly publish registry artifacts.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The UI denies promotion, but a stale automation credential replaces the production model.

## `USEQ-C493B4D5` — Nondeterministic

**Exact statement**

> Separate experimental, internal, customer-specific, and production models and credentials.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Experimental, internal, customer-specific, and production models and credentials use distinct environment scopes.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Model names are separated, but experimental credentials can still read the production registry.

## `USEQ-6A22F149` — Nondeterministic

**Exact statement**

> Review community models, plugins, tools, skills, prompts, and agent packages as untrusted supply-chain components.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Untrusted classification and review records are objective, but adequacy of supply-chain review for diverse community components requires security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every community model, plugin, tool, skill, prompt, or agent package is marked untrusted and has a review record before use.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7F60FE82` — Nondeterministic

**Exact statement**

> State a falsifiable hypothesis, baseline, target metric, guardrails, population, and decision rule before a material experiment.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Experiment-plan fields are checkable, but materiality and whether a hypothesis is meaningfully falsifiable require research judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every experiment classified as material records a hypothesis, baseline, target metric, guardrails, population, and decision rule before execution.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-73B85E38` — Nondeterministic

**Exact statement**

> Record code, data, model, prompt, hyperparameter, seed, environment, hardware, dependency, compiler, runtime, and configuration versions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every experiment records code, data, model, prompt, hyperparameter, seed, environment, hardware, dependency, compiler, runtime, and configuration versions.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Tracked jobs record all fields, but an unregistered notebook creates the selected artifact.

## `USEQ-F2B8DD90` — Nondeterministic

**Exact statement**

> Make material experiments reproducible within defined tolerances or document why nondeterminism prevents exact reproduction.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Every material experiment reproduces within its predefined tolerances or has an approved explanation of nondeterminism and observed variance.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A rerun meets a loose accuracy tolerance but produces materially different safety failures.

## `USEQ-37A37302` — Nondeterministic

**Exact statement**

> Capture random seeds and sources of nondeterminism without falsely implying that one seed establishes robustness.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Seed and nondeterminism metadata are objective, but whether reporting falsely implies robustness requires semantic judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every stochastic experiment records random seeds and identified sources of nondeterminism.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9D121A7B` — Nondeterministic

**Exact statement**

> Use representative baselines, including a no-model or simpler-model baseline where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Baseline results are objective, but representativeness and relevance of no-model or simpler-model baselines require research judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every experiment has a recorded baseline and every baseline marked relevant has no-model or simpler-model results.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-965D57C2` — Nondeterministic

**Exact statement**

> Prevent test-set and production-outcome leakage into model selection.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Split membership and detected overlap are objective, but complete prevention of outcome leakage through indirect features is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Approved test-set and production-outcome leakage checks pass before model selection.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `artifact`

## `USEQ-675A6565` — Nondeterministic

**Exact statement**

> Predefine primary, secondary, safety, fairness, privacy, cost, and operational metrics.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Before execution, every material experiment defines primary, secondary, safety, fairness, privacy, cost, and operational metrics.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

One metric is predefined per category, but the metric that matters to affected users is omitted.

## `USEQ-EEEFE531` — Nondeterministic

**Exact statement**

> Use sample sizes, uncertainty intervals, repeated runs, statistical tests, and multiple-comparison controls appropriate to the decision.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Appropriate sample size, intervals, repetition, tests, and multiple-comparison controls require statistical expertise tied to the decision.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-7E7D22B7` — Nondeterministic

**Exact statement**

> Report negative, inconclusive, and failed experiments rather than retaining only favorable results.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every executed experiment is reported and labeled favorable, negative, inconclusive, or failed.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The registry reports registered failures, while failed local runs are deleted and only favorable results remain.

## `USEQ-FFA16547` — Nondeterministic

**Exact statement**

> Record experiment lineage and approval so a production artifact can be traced to its evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every production artifact has an unbroken lineage to its experiment results and authorized promotion approval.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

A production digest links to an approved run that excludes a failed safety branch.

## `USEQ-8EA244EE` — Nondeterministic

**Exact statement**

> Review whether offline metrics predict real user outcomes before relying on them.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether offline metrics predict real user outcomes needs a longitudinal validation design, population, and acceptance threshold not supplied here.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

## `USEQ-49F0F65F` — Nondeterministic

**Exact statement**

> Use controlled online experiments only when exposure, consent, monitoring, stopping, rollback, and harm controls are adequate.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequate exposure, consent, monitoring, stopping, rollback, and harm controls for online experiments require legal and safety judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-8EF29710` — Nondeterministic

**Exact statement**

> Prevent an experiment from changing a consequential decision rule without the required approval and notice.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The statement requires legal or specialist interpretation and accountable approval that cannot be reduced to a bounded file, schema, or test result. The proposed clause only establishes: No experiment may activate a consequential decision-rule change without its policy-required approval and notice records.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

### Skeptical rejection counterexample

A signed approval exists, but the changed credit rule receives no legally required notice.

## `USEQ-8D4A14CC` — Nondeterministic

**Exact statement**

> Test interactions among model, prompt, retrieval, tool, policy, UI, latency, and fallback rather than evaluating each in isolation only.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Integrated tests cover model, prompt, retrieval, tool, policy, UI, latency, and fallback interactions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Pairwise tests pass, but retrieval timeout plus UI retry creates an unsafe duplicate action.

## `USEQ-2E44B61A` — Nondeterministic

**Exact statement**

> Re-run affected evaluations after any material data, model, prompt, tool, provider, infrastructure, or policy change.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Change-to-evaluation linkage is objective, but materiality and complete affected-evaluation selection require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change marked material has new results for each evaluation marked affected.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7EB60DD2` — Nondeterministic

**Exact statement**

> Retain enough artifacts and metadata to investigate a later incident without retaining unnecessary sensitive data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Enough incident evidence and unnecessary sensitive retention require security, privacy, legal, and future-incident judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-CB5CDE0E` — Nondeterministic

**Exact statement**

> Define the training objective and verify that optimizing it does not create unacceptable proxy behavior or incentives.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Unacceptable proxy behavior and incentives require causal, domain, ethics, and risk interpretation of the training objective.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-9257FF1A` — Nondeterministic

**Exact statement**

> Validate preprocessing, tokenization, normalization, augmentation, feature generation, and label transformations independently.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Preprocessing, tokenization, normalization, augmentation, feature generation, and label transformation each pass independent validation tests.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Common tokenization tests pass, but a Unicode normalization edge case corrupts labels.

## `USEQ-ED1D1451` — Nondeterministic

**Exact statement**

> Prevent train-serving skew by sharing validated transformation definitions or verifying exact equivalence.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Training and serving use the same validated transformation artifact or produce exactly equivalent outputs on the approved fixture set.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

A shared transform is used, but one serving cache adds a different normalization step.

## `USEQ-B7458F12` — Nondeterministic

**Exact statement**

> Use bounded, reviewed, and observable training jobs with resource, cost, security, and privacy limits.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every training job has approved resource and cost limits, security/privacy scopes, review approval, and observable execution state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Registered jobs have caps, while an unregistered job exposes data and exceeds the privacy budget.

## `USEQ-75F53D23` — Nondeterministic

**Exact statement**

> Protect training credentials, checkpoints, intermediate artifacts, logs, and distributed communication.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequate protection of credentials, artifacts, logs, checkpoints, and distributed communication requires system-specific security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`

## `USEQ-2C07474B` — Deterministic

**Exact statement**

> Validate checkpoint integrity before resume, fine-tuning, merge, quantization, conversion, or deployment.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every listed checkpoint-consuming action can be forced through a bounded gate that verifies the immutable checkpoint digest and provenance of the exact bytes consumed before continuing.

### Exact deterministic check

1. Validate checkpoint integrity before resume, fine-tuning, merge, quantization, conversion, or deployment.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A corrupted shard, wrong digest, missing signer, altered provenance, or substitution is rejected before use.

## `USEQ-AF891A4E` — Nondeterministic

**Exact statement**

> Review fine-tuning and preference data for poisoning, hidden triggers, sensitive content, policy conflict, and representation gaps.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Poisoning, hidden triggers, sensitive content, policy conflict, and representation gaps require adversarial and domain expert review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `contextual_judgment`

## `USEQ-D244FD2A` — Nondeterministic

**Exact statement**

> Measure catastrophic forgetting, capability regression, safety regression, and unexpected transfer after fine-tuning or adaptation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Every fine-tuned or adapted model has measured catastrophic-forgetting, capability-regression, safety-regression, and unexpected-transfer results.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

The benchmark shows no regression, but rare multilingual refusal behavior disappears after tuning.

## `USEQ-B93196E6` — Nondeterministic

**Exact statement**

> Control model merging, distillation, compression, pruning, quantization, and conversion as material changes requiring renewed evaluation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Merge, distillation, compression, pruning, quantization, and conversion outputs cannot be promoted without a new evaluation result.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Quantization gets a new evaluation record, but tool-use safety that regressed is not tested.

## `USEQ-30D5B880` — Nondeterministic

**Exact statement**

> Document hyperparameter search space, stopping criteria, selection rule, compute budget, and discarded candidates.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every hyperparameter search records its search space, stopping criteria, selection rule, compute budget, and discarded candidates.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The report lists a search space, while an unrecorded candidate is hidden to favor the winner.

## `USEQ-9CA4ABA8` — Nondeterministic

**Exact statement**

> Prevent production selection based on one favorable run or an unrepresentative benchmark.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Multiple-run and benchmark records are objective, but favorability and representativeness require statistical and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Production selection requires results from more than one approved run and an approved benchmark set.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-CD2A10DD` — Nondeterministic

**Exact statement**

> Assess memorization and extraction risk, especially for rare, identifying, confidential, or copyrighted examples.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Memorization and extraction risk for rare, identifying, confidential, or copyrighted examples requires privacy and attack expertise.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`

## `USEQ-04E2B43F` — Nondeterministic

**Exact statement**

> Apply privacy-enhancing training methods when required and validate their actual utility and privacy tradeoffs.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** When privacy-enhancing training is required and whether utility/privacy trade-offs are acceptable require privacy and statistical judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-2454E46F` — Nondeterministic

**Exact statement**

> Track environmental and resource impact where training or inference is material and consider smaller or more efficient alternatives.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Resource measurements are objective, but materiality and whether smaller alternatives are acceptable require sustainability and product judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every training or inference workload classified as material records environmental and resource impact plus evaluated alternative candidates.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-188AEBC2` — Nondeterministic

**Exact statement**

> Prevent training infrastructure from writing unrestricted artifacts to public or weakly controlled storage.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Training workloads are denied artifact writes to public or nonapproved storage destinations.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The orchestrated job cannot write public storage, but a notebook uploads via a presigned URL.

## `USEQ-1584DD7E` — Nondeterministic

**Exact statement**

> Review generated training data for model collapse, inherited errors, homogenization, attribution loss, and circular evaluation.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Model collapse, inherited errors, homogenization, attribution loss, and circular evaluation require semantic and statistical expert review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-32D1AB90` — Nondeterministic

**Exact statement**

> Validate safety tuning in the actual supported languages and contexts rather than assuming transfer from a dominant language.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Safety transfer across supported languages and contexts needs language-specific evaluation protocols, populations, and thresholds not defined here.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`, `legal_or_specialist_authority`

## `USEQ-B5FE544A` — Nondeterministic

**Exact statement**

> Create an evaluation plan derived from intended use, affected parties, threat model, hazards, quality attributes, and failure consequences.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every evaluation plan links to intended use, affected parties, threat model, hazards, quality attributes, and failure consequences.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The plan names all topics, but omits a predictable harm to a small affected population.

## `USEQ-0A64DD0D` — Nondeterministic

**Exact statement**

> Evaluate correctness, usefulness, robustness, reliability, calibration, uncertainty, safety, security, privacy, fairness, accessibility, latency, throughput, cost, and environmental impact as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Evaluation results are objective for approved dimensions, but applicability and complete coverage require multidisciplinary expert judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every quality dimension marked applicable has a current evaluation result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-F16359EE` — Nondeterministic

**Exact statement**

> Use task-representative, current, sufficiently difficult, independently reviewed evaluation sets.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Task representativeness, currency, sufficient difficulty, and reviewer independence require domain and evaluation judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-07D4E2DB` — Nondeterministic

**Exact statement**

> Include normal, boundary, malformed, ambiguous, adversarial, multilingual, accessibility-related, rare, and high-consequence cases.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: The evaluation suite includes normal, boundary, malformed, ambiguous, adversarial, multilingual, accessibility, rare, and high-consequence cases.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

One passing case per category exists, but malformed bidirectional text bypasses policy.

## `USEQ-69C50645` — Nondeterministic

**Exact statement**

> Measure per-group and per-context performance rather than relying only on aggregate averages.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Evaluation results include per-group and per-context metrics for every group and context in the approved evaluation plan.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Metrics are split by country but not disability, hiding a severe screen-reader failure.

## `USEQ-771A15A8` — Nondeterministic

**Exact statement**

> Report distribution, confidence intervals, worst-case behavior, and failure examples in addition to a headline score.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Every evaluation report includes distribution, confidence intervals, worst-case behavior, and failure examples.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A confidence interval is reported, but the harmful operational tail is left out.

## `USEQ-5FDC029A` — Nondeterministic

**Exact statement**

> Evaluate both false-positive and false-negative harm and the distribution of burden created by errors.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Error rates and burden measurements are objective, but defining harm and affected burden groups requires domain and fairness judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Evaluation reports false-positive and false-negative rates and burden metrics for every approved affected group.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-356476AA` — Nondeterministic

**Exact statement**

> Calibrate scores and confidence claims against observed outcome frequencies where probabilities are presented or used.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Every probability score or confidence claim has a current calibration result against observed frequencies and its approved tolerance.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Calibration passes on historical adults but fails badly for current children.

## `USEQ-03C47BD1` — Nondeterministic

**Exact statement**

> Test abstention and escalation quality, including whether the model abstains disproportionately or at the wrong times.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Abstention rates and group differences are measurable, but whether abstention occurs at the wrong times requires domain judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Evaluation reports abstention and escalation outcomes overall and for every approved group and context.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F1649342` — Nondeterministic

**Exact statement**

> Use blinded or independent human evaluation where subjective judgment is material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Human-evaluation provenance is objective, but whether subjective judgment is material and evaluators are adequately independent requires judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every evaluation marked subjective records blinded or independent human evaluator results.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-4F510C10` — Nondeterministic

**Exact statement**

> Define evaluator qualifications, rubrics, disagreement resolution, inter-rater reliability, and conflict controls.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Every human evaluation defines qualifications, rubrics, disagreement resolution, inter-rater reliability, and conflict controls.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A rubric and reliability score exist, but conflicted unqualified evaluators approve unsafe answers.

## `USEQ-13A7712A` — Nondeterministic

**Exact statement**

> Protect evaluation workers from harmful content and provide suitable training, support, and exposure limits.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Training, support, and exposure limits are checkable, but suitability and adequate worker protection require occupational and legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every harmful-content evaluation project records worker training, support channels, and exposure limits.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-C8AE2073` — Nondeterministic

**Exact statement**

> Assess accessibility of AI-generated interfaces, summaries, captions, descriptions, code, documents, and interaction patterns.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Accessibility of generated interfaces and content requires disabled-user testing and qualified accessibility review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

## `USEQ-C6205AB5` — Nondeterministic

**Exact statement**

> Validate factuality using authoritative references and source-grounding checks suitable to the domain.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reference identity and grounding tests are objective, but authority and suitability vary by domain and claim.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every factuality evaluation records its source references and executed grounding-check results.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-230C12A5` — Nondeterministic

**Exact statement**

> Evaluate citation correctness, source relevance, quote accuracy, and whether generated references actually support claims.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Source existence and exact quotation are objective, but relevance and support for a claim require semantic and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every generated citation resolves to an existing source and every quoted span matches that source.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-BCC4C62F` — Nondeterministic

**Exact statement**

> Test long-context behavior, context ordering, truncation, conflicting sources, stale retrieval, and distractors when relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Test executions are objective, but relevance of long-context risks and sufficient case coverage require context.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every long-context behavior marked relevant has tests for ordering, truncation, conflicts, stale retrieval, and distractors.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-6E978130` — Nondeterministic

**Exact statement**

> Evaluate multi-turn consistency, memory boundaries, identity continuity, refusal persistence, and recovery from earlier mistakes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Current multi-turn tests cover consistency, memory boundaries, identity continuity, refusal persistence, and recovery from earlier mistakes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Scripted dialogues pass, but a longer multilingual exchange breaks refusal persistence.

## `USEQ-DF342A34` — Nondeterministic

**Exact statement**

> Test the integrated product under production-like latency, concurrency, quotas, failures, moderation, and UI conditions.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: The integrated product passes tests under production-like latency, concurrency, quotas, failures, moderation, and UI conditions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A production-like load test passes, but moderation timeout at peak leaks unsafe output.

## `USEQ-999B68D7` — Nondeterministic

**Exact statement**

> Use adversarial testing and red teams independent from the builders for systems with material impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Red-team execution and independence are checkable, but material impact and adequate adversarial coverage require security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every system classified as materially impactful has a red-team result from reviewers independent of its builders.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E5EDCDBA` — Nondeterministic

**Exact statement**

> Track evaluation-set version, model version, configuration, prompt, tool permissions, and result provenance.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every result records evaluation-set version, model version, configuration, prompt, tool permissions, and result provenance.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Tracked evaluations have all fields, but a hidden system-prompt variant changes deployment.

## `USEQ-0EF2BECE` — Nondeterministic

**Exact statement**

> Set release thresholds and no-go failures before observing the final results where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Threshold timestamps are objective, but practicality exceptions and suitable no-go criteria require accountable risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every final evaluation has release thresholds and no-go failures recorded before its result timestamp, unless an approved exception exists.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0AC929A5` — Nondeterministic

**Exact statement**

> Retain representative failure cases as regression tests without exposing sensitive or harmful data unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Regression-case retention and scanning are objective, but representativeness and necessary sensitive or harmful content require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every approved failure case retained as a regression test passes sensitive and harmful-data handling controls.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B4BBA5B4` — Nondeterministic

**Exact statement**

> Identify protected, vulnerable, underrepresented, and contextually disadvantaged groups relevant to the actual use.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Relevant protected, vulnerable, underrepresented, and disadvantaged groups require legal, social, and use-context judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-25746967` — Nondeterministic

**Exact statement**

> Assess whether the problem definition, target, label, proxy, data, interface, deployment, or feedback loop can create inequitable outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Inequitable outcomes arising from targets, proxies, data, deployment, and feedback require causal and fairness expertise.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-F6FE00F8` — Nondeterministic

**Exact statement**

> Select fairness concepts and metrics that correspond to the concrete harm rather than applying a generic parity metric blindly.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Choosing fairness concepts that match concrete harm requires normative, legal, statistical, and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-313223A4` — Nondeterministic

**Exact statement**

> Document incompatible fairness objectives and who authorized the chosen tradeoff.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Every identified incompatible fairness objective has a decision record naming the authorized trade-off approver.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A signed fairness memo exists, but ignores an affected group and chooses an unlawful tradeoff.

## `USEQ-A2551695` — Nondeterministic

**Exact statement**

> Evaluate intersectional and small-group outcomes where aggregate categories can hide harm.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Per-group measurements are objective, but deciding where aggregates hide harm and which intersections matter requires fairness judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every intersectional or small group selected in the fairness plan has separately reported outcomes.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9F00F826` — Nondeterministic

**Exact statement**

> Investigate missingness, lower-quality data, language coverage, assistive-technology use, and differing access conditions as sources of unequal error.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Unequal error caused by missingness, data quality, language, assistive technology, or access needs causal and accessibility analysis.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-CF63258B` — Nondeterministic

**Exact statement**

> Test whether thresholds, abstention, escalation, identity checks, fraud controls, and moderation burden groups differently.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Per-group outcome measures are objective, but whether burdens are unequal or unacceptable requires fairness and domain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Threshold, abstention, escalation, identity, fraud, and moderation evaluations report outcomes by every approved group.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0A97658F` — Nondeterministic

**Exact statement**

> Ensure human reviewers receive enough context and training to correct rather than amplify model bias.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Enough reviewer context and training to correct rather than amplify bias requires human-factors and fairness evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

## `USEQ-9D4D9C88` — Nondeterministic

**Exact statement**

> Avoid presenting protected-trait inference, emotion inference, personality inference, or risk scoring as objective fact when scientific validity is weak or context dependent.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Scientific validity, objectivity, and contextual weakness of trait or risk inferences require domain and claims expertise.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-D8207FA6` — Nondeterministic

**Exact statement**

> Do not use accessibility needs, disability signals, language, device quality, or behavioral differences as harmful proxies.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether accessibility, disability, language, device, or behavior signals act as harmful proxies requires causal and rights analysis.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-10FA0F4D` — Nondeterministic

**Exact statement**

> Make explanations and recourse understandable to the affected audience and accessible through supported modalities.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Understandability and modality accessibility for affected audiences require representative user and accessibility evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `contextual_judgment`

## `USEQ-8298717B` — Nondeterministic

**Exact statement**

> Provide reasonable alternatives when voice, vision, motion, cognition, literacy, language, or device assumptions exclude users.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reasonable alternatives and exclusion across diverse ability, language, literacy, and device assumptions require accessibility judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-F3A9EE15` — Nondeterministic

**Exact statement**

> Test whether generated descriptions, captions, summaries, translations, and recommendations preserve essential meaning for disabled users.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Preservation of essential meaning for disabled users requires representative evaluation protocols and acceptance thresholds not specified.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`, `legal_or_specialist_authority`

## `USEQ-F38A1B69` — Nondeterministic

**Exact statement**

> Include affected communities and domain experts in design and evaluation proportionate to risk.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Which communities and experts to include and what participation is proportionate to risk require accountable stakeholder judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-4EA0B410` — Nondeterministic

**Exact statement**

> Monitor disparate impact and complaint patterns after deployment, not only before launch.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Monitoring records disparate-impact metrics and complaint trends for approved affected groups.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Reported complaints are monitored, but an inaccessible complaint channel hides group harm.

## `USEQ-84239BB3` — Nondeterministic

**Exact statement**

> Prevent personalization from creating discriminatory prices, eligibility, reach, service quality, or information access without lawful and justified grounds.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Discrimination, lawfulness, and justified grounds in personalized price, eligibility, reach, service, or information decisions require legal review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-EAECD5B4` — Nondeterministic

**Exact statement**

> Define the validated operating domain, input ranges, languages, modalities, task types, and environmental conditions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every AI capability defines validated input ranges, languages, modalities, task types, and environmental conditions.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The registry lists ranges and languages, but includes an unvalidated emergency medical task.

## `USEQ-07AA5338` — Nondeterministic

**Exact statement**

> Detect or safely handle out-of-distribution, corrupted, adversarial, incomplete, stale, and conflicting input.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Tests cover out-of-distribution, corrupted, adversarial, incomplete, stale, and conflicting inputs and their safe-handling paths.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Known corruption tests pass, but a novel stale and conflicting input triggers harm.

## `USEQ-6EF822C9` — Nondeterministic

**Exact statement**

> Measure sensitivity to paraphrase, ordering, formatting, spelling, noise, prompt framing, irrelevant context, and minor data changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Sensitivity tests cover paraphrase, order, format, spelling, noise, framing, irrelevant context, and minor data changes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A fixed paraphrase suite passes, but punctuation reverses a decision in an unseen language.

## `USEQ-8607FB55` — Nondeterministic

**Exact statement**

> Evaluate stability across repeated runs and quantify nondeterminism where it affects user expectations or decisions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Repeated-run variance is measurable, but when nondeterminism affects expectations or decisions requires product and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every task classified as nondeterminism-sensitive has repeated-run stability results and quantified variance.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-25B208C7` — Nondeterministic

**Exact statement**

> Use deterministic validation for formats, ranges, permissions, calculations, business rules, and side effects that must be exact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every exact sink validates formats, ranges, permissions, calculations, business rules, and side effects outside the model.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Format and range validation pass, while a semantically invalid refund destination is accepted.

## `USEQ-1C0FC607` — Nondeterministic

**Exact statement**

> Do not use model confidence as a substitute for calibrated uncertainty or external validation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: No model confidence value is used as calibrated uncertainty or final validation without current calibration or external evidence.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Documentation warns against confidence, but an approval flow still treats token probability as proof.

## `USEQ-26D2EEF9` — Nondeterministic

**Exact statement**

> Define fallback behavior for model timeout, quota exhaustion, provider outage, safety-system outage, retrieval failure, tool failure, malformed output, and policy conflict.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Fallback behavior is defined for timeout, quota exhaustion, provider outage, safety outage, retrieval failure, tool failure, malformed output, and policy conflict.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Each named failure has fallback, but malformed output triggers a stale transaction.

## `USEQ-5AEB2EF3` — Nondeterministic

**Exact statement**

> Ensure degraded modes fail visibly and safely rather than silently substituting lower-quality or differently governed behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Visible degradation and governance identity are testable, but safe and unacceptable quality loss require domain and safety judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every degraded mode visibly identifies its active behavior and preserves its registered policy and governance identifier.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-4C760B55` — Nondeterministic

**Exact statement**

> Prevent retries from creating duplicate external actions or unbounded cost.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every model retry test produces at most one external action and remains within the approved retry and cost limits.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Payment retries are idempotent, but a side notification route repeats the charge.

## `USEQ-553B4185` — Nondeterministic

**Exact statement**

> Use idempotency, transaction boundaries, compensation, and confirmation for model-initiated side effects.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every registered model-initiated side effect has an idempotency key, transaction boundary, compensation path, and confirmation policy.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Payments have transactions and confirmation, but infrastructure deletion has no compensation.

## `USEQ-11D17067` — Nondeterministic

**Exact statement**

> Bound context, output, recursion, tool calls, steps, time, memory, compute, network, and monetary cost.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every agentic workflow bounds context, output, recursion, tool calls, steps, time, memory, compute, network, and monetary cost.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The parent agent has limits, but each child resets its budget and fans out forever.

## `USEQ-50EEA27A` — Nondeterministic

**Exact statement**

> Test long-running and agentic workflows for drift from the original goal, accumulating error, and unsafe continuation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Goal drift, accumulating error, and unsafe continuation in long agent workflows require semantic evaluation without defined scenarios or thresholds.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

## `USEQ-5E9067FC` — Nondeterministic

**Exact statement**

> Verify that a human can pause, inspect, correct, resume, and terminate consequential workflows.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Authorized humans can pause, inspect, correct, resume, and terminate every registered consequential workflow.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Pause works in a test, but a transfer completes before a human can inspect it.

## `USEQ-5EDE5164` — Nondeterministic

**Exact statement**

> Monitor upstream data freshness and distinguish missing evidence from evidence of absence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every upstream data source has a freshness indicator and uses distinct missing-evidence and evidence-of-absence states.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The feed meets freshness SLA, but a missing partition is interpreted as zero fraud.

## `USEQ-9131BDD3` — Nondeterministic

**Exact statement**

> Prevent stale models, embeddings, policies, prompts, or retrieval indexes from being treated as current without detection.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Monitoring flags every registered stale AI artifact and prevents it from carrying current status.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The model version is current, but an unversioned retrieval index is stale.

## `USEQ-270F3C01` — Nondeterministic

**Exact statement**

> Validate behavior after dependency, hardware, quantization, compiler, runtime, locale, and infrastructure changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Behavior validation runs after dependency, hardware, quantization, compiler, runtime, locale, and infrastructure changes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Compiler-upgrade tests pass common cases, but one hardware path changes safety behavior.

## `USEQ-98682F5F` — Nondeterministic

**Exact statement**

> Define recovery and reconciliation for incomplete AI-driven workflows.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every incomplete AI workflow state has documented recovery and reconciliation actions.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A runbook retries incomplete work, but duplicates an already partial refund.

## `USEQ-9DF37D00` — Nondeterministic

**Exact statement**

> Threat-model model theft, extraction, inversion, membership inference, training-data leakage, poisoning, evasion, adversarial examples, hidden triggers, unsafe serialization, and supply-chain compromise.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: The AI threat model covers theft, extraction, inversion, membership inference, leakage, poisoning, evasion, adversarial examples, hidden triggers, unsafe serialization, and supply-chain compromise.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every named threat has a section, but serialized adapters can still execute code.

## `USEQ-15EDD801` — Nondeterministic

**Exact statement**

> Restrict access to training data, model weights, checkpoints, system prompts, evaluation sets, embeddings, safety policies, and privileged endpoints according to sensitivity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Access-control configuration is objective, but sensitivity classification and least required access need security and privacy judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every registered AI asset has an access policy keyed to its approved sensitivity classification.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-B15DC6F6` — Nondeterministic

**Exact statement**

> Use separate identities and least privilege for training, evaluation, deployment, retrieval, monitoring, and administration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Distinct workload identities are objective, but least privilege for each lifecycle role requires contextual security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Training, evaluation, deployment, retrieval, monitoring, and administration use separate identities.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-B7EDE7E8` — Nondeterministic

**Exact statement**

> Prevent public or tenant-scoped inputs from crossing user, tenant, region, or confidentiality boundaries through caches, memory, retrieval, logs, fine-tuning, or provider behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Declared cross-boundary isolation tests are objective, but complete coverage across provider behavior and all data paths is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Registered cache, memory, retrieval, log, fine-tuning, and provider paths pass user, tenant, region, and confidentiality isolation tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-6BAFF5D7` — Nondeterministic

**Exact statement**

> Apply authentication, authorization, rate limits, quotas, anomaly detection, and abuse controls to inference and model-management endpoints.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every inference and model-management endpoint has authentication, authorization, rate limits, quotas, anomaly detection, and abuse controls.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The public inference API has controls, but a debug management endpoint allows anonymous replacement.

## `USEQ-EBFC942E` — Nondeterministic

**Exact statement**

> Protect model and prompt intellectual property without relying on secrecy as the only safety control.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Presence of nonsecrecy safety controls is objective, but adequate intellectual-property protection requires threat and legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every protected model or prompt has at least one approved safety control independent of secrecy.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-73282252` — Nondeterministic

**Exact statement**

> Test for extraction through repeated querying, probability outputs, embeddings, error messages, and side channels.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Extraction tests cover repeated queries, probability outputs, embeddings, error messages, and side channels.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Repeated-query tests pass, but embedding distances leak membership through a side channel.

## `USEQ-7E587625` — Nondeterministic

**Exact statement**

> Minimize output detail, logits, confidence data, or debug information when it materially increases extraction or inference risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Output-field policy is checkable, but material extraction risk and minimal detail require specialist risk judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every output field classified as extraction-sensitive is removed, reduced, or protected by an approved exception.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-987DF294` — Nondeterministic

**Exact statement**

> Protect feedback and reinforcement channels from coordinated manipulation and sybil attacks.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Adequate resistance to coordinated manipulation and sybil attacks requires adversarial threat modeling and evolving empirical evaluation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `unbounded_scope`

## `USEQ-37715BDE` — Nondeterministic

**Exact statement**

> Validate uploaded models, adapters, datasets, notebooks, prompts, tools, and serialized artifacts in isolated environments.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Uploaded models, adapters, datasets, notebooks, prompts, tools, and serialized artifacts are validated in an isolated sandbox before use.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Models are sandboxed, but a serialized notebook executes on the host during preview.

## `USEQ-DE3A04CA` — Nondeterministic

**Exact statement**

> Prevent training or inference code from executing untrusted content with host, cloud, network, or secret access.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Execution of untrusted training or inference content is denied host, cloud, network, and secret access.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Containers deny ordinary network access, but a mounted metadata socket exposes cloud credentials.

## `USEQ-FC4C23B5` — Nondeterministic

**Exact statement**

> Monitor unexpected model size, digest, registry, permission, network, cost, latency, refusal, and output-distribution changes.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Monitoring tracks model size, digest, registry, permissions, network, cost, latency, refusal, and output-distribution changes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Digest alerts work, but a harmful output shift stays below the anomaly threshold.

## `USEQ-8A02B572` — Nondeterministic

**Exact statement**

> Use tamper-evident provenance and promotion records for high-impact models.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Provenance and tamper evidence are objective, but high-impact classification requires accountable risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every model classified as high impact has tamper-evident provenance and promotion records.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-E82328B8` — Nondeterministic

**Exact statement**

> Red-team cross-tenant leakage, secret elicitation, policy bypass, encoded attacks, multilingual attacks, indirect instruction, and persistence.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Red-team tests cover cross-tenant leakage, secret elicitation, policy bypass, encoded and multilingual attacks, indirect instruction, and persistence.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Named attacks pass, but a new mixed-script indirect instruction leaks another tenant's secret.

## `USEQ-1FA84F69` — Nondeterministic

**Exact statement**

> Include AI systems in vulnerability management, incident response, forensic readiness, backup, recovery, and supplier-risk processes.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: AI systems are included in vulnerability management, incident response, forensics, backup, recovery, and supplier-risk processes.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The main model is in the incident plan, but a vendor embedding service is absent.

## `USEQ-C9DEAD72` — Nondeterministic

**Exact statement**

> Define a coordinated disclosure path for model, prompt, evaluation, data, and agent vulnerabilities.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: A published coordinated disclosure path covers model, prompt, evaluation, data, and agent vulnerabilities.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A disclosure form exists, but agent vulnerability reports go to nobody with authority.

## `USEQ-13602AAE` — Nondeterministic

**Exact statement**

> Prevent security filters from becoming the sole authorization or data-loss-prevention boundary.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every privileged AI action has authorization and data-loss-prevention enforcement independent of model security filters.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Main tools reauthorize, but a direct export path relies only on the model's refusal filter.

## `USEQ-AF20A1A9` — Nondeterministic

**Exact statement**

> Treat system instructions, developer instructions, user prompts, retrieved documents, web pages, files, tool results, memory, metadata, and model outputs as inputs with distinct trust levels.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: System, developer, user, retrieval, web, file, tool, memory, metadata, and model-output inputs each have an explicit trust classification.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every input type has a trust label, but administrator content is incorrectly trusted.

## `USEQ-82711812` — Nondeterministic

**Exact statement**

> Enforce instruction priority, authorization, data boundaries, and tool policy in trusted code rather than relying only on natural-language instructions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Instruction priority, authorization, data boundaries, and tool policy are enforced outside natural-language instructions.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The official gateway checks scopes, but a plugin executes commands from natural-language instructions alone.

## `USEQ-3CC8B60E` — Nondeterministic

**Exact statement**

> Assume direct and indirect prompt injection can occur and design so successful injection cannot exceed independently enforced authority.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Under direct and indirect injection scenarios, every model-initiated action remains within independent authorization limits.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Prompt-injection fixtures pass, but a retrieved document reaches an overprivileged legacy tool.

## `USEQ-0D2EF3BB` — Nondeterministic

**Exact statement**

> Keep secrets, credentials, hidden policies, unrelated user data, internal reasoning artifacts, and privileged context out of model context unless strictly necessary and protected.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Context data-flow checks are objective, but strict necessity and adequate protection require privacy and security judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every context item classified as secret, credential, hidden policy, unrelated user data, reasoning artifact, or privileged has a scoped necessity approval and protection policy.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9066469A` — Nondeterministic

**Exact statement**

> Sanitize and label retrieved content without treating sanitization as a complete prompt-injection defense.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every retrieved item is sanitized and trust-labeled, and privileged actions remain protected by a separate authorization control.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Documents are labeled untrusted, but a downstream agent treats sanitized text as safe instructions.

## `USEQ-2B691284` — Nondeterministic

**Exact statement**

> Authorize retrieval before content enters the model and recheck authorization before output or side effects.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every retrieval result is authorized before model context entry and reauthorized before output disclosure or action.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Fresh search is authorized, but cached chunks bypass checks after a role change.

## `USEQ-341FBC64` — Nondeterministic

**Exact statement**

> Preserve tenant, user, purpose, region, retention, and sensitivity metadata through chunking, embedding, indexing, retrieval, caching, and citation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Tenant, user, purpose, region, retention, and sensitivity metadata survive chunking, embedding, indexing, retrieval, caching, and citation.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The main index keeps metadata, but a reranker cache drops region and retention labels.

## `USEQ-8B6857A6` — Nondeterministic

**Exact statement**

> Validate retrieval freshness, source identity, relevance, authority, coverage, contradiction, and permission.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Freshness, identity, and permission are objective, but relevance, authority, coverage, and contradiction resolution require semantic judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every retrieved item has validated freshness, source identity, and request-time permission evidence.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-25F2FD1D` — Nondeterministic

**Exact statement**

> Provide citations or source traces when needed, and verify that every cited source exists and supports the associated claim.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Citation existence and exact source tracing are objective, but need and whether a source supports a claim require domain judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every generated citation resolves to an existing source trace.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B5516B69` — Nondeterministic

**Exact statement**

> Distinguish model-generated statements from retrieved or deterministic facts in the product architecture and user interface where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Architectural provenance labels are objective, but materiality and user-interface sufficiency require product and human-factors judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every statement class marked material is labeled model-generated, retrieved, or deterministic in product data structures.
   - **Checker family:** `source_ast`
   - **Evidence authority:** `repository`

## `USEQ-EA11C0C3` — Nondeterministic

**Exact statement**

> Constrain structured output with schema validation and reject, repair, or escalate malformed output safely.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every structured output is schema-validated and malformed output triggers an approved rejection, repair, or escalation path.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Malformed JSON is rejected, but automatic repair changes a beneficiary and proceeds.

## `USEQ-309DD707` — Nondeterministic

**Exact statement**

> Encode or sanitize generated output for its destination context before rendering, executing, storing, sending, querying, or compiling it.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every generated output reaching render, execute, store, send, query, or compile sinks passes a destination-context encoder or sanitizer.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

HTML is escaped, but the same output enters a shell command unsanitized.

## `USEQ-17CCC437` — Nondeterministic

**Exact statement**

> Never execute generated code, commands, queries, templates, URLs, configurations, or workflow definitions without independent policy, validation, isolation, and approval appropriate to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Independent controls are testable, but impact classification and appropriate validation, isolation, and approval require specialist judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every generated executable artifact passes its registered policy, validation, isolation, and approval gates before execution.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DE757839` — Nondeterministic

**Exact statement**

> Give each tool a narrow identity, explicit operation set, parameter schema, resource scope, budget, timeout, and audit trail.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed clause has an objective fragment, but it cannot decide the full compound statement because complete scope and the semantic or accountable parts are not bounded. The proposed clause only establishes: Every tool defines a narrow identity, operation set, parameter schema, resource scope, budget, timeout, and audit trail.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Tools have schemas and timeouts, but a generic execute tool has scope star and unsafe budget.

## `USEQ-8BFD9B81` — Nondeterministic

**Exact statement**

> Require renewed authorization for high-impact tool calls and prevent the model from self-granting permissions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every high-impact tool call requires renewed user or policy authorization and rejects model-originated self-grants.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Payments ask again, but account deletion accepts a model-created approval token.

## `USEQ-191B4C8D` — Nondeterministic

**Exact statement**

> Use previews and human confirmation for destructive, financial, public, legal, security, privacy, account, infrastructure, or irreversible actions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The full statement depends on a named person's authority or an accountable risk decision; artifact presence and execution evidence cannot prove that judgment valid. The proposed clause only establishes: Destructive, financial, public, legal, security, privacy, account, infrastructure, and irreversible actions require preview and human confirmation.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A confirmation dialog exists, but hides the legal consequence and defaults to approve.

## `USEQ-7F7AA7C2` — Nondeterministic

**Exact statement**

> Limit agent recursion, delegation, parallelism, child agents, self-modification, memory, context expansion, and external communication.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Every agent limits recursion, delegation, parallelism, child agents, self-modification, memory, context expansion, and external communication.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The parent is capped, but children create more children through an uncounted queue.

## `USEQ-D296B587` — Nondeterministic

**Exact statement**

> Prevent agents from creating hidden persistence, accounts, credentials, scheduled work, infrastructure, or subscriptions without explicit authority.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Agents cannot create persistence, accounts, credentials, schedules, infrastructure, or subscriptions without a matching authority grant.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Scheduled work is blocked, but the agent creates a cloud subscription with retained credentials.

## `USEQ-3425618B` — Nondeterministic

**Exact statement**

> Isolate browser, interpreter, file, network, code-execution, and computer-control environments and reset them between users or tasks as required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Isolation and reset tests are objective, but which environments and reset boundaries are required needs security context.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every environment marked isolation-required is sandboxed and passes between-user or between-task reset tests.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5212224D` — Nondeterministic

**Exact statement**

> Restrict outbound network destinations and defend against server-side request forgery, data exfiltration, malicious downloads, and unsafe redirects.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposed check lacks an authoritative complete inventory of every relevant model, data, identity, runtime, tool, copy, or execution path required by the universal claim. The proposed clause only establishes: Agent network access is destination-restricted and passes SSRF, exfiltration, malicious-download, and unsafe-redirect tests.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Localhost is blocked, but DNS rebinding reaches cloud metadata and steals a secret.

## `USEQ-6AC50F7F` — Nondeterministic

**Exact statement**

> Treat external tool output as untrusted even when the tool itself is approved.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The artifact can show that a field, document, or mechanism exists, but cannot objectively decide its semantic completeness, correctness, or fitness for this system. The proposed clause only establishes: Every external tool output is labeled untrusted and enters the same validation boundary as other untrusted input.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Tool output is tagged untrusted, but is interpolated into a privileged command.

## `USEQ-773557D7` — Nondeterministic

**Exact statement**

> Verify that memory creation, retrieval, correction, expiry, deletion, and cross-session use are transparent and authorized.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Authorization and lifecycle behavior are testable, but transparency to users requires contextual interface and notice judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Memory creation, retrieval, correction, expiry, deletion, and cross-session use each enforce explicit authorization and emit lifecycle records.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-11C0DFF6` — Nondeterministic

**Exact statement**

> Do not store sensitive conversation memory merely because personalization is convenient.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Sensitive memory, necessity, and mere personalization convenience require privacy-purpose and proportionality judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-C3984280` — Nondeterministic

**Exact statement**

> Test jailbreaks, prompt leakage, policy conflict, role confusion, multi-turn escalation, context poisoning, tool misuse, hidden instructions, and malicious files.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The finite evaluation does not define an exhaustive population, complete oracle, sampling protocol, or acceptance threshold for the full behavioral claim. The proposed clause only establishes: Tests cover jailbreaks, prompt leakage, policy conflict, role confusion, multi-turn escalation, context poisoning, tool misuse, hidden instructions, and malicious files.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All listed jailbreak fixtures pass, but a new multilingual file instruction escalates tool access.

## `USEQ-F2FE87FF` — Nondeterministic

**Exact statement**

> Ensure refusals do not reveal prohibited material and do not prevent legitimate safety, accessibility, or recovery use without recourse.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Prohibited disclosure, legitimate safety or accessibility use, and adequate recourse require semantic, legal, and human review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-FE69F909` — Nondeterministic

**Exact statement**

> Moderate both inputs and outputs according to the actual harm model while retaining the minimum necessary data.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The actual harm model and minimum necessary moderation data require safety, policy, privacy, and legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-952B24B4` — Nondeterministic

**Exact statement**

> Provide a kill switch for tool use and a separate capability to disable the model while preserving essential deterministic product functions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Switch actuation can be tested, but the complete tool path inventory and which deterministic functions are essential are not objectively bounded.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The documented switch disables registered tools, while a plugin still invokes an unregistered tool and a truly essential checkout function is lost.

## `USEQ-F3DA7D88` — Nondeterministic

**Exact statement**

> Use version control and review for training code, inference code, prompts, policies, feature definitions, data transformations, evaluation logic, deployment manifests, and infrastructure.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Repository history is checkable, but the complete inventory of all nine artifact classes and validity of human review are not established.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

All tracked files require review, while a deployment prompt stored in an external console changes without version history.

## `USEQ-13FDD0E8` — Nondeterministic

**Exact statement**

> Use immutable identifiers for models, datasets, prompts, retrieval indexes, feature definitions, policies, and evaluation suites.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal assumes a complete inventory of every model, dataset, prompt, index, feature, policy, and evaluation suite without defining its authority.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Registered releases use digests, while an unregistered prompt loaded from configuration has only a mutable name.

## `USEQ-CFD67264` — Nondeterministic

**Exact statement**

> Trace every production response or decision to the relevant deployed versions where impact and privacy permit.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Version tracing is objective for approved paths, but impact and privacy permission require risk and legal judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every production response or decision marked trace-required records the relevant deployed component versions.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-EDEEF0CC` — Nondeterministic

**Exact statement**

> Separate development, experimentation, validation, shadow, canary, and production environments and data access.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Distinct identifiers and credentials are observable, but complete environment and data-flow separation across all six stages is not proven.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Each stage has a different account, but an experiment credential can still read production training data through a shared store.

## `USEQ-60454DA3` — Nondeterministic

**Exact statement**

> Require reproducible, authenticated, integrity-checked build and promotion paths for models and associated artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authentication and digest checks are bounded, but reproducibility and complete coverage of all associated artifacts require an undefined build oracle and inventory.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `undefined_protocol`

### Skeptical rejection counterexample

The model digest and signer verify, while an untracked tokenizer changes the output and the build cannot be reproduced.

## `USEQ-7C1585D4` — Nondeterministic

**Exact statement**

> Prevent direct manual replacement of a production model or prompt without an auditable emergency process.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A known mutation interface can deny writes, but complete replacement-path coverage and approval of an emergency process remain unbounded.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

The registry UI blocks manual replacement, while an operator overwrites a mounted prompt file outside the audited emergency path.

## `USEQ-3BC056D6` — Nondeterministic

**Exact statement**

> Validate feature and preprocessing equivalence between training and serving.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A fixed fixture set cannot prove feature and preprocessing equivalence over the complete valid input domain.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Approved fixtures match, while a Unicode timestamp edge case transforms differently in training and serving.

## `USEQ-1EEA8D0C` — Nondeterministic

**Exact statement**

> Use a model registry or equivalent controlled inventory with stages, owners, evidence, approvals, and retirement state.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields can be validated, but a complete model inventory and meaningful ownership, evidence, and approval cannot be inferred from records.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

Every registry row has all fields, while an unregistered shadow model serves users and an unauthorized person is listed as approver.

## `USEQ-9CCF6F87` — Nondeterministic

**Exact statement**

> Package required tokenizer, preprocessing, schema, policy, calibration, and compatibility metadata with the model release.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Package-field presence does not prove a complete release inventory or that calibration and compatibility metadata are correct for the deployed model.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The package contains six named files, but calibration.json belongs to an earlier model and a hidden adapter is missing.

## `USEQ-767F858F` — Nondeterministic

**Exact statement**

> Test backward and forward compatibility among model, client, feature, schema, retrieval, prompt, tool, and output consumers.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A finite compatibility matrix cannot cover every version, interaction, consumer, and valid behavior in both directions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Named component pairs pass, while an older client and new retrieval schema silently drop authorization metadata.

## `USEQ-004EB3A6` — Nondeterministic

**Exact statement**

> Use shadow evaluation, canary cohorts, champion-challenger comparison, or staged rollout where impact warrants it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Staged rollout evidence is objective, but whether impact warrants it and which mechanism is adequate require accountable risk judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change marked staged-rollout-required uses shadow, canary, champion-challenger, or staged deployment evidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-8002BB09` — Nondeterministic

**Exact statement**

> Define automated and human rollout gates for quality, safety, security, fairness, latency, throughput, availability, cost, and business integrity.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Gate names and types are record facts, but suitable metrics, thresholds, fairness choices, and approval authority require accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

All ten dimensions have automated and human gates, but the safety threshold is meaningless and the approver lacks authority.

## `USEQ-E63926B7` — Nondeterministic

**Exact statement**

> Maintain a previous known-good model, prompt, policy, and retrieval index when rollback is feasible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Known-good artifact retention is objective, but rollback feasibility requires architecture and state-compatibility judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every component marked rollback-feasible retains previous known-good model, prompt, policy, and retrieval-index artifacts.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-67F31DA5` — Nondeterministic

**Exact statement**

> Test rollback and roll-forward, including state, memory, embeddings, cache, and schema compatibility.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Rollback tests do not define exhaustive state combinations, compatibility oracles, failure points, or acceptable recovery behavior.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

The standard rollback fixture passes, while cached embeddings from a partial rollout are incompatible and corrupt results.

## `USEQ-39098812` — Nondeterministic

**Exact statement**

> Handle provider or model nondeterminism when comparing canary and control populations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Repeated-run comparison methods are checkable, but adequate handling of nondeterminism requires statistical protocol and tolerance decisions.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every canary comparison involving nondeterministic models records repeated runs, variance, and the declared comparison method.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2EEA3BC7` — Nondeterministic

**Exact statement**

> Prevent online learning, feedback updates, and self-modification from entering production without bounded, reviewed promotion.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A production admission gate is observable, but complete update-path coverage, safe bounds, and validity of review are not objectively established.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `human_accountability`

### Skeptical rejection counterexample

The official online-learning job requires promotion, while a feedback worker writes weights directly through an unregistered path.

## `USEQ-683E2947` — Nondeterministic

**Exact statement**

> Validate accelerator, runtime, compiler, quantization, serving engine, batching, and hardware changes as model changes when output can differ.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Before-and-after samples cannot prove complete behavioral equivalence or define which output differences require adequate renewed validation.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Common cases show no change, while one hardware-specific quantization path changes refusal behavior for an unseen language.

## `USEQ-37EF3EDD` — Nondeterministic

**Exact statement**

> Capacity-test inference, retrieval, safety filters, tool services, queues, and provider quotas under peak and abuse load.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Peak and abuse load, dependency coverage, workload distributions, and passing capacity thresholds are undefined.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

All six services pass the chosen load test, while a longer tool-loop attack exhausts provider quota and queues.

## `USEQ-58DDDF7C` — Nondeterministic

**Exact statement**

> Use admission control, prioritization, backpressure, caching, batching, and load shedding without violating isolation, freshness, or user safety.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Registered invariants and finite load tests cannot exhaust combinations or prove isolation, freshness, and user safety semantics.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Backpressure tests pass, while load shedding drops a tenant boundary update and serves another tenant's stale result.

## `USEQ-014BF5FE` — Nondeterministic

**Exact statement**

> Monitor cost per task, token or compute amplification, runaway agents, retry storms, and unexpectedly expensive inputs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Metric-series presence is queryable, but complete agent/input scope and thresholds that detect genuinely unexpected cost behavior are not bounded.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Five dashboards exist, while a child-agent route emits no task tag and its runaway compute is never detected.

## `USEQ-45CF19C0` — Nondeterministic

**Exact statement**

> Include AI components in deployment records, configuration drift detection, disaster recovery, and production-readiness evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Record references are checkable, but complete AI component coverage and adequate drift, recovery, and readiness evidence are not.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The registered model appears in every record, while its vendor retrieval service is absent from disaster recovery.

## `USEQ-FFCBD920` — Nondeterministic

**Exact statement**

> Define production indicators for input quality, output quality, safety, security, fairness, refusal, calibration, latency, availability, saturation, cost, and user outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Indicator names can be counted, but whether they correctly measure all thirteen concepts for the intended system requires domain definitions and thresholds.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

The catalog has one metric per label, but fairness is measured only as global accuracy and hides severe group harm.

## `USEQ-81C152F9` — Nondeterministic

**Exact statement**

> Monitor input, feature, label, concept, population, retrieval, embedding, output, policy, and provider drift as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor input, feature, label, concept, population, retrieval, embedding, output, policy, and provider drift as applicable.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9666549A` — Nondeterministic

**Exact statement**

> Use reference distributions and thresholds appropriate to each signal; do not treat every statistical difference as harmful drift.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use reference distributions and thresholds appropriate to each signal; do not treat every statistical difference as harmful drift.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-F280064F` — Nondeterministic

**Exact statement**

> Detect silent upstream schema, semantic, unit, category, timestamp, and missingness changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect silent upstream schema, semantic, unit, category, timestamp, and missingness changes.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-30EC58C5` — Nondeterministic

**Exact statement**

> Measure quality using delayed ground truth, expert review, user correction, audits, reconciliation, and representative sampling where direct labels are unavailable.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Measure quality using delayed ground truth, expert review, user correction, audits, reconciliation, and representative sampling where direct labels are unavailable.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-99CE268F` — Nondeterministic

**Exact statement**

> Protect feedback metrics against gaming, selection bias, survivorship bias, harassment, coordinated manipulation, and automation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Protect feedback metrics against gaming, selection bias, survivorship bias, harassment, coordinated manipulation, and automation.” makes a universal robustness claim against gaming, bias, manipulation, harassment, or automated attack behavior that bounded evidence cannot prove.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-5C19854F` — Nondeterministic

**Exact statement**

> Separate user preference from factual correctness, safety, legal compliance, and protected rights.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A UI labels preference separately, but a preferred answer is still accepted as legally compliant without review.

## `USEQ-5D28AC18` — Nondeterministic

**Exact statement**

> Monitor by relevant language, region, device, group, task, tenant, model version, prompt version, provider, and failure mode.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor by relevant language, region, device, group, task, tenant, model version, prompt version, provider, and failure mode.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-27A8B5D5` — Nondeterministic

**Exact statement**

> Track harmful exposure and downstream impact, not only the number of generated outputs or blocked requests.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A dashboard records blocked requests and one exposure metric, while downstream denial harm is never observed.

## `USEQ-627BB632` — Nondeterministic

**Exact statement**

> Detect data leakage, repeated sensitive strings, anomalous citations, prohibited tool use, unexpected network destinations, and cross-user similarity where appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect data leakage, repeated sensitive strings, anomalous citations, prohibited tool use, unexpected network destinations, and cross-user similarity where appropriate.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-1257C1CC` — Nondeterministic

**Exact statement**

> Monitor model and provider changes using recurring golden tests, behavioral fingerprints, and change-point analysis.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Golden tests and fingerprints pass, but a provider change harms an unrepresented language.

## `USEQ-DA805FFF` — Nondeterministic

**Exact statement**

> Ensure monitoring itself does not collect excessive prompts, outputs, personal data, secrets, or copyrighted content.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure monitoring itself does not collect excessive prompts, outputs, personal data, secrets, or copyrighted content.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-A8219EEA` — Nondeterministic

**Exact statement**

> Provide protected sampling and access controls for human review of production interactions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide protected sampling and access controls for human review of production interactions.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CB3AFE81` — Nondeterministic

**Exact statement**

> Define when drift triggers investigation, recalibration, retraining, policy change, rollback, restricted scope, or suspension.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every drift action has a threshold, but the threshold delays suspension after a dangerous safety shift.

## `USEQ-5795AC64` — Nondeterministic

**Exact statement**

> Track unresolved model limitations, recurring user corrections, escalations, appeals, incidents, and near misses.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

The issue tracker has all categories, but repeated corrections outside the tracker remain unresolved.

## `USEQ-078062DD` — Nondeterministic

**Exact statement**

> Close the loop by converting production failures into data fixes, evaluation cases, product changes, safety controls, documentation, and regression tests.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Close the loop by converting production failures into data fixes, evaluation cases, product changes, safety controls, documentation, and regression tests.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-F4627883` — Nondeterministic

**Exact statement**

> Review whether optimization feedback loops amplify popularity, bias, fraud, polarization, low-quality content, or exclusion.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review whether optimization feedback loops amplify popularity, bias, fraud, polarization, low-quality content, or exclusion.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F7E09645` — Nondeterministic

**Exact statement**

> Maintain an external reporting path for affected users, researchers, customers, and security reporters.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** No authoritative complete inventory covers every relevant AI artifact, actor, data flow, tool, dependency, state, or execution path required by the rule. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

A public form accepts reports, but affected users in a blocked region cannot reach it.

## `USEQ-61953451` — Nondeterministic

**Exact statement**

> Define the exact role, authority, competence, workload, information, and response time required of human reviewers.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

All reviewer fields exist, but the named reviewer lacks domain competence and has impossible workload.

## `USEQ-27053648` — Nondeterministic

**Exact statement**

> Ensure a human-in-the-loop control is operationally real rather than a nominal approval under impossible volume or time pressure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure a human-in-the-loop control is operationally real rather than a nominal approval under impossible volume or time pressure.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-8410B1CC` — Nondeterministic

**Exact statement**

> Provide reviewers the original input, relevant evidence, model output, uncertainty, applicable policy, prior history, and limitations needed to decide.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide reviewers the original input, relevant evidence, model output, uncertainty, applicable policy, prior history, and limitations needed to decide.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-027CBA6B` — Nondeterministic

**Exact statement**

> Prevent automation bias by training reviewers, presenting uncertainty, and making independent assessment possible.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent automation bias by training reviewers, presenting uncertainty, and making independent assessment possible.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-5283D0F9` — Nondeterministic

**Exact statement**

> Measure reviewer disagreement, override, fatigue, consistency, delay, and error.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Six reviewer metrics are recorded, but sampling excludes the exhausted night shift where errors occur.

## `USEQ-372E6D8F` — Nondeterministic

**Exact statement**

> Escalate cases beyond a reviewer's competence or authority.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Escalate cases beyond a reviewer's competence or authority.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-09AA097A` — Nondeterministic

**Exact statement**

> Record consequential human overrides and reasons without discouraging justified correction.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Override records exist, but the interface warns reviewers and suppresses justified corrections.

## `USEQ-EA629B07` — Nondeterministic

**Exact statement**

> Provide explanations that are faithful to the actual decision mechanism and useful for the user's action or appeal.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide explanations that are faithful to the actual decision mechanism and useful for the user's action or appeal.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-FF8035D0` — Nondeterministic

**Exact statement**

> Do not present generated rationales as causal explanations unless their fidelity has been established.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not present generated rationales as causal explanations unless their fidelity has been established.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-3836C247` — Nondeterministic

**Exact statement**

> Explain material data categories, factors, thresholds, source limitations, and human involvement at an appropriate level.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Explain material data categories, factors, thresholds, source limitations, and human involvement at an appropriate level.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-64875AE5` — Nondeterministic

**Exact statement**

> Provide meaningful notice and recourse without exposing trade secrets, security controls, other people's data, or evasion-sensitive detail unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide meaningful notice and recourse without exposing trade secrets, security controls, other people's data, or evasion-sensitive detail unnecessarily.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-1DC6F83B` — Nondeterministic

**Exact statement**

> Allow correction of inaccurate source data and propagate corrections to future decisions where applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Allow correction of inaccurate source data and propagate corrections to future decisions where applicable.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A3805124` — Nondeterministic

**Exact statement**

> Ensure appeals are reviewed by a qualified person or independent mechanism and are not decided solely by the same unchanged model.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure appeals are reviewed by a qualified person or independent mechanism and are not decided solely by the same unchanged model.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-82AF898D` — Nondeterministic

**Exact statement**

> Track restoration and remediation after a successful appeal, including downstream records and notifications.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Appeal records contain restoration fields, but a downstream credit bureau is never corrected.

## `USEQ-906B5981` — Nondeterministic

**Exact statement**

> Provide additional safeguards where users cannot reasonably understand, contest, or avoid the system.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide additional safeguards where users cannot reasonably understand, contest, or avoid the system.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-4E890ABE` — Nondeterministic

**Exact statement**

> Maintain system documentation describing purpose, architecture, data, models, prompts, tools, operating domain, metrics, limitations, risks, controls, owners, and change history.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Documentation has every heading, but omits a hidden tool and its material risk.

## `USEQ-57485A3D` — Nondeterministic

**Exact statement**

> Maintain model, data, prompt, retrieval, evaluation, and deployment documentation proportionate to impact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain model, data, prompt, retrieval, evaluation, and deployment documentation proportionate to impact.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-8E763CF7` — Nondeterministic

**Exact statement**

> Document known failure modes and conditions under which outputs must not be relied upon.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document known failure modes and conditions under which outputs must not be relied upon.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-268830F2` — Nondeterministic

**Exact statement**

> State whether outputs are generated, predicted, retrieved, transformed, or deterministically calculated where the distinction is material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “State whether outputs are generated, predicted, retrieved, transformed, or deterministically calculated where the distinction is material.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-85E488CB` — Nondeterministic

**Exact statement**

> Document source attribution, intellectual-property treatment, data retention, provider use, human review, and complaint paths.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document source attribution, intellectual-property treatment, data retention, provider use, human review, and complaint paths.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-C56A451D` — Nondeterministic

**Exact statement**

> Publish appropriate user-facing transparency without revealing security-sensitive implementation detail.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Publish appropriate user-facing transparency without revealing security-sensitive implementation detail.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-140A1C57` — Nondeterministic

**Exact statement**

> Make contractual and marketing claims consistent with measured evidence and supported versions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make contractual and marketing claims consistent with measured evidence and supported versions.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-CD81C32A` — Nondeterministic

**Exact statement**

> Do not imply certification, neutrality, fairness, accuracy, safety, explainability, or human equivalence beyond the actual assessment scope.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not imply certification, neutrality, fairness, accuracy, safety, explainability, or human equivalence beyond the actual assessment scope.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-33B02D9F` — Nondeterministic

**Exact statement**

> Maintain an auditable mapping from risks to controls, tests, monitoring, incidents, and responsible owners.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every risk row links controls and owners, but the listed control does not mitigate the risk.

## `USEQ-275D0D2D` — Nondeterministic

**Exact statement**

> Use independent assessment, conformity evaluation, external audit, red teaming, or expert review where consequence and obligation warrant it.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use independent assessment, conformity evaluation, external audit, red teaming, or expert review where consequence and obligation warrant it.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-D4ABA242` — Nondeterministic

**Exact statement**

> Retain evidence sufficient to reproduce or reconstruct consequential releases and decisions for the required period.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retain evidence sufficient to reproduce or reconstruct consequential releases and decisions for the required period.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-A687DF4E` — Nondeterministic

**Exact statement**

> Review documentation whenever the system changes and make stale documents visibly invalid.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

A version mismatch marks one document stale, while an untracked provider policy change leaves another current.

## `USEQ-7E3E586D` — Nondeterministic

**Exact statement**

> Define approved AI coding, review, documentation, testing, design, operations, and support tools and the data classifications each may receive.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The catalog lists approved tools and classifications, but omits a browser extension used with secrets.

## `USEQ-D13D7904` — Nondeterministic

**Exact statement**

> Prevent source code, secrets, credentials, customer data, incident evidence, vulnerabilities, proprietary designs, and regulated data from being submitted to unapproved services.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent source code, secrets, credentials, customer data, incident evidence, vulnerabilities, proprietary designs, and regulated data from being submitted to unapproved services.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-DC32CD99` — Nondeterministic

**Exact statement**

> Configure provider retention, training, sharing, logging, regional processing, and access settings according to organizational policy.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Provider settings match a policy field, but a regional subprocessor still retains prompts contrary to the real obligation.

## `USEQ-0E18CBD8` — Nondeterministic

**Exact statement**

> Treat generated code, tests, configurations, commands, infrastructure, migrations, dependencies, licenses, and documentation as untrusted contributions requiring accountable human review.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A review checkbox exists for generated code, but the human rubber-stamps a malicious migration.

## `USEQ-9A66039A` — Nondeterministic

**Exact statement**

> Make the human author responsible for understanding, validating, maintaining, and defending accepted AI-generated changes.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Make the human author responsible for understanding, validating, maintaining, and defending accepted AI-generated changes.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-482FDF4A` — Nondeterministic

**Exact statement**

> Require the same requirement, architecture, security, privacy, accessibility, test, review, and release controls for generated and manually written work.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

CI applies named gates to tracked code, while generated infrastructure outside the repository bypasses them.

## `USEQ-CAE04E9B` — Nondeterministic

**Exact statement**

> Do not merge, deploy, execute, publish, or apply generated output solely because it compiles, passes a narrow test, or appears plausible.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not merge, deploy, execute, publish, or apply generated output solely because it compiles, passes a narrow test, or appears plausible.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-700BB2C2` — Nondeterministic

**Exact statement**

> Verify APIs, library names, versions, options, standards references, citations, and commands against authoritative sources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify APIs, library names, versions, options, standards references, citations, and commands against authoritative sources.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7221EBC7` — Nondeterministic

**Exact statement**

> Check generated code for invented functions, deprecated practices, insecure defaults, hidden side effects, excessive privilege, weak error handling, and incorrect concurrency.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check generated code for invented functions, deprecated practices, insecure defaults, hidden side effects, excessive privilege, weak error handling, and incorrect concurrency.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-91CFE8F2` — Nondeterministic

**Exact statement**

> Review provenance, license, attribution, trade-secret, and substantial-similarity risks before accepting generated material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review provenance, license, attribution, trade-secret, and substantial-similarity risks before accepting generated material.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-3A1BED2B` — Nondeterministic

**Exact statement**

> Do not let generated tests merely reproduce the same misunderstanding as generated implementation; derive tests independently from requirements and invariants.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not let generated tests merely reproduce the same misunderstanding as generated implementation; derive tests independently from requirements and invariants.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-3654A2CD` — Nondeterministic

**Exact statement**

> Use adversarial, boundary, mutation, integration, and production-like validation for generated changes proportionate to impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use adversarial, boundary, mutation, integration, and production-like validation for generated changes proportionate to impact.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-86CAAD7F` — Nondeterministic

**Exact statement**

> Require explicit review for authentication, authorization, cryptography, payments, privacy, data deletion, migrations, infrastructure, safety, and incident code.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Review rules cover listed files, but generated authorization code hidden in a template is not classified for review.

## `USEQ-CC9F4CF0` — Nondeterministic

**Exact statement**

> Prevent agents from committing, opening privileged changes, modifying policies, rotating secrets, deploying, deleting resources, or changing production without scoped identity, policy gates, audit, and required approval.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The agent gateway blocks listed actions, while a plugin uses a developer token to deploy outside the gateway.

## `USEQ-1D87F2FE` — Nondeterministic

**Exact statement**

> Sandbox generated commands and code; inspect effects before execution and avoid granting broad filesystem, network, cloud, repository, or secret access.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Sandbox generated commands and code; inspect effects before execution and avoid granting broad filesystem, network, cloud, repository, or secret access.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-1AE8D9E6` — Nondeterministic

**Exact statement**

> Keep AI tool instructions, repository context files, reusable prompts, and agent policies versioned, reviewed, and protected from untrusted content.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep AI tool instructions, repository context files, reusable prompts, and agent policies versioned, reviewed, and protected from untrusted content.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-54BE67BA` — Nondeterministic

**Exact statement**

> Treat issue text, documentation, dependency files, comments, test output, web pages, and repository content as possible indirect instructions to coding agents.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Input types are labeled possible instructions, but a comment is still interpolated into a privileged command.

## `USEQ-1765CB7B` — Nondeterministic

**Exact statement**

> Measure AI-assisted development using quality, reliability, security, review effort, rework, learning, flow, and outcomes rather than generated volume or apparent speed alone.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Eight metrics exist, but they reward superficial review and miss defects reaching users.

## `USEQ-8F607BCB` — Nondeterministic

**Exact statement**

> Monitor whether AI use increases change size, dependency sprawl, duplicated code, shallow understanding, review burden, incidents, or knowledge concentration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Monitor whether AI use increases change size, dependency sprawl, duplicated code, shallow understanding, review burden, incidents, or knowledge concentration.” has checkable telemetry, but shallow understanding and causal increase require empirical definitions and interpretation.
- **Nondeterministic reason codes:** `undefined_protocol`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Read the authorized monitoring or management system and verify current observations exist for every named signal with bound provider, model, tool, dependency, owner, and immutable query timestamp.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-29A64D47` — Nondeterministic

**Exact statement**

> Provide training on verification, security, privacy, licensing, prompt injection, overreliance, and tool limitations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Training records cover every topic, but attendees cannot recognize a real indirect-injection attack.

## `USEQ-6A8DD723` — Nondeterministic

**Exact statement**

> Preserve meaningful authorship and review accountability in commit, approval, and incident records.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Commit and incident records name a person, but that person never reviewed or accepted the output.

## `USEQ-7071707F` — Nondeterministic

**Exact statement**

> Allow workers to report unsafe or low-quality AI pressure and do not mandate use where it reduces quality or accessibility.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Allow workers to report unsafe or low-quality AI pressure and do not mandate use where it reduces quality or accessibility.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-0B94F8FC` — Nondeterministic

**Exact statement**

> Regularly reassess whether the tool remains beneficial as models, terms, behavior, data practices, and organizational needs change.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Regularly reassess whether the tool remains beneficial as models, terms, behavior, data practices, and organizational needs change.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-0993C353` — Nondeterministic

**Exact statement**

> Define incident categories for harmful output, data leakage, unauthorized action, model compromise, poisoning, provider change, systemic bias, safety-control failure, model theft, fraud, and widespread misinformation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

All incident category labels exist, but a new combined fraud and poisoning event is misclassified and ignored.

## `USEQ-DE662B2B` — Nondeterministic

**Exact statement**

> Make incident detection, declaration, containment, evidence preservation, communication, recovery, and post-incident review part of the ordinary response program.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The response plan lists every phase, but evidence is destroyed during actual containment.

## `USEQ-49974079` — Nondeterministic

**Exact statement**

> Retain the minimum necessary input, output, model, prompt, tool, retrieval, policy, version, identity, and timeline evidence to investigate material events.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retain the minimum necessary input, output, model, prompt, tool, retrieval, policy, version, identity, and timeline evidence to investigate material events.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-0DD20497` — Nondeterministic

**Exact statement**

> Enable rapid disablement of a model, prompt, tool, data source, retrieval collection, tenant, capability, or provider without destroying unrelated essential service.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Enable rapid disablement of a model, prompt, tool, data source, retrieval collection, tenant, capability, or provider without destroying unrelated essential service.” has a bounded disablement test, but complete identification of unrelated essential service and acceptable impact is contextual.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-19BC3298` — Nondeterministic

**Exact statement**

> Revoke credentials, isolate endpoints, freeze artifacts, preserve provenance, and stop feedback ingestion during suspected compromise.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Revoke credentials, isolate endpoints, freeze artifacts, preserve provenance, and stop feedback ingestion during suspected compromise.” has checkable incident actions, but suspicion, scope, timing, and containment adequacy require incident-lead judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-89D0100E` — Nondeterministic

**Exact statement**

> Identify all users, decisions, content, records, and downstream systems affected by a faulty release or compromised source.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify all users, decisions, content, records, and downstream systems affected by a faulty release or compromised source.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3251BED0` — Nondeterministic

**Exact statement**

> Provide correction, reprocessing, notification, restoration, compensation, appeal, or human review appropriate to the impact.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide correction, reprocessing, notification, restoration, compensation, appeal, or human review appropriate to the impact.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-972E5264` — Nondeterministic

**Exact statement**

> Test rollback to a known-good model and verify that memories, caches, embeddings, queues, and prior unsafe outputs do not persist unnoticed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Test rollback to a known-good model and verify that memories, caches, embeddings, queues, and prior unsafe outputs do not persist unnoticed.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A7317A41` — Nondeterministic

**Exact statement**

> Define retraining and reevaluation criteria after incidents; do not resume solely because the immediate symptom disappeared.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Retraining criteria are documented, but service resumes after symptoms disappear without meeting them.

## `USEQ-9E544317` — Nondeterministic

**Exact statement**

> Retire obsolete models, prompts, datasets, indexes, feature definitions, credentials, endpoints, and provider access.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Retire obsolete models, prompts, datasets, indexes, feature definitions, credentials, endpoints, and provider access.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-21F4B26E` — Nondeterministic

**Exact statement**

> Preserve required records while deleting data and artifacts no longer authorized or needed.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve required records while deleting data and artifacts no longer authorized or needed.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-C1801C4C` — Nondeterministic

**Exact statement**

> Prevent retired artifacts from being accidentally redeployed through caches, registries, old clients, automation, or disaster recovery.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent retired artifacts from being accidentally redeployed through caches, registries, old clients, automation, or disaster recovery.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-85F03B12` — Nondeterministic

**Exact statement**

> Track successor compatibility and communicate material behavior changes to users and downstream consumers.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Track successor compatibility and communicate material behavior changes to users and downstream consumers.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-6F0FBC39` — Nondeterministic

**Exact statement**

> Review whether retirement changes legal holds, reproducibility, audit, deletion, or long-term support obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review whether retirement changes legal holds, reproducibility, audit, deletion, or long-term support obligations.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-6EDFEFA5` — Nondeterministic

**Exact statement**

> Do not deploy an AI system whose intended purpose, prohibited uses, affected population, decision authority, data provenance, and accountable owner are unknown.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not deploy an AI system whose intended purpose, prohibited uses, affected population, decision authority, data provenance, and accountable owner are unknown.” has an exact fail-closed evidence gate, but proving the full affected population and provenance are known is unbounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Compare the linked versioned policy, artifact, deployment, identity, permission, registry, or execution evidence and fail closed on missing, unreviewed, or mismatched identities.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-28269E66` — Nondeterministic

**Exact statement**

> Do not deploy a consequential system without representative evaluation, defined thresholds, known limitations, recourse, monitoring, and an effective stop mechanism.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not deploy a consequential system without representative evaluation, defined thresholds, known limitations, recourse, monitoring, and an effective stop mechanism.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-8CED23FB` — Nondeterministic

**Exact statement**

> Do not treat a provider benchmark, model card, certification, demo, or automated evaluation as sufficient local assurance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not treat a provider benchmark, model card, certification, demo, or automated evaluation as sufficient local assurance.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-B22A2372` — Nondeterministic

**Exact statement**

> Do not allow model output to authorize itself, bypass deterministic access control, or directly trigger high-impact action without independent controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The main tool requires independent authorization, while a hidden side-effect endpoint trusts model output.

## `USEQ-926988B6` — Nondeterministic

**Exact statement**

> Do not expose secrets, unrelated user data, privileged system context, or unauthorized retrieval content to a model.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not expose secrets, unrelated user data, privileged system context, or unauthorized retrieval content to a model.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FDE77A89` — Nondeterministic

**Exact statement**

> Do not deploy when material test leakage, benchmark contamination, unresolved cross-user leakage, poisoning, model compromise, or unsafe artifact provenance is suspected.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not deploy when material test leakage, benchmark contamination, unresolved cross-user leakage, poisoning, model compromise, or unsafe artifact provenance is suspected.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-7D42569E` — Nondeterministic

**Exact statement**

> Do not deploy a system that cannot abstain, degrade, or hand off safely outside its validated operating domain.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not deploy a system that cannot abstain, degrade, or hand off safely outside its validated operating domain.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-88BA6A09` — Nondeterministic

**Exact statement**

> Do not use a human-review label when reviewers lack the authority, competence, context, capacity, or time to prevent harm.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not use a human-review label when reviewers lack the authority, competence, context, capacity, or time to prevent harm.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-28F27AB9` — Nondeterministic

**Exact statement**

> Do not accept fairness, safety, privacy, or security performance based only on aggregate results that conceal material subgroup or high-consequence failures.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not accept fairness, safety, privacy, or security performance based only on aggregate results that conceal material subgroup or high-consequence failures.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-24C04A1C` — Nondeterministic

**Exact statement**

> Do not allow online learning, autonomous self-modification, or unrestricted agent capability without bounded governance, evaluation, authorization, and recovery.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not allow online learning, autonomous self-modification, or unrestricted agent capability without bounded governance, evaluation, authorization, and recovery.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5D5D0DDC` — Nondeterministic

**Exact statement**

> Do not use AI-generated code or infrastructure in production without accountable review and the same evidence required for human-authored work.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Generated code cannot merge without review, but the accountable reviewer lacks access to required security evidence.

## `USEQ-5C8EEFD1` — Nondeterministic

**Exact statement**

> Do not continue operation after a provider or model change invalidates the evidence package until affected controls are reassessed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not continue operation after a provider or model change invalidates the evidence package until affected controls are reassessed.” has exact version bindings and reassessment state, but deciding whether a change invalidates evidence requires accountable technical judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Compare the linked versioned policy, artifact, deployment, identity, permission, registry, or execution evidence and fail closed on missing, unreviewed, or mismatched identities.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

## `USEQ-2F8E6FA3` — Nondeterministic

**Exact statement**

> Maintain an inventory of AI-assisted development, review, testing, documentation, design, search, operations, support, and content tools used by the organization.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Maintain an inventory of AI-assisted development, review, testing, documentation, design, search, operations, support, and content tools used by the organization.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0E231454` — Nondeterministic

**Exact statement**

> Assign an accountable owner for each approved tool, provider, model family, deployment mode, integration, and use case.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every registered capability has an owner, while an unregistered local model processes production data.

## `USEQ-5DA9BD1E` — Nondeterministic

**Exact statement**

> Define permitted, restricted, and prohibited uses based on data sensitivity, code criticality, intellectual-property risk, safety, security, privacy, regulation, and customer commitments.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Define permitted, restricted, and prohibited uses based on data sensitivity, code criticality, intellectual-property risk, safety, security, privacy, regulation, and customer commitments.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-9F2C9B84` — Nondeterministic

**Exact statement**

> Require risk review before AI is used for security controls, authorization rules, cryptography, safety logic, financial calculations, migrations, production changes, legal interpretation, or other high-consequence work.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Risk-review records exist for named work, but an AI-generated legal interpretation is misclassified as low consequence.

## `USEQ-3FB8AC14` — Nondeterministic

**Exact statement**

> Distinguish advisory assistance from delegated decision-making and autonomous execution.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A record says advisory, while the integration automatically acts on the recommendation.

## `USEQ-7322984E` — Nondeterministic

**Exact statement**

> Ensure a named human remains accountable for every accepted artifact, decision, change, deployment, and communication produced with AI assistance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Each tracked artifact names a human, while model-generated customer messages outside the tracker have no accountable owner.

## `USEQ-26AC7D70` — Nondeterministic

**Exact statement**

> Do not treat provider availability, popularity, benchmark scores, or marketing claims as assurance of suitability.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not treat provider availability, popularity, benchmark scores, or marketing claims as assurance of suitability.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-3AC1CD29` — Nondeterministic

**Exact statement**

> Document model and tool limitations, known failure modes, supported contexts, retention behavior, geographic processing, and provider dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Document model and tool limitations, known failure modes, supported contexts, retention behavior, geographic processing, and provider dependencies.” has checkable document fields, but a bounded review cannot prove that all limitations and failure modes are known.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3B2F2EE8` — Nondeterministic

**Exact statement**

> Define the minimum review and verification required for each class of AI-generated output.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Minimum review levels are filled in, but a destructive output class is given only spell-check review.

## `USEQ-EE003D19` — Nondeterministic

**Exact statement**

> Prohibit bypassing review, testing, separation of duties, change control, or release gates because content was generated quickly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Fast-generated code cannot skip CI, while an emergency path bypasses separation of duties.

## `USEQ-8EF9CA6E` — Nondeterministic

**Exact statement**

> Prohibit representing AI-generated work as independently authored, reviewed, tested, or certified when it was not.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prohibit representing AI-generated work as independently authored, reviewed, tested, or certified when it was not.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-AB9ADB62` — Nondeterministic

**Exact statement**

> Train users in hallucination, automation bias, data leakage, prompt injection, insecure code, fabricated citations, licensing uncertainty, and overreliance risks.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Training completion is recorded, but users still trust fabricated citations in production.

## `USEQ-12008C5B` — Nondeterministic

**Exact statement**

> Provide an accessible process for reporting harmful, insecure, inaccurate, biased, infringing, or confidential AI output.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Provide an accessible process for reporting harmful, insecure, inaccurate, biased, infringing, or confidential AI output.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-3F338012` — Nondeterministic

**Exact statement**

> Review governance after model changes, provider terms changes, incidents, new integrations, new agent capabilities, or expansion into higher-risk work.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Governance review events are recorded, but a new high-risk agent tool is not recognized as triggering review.

## `USEQ-5E567DE3` — Nondeterministic

**Exact statement**

> Apply stricter controls to autonomous or background agents than to read-only interactive assistants.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Apply stricter controls to autonomous or background agents than to read-only interactive assistants.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-5F29B160` — Nondeterministic

**Exact statement**

> Record the provider, model identifier, model version or update channel, hosting location, integration version, enabled features, data flows, tools, plugins, and permissions for each AI capability.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Each catalog row has all fields, but an active plugin and its permissions are absent.

## `USEQ-746C8D31` — Nondeterministic

**Exact statement**

> Record whether prompts, files, code, outputs, telemetry, feedback, embeddings, and metadata are retained or used for provider training.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Retention fields are populated, but provider training actually remains enabled despite the recorded value.

## `USEQ-AC2FD44B` — Nondeterministic

**Exact statement**

> Record the contractual terms, service levels, security commitments, privacy terms, subprocessors, data locations, deletion behavior, and incident-notification obligations.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Contract fields are copied into a record, but a deletion carve-out permits indefinite storage.

## `USEQ-29597410` — Nondeterministic

**Exact statement**

> Identify whether model behavior can change without an explicit customer-controlled version update.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A field says behavior can change, but undocumented provider routing changes behavior anyway.

## `USEQ-100B3A4A` — Nondeterministic

**Exact statement**

> Pin or validate model versions for workflows where reproducibility and regression assurance matter.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Pin or validate model versions for workflows where reproducibility and regression assurance matter.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-054DF3AB` — Nondeterministic

**Exact statement**

> Monitor model deprecation, behavior changes, safety-policy changes, context limits, pricing changes, and regional availability.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Read the authorized monitoring or management system and verify current observations exist for every named signal with bound provider, model, tool, dependency, owner, and immutable query timestamp.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Six monitoring feeds are current, but a silent safety-policy change has no observable signal.

## `USEQ-1CAE8F75` — Nondeterministic

**Exact statement**

> Inventory extensions, browser integrations, editor plugins, local agents, command-line tools, code-review bots, and hidden AI features in existing products.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Inventory extensions, browser integrations, editor plugins, local agents, command-line tools, code-review bots, and hidden AI features in existing products.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-C849B749` — Nondeterministic

**Exact statement**

> Prevent unapproved tools from accessing source repositories, tickets, production data, customer content, secrets, or internal documentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent unapproved tools from accessing source repositories, tickets, production data, customer content, secrets, or internal documentation.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-BC94E90D` — Nondeterministic

**Exact statement**

> Use organization-managed identities and configuration rather than uncontrolled personal accounts for business use.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Managed accounts are required in policy, while a personal API key remains active in a local tool.

## `USEQ-D4210FCC` — Nondeterministic

**Exact statement**

> Restrict tool permissions and repository access to the minimum required scope.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Restrict tool permissions and repository access to the minimum required scope.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-8BFAB4A7` — Nondeterministic

**Exact statement**

> Review integration webhooks, OAuth grants, tokens, local file access, network access, and command execution.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

All integration access types have review records, but an OAuth grant added later has excessive scope.

## `USEQ-81875F3C` — Nondeterministic

**Exact statement**

> Ensure provider compromise or outage has an isolation, disablement, replacement, and continuity plan.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A continuity plan has every section, but replacement cannot process the required data format during outage.

## `USEQ-58409064` — Nondeterministic

**Exact statement**

> Verify that disabling the AI feature also removes active credentials, agents, callbacks, scheduled tasks, and data flows.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** No authoritative complete inventory covers every relevant AI artifact, actor, data flow, tool, dependency, state, or execution path required by the rule. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

The disable test removes registered resources, while an unknown callback and scheduled task remain active.

## `USEQ-F936BCAB` — Nondeterministic

**Exact statement**

> Reconcile the approved inventory with actual network, repository, identity, procurement, and endpoint telemetry.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Five telemetry sources reconcile for registered tools, while an endpoint agent is invisible to all of them.

## `USEQ-46F439A2` — Nondeterministic

**Exact statement**

> Expire temporary pilots automatically unless they receive formal approval.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

A registered pilot expires, while an unregistered pilot continues using customer data indefinitely.

## `USEQ-19F84B67` — Nondeterministic

**Exact statement**

> Classify source code, prompts, tickets, designs, logs, customer data, credentials, vulnerabilities, contracts, and documents before they are supplied to an AI system.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Inputs have classifications, but a copied secret inside an ordinary ticket is misclassified before upload.

## `USEQ-3E8114EE` — Nondeterministic

**Exact statement**

> Do not submit secrets, private keys, reusable tokens, production credentials, payment data, regulated data, or unredacted sensitive personal data unless explicitly approved and technically protected.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not submit secrets, private keys, reusable tokens, production credentials, payment data, regulated data, or unredacted sensitive personal data unless explicitly approved and technically protected.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-5BC689C8` — Nondeterministic

**Exact statement**

> Minimize prompt and context data to what is necessary for the task.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Minimize prompt and context data to what is necessary for the task.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-C0D20FDF` — Nondeterministic

**Exact statement**

> Redact or synthesize examples where real identities, customer content, incidents, vulnerabilities, or proprietary algorithms are not necessary.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Redact or synthesize examples where real identities, customer content, incidents, vulnerabilities, or proprietary algorithms are not necessary.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3227DC4E` — Nondeterministic

**Exact statement**

> Prevent tools from automatically indexing repositories, drives, conversations, or documents beyond the approved scope.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Repository indexing respects configured paths, while symlink traversal indexes an unapproved drive.

## `USEQ-949B76FD` — Nondeterministic

**Exact statement**

> Verify retention, deletion, model-training, abuse-monitoring, human-review, and backup terms before supplying confidential material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify retention, deletion, model-training, abuse-monitoring, human-review, and backup terms before supplying confidential material.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-393DB5A1` — Nondeterministic

**Exact statement**

> Ensure data-subject rights, contractual deletion, legal holds, and retention requirements extend to prompts, outputs, embeddings, caches, and provider copies where applicable.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure data-subject rights, contractual deletion, legal holds, and retention requirements extend to prompts, outputs, embeddings, caches, and provider copies where applicable.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-D3455DAB` — Nondeterministic

**Exact statement**

> Assess cross-border transfer and data-residency implications of provider processing.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Assess cross-border transfer and data-residency implications of provider processing.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-BDBA143D` — Nondeterministic

**Exact statement**

> Do not assume output is free of third-party intellectual property, confidential information, personal data, or license obligations.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not assume output is free of third-party intellectual property, confidential information, personal data, or license obligations.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-C6ED3A2C` — Nondeterministic

**Exact statement**

> Review generated code, text, media, designs, and tests for substantial similarity, copied notices, trademarks, confidential patterns, and incompatible licenses when risk warrants it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review generated code, text, media, designs, and tests for substantial similarity, copied notices, trademarks, confidential patterns, and incompatible licenses when risk warrants it.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-8FDF8863` — Nondeterministic

**Exact statement**

> Preserve required attribution, license, and provenance information for incorporated material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve required attribution, license, and provenance information for incorporated material.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-A41AE117` — Nondeterministic

**Exact statement**

> Prevent prompts from requesting unauthorized access, circumvention, copying, de-anonymization, or reconstruction of restricted information.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent prompts from requesting unauthorized access, circumvention, copying, de-anonymization, or reconstruction of restricted information.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A873CC6F` — Nondeterministic

**Exact statement**

> Ensure provider telemetry and organizational monitoring do not create disproportionate employee surveillance.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure provider telemetry and organizational monitoring do not create disproportionate employee surveillance.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-9555C8D3` — Nondeterministic

**Exact statement**

> Document legitimate purpose, access, retention, and review for AI-use telemetry.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Telemetry documentation has four fields, but the claimed legitimate purpose is legally invalid.

## `USEQ-69C5D0B6` — Nondeterministic

**Exact statement**

> Test deletion and account termination across local caches, provider stores, embeddings, logs, and integrations.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Deletion fixtures pass known stores, while a provider backup and plugin cache retain the account data.

## `USEQ-525FCCE0` — Nondeterministic

**Exact statement**

> Treat user prompts, retrieved documents, source comments, issue text, web content, dependency metadata, tool output, and model output as untrusted input.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every input type is tagged untrusted, but tool output is still executed as a shell command.

## `USEQ-464BA3EB` — Nondeterministic

**Exact statement**

> Separate system policy, developer instructions, user requests, retrieved content, and tool results so that untrusted text cannot silently become authoritative instructions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Separate system policy, developer instructions, user requests, retrieved content, and tool results so that untrusted text cannot silently become authoritative instructions.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FC5D2312` — Nondeterministic

**Exact statement**

> Do not rely on prompt wording alone to enforce authorization, confidentiality, financial limits, safety, or change-control policy.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The primary path uses code authorization, while a fallback prompt alone decides financial limits.

## `USEQ-D1A9A000` — Nondeterministic

**Exact statement**

> Validate identity, authorization, tenant, data classification, and purpose before retrieving or supplying context.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Identity and purpose checks run on live retrieval, while cached context bypasses them after a role change.

## `USEQ-22B94B7A` — Nondeterministic

**Exact statement**

> Apply access control before retrieval rather than filtering unauthorized results after they have reached the model.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The search service filters before retrieval, while a secondary vector cache sends unauthorized chunks to the model.

## `USEQ-F5A43B34` — Nondeterministic

**Exact statement**

> Preserve tenant and user boundaries in indexes, embeddings, caches, conversation memory, tool calls, and logs.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Preserve tenant and user boundaries in indexes, embeddings, caches, conversation memory, tool calls, and logs.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DCD08BD7` — Nondeterministic

**Exact statement**

> Detect and handle direct and indirect prompt injection, malicious repository instructions, poisoned documentation, and adversarial tool output.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Detect and handle direct and indirect prompt injection, malicious repository instructions, poisoned documentation, and adversarial tool output.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-5BD7D44C` — Nondeterministic

**Exact statement**

> Limit the sources, domains, paths, repositories, branches, documents, and record types the system may retrieve.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Allowlist fields cover all named source types, but wildcard paths expose a private branch.

## `USEQ-17A2E4C0` — Nondeterministic

**Exact statement**

> Record source provenance and freshness for material facts used in generated output.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Citations store source and timestamp, but a material claim comes from an untracked model memory.

## `USEQ-AD2E1C13` — Nondeterministic

**Exact statement**

> Require primary or authoritative sources for claims whose accuracy materially affects engineering or compliance decisions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Require primary or authoritative sources for claims whose accuracy materially affects engineering or compliance decisions.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-0E651845` — Nondeterministic

**Exact statement**

> Reject or clearly mark unsupported citations, fabricated identifiers, nonexistent APIs, invented standards, and unverifiable facts.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reject or clearly mark unsupported citations, fabricated identifiers, nonexistent APIs, invented standards, and unverifiable facts.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-D19D4771` — Nondeterministic

**Exact statement**

> Bound context size and prioritize relevant evidence rather than truncating critical policy or requirements silently.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Bound context size and prioritize relevant evidence rather than truncating critical policy or requirements silently.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-3C9530AD` — Nondeterministic

**Exact statement**

> Do not permit hidden conversation state or memory to alter high-impact behavior without visibility and control.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not permit hidden conversation state or memory to alter high-impact behavior without visibility and control.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-DA4C03E8` — Nondeterministic

**Exact statement**

> Make context expiration, refresh, and invalidation rules explicit after source changes or permission changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Expiration rules are explicit, but a permission revocation does not invalidate cached embeddings.

## `USEQ-3B2A4D90` — Nondeterministic

**Exact statement**

> Test the system with conflicting instructions, malicious comments, deceptive filenames, encoded payloads, and poisoned retrieval content.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Five attack fixtures pass, while a mixed-script poisoned document triggers a privileged tool.

## `USEQ-45E865D0` — Nondeterministic

**Exact statement**

> Require the accepting engineer or reviewer to understand the generated artifact well enough to explain its purpose, assumptions, risks, and failure modes.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Require the accepting engineer or reviewer to understand the generated artifact well enough to explain its purpose, assumptions, risks, and failure modes.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-706392C8` — Nondeterministic

**Exact statement**

> Reject code or configuration that no accountable maintainer can understand and support.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reject code or configuration that no accountable maintainer can understand and support.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-27ADA51E` — Nondeterministic

**Exact statement**

> Require independent review for high-impact AI-generated changes even when the generator also performs a review.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Policy requires another reviewer, but the same model instance signs under a second automated identity.

## `USEQ-3DF6CB04` — Nondeterministic

**Exact statement**

> Ensure reviewers know which portions were generated, transformed, or suggested by AI when that knowledge affects review strategy.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure reviewers know which portions were generated, transformed, or suggested by AI when that knowledge affects review strategy.” has a separable record or technical-evidence requirement, but completeness, review quality, materiality, user impact, accountability, or adequacy still requires human judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-159E86E9` — Nondeterministic

**Exact statement**

> Prevent the same AI output from serving as implementation, expected result, reviewer, and approval evidence without independent validation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Evidence records separate roles, while implementation and expected result still derive from the same hidden context.

## `USEQ-1023D68F` — Nondeterministic

**Exact statement**

> Use checklists and evidence prompts that direct reviewers to correctness, security, privacy, performance, accessibility, maintainability, licensing, and operational impact.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

The checklist names all topics, but reviewers click through without testing a licensing problem.

## `USEQ-C2BD1A86` — Nondeterministic

**Exact statement**

> Require reviewers to inspect surrounding code and system behavior rather than only the generated diff.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Require reviewers to inspect surrounding code and system behavior rather than only the generated diff.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-97B09BF8` — Nondeterministic

**Exact statement**

> Verify all assumptions, external facts, API behavior, version compatibility, and cited requirements against authoritative sources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify all assumptions, external facts, API behavior, version compatibility, and cited requirements against authoritative sources.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-568D42D2` — Nondeterministic

**Exact statement**

> Challenge plausible-looking complexity, abstractions, dependencies, and comments that lack a demonstrated need.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Challenge plausible-looking complexity, abstractions, dependencies, and comments that lack a demonstrated need.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-35A8F0B6` — Nondeterministic

**Exact statement**

> Require explicit approval for deletions, migrations, permission changes, security-control changes, and production commands.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Approval gates cover known commands, while a deletion embedded in a generated migration is not recognized.

## `USEQ-5B11CA85` — Nondeterministic

**Exact statement**

> Use dual control for irreversible, financially consequential, safety-related, or organization-wide actions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Two approvers sign, but both belong to the same compromised automation account.

## `USEQ-7299FE1A` — Nondeterministic

**Exact statement**

> Measure escaped defects and review failures involving AI-assisted work without using metrics to hide or discourage appropriate use.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Measure escaped defects and review failures involving AI-assisted work without using metrics to hide or discourage appropriate use.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-A0D8343C` — Nondeterministic

**Exact statement**

> Design interfaces to show uncertainty, missing evidence, tool actions, changed files, affected systems, and unresolved warnings.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

The interface shows six panels, but hides the affected production system behind a collapsed default.

## `USEQ-00A677A3` — Nondeterministic

**Exact statement**

> Prevent speed or productivity targets from encouraging rubber-stamping of generated work.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent speed or productivity targets from encouraging rubber-stamping of generated work.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-EC36170F` — Nondeterministic

**Exact statement**

> Retain accountability records for accepted high-impact outputs and decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

High-impact records exist, but an accepted model decision is never linked to the responsible human.

## `USEQ-BB79B60F` — Nondeterministic

**Exact statement**

> Apply every ordinary code-quality, architecture, security, privacy, performance, accessibility, and maintainability rule to AI-generated code without reduction.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

CI lists every ordinary rule, but generated code bypasses a language-specific analyzer.

## `USEQ-28217B46` — Nondeterministic

**Exact statement**

> Verify that generated code satisfies the exact requirement rather than a superficially similar generic pattern.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify that generated code satisfies the exact requirement rather than a superficially similar generic pattern.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-B5C04073` — Nondeterministic

**Exact statement**

> Confirm names, abstractions, boundaries, data types, error behavior, state transitions, and concurrency semantics match the existing system.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Confirm names, abstractions, boundaries, data types, error behavior, state transitions, and concurrency semantics match the existing system.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-439A8D87` — Nondeterministic

**Exact statement**

> Remove unnecessary duplication, speculative generality, dead code, excessive comments, unused dependencies, and invented configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Remove unnecessary duplication, speculative generality, dead code, excessive comments, unused dependencies, and invented configuration.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0A35B7B4` — Nondeterministic

**Exact statement**

> Check for fabricated APIs, obsolete methods, invalid options, deprecated protocols, unsupported versions, and nonexistent packages.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for fabricated APIs, obsolete methods, invalid options, deprecated protocols, unsupported versions, and nonexistent packages.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-80CB3947` — Nondeterministic

**Exact statement**

> Check for injection, unsafe deserialization, path traversal, authorization bypass, secret exposure, insecure randomness, weak cryptography, and dangerous defaults.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for injection, unsafe deserialization, path traversal, authorization bypass, secret exposure, insecure randomness, weak cryptography, and dangerous defaults.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-069C7D6E` — Nondeterministic

**Exact statement**

> Check for integer, numeric, time, locale, Unicode, encoding, null, overflow, and precision errors.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for integer, numeric, time, locale, Unicode, encoding, null, overflow, and precision errors.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-90FD5CD0` — Nondeterministic

**Exact statement**

> Check for resource leaks, unbounded work, missing timeouts, unsafe retries, race conditions, deadlocks, cancellation defects, and duplicate effects.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for resource leaks, unbounded work, missing timeouts, unsafe retries, race conditions, deadlocks, cancellation defects, and duplicate effects.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-FDF5A263` — Nondeterministic

**Exact statement**

> Check for missing observability, misleading logs, swallowed errors, broad exception handling, and loss of diagnostic context.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for missing observability, misleading logs, swallowed errors, broad exception handling, and loss of diagnostic context.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-E92C614F` — Nondeterministic

**Exact statement**

> Check for accidental cross-tenant access, global mutable state, cache-key omissions, and authorization context loss.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check for accidental cross-tenant access, global mutable state, cache-key omissions, and authorization context loss.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2BB3F020` — Nondeterministic

**Exact statement**

> Check whether generated code duplicates an existing internal capability or violates established architecture decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check whether generated code duplicates an existing internal capability or violates established architecture decisions.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-8BBA5659` — Nondeterministic

**Exact statement**

> Prefer small changes that fit the current design over broad rewrites generated without full system context.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prefer small changes that fit the current design over broad rewrites generated without full system context.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-471DDE50` — Nondeterministic

**Exact statement**

> Require generated public interfaces and persistent schemas to undergo normal compatibility and evolution review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Compatibility review is required for schema files, while an inferred public interface is not identified as such.

## `USEQ-FF208505` — Nondeterministic

**Exact statement**

> Run formatting, type checking, static analysis, dependency analysis, tests, and relevant security checks on the exact accepted output.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Run formatting, type checking, static analysis, dependency analysis, tests, and relevant security checks on the exact accepted output.” has checkable tool execution and artifact identity, but which security checks are relevant depends on the generated change and project risk.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
   - **Checker family:** `ci_policy`
   - **Evidence authority:** `repository`

## `USEQ-592A4BA4` — Nondeterministic

**Exact statement**

> Retain regression tests for defects found in generated code and update organizational guidance to prevent recurrence.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A regression test exists for the defect, but does not fail when the defect is reintroduced.

## `USEQ-239E5251` — Nondeterministic

**Exact statement**

> Review whether generated tests assert intended requirements or merely reproduce current implementation behavior.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review whether generated tests assert intended requirements or merely reproduce current implementation behavior.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-BF92D3B4` — Nondeterministic

**Exact statement**

> Prevent the same model context from generating code and expected outputs without independent oracle validation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Contexts are recorded as independent, while both code and oracle came from the same upstream model answer.

## `USEQ-7BAE92AE` — Nondeterministic

**Exact statement**

> Verify that tests fail when the intended behavior is deliberately broken.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

One deliberate mutation makes a test fail, while other intended behavior has no effective assertion.

## `USEQ-36C03659` — Nondeterministic

**Exact statement**

> Use mutation or controlled fault insertion to evaluate assertion strength for critical generated tests.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Use mutation or controlled fault insertion to evaluate assertion strength for critical generated tests.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-524C0709` — Nondeterministic

**Exact statement**

> Ensure generated tests cover negative, boundary, error, recovery, authorization, concurrency, and data-integrity behavior rather than only happy paths.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure generated tests cover negative, boundary, error, recovery, authorization, concurrency, and data-integrity behavior rather than only happy paths.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-12534F05` — Nondeterministic

**Exact statement**

> Review generated mocks and stubs for incorrect assumptions about real dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review generated mocks and stubs for incorrect assumptions about real dependencies.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F374893D` — Nondeterministic

**Exact statement**

> Ensure generated fixtures do not contain real personal data, credentials, proprietary examples, or unsafe production identifiers.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure generated fixtures do not contain real personal data, credentials, proprietary examples, or unsafe production identifiers.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-ED97CA2A` — Nondeterministic

**Exact statement**

> Control randomness and retain seeds for generated property, fuzz, and scenario tests.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Seeds are retained for registered fuzz jobs, while an untracked scenario generator uses uncontrolled randomness.

## `USEQ-44E65FA4` — Nondeterministic

**Exact statement**

> Remove redundant or brittle tests that create maintenance cost without meaningful assurance.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Remove redundant or brittle tests that create maintenance cost without meaningful assurance.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-44661241` — Nondeterministic

**Exact statement**

> Verify that snapshots and golden files represent approved outcomes and do not normalize accidental output.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify that snapshots and golden files represent approved outcomes and do not normalize accidental output.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-BAE2D2AE` — Nondeterministic

**Exact statement**

> Review test names, diagnostics, setup, cleanup, isolation, and failure messages for maintainability.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review test names, diagnostics, setup, cleanup, isolation, and failure messages for maintainability.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9142AF7A` — Nondeterministic

**Exact statement**

> Ensure generated test data exercises representative Unicode, locale, time, size, tenant, role, and lifecycle cases.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure generated test data exercises representative Unicode, locale, time, size, tenant, role, and lifecycle cases.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-16875252` — Nondeterministic

**Exact statement**

> Run generated tests against the actual artifact and production-like configuration where relevant.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Run generated tests against the actual artifact and production-like configuration where relevant.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-46A93573` — Nondeterministic

**Exact statement**

> Track defects escaped despite generated tests and improve prompts, review, or techniques based on root cause.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Track defects escaped despite generated tests and improve prompts, review, or techniques based on root cause.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F7978D60` — Nondeterministic

**Exact statement**

> Do not count generated test volume as quality evidence without coverage, assertion, and fault-detection analysis.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Coverage and mutation scores are present, but assertions only check that execution does not crash.

## `USEQ-6DD50F52` — Nondeterministic

**Exact statement**

> Validate generated requirements through user research, stakeholder agreement, domain evidence, legal review, and operational feasibility.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Validate generated requirements through user research, stakeholder agreement, domain evidence, legal review, and operational feasibility.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-DD4E49F4` — Nondeterministic

**Exact statement**

> Reject invented user needs, regulations, standards, constraints, metrics, competitors, incidents, or customer commitments.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reject invented user needs, regulations, standards, constraints, metrics, competitors, incidents, or customer commitments.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-B4A6F52A` — Nondeterministic

**Exact statement**

> Identify assumptions and uncertainty explicitly rather than converting them into authoritative prose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Identify assumptions and uncertainty explicitly rather than converting them into authoritative prose.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Use a pinned analyzer, authoritative query, or bounded test to record the concrete model, code, configuration, dependency, permission, or runtime behavior named by this control without claiming complete AI or production coverage.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-3E4A6B5A` — Nondeterministic

**Exact statement**

> Verify that generated acceptance criteria are measurable, complete, noncontradictory, and technology-neutral where intended.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify that generated acceptance criteria are measurable, complete, noncontradictory, and technology-neutral where intended.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-3BCFBFCB` — Nondeterministic

**Exact statement**

> Review generated architecture against actual scale, data, trust boundaries, failure domains, existing systems, skills, cost, and lifecycle constraints.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review generated architecture against actual scale, data, trust boundaries, failure domains, existing systems, skills, cost, and lifecycle constraints.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-1CD27635` — Nondeterministic

**Exact statement**

> Require alternatives, tradeoffs, consequences, and reversal strategy in material generated design decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Decision records have four headings, but omit the viable alternative that would avoid the main risk.

## `USEQ-B9E44CC3` — Nondeterministic

**Exact statement**

> Avoid adopting fashionable patterns, services, abstractions, or distributed architectures without demonstrated need.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Avoid adopting fashionable patterns, services, abstractions, or distributed architectures without demonstrated need.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-7BA2175A` — Nondeterministic

**Exact statement**

> Verify diagrams, flows, schemas, sequence descriptions, and dependency maps against the deployed or intended system.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify diagrams, flows, schemas, sequence descriptions, and dependency maps against the deployed or intended system.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-4344CC44` — Nondeterministic

**Exact statement**

> Threat-model generated architectures independently and check for hidden trust, control-plane, tenant, and data-flow assumptions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Threat-model generated architectures independently and check for hidden trust, control-plane, tenant, and data-flow assumptions.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-B77FC1DD` — Nondeterministic

**Exact statement**

> Review generated data models for integrity, retention, privacy, migration, localization, and reporting requirements.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review generated data models for integrity, retention, privacy, migration, localization, and reporting requirements.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-F3633846` — Nondeterministic

**Exact statement**

> Review generated performance and capacity estimates against measurements and defensible workload models.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review generated performance and capacity estimates against measurements and defensible workload models.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-D7408E34` — Nondeterministic

**Exact statement**

> Ensure generated decisions preserve accessibility, supportability, observability, rollback, recovery, and decommissioning.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure generated decisions preserve accessibility, supportability, observability, rollback, recovery, and decommissioning.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-5E4618A5` — Nondeterministic

**Exact statement**

> Record AI assistance and source evidence in material architecture decision records where relevant to future review.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Record AI assistance and source evidence in material architecture decision records where relevant to future review.” has checkable decision-record fields, but materiality and future relevance require accountable architecture judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E7FE1B6C` — Nondeterministic

**Exact statement**

> Do not let generated documentation substitute for accountable stakeholder decisions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not let generated documentation substitute for accountable stakeholder decisions.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-AE086A58` — Nondeterministic

**Exact statement**

> Revalidate generated designs after requirements, providers, standards, risks, or constraints change.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A review timestamp follows a provider change, but no affected design assumption is actually revalidated.

## `USEQ-24BBED4A` — Nondeterministic

**Exact statement**

> Treat generated infrastructure, deployment, policy, database, query, migration, and operational code as high-impact executable artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Artifacts are labeled high impact, but a generated database query is executed through a route that ignores the label.

## `USEQ-802762AD` — Nondeterministic

**Exact statement**

> Run schema validation, static analysis, policy checks, security analysis, plan or dry-run review, and peer review before execution.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

All six pre-execution steps have records, but peer review misses a destructive region change.

## `USEQ-764B2420` — Nondeterministic

**Exact statement**

> Verify resource names, accounts, regions, networks, identities, permissions, quotas, retention, encryption, and deletion protection.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Resource fields verify against a plan, but the plan names the wrong production account.

## `USEQ-ECEF8251` — Nondeterministic

**Exact statement**

> Check that generated permissions are least-privilege and do not use broad wildcards for convenience.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check that generated permissions are least-privilege and do not use broad wildcards for convenience.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-43965569` — Nondeterministic

**Exact statement**

> Check that generated network rules do not expose administrative, database, storage, metadata, or internal services unnecessarily.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check that generated network rules do not expose administrative, database, storage, metadata, or internal services unnecessarily.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-A6D8DA54` — Nondeterministic

**Exact statement**

> Check that generated configuration does not enable debug mode, insecure defaults, public access, unbounded resources, or disabled verification.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Check that generated configuration does not enable debug mode, insecure defaults, public access, unbounded resources, or disabled verification.” has an objectively checkable record, policy, artifact, analyzer, registry fact, or bounded test, but full pass meaning also depends on semantic completeness, context, or AI behavior.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the bounded AI policy, review, inventory, provider, architecture, dependency, or evidence record explicitly named by this control without treating it as proof of semantic correctness or safety.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-B9138B79` — Nondeterministic

**Exact statement**

> Evaluate generated database queries for correctness, authorization, injection, locking, resource cost, indexing, consistency, and data minimization.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Evaluate generated database queries for correctness, authorization, injection, locking, resource cost, indexing, consistency, and data minimization.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-F11E331B` — Nondeterministic

**Exact statement**

> Rehearse generated migrations against representative data and validate compatibility, duration, locking, restart, rollback, roll-forward, and integrity checks.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Rehearse generated migrations against representative data and validate compatibility, duration, locking, restart, rollback, roll-forward, and integrity checks.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-F7C0B574` — Nondeterministic

**Exact statement**

> Require previews and explicit confirmation before destructive or irreversible actions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A preview and confirmation appear, but the preview hides an irreversible dependent deletion.

## `USEQ-D20D5B1C` — Nondeterministic

**Exact statement**

> Prevent agents from executing production commands with credentials inherited from a developer session without controlled elevation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The official command runner elevates safely, while an agent invokes a production CLI with inherited shell credentials.

## `USEQ-6A91D81E` — Nondeterministic

**Exact statement**

> Use isolated sandboxes and nonproduction environments for generated command experimentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Registered experiments use sandboxes, while a generated command runs in a developer session connected to production.

## `USEQ-53136269` — Nondeterministic

**Exact statement**

> Record exact generated commands, plans, affected resources, approvals, execution output, and resulting state.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Command records contain every field, but the recorded resulting state differs from the actual partially failed state.

## `USEQ-21445766` — Nondeterministic

**Exact statement**

> Verify idempotency and safe rerun behavior for automation that may be retried.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify idempotency and safe rerun behavior for automation that may be retried.” has a bounded duplicate-execution test, but safe rerun behavior across all production states and side effects is not fully specified.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Run the registered bounded protocol and verify authenticated evidence records the exact inputs, identity and permission context, expected transitions, side effects, cleanup, and successful result.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-386CA87E` — Nondeterministic

**Exact statement**

> Ensure generated cleanup does not delete shared, retained, legally held, or customer-owned resources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Ensure generated cleanup does not delete shared, retained, legally held, or customer-owned resources.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7D1589E9` — Nondeterministic

**Exact statement**

> Maintain a tested rollback or recovery path before high-impact generated changes are applied.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A rollback test passes a clean fixture, but cannot restore data after the actual high-impact migration.

## `USEQ-A42E53F8` — Nondeterministic

**Exact statement**

> Verify every suggested package, image, action, plugin, model, data set, API, and service exists and comes from the intended publisher.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** No authoritative complete inventory covers every relevant AI artifact, actor, data flow, tool, dependency, state, or execution path required by the rule. The proposed deterministic clause only establishes: Query the authoritative registry for each declared package, image, action, plugin, model, data set, API, or service and verify canonical identity, existence, publisher, and requested version.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Registry identity checks pass a lookalike package whose publisher account was compromised.

## `USEQ-ED0BD0F8` — Nondeterministic

**Exact statement**

> Check spelling, namespace, registry, repository, ownership, signatures, provenance, release history, and known compromise before adoption.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Names, signatures, history, and advisories pass, while malicious ownership transfer occurs just before adoption.

## `USEQ-D2946F6A` — Nondeterministic

**Exact statement**

> Prefer established existing dependencies already approved for the system when they satisfy the requirement.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prefer established existing dependencies already approved for the system when they satisfy the requirement.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-E4E7B8C5` — Nondeterministic

**Exact statement**

> Reject unnecessary dependencies introduced to solve trivial problems or because they appear in generic training examples.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Reject unnecessary dependencies introduced to solve trivial problems or because they appear in generic training examples.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-EC5B4CD4` — Nondeterministic

**Exact statement**

> Verify supported versions, maintenance health, security process, license, data use, portability, and replacement feasibility.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Verify supported versions, maintenance health, security process, license, data use, portability, and replacement feasibility.” has checkable artifact, attribution, dependency, test, preservation, or source evidence, but full compliance also depends on legal, privacy, licensing, or specialist authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-69099CAD` — Nondeterministic

**Exact statement**

> Pin or constrain dependencies according to supply-chain policy and generate updated SBOM and provenance evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

The SBOM and provenance update, but a loosely constrained transitive dependency violates supply-chain policy.

## `USEQ-8BF5FC3E` — Nondeterministic

**Exact statement**

> Review copied or generated license headers, attribution, notices, and source obligations for accuracy.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Review copied or generated license headers, attribution, notices, and source obligations for accuracy.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-6C4E1F4A` — Nondeterministic

**Exact statement**

> Do not assume generated code is original or compatible with the project license.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Do not assume generated code is original or compatible with the project license.” depends on law, privacy, employment, licensing, intellectual property, fairness, accessibility, data rights, or domain-specialist authority unavailable to a generic scanner.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_scope`

## `USEQ-0BE33354` — Nondeterministic

**Exact statement**

> Search for suspiciously distinctive fragments or notices when generated output resembles known external code.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Search for suspiciously distinctive fragments or notices when generated output resembles known external code.” requires model fidelity, reviewer behavior, representative evaluation, safety quality, semantic correctness, performance evidence, or similarity judgment without a complete protocol, oracle, and threshold.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`

## `USEQ-36A0CE74` — Nondeterministic

**Exact statement**

> Prevent AI from choosing packages or services solely through popularity or unverifiable recommendation.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent AI from choosing packages or services solely through popularity or unverifiable recommendation.” requires accountable understanding, ownership, governance, benefit, design, requirement, review, or professional judgment whose substantive correctness cannot be automated.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-0D1E4378` — Nondeterministic

**Exact statement**

> Require architecture review before generated changes introduce a new database, queue, framework, cloud service, model provider, or operational platform.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Architecture review is required for declared services, while a generated library silently creates a new external queue.

## `USEQ-6C07A1CF` — Nondeterministic

**Exact statement**

> Monitor AI-suggested dependencies for advisories, ownership change, deprecation, and malicious releases after adoption.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Finite tests or monitoring do not define a complete population, behavioral oracle, sampling protocol, and acceptance threshold for the original claim. The proposed deterministic clause only establishes: Read the authorized monitoring or management system and verify current observations exist for every named signal with bound provider, model, tool, dependency, owner, and immutable query timestamp.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Monitoring covers advisories and deprecation, but a malicious release is installed before the feed reports it.

## `USEQ-31D5D8DE` — Nondeterministic

**Exact statement**

> Record why each new dependency is necessary and who owns its lifecycle.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A dependency record has rationale and owner, but the rationale is false and the owner has no lifecycle authority.

## `USEQ-0A9114C1` — Nondeterministic

**Exact statement**

> Remove hallucinated, unused, duplicate, obsolete, or insecure dependencies before merge.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Remove hallucinated, unused, duplicate, obsolete, or insecure dependencies before merge.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-4C7730ED` — Nondeterministic

**Exact statement**

> Include models, prompts, adapters, embeddings, data sets, and agent tools in relevant supply-chain inventories.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Inventories list registered artifacts, while an adapter loaded from object storage is absent.

## `USEQ-E1582655` — Nondeterministic

**Exact statement**

> Define each agent capability explicitly, including files, repositories, commands, network destinations, APIs, credentials, data, and environments it may access.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Required fields are observable, but their semantic truth, completeness, fitness, or correct application to the system is not. The proposed deterministic clause only establishes: Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Capability fields are complete, but a wildcard API permission grants undeclared production access.

## `USEQ-EE41DE02` — Nondeterministic

**Exact statement**

> Deny capabilities by default and grant the minimum scope required for the current task.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Deny capabilities by default and grant the minimum scope required for the current task.” depends on proportionality, meaningful review, necessity, architecture fit, least privilege, design quality, or project-specific risk with no universal pass threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-DBCE39F6` — Nondeterministic

**Exact statement**

> Use separate identities and sandboxes for agents rather than sharing a user or production administrator session.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The proposal checks a bounded record or gate fragment, but the full rule also requires unbounded coverage or contextual and accountable meaning. The proposed deterministic clause only establishes: Validate the authorized AI capability, review, approval, inventory, lifecycle, or action record contains every explicitly required state and evidence binding.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`, `contextual_judgment`

### Skeptical rejection counterexample

Agents have distinct accounts and containers, but share a mounted administrator credential.

## `USEQ-C5639E97` — Nondeterministic

**Exact statement**

> Keep untrusted prompts, issue content, code comments, retrieved documents, and web pages from expanding agent permissions.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Keep untrusted prompts, issue content, code comments, retrieved documents, and web pages from expanding agent permissions.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-A1439915` — Nondeterministic

**Exact statement**

> Require human confirmation at clearly identified boundaries for destructive, external, privileged, financial, production, publication, and communication actions.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on meaningful review, authority, purpose, consequence, or approval that a record or gate cannot objectively decide. The proposed deterministic clause only establishes: Inspect the versioned CI or agent policy and run fixtures proving the governed output or action cannot advance without the explicitly required review, evidence, independence, or confirmation state.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Confirmation gates cover named actions, while a public communication through a plugin is not classified as a boundary.

## `USEQ-87469F66` — Nondeterministic

**Exact statement**

> Show the exact proposed action, target, scope, side effects, credentials, and rollback implications before confirmation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Show the exact proposed action, target, scope, side effects, credentials, and rollback implications before confirmation.” has checkable preview fields, but exact side effects and rollback implications may require unbounded program and environment analysis.
- **Nondeterministic reason codes:** `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Validate the authorized AI, tool, review, incident, architecture, command, or dependency record contains every named field, role, state, category, source, version, permission, metric, or evidence link.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D22830A2` — Nondeterministic

**Exact statement**

> Prevent an agent from modifying its own policy, approval rules, audit records, safety controls, or credentials.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** The control “Prevent an agent from modifying its own policy, approval rules, audit records, safety controls, or credentials.” has a bounded test or analysis component, but full pass status depends on an undefined model population, attack set, oracle, threshold, or proof of coverage.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Execute the registered finite protocol and record the named observations or result, without treating an undefined model population, attack set, oracle, threshold, or scope as proven.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-15C830F0` — Nondeterministic

**Exact statement**

> Bound runtime, token use, cost, network requests, file changes, command count, concurrency, recursion, retries, and spawned agents.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Ten configured limits do not prove every agent, tool, child process and retry path is bounded or that the bounds are safe.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The primary executor can enforce all fields while a spawned helper or external tool runs beyond the token or command limit.

## `USEQ-71EE868E` — Nondeterministic

**Exact statement**

> Restrict outbound network destinations and validate downloaded content and tools.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Outbound restriction and safe download validation require complete network-path coverage and security judgment over arbitrary content and tools.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Allowlisted destinations can pass while a trusted host serves a malicious installer or a tool opens an unmonitored channel.

## `USEQ-A348D57E` — Nondeterministic

**Exact statement**

> Prevent agents from exfiltrating source, secrets, personal data, or private context through prompts, URLs, logs, errors, or generated artifacts.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent agents from exfiltrating source, secrets, personal data, or private context through prompts, URLs, logs, errors, or generated artifacts.” is nondeterministic because it makes an absolute no-exfiltration or no-fabrication claim that bounded evidence cannot prove for every input, output, channel, and future execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-E07F70B6` — Nondeterministic

**Exact statement**

> Use allowlisted commands or isolated execution for high-risk environments.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use allowlisted commands or isolated execution for high-risk environments.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use allowlisted commands or isolated execution for high-risk environments.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-EA332D5C` — Nondeterministic

**Exact statement**

> Record tool calls, inputs, outputs, file changes, decisions, approvals, and errors in an auditable form that protects sensitive data.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Record tool calls, inputs, outputs, file changes, decisions, approvals, and errors in an auditable form that protects sensitive data.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Record tool calls, inputs, outputs, file changes, decisions, approvals, and errors in an auditable form that protects sensitive data.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-172D4874` — Nondeterministic

**Exact statement**

> Provide immediate pause, cancellation, credential revocation, and kill-switch capabilities.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Immediate has no response-time threshold, and capability presence does not prove pause, cancellation, revocation and kill switch work in all execution states.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

Buttons and APIs can exist while a blocked tool call ignores cancellation or credential caches remain active.

## `USEQ-26B3A07B` — Nondeterministic

**Exact statement**

> Recover safely from partial execution and identify every side effect already performed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Recover safely from partial execution and identify every side effect already performed.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Recover safely from partial execution and identify every side effect already performed.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-D31234E4` — Nondeterministic

**Exact statement**

> Test prompt injection, confused-deputy behavior, privilege escalation, hidden instructions, malicious tool output, and infinite-loop scenarios.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Six adversarial test categories do not provide exhaustive prompts, tools, privilege paths, loop states or correct safety oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Named fixtures can pass while an indirect instruction in a downloaded file causes a privileged action.

## `USEQ-B55613A3` — Nondeterministic

**Exact statement**

> Do not permit unsupervised production changes until the agent has demonstrated reliable behavior under realistic adversarial evaluation and the residual risk is authorized.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Do not permit unsupervised production changes until the agent has demonstrated reliable behavior under realistic adversarial evaluation and the residual risk is authorized.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Do not permit unsupervised production changes until the agent has demonstrated reliable behavior under realistic adversarial evaluation and the residual risk is authorized.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-EDAB9D12` — Nondeterministic

**Exact statement**

> Inventory base models, fine-tunes, adapters, prompts, system instructions, safety policies, retrieval indexes, embeddings, data sets, evaluators, and external tools used in each AI-assisted workflow.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** An AI-component inventory cannot prove inclusion of every model, prompt, policy, index, data set, evaluator and dynamically selected external tool.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All eleven sections can be populated while provider routing silently introduces an unrecorded model or tool.

## `USEQ-9832AA48` — Nondeterministic

**Exact statement**

> Record source, provider, version, hash or immutable identifier, license, permitted use, training or fine-tuning data claims, and support status where available.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Record source, provider, version, hash or immutable identifier, license, permitted use, training or fine-tuning data claims, and support status where available.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Record source, provider, version, hash or immutable identifier, license, permitted use, training or fine-tuning data claims, and support status where available.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-68821BC1` — Deterministic

**Exact statement**

> Bind deployed AI configuration to a release record so behavior changes can be traced.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The deployed digest must come from environment authority; the expected binding must come independently from the release record. Equality across both proves traceability.

### Exact deterministic check

1. For every deployed AI service in the complete sealed service inventory, effective environment evidence reports the content digest of the deployed model, prompt, retrieval, routing, tool, and policy configuration.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

2. For every deployed AI service in that inventory, the exact effective configuration digest equals the configuration digest bound to the authenticated release record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A release record can contain a configuration digest while a different prompt, model, or tool configuration is deployed. The environment-to-record digest comparison fails.

## `USEQ-21EAD928` — Deterministic

**Exact statement**

> Verify downloaded model and adapter integrity and retrieve them through authenticated trusted channels.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Model and adapter artifacts can be verified against approved digests or signatures and retrieved only through authenticated policy-allowed channels.

### Exact deterministic check

1. Verify downloaded model and adapter integrity and retrieve them through authenticated trusted channels.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A hash mismatch, missing signature or unapproved unauthenticated source fails the bounded artifact and channel checks.

## `USEQ-9509D8FC` — Nondeterministic

**Exact statement**

> Assess model, prompt, adapter, and data poisoning risks before adoption.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Assess model, prompt, adapter, and data poisoning risks before adoption.” is nondeterministic because competent reviewers can differ about poisoning risk, understandability, minimization, necessity, tracking, harm, or fitness on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C41FAB76` — Nondeterministic

**Exact statement**

> Review provider and community models for unsafe serialization, executable code, custom loaders, hidden tools, and excessive permissions.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Review records for six hazards do not prove repositories and models are safe or that hidden executable behavior and permissions were fully found.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

All checklist fields can pass while a custom deserializer executes code through an unrecognized format.

## `USEQ-9F95E96A` — Nondeterministic

**Exact statement**

> Do not execute untrusted model repositories, notebooks, setup scripts, or custom code with production credentials.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The negative claim spans every repository, notebook, script, custom-code path and credential inheritance mechanism.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Approved runners can isolate known inputs while an extension executes untrusted setup code with cached production credentials.

## `USEQ-90F32C34` — Nondeterministic

**Exact statement**

> Scan model and data packages for secrets, personal data, malicious files, license obligations, and unexpected components.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Scan model and data packages for secrets, personal data, malicious files, license obligations, and unexpected components.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Scan model and data packages for secrets, personal data, malicious files, license obligations, and unexpected components.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-23C5B858` — Nondeterministic

**Exact statement**

> Separate evaluation, development, and production credentials and data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Credential and data separation must hold across every store, role, log, cache, tool and execution path; role labels alone are insufficient.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Three environments can have separate accounts while a shared retrieval index or inherited token exposes production data to evaluation.

## `USEQ-C9B74797` — Nondeterministic

**Exact statement**

> Control who may change prompts, retrieval sources, model routing, tool definitions, safety settings, and thresholds.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Access control over six AI configuration surfaces requires complete mutation-path and identity inventory, including provider and emergency paths.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Declared ACLs can pass while an indirect deployment variable or provider console changes routing without the authorized role.

## `USEQ-886D57EB` — Nondeterministic

**Exact statement**

> Review and test changes even when the provider labels them as minor or safety-related.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete provider-change detection and adequate review and testing cannot be proven from change records or provider labels.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known minor changes can be reviewed while a silent model update alters behavior without any event to trigger testing.

## `USEQ-6B2D33C4` — Nondeterministic

**Exact statement**

> Maintain a rollback path to a known evaluated model and configuration.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A rollback record and successful drill do not prove a known evaluated model remains available or works for every production state and dependency.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

The pinned version can roll back in staging while the provider withdraws it or a new data schema makes it unusable.

## `USEQ-57129030` — Nondeterministic

**Exact statement**

> Monitor provider outages, model withdrawals, silent updates, policy changes, and compromised distribution channels.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring five provider risks depends on incomplete external status, policy, distribution and model-update channels.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured feeds can be current while a silent weight update or compromised mirror is never reported.

## `USEQ-F05A29D5` — Nondeterministic

**Exact statement**

> Include AI components in SBOM-like, model-card, system-card, or provenance records appropriate to the system.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Include AI components in SBOM-like, model-card, system-card, or provenance records appropriate to the system.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Include AI components in SBOM-like, model-card, system-card, or provenance records appropriate to the system.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-B016EE0A` — Deterministic

**Exact statement**

> Define retirement procedures for models, embeddings, prompts, credentials, retained data, and downstream derivatives.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A retirement policy can define procedures for the six exact models, embeddings, prompts, credentials, retained-data and derivative categories.

### Exact deterministic check

1. Define retirement procedures for models, embeddings, prompts, credentials, retained data, and downstream derivatives.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting downstream derivatives or credential retirement fails the finite policy schema.

## `USEQ-F1783FEA` — Nondeterministic

**Exact statement**

> Define evaluations for the actual engineering tasks, repositories, languages, domains, users, tools, and risk conditions rather than relying only on general benchmarks.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Actual tasks, repositories, languages, domains, users, tools and risk conditions require complete scope discovery and representative evaluation judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

A custom benchmark can include one example per category while omitting the hardest production repository and user risk.

## `USEQ-EB79B3ED` — Nondeterministic

**Exact statement**

> Measure functional correctness, security, privacy, licensing, maintainability, performance, accessibility, documentation quality, and operational safety as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Measure functional correctness, security, privacy, licensing, maintainability, performance, accessibility, documentation quality, and operational safety as applicable.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Measure functional correctness, security, privacy, licensing, maintainability, performance, accessibility, documentation quality, and operational safety as applicable.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0C102A58` — Nondeterministic

**Exact statement**

> Include incomplete context, conflicting requirements, obsolete documentation, malicious instructions, unusual code, large changes, and unfamiliar domains.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Seven scenario labels do not establish realistic coverage, interaction combinations or authoritative expected outcomes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Each scenario can have a fixture while conflicting requirements combined with hidden instructions still cause unsafe behavior.

## `USEQ-8235AA68` — Nondeterministic

**Exact statement**

> Evaluate refusal and escalation behavior when the tool lacks evidence, permission, capability, or confidence.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Four refusal and escalation conditions do not define complete requests, permissions, capabilities, confidence states or correct escalation outcomes.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known low-confidence prompts can escalate while an unfamiliar tool action proceeds without evidence on an untested path.

## `USEQ-0F3DD770` — Nondeterministic

**Exact statement**

> Evaluate whether the tool fabricates APIs, packages, citations, standards, test results, vulnerabilities, or completed actions.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Seven fabrication categories and finite prompts cannot prove the tool avoids fabricated outputs over arbitrary engineering contexts.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All fixtures can pass while the model invents a package version or completed action for an unseen repository.

## `USEQ-D21E0A71` — Nondeterministic

**Exact statement**

> Test direct and indirect prompt injection, data exfiltration, tool abuse, confused deputy, secret discovery, sandbox escape, and policy bypass.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Seven security attack labels do not define exhaustive prompt, content, tool, identity and sandbox paths or a complete adversarial oracle.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Direct injection tests can pass while retrieved poisoned context exfiltrates a secret through an allowed tool.

## `USEQ-9994DB54` — Nondeterministic

**Exact statement**

> Test output under model updates, prompt changes, retrieval changes, tool changes, provider routing changes, and degraded dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Six change and degradation categories do not provide complete versions, combinations, tasks, dependencies or correctness and safety oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Single-change regressions can pass while a prompt and routing change together cause an untested unsafe action.

## `USEQ-F4C1D548` — Nondeterministic

**Exact statement**

> Use independently curated evaluation sets and protect them from contamination where scores are used for decisions.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** True curation independence and absence of evaluation-set contamination require provenance and open-world training-data knowledge unavailable from records alone.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

A separate team can curate cases already present in provider training data, producing inflated decision scores.

## `USEQ-B854BC2B` — Nondeterministic

**Exact statement**

> Retain representative historical failures and incidents as regression cases.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Retain representative historical failures and incidents as regression cases.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Retain representative historical failures and incidents as regression cases.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7633CD04` — Nondeterministic

**Exact statement**

> Use human expert review for semantic, architectural, security, legal, and safety qualities that automated scoring cannot establish.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use human expert review for semantic, architectural, security, legal, and safety qualities that automated scoring cannot establish.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use human expert review for semantic, architectural, security, legal, and safety qualities that automated scoring cannot establish.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-08934850` — Nondeterministic

**Exact statement**

> Measure uncertainty and confidence calibration where the system exposes confidence or autonomous thresholds.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Calibration validity requires representative outcome labels, statistical assumptions and task-specific thresholds, not just a confidence metric.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A reliability curve can look calibrated on biased data while autonomous high-risk actions remain badly miscalibrated.

## `USEQ-53A8E42B` — Deterministic

**Exact statement**

> Define acceptable performance by task and harm, not one aggregate score.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An evaluation policy can define separate numeric acceptance thresholds for every registered task-and-harm pair and prohibit aggregate-only approval.

### Exact deterministic check

1. Define acceptable performance by task and harm, not one aggregate score.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A task or harm with no threshold, or a gate using only one aggregate score, fails the exact policy matrix.

## `USEQ-48A484CF` — Nondeterministic

**Exact statement**

> Investigate subgroup, language, accessibility, repository, and domain differences rather than averaging them away.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Relevant subgroups, languages, accessibility needs, repositories and domains plus meaningful difference interpretation require contextual judgment.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

All five reports can exist while a small but seriously harmed subgroup is averaged within a broad category.

## `USEQ-B0B7E4B2` — Deterministic

**Exact statement**

> Require evaluation before expanding permissions, autonomy, data access, user population, or production scope.

- **Classification:** **Deterministic**
- **Route:** `bounded_execution`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A capability state machine can block expansion of permissions, autonomy, data, users or production scope unless new evaluation evidence predates the transition.

### Exact deterministic check

1. Require evaluation before expanding permissions, autonomy, data access, user population, or production scope.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

### Skeptical counterexample test

Any of the five expansions without a version-matched evaluation record fails the temporal gate.

## `USEQ-EEB12085` — Deterministic

**Exact statement**

> Publish internal limitations and approved use boundaries alongside evaluation results.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Alongside content is an artifact relation; publish is effective audience availability. The full promise requires both artifact and environment evidence.

### Exact deterministic check

1. Every evaluation-result artifact in the complete sealed result inventory contains the result plus the approved internal limitations and approved-use boundaries bound to the same evaluated model and version.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

2. Every declared internal publication surface exposes that exact bound evaluation-result artifact, limitation set, and approved-use boundary set to the sealed internal audience.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A package registry can claim the fields exist while the result artifact omits limitations or the internal audience cannot access it. The composite fails.

## `USEQ-3C2B0FE1` — Nondeterministic

**Exact statement**

> Reevaluate periodically and after incidents, model changes, new attacks, and meaningful shifts in production tasks.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Reevaluate periodically and after incidents, model changes, new attacks, and meaningful shifts in production tasks.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Reevaluate periodically and after incidents, model changes, new attacks, and meaningful shifts in production tasks.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-48B9998C` — Nondeterministic

**Exact statement**

> Verify technical facts, commands, configuration, API behavior, version requirements, standards status, dates, and external links against authoritative sources.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Verify technical facts, commands, configuration, API behavior, version requirements, standards status, dates, and external links against authoritative sources.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Verify technical facts, commands, configuration, API behavior, version requirements, standards status, dates, and external links against authoritative sources.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C1C0BF4C` — Nondeterministic

**Exact statement**

> Do not include fabricated citations, quotations, people, products, vulnerabilities, decisions, metrics, or incident details.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Do not include fabricated citations, quotations, people, products, vulnerabilities, decisions, metrics, or incident details.” is nondeterministic because it makes an absolute no-exfiltration or no-fabrication claim that bounded evidence cannot prove for every input, output, channel, and future execution.
- **Nondeterministic reason codes:** `unbounded_scope`

## `USEQ-C5181864` — Nondeterministic

**Exact statement**

> Distinguish facts, inference, recommendation, example, placeholder, and uncertainty clearly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Distinguish facts, inference, recommendation, example, placeholder, and uncertainty clearly.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Distinguish facts, inference, recommendation, example, placeholder, and uncertainty clearly.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-A3559530` — Nondeterministic

**Exact statement**

> Ensure generated documentation matches the exact released artifact and supported configuration.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Ensure generated documentation matches the exact released artifact and supported configuration.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Ensure generated documentation matches the exact released artifact and supported configuration.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-7B8E61BE` — Nondeterministic

**Exact statement**

> Review setup, migration, rollback, recovery, security, accessibility, and troubleshooting instructions through actual execution.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Executing seven documentation procedures does not prove instructions are correct, accessible, safe and complete across actual environments.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A setup and rollback walkthrough can pass while recovery instructions fail on a supported platform or accessibility flow.

## `USEQ-2D5D5C89` — Nondeterministic

**Exact statement**

> Protect internal, personal, confidential, and security-sensitive information during generation and publication.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect internal, personal, confidential, and security-sensitive information during generation and publication.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect internal, personal, confidential, and security-sensitive information during generation and publication.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-75862A50` — Nondeterministic

**Exact statement**

> Ensure generated user content is understandable, accessible, localized appropriately, and free of deceptive claims.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Ensure generated user content is understandable, accessible, localized appropriately, and free of deceptive claims.” is nondeterministic because competent reviewers can differ about poisoning risk, understandability, minimization, necessity, tracking, harm, or fitness on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-F06F2315` — Nondeterministic

**Exact statement**

> Review generated release notes so they do not omit breaking changes, security impact, required customer action, or known limitations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Review generated release notes so they do not omit breaking changes, security impact, required customer action, or known limitations.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Review generated release notes so they do not omit breaking changes, security impact, required customer action, or known limitations.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-44ADFB43` — Nondeterministic

**Exact statement**

> Use source attribution and content provenance where readers need to assess authority and freshness.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use source attribution and content provenance where readers need to assess authority and freshness.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use source attribution and content provenance where readers need to assess authority and freshness.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-FAC8F384` — Nondeterministic

**Exact statement**

> Version generated documentation and invalidate it after material product or provider changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Version generated documentation and invalidate it after material product or provider changes.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Version generated documentation and invalidate it after material product or provider changes.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-327CFE20` — Nondeterministic

**Exact statement**

> Prevent AI summaries from replacing authoritative records when detail or legal meaning matters.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent AI summaries from replacing authoritative records when detail or legal meaning matters.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Prevent AI summaries from replacing authoritative records when detail or legal meaning matters.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E547539A` — Nondeterministic

**Exact statement**

> Ensure search and knowledge systems surface the authoritative current source rather than an unreviewed generated duplicate.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Guaranteeing authoritative current search results over all queries and generated duplicates requires complete content inventory, ranking semantics and changing authority.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known queries can surface the source while another wording ranks a stale generated duplicate first.

## `USEQ-EE1E3012` — Nondeterministic

**Exact statement**

> Provide a correction route and update propagated copies after material errors are found.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide a correction route and update propagated copies after material errors are found.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Provide a correction route and update propagated copies after material errors are found.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E1EEAF8B` — Nondeterministic

**Exact statement**

> Label synthetic or AI-generated content when required by law, contract, policy, audience expectation, or risk.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Label synthetic or AI-generated content when required by law, contract, policy, audience expectation, or risk.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Label synthetic or AI-generated content when required by law, contract, policy, audience expectation, or risk.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-80D53A5E` — Deterministic

**Exact statement**

> Retain the reviewed final artifact rather than depending on transient conversation history.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. The reviewed final artifact can be content-addressed and retained as the authoritative output independently of transient conversation history.

### Exact deterministic check

1. Retain the reviewed final artifact rather than depending on transient conversation history.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

If only chat history remains or the retained digest differs from the reviewed artifact, the exact retention check fails.

## `USEQ-798E6E1D` — Nondeterministic

**Exact statement**

> Determine which images, audio, video, documents, designs, reports, releases, and public communications require provenance or authenticity information.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Determine which images, audio, video, documents, designs, reports, releases, and public communications require provenance or authenticity information.” is nondeterministic because it is a provenance-need, cryptographic-use, or vulnerable-user risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-5D158B5E` — Nondeterministic

**Exact statement**

> Define what provenance claims mean and avoid presenting them as proof that the content is true, harmless, complete, or unbiased.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The rule depends on what provenance claims communicate and whether their presentation implies truth, harmlessness, completeness, or lack of bias.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

The provenance record and its typed status can be correct while surrounding prose overstates what provenance proves, so record equality passes a broken presentation.

## `USEQ-EDD07EC1` — Nondeterministic

**Exact statement**

> Use a recognized provenance standard such as the current C2PA specification when interoperable content credentials are required.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Recognized, current, interoperable and required are contextual or external-standard judgments that change by format, ecosystem and use.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A valid C2PA version can be selected while another required profile or newer compatible specification applies.

## `USEQ-D9985041` — Deterministic

**Exact statement**

> Cryptographically bind provenance manifests to the exact asset or defined derivative relationship.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A provenance manifest signature and content digest can bind the exact asset or an explicitly typed, hashed derivative relationship.

### Exact deterministic check

1. Cryptographically bind provenance manifests to the exact asset or defined derivative relationship.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Asset substitution or an undefined derivative changes the authenticated binding and fails cryptographic verification.

## `USEQ-22BC5813` — Nondeterministic

**Exact statement**

> Protect signing identities and keys and define who is authorized to make each assertion.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Authorization policy is bounded, but key protection across copies, signers, backups and compromise paths is an open-world security claim.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Signer roles can be defined while an exported private key in an untracked backup allows unauthorized assertions.

## `USEQ-EE822CDB` — Nondeterministic

**Exact statement**

> Record creation, editing, transformation, generation, source, ingredient, tool, organization, and timestamp information only when accurate and permitted.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Record creation, editing, transformation, generation, source, ingredient, tool, organization, and timestamp information only when accurate and permitted.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Record creation, editing, transformation, generation, source, ingredient, tool, organization, and timestamp information only when accurate and permitted.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E59769F2` — Nondeterministic

**Exact statement**

> Minimize personal, precise-location, device, and confidential metadata in provenance records.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Minimize personal, precise-location, device, and confidential metadata in provenance records.” is nondeterministic because competent reviewers can differ about poisoning risk, understandability, minimization, necessity, tracking, harm, or fitness on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-C4489851` — Nondeterministic

**Exact statement**

> Provide user interfaces that explain provenance in understandable layers rather than only displaying a trust icon.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide user interfaces that explain provenance in understandable layers rather than only displaying a trust icon.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Provide user interfaces that explain provenance in understandable layers rather than only displaying a trust icon.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-841334CB` — Nondeterministic

**Exact statement**

> Show missing, invalid, broken, revoked, or untrusted provenance without claiming the underlying content is necessarily false.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The provenance state is objective, but deciding whether the full presentation claims the underlying content is necessarily false requires semantic UI review.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A revoked state token can be rendered correctly while explanatory text says the content is false; checking the finite state mapping alone therefore passes a violation.

## `USEQ-CF367760` — Nondeterministic

**Exact statement**

> Preserve provenance through supported transformations, exports, compression, transcoding, publication, and distribution where feasible.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Preserve provenance through supported transformations, exports, compression, transcoding, publication, and distribution where feasible.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Preserve provenance through supported transformations, exports, compression, transcoding, publication, and distribution where feasible.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2A380FCA` — Nondeterministic

**Exact statement**

> Detect and disclose when a workflow strips or cannot preserve provenance.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detecting and disclosing every provenance-stripping or nonpreserving transformation requires complete workflow, application and format coverage.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known export paths can disclose stripping while a crop, translation or third-party embed silently removes credentials.

## `USEQ-5A50E71F` — Nondeterministic

**Exact statement**

> Validate trust chains, certificates, timestamps, revocation, content bindings, and manifest structure.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Six validation categories do not define exhaustive chain-building, time, revocation, structure and content-binding cases across supported suites.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known credential fixtures can pass while an alternate chain or canonicalization path accepts a substituted asset.

## `USEQ-941EA8C0` — Nondeterministic

**Exact statement**

> Protect against manifest substitution, replay, downgrade, detached-asset confusion, and misleading partial histories.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Protecting against five provenance attacks requires an exhaustive adversarial protocol and complete history and application semantics.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Standard substitution and replay tests can pass while a partial history is presented misleadingly through an untested viewer.

## `USEQ-0C275FAF` — Deterministic

**Exact statement**

> Define how edited, combined, redacted, and derived content updates provenance.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A provenance-transformation policy can define exact update behavior for edited, combined, redacted and derived content.

### Exact deterministic check

1. Define how edited, combined, redacted, and derived content updates provenance.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A transformation category with no new assertion, relationship or preservation rule fails the four-way policy schema.

## `USEQ-D078F7CC` — Nondeterministic

**Exact statement**

> Test provenance handling across supported applications, platforms, media formats, and accessibility technologies.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Supported applications, platforms, formats and accessibility technologies require complete inventories, and test occurrence is not an exhaustive interoperability oracle.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All registered combinations can pass while a screen reader or mobile editor strips or misstates provenance.

## `USEQ-29786656` — Nondeterministic

**Exact statement**

> Maintain a recovery and key-rotation process that does not invalidate legitimate historical content without explanation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Key rotation and recovery preserving all legitimate historical content is a future behavioral and complete-content-inventory claim.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A rotation drill can validate samples while an archived item loses its trust chain or explanation later.

## `USEQ-2325DA4F` — Nondeterministic

**Exact statement**

> Use verifiable credentials or signed evidence only when cryptographic verification, issuer accountability, portability, or selective disclosure provides a real requirement.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use verifiable credentials or signed evidence only when cryptographic verification, issuer accountability, portability, or selective disclosure provides a real requirement.” is nondeterministic because it is a provenance-need, cryptographic-use, or vulnerable-user risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-EE4C0408` — Deterministic

**Exact statement**

> Define issuer, holder, verifier, subject, claims, purpose, audience, validity, revocation, status, and trust policy explicitly.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A credential policy can define the eleven exact issuer, holder, verifier, subject, claim, purpose, audience, validity, revocation, status and trust fields.

### Exact deterministic check

1. Define issuer, holder, verifier, subject, claims, purpose, audience, validity, revocation, status, and trust policy explicitly.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Any missing role or trust-policy field fails the finite credential schema.

## `USEQ-F7C75D9C` — Nondeterministic

**Exact statement**

> Use current interoperable standards such as the W3C Verifiable Credentials Data Model 2.0 family when applicable.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Current, interoperable and applicable standards require ecosystem and legal or technical interpretation beyond a version string.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

### Skeptical rejection counterexample

A VC 2.0 implementation can be syntactically current while an applicable profile or interoperable proof suite is absent.

## `USEQ-5FE49B3A` — Nondeterministic

**Exact statement**

> Verify issuer identity, key authorization, proof purpose, domain or challenge binding, validity period, status, schema, and audience before accepting a claim.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Nine verifier checks are finite labels, but correctness across arbitrary credentials, status sources, schemas, domains and proof suites lacks a closed input oracle.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

All standard fixtures can pass while a crafted audience or challenge binding is accepted on an untested path.

## `USEQ-6BF7123C` — Nondeterministic

**Exact statement**

> Do not treat a valid signature as proof that the claim is true, current, authorized for the present purpose, or ethically appropriate.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A signature-policy notice cannot prove every system and human decision avoids treating validity as truth, currency, authorization or ethics.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

The verifier can display a warning while downstream automation still authorizes a present action solely from the signature.

## `USEQ-B791AEA6` — Nondeterministic

**Exact statement**

> Request and disclose only the claims necessary for the transaction.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Request and disclose only the claims necessary for the transaction.” is nondeterministic because competent reviewers can differ about poisoning risk, understandability, minimization, necessity, tracking, harm, or fitness on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-93F664BC` — Nondeterministic

**Exact statement**

> Support selective disclosure and unlinkability where the use case and chosen standard permit it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Support selective disclosure and unlinkability where the use case and chosen standard permit it.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Support selective disclosure and unlinkability where the use case and chosen standard permit it.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-A80DCC8B` — Nondeterministic

**Exact statement**

> Prevent identifiers and status checks from enabling unnecessary correlation or tracking.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent identifiers and status checks from enabling unnecessary correlation or tracking.” is nondeterministic because competent reviewers can differ about poisoning risk, understandability, minimization, necessity, tracking, harm, or fitness on identical evidence.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-403D0FC0` — Nondeterministic

**Exact statement**

> Protect credential issuance, storage, presentation, recovery, revocation, and deletion.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect credential issuance, storage, presentation, recovery, revocation, and deletion.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Protect credential issuance, storage, presentation, recovery, revocation, and deletion.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-AABF5B36` — Nondeterministic

**Exact statement**

> Handle lost, compromised, expired, suspended, and superseded credentials.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Handle does not specify exact responses, authorities or success criteria for five credential states, so a state label alone is insufficient.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Every state can have a generic workflow while compromised credentials remain accepted or superseded ones are not migrated.

## `USEQ-CBD654F9` — Nondeterministic

**Exact statement**

> Validate canonicalization, serialization, proof suites, cryptographic parameters, and implementation interoperability.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Canonicalization, proof-suite and interoperability validation spans complex protocols, implementations and adversarial inputs without an exhaustive oracle.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known test vectors can pass while two supported serializers produce different signed canonical bytes.

## `USEQ-F2B1299D` — Nondeterministic

**Exact statement**

> Protect against replay, presentation substitution, confused-deputy, verifier impersonation, and malicious issuer metadata.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Five credential attacks do not define the full presentation, verifier, metadata and issuer attack space or all execution paths.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Replay tests can pass while a malicious issuer-metadata redirect impersonates a verifier on an untested client.

## `USEQ-AE60A435` — Nondeterministic

**Exact statement**

> Provide understandable user consent and display the verifier, requested claims, purpose, and consequences before presentation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide understandable user consent and display the verifier, requested claims, purpose, and consequences before presentation.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Provide understandable user consent and display the verifier, requested claims, purpose, and consequences before presentation.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-BF615392` — Nondeterministic

**Exact statement**

> Maintain audit evidence without retaining unnecessary credential contents.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Maintain audit evidence without retaining unnecessary credential contents.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Maintain audit evidence without retaining unnecessary credential contents.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-00A91D07` — Nondeterministic

**Exact statement**

> Test offline, degraded, revoked, clock-skewed, and trust-store-update scenarios.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Five scenario executions do not establish complete credential types, platforms, status states, clocks and recovery behavior.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

An offline revoked test can pass while trust-store update during clock skew accepts an expired credential.

## `USEQ-6EBF4486` — Nondeterministic

**Exact statement**

> Plan crypto-agile and post-quantum migration for long-lived credentials and evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Long-lived applicability, migration adequacy and future crypto-agility require complete credential inventory and specialist cryptographic planning.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

A plan can exist while an archived evidence format cannot carry replacement proofs or an offline verifier cannot migrate.

## `USEQ-FB59A946` — Deterministic

**Exact statement**

> Define when generated or materially altered content must be labeled, watermarked, credentialed, disclosed, or restricted.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A generated-content policy can define exact conditions triggering labeling, watermarking, credentials, disclosure or restriction.

### Exact deterministic check

1. Define when generated or materially altered content must be labeled, watermarked, credentialed, disclosed, or restricted.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A content class without a disposition rule or an unapproved trigger gap fails the finite decision table.

## `USEQ-24A27761` — Nondeterministic

**Exact statement**

> Make disclosures visible, accessible, durable, and understandable in the context where content is consumed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Make disclosures visible, accessible, durable, and understandable in the context where content is consumed.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Make disclosures visible, accessible, durable, and understandable in the context where content is consumed.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-6D2ED17A` — Nondeterministic

**Exact statement**

> Avoid disclosures that disappear when content is downloaded, embedded, cropped, translated, printed, or shared.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Disclosure persistence across seven transformations is a runtime property over applications, formats and user behavior.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Known downloads and crops can preserve labels while translation, printing or embedding removes them on another platform.

## `USEQ-87A2D323` — Nondeterministic

**Exact statement**

> Prevent generated content from impersonating real people, organizations, authorities, or official communications without authorization and appropriate safeguards.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Prevent generated content from impersonating real people, organizations, authorities, or official communications without authorization and appropriate safeguards.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Prevent generated content from impersonating real people, organizations, authorities, or official communications without authorization and appropriate safeguards.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-DFB7F6ED` — Nondeterministic

**Exact statement**

> Obtain necessary consent and rights for likeness, voice, biometric, personal, copyrighted, and confidential material.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Obtain necessary consent and rights for likeness, voice, biometric, personal, copyrighted, and confidential material.” is nondeterministic because it requires accountable legal, consent, copyright, biometric, confidentiality, or rights interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`

## `USEQ-9CD66A9B` — Nondeterministic

**Exact statement**

> Protect children, vulnerable users, and high-risk contexts with stricter generation and distribution controls.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Protect children, vulnerable users, and high-risk contexts with stricter generation and distribution controls.” is nondeterministic because it is a provenance-need, cryptographic-use, or vulnerable-user risk decision reserved for accountable humans.
- **Nondeterministic reason codes:** `human_accountability`

## `USEQ-31086253` — Nondeterministic

**Exact statement**

> Review generated public claims for factual accuracy, defamation, discrimination, safety, financial, medical, legal, and civic risks.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Review generated public claims for factual accuracy, defamation, discrimination, safety, financial, medical, legal, and civic risks.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Review generated public claims for factual accuracy, defamation, discrimination, safety, financial, medical, legal, and civic risks.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-9D635145` — Nondeterministic

**Exact statement**

> Use human review before publishing high-impact generated content under an organizational identity.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Use human review before publishing high-impact generated content under an organizational identity.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Use human review before publishing high-impact generated content under an organizational identity.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-8C754D48` — Nondeterministic

**Exact statement**

> Do not present detection scores as definitive proof that content is or is not AI-generated.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Whether a detection score is presented as definitive proof is a meaning and user-experience judgment rather than a closed numeric or record predicate.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

The stored score and label can satisfy the schema while surrounding language presents the score as conclusive proof, so structured facts pass a broken presentation.

## `USEQ-5710654C` — Deterministic

**Exact statement**

> Combine provenance, source verification, context, policy, and investigation rather than relying on one synthetic-content detector.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A synthetic-content decision record can require five distinct provenance, source, context, policy and investigation inputs and reject detector-only decisions.

### Exact deterministic check

1. Combine provenance, source verification, context, policy, and investigation rather than relying on one synthetic-content detector.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A decision relying solely on a detector or missing investigation evidence fails the finite evidence matrix.

## `USEQ-C731071C` — Deterministic

**Exact statement**

> Provide correction, takedown, appeal, and incident-reporting routes.

- **Classification:** **Deterministic**
- **Route:** `external_readonly_query`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Provide routes is an effective endpoint-availability promise and cannot be proved by route identifiers in a catalog alone.

### Exact deterministic check

1. The correction, takedown, appeal, and incident-reporting routes in the complete sealed route inventory each resolve to their declared live endpoint and return the independently sealed expected non-mutating availability response.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

### Skeptical counterexample test

A catalog can list all four URLs while one is dead or points to the wrong service. The bounded environment query fails.

## `USEQ-02B7A344` — Deterministic

**Exact statement**

> Record the approved final content, sources, model and tool versions, material transformations, reviewer, and publication channels.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Each published final-content record can be checked for seven exact artifact, source, model, tool, transformation, reviewer and channel fields.

### Exact deterministic check

1. Record the approved final content, sources, model and tool versions, material transformations, reviewer, and publication channels.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A publication missing source lineage or reviewer identity fails the finite provenance schema.

## `USEQ-10EDBDFD` — Nondeterministic

**Exact statement**

> Monitor for misuse of organizational names, marks, credentials, voices, and identities in synthetic media.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring all misuse of five organizational identity classes across open synthetic media is an unbounded discovery claim.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured brand searches can pass while a new voice clone or credential forgery on an unmonitored channel remains unseen.

## `USEQ-D3FD11E3` — Deterministic

**Exact statement**

> Prepare response procedures for forged, misleading, compromised, or incorrectly credentialed content.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. A response catalog can define procedures for four finite forged, misleading, compromised and incorrectly credentialed content classes.

### Exact deterministic check

1. Prepare response procedures for forged, misleading, compromised, or incorrectly credentialed content.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Omitting compromised-credential handling fails the exact procedure matrix.

## `USEQ-97B7BEBB` — Nondeterministic

**Exact statement**

> Preserve evidence needed to investigate provenance while respecting privacy and legal constraints.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Preserve evidence needed to investigate provenance while respecting privacy and legal constraints.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Preserve evidence needed to investigate provenance while respecting privacy and legal constraints.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-0CD1F465` — Nondeterministic

**Exact statement**

> Monitor AI-assisted workflows for unauthorized data access, secret exposure, insecure output, fabricated facts, malicious dependencies, excessive permissions, abnormal tool use, cost spikes, and repeated human overrides.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Monitor AI-assisted workflows for unauthorized data access, secret exposure, insecure output, fabricated facts, malicious dependencies, excessive permissions, abnormal tool use, cost spikes, and repeated human overrides.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Monitor AI-assisted workflows for unauthorized data access, secret exposure, insecure output, fabricated facts, malicious dependencies, excessive permissions, abnormal tool use, cost spikes, and repeated human overrides.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E44BDA17` — Nondeterministic

**Exact statement**

> Monitor model, provider, prompt, retrieval, permission, policy, and integration changes that can alter behavior.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Monitoring seven behavior-altering change classes requires complete provider, prompt, retrieval, permission, policy and integration change sources.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Configured events can pass while a silent provider update or out-of-band prompt mutation remains undetected.

## `USEQ-91F85F40` — Nondeterministic

**Exact statement**

> Detect whether agents perform actions outside the approved task, scope, identity, repository, environment, or time window.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detecting every agent action outside six approved dimensions requires complete action telemetry plus semantic interpretation of task and scope.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Logged commands can comply while a tool side effect changes another repository or acts outside the user's intent.

## `USEQ-D20D2DD1` — Nondeterministic

**Exact statement**

> Provide rapid disablement for each model, provider, integration, tool, retrieval source, agent, and autonomous capability.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Provide rapid disablement for each model, provider, integration, tool, retrieval source, agent, and autonomous capability.” has checkable evidence clauses, but its full result also requires an explicit numeric disablement-time threshold.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_threshold`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Provide rapid disablement for each model, provider, integration, tool, retrieval source, agent, and autonomous capability.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E0D7CFC5` — Nondeterministic

**Exact statement**

> Revoke associated credentials, webhooks, sessions, scheduled tasks, and network access when disabled.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Revoking every associated credential, webhook, session, scheduled task and network path requires complete identity and dependency inventory.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Known tokens can revoke while a cached session or hidden scheduled job remains active.

## `USEQ-39F46163` — Nondeterministic

**Exact statement**

> Maintain fallback workflows that do not depend on the unavailable or compromised AI system for essential operation.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fallback independence and essential-operation continuity require complete dependency analysis and empirical operation under unavailable or compromised AI.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

A manual workflow can exist while relying on the same AI-generated index or identity path and failing during compromise.

## `USEQ-A737503A` — Deterministic

**Exact statement**

> Define incident playbooks for confidential-data leakage, malicious action, unsafe code, poisoned context, provider compromise, model change, provenance-key compromise, and synthetic impersonation.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. An AI incident catalog can require eight exact leakage, action, code, context, provider, model, key and impersonation playbooks.

### Exact deterministic check

1. Define incident playbooks for confidential-data leakage, malicious action, unsafe code, poisoned context, provider compromise, model change, provenance-key compromise, and synthetic impersonation.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A catalog missing poisoned-context or provenance-key compromise handling fails the finite procedure join.

## `USEQ-4EE501B5` — Nondeterministic

**Exact statement**

> Identify every repository, artifact, deployment, document, decision, and communication affected by a compromised or defective AI workflow.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identifying every affected repository, artifact, deployment, document, decision and communication requires complete provenance and impact graphs.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

All registered items can be traced while an out-of-band document or decision derived from the defective workflow remains unknown.

## `USEQ-B7A152C3` — Nondeterministic

**Exact statement**

> Preserve prompts, context, outputs, tool calls, approvals, model versions, and audit evidence proportionately and lawfully for investigation.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Preserve prompts, context, outputs, tool calls, approvals, model versions, and audit evidence proportionately and lawfully for investigation.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Preserve prompts, context, outputs, tool calls, approvals, model versions, and audit evidence proportionately and lawfully for investigation.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-FA7DAC9D` — Nondeterministic

**Exact statement**

> Notify affected customers, users, employees, providers, and authorities according to impact and obligations.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Notify affected customers, users, employees, providers, and authorities according to impact and obligations.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Notify affected customers, users, employees, providers, and authorities according to impact and obligations.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-9CA9E828` — Nondeterministic

**Exact statement**

> Search for related generated defects and artifacts after an incident rather than fixing only the first instance.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Search for related generated defects and artifacts after an incident rather than fixing only the first instance.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Search for related generated defects and artifacts after an incident rather than fixing only the first instance.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-0A7A2F6C` — Nondeterministic

**Exact statement**

> Revalidate accepted output after a material model or tool defect is discovered.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Revalidate accepted output after a material model or tool defect is discovered.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Revalidate accepted output after a material model or tool defect is discovered.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-2C7F55DC` — Nondeterministic

**Exact statement**

> Perform root-cause analysis covering governance, permissions, interface design, review, evaluation, training, incentives, and technical controls.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Perform root-cause analysis covering governance, permissions, interface design, review, evaluation, training, incentives, and technical controls.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Perform root-cause analysis covering governance, permissions, interface design, review, evaluation, training, incentives, and technical controls.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3EEAD754` — Nondeterministic

**Exact statement**

> Retire obsolete models, prompts, embeddings, agents, indexes, credentials, provider data, and generated artifacts according to lifecycle and retention requirements.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Complete retirement of eight AI artifact classes depends on open inventories plus legal and lifecycle retention interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known objects can retire while a downstream derivative, provider-retained copy or generated artifact persists.

## `USEQ-86EF8E1F` — Nondeterministic

**Exact statement**

> Test disablement, rollback, provider replacement, data deletion, and evidence recovery periodically.

- **Classification:** **Nondeterministic**
- **Route:** `contract_incomplete`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Periodic has no cadence, and five test categories do not establish complete systems, providers, data, failure states or recovery oracles.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `undefined_threshold`

### Skeptical rejection counterexample

A shallow annual drill can pass while provider replacement cannot restore a critical index or delete retained data.

## `USEQ-4858F7E6` — Nondeterministic

**Exact statement**

> Block release when material AI-generated code, configuration, migration, policy, or architecture has no accountable human reviewer who understands it.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Block release when material AI-generated code, configuration, migration, policy, or architecture has no accountable human reviewer who understands it.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Block release when material AI-generated code, configuration, migration, policy, or architecture has no accountable human reviewer who understands it.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-3D7EFEF0` — Nondeterministic

**Exact statement**

> Block release when generated claims, citations, APIs, packages, standards, or compatibility assumptions have not been verified.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Detecting and verifying every generated claim, citation, API, package, standard and compatibility assumption requires complete generated-content provenance and semantic truth review.
- **Nondeterministic reason codes:** `unbounded_scope`

### Skeptical rejection counterexample

Tracked claims can be verified while an unmarked AI-generated API assumption remains in code and escapes the gate.

## `USEQ-6465C673` — Nondeterministic

**Exact statement**

> Block release when AI assistance exposed secrets, personal data, customer content, vulnerabilities, or proprietary material and containment is incomplete.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Block release when AI assistance exposed secrets, personal data, customer content, vulnerabilities, or proprietary material and containment is incomplete.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Block release when AI assistance exposed secrets, personal data, customer content, vulnerabilities, or proprietary material and containment is incomplete.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-E5E6E4D2` — Nondeterministic

**Exact statement**

> Block release when an agent executed high-impact actions outside the approved capability, identity, environment, or confirmation boundary.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The release gate cannot prove every high-impact action was logged or that task, capability and confirmation boundaries were semantically correct.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Recorded actions can all comply while an indirect tool side effect outside the repository is absent from telemetry.

## `USEQ-FD59DB11` — Nondeterministic

**Exact statement**

> Block release when critical generated tests use the same unverified logic as the implementation or have not demonstrated fault detection.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Block release when critical generated tests use the same unverified logic as the implementation or have not demonstrated fault detection.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Block release when critical generated tests use the same unverified logic as the implementation or have not demonstrated fault detection.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-C2B2D033` — Nondeterministic

**Exact statement**

> Block release when model, prompt, retrieval, tool, or provider changes invalidate prior assurance evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Determining which model, prompt, retrieval, tool and provider changes invalidate evidence requires complete change discovery and semantic impact judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

Known changes can mark evidence stale while a subtle retrieval-corpus update invalidates behavior assurance without being recognized.

## `USEQ-BA8ED02F` — Nondeterministic

**Exact statement**

> Block release when generated dependencies, licenses, provenance, or supplier identities remain unresolved.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Completeness of generated dependency and supplier inventories plus license and provenance resolution requires legal and supply-chain judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`, `unbounded_scope`

### Skeptical rejection counterexample

All registered items can be resolved while a transitive generated package or incompatible license remains undiscovered.

## `USEQ-14FDF5E4` — Nondeterministic

**Exact statement**

> Block release when disabling or rolling back the AI capability is not possible for a high-risk workflow.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Block release when disabling or rolling back the AI capability is not possible for a high-risk workflow.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Block release when disabling or rolling back the AI capability is not possible for a high-risk workflow.
   - **Checker family:** `execution_evidence`
   - **Evidence authority:** `executed`

## `USEQ-41AEF92F` — Nondeterministic

**Exact statement**

> Retain the use case, approved tool and model versions, prompts or prompt templates where appropriate, retrieved sources, permissions, tool calls, generated artifacts, human changes, reviews, tests, and approvals.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Retain the use case, approved tool and model versions, prompts or prompt templates where appropriate, retrieved sources, permissions, tool calls, generated artifacts, human changes, reviews, tests, and approvals.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Retain the use case, approved tool and model versions, prompts or prompt templates where appropriate, retrieved sources, permissions, tool calls, generated artifacts, human changes, reviews, tests, and approvals.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-3CFE6905` — Deterministic

**Exact statement**

> Retain evaluation scope, data, adversarial cases, results, limitations, model changes, incidents, and accepted residual risk.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Retain is an artifact persistence and binding promise. Direct retrieval and digest verification are required for every named artifact class.

### Exact deterministic check

1. The complete sealed AI-evaluation retention inventory contains retrievable content-addressed artifacts for evaluation scope, data, adversarial cases, results, limitations, model changes, incidents, and accepted residual risk, all bound to the assessed model and version.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

A manifest can list all eight artifact classes after evaluation data or adversarial cases were deleted. Direct artifact verification fails.

## `USEQ-85D0CB75` — Nondeterministic

**Exact statement**

> Record data classifications, provider processing terms, retention settings, deletion evidence, and intellectual-property review where material.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Record data classifications, provider processing terms, retention settings, deletion evidence, and intellectual-property review where material.” has checkable evidence clauses, but its full result also requires specialist interpretation of applicable legal, privacy, licensing, safety, or notification duties.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `legal_or_specialist_authority`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Record data classifications, provider processing terms, retention settings, deletion evidence, and intellectual-property review where material.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-A3940500` — Deterministic

**Exact statement**

> Record provenance and authenticity evidence for public content and signed claims where used.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The semantic-strength audit evaluates the original promise verbatim rather than a generic acceptance-contract proxy. Every registered public content item and signed claim can require linked provenance and authenticity evidence identities where those mechanisms are used.

### Exact deterministic check

1. Record provenance and authenticity evidence for public content and signed claims where used.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A signed claim without its verification evidence or public asset with an unbound provenance record fails the registry join.

## `USEQ-DC177F9F` — Nondeterministic

**Exact statement**

> Require independent sign-off for autonomous production action, safety-related output, security controls, identity decisions, financial movement, or high-impact public communications.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Require independent sign-off for autonomous production action, safety-related output, security controls, identity decisions, financial movement, or high-impact public communications.” has checkable evidence clauses, but its full result also requires a fixed rule for which models, content, risks, changes, audiences, claims, or workflows are applicable or material.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_scope`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Require independent sign-off for autonomous production action, safety-related output, security controls, identity decisions, financial movement, or high-impact public communications.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-54B598C4` — Nondeterministic

**Exact statement**

> Ensure ongoing monitoring detects behavior drift, unauthorized use, provider changes, data leakage, and control degradation after release.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “Ensure ongoing monitoring detects behavior drift, unauthorized use, provider changes, data leakage, and control degradation after release.” has checkable evidence clauses, but its full result also requires a representative adversarial, semantic, behavioral, or causal protocol and acceptance oracle.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `undefined_protocol`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: Ensure ongoing monitoring detects behavior drift, unauthorized use, provider changes, data leakage, and control degradation after release.
   - **Checker family:** `environment_evidence`
   - **Evidence authority:** `environment`

## `USEQ-AEE1EB03` — Nondeterministic

**Exact statement**

> State the exact scope of AI assistance and residual uncertainty rather than claiming the work was fully verified merely because automated checks passed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Rule “State the exact scope of AI assistance and residual uncertainty rather than claiming the work was fully verified merely because automated checks passed.” has checkable evidence clauses, but its full result also requires human judgment about safety, clarity, necessity, adequacy, completeness, or residual uncertainty.
- **Nondeterministic reason codes:** `mixed_with_nondeterministic_child`, `contextual_judgment`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. For cases already selected by a versioned applicability record, verify the bounded record, artifact, configuration, or test predicates in: State the exact scope of AI assistance and residual uncertainty rather than claiming the work was fully verified merely because automated checks passed.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`
