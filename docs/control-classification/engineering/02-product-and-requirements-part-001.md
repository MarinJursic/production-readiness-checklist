<!-- Generated by scripts/build_control_classification_docs.py. Do not edit by hand. -->

# `docs/engineering/02-product-and-requirements.md` — classification reference, part 1 of 1

[Back to the classification index](../README.md).

Controls: **105** · Deterministic: **12** · Nondeterministic: **93**

## `USEQ-13EC8AE3` — Nondeterministic

**Exact statement**

> The problem, intended users, release scope, non-goals, and deferred features are documented.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Five section names do not prove that the problem, users, scope, non-goals, and deferred features are accurately and usefully documented.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A document can contain those headings with TBD or one vague sentence in each. Presence passes while the product definition remains unusable.

## `USEQ-429BBD1E` — Nondeterministic

**Exact statement**

> Critical user journeys are identified and ranked by user and business impact.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Journey records and rank values are objective, but identifying critical journeys and weighing user against business impact require product judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every journey marked critical has explicit user-impact and business-impact ranks.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9DBA28BF` — Nondeterministic

**Exact statement**

> Critical journeys cover onboarding, normal use, error recovery, account recovery, cancellation, and data/account deletion where applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Criticality and applicability are product judgments, and a declared journey register cannot prove that every real onboarding, recovery, cancellation, and deletion path is included.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

The register can mark account recovery not applicable and list one happy-path onboarding while the product has passwords and multiple cancellation routes. Schema coverage passes although critical journeys are missing.

## `USEQ-AFDBA1B7` — Nondeterministic

**Exact statement**

> Behavior is specified for unavailable dependencies, partial failures, empty data, malformed data, incomplete data, stale data, and conflicting data.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Behavior references for seven labels do not prove that behavior is complete, coherent, or correct for every dependency and data source.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Each field can say show error while failing to define retry, partial state, or user impact for a stale payment response. The checklist passes but behavior remains unspecified.

## `USEQ-6925AA24` — Nondeterministic

**Exact statement**

> Pricing, entitlements, quotas, limits, billing, cancellation, refund, reversal, dispute, and undo behavior are unambiguous.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Presence of commercial and reversal behavior specifications is objective, but whether their language is unambiguous requires semantic and legal interpretation.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Each applicable pricing, entitlement, quota, limit, billing, cancellation, refund, reversal, dispute, and undo behavior has a versioned specification.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-CE594458` — Nondeterministic

**Exact statement**

> Customer-support obligations, service hours, maintenance expectations, SLOs, SLAs, and contractual commitments are documented and aligned.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Support and commitment records can be compared, but complete contractual obligation capture and substantive alignment require legal and accountable review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Support obligations, hours, maintenance expectations, SLOs, SLAs, and contractual commitments share consistent versioned identifiers and values in the release record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-F951491B` — Nondeterministic

**Exact statement**

> Product and marketing claims are supported by implemented behavior and evidence.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Claims, behavior references, and evidence links are observable, but deciding whether evidence substantively supports each product or marketing claim requires contextual and legal judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every published product or marketing claim links to implemented behavior and current evidence.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-42AA91B8` — Deterministic

**Exact statement**

> Define measurable launch success, failure, stop, and rollback criteria.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Success, failure, stop, and rollback criteria are complete typed predicates with metrics, operators, thresholds, windows, and actions fixed before launch.

### Exact deterministic check

1. The launch plan defines metric, operator, threshold, window, and action for success, failure, stop, and rollback criteria.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A missing threshold, unevaluable operator, absent action, or post-launch change fails; no product judgment is needed to verify that measurable criteria were defined.

## `USEQ-6E15410E` — Nondeterministic

**Exact statement**

> Define adoption, conversion, quality, reliability, support, safety, and abuse indicators as applicable.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Indicator definitions are objective, but applicability and selection of meaningful adoption, safety, abuse, and other indicators require product and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every indicator category declared applicable has a metric, data source, window, and threshold in the launch plan.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E84CA4EA` — Nondeterministic

**Exact statement**

> Define who may pause, continue, expand, or roll back the launch.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A role matrix cannot prove that the roles have legitimate organizational authority to pause, continue, expand, or roll back a launch.
- **Nondeterministic reason codes:** `human_accountability`

### Skeptical rejection counterexample

An incorrectly privileged role can be listed for every action and pass membership checks while the actual authorized release owner is absent.

## `USEQ-B636D78E` — Deterministic

**Exact statement**

> Establish whether launch is internal, private, regional, percentage-based, invitation-only, or public.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Launch mode is a closed enum with objective regional or percentage parameters, so one declared mode is fully schema-validatable.

### Exact deterministic check

1. The launch plan declares internal, private, regional, percentage-based, invitation-only, or public mode and its required parameters.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An unknown, missing, or parameter-incomplete mode fails; one valid configured mode passes.

## `USEQ-CD424A12` — Deterministic

**Exact statement**

> Ensure product, marketing, sales, support, and operations use the same release date and scope.

- **Classification:** **Deterministic**
- **Route:** `deterministic_composite`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The five named functions and canonical release record form a fixed set, and exact date and scope-digest equality is mechanically decidable.

### Exact deterministic check

1. Product, marketing, sales, support, and operations records reference one canonical release identifier.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. All five records match the canonical release date and scope digest.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Any function using a different date, release identifier, or scope digest fails the complete five-way comparison.

## `USEQ-B319D366` — Nondeterministic

**Exact statement**

> Ensure help content, screenshots, tours, pricing pages, documentation, and legal text match released behavior.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Artifact versions can be bound to a release, but determining that help, screenshots, pricing, documentation, and legal text semantically match behavior needs human and legal review.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Each named customer-facing artifact is bound to the same release behavior version and has a current review record.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-2A991D71` — Nondeterministic

**Exact statement**

> Notify customers of breaking changes, downtime, migrations, or changed data use where required.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Notification delivery is observable, but determining when notice is required and whether it is sufficient requires contractual, legal, and customer-impact judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change marked notice-required has authenticated customer notification evidence for the declared audience and deadline.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3F2DC642` — Nondeterministic

**Exact statement**

> State the user or societal problem independently of a preferred solution.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a problem statement is genuinely independent of a preferred solution is a semantic product-strategy judgment, not a keyword or format check.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-232B216A` — Nondeterministic

**Exact statement**

> Define target users, buyers, operators, administrators, support teams, and affected non-users.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Named audience fields are objective, but proving complete coverage of target and affected people, especially non-users, is an unbounded stakeholder judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The strategy defines target users, buyers, operators, administrators, support teams, and affected non-users.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E1BD3483` — Nondeterministic

**Exact statement**

> Define the intended outcome and observable evidence that the problem is reduced.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Typed measure fields do not prove that the observable evidence is causally relevant to the intended outcome or shows that the stated problem is reduced.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A strategy for reducing checkout abandonment can measure homepage visits. All metric fields are valid, but the evidence does not show the problem was reduced.

## `USEQ-66F98F70` — Nondeterministic

**Exact statement**

> Define strategic non-goals and choices to prevent unbounded scope.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Nonempty non-goal and choice fields do not prove that choices are strategic, meaningful, or sufficient to prevent unbounded scope.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

The strategy can state non-goal none and choice do work, with valid dates and owners. The record passes while scope remains unbounded.

## `USEQ-4E6FBFF6` — Nondeterministic

**Exact statement**

> Validate that the product aligns with organizational mission, capabilities, risk appetite, and operating model.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Alignment with mission, capabilities, risk appetite, and operating model is an accountable strategic decision requiring organizational context.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-F06D1CFF` — Nondeterministic

**Exact statement**

> Describe the value exchange, including what users provide in money, data, attention, effort, or lock-in.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The true user value exchange and applicability of money, data, attention, effort, and lock-in require product and user interpretation, not field presence.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A product can mark lock-in not applicable while exported data is unusable elsewhere, and describe data as email only while collecting behavior. The schema passes but the exchange is misrepresented.

## `USEQ-F7E05701` — Nondeterministic

**Exact statement**

> Identify alternatives, substitutes, and the cost of not building the product.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Alternative records are observable, but completeness of substitutes and credible cost of not building require market and strategic judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The strategy lists considered alternatives and substitutes and records a cost-of-not-building analysis.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-BA54C71F` — Nondeterministic

**Exact statement**

> Define quality attributes as part of the value proposition rather than downstream technical concerns.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Quality-attribute references can be located, but whether they are integral to the value proposition rather than downstream concerns is semantic strategy judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The value proposition explicitly maps declared material quality attributes to user or business value outcomes.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-FB65AC3C` — Nondeterministic

**Exact statement**

> Define ethical, legal, accessibility, privacy, security, reliability, and sustainability boundaries.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Ethical, legal, accessibility, privacy, security, reliability, and sustainability boundaries require multidisciplinary specialist and accountable decisions.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-1BF8A0A2` — Nondeterministic

**Exact statement**

> Specify assumptions about demand, willingness, behavior, channels, support, and unit economics.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Identifiers and expected values do not establish meaningful assumptions about demand, willingness, behavior, channels, support, or unit economics.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

Every class can contain assumption unknown with an arbitrary expected value. The record passes while the decision assumptions remain unstated in useful terms.

## `USEQ-02161588` — Nondeterministic

**Exact statement**

> Test the riskiest assumptions before scaling irreversible investment.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Experiment timing and results are objective, but selecting the riskiest assumptions and defining irreversible scaling require strategic risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every assumption marked highest-risk has a completed test before its linked irreversible-investment milestone.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-92DBE30F` — Nondeterministic

**Exact statement**

> Ensure strategy can be understood and used to make prioritization trade-offs.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Strategy understandability and usefulness for prioritization are user outcomes requiring an evaluation protocol, population, tasks, and thresholds not supplied here.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

## `USEQ-0E97500F` — Nondeterministic

**Exact statement**

> Review strategy when market evidence, regulation, technology, incidents, or user needs change.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Change events and review timestamps are observable, but material changes in markets, technology, regulation, or user needs require contextual and specialist classification.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every qualifying strategy-change trigger in the approved event register links to a later strategy review.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-48034FED` — Nondeterministic

**Exact statement**

> Do not confuse a roadmap, feature list, architecture, or revenue target with a product strategy.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Determining whether an artifact is being mistaken for product strategy requires semantic interpretation of decision intent and content.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

## `USEQ-43785162` — Nondeterministic

**Exact statement**

> Use multiple evidence sources rather than relying on stakeholder opinion or a single research method.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Different source and method identifiers do not prove independence, evidence quality, or that stakeholder opinion is not being duplicated.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Two interviews with the same stakeholder can be labeled different sources and methods, or one report copied twice. Cardinality passes while the conclusion still relies on one opinion.

## `USEQ-B77E43E7` — Nondeterministic

**Exact statement**

> Recruit participants representative of relevant abilities, contexts, languages, devices, roles, and risk profiles.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Representative recruitment across abilities, contexts, languages, devices, roles, and risks requires sampling and accessibility judgment without defined quotas.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

## `USEQ-0ED90387` — Nondeterministic

**Exact statement**

> Separate observed behavior from stated preference and researcher interpretation.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Labels do not prove that observed behavior, stated preference, and researcher interpretation were correctly separated rather than misclassified.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A researcher's inference can be placed in the observed field and empty preference text added. All labels exist, yet provenance categories remain mixed.

## `USEQ-1EC01EEF` — Nondeterministic

**Exact statement**

> Document research questions, sampling, methods, consent, limitations, and analysis approach.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Research field presence does not prove adequate sampling, methods, consent, limitations, or analysis, and valid consent may require ethical or legal authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

A record can list convenience sample, method interview, and consent yes without an actual consent process. Every field passes while the study plan is invalid.

## `USEQ-D64D10C7` — Nondeterministic

**Exact statement**

> Protect participant privacy, dignity, safety, compensation fairness, and withdrawal rights.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Participant privacy, dignity, safety, compensation fairness, and withdrawal rights require research-ethics, legal, and contextual judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-372C9D37` — Nondeterministic

**Exact statement**

> Validate problem frequency, severity, current workarounds, switching costs, and consequences.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Validating problem frequency, severity, workarounds, switching costs, and consequences needs an empirical design whose population, measures, and thresholds are undefined.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

## `USEQ-6D65C0B7` — Nondeterministic

**Exact statement**

> Investigate why the problem exists before optimizing the visible symptom.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Determining why a problem exists and whether visible symptoms mask its cause is causal product-research judgment, not a deterministic fact.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

## `USEQ-638BEA5E` — Nondeterministic

**Exact statement**

> Include edge users and people most likely to be harmed or excluded.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Selecting edge users and people most likely to be harmed or excluded requires inclusive-research and risk judgment with no bounded population definition.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`, `contextual_judgment`

## `USEQ-FB9037E1` — Nondeterministic

**Exact statement**

> Use prototypes and experiments appropriate to the uncertainty being reduced.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a prototype or experiment is appropriate to the uncertainty requires research-method and risk judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

## `USEQ-446AFA18` — Nondeterministic

**Exact statement**

> Avoid presenting speculative or unsafe prototypes as operational promises.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Speculation, unsafe prototypes, and operational promises are semantic, safety, and communications judgments that require accountable review.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-AA7C1D23` — Nondeterministic

**Exact statement**

> Triangulate qualitative findings with behavioral, operational, market, and support evidence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Evidence-graph links do not prove that sources actually triangulate the qualitative finding, and inapplicability rationales require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A finding about pricing can link to an unrelated support ticket and generic market report. Every edge exists while the conclusion has not been triangulated.

## `USEQ-2E0A4FD3` — Nondeterministic

**Exact statement**

> Record disconfirming evidence and avoid confirmation-biased research plans.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Disconfirming-evidence fields are checkable, but proving absence of confirmation bias in research planning requires interpretation of human choices and omitted evidence.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Each research conclusion records considered disconfirming evidence and its effect on the conclusion.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-EE58AEEE` — Nondeterministic

**Exact statement**

> Validate desirability, usability, feasibility, viability, accessibility, security, privacy, and operability together.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Joint validation of desirability, usability, feasibility, viability, accessibility, security, privacy, and operability is a multidisciplinary acceptance judgment.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-630E7D69` — Nondeterministic

**Exact statement**

> Stop discovery activities when additional evidence no longer changes decisions materially.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether new evidence still changes decisions materially depends on strategy, uncertainty, cost, and an undefined materiality threshold.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

## `USEQ-82E8E2F9` — Nondeterministic

**Exact statement**

> Translate validated findings into traceable requirements, design constraints, and measurable outcomes.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Resolvable links do not prove that a requirement, constraint, or outcome faithfully translates the validated finding.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A finding that users need account recovery can link to a color requirement and homepage metric. The graph passes but the finding was not translated into the product.

## `USEQ-735B6BCE` — Nondeterministic

**Exact statement**

> Identify all requirement sources and their authority, including users, operators, law, contracts, risk, standards, and dependencies.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Source and authority fields are observable, but proving that all sources, especially law, contracts, risk, standards, and dependencies, were discovered is unbounded and specialist-dependent.
- **Nondeterministic reason codes:** `unbounded_scope`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The requirements-source register addresses users, operators, law, contracts, risk, standards, and dependencies with authority references.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-7B2F50E9` — Nondeterministic

**Exact statement**

> Distinguish needs, goals, constraints, functional requirements, quality requirements, interface requirements, and design decisions.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** An enum label is objective, but deciding whether an artifact is truly a need, goal, constraint, requirement, or design decision depends on its meaning.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A fixed database choice can be labeled user need and pass enum validation while the categories are not actually distinguished.

## `USEQ-FD9E56C3` — Nondeterministic

**Exact statement**

> Write requirements that are necessary, singular, unambiguous, feasible, verifiable, consistent, and traceable.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Necessity, singularity, ambiguity, feasibility, verifiability, consistency, and traceability include semantic requirement-quality judgments beyond syntax.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

## `USEQ-639F791F` — Nondeterministic

**Exact statement**

> State the subject, required behavior or property, conditions, thresholds, units, and tolerances.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Typed fields do not prove that the subject, behavior, conditions, thresholds, units, and tolerances form a coherent, measurable requirement.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`

### Skeptical rejection counterexample

A latency requirement can use users as its unit and an irrelevant tolerance while filling every field. The schema passes but the requirement is not validly stated.

## `USEQ-60E89506` — Nondeterministic

**Exact statement**

> Avoid subjective terms such as fast, intuitive, secure, scalable, or robust without measurable meaning.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** A vocabulary scan can find listed subjective words and missing measures, but deciding whether surrounding language gives measurable meaning requires semantic review.
- **Nondeterministic reason codes:** `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every occurrence of a configured subjective requirement term links to an explicit metric, unit, threshold, and condition.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-00B1BC4F` — Nondeterministic

**Exact statement**

> Define normal, alternate, exception, degraded, recovery, and prohibited behavior.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Applicability and substantive behavior for six categories depend on feature semantics; references or rationales alone do not prove complete specification.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A payment feature can mark degraded behavior not applicable and use fail for every other field. All categories are present but usable behavior is not defined.

## `USEQ-3640A26A` — Nondeterministic

**Exact statement**

> Specify data, state, timing, ordering, concurrency, retention, and authorization semantics.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Data, state, timing, ordering, concurrency, retention, and authorization semantics require meaningful domain definitions, not merely typed fields.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

Every field can say default while omitting tenant boundaries and retry ordering. Presence passes although the requirement's semantics remain unspecified.

## `USEQ-E6110D89` — Nondeterministic

**Exact statement**

> Specify accessibility, privacy, security, reliability, support, observability, and lifecycle requirements explicitly.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Explicit domain entries are observable, but determining complete applicable accessibility, privacy, security, reliability, support, observability, and lifecycle requirements requires specialist context.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The requirement set contains explicit entries or applicability decisions for every named quality domain.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-9C6943D9` — Nondeterministic

**Exact statement**

> Resolve conflicts and record the rationale and authority for resolution.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Resolving requirement conflicts and judging the authority and rationale are accountable product, legal, and technical decisions.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-2AFFB86B` — Nondeterministic

**Exact statement**

> Validate requirements with representative stakeholders and intended users.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Representative stakeholder and user validation requires sampling, task, and acceptance protocols not defined by this rule.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_threshold`, `contextual_judgment`

## `USEQ-15B9169F` — Nondeterministic

**Exact statement**

> Link every approved requirement to acceptance criteria and planned verification.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Links establish graph connectivity, not that acceptance criteria and planned verification actually test the approved requirement.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A privacy requirement can link to an uptime threshold and a lint command. Both references resolve while no acceptance criterion or verification addresses privacy.

## `USEQ-F8007542` — Nondeterministic

**Exact statement**

> Control requirement changes and analyze downstream impact before approval.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Change history and downstream links are objective, but completeness and adequacy of impact analysis before approval require semantic judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every requirement change links to a preapproval downstream-impact record and authenticated approval.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-52E6B01C` — Deterministic

**Exact statement**

> Preserve history and effective dates; do not silently rewrite accepted requirements.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Content-addressed accepted revisions, effective dates, and successor links make history preservation and silent rewriting objectively verifiable.

### Exact deterministic check

1. Every accepted requirement revision remains retrievable by digest with its effective date and successor links.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Changing or deleting accepted text breaks its digest or chain; missing dates and ambiguous successors cannot pass.

## `USEQ-CAAD1092` — Nondeterministic

**Exact statement**

> Identify derived requirements and the assumptions from which they follow.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Trace links do not prove that a derived requirement logically follows from the linked assumptions and source requirements.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A requirement to use a specific database can link to an assumption about mobile users. References resolve but the claimed derivation is invalid.

## `USEQ-E6A9E01F` — Nondeterministic

**Exact statement**

> Remove requirements that no longer support an approved need rather than preserving them by inertia.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a requirement still supports an approved need and is retained by inertia is an accountable semantic and prioritization judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-72F577BC` — Nondeterministic

**Exact statement**

> Critical quality attributes are implied but not specified.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Detecting material quality attributes that are only implied requires semantic interpretation and a context-owned criticality judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`

## `USEQ-E26272FA` — Nondeterministic

**Exact statement**

> Requirements cannot be verified objectively or traced to an authorized need.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Verification plans and need links are checkable, but objective verifiability and the authority of the underlying need require contextual and governance judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every requirement links to a bounded verification method and a need with a recorded authority reference.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-90F20378` — Nondeterministic

**Exact statement**

> Prioritize outcomes and risks before individual feature requests.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether outcomes and risks truly precede feature requests is a prioritization-intent and decision-quality judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-92888739` — Deterministic

**Exact statement**

> Use explicit criteria such as user impact, strategic fit, risk reduction, urgency, learning value, cost, reversibility, and dependency value.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The examples in the original statement are not a mandatory universal list; the exact promise is that an explicit, predeclared criterion set is actually used for each in-scope decision.

### Exact deterministic check

1. The versioned prioritization policy defines a nonempty explicit set of named decision criteria before the affected priority decisions are recorded.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

2. Every priority decision in the sealed assessment scope records its inputs under the exact criterion set from the bound prioritization-policy version.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A record that names all eight examples could pass even if real priorities are assigned from an undisclosed executive score, while a valid project may use different explicit criteria. Binding each decision to its predeclared criterion set preserves both Pass and Fail directions.

## `USEQ-D0EAD736` — Nondeterministic

**Exact statement**

> Include reliability, security, privacy, accessibility, maintenance, support, and debt work in the same prioritization system.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Registry membership for known items cannot prove that all reliability, security, privacy, accessibility, maintenance, support, and debt work was identified and entered.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Feature and listed maintenance items can share one registry while a known accessibility remediation is kept outside it. The clause passes although the same system is not used for all work.

## `USEQ-1CA42AE8` — Deterministic

**Exact statement**

> Make stakeholder influence and mandatory obligations visible rather than hiding them inside scores.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Separate stakeholder-influence and mandatory-obligation fields outside the aggregate score are exact record and calculation-structure properties.

### Exact deterministic check

1. Every priority item records stakeholder influence and mandatory obligations outside its aggregate score.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Hiding either input inside the score or omitting its explicit field fails the schema and score-definition inspection.

## `USEQ-137336BA` — Nondeterministic

**Exact statement**

> Represent uncertainty and sensitivity instead of treating prioritization scores as objective truth.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Fields named uncertainty and sensitivity analysis do not prove uncertainty is quantified honestly or that sensitivity was actually analyzed.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

A result can set uncertainty low and sensitivity none with a boilerplate warning. The record passes while presenting the score as falsely objective.

## `USEQ-79915E9D` — Nondeterministic

**Exact statement**

> Identify prerequisites, sequencing constraints, migration needs, and operational readiness work.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Roadmap fields cannot prove that all real prerequisites, sequencing constraints, migrations, and operational work were identified.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A database change can mark migrations not applicable and omit a provider prerequisite while every field is populated. The record passes but dependencies remain hidden.

## `USEQ-60BEFCD2` — Nondeterministic

**Exact statement**

> Define the smallest coherent scope that can safely produce evidence or value.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Smallest, coherent, safe, and value-producing scope require product, architecture, risk, and user judgment rather than a mechanical size metric.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-80609BBD` — Nondeterministic

**Exact statement**

> Protect critical quality attributes from being traded away implicitly to meet dates.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Quality-objective changes and date pressure are observable, but detecting an implicit trade-away and deciding criticality require contextual decision review.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every change to a critical quality objective links to an explicit authorized trade-off decision rather than only a schedule change.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-DE6C27A5` — Nondeterministic

**Exact statement**

> Use roadmaps to communicate intent, hypotheses, sequence, and confidence—not false certainty.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Intent, hypotheses, confidence, and false certainty are semantic communication qualities that cannot be reliably decided from roadmap fields alone.
- **Nondeterministic reason codes:** `contextual_judgment`

## `USEQ-5E98FA21` — Deterministic

**Exact statement**

> Distinguish committed obligations from targets, options, and exploratory work.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The four roadmap states form a closed classification, so exactly one explicit state per item is mechanically decidable.

### Exact deterministic check

1. Every roadmap item is labeled committed obligation, target, option, or exploratory work.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing, multiple, or invented labels fail; allowed values pass without judging priority.

## `USEQ-01BD0F61` — Deterministic

**Exact statement**

> Define entry and exit criteria for roadmap stages.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Versioned entry and exit predicates for every finite roadmap stage are objective structured requirements.

### Exact deterministic check

1. Every roadmap stage defines versioned entry and exit criteria.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

A stage missing either predicate or containing an unevaluable criterion fails; complete machine-readable criteria pass.

## `USEQ-01BCFAAE` — Nondeterministic

**Exact statement**

> Reassess priorities when assumptions, incidents, capacity, regulation, or user evidence changes.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Priority-review timestamps can be matched to declared events, but deciding material changes in assumptions, capacity, regulation, or user evidence requires context.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every qualifying trigger in the approved change register links to a later priority reassessment.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-3B778F2C` — Nondeterministic

**Exact statement**

> Limit work in progress and stop starting work that cannot be completed to the required quality.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Configured work-in-progress limits and completion forecasts are measurable, but whether work can meet required quality requires contextual delivery judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Each workflow has an enforced work-in-progress limit and blocks new starts when declared completion criteria cannot be met.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-5B61643E` — Nondeterministic

**Exact statement**

> Make descoping decisions explicit and analyze their impact on support, operations, safety, and future change.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Descoping decision and impact records are objective, but materiality and sufficient analysis of support, operations, safety, and future change require judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every descoping decision marked material records impacts on support, operations, safety, and future change.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-10689ED1` — Nondeterministic

**Exact statement**

> Document who made material priority decisions and why.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Materiality, legitimate decision-maker authority, and a meaningful reason are human judgments not proved by identity and nonempty-text fields.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

A major decision can be marked non-material, or an unauthorized signer can enter because. The clause passes while material decisions and reasons remain undocumented.

## `USEQ-0A03EEA5` — Nondeterministic

**Exact statement**

> Define a small set of outcome metrics linked to user value and product purpose.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Metric count and links are checkable, but small, user value, and alignment with product purpose require product judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_threshold`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every outcome metric links to a declared user-value outcome and product-purpose identifier.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-590E9EFF` — Nondeterministic

**Exact statement**

> Pair outcome metrics with guardrails for quality, safety, privacy, accessibility, reliability, abuse, and cost.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Guardrail definitions are objective, but complete applicable coverage across quality, safety, privacy, accessibility, reliability, abuse, and cost requires multidisciplinary judgment.
- **Nondeterministic reason codes:** `contextual_judgment`, `legal_or_specialist_authority`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every outcome metric has guardrails or explicit applicability decisions for each named risk and quality domain.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-D470C55C` — Nondeterministic

**Exact statement**

> Define event semantics and data-quality requirements before instrumenting them.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Version and timestamp checks do not prove that event semantics and data-quality requirements are complete or correct for the instrumented event.
- **Nondeterministic reason codes:** `contextual_judgment`

### Skeptical rejection counterexample

An event can define semantics as click and quality as valid before deployment while omitting units, identity, and duplication rules. The clause passes but semantics are undefined.

## `USEQ-8180A0E5` — Nondeterministic

**Exact statement**

> Verify that telemetry reflects actual user behavior and not duplicate, missing, synthetic, or bot events.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reconciliation tests can detect declared duplicate, missing, synthetic, and bot patterns, but full correspondence to actual human behavior is not bounded.
- **Nondeterministic reason codes:** `unbounded_scope`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every telemetry stream has current source-of-truth reconciliation and approved duplicate, missing, synthetic, and bot-event detector results.
   - **Checker family:** `analysis_adapter`
   - **Evidence authority:** `executed`

## `USEQ-9C795830` — Deterministic

**Exact statement**

> Pre-register experiment hypothesis, population, unit, allocation, duration, primary measure, guardrails, and stopping rules.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The eight experiment fields and immutable preregistration time precede exposure, making preregistration completeness and timing objectively decidable.

### Exact deterministic check

1. Before exposure starts, the experiment record fixes hypothesis, population, unit, allocation, duration, primary measure, guardrails, and stopping rules.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing fields or edits after allocation starts fail; sealed pre-exposure values pass without interpreting the hypothesis quality.

## `USEQ-F5E0C7E3` — Nondeterministic

**Exact statement**

> Use randomization or a credible causal method when making causal claims.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether a nonrandom method supports a causal claim requires statistical design expertise and assumptions not defined by this rule.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`

## `USEQ-FA653F6A` — Nondeterministic

**Exact statement**

> Prevent sample-ratio mismatch, cross-group contamination, novelty effects, peeking, and repeated-testing bias.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Preventing contamination, novelty, peeking, repeated testing, and ratio mismatch requires experiment-specific statistical protocols and specialist interpretation.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `undefined_protocol`, `unbounded_scope`

## `USEQ-C9444031` — Nondeterministic

**Exact statement**

> Segment results to detect harmed cohorts without data dredging or privacy-invasive profiling.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Harmed-cohort segmentation without data dredging or privacy-invasive profiling requires statistical, privacy, and ethical judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-B58791C5` — Nondeterministic

**Exact statement**

> Do not ship a statistically significant result that is practically harmful or strategically irrelevant.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Practical harm and strategic relevance are accountable product and risk judgments that statistical significance cannot decide.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-5391DDEE` — Nondeterministic

**Exact statement**

> Treat inconclusive and negative results as learning rather than pressure to manipulate analysis.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Treating negative results as learning and detecting pressure to manipulate analysis concern human intent, incentives, and research conduct.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`

## `USEQ-11E13BB4` — Deterministic

**Exact statement**

> Preserve experiment configuration, code, data lineage, analysis, and decision records.

- **Classification:** **Deterministic**
- **Route:** `artifact_verification`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** Versioned configuration, code digest, lineage graph, analysis artifact, and decision record are immutable experiment artifacts whose preservation is verifiable.

### Exact deterministic check

1. Every experiment preserves versioned configuration, code digest, data-lineage graph, analysis artifact, and decision record.
   - **Checker family:** `artifact_integrity`
   - **Evidence authority:** `artifact`

### Skeptical counterexample test

Deletion, mutation, broken lineage, or an unbound record breaks referential or digest checks and cannot pass.

## `USEQ-86D51237` — Nondeterministic

**Exact statement**

> Ensure users are not exposed to materially unsafe or deceptive variants.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Material safety and deception in experimental variants require ethical, legal, safety, and user-impact judgment.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`, `contextual_judgment`

## `USEQ-883443BC` — Nondeterministic

**Exact statement**

> Define feature-flag cleanup, long-term holdout, and post-launch validation plans.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Plan presence and explicit inapplicability do not establish adequate cleanup, holdout, and post-launch validation for each experimental flag.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_scope`

### Skeptical rejection counterexample

A persistent experiment flag can mark cleanup not applicable and provide a one-line validation plan. All artifacts exist while lifecycle work is not actually planned.

## `USEQ-37C2F56F` — Nondeterministic

**Exact statement**

> Reconcile product analytics with operational, financial, support, and qualitative evidence.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Reconciliation across analytics, operations, finance, support, and qualitative evidence requires causal and semantic interpretation, not only numeric joins.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`

## `USEQ-8EA9F41B` — Nondeterministic

**Exact statement**

> Review whether optimizing a metric is degrading the underlying user outcome.

- **Classification:** **Nondeterministic**
- **Route:** `contextual_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Whether optimizing a metric degrades the underlying user outcome is a causal product judgment requiring evidence and interpretation.
- **Nondeterministic reason codes:** `contextual_judgment`, `undefined_protocol`

## `USEQ-0B5ECDD2` — Deterministic

**Exact statement**

> Define lifecycle states and entry, support, maintenance, deprecation, and retirement criteria.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The policy requirement is to define lifecycle states and five classes of transition or service criteria, all expressible as versioned structured predicates.

### Exact deterministic check

1. The lifecycle policy defines states plus entry, support, maintenance, deprecation, and retirement criteria.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

Missing states or unevaluable entry, support, maintenance, deprecation, or retirement criteria fail; policy wisdom is outside the control.

## `USEQ-E363C389` — Nondeterministic

**Exact statement**

> Publish support windows and compatibility commitments that the organization can sustain.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Published windows and commitments are observable, but whether the organization can sustain them requires capacity, financial, and accountable judgment.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every supported version has published support windows and compatibility commitments with accountable owner and capacity-plan reference.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-E028DE12` — Nondeterministic

**Exact statement**

> Track adoption, usage, critical customers, integrations, dependencies, and data before deprecation.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Recorded inventories cannot prove complete adoption, usage, customer, integration, dependency, and data populations before deprecation.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

All six fields can exist while an untracked critical customer or integration is omitted. The timestamp passes although deprecation inputs are incomplete.

## `USEQ-3ACFDDBF` — Nondeterministic

**Exact statement**

> Provide proportionate notice, migration guidance, export, tooling, and support.

- **Classification:** **Nondeterministic**
- **Route:** `accountable_human_decision`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Proportionate notice, guidance, export, tooling, and support depend on user impact, contracts, accessibility, and migration risk.
- **Nondeterministic reason codes:** `human_accountability`, `legal_or_specialist_authority`, `contextual_judgment`

## `USEQ-43188F99` — Nondeterministic

**Exact statement**

> Avoid breaking security, accessibility, legal, or data-access obligations during reduced support.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Determining security, accessibility, legal, and data-access obligations during reduced support requires current specialist and legal authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `human_accountability`

## `USEQ-33C4E64B` — Deterministic

**Exact statement**

> Define how defects and vulnerabilities are handled during maintenance and extended-support periods.

- **Classification:** **Deterministic**
- **Route:** `structured_record_validation`
- **Decision basis:** `strength_audit_confirmed` — A third rule-by-rule strength audit confirmed that the exact clauses preserve the original promise and remain mechanically decidable.
- **Reason:** The control asks the policy to define handling, and intake, severity, response, fix, disclosure, and exception fields for both support periods form a complete bounded schema.

### Exact deterministic check

1. The lifecycle policy defines intake, severity, response, fix, disclosure, and exception handling for defects and vulnerabilities in each support period.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

### Skeptical counterexample test

An omitted process step or support period fails; actual case execution is assessed elsewhere.

## `USEQ-B5593A92` — Nondeterministic

**Exact statement**

> Prevent new dependencies on components already marked for retirement.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Repository dependency edges do not form a complete inventory of runtime, configuration, infrastructure, and external dependencies on retiring components.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

No source edge can target the component while a new environment configuration or dynamically resolved plugin depends on it. Both clauses pass but a new dependency was created.

## `USEQ-B7C71F97` — Nondeterministic

**Exact statement**

> Measure migration progress and contact customers at risk of disruption.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** The migration population and disruption-risk rule may omit customers, while send evidence does not prove the correct at-risk customers were identified and contacted.
- **Nondeterministic reason codes:** `contextual_judgment`, `unbounded_scope`, `mixed_with_nondeterministic_child`

### Skeptical rejection counterexample

All listed subjects can have progress and notices while an unlisted customer is at risk, or a risk rule wrongly marks them safe. Both clauses pass but required contact is missed.

## `USEQ-65B667B8` — Nondeterministic

**Exact statement**

> Keep old and new versions interoperable for the documented transition where required.

- **Classification:** **Nondeterministic**
- **Route:** `empirical_protocol_undefined`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** One successful result per version pair does not define the promised operations, data shapes, durations, and failure cases needed to prove interoperability.
- **Nondeterministic reason codes:** `undefined_protocol`, `undefined_scope`

### Skeptical rejection counterexample

Old and new versions can pass a health check while mixed-version writes corrupt data. Every matrix cell is green but the versions are not interoperable for the transition.

## `USEQ-BD2D6F7B` — Nondeterministic

**Exact statement**

> Define final data export, retention, deletion, legal hold, and backup behavior.

- **Classification:** **Nondeterministic**
- **Route:** `specialist_or_legal_judgment`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Final export, retention, deletion, legal hold, and backup behavior depend on data classification, contracts, and legal obligations, not field presence alone.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`

### Skeptical rejection counterexample

A plan can delete held records immediately and mark backup retention not applicable while filling every data-class row. The schema passes but required legal and data behavior is wrong.

## `USEQ-CD275A7D` — Nondeterministic

**Exact statement**

> Revoke credentials, domains, certificates, endpoints, jobs, queues, infrastructure, and supplier access during retirement.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** A retirement manifest cannot prove it enumerates every credential, domain, endpoint, job, queue, infrastructure asset, and supplier access path.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

Every listed resource can be disabled while a forgotten DNS name and supplier token remain active outside the manifest. Both clauses pass although retirement revocation is incomplete.

## `USEQ-79D1353B` — Nondeterministic

**Exact statement**

> Preserve required records and evidence after operational systems are removed.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Retention artifacts and storage checks are objective, but deciding which records and evidence remain required needs legal and governance authority.
- **Nondeterministic reason codes:** `legal_or_specialist_authority`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every record declared retention-required remains retrievable with integrity evidence after the operational system is removed.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-0151D776` — Nondeterministic

**Exact statement**

> Verify that retired assets are no longer reachable, billed, monitored as active, or processing data.

- **Classification:** **Nondeterministic**
- **Route:** `unbounded_claim`
- **Decision basis:** `skeptically_rejected` — A primary deterministic proposal failed the independent counterexample review, so the final rule is nondeterministic.
- **Reason:** Queries over a declared retired-asset inventory cannot prove universal reachability, billing, monitoring, or data-processing absence for omitted paths and uninstrumented events.
- **Nondeterministic reason codes:** `unbounded_scope`, `undefined_scope`

### Skeptical rejection counterexample

All listed assets can be unreachable and absent from queried systems while a forgotten endpoint processes data or a separate provider keeps billing. The clause passes despite an active retired asset.

## `USEQ-8429659A` — Nondeterministic

**Exact statement**

> Provide rollback or continuity options for failed migrations where practical.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Declared migration rollback and continuity options can be tested, but where they are practical depends on cost, architecture, obligations, and risk.
- **Nondeterministic reason codes:** `contextual_judgment`, `human_accountability`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. Every migration marked rollback-or-continuity-applicable has a current successful option test.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`

## `USEQ-94B6F552` — Nondeterministic

**Exact statement**

> Conduct a retirement review and capture lessons for future lifecycle planning.

- **Classification:** **Nondeterministic**
- **Route:** `mixed`
- **Decision basis:** `primary_nondeterministic` — The primary review found that the complete rule needs judgment or evidence that cannot produce a closed automatic pass/fail result.
- **Reason:** Review and lesson records are objective, but whether the retirement review is meaningful and lessons are sufficient requires accountable human reflection.
- **Nondeterministic reason codes:** `human_accountability`, `contextual_judgment`, `mixed_with_nondeterministic_child`

### Partial deterministic clauses

These clauses can be checked, but they do **not** prove the complete nondeterministic rule.

1. The completed retirement links to an authenticated review record and documented lessons for lifecycle planning.
   - **Checker family:** `structured_record`
   - **Evidence authority:** `structured_record`
