openapi: 3.2.0
info:
  title: Example API
  version: 1.0.0
paths:
  /health:
    get:
      operationId: duplicateOperation
      responses:
        "200":
          description: Healthy
  /status:
    get:
      operationId: getStatus
      responses:
        "200":
          description: Current service status
