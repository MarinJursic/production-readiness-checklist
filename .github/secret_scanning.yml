# This raw upstream rules file exists only in Git history. The current scanner
# embeds its compressed replacement and verifies both compressed and expanded
# SHA-256 digests before use. Keep this exclusion to this one historical file;
# project source, tests, docs, and every other adapter file remain scanned.
paths-ignore:
  - "scanner/adapter/gitleaks-v8.30.0.toml"
